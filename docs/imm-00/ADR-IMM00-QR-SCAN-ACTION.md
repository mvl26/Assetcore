# ADR-IMM00-QR-SCAN-ACTION — Affordance model + capability-gate `available_actions` + deep-link contract + QR-gen coverage + label spec + quyền print/rotate + token whitespace normalization + reason-non-empty invariant

| Mục | Giá trị |
|---|---|
| Trạng thái | **Accepted** ([R1-GATE] — factory QR-SCAN-ACTION, Vòng 1 PHÂN TÍCH). **D6 = EXECUTED (Vòng 3, 2026-06-08)** — đã thực thi phương án B trong code+test (xem §D6). **D8 = ACCEPTED (factory vòng 6, 2026-06-11)** — token whitespace normalization, chờ BE-5/QA-7 thực thi (xem §D8). **D9 = ACCEPTED (factory vòng 7, 2026-06-11)** — reason-non-empty invariant (Self-Correction lỗi thiết kế gốc D2), chờ BE-6/FE-5/QA-8 thực thi (xem §D9). **D10 = ACCEPTED (factory vòng 8, 2026-06-11)** — status-pill no-EN-leak (FE-only), xem §D10. **D11 = ACCEPTED (factory vòng 25, 2026-06-12)** — lean action-meta endpoint `get_asset_action_meta` bịt over-fetch field tài chính ở 3 màn tạo WO (Self-Correction panel 5-field kéo full-doc), chờ BE-7/FE-7/QA-10 thực thi (xem §D11). **D12 = ACCEPTED (factory vòng 31, 2026-06-12)** — chuẩn hoá whitespace tham số `name` ở `get_asset_scan_info` (parity nhánh token Vòng 6; Self-Correction bất đối xứng 2 nhánh CÙNG 1 endpoint), BE-only 1 dòng, chờ BE/QA thực thi (xem §D12). **D13 = ACCEPTED (factory vòng 37, 2026-06-12)** — bổ sung `manufacturer_sn` (Số serial NSX) vào payload `build_asset_scan_info` + card định danh màn quét QR (Self-Correction bất đối xứng định danh truy xuất NĐ98: label-PDF D5 có serial, scan-info song sinh thiếu), BE+FE, payload 11→12-field, `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`, chờ BE-8/FE-8/QA-11 thực thi (xem §D13). **D14 = ACCEPTED (factory vòng 39, 2026-06-12)** — status-pill lifecycle `role="status"`+`aria-label` VI (SSoT `statusLabel`)+`data-test="scan-status"` (WCAG 1.4.1 parity badge quá hạn + test bám 1-anchor), FE-only 3 attr template, `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d` (xem §D14). **D15 = ACCEPTED (factory vòng 47, 2026-06-12)** — scan-risk urgency affordance + a11y: dòng "Phân loại rủi ro" mang cờ CẢNH BÁO trực quan (`role="status"`+`aria-label` VI SSoT-shared `riskText`+icon+nhãn VI 'Rủi ro cao'+`data-test="scan-risk-urgent"`) khi `risk_classification ∈ {High,Critical}`, derive THUẦN enum-equality từ giá-trị server (no client-clock, parity overdue-SSoT vòng 21), `riskText`+6 nhãn-mức GIỮ no-regress vòng 38/40, FE-only (1 computed+3 hằng VI+template), `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`, chờ FE-9/QA-12 thực thi (xem §D15). **D16 = ACCEPTED (factory vòng 49, 2026-06-12)** — warranty-CTA affordance: 2 nút đường-sửa `report_failure`/`request_cm` mang chip "Ngoài bảo hành" (`role="status"`+`aria-label` hậu tố VI 'lưu ý chi phí ngoài bảo hành'+icon+`data-test="cta-out-of-warranty"`+`data-warranty-cta=<key>`) khi `warranty_expired === true` ∧ `effectiveEnabled`, derive THUẦN cờ server (no client-clock, đọc `warrantyExpired` vòng 48), map THUẦN-FE `WARRANTY_ACTION_KEY` Readonly CHỈ 2 đường-sửa (request_pm/calibration KHÔNG), 2 affordance overdue(amber)/warranty(rose) ĐỘC LẬP no-leak-chéo, overdue vòng 21 + badge warranty card vòng 48 GIỮ no-regress, FE-only (1 map+1 predicate+2 hằng VI+chip template), `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`, chờ FE-10/QA-13 thực thi (xem §D16). **D17 = ACCEPTED (factory vòng 50, 2026-06-12)** — crash-safe `getdate` ở 4 hàm xử-lý-ngày của `build_asset_scan_info` (`_is_warranty_expired`/`_is_pm_overdue`/`_is_calibration_overdue`/`_date_str_or_none`): giá trị ngày dị-dạng (legacy/drift import — `'not-a-date'`/`'2020-13-45'`) KHÔNG còn để `getdate` ném `frappe.exceptions.ValidationError` → HTTP-500 traceback-leak ở endpoint quét QR; degrade graceful về `None`/`False` (date-không-xác-định ≠ hết hạn/quá hạn — no-false-alarm), 13 field còn lại của payload GIỮ NGUYÊN (1 record xấu KHÔNG kéo sập cả màn quét). Self-Correction lỗi thiết kế gốc: 4 helper giả-định `getdate` luôn parse được — đúng cho input qua Frappe-validated Date field, SAI khi DB chứa chuỗi drift (raw SQL / legacy import bypass validate). BE-only guard CHỈ nuốt parse-error (`ValidationError`/`ValueError`/`TypeError`) — KHÔNG catch-all (no-mask-real-bug). Schema KHÔNG đổi, `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`, chờ BE-9/QA-14 thực thi (xem §D17). |
| Ngày | 2026-06-08 (D8/D9: 2026-06-11) |
| Phạm vi | IMM-00 (registry — màn quét QR `AssetScanInfoView`) → chạm IMM-08/09/11/12 (action create) + import + IMM-04/05 (registration) |
| Owner | BA Lead + System Architect |
| Liên quan | `./ADR-IMM00-ASSETCODE.md` (asset_code=PK) + `../imm-04/ADR-001-asset-qr.md` (QR token, deep-link `/a/<token>`, RBAC read/write) |
| Supersedes | Không — **bổ sung** action surface cho màn quét (ADR-001 chốt resolve/scan-info/label read-only; ADR này chốt CTA hành động + gate) |

> ADR này là **quyết định cuối** cho action surface trên màn quét QR + contract `available_actions` BE↔FE + deep-link truyền asset. Mọi spec `04_Backend_Design.md`, `05_API_Specification.md`, `06_Frontend_Design.md` và task BE/FE/QA Vòng 2+ phải nhất quán với ADR này. Khi mâu thuẫn → ADR thắng.
>
> **Bản chất GATE:** đây là gate PHÂN TÍCH — Vòng 1 **KHÔNG đụng code** (`.py`/`.vue`/`.ts`). Chỉ chốt affordance + contract + spec để Vòng 2+ thực thi mà KHÔNG phải hỏi lại. Mỗi quyết định D1–D6 **đo được**; mỗi task xuống dòng map tới đúng 1 quyết định.

---

## Bối cảnh (vì sao cần GATE này)

Màn quét QR `AssetScanInfoView` hiện CHỈ là read-only (định danh + lifecycle + bảo trì gần nhất + next_pm/calibration + cờ overdue server-side). KTV cầm điện thoại quét tem dán trên máy → thấy thông tin nhưng **KHÔNG có lối hành động** (báo hỏng / yêu cầu PM / yêu cầu CM / hiệu chuẩn) ngay tại điểm quét. Đây là use-case lõi của QR tại hiện trường (point-of-care).

**Nguy cơ nếu KHÔNG chốt:**
- (a) FE render nút hành động **literal mỗi action** (hardcode route + điều kiện inline) → drift gate, nút chết, leo quyền.
- (b) Deep-link truyền **raw `qr_token`** sang form action → token (định danh phụ, rotate được) lọt URL action + lịch sử trình duyệt → rò rỉ + form khoá sai field.
- (c) Gate bằng **role-name** (anti-pattern RBAC dead-gate) → nút chết âm thầm khi đổi role.
- (d) Cho tạo WO/Incident trên asset **đã thanh lý** (Decommissioned) → record rác, vỡ lifecycle.
- (e) CM gate ở route (`repair.write`) ≠ gate ở service (`repair.create`) → user thấy nút (FE cap-check 1 nơi) nhưng BE 403 ở nơi khác → trải nghiệm gãy.

**5 câu hỏi domain (assetcore-doc Phần 2):**
1. **WHO HTM stage:** Cross-cutting (IMM-00 foundation) — điểm vào action ở **Maintenance** (báo hỏng→Corrective IMM-12, PM IMM-08, CM IMM-09, Calibration IMM-11). Quét tem phát sinh ở Operation/Maintenance.
2. **NĐ98:** Truy xuất nguồn gốc + báo cáo sự cố (Article 67 — incident reporting). Action từ điểm quét phải gắn đúng asset (NAME, không phải token) để audit trail liên tục. Nhãn QR phục vụ định danh tại hiện trường (UDI/serial Article — truy xuất nguồn gốc).
3. **Stakeholder:** KTV TBYT (quét → báo hỏng/yêu cầu WO), QL phòng vật tư (in/cấp tem), người vận hành lâm sàng (báo hỏng). Action gate theo capability của từng persona.
4. **Lifecycle event:** action KHÔNG phát sinh event mới ở IMM-00 — chỉ **điều hướng** sang form tạo WO/Incident của module đích (module đó tự ghi event). Quét = read-only (KHÔNG audit, đồng nhất ADR-001).
5. **Hậu quả nếu data sai:** token lọt URL action → rò rỉ định danh phụ + đứt liên kết khi rotate; cho action trên asset thanh lý → record rác phá lifecycle; gate sai → leo quyền (user không-quyền tạo được WO) hoặc nút chết (user-có-quyền bị chặn).

---

## FACTS đã verify tại source (cơ sở quyết định — KHÔNG phỏng đoán)

| # | FACT | Evidence (`file:line`) |
|---|---|---|
| F1 | `build_asset_scan_info()` trả payload read-only: name, asset_code, asset_name, **manufacturer_sn** (Số serial NSX — định danh truy xuất NĐ98, str coalesce `''`, D13/BR-00-52), lifecycle_status (mã canonical), device_model_name, location_name, next_pm_date, next_calibration_date, recent_maintenance, **pm_overdue**, **calibration_overdue**, available_actions. **Cả hai field Date `next_pm_date`/`next_calibration_date` = `str\|None` (`'YYYY-MM-DD'`/None) qua `_date_str_or_none` — đối xứng (Vòng 11, 04 §II.1.8d-PMDATESTR).** | `services/imm00.py` (`build_asset_scan_info`) |
| F2 | `get_asset_scan_info(token, name)` gate `rbac.require("asset.read")` + `assert_vendor_can_access` + rate_limit 30/60s; resolve token HOẶC name → 404 no-leak. Read-only (KHÔNG audit). | `api/imm00.py:332-380` |
| F3 | `AssetScanInfoView.vue` hiện **KHÔNG có nút hành động nào** (báo hỏng/PM/CM/calibration) — chỉ card thông tin + "Quét lại" + "Về trang chủ". | `AssetScanInfoView.vue` (grep action routes = 0) |
| F4 | **Capability tồn tại trong `CAPABILITY_MAP`** (sinh từ `_DOMAIN_PRIMARY × _PTYPES`): `corrective.create`→(Incident Report, create); `pm.create`→(PM Work Order, create); `repair.create`→(Asset Repair, create); `calibration.create`→(IMM Asset Calibration, create). Cũng có `.write` cho cả 4. | `rbac.py:66-91` |
| F5 | **Service-tier create gate:** tạo CM (`create_repair`) gate **`repair.create`**; báo hỏng (incident) gate **`corrective.create`**. (PM/calibration create gate `.create` theo cùng pattern domain.) | `api/imm09.py:40` |
| F6 | **Route-guard cap (FE) hiện tại — MISMATCH với svc:** `IncidentCreate` `/incidents/new` → `corrective.create` (KHỚP svc); `PMWorkOrderCreate` `/pm/work-orders/new` → **`pm.write`** (svc `pm.create`); `CMCreate` `/cm/create` → **`repair.write`** (svc `repair.create`); `CalibrationCreate` `/calibration/new` → **`calibration.write`** (svc `calibration.create`). 3/4 route gate `.write`, svc gate `.create`. | `router/index.ts:307-310,341-344,414-417,447-450`; `api/imm09.py:40` |
| F7 | **`available_actions` KHÔNG tồn tại** ở bất kỳ đâu (BE/FE/docs) — contract greenfield. | grep `available_actions` toàn repo = 0 |
| F8 | **Deep-link `?asset=<name>` ĐÃ wire PARTIAL (đính chính — verified at source 2026-06-08 [FE]):** **3/4 create view ĐÃ đọc `route.query.asset` → prefill** (IncidentCreate `form.asset`=`route.query.asset` `IncidentCreateView.vue:13,29-30`; CMCreate `form.asset_ref`=`route.query.asset` `CMCreateView.vue:40,84`; CalibrationCreate `form.asset`=`route.query.asset` `CalibrationCreateView.vue:40`). **CHỈ `PMWorkOrderCreateView` THIẾU** — import `useRouter` (KHÔNG `useRoute`), `form.asset_ref` init `''`, asset chọn qua SmartSelect, KHÔNG đọc query (`PMWorkOrderCreateView.vue:4,36,40,183`). **3 gap PHỤ (cả 4 view):** (a) prefill nhưng **KHÔNG khoá** field asset (vẫn sửa được — không có `:disabled` theo nguồn query); (b) **`source=qr-scan` KHÔNG được đọc ở bất kỳ view nào** (grep `source` = chỉ `source_pm_wo` CM-nội-bộ, KHÔNG phải cờ nguồn qr-scan); (c) tên field nội bộ KHÁC nhau (`asset` ở Incident/Cal vs `asset_ref` ở PM/CM) — query param `asset` map sang field khác nhau. | grep `route.query.asset` 4 view = 3 (PM thiếu); grep `qr-scan` 4 view = 0 |
| F18 | **i18n VI SSoT cho nhãn 4 action = greenfield (FE):** `labels.ts` CHƯA có nhãn `Báo hỏng`/`Yêu cầu bảo trì`/`Yêu cầu sửa chữa`; CHỈ `'Calibration': 'Hiệu chuẩn'` (STATUS map, không phải action-label SSoT). Vòng 2+ FE PHẢI thêm 1 map SSoT `SCAN_ACTION_LABELS` (key→VI) — KHÔNG hardcode nhãn trong .vue. | `labels.ts:428` (chỉ Calibration); grep `Báo hỏng`/`Yêu cầu` labels.ts = 0 |
| F19 | **`AssetScanInfoView` phần info read-only đã đúng** — chỉ 2 nút `Quét lại mã QR` + `Về trang chủ` (comment `:255` "read-only: chỉ Quét lại + Về trang chủ"); KHÔNG card/field nào editable; KHÔNG `available_actions` render. Vòng 2+ thêm action surface PHẢI GIỮ phần info read-only (chỉ thêm cụm nút, không sửa info → editable). | `AssetScanInfoView.vue:153-154,255-258` |
| F20 | **`getdate()` NÉM `frappe.exceptions.ValidationError` trên chuỗi không-parse-được (verified @source 2026-06-12).** `getdate('not-a-date')` / `getdate('2020-13-45')` / `getdate('2020-99-99')` / `getdate('garbage')` → **RAISE** `frappe.exceptions.ValidationError` (`"<strong>...</strong> is not a valid date string."`). `getdate('')` / `getdate(None)` → trả **ngày HÔM NAY** (KHÔNG raise) — nhưng 4 helper đã guard `if not value` TRƯỚC `getdate` ⇒ rỗng/None KHÔNG bao giờ chạm `getdate` (no behavior change). `getdate('2020-01-15')` / `getdate(date(2020,1,15))` → trả `date` đúng. **MRO `ValidationError` = `[ValidationError, Exception, object]` — KHÔNG subclass `ValueError`/`TypeError`** ⇒ `except (ValueError, TypeError)` đơn thuần KHÔNG bắt được nó (vẫn 500). Guard PHẢI liệt kê `frappe.exceptions.ValidationError` TƯỜNG MINH + `ValueError`/`TypeError` (phòng-thủ input bypass Frappe). | `bench execute getdate` probe 2026-06-12; `frappe/utils/data.py::getdate` |
| F21 | **4 helper ngày của `build_asset_scan_info` gọi `getdate(value)` KHÔNG guard parse-error (root flaw).** `_is_warranty_expired` (`:402`), `_is_pm_overdue` (`:363`), `_is_calibration_overdue` (`:382`), `_date_str_or_none` (`:414`) đều `if not value: return False/None` rồi `getdate(value)` TRỰC TIẾP. Cả 4 nhận giá trị từ `row.get('warranty_expiry_date'/'next_pm_date'/'next_calibration_date')` (= `frappe.db.get_value` Date col). DB col `Date` (3 field — verified fieldtype) KHÔNG chặn chuỗi drift khi ghi qua raw SQL / legacy import bypass Frappe-validate (`UPDATE ... SET warranty_expiry_date='not-a-date'` chấp nhận syntax @MariaDB). ⇒ 1 record drift → `getdate` raise → `build_asset_scan_info` raise → endpoint `get_asset_scan_info` 500 traceback-leak. | `services/imm00.py:350-414`; fieldtype `Date` (ac_asset.json); raw-UPDATE probe 2026-06-12 |
| F9 | **QR-gen ở model layer:** `ac_asset.py::before_insert → _ensure_qr_token` sinh token KHI trống (idempotent — `if self.qr_token: return`, KHÔNG clobber); `after_insert` emit `qr_generated` đúng 1 lần (cờ `qr_token_just_generated`). | `ac_asset.py:50,63,65-94` |
| F10 | **Import dùng Document API:** `import_data.py` `frappe.new_doc(...).update(...).insert()` ⇒ FIRE `before_insert` ⇒ token tự sinh. KHÔNG cần code QR riêng cho import. | `import_data.py:348-350` |
| F11 | **Backfill legacy idempotent đã có:** `ensure_asset_qr_token(asset)` (service) đọc token hiện hữu → no-op nếu có, sinh + emit nếu thiếu. Dùng được làm 1-shot backfill asset legacy thiếu token. | `services/imm00.py:217-247` |
| F12 | **`build_asset_label_data()` trả 6 field:** name, asset_code, device_model_name, location_name, lifecycle_status, qr_url. **THIẾU `manufacturer_sn` + `asset_name`** (theo AC-5 cần 5 field hiển thị: QR + model + serial + tên + mã). | `services/imm00.py:563-594` |
| F13 | **`_build_qr_url` ĐÃ wire `frappe.conf.assetcore_qr_base_url`** (host công khai) → fallback `get_url` — validate scheme/netloc, reject path/query, log-1-lần. (AC-4 mục "(tuỳ)" đã DONE ở BE.) | `services/imm00.py:500-560` |
| F14 | **[EXECUTED Vòng 3 — D6]** In nhãn gate **`asset.print`**: `get_asset_label_data` (:429) + `get_asset_label_data_batch` (:456) + `mark_label_printed` (:493) require `asset.print`. **Rotate** (`regenerate_asset_qr_token` :554) gate **`asset.qr.rotate`** + rate-limit 10/60s. *(Trước Vòng 3 cả 4 đều `asset.write` — nay đã tách.)* | `api/imm00.py:429,456,493,554`; rbac.py CAPABILITY_MAP |
| F15 | **[RESOLVED Vòng 3 — D6]** `asset.write` thực tế chỉ Super Admin có → trước đây KTV/QL vật tư KHÔNG in/rotate được (self-correction P2). **Đã sửa:** in nhãn gate `asset.print` (print=1 sẵn cho mọi role) → persona vận hành **in được**; rotate gate `asset.qr.rotate` (=write, chỉ Super Admin/được-cấp) — least-privilege chính xác. | DocPerm matrix §D6 (verified 2026-06-08) |
| F16 | **Lifecycle status canonical = 7 mã** (Draft + Commissioned + Active + Under Maintenance + Under Repair + Calibrating + Out of Service + Decommissioned). `BLOCKED_FOR_WO = (Out of Service, Decommissioned)` là SSoT cho overdue-exempt + WO-block. `OPERATIONAL = (Commissioned, Active)`. | `constants.py:85-99` |
| F17 | `_strip_qr_token(doc)` xoá `qr_token` khỏi MỌI payload đọc AC Asset (no-raw-token parity) — deep-link dựng server-side qua `_build_qr_url`. | `api/imm00.py:85-97,284` |

---

## Quyết định (6 quyết định — DỨT KHOÁT, mỗi quyết định đo được)

### D1 — AFFORDANCE MODEL: action surface trên màn quét + map 1-1 {label VI, module, route, capability SSoT}

**Quyết định (1 dòng):** màn quét QR phơi **đúng 4 action** (báo hỏng / yêu cầu PM / yêu cầu CM / hiệu chuẩn); mỗi action map **1-1** tới bảng dưới — KHÔNG thêm action ngoài bảng, KHÔNG inline literal route.

| Action (label VI) | IMM module | Route name | Route path | **Capability SSoT (chốt)** | Mismatch ghi nhận |
|---|---|---|---|---|---|
| **Báo hỏng** | IMM-12 Corrective | `IncidentCreate` | `/incidents/new` | **`corrective.create`** | KHỚP route↔svc (F5,F6) |
| **Yêu cầu bảo trì (PM)** | IMM-08 PM | `PMWorkOrderCreate` | `/pm/work-orders/new` | **`pm.create`** | route hiện `pm.write` ≠ svc `pm.create` (F6) → **chốt `pm.create`** |
| **Yêu cầu sửa chữa (CM)** | IMM-09 Repair | `CMCreate` | `/cm/create` | **`repair.create`** | route hiện `repair.write` ≠ svc `repair.create` (F5,F6) → **chốt `repair.create`** |
| **Hiệu chuẩn** | IMM-11 Calibration | `CalibrationCreate` | `/calibration/new` | **`calibration.create`** | route hiện `calibration.write` ≠ svc `calibration.create` (F6) → **chốt `calibration.create`** |

**Chốt 1 capability SSoT cho mỗi action = `<domain>.create`** (lý do: action trên màn quét = **TẠO** WO/Incident, ngữ nghĩa đúng nhất là `.create`; service-tier ĐÃ gate `.create` ở F5; `.create` resolve `frappe.has_permission(DocType, "create")` = đúng quyền tạo record).

**Self-correction (đổi thiết kế gốc — đo được):** route-guard hiện gate `.write` cho 3/4 (PM/CM/Calibration, F6) → **lệch** svc `.create`. Vòng 2+ FE PHẢI đổi `requiredCapabilities` của 3 route này về `<domain>.create` để **route-guard = available_actions = svc** (1 capability/action xuyên 3 tầng). Báo hỏng đã đúng (`corrective.create` cả route lẫn svc) — no-op.

> Đo được: sau Vòng 2+, với mỗi action, 3 giá trị PHẢI bằng nhau: (a) `meta.requiredCapabilities` của route, (b) `key→capability` trong `available_actions` của BE, (c) `rbac.require(...)` ở service create. QA assert tương đẳng.

---

### D2 — CAPABILITY-GATE CONTRACT: shape `available_actions` = capability ∩ lifecycle, derive SERVER-SIDE

**Quyết định (1 dòng):** `get_asset_scan_info` emit field MỚI **`available_actions: list[dict]`**, mỗi phần tử shape **CHÍNH XÁC**:

```
{
  "key":     str,    # định danh action ổn định: 'report_failure' | 'request_pm' | 'request_cm' | 'request_calibration'
  "label":   str,    # nhãn VI (D1): 'Báo hỏng' | 'Yêu cầu bảo trì' | 'Yêu cầu sửa chữa' | 'Hiệu chuẩn'
  "route":   str,    # route NAME (D3): 'IncidentCreate' | 'PMWorkOrderCreate' | 'CMCreate' | 'CalibrationCreate'
  "enabled": bool,   # = (user CÓ capability) ∩ (lifecycle asset cho phép action) — derive SERVER-SIDE
  "reason":  str     # chuỗi VI khi enabled=false; "" khi enabled=true
}
```

**Quy tắc derive `enabled` (SSoT — 1 predicate chung, KHÔNG inline literal mỗi action):**

```
enabled(action) = has_cap(action.capability) AND lifecycle_allows(lifecycle_status, action.key)
```

- `has_cap` = `rbac.can(action.capability)` (capability SSoT D1, KHÔNG hardcode role).
- `lifecycle_allows` = **1 predicate chung** tra **bảng lifecycle×action TƯỜNG MINH** (dưới) — KHÔNG viết `if status == "..."` rải rác mỗi action.

**Bảng lifecycle × action (TƯỜNG MINH — chốt từng ô):**

| lifecycle_status | Báo hỏng (report_failure) | Yêu cầu PM (request_pm) | Yêu cầu CM (request_cm) | Hiệu chuẩn (request_calibration) |
|---|---|---|---|---|
| **Active** | ✅ | ✅ | ✅ | ✅ |
| **Commissioned** | ✅ | ✅ | ✅ | ✅ |
| **Under Maintenance** | ✅ | ✅ | ✅ | ✅ |
| **Under Repair** | ✅ | ✅ | ✅ | ✅ |
| **Calibrating** | ✅ | ✅ | ✅ | ✅ |
| **Out of Service** | ✅ (cho báo hỏng) | ❌ | ✅ (cho CM — sửa để đưa lại vận hành) | ❌ |
| **Decommissioned** | ❌ | ❌ | ❌ | ❌ (KHÔNG cho tạo BẤT KỲ WO/Incident — đã thanh lý) |
| **Draft** | ❌ | ❌ | ❌ | ❌ (chưa commissioned — registry chưa hoàn tất) |

**Lý do từng nhóm:**
- **Decommissioned** = đã thanh lý (vòng đời kết thúc) → cấm TẤT CẢ action tạo record. `reason = "Thiết bị đã thanh lý"`.
- **Out of Service** = ngừng vận hành tạm nhưng còn trong registry → vẫn cho **báo hỏng** (ghi nhận thêm sự cố) + **CM** (sửa để đưa lại vận hành); **KHÔNG cho PM/calibration** (bảo trì định kỳ/hiệu chuẩn vô nghĩa khi máy đang dừng — đồng nhất `BLOCKED_FOR_WO` exempt overdue F16). `reason = "Thiết bị đang ngừng hoạt động — chỉ cho phép báo hỏng / yêu cầu sửa chữa"`.
- **Draft** = chưa commissioned → registry chưa hoàn tất, không vận hành → cấm action. `reason = "Thiết bị chưa đưa vào vận hành"`.
- **Operational + downtime đang-xử-lý** (Active/Commissioned/Under Maintenance/Under Repair/Calibrating) = full 4 action.

**Quy tắc `reason` (chuỗi VI, chỉ khi `enabled=false`):**
- Thiếu capability → `"Bạn không có quyền thực hiện thao tác này"`.
- Lifecycle chặn → message theo nhóm trên (`"Thiết bị đã thanh lý"` / `"Thiết bị đang ngừng hoạt động …"` / `"Thiết bị chưa đưa vào vận hành"`).
- **Ưu tiên hiển thị:** nếu cả thiếu-cap LẪN lifecycle-chặn → ưu tiên reason **lifecycle** (trạng thái asset là sự thật khách quan, hiển thị trước; thiếu-quyền là thứ cấp). Chốt thứ tự: `lifecycle_reason or capability_reason`.
- **Bất biến reason-non-empty (D9 — Self-Correction):** với MỌI `lifecycle_status` (kể cả `''` rỗng và mã LẠ ngoài enum `AssetStatus`), mỗi phần tử `enabled=false` **PHẢI** có `reason` là chuỗi VI **KHÁC RỖNG**. Khi lifecycle chặn nhưng KHÔNG khớp 3 nhóm trên (status rỗng/lạ) + user CÓ cap → fallback hằng VI mới `_LIFECYCLE_REASON_UNKNOWN = "Thiết bị không ở trạng thái cho phép thao tác này"`. Chốt thứ tự đầy đủ: `lifecycle_reason or (capability_reason nếu thiếu cap) or _LIFECYCLE_REASON_UNKNOWN`. Chi tiết + acceptance đo được tại **§D9**.

**KHÔNG render nút chết:** FE render **MỌI** phần tử `available_actions` nhưng nút `enabled=false` ở trạng thái **disabled + tooltip = reason** (KHÔNG ẩn hẳn — để KTV biết "vì sao không bấm được", hỗ trợ self-service). Nút `enabled=false` KHÔNG điều hướng khi click. (Khác RBAC dead-gate: nút disabled CÓ lý do hiển thị, KHÔNG phải nút-không-làm-gì-im-lặng.)

**SSoT 1 predicate (chốt vị trí code Vòng 2+):** định nghĩa **1 hàm** `_scan_action_specs()` (bảng 4 action: key/label/route/capability/lifecycle-rule) + **1 predicate** `_lifecycle_allows(status, key)` đọc bảng trên. `build_asset_scan_info` lặp specs → derive `enabled`+`reason`. KHÔNG literal hoá điều kiện ở mỗi action.

> Đo được: QA mock từng `lifecycle_status` × từng capability-set → assert `available_actions` đúng bảng (enabled + reason). Vd Decommissioned + đủ-quyền → 4 phần tử enabled=false reason="Thiết bị đã thanh lý"; Out of Service + đủ-quyền → report_failure/request_cm enabled=true, request_pm/request_calibration enabled=false.

---

### D3 — DEEP-LINK CONTRACT: route truyền `?asset=<name>` — TUYỆT ĐỐI KHÔNG raw `qr_token`

**Quyết định (1 dòng):** mỗi action route = `/<flow>/new?asset=<name>` truyền **asset NAME** (= `asset_code`, vì invariant `asset_code == name` — ADR-ASSETCODE D5); **TUYỆT ĐỐI KHÔNG** truyền `qr_token` thô trong URL hành động.

**Chốt rõ:**
- **Field truyền:** query param **`asset`** = giá trị `name` của AC Asset (đã có trong payload scan-info F1; cũng = `asset_code` do invariant). KHÔNG truyền token (token là định danh phụ, rotate được, đã bị `_strip_qr_token` xoá khỏi mọi payload đọc — F17).
- **Nguồn (audit):** thêm param phụ **`source=qr-scan`** (hằng) để FE đánh dấu nguồn → form create biết "asset đến từ quét QR" để (a) **khoá field asset** (read-only, không cho đổi — tránh tạo nhầm asset khác), (b) ghi nhận nguồn vào audit của record tạo ra (module đích tự xử l. IMM-00 KHÔNG ghi event).
- **URL mẫu:** `/incidents/new?asset=TS-2025-USG-001&source=qr-scan`. Token KHÔNG bao giờ xuất hiện trong URL action (chỉ ở deep-link landing `/a/<token>` → resolve nội bộ → điều hướng `/scan/:token` hoặc `/assets/:id/info`, ADR-001).
- **`available_actions.route`** trả route **NAME** (D2); FE tự dựng URL = `router.resolve({ name, query: { asset, source: 'qr-scan' } })` → KHÔNG để BE ghép query-string (tránh BE biết cấu trúc URL FE).

**Ràng buộc FE (Vòng 2+) — delta CHÍNH XÁC PER-VIEW (đính chính F8 — verified at source [FE] 2026-06-08):**

| Create view | Đọc `?asset`? | Field nội bộ | Delta Vòng 2+ |
|---|---|---|---|
| `IncidentCreateView` | ✅ ĐÃ đọc (`:13,29-30`) → prefill `form.asset` | `asset` | **THÊM khoá** field khi `source==='qr-scan'` + đọc cờ nguồn. (prefill: no-op) |
| `CMCreateView` | ✅ ĐÃ đọc (`:40,84`) → prefill `form.asset_ref` | `asset_ref` | **THÊM khoá** field khi `source==='qr-scan'` + đọc cờ nguồn. (prefill: no-op) |
| `CalibrationCreateView` | ✅ ĐÃ đọc (`:40`) → prefill `form.asset` | `asset` | **THÊM khoá** field khi `source==='qr-scan'` + đọc cờ nguồn. (prefill: no-op) |
| `PMWorkOrderCreateView` | ❌ **THIẾU** (`:4,36,40` — `useRouter` only) | `asset_ref` | **THÊM** `useRoute` + đọc `route.query.asset`→prefill `form.asset_ref` + **khoá** + cờ nguồn. (gap LỚN — wire mới) |

- **Gap chính (FE-gate chốt):** chỉ **`PMWorkOrderCreateView`** chưa đọc `?asset` (3 view kia ĐÃ có). Vòng 2+ FE thêm prefill+khoá cho PM theo mẫu 3 view kia.
- **Gap phụ (cả 4 view):** (a) hiện prefill nhưng **KHÔNG khoá** asset field → Vòng 2+ thêm `:disabled` (hoặc render read-only) khi `source==='qr-scan'` (tránh đổi nhầm asset sau khi từ quét QR vào); (b) **`source=qr-scan` chưa đọc ở bất kỳ view nào** (F8) → thêm `const fromQrScan = route.query.source === 'qr-scan'` làm cờ điều kiện khoá + đánh dấu nguồn audit.
- **Lưu ý map field:** query param `asset` (hằng theo D2/D3) → field nội bộ KHÁC nhau (`asset` ở Incident/Cal, `asset_ref` ở PM/CM). FE đọc cùng `route.query.asset` nhưng gán vào field đúng của từng view. KHÔNG đổi tên query param theo view.

> Đo được: grep `qr_token` trong URL action = 0 (parity no-raw-token giữ); grep `route.query.asset` 4 view = 4 (PM được wire); grep `route.query.source` 4 view ≥ cờ-khoá; Playwright bấm nút từ màn quét → URL chứa `?asset=<name>&source=qr-scan`, KHÔNG chứa token; form đích field asset **disabled** + đúng asset.

---

### D4 — QR-GEN COVERAGE: token sinh ở đâu khi import + backfill legacy

**Quyết định (1 dòng):** token sinh ở **model layer** `ac_asset.py::before_insert → _ensure_qr_token` (idempotent, KHÔNG clobber) → fire ở **MỌI đường tạo** (form-create + import `doc.insert` + registration IMM-04/05) ⇒ **KHÔNG cần code QR riêng cho import**.

**Trả lời câu hỏi user "QR sinh ở đâu khi import":**
- Import (`import_data.py`) dùng `frappe.new_doc("AC Asset").update(...).insert()` (F10) → `insert()` chạy `before_insert` (F9) → `_ensure_qr_token()` sinh token nếu trống. **Cùng 1 đường với form-create + registration** (tất cả qua Document API `.insert()`). Không có đường ghi nào bỏ qua `before_insert` (trừ `frappe.db.sql` thô — KHÔNG dùng cho tạo asset).
- Idempotent: `if self.qr_token: return` (F9) → asset đã có token (vd file import có sẵn cột qr_token) KHÔNG bị ghi đè; `after_insert` emit `qr_generated` đúng 1 lần (cờ).

**Chốt GAP (Vòng 2+ thực thi):**
- **(a) Test verify từng path:** QA viết test khẳng định token sinh sau: (1) form-create `create_asset`, (2) import `doc.insert` (qua `import_data`), (3) registration commissioning (IMM-04/05). Test gap hiện tại — chưa có test phủ "import → token tự sinh".
- **(b) Backfill legacy:** asset legacy (tạo trước khi có `qr_token` field, hoặc import data thiếu token) → chạy 1-shot `ensure_asset_qr_token(asset)` (F11, idempotent — no-op nếu đã có) cho toàn bộ asset thiếu token. (Patch backfill `v3_2.008` đã có ở ADR-001 §IV.3a — xác nhận coverage; nếu còn legacy sót → re-run idempotent.) *(Cần khảo sát baseline)* `COUNT(*) AC Asset WHERE qr_token IS NULL OR qr_token = ''` trước khi đóng gap.
- **(c) [DONE — không phải gap nữa] `_build_qr_url` đọc `frappe.conf.assetcore_qr_base_url`:** đã wire ở BE (F13, `services/imm00.py:500-560`). AC-4 mục "(tuỳ)" → **đóng**. Vận hành chỉ cần set site_config key khi go-live (host công khai).

> Đo được: 3 test path (form/import/registration) GREEN; `COUNT qr_token rỗng = 0` sau backfill; `_build_qr_url` với site_config set → URL host công khai (test đã có ở suite QR).

---

### D5 — LABEL SPEC: nhãn = 5 field (QR + model + serial + tên + mã) + khổ tem + loại máy in

**Quyết định (1 dòng):** nhãn QR in **ĐỦ 5 field hiển thị** = **QR** (encode `qr_url` `/a/<token>`) + **Model** + **Số serial NSX** (`manufacturer_sn`) + **Tên tài sản** (`asset_name`) + **Mã tài sản** (`asset_code`).

**Bảng 5 field + nhãn VI nguyên văn:**

| Field nguồn | Nhãn VI in trên tem (nguyên văn) | Trạng thái payload hiện tại |
|---|---|---|
| `qr_url` (encode QR `/a/<token>`) | *(QR code, không nhãn chữ)* | ✅ có (F12) |
| `device_model_name` | **Model** | ✅ có (F12) |
| `manufacturer_sn` | **Số serial NSX** | ❌ **THIẾU** trong `build_asset_label_data` (F12) |
| `asset_name` | **Tên tài sản** | ❌ **THIẾU** trong `build_asset_label_data` (F12) |
| `asset_code` | **Mã tài sản** | ✅ có (F12) |

**Chốt GAP (Vòng 2+ thực thi):** `build_asset_label_data` (+`_batch`) PHẢI thêm 2 field đọc từ AC Asset: **`manufacturer_sn`** + **`asset_name`** (cả 2 đã là field AC Asset — chỉ thêm vào `get_value` columns + dict return). KHÔNG đổi `qr_url`/token. Nhãn VI "Mã tài sản"/"Số serial NSX" đồng nhất ADR-ASSETCODE D4.

**Khổ tem + loại máy in (chốt 3 preset):**

| Preset | Khổ | Loại máy in | Layout | Dùng khi |
|---|---|---|---|---|
| **Tem nhỏ** | **50×30 mm** | Máy in nhãn barcode (nhiệt) | QR trái + 4 dòng chữ phải (mã/tên/model/serial), font ≥7pt | Dán trực tiếp lên thiết bị nhỏ/tay cầm |
| **Tem vừa** | **70×40 mm** | Máy in nhãn barcode (nhiệt) | QR lớn trái + 4 dòng chữ phải, font ≥9pt | Thiết bị lớn, đọc xa hơn |
| **A4 nhiều tem** | **A4 (grid N×M)** | Máy in laser A4 thường | Lưới nhiều tem/trang, `@page size: A4` | In hàng loạt khi nhập kho/import |

- FE `@page size: …mm` đặt theo preset chọn (print-CSS). QR encode `qr_url` (host công khai nếu site_config set — D4c/F13).
- **Defer code sang Vòng 2+ (FE-only):** chọn preset + print-CSS là FE; BE chỉ đảm bảo payload đủ 5 field (gap trên).

> Đo được: `build_asset_label_data` return chứa `manufacturer_sn`+`asset_name` (test assert keys); preview nhãn hiển thị đủ 5 field + QR quét được; 3 preset render đúng `@page size`.

---

### D6 — QUYỀN PRINT / ROTATE cho persona vận hành (self-correction P2 carry) — ✅ EXECUTED (Vòng 3, 2026-06-08)

> **Trạng thái thực thi:** ADR↔code ĐÃ KHỚP. Phương án B đã code + test (BE 254 OK + 53 rbac OK; FE 941 OK). KHÔNG còn lệch "1 bên Accepted 1 bên đã-BỎ" — note stale cũ ở `api/imm00.py` + `test_imm00.py` đã bị thay bằng gate `asset.print`/`asset.qr.rotate` + version mới.

**Quyết định (1 dòng):** **TÁCH capability riêng** cho print/rotate — KHÔNG gate `asset.write` (quá rộng) cũng KHÔNG `asset.read` (quá lỏng): thêm **`asset.print`**→(AC Asset,"print") (in/re-print nhãn) + **`asset.qr.rotate`**→(AC Asset,"write") (sinh lại token); gate theo DocPerm/capability, KHÔNG hardcode role-name.

**Vấn đề (self-correction nguồn — F14/F15):** hiện in nhãn + rotate gate `asset.write`, mà `asset.write` THỰC TẾ chỉ AssetCore Super Admin có → **KTV/QL vật tư (persona vận hành) KHÔNG in/rotate được** (nút ẩn ĐÚNG gate nhưng persona cần dùng lại không dùng được). 2 phương án:

| Phương án | Print | Rotate | Đánh giá |
|---|---|---|---|
| **A — nới về `asset.read`** | `asset.read` | `asset.read` | ❌ quá lỏng: rotate token = thao tác ghi (đổi định danh phụ, vô hiệu tem cũ) — không thể chỉ-đọc; print cũng có side-effect backfill token. |
| **B — TÁCH cap riêng (CHỐT)** | **`asset.print`** | **`asset.qr.rotate`** | ✅ least-privilege chính xác: persona vận hành được cấp `asset.print`(+`asset.qr.rotate` cho QL vật tư) qua DocPerm/Role mà KHÔNG cần `asset.write` (vốn cho sửa toàn asset). |

**Chốt phương án B — thay đổi RBAC ĐÃ THỰC THI (đo được, EXECUTED Vòng 3):**
- **Thêm 2 capability vào `CAPABILITY_MAP`** (`rbac.py` block `.update(...)`): **`asset.print`→(AC Asset,"print")** + **`asset.qr.rotate`→(AC Asset,"write")`**. **Binding ĐÃ CHỐT = lựa chọn (i)** (KHÔNG tạo ptype custom): `print` là permtype DocPerm chuẩn của Frappe (đã có sẵn trên AC Asset); rotate = thao tác GHI ⇒ nhóm `write`. **Grounding verified at source (2026-06-08, `bench --site miyano console`):** DocPerm AC Asset có **`print=1` cho MỌI role** (AssetCore Auditor/System User + 13 `* User` gồm Calibration/Commissioning/Corrective/Inventory/Repair…) → `asset.print` resolve TRUE NGAY cho mọi persona vận hành, **KHÔNG cần đổi DocPerm cho in**; **`write=1` CHỈ `AssetCore Super Admin`** → `asset.qr.rotate` mặc định chỉ Super Admin (QL vật tư cấp thêm write/grant qua DocPerm khi cần rotate).
- **CAP_SET_VERSION ĐÃ ĐỔI: `v95.3388ee5629c1` → `v97.c30c69b8974d`** (97 cap, hash recompute từ `sorted(CAPABILITY_MAP)`). FE `auth.ts::CAP_SET_VERSION` ĐÃ bump khớp → `isCapCacheStale` tự bỏ persisted-caps cũ; `after_migrate→invalidate_capabilities()` (hook `hooks.py:4`) self-heal cache server. **ADR-001 ghi "GIỮ v95.3388ee5629c1" → ADR NÀY supersede** (thêm cap = version PHẢI đổi; cơ chế self-heal đã có).
- **Đổi gate endpoint (`api/imm00.py`) — ĐÃ THỰC THI:** `get_asset_label_data` (:429) + `get_asset_label_data_batch` (:456) + `mark_label_printed` (:493) đổi `asset.write`→**`asset.print`**; `regenerate_asset_qr_token` (:554) đổi `asset.write`→**`asset.qr.rotate`**. KHÔNG đụng rate-limit rotate (10/60s), no-raw-token parity (`_strip_qr_token`), `_MAX_LABEL_BATCH=200` (413), available_actions/label payload (D5 đã DONE).
- **Đổi gate FE (mirror BE, chống dead-button) — ĐÃ THỰC THI:** route `AssetLabelPrint` (`router/index.ts`) `asset.write`→`asset.print`; `AssetListView.vue::canPrintLabel`→`asset.print`; `AssetDetailView.vue` nút "In nhãn QR"→`asset.print`, nút "Sinh lại mã QR"→`asset.qr.rotate`. Nút "Chỉnh sửa" GIỮ `asset.write` (sửa asset ≠ in/rotate).
- **Cấp quyền persona (DocPerm/Role, KHÔNG hardcode role-name trong code):** KTV TBYT + QL vật tư ĐÃ có `asset.print` (print=1 sẵn — không thao tác gì thêm); QL vật tư cần rotate tem hỏng → admin cấp `write` (hoặc Custom DocPerm permtype write) trên AC Asset cho role tương ứng ở `/app` — KHÔNG deploy code. Admin (Super Admin) giữ cả hai (print=1+write=1).

**DocPerm matrix (verified 2026-06-08 — SSoT cho phân quyền D6):**

| Role | read | write | print | → `asset.print` | → `asset.qr.rotate` | Persona |
|---|---|---|---|---|---|---|
| AssetCore Super Admin | 1 | 1 | 1 | ✅ | ✅ | Admin (in + rotate) |
| Repair User / Calibration User / Corrective User / Inventory User / … (mọi `* User`) | 1 | 0 | 1 | ✅ | ❌ | KTV/QL vận hành (in được, KHÔNG rotate) |
| AssetCore Auditor / System User | 1 | 0 | 1 | ✅ | ❌ | Đọc + in |
| (Guest / role không-print) | 0 | 0 | 0 | ❌ | ❌ | 403 cả in lẫn rotate |

> Đo được (ĐÃ XANH): (1) user có DocPerm print AC Asset NHƯNG KHÔNG write → `get_asset_label_data`/`mark_label_printed` = **200** (test `TestLabelWriteCapability.test_label_data_print_user_200` + `test_mark_printed_print_user_200`); (2) user KHÔNG print (Guest) → **403** VI sạch + 0 side-effect (`test_label_data_no_print_user_403` + `test_mark_printed_no_print_user_403_no_side_effect`); (3) user có `asset.qr.rotate` (write) → rotate **200** (`test_regenerate_write_user_200_new_token`); user chỉ print → rotate **403** (`test_regenerate_print_only_user_403`); (4) `CAP_SET_VERSION` = `v97.c30c69b8974d` (≠ v95…, test `test_cap_set_version_changed_after_split_caps`); (5) `grep -rE 'if.*role ?== ' ` quanh 4 endpoint = 0 (không hardcode role — gate thuần capability); (6) IDOR bất biến (`test_label_idor_unchanged_after_print_gate` + `test_regenerate_vendor_out_of_scope_forbidden_no_leak`).

---

### D7 — FE CONTRACT: `AssetScanInfoView` render BE-driven + deep-link + i18n SSoT (gate [FE] — DOC-ONLY)

**Quyết định (1 dòng):** FE màn quét render nút hành động **HOÀN TOÀN từ `available_actions` BE-driven** — FE **KHÔNG** tự tính quyền/lifecycle (BE là SSoT D2); mỗi nút deep-link `?asset=<name>&source=qr-scan` (D3); disabled+reason VI khi `enabled=false`; **GIỮ phần info read-only** (F19); nhãn action i18n VI qua SSoT (F18).

**Contract FE đo được (FE thực thi Vòng 2+, KHÔNG code Vòng 1):**

- **C1 — render thuần BE-driven (KHÔNG tự tính quyền):** `AssetScanInfoView` lặp `info.available_actions` → 1 nút/phần tử. `enabled` quyết định nút bấm được hay không; FE **KHÔNG** đọc `lifecycle_status` hay capability để tự suy ra enabled (tránh drift FE↔BE + leo quyền client-side). Nếu BE chưa trả `available_actions` (payload cũ) → KHÔNG render cụm nút (defensive absent-vs-empty: `available_actions == null` → ẩn cụm; `== []` → ẩn cụm; có phần tử → render). *Đo:* mock payload thiếu key → KHÔNG vỡ; FE không có nhánh `if lifecycle_status === ...` quanh nút action (grep = 0).
- **C2 — disabled + reason VI (KHÔNG nút chết):** `enabled=false` → nút `disabled` + `:title`/tooltip = `reason` + `aria-disabled="true"`; click KHÔNG điều hướng. reason hiển thị nguyên văn BE (đã VI D2) — FE KHÔNG tự dịch/ghép. a11y: cụm reason có `role="status"`/`aria-live="polite"` để screen-reader đọc lý do. *Đo:* Decommissioned → 4 nút disabled, tooltip "Thiết bị đã thanh lý"; thiếu-cap → tooltip "Bạn không có quyền…".
- **C3 — deep-link KHÔNG raw token:** nút enabled → `router.push({ name: action.route, query: { asset: info.name, source: 'qr-scan' } })`. `info.name` (= asset_code, invariant) — **KHÔNG** dùng `token`/`qr_url`. FE KHÔNG ghép query-string thủ công (dùng router object-form). *Đo:* Playwright URL sau click chứa `?asset=<name>&source=qr-scan`, grep `qr_token`/`/a/` trong URL action = 0.
- **C4 — info GIỮ read-only:** cụm nút action là **THÊM MỚI** dưới các card info; KHÔNG biến card định danh/lifecycle/bảo trì thành editable; 2 nút cũ `Quét lại`/`Về trang chủ` GIỮ (F19). *Đo:* không có `<input>`/`v-model` mới ở phần info; chỉ thêm `<button>` action.
- **C5 — i18n VI SSoT (F18):** nhãn nút ưu tiên `action.label` (BE VI). FE thêm `SCAN_ACTION_LABELS` (`labels.ts`) làm SSoT parity (key→VI) cho guard-test + fallback; KHÔNG hardcode 'Báo hỏng'/'Yêu cầu…' rải rác trong .vue. *Đo:* parity test `SCAN_ACTION_LABELS[key] === BE.label` cho 4 key; grep nhãn action hardcode trong .vue = 0.
- **C6 — deep-link nhận phía create-view (gap chính = PM, F8):** 3 view (Incident/CM/Calibration) ĐÃ prefill `?asset` → CHỈ thêm khoá field + cờ `source`; **PMWorkOrderCreateView** wire MỚI (`useRoute`+`route.query.asset`→`form.asset_ref`+khoá). KHÔNG hardcode token. *Đo:* grep `route.query.asset` 4 view = 4; field asset disabled khi `source==='qr-scan'`.

**Self-correction [FE] so ADR draft trước (đính chính F8 — verified at source):** F8 cũ ghi "4 create view KHÔNG đọc `route.query.asset`" = SAI. Thực tế **3/4 ĐÃ đọc** (Incident/CM/Calibration prefill), **CHỈ PM thiếu**. Gap thực = (a) PM wire mới, (b) cả 4 thêm khoá field + cờ `source=qr-scan` (chưa view nào đọc cờ nguồn). Đã sửa F8 + bảng delta per-view ở D3.

> Đo được tổng (FE-gate): sau Vòng 2+, (1) `AssetScanInfoView` render N nút = `len(available_actions)`; (2) nút disabled hiện reason VI; (3) deep-link `?asset=<name>&source=qr-scan` KHÔNG token; (4) phần info vẫn read-only; (5) nhãn action qua SSoT (parity test xanh); (6) 4 create view nhận `?asset` (PM được wire) + khoá field.

---

### D8 — INPUT NORMALIZATION: chuẩn hoá whitespace `qr_token` ở SSoT service resolve — token tem nhiệt kèm `\n`/space vẫn mở đúng hồ sơ (factory vòng 6, 2026-06-11 — Self-Correction A2/A6, BE-gate)

**Bối cảnh (lỗi thiết kế gốc):** A2/A6 (D2/D3/D4) đặc tả resolve `qr_token → asset` + "404 leak-safe" nhưng **CHƯA đặc tả chuẩn-hoá input**. SSoT `services/imm00.py::resolve_qr_token(token)` guard rỗng/None rồi tra THẲNG `frappe.db.get_value("AC Asset", {"qr_token": token}, "name")` — KHÔNG strip whitespace. Token DB sạch `secrets.token_urlsafe(16)` ∈ `[A-Za-z0-9_-]` (KHÔNG space). Hệ quả thực tế (eval QR-PDF-PRINT): tem QR in **máy nhiệt** (preset `tem-60x100`, D5/BR-00-30) encode deep-link `/a/<token>` có thể đính **trailing `\n`** (artifact một số encoder/driver) → KTV quét tem hợp lệ → **false-404** (mở hồ-sơ-trống) = gap truy xuất nguồn gốc tại điểm sử dụng. Thêm: token **TOÀN khoảng trắng** (`'   '`/`'\t'`/`'\n'`) là chuỗi non-empty → vượt guard `not token` → VẪN query DB (1 round-trip vô ích, bất đối xứng với `''`).

**Quyết định (1 dòng — DỨT KHOÁT, đo được):** chuẩn hoá whitespace **DUY NHẤT** ở SSoT `resolve_qr_token` — strip token **đầu hàm** (`token = token.strip() if isinstance(token, str) else ""`) TRƯỚC guard rỗng + TRƯỚC query; `get_asset_scan_info` nhánh `token` kế thừa qua `_svc_resolve_qr_token` (KHÔNG fork nhánh strip thứ 2). FE trim GIỮ làm defense-in-depth lớp 1, server tự đúng độc lập.

| Quy tắc D8 | Acceptance đo được |
|---|---|
| **D8-1** Strip đầu hàm `resolve_qr_token` (SSoT, service-tier — KHÔNG API-tier) | `resolve_qr_token(' <tok> ')` / `'<tok>\n'` / `'\t<tok>\t'` → ĐÚNG payload asset `<tok>` (5-field). Hết false-404 do whitespace tem nhiệt. |
| **D8-2** Guard rỗng-sau-strip return None TRƯỚC query | token TOÀN whitespace → strip `''` → return None **KHÔNG query DB** → **query-count = 0** (đối xứng `''` đã guard) |
| **D8-3** Whitespace KHÔNG khớp asset → 404 leak-safe | `' khong-ton-tai '` → không khớp → 404; KHÔNG phân biệt sai-định-dạng vs không-tồn-tại; KHÔNG 500/417; KHÔNG raw exception |
| **D8-4** Parity `get_asset_scan_info` qua CÙNG SSoT (KHÔNG fork) | `get_asset_scan_info(token=' <tok> ')` / `'<tok>\n'` → payload A6 (11-field) đúng; whitespace-only → 404 KHÔNG query asset thừa. grep: chỉ 1 `.strip()` cho token-resolve toàn codebase |
| **D8-5** FE defense-in-depth GIỮ, server độc lập | `QrResolveView.vue:34` (`.trim()`) + `QRScanView.vue:45` (`extractToken` `raw.trim()`) GIỮ lớp 1; mobile-BE / encode-artifact / caller KHÔNG qua FE trim vẫn resolve đúng (truy về source — CLAUDE.md §5/§20) |
| **D8-6** No-regression (read-only/no-leak/parity GIỮ) | KHÔNG audit/lifecycle (D4); RBAC `asset.read` + IDOR `assert_vendor_can_access` + rate-limit `AC_QR_RESOLVE_RATE_LIMIT=30` GIỮ; payload shape + `qr_url`/encode/no-raw-token (D3/BR-00-34) KHÔNG đụng; 100% test QR-asset BE GREEN |

**Schema-delta: KHÔNG** (thuần 1 dòng strip + guard + test). `CAP_SET_VERSION` GIỮ NGUYÊN. **FE: KHÔNG đổi** (BE-only — FE trim đã có làm lớp 1). Map FR/BR: **FR-00-90/FR-00-91 + BR-00-40** (02 §IV.17). Cài đặt: [04 §II.1.8a-NORM](./04_Backend_Design.md). Contract: [05 §III.1 resolve_qr_token / get_asset_scan_info](./05_API_Specification.md). Test: [07 §III.6.c-TOKENNORM](./07_Testing_QA.md).

> Đo được tổng (BE-gate D8): (1) token hợp lệ kèm space/`\n` resolve đúng (2 endpoint); (2) whitespace-only → query-count = 0; (3) whitespace-no-match → 404 leak-safe no-500/417; (4) chỉ 1 `.strip()` SSoT (grep); (5) `TestResolveQrToken`/`TestAssetScanInfo`/label-pdf GREEN — 0 regression.

---

### D9 — REASON-NON-EMPTY INVARIANT: action disabled LUÔN kèm lý do VI — bịt lỗ hổng status rỗng/lạ trả disabled-không-lý-do (factory vòng 7, 2026-06-11 — Self-Correction lỗi thiết kế gốc D2, BE+FE-gate)

**Bối cảnh (lỗi thiết kế gốc — đào tới source):** D2 chốt thứ tự `reason = lifecycle_reason or capability_reason`, nhưng cả 2 nhánh có **lỗ rỗng** khi `lifecycle_status` rỗng/lạ:
- `_lifecycle_allows(status, key)` (services/imm00.py:475) safe-default **False** cho status rỗng/lạ (đúng — không cho phép action). NHƯNG `_lifecycle_reason(status, key)` (:490) chỉ map 3 nhóm `Decommissioned`/`Out of Service`/`Draft` → status rỗng/lạ rơi vào `return ""` cuối hàm (:508).
- Trong `_build_available_actions` (:536): `reason = _lifecycle_reason(...) or ("" if has_cap else _CAPABILITY_REASON)`. Khi status rỗng/lạ **+ user CÓ cap** (vd Administrator có mọi DocPerm) → `_lifecycle_reason=""` **và** `has_cap=True` ⇒ `reason=""` trong khi `enabled=False`.
- **Hệ quả:** payload có phần tử `{enabled:false, reason:""}`. FE `AssetScanInfoView.vue` (:300/:304/:315) khi đó: `:title` rỗng (mất tooltip), `:aria-describedby="reason-<key>"` trỏ tới `<li>` **KHÔNG render** (cụm reason `v-for` lọc `!x.enabled && x.reason` → bỏ qua reason rỗng) → **aria-describedby dangling** + `aria-label` kết thúc `"… không khả dụng: "` (trailing rỗng). KTV thấy nút xám KHÔNG biết vì sao = gap a11y + UX self-service. Test cũ `test_unknown_status_safe_default` (test_imm00.py:3719) chỉ assert `isinstance(reason, str)` → `""` vẫn pass = **false-green** (lỗ hổng không bị bắt).

**Trạng thái thực tế "rỗng/lạ" CÓ xảy ra:** `lifecycle_status` có thể là `''` (asset legacy chưa set, hoặc bị clear — test mô phỏng `set_value("",…)`) hoặc mã LẠ ngoài enum (drift dữ liệu / migration cũ / import sai). `build_asset_scan_info` đọc THẲNG field DB (`row.get("lifecycle_status") or ""`) — KHÔNG validate enum → status lạ truyền nguyên xuống `_build_available_actions`. Đây là input thật, KHÔNG giả định.

**Quyết định (1 dòng — DỨT KHOÁT, đo được):** thêm **1 hằng VI mới** `_LIFECYCLE_REASON_UNKNOWN = "Thiết bị không ở trạng thái cho phép thao tác này"` (literal SSoT BE, services/imm00.py cạnh 3 hằng `_LIFECYCLE_REASON_*` hiện có) làm **fallback cuối** trong derive reason. Chốt thứ tự đầy đủ (3 bậc):
```
reason = _lifecycle_reason(status, key)              # bậc 1: lifecycle khớp 3 nhóm → VI nhóm
         or ("" if has_cap else _CAPABILITY_REASON)  # bậc 2: thiếu cap → VI quyền
         or _LIFECYCLE_REASON_UNKNOWN                 # bậc 3: lifecycle chặn nhưng rỗng/lạ + có cap → VI unknown
```
Bậc 3 CHỈ kích hoạt khi `enabled=False` mà bậc 1 + bậc 2 đều rỗng (⟺ status rỗng/lạ + user có cap). Vị trí sửa: **CHỈ** `_build_available_actions` (1 SSoT) — KHÔNG đụng `_lifecycle_reason` (giữ contract trả `""` khi không-khớp-nhóm; fallback ráp ở caller, tránh `_lifecycle_reason` phải biết về capability).

| Quy tắc D9 | Acceptance đo được |
|---|---|
| **D9-1** Bất biến reason-non-empty TOÀN status | `for a in available_actions: a["enabled"] is False ⟹ a["reason"] != ""` — đúng với **cả 4 action**, **mọi** `lifecycle_status` (5 status đã biết + `''` + ≥1 mã lạ). |
| **D9-2** Status rỗng/lạ + CÓ cap → hằng unknown | `lifecycle_status ∈ {'', 'Trạng-thái-lạ', …}` + user đủ cap → 4 action `enabled=False`, `reason == _LIFECYCLE_REASON_UNKNOWN` ("Thiết bị không ở trạng thái cho phép thao tác này"). |
| **D9-3** Status rỗng/lạ + THIẾU cap → reason quyền (ưu tiên giữ) | status rỗng/lạ + user thiếu cap action đó → `reason == _CAPABILITY_REASON` (bậc 2 trước bậc 3 — KHÔNG rỗng, KHÔNG nhảy thẳng unknown). |
| **D9-4** No-regression 5 status đã biết (byte-for-byte) | Active/Commissioned → 4 enabled `reason==""`. Decommissioned → 4 disabled `"Thiết bị đã thanh lý"`. Out of Service → report_failure/request_cm enabled, request_pm/request_calibration disabled `"Thiết bị đang ngừng hoạt động …"`. Draft → 4 disabled `"Thiết bị chưa đưa vào vận hành"`. **Reason cũ KHÔNG đổi 1 byte** (chỉ THÊM nhánh bậc-3, không sửa bậc 1/2). |
| **D9-5** enabled=True ⟹ reason=="" GIỮ | Bất biến cũ D2 không đổi: action enabled → `reason==""` (bậc 3 KHÔNG chạm path enabled). |
| **D9-6** FE render an toàn — hết dangling aria-describedby + trailing rỗng | `AssetScanInfoView.vue`: disabled action reason non-rỗng → `:title=reason` hiển thị; có `<li id="reason-<key>">` tương ứng (aria-describedby KHÔNG dangling); `aria-label` kết thúc bằng reason thực (KHÔNG trailing `: `). FE phòng-thủ-kép: cụm `<li>` lọc theo `!x.enabled` (BỎ điều kiện `&& x.reason`) HOẶC `aria-describedby` chỉ set khi `reason` non-rỗng → KHÔNG còn nút disabled thiếu cả tooltip lẫn dòng giải thích, kể cả nếu BE (giả định) trả rỗng. SSoT reason VẪN ở BE (FE KHÔNG bịa chuỗi). |
| **D9-7** Shape + version + no-EN-leak GIỮ | KHÔNG field payload mới; shape `{key,label,route,enabled,reason}` KHÔNG đổi; `CAP_SET_VERSION` GIỮ; reason 100% VI (no-EN-leak); KHÔNG schema/migration/reload (thuần logic derive + literal + test). |

**Schema-delta: KHÔNG** (1 hằng VI + 1 nhánh `or` ở `_build_available_actions` + FE 1 attr-guard + test). `CAP_SET_VERSION` GIỮ NGUYÊN. Map FR/BR: **FR-00-92 + BR-00-41** (02 §IV.18). Cài đặt BE: [04 §II.1.8f](./04_Backend_Design.md). Cài đặt FE: [06 §reason-render](./06_Frontend_Design.md). Contract: [05 §III.1 `available_actions` reason invariant](./05_API_Specification.md). Test: [07 §III.6.d-REASONNONEMPTY](./07_Testing_QA.md).

> Đo được tổng (gate D9): (1) ∀status×4action: `enabled=False ⟹ reason!=""`; (2) rỗng/lạ+cap → `_LIFECYCLE_REASON_UNKNOWN`; (3) rỗng/lạ+thiếu-cap → reason quyền (KHÔNG rỗng); (4) 5 status đã biết reason byte-for-byte cũ; (5) enabled⟹reason=="" giữ; (6) FE 0 dangling aria-describedby + 0 trailing-rỗng aria-label; (7) `bench run-tests test_imm00` + vitest `AssetScanInfoView` GREEN (test mới RED-trước-fix). Reason là literal VI SSoT BE — no-EN-leak; KHÔNG đổi shape/version/schema.

---

### D10 — STATUS-PILL NO-LEAK INVARIANT: `lifecycleStatusLabel` LUÔN trả nhãn VI an toàn — bịt lỗ leak mã EN/code thô/rỗng trên pill màn quét QR (factory vòng 8, 2026-06-11 — Self-Correction lỗi thiết kế gốc FE-formatter, FE-only gate)

**Bối cảnh (lỗi thiết kế gốc — đào tới source):** màn quét QR render **status pill VI** từ `lifecycleStatusLabel(info.lifecycle_status)` (AssetScanInfoView.vue:37-38 → frontend/src/constants/labels.ts:237). Formatter:
```ts
export function lifecycleStatusLabel(v: string) { return LIFECYCLE_STATUS_LABEL[v] ?? v }
```
- `LIFECYCLE_STATUS_LABEL` (labels.ts:215-227) map **đúng 7 mã canonical** (`Active`/`Commissioned`/`Under Maintenance`/`Under Repair`/`Calibrating`/`Out of Service`/`Decommissioned`).
- Fallback `?? v` **trả lại chính `v` thô** khi `v` ∉ map ⟹ (a) **leak mã EN/code thô** với `v` legacy/drift (`'In Use'`, `'Retired'`, `'active'` chữ thường — import cũ / migration / nhập tay): pill hiển thị nguyên văn mã English; (b) **box trống**: BE phát `lifecycle_status or ""` (`services/imm00.py:317` payload `resolve_qr_token` + `:597` `build_asset_scan_info`) cho asset legacy chưa-set → `LIFECYCLE_STATUS_LABEL[''] === undefined` ⟹ `?? ''` trả `''` ⟹ pill render chip màu **không chữ** (vô nghĩa).

**Trạng thái rỗng/lạ CÓ xảy ra** (đối xứng phân tích D9/BR-00-41): `build_asset_scan_info` đọc THẲNG field DB không validate enum; `''` là output thật cho legacy asset; mã drift ngoài enum tồn tại do import/migration cũ. Đây là input thật, KHÔNG giả định.

**KHÁC D9** (đó là `available_actions[].reason` cho nút disabled, SSoT ở BE): đây là **nhãn pill trạng thái** ở tầng formatter VI **FE** — 1 lỗ leak EN/raw-code/empty riêng biệt.

**Quyết định (1 dòng — DỨT KHOÁT, đo được):** thêm hằng VI `LIFECYCLE_STATUS_UNKNOWN_LABEL = 'Không xác định'` (cạnh `LIFECYCLE_STATUS_LABEL`/`LIFECYCLE_STATUS_CLASS` trong labels.ts); đổi fallback formatter:
```ts
export function lifecycleStatusLabel(v: string) { return LIFECYCLE_STATUS_LABEL[v] ?? LIFECYCLE_STATUS_UNKNOWN_LABEL }
```
`lifecycleStatusClass` **GIỮ NGUYÊN** fallback `'bg-gray-100 text-gray-600'` (đã có — chip trung tính). 7 mã canonical GIỮ nhãn + màu byte-for-byte. Đây là **SSoT FE** — mọi consumer của `lifecycleStatusLabel` (scan pill + bất kỳ nơi khác) tự hưởng nhãn an toàn; **BE KHÔNG đổi** (`or ""` giữ — FE chịu trách nhiệm nhãn).

| Quy tắc D10 | Acceptance đo được |
|---|---|
| **D10-1** No-EN/raw-code leak với mã lạ | `lifecycleStatusLabel(v)` cho `v` ∉ 7 canonical (`'In Use'`/`'Retired'`/`'active'` …) → KHÔNG trả `v` thô; `lifecycleStatusLabel('In Use') !== 'In Use'`; kết quả KHÔNG chứa mã English (`/[A-Za-z]{2,}/` ngoài từ VI có dấu = 0). |
| **D10-2** Non-empty cho rỗng | `lifecycleStatusLabel('') === 'Không xác định'` (KHÔNG `''`); `lifecycleStatusLabel(null as any)`/`undefined` → `'Không xác định'`. Pill KHÔNG render box trống. |
| **D10-3** `lifecycleStatusClass` GIỮ NGUYÊN | mã lạ/rỗng → `'bg-gray-100 text-gray-600'` (chip trung tính); 7 canonical GIỮ màu cũ; KHÔNG rơi sang màu trạng thái khác. |
| **D10-4** No-regression 7 canonical byte-for-byte | mỗi mã canonical → ĐÚNG nhãn VI cũ + ĐÚNG màu cũ (test FROZEN labels.test.ts giữ xanh). |
| **D10-5** `AssetScanInfoView` pill | `info.lifecycle_status` rỗng/lạ → text pill = `'Không xác định'`, KHÔNG raw code; snapshot/text assert KHÔNG chứa mã English ngoài từ VI. |
| **D10-6** FE-only, BE giữ | `build_asset_scan_info`/`resolve_qr_token` GIỮ `or ""`; shape payload + `CAP_SET_VERSION` + schema KHÔNG đổi; KHÔNG migration/reload. |

**Schema-delta: KHÔNG** (1 hằng VI + đổi 1 dòng fallback formatter FE + test). `CAP_SET_VERSION` GIỮ NGUYÊN. **BE: KHÔNG đổi.** Map FR/BR: **FR-00-93 + BR-00-42** (02 §IV.19). Cài đặt FE: [06 §status-pill-safe](./06_Frontend_Design.md). Test: [07 §III.6.e-PILLNOLEAK](./07_Testing_QA.md).

> Đo được tổng (gate D10): (1) ∀ `v` lạ: `lifecycleStatusLabel(v)` no-EN/raw-code leak (`!== v`, không chứa mã English); (2) `''`/null/undefined → `'Không xác định'` (non-empty); (3) `lifecycleStatusClass` fallback gray GIỮ; (4) 7 canonical nhãn+màu byte-for-byte; (5) `AssetScanInfoView` pill rỗng/lạ → `'Không xác định'` no-raw-code; (6) `vitest` (`labels.test.ts` + `AssetScanInfoView.test.ts`, RED-first) GREEN + `vue-tsc` 0 + full asset-domain suite no-regression. FE-only — BE KHÔNG đổi (KHÔNG reload/migrate).

---

### D11 — LEAN ACTION-META ENDPOINT: bịt OVER-FETCH field tài chính trên đường QR scan-action — endpoint nạc `get_asset_action_meta(name)` cho 3 màn tạo WO (CM/Hiệu chuẩn/PM) thay `get_asset` full-doc (factory vòng 25, 2026-06-12 — Self-Correction lỗi thiết kế gốc: panel 5-field kéo full-doc tài chính, BE+FE-gate)

**Bối cảnh (lỗi thiết kế gốc — đào tới source):** 3 màn tạo WO đến từ deep-link QR scan-action (D1/D3 — `?asset=<name>&source=qr-scan`) render **panel meta thiết bị** chỉ ~5 field hiển thị (Tên / Model / Vị trí / Trạng thái / Mức rủi ro). Cả 3 nạp meta qua **`getAsset(name)`** (`frontend/src/api/imm00.ts:26` → BE `api/imm00.py:387 get_asset`):
- `get_asset` trả **`frappe.get_doc(_DT_ASSET, name).as_dict()`** (`:396`) = **TOÀN BỘ field AC Asset** — gồm `gross_purchase_amount` / `accumulated_depreciation` / `current_book_value` (+`purchase_cost`/`salvage_value` nếu có) — chỉ `qr_token` bị pop qua `_strip_qr_token` (`:423`). Type FE `AcAsset` (`types/imm00.ts:52`) khai báo nguyên các field tài chính (`:61/:73/:74`).
- Hệ quả (rò rỉ dữ liệu nhạy cảm theo phân loại NĐ98 — câu hỏi domain 5): payload tài chính (giá mua / khấu hao / giá trị sổ sách) **lọt xuống mọi persona vận hành** (KTV/QL vật tư) **chỉ để render panel 5 field** — KTV không cần (và theo least-privilege KHÔNG nên thấy) giá trị tài chính khi tạo phiếu CM/PM/calibration tại điểm quét. Over-fetch = bề-mặt-rò-rỉ + payload phình (DevTools/Network/cache trình duyệt giữ raw JSON tài chính).
- 3 view gọi: `CMCreateView.vue:128` (`getAsset(form.asset_ref)`), `CalibrationCreateView.vue:92` (`getAsset(form.asset)`), `PMWorkOrderCreateView.vue:131` (`getAsset(form.asset_ref)` trong `allSettled`). Cả 3 đã map về `assetMeta` 5-field rồi vứt phần còn lại — full-doc fetch là phí + rò.

**KHÁC `get_asset` (đường admin-detail GIỮ NGUYÊN):** `get_asset` PHỤC VỤ màn `AssetDetailView` (admin/senior sửa asset — CẦN field tài chính + overdue + enrich đầy đủ) → **KHÔNG đụng** `get_asset` (no-regression). D11 thêm 1 endpoint **RIÊNG, nạc** cho đường scan-action create-view panel — tách bề-mặt đọc theo nhu-cầu-tối-thiểu (cùng triết lý A2 `resolve_qr_token` "CHỈ resolve + field tối thiểu, KHÔNG trả toàn bộ asset", `api/imm00.py:433`).

**Quyết định (1 dòng — DỨT KHOÁT, đo được):** thêm endpoint whitelist MỚI **`get_asset_action_meta(name: str = "")`** trả **ĐÚNG 6 key cố định** (allowlist tường minh — KHÔNG `as_dict()`); 3 view CM/Calibration/PM gọi `getAssetActionMeta(name)` THAY `getAsset(name)` cho panel meta; KHÔNG còn đường nào kéo full-doc tài chính chỉ để render panel.

**Shape payload `get_asset_action_meta` (CHỐT — 6 key, KHÔNG hơn KHÔNG kém):**
```
{
  "name":                str,   # = asset_code (invariant ADR-ASSETCODE) — định danh
  "asset_name":          str,   # tên tài sản (hiển thị)
  "device_model_name":   str,   # ENRICH từ device_model (Link IMM Device Model.model_name) — KHÔNG raw Link id
  "lifecycle_status":    str,   # mã canonical (FE dịch VI qua lifecycleStatusLabel — D10)
  "risk_classification": str,   # field STORED trên AC Asset (Select, fetch_from device_model.risk_classification) — đọc THẲNG, KHÔNG enrich
  "location_name":       str    # ENRICH từ location (Link AC Location.location_name) — KHÔNG raw Link id
}
```
- **TUYỆT ĐỐI KHÔNG** chứa: `gross_purchase_amount` / `accumulated_depreciation` / `current_book_value` / `purchase_cost` / `salvage_value` / `qr_token` / audit-chain / bất kỳ field tài chính-nhạy cảm nào khác. Allowlist = **đúng 6 key trên** (assert keys == tập 6 key cố định, KHÔNG thừa).
- **Cách dựng (KHÔNG `as_dict()`):** `frappe.db.get_value(_DT_ASSET, name, ["name","asset_name","device_model","lifecycle_status","risk_classification","location"], as_dict=True)` → 4 field đọc thẳng + 2 Link (`device_model`/`location`) ENRICH qua `get_value(...model_name/location_name)` rồi DROP raw Link key trước return. Chỉ surface đúng 6 key allowlist.
- **`risk_classification` enrich đúng:** field STORED (Select, read_only, `fetch_from device_model.risk_classification` — đã verified `ac_asset.json:284`) → đọc THẲNG từ AC Asset, KHÔNG resolve link. `device_model_name`/`location_name` resolve từ Link như `get_asset`/`list_assets` (cùng `_enrich` pattern).

**3 lớp bảo mật GIỮ NGUYÊN như `get_asset` (KHÔNG nới, KHÔNG bỏ):**

| Lớp | Quy tắc D11 | Acceptance đo được |
|---|---|---|
| **D11-1** name rỗng/None/∄ → 404 leak-safe | `name=""`/`None`/không tồn tại → `_err(_ERR_ASSET_NOT_FOUND, 404)` (HTTP-200 + Error envelope **in-handler**, KHÔNG raise→4xx). **KHÔNG 500/traceback-leak** (LL-BE-42); KHÔNG phân biệt sai-định-dạng vs không-tồn-tại. | `get_asset_action_meta("")` / `"AC-KHONG-TON-TAI"` → envelope `code=NOT_FOUND http_status=404`, body KHÔNG traceback/field tài chính. |
| **D11-2** Vendor ngoài scope → 403 IDOR no-leak | `assert_vendor_can_access(_DT_ASSET, name)` ÁP TRƯỚC khi build payload (như `get_asset:393`); vendor ngoài scope → `_err(e.message, e.code)` = **in-handler cap-403** (HTTP-200 + Error envelope), KHÔNG leak 6-field payload. Phân biệt với **dispatcher-403** (guest/no-token → re-auth, KHÔNG vào handler). | vendor-A đọc asset của vendor-B → 403, payload KHÔNG xuất hiện (0 byte 6-field); asset hợp-scope → 200 6-field. |
| **D11-3** RBAC/DocPerm read áp như get_asset | `@frappe.whitelist()` (session-gated, KHÔNG allow_guest) — DocPerm read AC Asset áp như đường `get_asset` (Frappe `frappe.db.get_value` tôn trọng quyền field-level? KHÔNG — `db.get_value` bypass permission_query; ⟹ IDOR guard D11-2 + whitelist session-gate là hàng rào). KHÔNG gate cap-mới (đọc meta = đọc asset cơ bản, không thêm capability). | guest/no-token → dispatcher-403 (re-auth); session hợp-lệ + IDOR-pass → 200. |

**Quy tắc derive + no-regression:**

| Quy tắc D11 | Acceptance đo được |
|---|---|
| **D11-4** Allowlist 6-key EXACT (no over-fetch) | `set(payload.keys()) == {"name","asset_name","device_model_name","lifecycle_status","risk_classification","location_name"}`. Quét tập key cấm `{gross_purchase_amount, accumulated_depreciation, current_book_value, purchase_cost, salvage_value, qr_token}` ∩ keys == ∅. (BE assert + FE type CHỈ 6 field — KHÔNG kế thừa `AcAsset`.) |
| **D11-5** Enrich đúng (no raw Link id) | `device_model_name` = model_name của Link `device_model` (KHÔNG `DM-...`); `location_name` = location_name của Link `location` (KHÔNG `LOC-...`); `risk_classification` đọc thẳng (Low/Medium/High/Critical hoặc rỗng). Link rỗng → enrich `""` (KHÔNG vỡ). |
| **D11-6** `get_asset` GIỮ NGUYÊN (admin-detail no-regression) | `get_asset` shape/field tài chính/overdue/enrich KHÔNG đổi 1 byte (`AssetDetailView` còn dùng). D11 là endpoint THÊM MỚI song song — KHÔNG sửa `get_asset`. |
| **D11-7** No schema/cap/migration | KHÔNG field/DocType mới; KHÔNG capability mới; `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`; KHÔNG migration/reload/restart. Thuần thêm 1 whitelist read-only + đổi import-site FE + 1 type FE. |

**Ràng buộc FE (Vòng 25 thực thi — delta PER-VIEW):**

- **`api/imm00.ts`:** thêm `getAssetActionMeta(name): Promise<AssetActionMeta>` + interface `AssetActionMeta` **CHỈ 6 field** (KHÔNG `extends AcAsset` — tránh kế thừa field tài chính). `getAsset`/`AcAsset` GIỮ (admin-detail còn dùng).
- **3 view:** đổi import `getAsset` → `getAssetActionMeta`; loader gọi `getAssetActionMeta(<asset>)` THAY `getAsset(<asset>)`; map về `assetMeta` 5-field như cũ (PM: 4-field, không có risk). KHÔNG còn `getAsset(` cho panel meta ở 3 view (grep = 0).
- **Nhãn rỗng VI an toàn (parity Vòng 22 — no-em-dash câm, no-EN-leak):** ô panel meta CM (`CMCreateView.vue:331` risk_classification) + Hiệu chuẩn (`CalibrationCreateView.vue:226` risk_classification) thay fallback `'—'` (em-dash trơ) cho `risk_classification` rỗng bằng nhãn VI **`'Chưa gán'`**. Các dòng meta rỗng khác (Tên/Model/Vị trí/Trạng thái) cũng dùng nhãn VI an toàn (`'Chưa gán'` / nhãn-status-unknown D10), KHÔNG `'—'` câm. (Self-Correction so test cũ `CMCreateView.assetMeta.test.ts:11-12` ghi "risk rỗng → '—'" — Vòng 25 ĐỔI sang `'Chưa gán'`; cập nhật assert test cho khớp.)
- **Fail-safe GIỮ NGUYÊN:** 403 vendor-IDOR / 404 / network → `assetMeta = null` (panel ẩn, KHÔNG vỡ trang, KHÔNG leak raw exception/email/qr_token). Đường QR-prefill **locked SmartSelect** (D3 / Vòng 2/23) + **zero-schedule guidance** (Vòng 3) KHÔNG regress.

| Quy tắc FE D11 | Acceptance đo được |
|---|---|
| **D11-8** type nạc — KHÔNG kế thừa financial | `interface AssetActionMeta` = đúng 6 field optional/required; `grep 'extends AcAsset' getAssetActionMeta`=0; `vue-tsc` 0 lỗi; KHÔNG field tài chính trong type. |
| **D11-9** 3 view migrate (no full-doc cho panel) | grep `getAsset(` (KHÔNG `getAssetActionMeta`) trong 3 view cho panel meta = 0; loader gọi `getAssetActionMeta`; panel render Tên/Model/Vị trí/Trạng thái(+Risk CM/Cal) đúng. |
| **D11-10** nhãn rỗng VI `'Chưa gán'` | `risk_classification` rỗng → ô "Risk class"/"Mức rủi ro" render `'Chưa gán'` (KHÔNG `'—'`, KHÔNG rỗng); test cũ assert `'—'` → cập nhật `'Chưa gán'`; KHÔNG mã EN leak. |
| **D11-11** fail-safe + QR-prefill no-regress | `getAssetActionMeta` reject (403/404/network) → `assetMeta=null`, panel ẩn, KHÔNG leak email/qr_token/IDOR; lock SmartSelect (`source==='qr-scan'`) + canSubmit gating Decommissioned + zero-schedule guidance GIỮ XANH (suite vitest 3 view no-regression). |

**Schema-delta: KHÔNG** (1 endpoint whitelist read-only + 1 api-client fn + 1 type FE + đổi import-site 3 view + đổi 2 fallback nhãn). `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`. Map FR/BR: **FR-00-99 + BR-00-48** (02 §IV.24). Cài đặt BE: [04 §II.1.8g-ACTIONMETA](./04_Backend_Design.md). Contract: [05 §III.2 get_asset_action_meta](./05_API_Specification.md). Cài đặt FE: [06 §action-meta-lean](./06_Frontend_Design.md). Test: [07 §III.6.f-ACTIONMETA](./07_Testing_QA.md).

> **DONE-gate spec-contract (đo được, chống-false-green):** BE — `bench run-tests test_imm00` XANH gồm: (1) keys == 6-key allowlist EXACT (assert tập cấm tài chính ∩ keys = ∅); (2) name rỗng/∄ → 404 in-handler no-500/no-traceback; (3) vendor ngoài scope → 403 IDOR no-leak; (4) `device_model_name`/`location_name`/`risk_classification` enrich đúng; (5) `get_asset` no-regression. FE — `vitest` 3 view + `api/imm00` XANH (loader gọi `getAssetActionMeta`, type 6-field, nhãn `'Chưa gán'`, fail-safe null), `vue-tsc` 0. Code-audit — grep `getAsset(` cho panel meta ở 3 view = 0. **Bất kỳ verify HTTP/Playwright/quét-QR-thật → BLOCKED reload (api/imm00.py edit cần gunicorn reload — HARD-STOP USER), KHÔNG đánh DONE.** RED-first. KHÔNG git commit/push/merge/reset DB/bench migrate/reload.

---

### D12 — INPUT NORMALIZATION (nhánh `name`): chuẩn hoá whitespace tham số `name` ở `get_asset_scan_info` — parity nhánh token (factory vòng 31, 2026-06-12 — Self-Correction lỗi thiết kế gốc: bất đối xứng chuẩn-hoá 2 nhánh CÙNG 1 endpoint, BE-only gate)

**Bối cảnh.** D8 (Vòng 6) chốt strip whitespace `qr_token` ở **SSoT service** `resolve_qr_token` → nhánh `token` của `get_asset_scan_info` hưởng parity. NHƯNG nhánh `name` của CÙNG endpoint (`api/imm00.py::get_asset_scan_info` `:574` — `elif name and frappe.db.exists(_DT_ASSET, name)`) tra `name` **THÔ, KHÔNG `.strip()`** (`:568` chỉ coerce-str `name = name if isinstance(name,str) else ""`). Bất đối xứng: cùng 1 endpoint, 2 nhánh chuẩn-hoá khác nhau. `name` đến qua điều hướng nội bộ KHÔNG-QR (deep-link `/assets/:id/info`, copy-paste asset name, caller mobile-BE) → có thể đính space/`\n`; `frappe.db.exists("AC Asset", "  A-042  ")` = False → **false-404 giả**. D8/BR-00-40 §(5) cố ý KHÔNG fork strip-TOKEN lên `name` (đúng — đường khác); D12 đóng việc nhánh `name` tự chuẩn-hoá INPUT của chính nó.

**Quyết định (Accepted — BE-only, 1 dòng).** Trong `get_asset_scan_info`, sau coerce-str `name`, THÊM `name = name.strip()` TRƯỚC nhánh `elif name and frappe.db.exists(_DT_ASSET, name)`. **CHỈ strip leading/trailing** (parity quy tắc token Vòng 6 — KHÔNG lowercase/collapse-inner/transform). Nhánh `name` là đường riêng KHÔNG qua `_svc_resolve_qr_token` → strip đặt tại API handler (KHÔNG ở service token SSoT, KHÔNG fork strip-token). **token-path BẤT ĐỘNG.**

| Acceptance D12 | Đo được |
|---|---|
| **D12-1** name hợp lệ + whitespace 2 đầu → 200 | `get_asset_scan_info(name='  A-042  ')` / `'A-042\n'` / `'\tA-042\t'` → strip → `'A-042'` → `db.exists` khớp → payload A6 (11-field) HTTP-200; hết false-404 |
| **D12-2** whitespace-only → 404 leak-safe, KHÔNG full-scan | `name='   '`/`'\n'` → strip `''` → `elif name and …` short-circuit (`name` falsy → KHÔNG gọi `db.exists`) → 404 `AC-E001`; query-count `db.exists` AC Asset = 0 |
| **D12-3** space GIỮA = name hỏng thật → 404 (KHÔNG over-normalize) | `name='A 042'` → strip 2 đầu VẪN `'A 042'` → không khớp → 404; KHÔNG lowercase/collapse-inner |
| **D12-4** token-path BẤT ĐỘNG | `get_asset_scan_info(token=' <tok> ')` GIỮ parity Vòng 6 qua `_svc_resolve_qr_token`; grep: chỉ 1 `name.strip()` cho nhánh name, KHÔNG đụng SSoT token |
| **D12-5** contract bất biến | payload 11-field A6 GIỮ; RBAC `asset.read`→403 (gate TRƯỚC resolve) GIỮ; 404 no-leak GIỮ; vendor IDOR `assert_vendor_can_access`→403 GIỮ; `assert 'qr_token' not in data` (BR-00-34) GIỮ; read-only no-audit (A2/D4) GIỮ; rate-limit `AC_QR_RESOLVE_RATE_LIMIT=30` (BR-00-29) GIỮ |

**Schema-delta: KHÔNG** (1 dòng strip + test class `TestAssetScanInfoNameWhitespace`). `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`. Map FR/BR: **FR-00-101 + BR-00-50** (02 §IV.26). Cài đặt BE: [04 §II.1.8a-NAMENORM](./04_Backend_Design.md). Contract: [05 §get_asset_scan_info case-table + resolve-order](./05_API_Specification.md). Test: [07 §III.6.l-NAMENORM](./07_Testing_QA.md).

> **DONE-gate spec-contract (đo được, chống-false-green):** BE — `bench run-tests test_imm00` XANH gồm: (1) name + whitespace 2 đầu → 200 payload A6 đúng asset (RED-first: 404 trước fix); (2) whitespace-only → 404 + 0 query `db.exists` (no-full-scan); (3) space-giữa → 404 (no-over-normalize); (4) token-path parity Vòng 6 GIỮ; (5) IDOR 403 / no-audit / no-raw-token / rate-limit no-regression. Grep — đúng 1 `name.strip()` nhánh name, KHÔNG fork strip-token. **Verify chỉ qua `bench run-tests` fresh-import (sửa api/imm00.py live ở run-tests, CHƯA live HTTP); bất kỳ verify HTTP/Playwright/quét-QR-thật → BLOCKED reload gunicorn --preload (HARD-STOP USER) → backlog, KHÔNG đánh DONE.** RED-first. KHÔNG git commit/push/merge/reset DB/bench migrate/reload.

---

### D13 — SCAN-INFO IDENTITY PARITY: bổ sung `manufacturer_sn` (Số serial NSX) vào payload `build_asset_scan_info` + card định danh màn quét — KTV xác nhận ĐÚNG thiết bị vật lý trước khi báo hỏng/tạo WO (factory vòng 37, 2026-06-12 — Self-Correction lỗi thiết kế gốc: đường label-PDF (D5) đã mang serial NSX, đường scan-info SONG SINH thiếu → bất đối xứng định danh truy xuất NĐ98 trên CÙNG 1 thiết bị)

**Bối cảnh (lỗi thiết kế gốc — đào tới source).** D5 (label-PDF) chốt: nhãn QR mang **5 field định danh** gồm **Số serial NSX (`manufacturer_sn`)** — `build_asset_label_data` đã đọc field này (`services/imm00.py:769-782`) làm **định danh truy xuất NĐ98** (codebase đã coi `name/asset_code/manufacturer_sn` là 3 định danh nghiệp vụ hợp lệ, đối lập `qr_token` opaque — `services/imm00.py:95`). NHƯNG đường **scan-info** (`build_asset_scan_info` `:596-625`) — payload màn KTV cầm điện thoại quét tem tại point-of-care — **KHÔNG** trả `manufacturer_sn`. Bất đối xứng trên CÙNG 1 thiết bị: tem in có serial NSX, nhưng màn quét (nơi KTV THỰC SỰ xác nhận đúng máy trước khi báo hỏng / yêu cầu PM/CM/hiệu chuẩn) lại thiếu.

**Hệ quả nghiệp vụ (regulatory consequence — câu hỏi domain #5).** Khi 2 thiết bị cùng model dán nhầm/bong tem, KTV chỉ thấy `asset_code`+tên trên màn quét → KHÔNG đối chiếu được số serial nhà sản xuất khắc trên thân máy ⇒ rủi ro **báo hỏng/tạo WO nhầm thiết bị vật lý** — sai lệch hồ sơ truy xuất nguồn gốc (NĐ98 Điều — UDI/Serial). Serial NSX là **dấu hằng số vật lý** (khắc trên máy, không đổi khi thay tem) ⇒ là field xác nhận-đúng-máy mạnh nhất tại hiện trường.

**5 câu hỏi domain (gate feasibility).** (1) **WHO HTM stage:** Maintenance (point-of-care identity check trước CM/PM/Cal). (2) **NĐ98:** Truy xuất nguồn gốc UDI/Serial — `manufacturer_sn` là định danh truy xuất hợp lệ (D5, KHÔNG phải docname/giá/khấu hao). (3) **Stakeholder:** KTV TBYT (quét QR tại hiện trường). (4) **Lifecycle event:** KHÔNG sinh event — read-only (đồng nhất A2/D4: scan-info KHÔNG emit audit, chống spam chain mỗi lần quét). (5) **Consequence nếu sai:** báo hỏng/WO nhầm thiết bị → hồ sơ truy xuất sai.

**Quyết định (Accepted — BE+FE, đo được).**

**(BE)** `build_asset_scan_info` THÊM key **`manufacturer_sn: str`** vào payload:
- Đọc từ field thật **`AC Asset.manufacturer_sn`** (Data field — verified tồn tại `ac_asset.json:296`) bằng cách THÊM `"manufacturer_sn"` vào **CÙNG 1** `frappe.db.get_value([...], as_dict=True)` ở `:596-600` (cột bổ sung — **KHÔNG** thêm round-trip DB / KHÔNG N+1, parity cách `asset_name` được thêm).
- Coalesce **`row.get("manufacturer_sn") or ""`** ⇒ str — rỗng/null → `''`, **KHÔNG `None`, KHÔNG raw object** (đối xứng tuyệt đối với `asset_code`/`asset_name` `:606-607`).
- Guard **asset rỗng-name → vẫn `None`** (no-regress: early-return `:594` GIỮ NGUYÊN — payload-key chỉ thêm trong nhánh có-row).
- **KHÔNG rò field nhạy cảm mới:** `manufacturer_sn` là định danh truy xuất hợp lệ (D5/NĐ98), KHÔNG phải giá mua/khấu hao/audit chain/supplier code nội bộ/docname. Invariant BR-00-34 (`'qr_token' not in payload`) GIỮ.

**(FE-type)** `frontend/src/api/imm00.ts` interface **`AssetScanInfo`** THÊM field **`manufacturer_sn: string`** (contract parity BE — `getAssetScanInfo` mirror 1-1). Không đổi shape `ScanAction`/`RecentMaintenance`.

**(FE-render)** `AssetScanInfoView.vue` card định danh hiển thị 1 dòng **`Số serial NSX: <value>`** qua **computed `serialText`**:
- Pattern presence-aware parity `modelText`/`locationText` (vòng 22) + `assetCodeText` (vòng 27): trim rồi kiểm presence.
- Có giá trị → render **NGUYÊN VĂN** (no-regress, vd `'SN-12345'`).
- `''` / `null` / `undefined` / **chỉ-whitespace** → nhãn VI SSoT **`'Chưa rõ'`** (hằng mới `SERIAL_UNKNOWN`) — **KHÔNG `'—'` câm**, **KHÔNG leak `'null'`/`'undefined'`**.
- **TUYỆT ĐỐI KHÔNG fallback sang `info.name`** (docname Frappe nội bộ — record-ID thô, có thể hash) — CHỈ `manufacturer_sn || 'Chưa rõ'` (parity no-raw-docname-leak vòng 27/28). No-EN-leak, i18n VI.
- Vị trí render: dòng mới trong card "Model & Vị trí" (cạnh Model/Vị trí) HOẶC card định danh dưới dòng "Mã thiết bị" — đặt cạnh các dòng định danh, `data-test="scan-serial"` để test bám ổn định.

**Nhãn VI SSoT (chốt nguyên văn).**

| Field nguồn | Nhãn dòng VI (card) | Fallback khi rỗng |
|---|---|---|
| `manufacturer_sn` | **Số serial NSX** | **`Chưa rõ`** (hằng `SERIAL_UNKNOWN` — KHÁC `UNASSIGNED='Chưa gán'`/`ASSET_CODE_UNASSIGNED='Chưa gán mã'`; "Chưa rõ" = chưa-biết-giá-trị, đúng ngữ nghĩa serial KTV chưa nhập) |

| Acceptance D13 | Đo được (test — KHÔNG cần HTTP/reload) |
|---|---|
| **D13-1** BE payload có key | `build_asset_scan_info(<asset có manufacturer_sn>)` → `'manufacturer_sn' in payload` ∧ `== <giá trị thật>` (str nguyên văn) |
| **D13-2** BE coalesce rỗng → `''` | asset `manufacturer_sn` null/rỗng → `payload['manufacturer_sn'] == ''` (KHÔNG None, KHÔNG raw object) — đối xứng `asset_code`/`asset_name` |
| **D13-3** BE no extra round-trip | `manufacturer_sn` nằm trong CÙNG `get_value` cột list (`:598-599`); grep: KHÔNG thêm `frappe.db.get_value(_DOCTYPE_ASSET, …, "manufacturer_sn")` riêng (no-N+1) |
| **D13-4** BE no-regress rỗng-name → None | `build_asset_scan_info('')`/`None`/non-str → `None` (early-return `:594` GIỮ); KHÔNG query toàn bảng |
| **D13-5** BE no sensitive leak | payload KHÔNG chứa field tài chính mới; BR-00-34 (`'qr_token' not in payload`) GIỮ; `manufacturer_sn` ⊂ {định danh truy xuất hợp lệ} |
| **D13-6** FE type parity | `AssetScanInfo.manufacturer_sn: string` (bắt buộc, KHÔNG optional); `vue-tsc` 0 lỗi |
| **D13-7** FE render có-giá-trị | mock payload `manufacturer_sn='SN-12345'` → `serialText === 'SN-12345'`; DOM `[data-test="scan-serial"]` text chứa `'SN-12345'` nguyên văn |
| **D13-8** FE fallback rỗng → 'Chưa rõ' | `manufacturer_sn` ∈ {`''`, `null`, `undefined`, `'   '`} → `serialText === 'Chưa rõ'`; KHÔNG `'—'`, KHÔNG `'null'`/`'undefined'` trong DOM |
| **D13-9** FE no-docname-leak | `serialText` KHÔNG fallback `info.name`: payload `manufacturer_sn=''` + `name='AST-0042-x9'` → `serialText === 'Chưa rõ'` (KHÔNG `'AST-0042-x9'`); grep `info.name`/`?? info.value?.name` trong `serialText` = 0 |
| **D13-10** FE no-EN-leak | `serialText` rỗng → chỉ từ VI (`'Chưa rõ'`); KHÔNG mã English/raw-code |

**Schema-delta: KHÔNG DocType/field mới** (`manufacturer_sn` đã là field AC Asset). Payload scan-info **11-field → 12-field** (thêm 1 key str). `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d` (KHÔNG đụng RBAC). RBAC/IDOR/rate-limit/no-audit của `get_asset_scan_info` GIỮ NGUYÊN (chỉ làm giàu payload trong nhánh có-row). Map FR/BR: **FR-00-103 + BR-00-52** (02 §IV.28). Cài đặt BE: [04 §II.1.8d-SCANSN](./04_Backend_Design.md). Contract: [05 §get_asset_scan_info payload 12-field](./05_API_Specification.md). FE: [06 §AssetScanInfoView serialText](./06_Frontend_Design.md). Test: [07 §III.6.m-SCANSN](./07_Testing_QA.md).

> **DONE-gate spec-contract (đo được, chống-false-green):** BE — `bench run-tests test_imm00` XANH gồm: (1) payload có `manufacturer_sn` == giá trị thật (RED-first: KeyError/absent trước fix); (2) rỗng/null → `''` (KHÔNG None); (3) rỗng-name → None no-regress; (4) no extra round-trip (cùng get_value); (5) no sensitive leak + BR-00-34 GIỮ. FE — `vitest AssetScanInfoView` XANH gồm: (6) có-giá-trị → render nguyên văn; (7) `''`/null/undefined/whitespace → `'Chưa rõ'` (RED-first); (8) no-docname-leak (`name` ≠ render khi serial rỗng); (9) no-EN-leak; `vue-tsc` 0; full asset-domain vitest no-regression. Grep — `serialText` KHÔNG `info.name`; đúng 1 hằng `SERIAL_UNKNOWN='Chưa rõ'`. **Verify chỉ qua `bench run-tests` fresh-import + `vitest` (FE-only render KHÔNG cần reload); bất kỳ verify HTTP/Playwright/quét-QR-thật → BLOCKED reload gunicorn --preload (HARD-STOP USER) → backlog, KHÔNG đánh DONE.** RED-first. KHÔNG git commit/push/merge/reset DB/bench migrate/reload/pip install.

---

### D14 — STATUS-PILL A11Y+ANCHOR PARITY: status pill lifecycle ở màn quét QR mang `role="status"` + `aria-label` VI (SSoT `statusLabel`) + `data-test="scan-status"` ổn định — WCAG 1.4.1 parity badge quá hạn + test bám đúng-1-anchor (factory vòng 39, 2026-06-12 — Self-Correction lỗi thiết kế gốc: bất đối xứng a11y CÙNG card định danh + test bám heuristic `rounded-full` mong manh — FE-only gate)

**Bối cảnh (lỗi thiết kế gốc — đào tới source, 2 mặt).** Status pill lifecycle (`AssetScanInfoView.vue:445-450`) render `{{ statusLabel }}` với `:class="statusClass"` — `statusLabel = lifecycleStatusLabel(info.lifecycle_status)`, `statusClass = lifecycleStatusClass(...)`.
- **(a) A11y bất đối xứng — trạng-thái-truyền-tải-CHỈ-bằng-màu.** 2 badge "Quá hạn bảo trì"/"Quá hạn hiệu chuẩn" CÙNG màn đã mang `role="status"` + `aria-label` VI (đạt WCAG 1.4.1 — D9/§II.3c-PMOVERDUE/§II.3d-CALOVERDUE, vòng 27/28). NHƯNG status pill — tín hiệu **trạng-thái cốt-lõi** của card định danh (Đang hoạt động / Đang bảo trì / Đã thanh lý…) — KHÔNG có `role`/`aria-label`: screen-reader đọc text trần không ngữ-cảnh "đây là trạng thái thiết bị", và phân-biệt-trạng-thái dựa CHÍNH vào **màu chip** `statusClass` ⇒ vi phạm WCAG 1.4.1 (Use of Color) cho người mù-màu / dùng screen-reader.
- **(b) Test-anchor mong manh.** Pill thiếu `data-test` ⇒ test bám `findAll('span').find(s => s.classes().includes('rounded-full'))` — NHƯNG `rounded-full` xuất hiện **5 lần** trong view (status pill + badge PM-overdue + badge cal-overdue + chip CTA-urgency "Cần làm ngay" + nút CTA) ⇒ heuristic match NHẦM phần tử (đụng overdue-badge / CTA-chip) → test giòn, false-green/false-red khi layout đổi.

**Hệ quả nghiệp vụ (regulatory consequence — câu hỏi domain #5).** KTV cầm điện thoại quét tem tại point-of-care KHÔNG nhận-diện được trạng thái thiết bị nếu dùng screen-reader / mù-màu (tín hiệu chỉ bằng màu) ⇒ rủi ro thao tác trên thiết bị sai-trạng-thái (vd báo hỏng máy đã thanh lý) — đồng thời regression-guard yếu để bảo vệ chính tín hiệu này.

**5 câu hỏi domain (gate feasibility).** (1) **WHO HTM stage:** Maintenance (point-of-care identity/status check). (2) **NĐ98/WHO HTM:** nhận-diện đúng trạng thái thiết bị tại hiện trường trước thao tác. (3) **Stakeholder:** KTV TBYT / QL vật tư (a11y: người dùng screen-reader / mù-màu). (4) **Lifecycle event:** KHÔNG sinh event — thuần presentation FE (đồng nhất A2/D4 read-only). (5) **Consequence nếu sai:** trạng-thái-chỉ-bằng-màu = WCAG 1.4.1 fail + thao tác sai-trạng-thái.

**Quyết định (Accepted — FE-only, đo được).** THÊM lên CHÍNH `<span>` status pill (`:445-450`) ĐÚNG 3 attr — `statusLabel`/`statusClass`/class màu **GIỮ NGUYÊN byte-for-byte**:
- **`data-test="scan-status"`** — anchor ổn định, đúng-1-pill. Test định-vị QUA `wrapper.get('[data-test="scan-status"]')`, KHÔNG còn heuristic `rounded-full` (đụng 2 overdue-badge + CTA-chip + nút CTA).
- **`role="status"`** — cố định (parity badge quá hạn).
- **`:aria-label="`Trạng thái thiết bị: ${statusLabel}`"`** — **dùng CHUNG `statusLabel`** (SSoT `lifecycleStatusLabel`). KHÔNG rải literal nhãn-trạng-thái, KHÔNG hardcode wording riêng cho aria-label. Wording cố định DUY NHẤT = tiền-tố VI `'Trạng thái thiết bị: '`.

**Bất biến no-leak (parity D10/§II.3e-PILLNOLEAK — vòng 8).** Vì aria-label ghép từ CHÍNH `statusLabel`, mọi bảo đảm no-EN/raw-code/empty của `lifecycleStatusLabel` lan sang aria-label: `lifecycle_status` ∈ {`'In Use'`,`'LegacyUnknown'`,`''`,null,undefined} → `statusLabel === 'Không xác định'` ⇒ CẢ text pill LẪN `aria-label === 'Trạng thái thiết bị: Không xác định'` — **TUYỆT ĐỐI KHÔNG** mã English/code thô trong aria-label.

| Acceptance D14 | Đo được (test — FE-only, KHÔNG cần HTTP/reload/BE-test) |
|---|---|
| **D14-1** Anchor ổn định | `wrapper.get('[data-test="scan-status"]')` tồn tại; text == `statusLabel` (RED-first: selector absent trước thêm attr) |
| **D14-2** `role="status"` | `[data-test="scan-status"]` attr `role === 'status'` |
| **D14-3** aria-label SSoT-shared | `lifecycle_status='Active'` → `aria-label === 'Trạng thái thiết bị: Đang hoạt động'` (== `'Trạng thái thiết bị: ' + statusLabel`); KHÔNG hardcode wording riêng |
| **D14-4** WCAG 1.4.1 non-empty | pill có (text != '' ∧ role=status ∧ aria-label != '') — parity 2 overdue badge ⇒ KHÔNG-chỉ-bằng-màu |
| **D14-5** no-EN/raw-code/empty leak (aria-label) | `lifecycle_status` ∈ {`''`,`'In Use'`,`'LegacyUnknown'`,null,undefined} → text VÀ `aria-label === 'Trạng thái thiết bị: Không xác định'`; aria-label KHÔNG chứa `'In Use'`/`'LegacyUnknown'` |
| **D14-6** class/màu GIỮ NGUYÊN | `statusClass` + class pill byte-for-byte KHÔNG đổi (Active → màu cũ; rỗng/lạ → `bg-gray-100 text-gray-600`); `lifecycleStatusLabel`/`lifecycleStatusClass` logic KHÔNG đổi |
| **D14-7** exactly-one anchor | `wrapper.findAll('[data-test="scan-status"]').length === 1` (CTA buttons + 2 overdue badges + CTA-urgency chip KHÔNG nhận selector) |
| **D14-8** overdue-badge a11y intact (regression) | badge `getByText('Quá hạn bảo trì')`/`('Quá hạn hiệu chuẩn')` vẫn `role=status`+`aria-label` riêng; KHÔNG bị anchor mới ảnh hưởng |

**Schema-delta: KHÔNG** (3 attr template + dùng lại computed `statusLabel`; KHÔNG DocType/field/endpoint/cap mới). FE-only — KHÔNG đụng BE (`build_asset_scan_info`/`resolve_qr_token` GIỮ) / payload shape / `constants/labels.ts` logic / RBAC. `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`. Map FR/BR: **FR-00-104 + BR-00-53** (02 §IV.29). FE: [06 §II.3e-PILLA11Y](./06_Frontend_Design.md). Test: [07 §III.6.n-PILLA11Y](./07_Testing_QA.md).

> **DONE-gate spec-contract (đo được, chống-false-green):** FE-only — `vitest AssetScanInfoView.test.ts` XANH gồm: (1) `[data-test="scan-status"]` tồn tại + text==`statusLabel` (RED-first: selector absent trước thêm attr); (2) `role==='status'`; (3) `aria-label === 'Trạng thái thiết bị: ' + statusLabel` (SSoT-shared, KHÔNG literal cứng); (4) WCAG 1.4.1 non-empty; (5) no-EN/raw-code/empty leak ở aria-label (`it.each(['','In Use','LegacyUnknown',null,undefined])` → `'Trạng thái thiết bị: Không xác định'`); (6) class/màu GIỮ; (7) `findAll('[data-test="scan-status"]').length===1`; (8) overdue-badge a11y intact (regression). `vue-tsc` 0; full asset-domain vitest no-regression. **KHÔNG BE-test / KHÔNG bench migrate / KHÔNG reload** (FE template attr — payload/labels logic KHÔNG đổi). **Verify Playwright/quét-QR-thật → BLOCKED reload gunicorn --preload (HARD-STOP USER) → backlog, KHÔNG đánh DONE live.** RED-first. KHÔNG git commit/push/merge/reset DB/bench migrate/reload/pip install.

---

### D15 — SCAN-RISK URGENCY AFFORDANCE: dòng "Phân loại rủi ro" mang cờ CẢNH BÁO trực quan + a11y khi `risk_classification ∈ {High, Critical}` — derive THUẦN enum-equality từ giá trị server (no client-clock), parity overdue-CTA (vòng 21) + status-pill a11y (vòng 39) (factory vòng 47, 2026-06-12 — Self-Correction lỗi thiết kế gốc: tín hiệu rủi-ro-cao bị render NEUTRAL giống Low/Medium — KTV không phân biệt được mức nguy hiểm tại point-of-care — FE-only gate)

**Bối cảnh (lỗi thiết kế gốc — đào tới source).** Dòng "Phân loại rủi ro" (`AssetScanInfoView.vue:466-473`, vòng 38 / D13-parity) render `{{ riskText }}` với class `text-sm text-slate-500` — **đồng nhất NEUTRAL slate** cho MỌI mức: `Low`('Thấp')/`Medium`('Trung bình') ngang hàng thị-giác với `High`('Cao')/`Critical`('Nghiêm trọng').
- **Tín hiệu rủi-ro-cao bị nuốt.** `risk_classification` là phân-loại an-toàn thiết bị (NĐ98 Class A/B/C/D ↔ Low/Medium/High/Critical — IMM-08 PM frequency, IMM-11 calibration, BR-08-06 photo evidence). Tại point-of-care, KTV quét tem TRƯỚC khi thao tác cần nhận-diện NGAY thiết bị mức C/D (cao/nghiêm trọng — cần thận trọng/redundancy/24h CAPA SLA). Render neutral slate ⇒ thiết bị High/Critical KHÔNG có affordance cảnh báo nào — KTV phải ĐỌC nhãn chữ mới biết, không có tín hiệu thị-giác/a11y ưu tiên.
- **Bất đối xứng affordance CÙNG card.** 2 badge "Quá hạn bảo trì"/"Quá hạn hiệu chuẩn" (vòng 27/28) + CTA-urgency "Cần làm ngay" (vòng 21) CÙNG màn đã mang affordance cảnh báo (màu + `role="status"` + `aria-label`). Dòng rủi-ro-cao — tín hiệu an-toàn cốt-lõi — KHÔNG có ⇒ vi phạm WCAG 1.4.1 (Use of Color, nếu chỉ thêm màu) + thiếu tín hiệu ưu tiên.

**Hệ quả nghiệp vụ (regulatory consequence — câu hỏi domain #5).** KTV cầm điện thoại quét tem thiết bị **Critical** (vd máy thở ICU — Class D) KHÔNG nhận tín hiệu cảnh báo trực quan/a11y ⇒ rủi ro thao tác thiếu thận trọng trên thiết bị an-toàn-cao (NĐ98 phân-loại rủi ro drive PM/calibration/CAPA SLA). Người mù-màu / dùng screen-reader hoàn toàn không nhận-diện được mức nguy hiểm.

**5 câu hỏi domain (gate feasibility).** (1) **WHO HTM stage:** Maintenance (point-of-care risk-class identity check trước thao tác). (2) **NĐ98/WHO HTM:** phân-loại rủi ro thiết bị (Class A/B/C/D) — High/Critical = Class C/D cần thận trọng + calibration bắt buộc + (D) redundancy + 24h CAPA SLA; nhận-diện đúng mức TRƯỚC thao tác. (3) **Stakeholder:** KTV TBYT / QL vật tư (a11y: người mù-màu / screen-reader). (4) **Lifecycle event:** KHÔNG sinh event — thuần presentation FE (read-only, đồng nhất A6/D13/D14). (5) **Consequence nếu sai:** mức-rủi-ro-cao KHÔNG có affordance = thao tác thiếu thận trọng trên thiết bị Class C/D + WCAG 1.4.1 fail.

**Quyết định (Accepted — FE-only, đo được).** GIỮ NGUYÊN computed `riskText` + 3 nhãn-VI cũ (Thấp/Trung bình/Cao/Nghiêm trọng/Chưa phân loại/Khác — no-regress vòng 38/40) + `data-test="scan-risk"` (anchor cũ). THÊM 1 cờ urgency derive THUẦN enum-equality trên giá trị server + 1 phần tử cảnh báo a11y:
- **`riskUrgent` (computed, presentation-only):** `const raw = (info.value?.risk_classification ?? '').trim(); riskUrgent = raw === 'High' || raw === 'Critical'`. **Derive THUẦN enum-equality** trên giá trị-server (parity nguyên-tắc overdue-SSoT vòng 21 — đọc cờ/enum server, KHÔNG so client-clock, KHÔNG nghiệp vụ FE). Tập urgency cố định = **{`High`, `Critical`}** (SSoT hằng `RISK_URGENT_VALUES` để test/grep bám). `Low`/`Medium`/rỗng/whitespace/ngoài-4-enum → `riskUrgent=false` (KHÔNG false-alarm).
- **`role` choice (BA chốt):** **`role="status"`** (KHÔNG `role="alert"`). Lý do: `risk_classification` là **thuộc-tính tĩnh** của thiết bị có-sẵn-khi-load (không phải sự-kiện ngắt). `role="status"`(aria-live polite) parity ĐÚNG với status-pill (vòng 39) + 2 overdue badge (vòng 27/28) cùng card — màn này 100% dùng `role="status"`. `role="alert"`(assertive) sẽ ngắt screen-reader cho thông-tin-steady-state, sai ngữ-cảnh.
- **`aria-label` VI SSoT-shared:** `:aria-label="`${RISK_URGENT_ARIA_PREFIX}${riskText}`"` với hằng VI `RISK_URGENT_ARIA_PREFIX = 'Cảnh báo rủi ro cao: '` ⇒ vd `Critical` → `aria-label === 'Cảnh báo rủi ro cao: Nghiêm trọng'`. **Dùng CHUNG `riskText`** (SSoT, KHÔNG rải literal nhãn-mức, KHÔNG hardcode wording riêng — parity aria-label status-pill vòng 39). Vì ghép từ `riskText`, no-EN-leak lan sang aria-label (KHÔNG bao giờ chứa `High`/`Critical` thô).
- **Affordance thị-giác + nhãn/icon urgency:** khi `riskUrgent` → phần tử cảnh báo (icon ⚠ + nhãn VI urgency `'Rủi ro cao'` — hằng `RISK_URGENT_LABEL`) màu cảnh báo (rose/amber, parity overdue badge) cạnh `riskText`; mang `role="status"` + `aria-label` trên. KHÔNG-chỉ-bằng-màu (có icon + text VI).
- **Anchor test ổn định:** `data-test="scan-risk"` GIỮ NGUYÊN trên dòng (no-regress anchor cũ) + THÊM `data-test="scan-risk-urgent"` trên CHÍNH phần tử cảnh báo (test bám không-heuristic; chỉ render khi `riskUrgent` → `findAll('[data-test="scan-risk-urgent"]').length === (riskUrgent ? 1 : 0)`).

**Bất biến no-false-alarm + no-leak (parity D13/§II.3f-SCANRISK vòng 38).** `riskUrgent` CHỈ true cho {`High`,`Critical`}: `Low`/`Medium` → `riskText` ='Thấp'/'Trung bình', KHÔNG cờ; rỗng/whitespace → 'Chưa phân loại', KHÔNG cờ; ngoài-4-enum (drift/legacy) → 'Khác', KHÔNG cờ (KHÔNG dụ cảnh-báo sai). Nhãn hiển thị `riskText` + `aria-label` đều VI — KHÔNG leak enum EN thô.

| Acceptance D15 | Đo được (test — FE-only, KHÔNG cần HTTP/reload/BE-test) |
|---|---|
| **D15-1** Cờ urgency High/Critical | `risk_classification='High'` HOẶC `'Critical'` → `riskUrgent===true`; `findAll('[data-test="scan-risk-urgent"]').length===1`; phần tử chứa icon + nhãn VI `'Rủi ro cao'` (RED-first: anchor absent trước thêm) |
| **D15-2** No-false-alarm Low/Medium/rỗng/Khác | `risk_classification` ∈ {`'Low'`,`'Medium'`,`''`,null,undefined,`'   '`,`'UNKNOWN_DRIFT'`} → `riskUrgent===false`; `findAll('[data-test="scan-risk-urgent"]').length===0` (KHÔNG cờ) |
| **D15-3** Derive THUẦN enum-equality (no client-clock) | `riskUrgent` = `raw==='High' \|\| raw==='Critical'` (so trên giá-trị server); grep `riskUrgent`/source KHÔNG `Date(`/`Date.now`/`new Date`/so-ngày; tập urgency = hằng `RISK_URGENT_VALUES` SSoT |
| **D15-4** role="status" (BA chốt, KHÔNG alert) | `[data-test="scan-risk-urgent"]` attr `role === 'status'` (KHÔNG `'alert'`) — parity overdue badge + status-pill cùng card |
| **D15-5** aria-label VI SSoT-shared | `risk_classification='Critical'` → `aria-label === 'Cảnh báo rủi ro cao: Nghiêm trọng'` (== `RISK_URGENT_ARIA_PREFIX + riskText`); `'High'` → `'Cảnh báo rủi ro cao: Cao'`; KHÔNG hardcode wording riêng |
| **D15-6** No-EN-leak ở cờ + aria-label | phần tử urgency + aria-label KHÔNG chứa `'High'`/`'Critical'` thô (ghép từ `riskText` VI); nhãn urgency = từ VI `'Rủi ro cao'` |
| **D15-7** `riskText` + nhãn-mức GIỮ NGUYÊN (no-regress vòng 38/40) | dòng `data-test="scan-risk"` vẫn `Phân loại rủi ro: {{ riskText }}`; `'High'`→'Cao'/`'Critical'`→'Nghiêm trọng'/`'Low'`→'Thấp'/`'Medium'`→'Trung bình'/rỗng→'Chưa phân loại'/ngoài-enum→'Khác' byte-for-byte; `RISK_CLASSIFICATION_LABEL`/`riskClassificationLabel` logic KHÔNG đổi |
| **D15-8** Anchor `scan-risk` GIỮ + exactly-one urgent | `findAll('[data-test="scan-risk"]').length===1` (no-regress); urgent-anchor đúng-1 khi true, 0 khi false; KHÔNG đụng overdue badge / status-pill / CTA chip |
| **D15-9** No-WCAG-1.4.1-fail (không-chỉ-bằng-màu) | khi `riskUrgent` → phần tử có (icon ⚠ aria-hidden + text VI `'Rủi ro cao'` ∧ `role=status` ∧ `aria-label != ''`) — parity 2 overdue badge ⇒ KHÔNG truyền tải CHỈ bằng màu |

**Schema-delta: KHÔNG** (1 computed `riskUrgent` + 3 hằng VI `RISK_URGENT_VALUES`/`RISK_URGENT_LABEL`/`RISK_URGENT_ARIA_PREFIX` + 1 phần tử template + test; KHÔNG DocType/field/endpoint/cap/enum mới). **FE-only — KHÔNG đụng BE** (`build_asset_scan_info` GIỮ — đã emit `risk_classification` as str enum coalesce `''`, KHÔNG thêm field/round-trip) / payload shape / `constants/labels.ts` logic (`RISK_CLASSIFICATION_LABEL` GIỮ; hằng urgency có thể đặt ở `labels.ts` HOẶC view-local — BA cho phép `labels.ts` SSoT theo acceptance) / RBAC. `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`. Map FR/BR: **FR-00-105 + BR-00-54** (02 §IV.30). FE: [06 §II.3f-SCANRISKURGENT](./06_Frontend_Design.md). Test: [07 §III.6.o-SCANRISKURGENT](./07_Testing_QA.md).

> **DONE-gate spec-contract (đo được, chống-false-green):** FE-only — `vitest` (`AssetScanInfoView.test.ts` + `assetScanInfoRisk.test.ts`) XANH gồm: (1) High/Critical → `riskUrgent===true` + `[data-test="scan-risk-urgent"]` tồn tại (RED-first: anchor absent trước thêm); (2) Low/Medium/rỗng/null/undefined/whitespace/ngoài-enum → `riskUrgent===false` + 0 urgent-anchor (no-false-alarm); (3) derive thuần enum-equality (grep source KHÔNG `Date`/so-ngày; tập = `RISK_URGENT_VALUES`); (4) `role==='status'` (KHÔNG `'alert'`); (5) `aria-label === 'Cảnh báo rủi ro cao: ' + riskText` (SSoT-shared, KHÔNG literal cứng); (6) no-EN-leak (KHÔNG `'High'`/`'Critical'` thô ở cờ/aria-label); (7) `riskText` + 6 nhãn-mức byte-for-byte GIỮ (no-regress vòng 38/40); (8) `[data-test="scan-risk"]` đúng-1 (no-regress) + urgent-anchor exactly-one khi true; (9) WCAG 1.4.1 non-color-only (icon+text+role+aria-label). `vue-tsc` 0; full asset-domain vitest no-regression (overdue badge / status-pill a11y / serial GIỮ XANH). **KHÔNG BE-test / KHÔNG bench migrate / KHÔNG reload** (FE template + computed — payload/labels logic KHÔNG đổi; BE đã emit `risk_classification`). **Verify Playwright/quét-QR-thật → BLOCKED reload gunicorn --preload (HARD-STOP USER) → backlog, KHÔNG đánh DONE live.** RED-first. KHÔNG git commit/push/merge/reset DB/bench migrate/reload/pip install.

---

### D16 — WARRANTY-CTA AFFORDANCE: 2 CTA đường-sửa (`report_failure`/`request_cm`) mang affordance "NGOÀI BẢO HÀNH" (cảnh báo chi-phí-sửa) khi `warranty_expired === true` ∧ `effectiveEnabled` — derive THUẦN từ cờ server (no client-clock), 2 affordance overdue/warranty ĐỘC LẬP, parity overdue-CTA (vòng 21) + warranty badge (vòng 48) (factory vòng 49, 2026-06-12 — Self-Correction lỗi thiết kế gốc: cờ HẾT BẢO HÀNH chỉ render trên card định danh (vòng 48) NHƯNG KHÔNG bám vào điểm-quyết-định chi-phí — nút "Báo hỏng"/"Yêu cầu sửa chữa" — KTV bấm tạo phiếu KHÔNG được cảnh báo chi-phí-ngoài-bảo-hành tại đúng affordance — FE-only gate)

**Bối cảnh (lỗi thiết kế gốc — đào tới source).** Vòng 48 (`AssetScanInfoView.vue:659-676` / `WARRANTY_EXPIRED_LABEL='Hết bảo hành'` / `warrantyExpired` computed `:297`) đã render **badge "Hết bảo hành"** trên dòng "Bảo hành" của **card định danh** khi `warranty_expired === true`. Đó là tín hiệu *trạng-thái-tài-sản* — đúng chỗ để KTV biết tình trạng bảo hành. **Lỗi thiết kế gốc:** cờ này KHÔNG nối vào **điểm-quyết-định chi-phí** — 2 nút CTA đường-sửa `report_failure` ("Báo hỏng") + `request_cm` ("Yêu cầu sửa chữa") ở cụm "Thao tác nhanh" (`:696-730`). KTV cầm điện thoại quét tem, đọc card, rồi **bấm nút tạo phiếu** — tại affordance bấm-nút đó KHÔNG có cảnh báo nào rằng sửa chữa thiết bị này sẽ **phát sinh chi phí ngoài bảo hành**. Card-badge ở trên, nút ở dưới (khác cụm/khác section) → KTV phải tự nối 2 thông tin; người lướt nhanh tới cụm nút bỏ lỡ badge ⇒ tạo phiếu sửa mà KHÔNG lường giá TRƯỚC.
- **Bất đối xứng affordance CÙNG màn (parity vòng 21).** Cụm CTA ĐÃ có affordance urgency "Cần làm ngay: quá hạn" (vòng 21 / `isOverdueCta` / `URGENT_CTA_HINT`) bám TRỰC TIẾP lên nút `request_pm`/`request_calibration` khi quá hạn. Nhưng affordance đó CHỈ phục vụ trục *lịch* (PM/hiệu chuẩn quá hạn) — KHÔNG phủ trục *chi-phí* (bảo hành). 2 nút đường-sửa (`report_failure`/`request_cm`) — đúng các nút dẫn tới **chi phí sửa chữa** — KHÔNG mang affordance chi-phí nào.
- **Tại sao CHỈ 2 nút đường-sửa, KHÔNG phải PM/Calibration.** Bảo hành liên quan **CHI PHÍ SỬA CHỮA** (CM/báo hỏng dẫn tới sửa) — KHÔNG liên quan PM/hiệu chuẩn (lịch định kỳ, không phải sự-cố-tốn-phí). Map warranty→CTA do đó là tập con CHẶT `{report_failure, request_cm}` — `request_pm`/`request_calibration` KHÔNG bao giờ mang affordance bảo hành (sai ngữ cảnh).

**Hệ quả nghiệp vụ (regulatory consequence — câu hỏi domain #5).** Thiết bị **hết bảo hành** → mọi sửa chữa = chi-phí-bệnh-viện-chịu (không còn NCC bảo hành miễn phí — IMM-09 CM / hợp đồng dịch vụ). KTV tạo phiếu báo hỏng / yêu cầu sửa mà KHÔNG biết thiết bị ngoài bảo hành ⇒ không lường được ngân sách / không kích hoạt quy trình duyệt-chi → phát sinh chi phí ngoài kế hoạch, hoặc chậm xử lý vì phải quay lại xác minh bảo hành. Người mù-màu / screen-reader: nếu affordance chỉ là màu → hoàn toàn không nhận-diện.

**5 câu hỏi domain (gate feasibility).** (1) **WHO HTM stage:** Maintenance (point-of-care: lường chi-phí-sửa TRƯỚC khi tạo CM/báo hỏng). (2) **NĐ98/WHO HTM:** hồ-sơ-thiết-bị / hợp-đồng bảo hành (IMM-05 doc expiry); trạng-thái bảo hành drive quyết-định-chi-phí-sửa (IMM-09 CM). (3) **Stakeholder:** KTV TBYT / QL vật tư (a11y: người mù-màu / screen-reader). (4) **Lifecycle event:** KHÔNG sinh event — thuần presentation FE (read-only, đồng nhất A6/D13/D14/D15). (5) **Consequence nếu sai:** tạo phiếu sửa thiết-bị-ngoài-bảo-hành mà không lường chi phí → chi ngoài kế hoạch / chậm duyệt; hoặc FALSE-ALARM (gắn "ngoài bảo hành" lên thiết-bị-còn-bảo-hành) → KTV nghi-ngờ-sai, dò lại phí không cần thiết.

**Quyết định (Accepted — FE-only, đo được).** GIỮ NGUYÊN cụm CTA (`:696-730`) + `isOverdueCta`/`URGENT_CTA_HINT`/`OVERDUE_ACTION_KEY` (vòng 21 no-regress) + badge card-định-danh `warranty-expired`/`WARRANTY_EXPIRED_LABEL` (vòng 48 no-regress) + computed `warrantyExpired` (`:297` — đọc `info.value?.warranty_expired === true`, ĐÃ CÓ). THÊM 1 map Readonly + 1 predicate + 2 hằng VI + 1 chip template trên nút CTA:
- **Map `WARRANTY_ACTION_KEY` (Readonly, THUẦN-FE presentation, parity `OVERDUE_ACTION_KEY`):** `const WARRANTY_ACTION_KEY: Readonly<Record<string, 'warranty_expired'>> = { report_failure: 'warranty_expired', request_cm: 'warranty_expired' }`. CHỈ 2 key đường-sửa. `request_pm`/`request_calibration` (∉ map) → KHÔNG bao giờ mang affordance bảo hành. **Readonly** (parity `OVERDUE_ACTION_KEY` — map presentation-only, KHÔNG thêm field BE).
- **Predicate `isWarrantyCta(a)` (parity `isOverdueCta`):** `function isWarrantyCta(a: ScanAction): boolean { if (!effectiveEnabled(a)) return false; if (!WARRANTY_ACTION_KEY[a.key]) return false; return warrantyExpired.value }`. **disabled ưu tiên** — `effectiveEnabled(a) === false` (lifecycle/capability/route-lạ) → return false NGAY (KHÔNG dụ KTV bấm nút khoá; parity `isOverdueCta:330`). Đọc cờ qua `warrantyExpired` (=== true server-flag SSoT, vòng 48) — **TUYỆT ĐỐI KHÔNG so `info.warranty_expiry_date` với client clock** ở bất kỳ nhánh nào (memory `overdue_server_flag_ssot`; parity `pmOverdue`/`calibrationOverdue`).
- **2 hằng VI SSoT (no-EN-leak, KHÔNG tái dùng `URGENT_CTA_HINT`):** `WARRANTY_CTA_LABEL = 'Ngoài bảo hành'` (chip text — PHÂN BIỆT ngữ-nghĩa với `URGENT_CTA_HINT='Cần làm ngay'` của overdue; PHÂN BIỆT cả với badge card `WARRANTY_EXPIRED_LABEL='Hết bảo hành'` — chip CTA nói "thao-tác-này-ngoài-bảo-hành/chi-phí", badge card nói "tài-sản-hết-bảo-hành") + `WARRANTY_CTA_ARIA_HINT = 'lưu ý chi phí ngoài bảo hành'` (hậu tố aria-label). **No-EN-leak:** KHÔNG `'Warranty'`/`'Out of warranty'`/`'Expired'`.
- **Affordance thị-giác PHÂN BIỆT overdue (BA chốt — chống đè/nhầm màu).** Chip bảo hành dùng **bảng màu RIÊNG** (vd `rose`/`red` — chi-phí/cảnh-báo-tài-chính) KHÁC chip overdue (`amber` — khẩn-cấp-lịch). 2 chip có thể CÙNG 1 nút *về lý thuyết* nhưng THỰC TẾ KHÔNG trùng (overdue chỉ trên `request_pm`/`request_calibration`; warranty chỉ trên `report_failure`/`request_cm` — 2 tập key RỜI NHAU) → test phải khẳng định **KHÔNG leak chéo** (nút overdue KHÔNG có chip warranty và ngược lại). Icon riêng (vd 💰/🛈) + text VI (KHÔNG chỉ màu — WCAG 1.4.1).
- **a11y `role="status"` + `aria-label` nối hậu tố VI:** chip mang `role="status"` (BA chốt — parity vòng 21/39/47: trạng-thái-tĩnh-có-sẵn-khi-load, KHÔNG `role="alert"`); aria-label nút khi `isWarrantyCta` = ghép `${actionLabel(a)} — ${WARRANTY_CTA_ARIA_HINT}` (vd "Báo hỏng — lưu ý chi phí ngoài bảo hành"). Vì ghép từ `actionLabel` VI SSoT → no-EN-leak.
- **Anchor test ổn định (parity `data-overdue-cta`):** chip mang `data-test="cta-out-of-warranty"` + nút mang `:data-warranty-cta="isWarrantyCta(a) ? a.key : undefined"` (chỉ set khi true — parity `:data-overdue-cta`). Test bám không-heuristic.

**Bất biến no-false-alarm + 2-affordance-độc-lập (parity vòng 21/48).** `isWarrantyCta` CHỈ true khi `warranty_expired === true` ∧ key ∈ {`report_failure`,`request_cm`} ∧ `effectiveEnabled`: `warranty_expired === false`/absent/undefined → KHÔNG chip (no false-alarm, parity nguyên-tắc "chỉ render khi === true" của `warrantyExpired`/`pmOverdue`/`calibrationOverdue`); nút disabled → KHÔNG chip (disabled ưu tiên). 2 affordance overdue (trục lịch) vs warranty (trục chi-phí) **ĐỘC LẬP** — đọc 2 cờ khác nhau (`pmOverdue`/`calibrationOverdue` vs `warrantyExpired`), 2 map key RỜI NHAU, 2 hằng VI riêng, 2 màu riêng → KHÔNG đè/loại nhau, KHÔNG leak chéo.

| Acceptance D16 | Đo được (test — FE-only, KHÔNG cần HTTP/reload/BE-test) |
|---|---|
| **D16-1** Chip warranty trên `report_failure`/`request_cm` khi expired ∧ enabled | `warranty_expired=true` ∧ action `report_failure`/`request_cm` ∧ `effectiveEnabled===true` → `isWarrantyCta===true`; nút có `[data-warranty-cta="<key>"]` + chip `[data-test="cta-out-of-warranty"]` chứa icon + text VI `'Ngoài bảo hành'` (RED-first: anchor absent trước thêm) |
| **D16-2** No-false-alarm khi còn/absent bảo hành | `warranty_expired` ∈ {`false`,absent,undefined} → `isWarrantyCta===false` cho MỌI action; `findAll('[data-test="cta-out-of-warranty"]').length===0` (no false-alarm, parity warrantyExpired/pmOverdue render-chỉ-khi-true) |
| **D16-3** CHỈ 2 key đường-sửa (map THUẦN-FE) | `WARRANTY_ACTION_KEY` Readonly = {`report_failure`,`request_cm`} → `request_pm`/`request_calibration` (kể cả `warranty_expired=true`+enabled) → `isWarrantyCta===false`, 0 chip warranty (sai ngữ cảnh: bảo hành ≠ PM/hiệu chuẩn) |
| **D16-4** disabled ưu tiên (KHÔNG dụ bấm nút khoá) | nút `report_failure`/`request_cm` mà `effectiveEnabled===false` (lifecycle/capability/route-lạ) + `warranty_expired=true` → `isWarrantyCta===false`, KHÔNG chip warranty (parity `isOverdueCta` vòng 21) |
| **D16-5** Derive THUẦN cờ server (no client-clock) | `isWarrantyCta` đọc `warrantyExpired.value` (= `info.warranty_expired === true`, vòng 48); grep `isWarrantyCta`/source KHÔNG `Date(`/`Date.now`/`new Date`/so `warranty_expiry_date` ở MỌI nhánh logic CTA (memory `overdue_server_flag_ssot`) |
| **D16-6** Affordance PHÂN BIỆT overdue (literal + màu + a11y) | chip warranty text=`'Ngoài bảo hành'` (`WARRANTY_CTA_LABEL`, KHÔNG tái dùng `URGENT_CTA_HINT='Cần làm ngay'`); màu RIÊNG (rose/red) ≠ chip overdue (amber); aria-label nút = `${actionLabel(a)} — lưu ý chi phí ngoài bảo hành` (≠ hậu tố overdue `— Cần làm ngay: quá hạn`); `role="status"` |
| **D16-7** 2 affordance ĐỘC LẬP, KHÔNG leak chéo | nút `request_cm` (∉ `OVERDUE_ACTION_KEY` — KHÔNG bao giờ overdue) + `warranty_expired=true` → CHỈ chip warranty, 0 chip overdue; nút `report_failure` + expired → CHỈ chip warranty; nút `request_pm` overdue → CHỈ chip overdue, 0 chip warranty (2 tập key rời nhau ⇒ thực tế không trùng 1 nút, nhưng test khẳng định no-leak-chéo) |
| **D16-8** No-EN-leak ở chip + aria-label | chip + aria-label KHÔNG chứa `'Warranty'`/`'Out of warranty'`/`'Expired'` thô; literal VI SSoT 1 hằng (`WARRANTY_CTA_LABEL`/`WARRANTY_CTA_ARIA_HINT`) |
| **D16-9** No-regression vòng 21 + 48 | `isOverdueCta`/`URGENT_CTA_HINT`/`OVERDUE_ACTION_KEY` + chip "Cần làm ngay" GIỮ byte-for-byte (overdue test XANH); badge card `[data-test="warranty-expired"]`/`WARRANTY_EXPIRED_LABEL='Hết bảo hành'` (vòng 48) GIỮ — `data-warranty-cta`/`cta-out-of-warranty` (cụm CTA) ≠ `warranty-expired` (card); `effectiveEnabled`/`runAction`/`available_actions` render KHÔNG đổi |
| **D16-10** WCAG 1.4.1 non-color-only | khi `isWarrantyCta` → chip có (icon aria-hidden + text VI `'Ngoài bảo hành'` ∧ `role=status` ∧ aria-label nút != '') — parity chip overdue + badge warranty card ⇒ KHÔNG truyền tải CHỈ bằng màu |

**Schema-delta: KHÔNG** (1 map Readonly `WARRANTY_ACTION_KEY` + 1 predicate `isWarrantyCta` + 2 hằng VI `WARRANTY_CTA_LABEL`/`WARRANTY_CTA_ARIA_HINT` + 1 chip template + 2 attr `data-warranty-cta`/`data-test=cta-out-of-warranty` + test; KHÔNG DocType/field/endpoint/cap/enum mới). **FE-only — KHÔNG đụng BE** (`build_asset_scan_info` GIỮ — đã emit `warranty_expired`+`warranty_expiry_date` vòng 48, KHÔNG thêm field/round-trip) / payload shape / `available_actions` contract / `constants/labels.ts` logic / RBAC. `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`. Map FR/BR: **FR-00-106 + BR-00-55** (02 §IV.31). FE: [06 §II.3g-WARRANTYCTA](./06_Frontend_Design.md). Test: [07 §III.6.p-WARRANTYCTA](./07_Testing_QA.md).

> **DONE-gate spec-contract (đo được, chống-false-green):** FE-only — `vitest` (`AssetScanInfoView.test.ts` + nếu tách `assetScanInfoWarrantyCta.test.ts`) XANH gồm: (1) `report_failure`/`request_cm` + `warranty_expired=true` + enabled → `isWarrantyCta===true` + `[data-test="cta-out-of-warranty"]` + `[data-warranty-cta=<key>]` tồn tại (RED-first: anchor absent trước thêm); (2) `warranty_expired` ∈ {false,absent,undefined} → 0 chip warranty (no-false-alarm); (3) `request_pm`/`request_calibration` + `warranty_expired=true` → `isWarrantyCta===false` 0 chip (map CHỈ 2 đường-sửa); (4) disabled (`effectiveEnabled=false`) + expired → 0 chip (disabled ưu tiên); (5) derive thuần cờ server (grep KHÔNG `Date`/so `warranty_expiry_date` trong logic CTA; đọc `warrantyExpired`); (6) chip text=`'Ngoài bảo hành'` (≠ `URGENT_CTA_HINT`) + màu rose/red (≠ amber overdue) + aria-label nút hậu tố `'lưu ý chi phí ngoài bảo hành'` + `role=status`; (7) `request_cm`+expired → CHỈ chip warranty 0 chip overdue; `request_pm` overdue → CHỈ chip overdue 0 chip warranty (no-leak-chéo); (8) no-EN-leak (KHÔNG `'Warranty'`/`'Out of warranty'`/`'Expired'` thô); (9) overdue (vòng 21) + badge warranty card (vòng 48) GIỮ byte-for-byte (no-regress); (10) WCAG 1.4.1 non-color-only (icon+text+role+aria-label). `vue-tsc` 0; full asset-domain vitest no-regression (overdue badge / warranty badge / status-pill a11y / scan-risk / serial GIỮ XANH). **KHÔNG BE-test / KHÔNG bench migrate / KHÔNG reload** (FE template + computed — payload/`available_actions`/labels logic KHÔNG đổi; BE đã emit `warranty_expired` vòng 48). **Verify Playwright/quét-QR-thật → BLOCKED reload gunicorn --preload (HARD-STOP USER) → backlog, KHÔNG đánh DONE live.** RED-first. KHÔNG git commit/push/merge/reset DB/bench migrate/reload/pip install.

---

### D17 — CRASH-SAFE `getdate`: 4 hàm xử-lý-ngày của `build_asset_scan_info` (`_is_warranty_expired`/`_is_pm_overdue`/`_is_calibration_overdue`/`_date_str_or_none`) degrade GRACEFUL trên ngày dị-dạng (legacy/drift) → `None`/`False`, KHÔNG để `getdate` ném `ValidationError` → HTTP-500 traceback-leak ở endpoint quét QR (factory vòng 50, 2026-06-12 — Self-Correction lỗi thiết kế gốc: 4 helper giả-định `getdate` LUÔN parse được — đúng cho input qua Frappe-validated Date field, SAI khi DB chứa chuỗi drift — BE-only gate)

**Bối cảnh (lỗi thiết kế gốc — đào tới source).** 4 hàm xử-lý-ngày trong `services/imm00.py` đều theo khuôn `if not value: return False/None` → `getdate(value)` TRỰC TIẾP (F21):
- `_is_warranty_expired(value)` (`:385-402`) → `getdate(value) < getdate(nowdate())`
- `_is_pm_overdue(next_pm_date, status)` (`:350-363`) → `getdate(next_pm_date) < getdate(nowdate())`
- `_is_calibration_overdue(next_calibration_date, status)` (`:366-382`) → `getdate(next_calibration_date) < getdate(nowdate())`
- `_date_str_or_none(value)` (`:405-414`) → `getdate(value).strftime('%Y-%m-%d')`

Cả 4 nhận giá trị từ `row.get(...)` (= `frappe.db.get_value` trên 3 Date col `warranty_expiry_date`/`next_pm_date`/`next_calibration_date`). **Giả-định ngầm:** Date col luôn chứa `date` object hợp lệ HOẶC NULL → `getdate` luôn thành công. Giả-định này **SAI** cho dữ liệu drift: `getdate('not-a-date')`/`getdate('2020-13-45')` **NÉM** `frappe.exceptions.ValidationError` (F20). Chuỗi drift lọt vào Date col qua **raw SQL** (`UPDATE ... SET warranty_expiry_date='...'` — MariaDB chấp nhận syntax) / **legacy import bypass** Frappe-validate / **migration cũ**. 1 record drift như vậy → `getdate` raise → `build_asset_scan_info` raise → endpoint `get_asset_scan_info` trả **HTTP-500 + traceback-leak** thay vì payload ⇒ **cả màn quét QR sập** vì 1 field xấu của 1 thiết bị.

- **Tại sao đây là lỗi nghiệp vụ, KHÔNG phải lỗi-lập-trình:** ngày dị-dạng = *dữ-liệu-xấu-có-thật* ở hiện trường (drift/legacy), KHÔNG phải bug logic. Đúng-đắn nghiệp vụ: "date không-xác-định" ⟹ "KHÔNG biết hết hạn/quá hạn hay chưa" ⟹ degrade về `False` (no-false-alarm — KHÔNG bịa cờ quá hạn) + `None` (KHÔNG leak chuỗi rác verbatim). KTV vẫn quét được tem, thấy 13 field còn lại, chỉ field-ngày-xấu hiển thị "Chưa lên lịch"/không-cờ. So sánh với crash: 1 record drift làm KTV KHÔNG quét được máy đó tại điểm-chăm-sóc → chặn nghiệp vụ.
- **Parity FE đã có (đối xứng BE).** FE `formatIsoDateLabel` (vòng 18-19) đã ISO-strict: chuỗi non-ISO/drift → nhãn VI an toàn, KHÔNG `new Date('not-a-date')` → `Invalid Date`. BE nay đối xứng: `_date_str_or_none('not-a-date')` → `None` (KHÔNG leak verbatim, KHÔNG mis-parse câm, KHÔNG crash). Hai đầu cùng coi chuỗi-không-parse-được là "không-xác-định".

**Hệ quả nghiệp vụ (regulatory consequence — câu hỏi domain #5).** Endpoint quét QR là **point-of-care** (KTV cầm điện thoại quét tem ở hiện trường để báo hỏng/tạo WO). HTTP-500 traceback-leak: (a) **chặn nghiệp vụ** — KTV không quét được thiết bị có record drift → không báo hỏng được tại chỗ; (b) **rò rỉ** — traceback Python ở response phơi đường-dẫn-file/cấu-trúc-nội-bộ (LL-BE-42 — lỗi nghiệp vụ KHÔNG được leak traceback); (c) **NĐ98 truy-xuất** — 1 thiết bị "biến mất" khỏi luồng quét vì 1 ngày-bảo-hành-xấu là gãy traceability tại điểm sử dụng. Degrade graceful giữ thiết bị quét-được + audit-trail liên tục.

**5 câu hỏi domain (gate feasibility).** (1) **WHO HTM stage:** Cross-cutting (IMM-00 foundation — robustness của endpoint đọc payload thiết bị tại điểm-chăm-sóc Maintenance). (2) **NĐ98/WHO HTM:** truy-xuất-nguồn-gốc tại điểm sử dụng (Article 67) — thiết bị PHẢI quét-được dù dữ-liệu-ngày drift; hồ-sơ-thiết-bị (IMM-05) có thể chứa ngày legacy chưa-chuẩn-hoá. (3) **Stakeholder:** KTV TBYT (quét tem báo hỏng) — người chịu hậu quả trực tiếp nếu màn quét sập. (4) **Lifecycle event:** KHÔNG sinh event — thuần read-only robustness (đồng nhất A6 KHÔNG-audit khi quét). (5) **Consequence nếu sai:** giữ nguyên crash → chặn nghiệp vụ + leak; HOẶC catch-all quá rộng → nuốt LUÔN lỗi-nghiệp-vụ-thật khác (vd lỗi RBAC/IDOR) → debug mù.

**Quyết định (Accepted — BE-only, đo được).** Bọc lời gọi `getdate` trong 4 helper bằng guard CHỈ-nuốt-parse-error (KHÔNG catch-all). Khuôn chung — 1 helper nội bộ SSoT (BA khuyến nghị, KHÔNG ép cấu-trúc — BE chọn cách hiện thực miễn đạt acceptance):
- **Khuyến nghị: 1 hàm `_safe_getdate(value)` SSoT** trả `date | None` — `try: return getdate(value) except (frappe.exceptions.ValidationError, ValueError, TypeError): return None`. 4 helper gọi `_safe_getdate` thay `getdate` trên giá-trị-từ-DB; nếu `None` → coi như "không-xác-định" (`_is_*` → `False`, `_date_str_or_none` → `None`). **KHÔNG rải `try/except` lặp 4 chỗ** (1 SSoT — parity `_str_or_blank`/`_date_str_or_none`). *Lưu ý:* `getdate(nowdate())` (vế "hôm nay") KHÔNG cần bọc — `nowdate()` luôn trả ISO hợp lệ; CHỈ bọc vế giá-trị-từ-DB.
- **Tập exception CHÍNH XÁC (no-mask-real-bug — F20).** `frappe.exceptions.ValidationError` (cái `getdate` THỰC SỰ ném — BẮT BUỘC liệt kê, vì MRO KHÔNG subclass `ValueError`) + `ValueError` + `TypeError` (phòng-thủ: input bypass Frappe → Python `datetime`/`str` raise các loại này). **TUYỆT ĐỐI KHÔNG** `except Exception`/`except:` trần — sẽ nuốt lỗi RBAC/IDOR/DB/logic-nghiệp-vụ khác (red flag "catch-all che lỗi nghiệp vụ"). Guard CHỈ phủ **parse-date**.
- **Comment giải-thích-vì-sao BẮT BUỘC.** Tại `_safe_getdate` (hoặc mỗi guard) PHẢI có comment nêu: degrade an-toàn vì *drift/legacy import* (date col chứa chuỗi rác qua raw SQL/bypass-validate) — 1 record xấu KHÔNG được chặn cả màn quét QR; "không-xác-định ≠ hết hạn/quá hạn" (no-false-alarm); chỉ nuốt parse-error KHÔNG catch-all.
- **Degrade-trong-payload độc-lập-từng-field (BE-WAR-EDGE-4).** `build_asset_scan_info` (`:637-768`) KHÔNG đổi cấu-trúc: 3 lời gọi `_date_str_or_none` + 1 `_is_warranty_expired` + 2 `_is_*overdue` GIỮ vị-trí. Khi 1 field-ngày drift → CHỈ field đó degrade (`warranty_expiry_date=None`/`warranty_expired=False` hoặc `next_pm_date=None`/`pm_overdue=False`), **13 field còn lại GIỮ NGUYÊN** (name/asset_code/asset_name/manufacturer_sn/risk_classification/lifecycle_status/device_model_name/location_name/recent_maintenance/available_actions + 2 field-ngày-khác hợp lệ). KHÔNG `try/except` quanh CẢ dict-build (sẽ nuốt nhầm field khác) — guard ở TỪNG helper ⇒ cô-lập field xấu.

**Bất biến no-regress + no-mask (parity vòng 48 + LL-BE-42/49).** Mọi giá-trị HỢP LỆ (`date` object từ DB / ISO `'YYYY-MM-DD'` / `None` / `''`) GIỮ NGUYÊN hành vi cũ: quá-khứ→`True`, hôm-nay→`False` (STRICT `<`), tương-lai→`False`, `None`/`''`→`False` (guard `if not value` chạy TRƯỚC, KHÔNG chạm `getdate`); `_date_str_or_none(date-obj)`→`'YYYY-MM-DD'`, `_date_str_or_none(None/'')`→`None`. Guard CHỈ kích hoạt trên nhánh `getdate` **raise** (chuỗi không-parse) — KHÔNG đổi nhánh thành-công. Toàn bộ `TestWarrantyExpiredHelper` (BE-WAR-1..5) + `TestWarrantyInScanInfo` (BE-WAR-6..8) + 3 trường-ngày scan-info cũ XANH byte-for-byte.

| Acceptance D17 | Đo được (test — BE-only, fresh-import, KHÔNG cần reload) |
|---|---|
| **D17-1 (BE-WAR-EDGE-1)** warranty parse-fail → `False` no-raise | `_is_warranty_expired('not-a-date')` / `_is_warranty_expired('2020-13-45')` → `False` (assert KHÔNG `frappe.exceptions.ValidationError` — `assertRaises`-negative HOẶC bọc try). "không-xác-định ≠ hết hạn" (no-false-alarm). |
| **D17-2 (BE-WAR-EDGE-2)** overdue parse-fail → `False` no-raise (parity warranty) | `_is_pm_overdue('garbage', None)` + `_is_calibration_overdue('2020-99-99', None)` → `False` (KHÔNG raise). Date dị-dạng KHÔNG bịa cờ quá hạn. |
| **D17-3 (BE-WAR-EDGE-3)** `_date_str_or_none` parse-fail → `None` no-raise | `_date_str_or_none('not-a-date')` → `None` (KHÔNG raise). Chuỗi phi-ISO/drift KHÔNG leak verbatim, KHÔNG mis-parse câm, KHÔNG crash (parity FE `formatIsoDateLabel` ISO-strict vòng 18-19). |
| **D17-4 (BE-WAR-EDGE-4, assert-chính, integration)** payload degrade field-độc-lập | `build_asset_scan_info` trên 1 AC Asset có `warranty_expiry_date` (hoặc `next_pm_date`/`next_calibration_date`) là chuỗi dị-dạng → **KHÔNG ném exception**; trả payload đầy-đủ với field-lỗi degrade (`warranty_expired=False`/`pm_overdue=False`/`warranty_expiry_date=None`) + **13 field còn lại GIỮ NGUYÊN** (assert key-set == 16-key đầy đủ; tài-chính ∩ keys = ∅). KHÔNG HTTP-500 traceback-leak. *(Test-design: inject drift qua `frappe.db.sql UPDATE` raw HOẶC monkeypatch `frappe.db.get_value` trả row có chuỗi xấu — BE chọn; KHÔNG dùng `get_doc().insert()` vì Frappe sẽ chặn Date-validate.)* |
| **D17-5 (BE-WAR-EDGE-5, no-regress)** giá-trị hợp-lệ GIỮ hành vi cũ | `date` object từ DB / ISO `'YYYY-MM-DD'` / `None` / `''`: quá-khứ→`True`, hôm-nay→`False` (STRICT `<`), tương-lai→`False`, `None`/`''`→`False`; `_date_str_or_none(date-obj)`→`'YYYY-MM-DD'`, `_date_str_or_none(None/'')`→`None`. **Toàn bộ `TestWarrantyExpiredHelper` BE-WAR-1..5 + `TestWarrantyInScanInfo` BE-WAR-6..8 + 3 trường-ngày scan-info cũ XANH** byte-for-byte. |
| **D17-6 (BE-WAR-EDGE-6, no-mask-real-bug)** guard CHỈ nuốt parse-error | guard catch CHỈ `frappe.exceptions.ValidationError` + `ValueError` + `TypeError` (KHÔNG `except Exception`/`except:` trần). Đo: grep `except` trong 4 helper / `_safe_getdate` KHÔNG có catch-all; (tùy) test inject 1 exception KHÁC parse (vd `KeyError`/`RuntimeError`) vào path getdate → VẪN propagate (KHÔNG bị nuốt). Comment giải-thích degrade-an-toàn (drift/legacy) tồn tại tại source. |

**Schema-delta: KHÔNG** (1 helper nội bộ `_safe_getdate` khuyến nghị + guard `try/except` parse-error trong 4 hàm hiện có + comment + test; KHÔNG DocType/field/endpoint/cap/enum/payload-key mới). **BE-only — KHÔNG đụng FE** (payload shape/key GIỮ y nguyên — `warranty_expiry_date`/`warranty_expired`/`next_pm_date`/`pm_overdue`/... vẫn đúng type `str|None`/`bool`; FE đọc cờ server như cũ) / `available_actions` contract / RBAC / `CAP_SET_VERSION` (GIỮ `v97.c30c69b8974d`). Map FR/BR: **FR-00-107 + BR-00-56** (02 §IV.32). BE: [04 §II.1.8e-SAFEDATE](./04_Backend_Design.md). Test: [07 §III.6.q-SAFEDATE](./07_Testing_QA.md).

> **DONE-gate spec-contract (đo được, chống-false-green):** BE-only — `bench --site miyano run-tests --module assetcore.tests.test_imm00` XANH (fresh-import, KHÔNG cần reload — `.py` service edit, test import trực tiếp module) gồm: (1) `_is_warranty_expired('not-a-date'/'2020-13-45')`→`False` no-raise; (2) `_is_pm_overdue('garbage',None)` + `_is_calibration_overdue('2020-99-99',None)`→`False` no-raise; (3) `_date_str_or_none('not-a-date')`→`None` no-raise; (4) **integration assert-chính:** `build_asset_scan_info` trên asset có 1 field-ngày drift → KHÔNG raise, payload 16-key đầy-đủ, field-lỗi degrade `None`/`False`, 13 field còn lại GIỮ, KHÔNG financial-leak; (5) no-regress: `TestWarrantyExpiredHelper` BE-WAR-1..5 + `TestWarrantyInScanInfo` BE-WAR-6..8 + 3 trường-ngày scan-info cũ XANH byte-for-byte (date-obj/ISO/None/'' giữ hành vi); (6) no-mask: guard CHỈ `ValidationError`/`ValueError`/`TypeError` — grep KHÔNG `except Exception`/`except:` trần trong 4 helper/`_safe_getdate`; comment degrade-an-toàn tồn tại. **TDD RED-first** (viết test 6 case TRƯỚC, confirm RED do `getdate` raise/`build_asset_scan_info` raise, rồi GREEN sau guard). **KHÔNG marker-trust** — chạy `bench run-tests` THẬT, đọc output OK count. **KHÔNG cần gunicorn reload** (service `.py`, test fresh-import; KHÁC `api/imm00.py` reload-gated). KHÔNG git commit/push/merge/reset DB/bench migrate/reload/pip install.

---

## Bàn giao Core Doc — task Vòng 2+ map tới đúng 1 quyết định

> Gate code: ADR chốt → Vòng 2+ thực thi. **KHÔNG đụng `.py/.vue/.ts` ở Vòng 1.**

| Task (BE/FE/QA) | Map | Mô tả delta |
|---|---|---|
| **BE-1** | D1/D2 | `build_asset_scan_info` + emit `available_actions` qua `_scan_action_specs()` + `_lifecycle_allows()` (1 predicate SSoT, bảng lifecycle×action D2). 4 phần tử {key,label,route,enabled,reason}. |
| **BE-2** | D5 | `build_asset_label_data`(+`_batch`) thêm `manufacturer_sn`+`asset_name` vào columns + dict return (5 field hiển thị). KHÔNG đổi qr_url/token. |
| **BE-3** ✅ | D6 | **DONE Vòng 3.** Thêm cap `asset.print`→(AC Asset,print) + `asset.qr.rotate`→(AC Asset,write) vào `CAPABILITY_MAP`; đổi 4 gate (`get_asset_label_data[_batch]`/`mark_label_printed`→`asset.print`, `regenerate_asset_qr_token`→`asset.qr.rotate`); CAP_SET_VERSION v95.3388ee5629c1→v97.c30c69b8974d; `after_migrate→invalidate_capabilities()` self-heal. |
| **BE-4** | D4 | (Nếu backfill sót) re-run `ensure_asset_qr_token` 1-shot cho asset legacy thiếu token (idempotent). *(Cần khảo sát)* count qr_token rỗng trước. |
| **FE-1** | D1/D2/F19 | `AssetScanInfoView.vue`: render `available_actions` (BE-driven, KHÔNG tự tính quyền FE) — nút enabled→điều hướng `router.push({name, query:{asset, source:'qr-scan'}})`, disabled+tooltip=`reason` (KHÔNG render nút chết, KHÔNG ẩn). **GIỮ phần info read-only** (F19 — chỉ THÊM cụm nút, KHÔNG biến info thành editable). Nhãn nút lấy từ `available_actions[].label` (BE đã VI) — KHÔNG hardcode nhãn FE; a11y `aria-disabled` + `role=status`/`aria-live` cho reason. |
| **FE-1b** | F18 | `labels.ts`: thêm SSoT `SCAN_ACTION_LABELS` (`report_failure`→'Báo hỏng', `request_pm`→'Yêu cầu bảo trì', `request_cm`→'Yêu cầu sửa chữa', `request_calibration`→'Hiệu chuẩn') làm fallback/parity nếu FE cần map theo `key`; BE đã emit `label` VI (D2) ⇒ FE ưu tiên `label` BE, dùng SSoT làm guard-test parity. KHÔNG hardcode nhãn rải rác .vue. |
| **FE-2** | D1 | `router/index.ts`: đổi `requiredCapabilities` 3 route (PM/CM/Calibration) `.write`→`.create` để route-guard = available_actions = svc (1 cap/action). |
| **FE-3** | D3/F8 | **PM view (gap chính):** `PMWorkOrderCreateView` thêm `useRoute`+đọc `route.query.asset`→prefill `form.asset_ref`+khoá. **3 view kia (Incident/CM/Calibration) ĐÃ prefill** (F8) → CHỈ thêm **khoá** field asset + đọc `route.query.source==='qr-scan'` cờ nguồn (gap phụ). URL dựng qua `router.push/resolve({name, query})` ở FE-1 (KHÔNG token). |
| **FE-4** | D5 | Print preview: layout 5 field + QR; 3 preset khổ tem (50×30/70×40mm/A4-grid) + `@page size`; nhãn VI nguyên văn. |
| **QA-1** | D2 | Test `available_actions` per lifecycle×capability (Decommissioned→4 disabled reason="Thiết bị đã thanh lý"; Out of Service→report/cm enabled, pm/cal disabled; Active+full-cap→4 enabled; thiếu-cap→reason quyền). |
| **QA-2** | D1 | Test tương đẳng 3-tầng: route `meta.requiredCapabilities` == `available_actions[key].capability` == svc `rbac.require` cho cả 4 action. |
| **QA-3** | D3 | Playwright: bấm nút từ màn quét → URL `?asset=<name>&source=qr-scan` (KHÔNG token); form đích field asset khoá + đúng asset. grep qr_token trong URL action = 0. |
| **QA-4** | D4 | Test token tự sinh qua 3 path (form/import/registration); count qr_token rỗng = 0 sau backfill. |
| **QA-5** | D5 | Test `build_asset_label_data` keys ⊇ {manufacturer_sn, asset_name, asset_code, device_model_name, qr_url}; preview render 5 field; QR quét được. |
| **QA-6** ✅ | D6 | **DONE Vòng 3.** `TestLabelWriteCapability`: print-user (write=0)→in 200, no-print→403+0-side-effect; `TestRegenerateQrToken`: print-only→rotate 403, write→rotate 200; version-guard v97.c30c69b8974d; IDOR bất biến. FE: `assetDetailQrPrint/Regenerate/RbacAffordance` + route `asset.print`. |
| **BE-5** | D8 | **factory vòng 6 (token whitespace norm).** `services/imm00.py::resolve_qr_token` thêm `token = token.strip() if isinstance(token,str) else ""` đầu hàm (TRƯỚC guard rỗng + query). `get_asset_scan_info` kế thừa qua `_svc_resolve_qr_token` (KHÔNG fork). +class test `TestResolveQrTokenWhitespace` (token+space/`\n` resolve đúng; whitespace-only→query-count=0; whitespace-no-match→404 no-500). 0 schema/cap/FE delta. |
| **QA-7** | D8 | Test `resolve_qr_token(' <tok> ')`/`'<tok>\n'` → 200 đúng asset (2 endpoint parity); whitespace-only → 404 + assert query-count=0 (đo SQL trên path rỗng-sau-strip); whitespace-no-match → 404 leak-safe no-500/417; grep chỉ 1 `.strip()` token-resolve SSoT. `TestResolveQrToken`/`TestAssetScanInfo`/label-pdf GREEN. |
| **BE-6** | D9 | **factory vòng 7 (reason-non-empty).** `services/imm00.py`: thêm hằng `_LIFECYCLE_REASON_UNKNOWN = "Thiết bị không ở trạng thái cho phép thao tác này"` cạnh 3 hằng `_LIFECYCLE_REASON_*`; trong `_build_available_actions` đổi nhánh disabled-reason thành 3 bậc `_lifecycle_reason(...) or (cap thiếu→_CAPABILITY_REASON) or _LIFECYCLE_REASON_UNKNOWN`. KHÔNG đụng `_lifecycle_reason`/`_lifecycle_allows`/shape/cap-version. +bổ sung `TestScanInfoAvailableActions`: unknown/empty status + cap → `_LIFECYCLE_REASON_UNKNOWN`; bất biến `enabled=False ⟹ reason!=""` quét 7 status × 4 action; siết `test_unknown_status_safe_default` assert `reason != ""` (RED trước fix). |
| **FE-5** | D9 | `AssetScanInfoView.vue`: làm cụm reason + aria-describedby phòng-thủ — `<li v-for>` lọc theo `!x.enabled` (BỎ `&& x.reason`, dựa BE đảm bảo non-rỗng) HOẶC set `:aria-describedby` chỉ khi `reason` non-rỗng → hết dangling. `:title`/`aria-label` đã = reason (BE non-rỗng) ⇒ hết trailing rỗng. KHÔNG bịa chuỗi FE (SSoT BE). +test `AssetScanInfoView.test.ts`: payload disabled+reason non-rỗng → `title`/`aria-describedby` trỏ `<li>` tồn tại + `aria-label` kết thúc bằng reason thực. |
| **QA-8** | D9 | BE: `bench run-tests test_imm00` GREEN (gồm test mới reason-non-empty + unknown→UNKNOWN). FE: `vitest AssetScanInfoView` GREEN. Đo bất biến `enabled=False ⟹ reason!=""` qua payload mọi status; grep no-EN-leak (reason 100% VI); 5 status đã biết reason byte-for-byte (so chuỗi cũ). KHÔNG tuyên bố verify HTTP live (BE .py edit reload-pending — 🔴 STATE#1). |
| **FE-6** | D10 | **factory vòng 8 (status-pill no-EN-leak), FE-only.** `frontend/src/constants/labels.ts`: thêm hằng `LIFECYCLE_STATUS_UNKNOWN_LABEL = 'Không xác định'`; đổi `lifecycleStatusLabel(v)` fallback `?? v` → `?? LIFECYCLE_STATUS_UNKNOWN_LABEL`. `lifecycleStatusClass` GIỮ NGUYÊN (verify fallback gray). +`labels.test.ts`: case mã-lạ (`'In Use'`/`'Retired'`/`'active'`) + rỗng (`''`/null/undefined) → `'Không xác định'` no-raw-code (RED-first — fail trước fix do `?? v`); 7 canonical FROZEN nhãn+màu giữ xanh. +`AssetScanInfoView.test.ts`: payload `lifecycle_status` rỗng/lạ → text pill = `'Không xác định'`, snapshot/text KHÔNG mã English. KHÔNG đụng BE. |
| **QA-9** | D10 | FE-only. `vitest` (`labels.test.ts` + `AssetScanInfoView.test.ts`) GREEN (gồm case mã-lạ/rỗng RED-first); `vue-tsc` 0 lỗi; full asset-domain vitest suite no-regression. Đo: `lifecycleStatusLabel('')==='Không xác định'`, `lifecycleStatusLabel('In Use')!=='In Use'`, pill rỗng/lạ no-raw-code; grep no `[A-Za-z]{2,}` mã English trên pill (ngoài từ VI có dấu); 7 canonical byte-for-byte. KHÔNG cần reload/migrate (FE-only — BE KHÔNG đổi). |
| **BE-7** | D11 | **factory vòng 25 (lean action-meta — bịt over-fetch tài chính).** `api/imm00.py`: thêm `@frappe.whitelist()` `get_asset_action_meta(name: str = "")` — name rỗng/∄→`_err(_ERR_ASSET_NOT_FOUND,404)`; `assert_vendor_can_access(_DT_ASSET,name)` (IDOR 403 no-leak); build qua `frappe.db.get_value(_DT_ASSET, name, [name,asset_name,device_model,lifecycle_status,risk_classification,location], as_dict=True)` → enrich `device_model_name`(IMM Device Model.model_name)+`location_name`(AC Location.location_name), drop raw Link → `_ok({6-key allowlist})`. KHÔNG `as_dict()`, KHÔNG field tài chính/qr_token. `get_asset` GIỮ NGUYÊN. KHÔNG cap/schema/version delta. +class test `TestAssetActionMeta`: keys==6-key EXACT (cấm-financial ∩ keys=∅); name rỗng/∄→404 no-500; vendor-out-scope→403 no-leak; enrich đúng; `get_asset` no-regression. RED-first `bench run-tests test_imm00`. |
| **FE-7** | D11 | **factory vòng 25 (migrate 3 view sang action-meta).** `api/imm00.ts`: thêm `getAssetActionMeta(name): Promise<AssetActionMeta>` + interface `AssetActionMeta` CHỈ 6 field (KHÔNG `extends AcAsset`). `getAsset`/`AcAsset` GIỮ (admin-detail). 3 view CM(`:12,128`)/Calibration(`:6,92`)/PM(`:7,131`): đổi import + loader `getAsset`→`getAssetActionMeta` cho panel meta. Nhãn rỗng VI: CM `:331`/Cal `:226` risk rỗng `'—'`→`'Chưa gán'` (parity Vòng 22); các dòng meta rỗng khác dùng nhãn VI an toàn (KHÔNG `'—'` câm). Fail-safe (403/404/network→`assetMeta=null` panel ẩn) + lock SmartSelect + zero-schedule GIỮ. Cập nhật `*.assetMeta.test.ts` 3 view: mock `getAssetActionMeta`, assert loader gọi nó (KHÔNG `getAsset`), risk rỗng→`'Chưa gán'`. `vue-tsc` 0. |
| **QA-10** | D11 | BE: `bench run-tests test_imm00` GREEN (`TestAssetActionMeta`: 6-key EXACT + cấm-financial ∩=∅ + 404 in-handler no-500 + 403 IDOR no-leak + enrich + get_asset no-regress). FE: `vitest` 3 `*.assetMeta.test.ts` + `api/imm00` GREEN (loader gọi `getAssetActionMeta`, type 6-field, `'Chưa gán'`, fail-safe null), `vue-tsc` 0. Code-audit: grep `getAsset(` cho panel meta 3 view = 0; grep field tài chính trong `AssetActionMeta` type = 0. **Verify HTTP/Playwright/quét-QR-thật BLOCKED reload (HARD-STOP USER) — KHÔNG đánh DONE.** |
| **BE-8** | D13 | **factory vòng 37 (scan-info serial NSX parity).** `services/imm00.py::build_asset_scan_info`: THÊM `"manufacturer_sn"` vào cột list của `frappe.db.get_value(...as_dict=True)` (`:598-599` — CÙNG query, no extra round-trip) + key payload `"manufacturer_sn": row.get("manufacturer_sn") or ""` (`:606`-cụm, coalesce `''` parity asset_code/asset_name). Early-return rỗng-name→None (`:594`) GIỮ. KHÔNG đụng RBAC/IDOR/rate-limit/no-audit/`CAP_SET_VERSION`/shape khác. +class test `TestScanInfoManufacturerSn`: payload có key==giá-trị-thật (RED-first absent); rỗng/null→`''` (KHÔNG None); rỗng-name→None; no extra get_value (grep); BR-00-34 `'qr_token' not in payload` GIỮ. |
| **FE-8** | D13 | **factory vòng 37 (type + card serialText).** `api/imm00.ts`: interface `AssetScanInfo` THÊM `manufacturer_sn: string` (bắt buộc, parity BE). `AssetScanInfoView.vue`: thêm hằng `SERIAL_UNKNOWN = 'Chưa rõ'` + computed `serialText = (info.value?.manufacturer_sn ?? '').trim() || SERIAL_UNKNOWN` (TUYỆT ĐỐI KHÔNG `info.name`); render dòng `Số serial NSX: {{ serialText }}` (`data-test="scan-serial"`) trong card định danh/`Model & Vị trí`. +`AssetScanInfoView.test.ts`: serial có-giá-trị→nguyên văn; `''`/null/undefined/whitespace→`'Chưa rõ'` (RED-first); name set + serial rỗng → render `'Chưa rõ'` KHÔNG docname; no-EN-leak. `vue-tsc` 0. |
| **QA-11** | D13 | BE: `bench run-tests test_imm00` GREEN (`TestScanInfoManufacturerSn` D13-1..5; `TestAssetScanInfo` no-regression). FE: `vitest AssetScanInfoView` GREEN (D13-6..10 RED-first), `vue-tsc` 0, full asset-domain vitest no-regression. Đo: payload-key str∀case; `serialText` fallback `'Chưa rõ'` no-`'—'`/no-docname/no-EN; grep `serialText` KHÔNG `info.name` + đúng 1 hằng `SERIAL_UNKNOWN`. **Verify HTTP/Playwright/quét-QR-thật BLOCKED reload gunicorn --preload (HARD-STOP USER) → backlog, KHÔNG đánh DONE.** |
| **FE-9** | D15 | **factory vòng 47 (scan-risk urgency affordance + a11y, FE-only).** `AssetScanInfoView.vue`: THÊM computed `riskUrgent` = `const raw=(info.value?.risk_classification ?? '').trim(); raw==='High' \|\| raw==='Critical'` (so trên giá-trị server, TUYỆT ĐỐI KHÔNG `Date()`/client-clock) + 3 hằng VI `RISK_URGENT_VALUES`(SSoT tập {High,Critical}) / `RISK_URGENT_LABEL='Rủi ro cao'` / `RISK_URGENT_ARIA_PREFIX='Cảnh báo rủi ro cao: '`. Render trong dòng `data-test="scan-risk"` (GIỮ): khi `riskUrgent` → phần tử cảnh báo `data-test="scan-risk-urgent"` (icon ⚠ aria-hidden + nhãn VI `RISK_URGENT_LABEL`, màu cảnh báo rose/amber parity overdue badge) mang `role="status"` (KHÔNG `alert` — BA chốt D15) + `:aria-label="`${RISK_URGENT_ARIA_PREFIX}${riskText}`"` (dùng CHUNG `riskText` SSoT). `riskText` + 6 nhãn-mức + `RISK_CLASSIFICATION_LABEL` GIỮ NGUYÊN byte-for-byte (no-regress vòng 38/40). Hằng urgency có thể đặt `constants/labels.ts` (SSoT, theo acceptance) HOẶC view-local. KHÔNG đụng BE/payload/`CAP_SET_VERSION`. +`assetScanInfoRisk.test.ts`/`AssetScanInfoView.test.ts`: High/Critical→1 urgent-anchor+role=status+aria-label (RED-first absent); Low/Medium/rỗng/null/whitespace/ngoài-enum→0 urgent-anchor (no-false-alarm); no-EN-leak; `scan-risk`+`riskText` no-regress. `vue-tsc` 0. |
| **FE-10** | D16 | **factory vòng 49 (warranty-CTA affordance, FE-only).** `AssetScanInfoView.vue`: THÊM map `WARRANTY_ACTION_KEY: Readonly<Record<string,'warranty_expired'>> = {report_failure:'warranty_expired', request_cm:'warranty_expired'}` (parity `OVERDUE_ACTION_KEY`) + predicate `isWarrantyCta(a)` = `if(!effectiveEnabled(a)) return false; if(!WARRANTY_ACTION_KEY[a.key]) return false; return warrantyExpired.value` (đọc cờ `warrantyExpired` vòng 48 — TUYỆT ĐỐI KHÔNG so `warranty_expiry_date` client-clock) + 2 hằng VI `WARRANTY_CTA_LABEL='Ngoài bảo hành'` (KHÔNG tái dùng `URGENT_CTA_HINT`) / `WARRANTY_CTA_ARIA_HINT='lưu ý chi phí ngoài bảo hành'`. Trên nút CTA (`:696-730`): thêm `:data-warranty-cta="isWarrantyCta(a) ? a.key : undefined"` (parity `:data-overdue-cta`); chip MỚI `v-if="isWarrantyCta(a)"` `data-test="cta-out-of-warranty"` (icon riêng + `{{ WARRANTY_CTA_LABEL }}`, màu rose/red ≠ amber overdue) mang `role="status"`; aria-label nút khi `isWarrantyCta` nối hậu tố `— ${WARRANTY_CTA_ARIA_HINT}`. `isOverdueCta`/`URGENT_CTA_HINT`/`OVERDUE_ACTION_KEY`/chip "Cần làm ngay" (vòng 21) + badge card `warranty-expired`/`WARRANTY_EXPIRED_LABEL` (vòng 48) GIỮ NGUYÊN byte-for-byte. KHÔNG đụng BE/`available_actions`/payload/`CAP_SET_VERSION`. +`AssetScanInfoView.test.ts` (hoặc tách `assetScanInfoWarrantyCta.test.ts`): expired+enabled `report_failure`/`request_cm`→1 chip+attr (RED-first absent); false/absent→0 chip; `request_pm`/`request_calibration`+expired→0 chip; disabled+expired→0 chip; no-leak-chéo overdue↔warranty; no-EN-leak; overdue+warranty-badge no-regress. `vue-tsc` 0. |
| **QA-13** | D16 | FE-only. `vitest` GREEN gồm D16-1..10 RED-first. Đo: expired+enabled `report_failure`/`request_cm`→`isWarrantyCta===true`+`findAll('[data-test="cta-out-of-warranty"]').length===1`+`[data-warranty-cta=<key>]`; `warranty_expired` ∈ {false,absent,undefined}→`false`+0 chip (no-false-alarm); `request_pm`/`request_calibration`+expired→`false`+0 chip (map CHỈ 2 đường-sửa); disabled+expired→`false`+0 chip (disabled ưu tiên); grep source KHÔNG `Date`/`new Date`/so `warranty_expiry_date` trong logic CTA (derive thuần cờ `warrantyExpired`); chip text=`'Ngoài bảo hành'` (≠ `'Cần làm ngay'`)+màu rose/red(≠amber)+aria-label nút hậu tố `'lưu ý chi phí ngoài bảo hành'`+`role='status'`; `request_cm`+expired→CHỈ chip warranty 0 overdue, `request_pm` overdue→CHỈ chip overdue 0 warranty (no-leak-chéo); no-EN-leak (KHÔNG `'Warranty'`/`'Out of warranty'`/`'Expired'` thô); overdue vòng 21 + badge warranty card vòng 48 byte-for-byte (no-regress); WCAG 1.4.1 icon+text+role+aria-label. `vue-tsc` 0; full asset-domain vitest no-regression. **KHÔNG BE-test / KHÔNG reload / KHÔNG migrate** (FE template+predicate — BE đã emit `warranty_expired` vòng 48). **Verify Playwright/quét-QR-thật BLOCKED reload gunicorn --preload (HARD-STOP USER) → backlog, KHÔNG đánh DONE live.** |
| **QA-12** | D15 | FE-only. `vitest` (`assetScanInfoRisk.test.ts` + `AssetScanInfoView.test.ts`) GREEN gồm D15-1..9 RED-first. Đo: High/Critical→`riskUrgent===true`+`findAll('[data-test="scan-risk-urgent"]').length===1`; Low/Medium/rỗng/null/undefined/whitespace/`'UNKNOWN_DRIFT'`→`false`+0 anchor; grep source KHÔNG `Date`/`new Date`/so-ngày (derive thuần enum); `role==='status'` (KHÔNG `'alert'`); `aria-label==='Cảnh báo rủi ro cao: '+riskText` (Critical→'…: Nghiêm trọng', High→'…: Cao'); urgent-anchor+aria-label KHÔNG chứa `'High'`/`'Critical'` thô; `[data-test="scan-risk"]` đúng-1 + `riskText` 6 nhãn byte-for-byte (no-regress vòng 38/40); WCAG 1.4.1 icon+text+role+aria-label. `vue-tsc` 0; full asset-domain vitest no-regression (overdue badge/status-pill a11y/serial GIỮ XANH). **KHÔNG BE-test / KHÔNG reload / KHÔNG migrate** (FE template+computed — BE đã emit `risk_classification`). **Verify Playwright/quét-QR-thật BLOCKED reload gunicorn --preload (HARD-STOP USER) → backlog, KHÔNG đánh DONE live.** |
| **BE-9** | D17 | **factory vòng 50 (crash-safe getdate — bịt HTTP-500 traceback-leak ngày drift), BE-only.** `services/imm00.py`: THÊM helper SSoT `_safe_getdate(value)` → `date|None` (`try: return getdate(value) except (frappe.exceptions.ValidationError, ValueError, TypeError): return None` + comment degrade-an-toàn drift/legacy + chỉ-nuốt-parse-error). Đổi 4 helper dùng `_safe_getdate` trên vế giá-trị-từ-DB: `_is_warranty_expired` (`:400-402`) `d=_safe_getdate(value); return bool(d) and d < getdate(nowdate())` (vế `nowdate()` KHÔNG bọc — luôn ISO); `_is_pm_overdue` (`:359-363`) + `_is_calibration_overdue` (`:378-382`) tương tự sau guard `if not ...`/exempt-status; `_date_str_or_none` (`:412-414`) `d=_safe_getdate(value); return d.strftime('%Y-%m-%d') if d else None`. Guard `if not value` cũ GIỮ TRƯỚC (rỗng/None KHÔNG chạm getdate). `build_asset_scan_info` (`:637-768`) GIỮ NGUYÊN cấu-trúc (degrade field-độc-lập qua 4 helper, KHÔNG `try/except` quanh cả dict). KHÔNG đụng FE/payload-key/shape/RBAC/`CAP_SET_VERSION`. +bổ sung `TestWarrantyExpiredHelper` (hoặc class mới `TestScanDateCrashSafe`): BE-WAR-EDGE-1..3 helper parse-fail→`False`/`None` no-raise; +`TestWarrantyInScanInfo` (hoặc class mới): BE-WAR-EDGE-4 integration payload degrade field-độc-lập (inject drift qua `frappe.db.sql UPDATE` raw / monkeypatch `get_value`) — 16-key đầy-đủ, no-raise, no-financial-leak; BE-WAR-EDGE-5 no-regress (date-obj/ISO/None/'' giữ); BE-WAR-EDGE-6 no-mask (grep no catch-all). **TDD RED-first.** |
| **QA-14** | D17 | BE-only. `bench --site miyano run-tests --module assetcore.tests.test_imm00` GREEN (fresh-import, KHÔNG reload) gồm BE-WAR-EDGE-1..6 RED-first. Đo: (1) `_is_warranty_expired('not-a-date'/'2020-13-45')`→`False` no-`ValidationError`; (2) `_is_pm_overdue('garbage',None)`+`_is_calibration_overdue('2020-99-99',None)`→`False` no-raise; (3) `_date_str_or_none('not-a-date')`→`None` no-raise; (4) **assert-chính** `build_asset_scan_info` asset 1-field-ngày-drift→no-raise+payload 16-key+field-lỗi `None`/`False`+13 field GIỮ+tài-chính∩keys=∅+no-traceback; (5) no-regress `TestWarrantyExpiredHelper` BE-WAR-1..5 + `TestWarrantyInScanInfo` BE-WAR-6..8 + 3 trường-ngày scan-info cũ byte-for-byte (date-obj→True/today→False/future→False/None→False; `_date_str_or_none(date-obj)`→ISO); (6) no-mask grep 4 helper/`_safe_getdate` KHÔNG `except Exception`/`except:` trần + comment degrade tồn tại + (tùy) exception-khác-parse VẪN propagate. **KHÔNG marker-trust — đọc OK count thật.** **KHÔNG reload / KHÔNG migrate** (service `.py` fresh-import; KHÁC `api/imm00.py` reload-gated). KHÔNG git commit/push/merge. |

---

## Tham chiếu chéo

- API scan/label: `assetcore/api/imm00.py::get_asset_scan_info` (332 — nhánh `token` qua `_svc_resolve_qr_token` strip SSoT D8; nhánh `name` `name.strip()` :568 TRƯỚC `db.exists` :574, D12/BR-00-50) / `get_asset_label_data[_batch]` (384/414) / `regenerate_asset_qr_token` (485)
- API action-meta (D11): `assetcore/api/imm00.py::get_asset_action_meta` (THÊM MỚI — lean 6-key cho 3 màn tạo WO scan-action; KHÁC `get_asset` :387 full-doc cho admin-detail) ↔ FE `frontend/src/api/imm00.ts::getAssetActionMeta` + type `AssetActionMeta` (6-field, KHÔNG `extends AcAsset`)
- FE create-view panel meta (D11): `views/cm/CMCreateView.vue` (loader `:128`, ô risk `:331`) / `views/calibration/CalibrationCreateView.vue` (loader `:92`, ô risk `:226`) / `views/pm/PMWorkOrderCreateView.vue` (loader `:131`) — đổi `getAsset`→`getAssetActionMeta`; test `*.assetMeta.test.ts` 3 view
- Service: `assetcore/services/imm00.py::build_asset_scan_info` (575 — payload 12-field gồm `manufacturer_sn` Số serial NSX, D13/BR-00-52; cùng `get_value` no-N+1) / `build_asset_label_data` (755 — `manufacturer_sn`+`asset_name` D5) / `ensure_asset_qr_token` (217) / `_build_qr_url` (546) / `resolve_qr_token` (277 — strip whitespace đầu hàm, D8/BR-00-40)
- FE scan-info serial NSX (D13): `frontend/src/api/imm00.ts::AssetScanInfo.manufacturer_sn` + `frontend/src/views/asset/AssetScanInfoView.vue` (`serialText` computed + hằng `SERIAL_UNKNOWN='Chưa rõ'` + dòng `Số serial NSX` `data-test="scan-serial"`; parity no-raw-docname-leak vòng 27/28, no-em-dash vòng 22) ↔ test `AssetScanInfoView.test.ts`
- FE scan-status pill a11y (D14): `frontend/src/views/asset/AssetScanInfoView.vue` (status pill `:481-489` — `role="status"`+`:aria-label="Trạng thái thiết bị: ${statusLabel}"`+`data-test="scan-status"`; SSoT `statusLabel`=`lifecycleStatusLabel`, WCAG 1.4.1 parity overdue badge) ↔ test `assetScanInfoStatusA11y.test.ts`
- FE scan-risk urgency affordance (D15): `frontend/src/views/asset/AssetScanInfoView.vue` (dòng `data-test="scan-risk"` GIỮ + computed `riskUrgent`=enum-equality {High,Critical} trên giá-trị server (no client-clock) + hằng VI `RISK_URGENT_VALUES`/`RISK_URGENT_LABEL='Rủi ro cao'`/`RISK_URGENT_ARIA_PREFIX='Cảnh báo rủi ro cao: '` + phần tử cảnh báo `data-test="scan-risk-urgent"` `role="status"`+`:aria-label` SSoT-shared `riskText`; parity overdue-CTA vòng 21 + status-pill a11y vòng 39) ↔ test `assetScanInfoRisk.test.ts`/`AssetScanInfoView.test.ts`. SSoT nhãn-mức `riskText`/`RISK_CLASSIFICATION_LABEL` (`constants/labels.ts` — KHÔNG đổi); hằng urgency có thể đặt `labels.ts` (SSoT) HOẶC view-local. BE `build_asset_scan_info` GIỮ (đã emit `risk_classification` coalesce `''`).
- FE warranty-CTA affordance (D16): `frontend/src/views/asset/AssetScanInfoView.vue` (cụm CTA `:696-730` GIỮ + map `WARRANTY_ACTION_KEY` Readonly {report_failure,request_cm} (parity `OVERDUE_ACTION_KEY:318`) + predicate `isWarrantyCta` (parity `isOverdueCta:329` — disabled ưu tiên, đọc cờ `warrantyExpired:297` vòng 48, no client-clock) + hằng VI `WARRANTY_CTA_LABEL='Ngoài bảo hành'`/`WARRANTY_CTA_ARIA_HINT='lưu ý chi phí ngoài bảo hành'` + chip `data-test="cta-out-of-warranty"` `role="status"` màu rose/red + attr `:data-warranty-cta`; 2 affordance overdue(amber)/warranty(rose) ĐỘC LẬP no-leak-chéo; parity overdue-CTA vòng 21 + warranty badge card vòng 48) ↔ test `AssetScanInfoView.test.ts` (hoặc `assetScanInfoWarrantyCta.test.ts`). Cờ `warrantyExpired`=`info.warranty_expired===true` (vòng 48, server-flag SSoT) GIỮ; badge card `[data-test="warranty-expired"]`/`WARRANTY_EXPIRED_LABEL='Hết bảo hành'` (vòng 48) ≠ chip CTA. BE `build_asset_scan_info` GIỮ (đã emit `warranty_expired`+`warranty_expiry_date` vòng 48).
- BE crash-safe getdate (D17): `assetcore/services/imm00.py` (helper SSoT `_safe_getdate(value)→date|None` chỉ-nuốt `frappe.exceptions.ValidationError`/`ValueError`/`TypeError`; 4 hàm xử-lý-ngày `_is_warranty_expired:385`/`_is_pm_overdue:350`/`_is_calibration_overdue:366`/`_date_str_or_none:405` dùng `_safe_getdate` trên vế giá-trị-DB → degrade `False`/`None` trên chuỗi drift; vế `getdate(nowdate())` KHÔNG bọc; guard `if not value` cũ GIỮ trước; `build_asset_scan_info:637` GIỮ cấu-trúc — degrade field-độc-lập, 13 field còn lại GIỮ; parity FE `formatIsoDateLabel` ISO-strict vòng 18-19) ↔ test `tests/test_imm00.py::TestWarrantyExpiredHelper`/`TestWarrantyInScanInfo` (BE-WAR-EDGE-1..6). Endpoint `api/imm00.py::get_asset_scan_info:332` (`_svc_build_asset_scan_info:684`) KHÔNG còn 500 traceback-leak khi record drift (LL-BE-42). Schema/payload-key/`CAP_SET_VERSION` GIỮ. FE KHÔNG đụng (payload type `str|None`/`bool` GIỮ).
- FE token trim (defense-in-depth lớp 1): `frontend/src/views/system/QrResolveView.vue:34` (`tokenParam().trim()`) / `QRScanView.vue:45` (`extractToken` `raw.trim()`)
- Model QR-gen: `assetcore/assetcore/doctype/ac_asset/ac_asset.py::before_insert/_ensure_qr_token/after_insert` (50-94)
- Import path: `assetcore/api/import_data.py:348-350` (`new_doc().insert()` → before_insert)
- RBAC capability SSoT: `assetcore/services/shared/rbac.py::CAPABILITY_MAP` (66-115) + `AssetStatus` `constants.py:85-99`
- FE routes: `frontend/src/router/index.ts` (IncidentCreate 447 / PMWorkOrderCreate 307 / CMCreate 341 / CalibrationCreate 414)
- FE scan view: `frontend/src/views/asset/AssetScanInfoView.vue`; create views: `views/incident|pm|cm|calibration/*CreateView.vue`
- ADR liên quan: `./ADR-IMM00-ASSETCODE.md` (asset_code=name invariant — deep-link asset=name) + `../imm-04/ADR-001-asset-qr.md` (token, deep-link /a/<token>, RBAC read/write baseline)
- Core Doc: `docs/imm-00/04_Backend_Design.md`, `05_API_Specification.md`, `06_Frontend_Design.md`, `07_Testing_QA.md`
