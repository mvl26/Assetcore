# IMM-00 — Tài liệu module

| Mục | Giá trị |
|---|---|
| Module | **IMM-00 — Master / Cross-cutting** |
| Wave | Master |
| Trạng thái | **Live ✅ — docs synced vs code 2026-05-27** · Notification Framework Wave N1 spec'd 2026-05-29 · RC-03 kế thừa luật khấu hao Category→Asset spec'd 2026-06-03 (FR-00-47..52 / BR-00-18..21) · RC-04 per-asset self-heal `regenerate_depreciation_schedule` (FR-00-53..55 / BR-00-22) · RC-05 `bulk_regenerate_by_category` về SoT (FR-00-56..58 / BR-00-23) · RC-06 SoT `effective_book_value` (BR-05-13) · **RC-07 thanh lý hủy kỳ Pending khấu hao spec'd 2026-06-03 Vòng 8 (FR-00-59..62 / BR-00-24 — schema-delta `event_type+=depreciation_stopped`)** · RC-08 Out of Service PAUSE + RESCHEDULE khấu hao Vòng 9 (FR-00-63..68 / BR-00-25 — no schema-delta) · RC-CAPA-EFF cổng hiệu quả CAPA SoT đơn Vòng 12 (FR-00-59 / BR-00-26) · **RC-09 nhãn sự kiện khôi phục `restored` ĐÚNG 1 — kill double-emit Vòng 14 (FR-00-69 / BR-00-27 — `_lifecycle_event_for(to,from)`, no schema-delta)** · **CR-WF-00-FXSRC-RECONCILE bất-biến 2-chiều `fixtures/workflow.json` ⇄ 22 source `workflow/*.json` (states+edges+admin-override, RED-first) spec'd 2026-07-15 Vòng 43 (07 §XV / 04 §II.1.8-FXSRC + ADR-IMM00-WF-FXSRC-RECONCILE — test-only, 0 runtime `.py`)** · **CR-32 APPROVAL-INBOX endpoint gộp "Phiếu chờ tôi duyệt" xuyên module (imm04+imm00+imm15) spec'd 2026-07-16 Vòng 2 (05 §III.22 / 06 §III.11 + ADR-IMM00-APPROVAL-INBOX + mobile 04-api-contract §8.54 — BR-00-INBOX-01/02, endpoint `.py` MỚI ⇒ cần reload worker khi deploy, 0 migrate)** |
| Số file hiện có | 8 (numbered 02–09) |
| Cập nhật cuối | 2026-07-30 (**🔒 AC-CR-98 + AC-CR-106 [BA, IMM-00 ADR + IMM-04] — enforce `INV-CONN-27` / `INV-CONN-21`:** `ADR-IMM00-LIST-SCOPE` bồi **PHẦN IV §10** = SSoT cho «ô đếm == nhánh drill cho MỌI persona». Hai lỗi độc lập: (a) `list_commissioning` dùng **2 engine** đều bỏ `permission_query_conditions` ⇒ *khớp số mà cùng thô* = **rò dữ liệu** ⇒ chốt MỘT ENGINE `frappe.get_list`; (b) `apply_vendor_scope` **GÁN** thay vì **GIAO** (`scope.py:174`) ⇒ deep-link 1 thiết bị bị ghi đè bởi tập được giao ⇒ chốt đại số **8 shape**, ra luôn `["in", list]`, giao rỗng ⇒ `["in", ["__none__"]]`, **dict-in → dict-out** (3 call site mutate ngay sau ⇒ đổi kiểu = `TypeError`). **SUPERSEDE** đoạn ratify sai ở `../imm-04/05 §20.4` (từng hợp thức hoá lỗ rò bằng lập luận "cùng bỏ qua thì vẫn khớp"). 3 self-correction acceptance + ma trận **3 persona** (`Super Admin` · `Commissioning User` · `Vendor Engineer`+`Commissioning User`; vendor **thuần** = Error envelope vì 0 DocPerm read) ở §10.1/§10.2. Drift phát hiện: `07 §XVIII.10.1` prescribe `tests/test_connections_list_promotion.py` — **file không tồn tại** ⇒ TC-CONN-P-01..08 vẫn là nợ (P-04/05/08 được §10.9 hấp thu). Nợ mới có tên: `AC-CR-107` (op không-giao-được đang fail-closed câm) · `AC-CR-108` (`vendor_engineer_name` là `Data`, không phải `Link → User` ⇒ nhánh vendor thực chất `owner`-only) · `AC-CR-109` (alias DocType hiệu chuẩn) · `AC-CR-110` (`_ORDER_MODIFIED` thiếu tiebreaker). File: `ADR-IMM00-LIST-SCOPE §10` · `05 §III.24.8/24.9` (INV-CONN-21/27 → **ENFORCE**) · `07 §XVIII.10.1` (drift note)) · Trước đó 2026-07-30 (**🧩 AC-CR-105 [BA, IMM-00 — BE + FE] — «Tạo từ ngữ cảnh cha» HẾT là nút chết:** LAND phần BE của `AC-CR-90` (nợ **AC-CR-90(b)**, `grep create_prefill assetcore/` = **0 hit** trên đĩa) ⇒ ô `get_connections` **9 → 10 khoá** (`create_prefill: dict[str,str]`, **luôn có mặt**), khoá là **khoá URL của FE** (`asset`/`pm_wo`/`incident`) **KHÔNG** phải Link fieldname (`asset_ref`/`source_pm_wo`/`incident_report`), + vị-từ quyền đổi sang **token** `CREATE_CAPABILITY` (5 doctype, parity 3 điểm, guard `CAPABILITY_MAP[token] == (dt,"create")`); FE dựng **chip** «Tạo …» cho ô **0 bản ghi** trong khối SIBLING `conn-empty-actions`. **2 SELF-CORRECTION**: (1) bất biến `⟺` chuỗi-3-vế của `05 §III.24.7.a` là **SAI** (mâu thuẫn 3 doctype `can_create true ∧ prefill {}`) ⇒ viết lại thành **3 mệnh đề rời**, **CẤM** assert `can_create true ⇒ prefill != {}`; (2) xung đột **D-CR93-4 ⇄ AC-CR-90** (blocker STATE #2) ⇒ **NARROW D-CR93-4**: câu «Chưa có: …» vẫn **0 affordance** và vẫn nêu tên **mọi** ô rỗng ⇒ `TC-FE-CONN-25/26/27` **xanh không sửa một chữ** (breakage khai trước: **đúng 2 dòng** `RelatedRecords.test.ts:772-773`). **0 DocType mới · 0 OAS delta · 3 counter 1024/1167/1193 delta 0 · 0 truy vấn thêm (ZERO-COST)**; ngoài biên (QA **không** chấm): P4 vòng đời per-doctype = **AC-CR-90(c)**, lỗ ghi `create_incident`, EC-12-05. DoD: `run-tests --module assetcore.tests.test_connections_tree` (**29 → ≥35 TC**) + `vitest` + `vue-tsc`, **KHÔNG curl** (blocked-reload). Doc: `02 §IV.42` · `04 §V.10` · `05 §III.24.11` · `06 §VIII.14` · `07 §XXI` · [ADR §18](./ADR-IMM00-CONNECTIONS-TREE.md).) · PREV 2026-07-30 (**🧩 AC-CR-102 [BA/FE-only, IMM-00 đọc IMM-08/09/12] — hồ sơ VẬN HÀNH của MỘT thiết bị RENDER THẬT trong tab «Bản ghi liên quan»:** 3 endpoint lịch sử ĐÃ LIVE on-disk mà **0 caller `.vue`** (mã chết) + `api/imm08.ts:251` **dối kiểu** (`history: PMWorkOrder[]` trong khi BE trả `PM Task Log` 10 field) ⇒ chốt **FR-00-OPH-01 / BR-00-OPH-01..18** + **ADR-IMM00-ASSET-OP-HISTORY** (D-OPH-1..16 · INV-OPH-1..18 · microcopy §5.3). **SELF-CORRECTION**: Core Doc trước đó CẤM đúng việc này ở 3 nơi (`ADR-IMM00-TRUNCATION-SSOT:371` · `02:1030` Never · `07:2553` A9) ⇒ đã **supersede có điều kiện** (C1 vị-từ-trong-tiêu-đề · C2 6-tín-hiệu-không-lặp-ô · C3 0-chi-phí-mở-máy) + chú thích phạm vi cả 3 nơi, **không xoá** văn bản cũ. **0 dòng `.py` prod · 0 OAS delta · 3 counter 1024/1167/1193 delta 0 · 0 blocker reload mới**; DoD `run-tests --module assetcore.tests.test_asset_operational_history_contract` + `vitest` (284 → ≥286 file) + `vue-tsc`. Doc: `02 §IV.41` · `05 §III.26` · `06 §VIII.13` · `07 §XX`. Ranh giới ghi rõ: dải «Đang xem M/N» = **VÒNG 5**.) · PREV 2026-07-28 (**🧩 AC-CR-100 [BA/Trục truncation, IMM-00 — FE + ĐÚNG 1 dòng BE] — tab «Lịch sử» Chi tiết tài sản HẾT cắt IM LẶNG ở lớp CLIENT:** BE **đã** trả `pagination.total` đủ (`api/imm00.py:1216-1247`) và OAS **đã** khai `Pagination.total` required (`:852-880`), nhưng view **vứt** bằng 1 dòng cast `as unknown` (`AssetDetailView.vue:202`) ⇒ tab hiện tối đa 100 dòng, **không có** đường sang trang 2, và `!timeline.length` gộp *lỗi API* với *"Chưa có sự kiện vòng đời"*; chốt hợp đồng **2 đầu**: công bố `pagination.total` (SERVER) + «Tải thêm» tăng `page` GIỮ `page_size=100=_MAX_PAGE_SIZE` + APPEND/dedupe theo `name` + 3 trạng thái *chưa tải / rỗng thật / lỗi* có vị-từ RIÊNG (`timelinePage===0`) + không dead-end (nút «Thử lại» đúng trang lỡ) + chống «Tải thêm» không tiến. **Self-Correction lỗi thiết kế gốc**: `_ORDER_EVENT_TS_DESC = "timestamp desc"` (`api/imm00.py:293`) **thiếu tiebreaker** ⇒ ALE trùng `timestamp` (ca THƯỜNG — 1 transition emit ≥2 event/giây) làm 2 trang liền kề **lặp + BỎ SÓT** im lặng ⇒ đổi thành `"timestamp desc, name desc"` (**1 dòng, 0 dịch dòng ⇒ 0 cite-drift**; precedent `services/imm10.py:76`). **0 OAS delta** (paths 110 · schemas 290 · parameters 38 GIỮ) · **3 counter guard delta 0** (1024/1167/1193) · **0 schema/cap/patch delta**; ⚠️ mở **1 blocker `bench restart`** (đụng `.py` prod, DoD chấm bằng `run-tests` KHÔNG curl). Quyết định: **ADR-IMM00-TRUNCATION-SSOT §8 (D-TL-1..9 · INV-TL-1..11 · microcopy §8.8)** · FR-00-TL-01 / BR-00-TL-01..09 (`02 §IV.40`) · `05 §III.25` · `06 §VIII.11` · `07 §XIX` (TC-TL-B1..B9 + TC-TL-F1..F14). **Đính chính đề mục PM**: số CR «AC-CR-96» **đã bị chiếm** (vendor-clobber INV-CONN-21) — 97/98/99 cũng đã dành ⇒ vòng này là **AC-CR-100** (ADR §8.0); baseline FE **283 file** (số 2591 trong đề mục là stale ⇒ chấm theo delta). Nợ khai tên: **AC-CR-101** gỡ 2 cast mù còn lại `loadKpi:220`/`loadChain:225` · **AC-CR-102** quyết định PM cho 3 nhánh lịch sử PM/CM/Sự cố 0-caller (xoá mã chết **HAY** dời sang màn chi tiết PM/CM — **CẤM** render lên màn Chi tiết tài sản: `get_asset_history` lọc `docstatus=1` @`services/imm09.py:2608` vs ô connections đếm mọi docstatus ⇒ 2 số chỏi nhau)) · 2026-07-28 (**🧩 AC-CR-95 [BA/Trục deep-link, IMM-00 cross-cutting — FE-only + 1 file test BE] — thăng hạng 4 màn đích còn lại CÓ hạ tầng BE sẵn:** `Firmware Change Request` · `Asset Commissioning` · `Asset Decommission` · `IMM CAPA Record` rời `LIST_TARGET_NO_FILTER` (**9→5**) vào `DOCTYPE_LIST_TARGET` (**11→15**), phân hoạch 20 giữ nguyên, **guard parity KHÔNG sửa**; 4 màn `/cm/firmware` `/commissioning` `/decommissions` `/capas` học đủ **bốn** vế (khởi tạo TRƯỚC lần nạp đầu · dịch đúng khoá BE `final_asset`/`asset` · chip VI «Thiết bị: …» + bỏ chip xoá **query** · `watch` drill lần 2); **0 file `.py` prod đổi** (cả 4 endpoint drill đã nhận đủ khoá — `list_firmware_crs(asset)` map `asset_ref` `:2902` · `_ALLOWED_FILTER_KEYS ∋ final_asset` `imm04.py:136` · `_DECOM_FILTER_KEYS ∋ asset` `imm14.py:393` · `list_capas(asset)` `:1870`) ⇒ **0 blocker `bench restart` mới**; hàng rào `sourceKeys` lần đầu chịu lực thật (4 doctype đến từ **8 hub / 6 anchor** — `{vendor}` `{master_item}` `{linked_incident}` `{name}` phải cho ra **null**); khai **chênh predicate có công thức** `ô.total == drill + #{docstatus==2}` cho phiếu nghiệm thu (INV-CONN-26). Quyết định: **ADR §16 (D-CR95-1..10 · INV-CONN-23..28 · INV-CONNFE8-1..10)** · FR-00-CONN-05 / BR-00-CONN-50..58 · `06 §VIII.10` · `07 §XVIII.10` · `05 §III.24.9`. Đính chính đề mục: counter guard **delta 0** (không phải "tăng") · khoá ngoại lai FCR là `{name:…}` không phải `{asset_repair_wo:…}` · baseline FE **282 file / 2682 test**. Nợ khai tên: **AC-CR-98** `list_commissioning` `get_all`→`get_list` (INV-CONN-27, vendor row-scope) · **AC-CR-99** ô đếm chưa loại `docstatus==2` · **AC-CR-97** = backlog nút-tạo-ô-rỗng (đổi số từ AC-CR-95)) · 2026-07-28 (**🧩 AC-CR-94 [BA/Trục deep-link, IMM-00 cross-cutting — FE + 1 nhánh BE] — «Xem tất cả» dẫn ĐẾN ĐÍCH cho 2 màn LỊCH:** `PM Schedule` + `IMM Calibration Schedule` thăng hạng `LIST_TARGET_NO_FILTER`→`DOCTYPE_LIST_TARGET` (11→9 / 9→11, phân hoạch 20 giữ nguyên, **guard parity KHÔNG sửa**); 2 màn `/pm/schedules` + `/calibration/schedules` học đủ **bốn** vế (khởi tạo · truyền API · chip VI «Thiết bị: …» + bỏ chip xoá **query** · `watch`); bịt **P0 câm ở BE** — `services/imm11.py::_extract_asset_in_scope` bỏ rơi bộ lọc `asset` **vô hướng** ⇒ `list_calibration_schedules(filters={"asset":X})` trả TOÀN BỘ lịch của mọi thiết bị; khoá bất biến **`count == drill` bằng test BE cross-endpoint** (INV-CONN-18..22 · TC-CONN-T-25/26) + gỡ assert vacuous `test_connections_tree.py:579-581`. Quyết định: **ADR §15 (D-CR94-1..9)** · FR-00-CONN-05 / BR-00-CONN-42..49 · `06 §VIII.9` · `07 §XVIII.9` · `05 §III.24.8` · `../imm-11/05 §6.2`. Nợ khai tên: **AC-CR-96** vendor-clobber `apply_vendor_scope` (INV-CONN-21) · **AC-CR-95** = backlog nút-tạo-ô-rỗng (đổi số từ AC-CR-94)) · 2026-07-28 (**🧩 AC-CR-93 [BA/Trục UX-diện-tích, IMM-00 cross-cutting — FE-only] — tab «Bản ghi liên quan» chỉ render ô CÓ dữ liệu, ô rỗng gộp MỘT dòng/nhóm:** D-FE-8 khai từ vòng 2 nhưng **chưa bao giờ được cài** (`RelatedRecords.vue:177` `v-for="item in group.items"` render **mọi** ô; `hasRecords` `:70`-`:72` chỉ gác **thân** ô `:205`; chuỗi `conn-empty-summary` **0 hit** trong `frontend/src`) ⇒ tab của 1 `AC Asset` dựng **19** khối cho **3** ô có dữ liệu — nửa *"chiếm quá nhiều diện tích"* của yêu cầu gốc còn nguyên. Chốt (**ADR §14 — D-CR93-1..7 · INV-CONNFE6-1..9**): ô render ⟺ `total ?? count ?? 0 > 0` qua **một** vị-từ SSoT `hasConnectionRecords` (**cấm** `items.length` — ô LEGACY bị gộp oan); ô rỗng gom vào **đúng 1** dòng `conn-empty-summary`/nhóm khuôn `Chưa có: {nhãn 1}, {nhãn 2}, …` **text tĩnh 0 affordance** (giữ phân biệt *"chưa có"* vs *"chưa tải"* — **ẩn hẳn** đã bị §10.3 loại); nhóm toàn rỗng ⇒ **0 tiêu đề nhóm**; payload mọi ô rỗng ⇒ câu VI **và** dòng gộp cùng tồn tại, `expose.total` vẫn = `payload.total`. **Đóng drift P2 §13.7 tường minh**: khuôn gộp ⇒ **sửa mã**, tên `data-testid` ⇒ **đính chính ADR theo mã** (`conn-item`/`conn-count`/`conn-meta`/`conn-row` là hợp đồng; `conn-cell`/`conn-badge`/`conn-band`/`conn-row-static` + 4 tên trạng-thái **retired**; **CẤM** thêm `data-doctype` — 3 TC assert `html()` sạch tên Anh). **Supersede 3 mệnh đề** (§14.7): D-FE-8 gạch 2 · INV-CONNFE4-5 *"ô rỗng vẫn được có nút tạo"* (mã chưa từng cài; TC-FE-CONN-10 `:301` đã khoá 0 nút) · INV-CONNFE-2 bậc-3 fallback `doctype`. Đánh đổi ghi rõ: mất nút «Tạo …» cho doctype 0 bản ghi ⇒ backlog **AC-CR-94** có tên. Breakage khai TRƯỚC: **đúng 1** TC sửa (TC-FE-CONN-10 `RelatedRecords.test.ts:283` — chuyển phạm vi chấm sang `conn-empty-summary`, giữ vế *0 nút / 0 conn-row*), 22/23 TC còn lại **0 assert bị sửa**. **FE-thuần**: 0 file `.py`, `DOCTYPE_LIST_TARGET`/`LIST_TARGET_NO_FILTER` **không đổi** (INV-CONNFE5-4 vẫn phủ 20 doctype), hợp đồng BE **0 byte**, guard count **delta 0** (1024/1167/1193 · OAS 110/290/38 — đọc từ đĩa 2026-07-28). Docs: `02 §IV.39` FR-00-CONN-04 + BR-00-CONN-35..41 · `06 §VIII.8` · `07 §XVIII.8` TC-FE-CONN-24..30/40..43. **PREV 🧩 AC-CR-91 [BA/Trục UX-dữ liệu, IMM-00 cross-cutting — vòng 5/5, FE-only] — «Xem tất cả» phải mở ra danh sách ĐÃ LỌC:** audit đo trên tab của **1 AC Asset** (16 ô bấm được) cho thấy **chỉ 3/16** ô mở ra danh sách đã lọc; **13 ô** dẫn người dùng ra danh sách **toàn hệ thống** ngay sau khi ô đó vừa hứa "6 bản ghi" — hai nguyên nhân **độc lập**: (a) **lệch tầng tên gọi** (BE phát **fieldname** `asset_ref`/`final_asset`/`critical_asset`, màn đích đọc **khoá query** `asset` ⇒ query vô hại và vô dụng — 4 ô) và (b) **màn đích chưa đọc khoá lọc nào** — 9 ô. Bốn vòng test xanh vẫn để lọt vì `INV-CONNFE-6` chỉ đếm **sự tồn tại của khoá**, không hỏi **khoá đó có ai đọc không** (*test đúng mệnh đề sai*). Chốt (ADR §13 — **D-CR5-1..8 · INV-CONNFE5-1..11 · INV-CONN-16/17**, **supersede §12.8**): thêm **2 bảng SSoT FE** `DOCTYPE_LIST_TARGET` (9 entry, `queryKey` = khoá **màn đích THẬT SỰ đọc**) + `LIST_TARGET_NO_FILTER` (11 doctype, allowlist **chỉ-giảm**) với bất biến **phân hoạch** 20 = 9 ⊎ 11 ⇒ **0 doctype vùng xám**; hàm **thuần** `listTarget(item)` **dịch khoá, giữ nguyên value**, trả `null` ⇒ **KHÔNG dựng nút** (liên kết nội bộ `{name:'a,b,c'}` ⇒ luôn `null` — không màn nào đọc `route.query.name` dạng tập); **ba lớp gác** như nút «Tạo …» (hợp đồng thuần → `router.resolve` → `canAccessDrill`, để không đá người dùng ra `/unauthorized`); **wire lọc-theo-thiết-bị THẬT** cho `/incidents/list` + `/rca` đủ **4 vế** (khởi tạo trước lần nạp đầu · truyền xuống API · chip «Thiết bị: mã» + nút bỏ lọc · `watch` query đổi) — store/API/BE đã sẵn `asset`, **0 mở rộng store**; **GUARD TĨNH MỚI** `router/connectionsListParity.guard.test.ts` mirror `connectionsCreateParity.guard.test.ts` (đọc tới **chính file view** của route đích ⇒ không ai khai được khoá không ai đọc). **Self-Correction**: `linkFilters` đang **phản D-FE-6 quy tắc 1** (`deep_link_filters === {}` rơi xuống fallback `filters` ⇒ tái sinh đúng bug này) và bị **ossify bởi chính test** ⇒ sửa **code + test theo hợp đồng**, không sửa hợp đồng theo code. **Hợp đồng BE payload 0 thay đổi** (A6) — BE chỉ **thêm 2 invariant** (INV-CONN-16/17) đóng đinh giả định mà `listTarget` dựa vào. Kết quả đo lại: **9/16 ô lọc đúng** (≥8 ✔) · **0 nút** mở ra danh sách không lọc · **0** route chết. **0 DocType ⇒ 0 migrate**; **0 op OAS ⇒ guard count delta 0** (giá trị ĐÚNG đọc thẳng từ đĩa 2026-07-28: **1024 / 1167 / 1193** — con số `983/1126/1152` lưu hành trong STATE là **STALE**). Docs: `06 §VIII.7` · `07 §XVIII.7` TC-CONNLIST-01..05 + TC-FE-CONN-30..35 + TC-CONNFE5-01..11 + TC-CONN-T-23/24 · `ADR-IMM00-CONNECTIONS-TREE §13`. · **🧩 AC-CR-90 [BA/Trục UX-dữ liệu, IMM-00 cross-cutting — vòng 4/5, BE+FE] — «Tạo từ ngữ cảnh cha»:** sau vòng 3 khối đã gọn và nói bằng dữ liệu thật, nhưng `can_create` vẫn là **quyền thô + cổng chặn-tất**: nó gọi `frappe.has_permission(dt,"create")` (giá trị **trùng** `rbac.can(cap)` hôm nay nhưng **không ràng buộc** ⇒ đổi binding một tầng, hai tầng kia trôi im lặng — khuôn *RBAC dead-gate*), và dùng **một** hằng `AssetStatus.BLOCKED_FOR_WO` cho **mọi** loại phiếu ⇒ thiết bị `Out of Service` bị tắt cả nút «Tạo phiếu sửa chữa» lẫn «Báo sự cố» — **đúng lúc cần nhất** — trong khi service THẬT cho phép (`imm09` dùng `is_valid_asset_transition(…, 'Under Repair')`, `imm12` không có cổng nào). Chốt (ADR §12 — **D-CR4-1..10 · INV-CONN4-1..10**, **supersede D8 điều kiện 3+4**): `can_create` = giao của **4 vị-từ độc lập** (màn tạo · liên kết ngược · **capability TOKEN** `CREATE_CAPABILITY` parity 3 điểm API↔ô↔route · **vòng đời PER-DOCTYPE** dùng lại đúng hằng/hàm của enforcement); khoá **MỚI** `create_prefill: {query_key: giá trị}` (12→**13** khoá) với bất biến **ba chiều** `can_create=false ⟺ hint="" ⟺ prefill={}`, khoá query lấy từ `CreateContext.query_keys` — **hợp đồng URL của FE** (`asset`/`incident`/`pm_wo`), **không** phải Link fieldname (đính chính mệnh đề prefill sai của D8); oracle **advertise ⇔ enforce** 4 doctype × 3 vòng đời gọi **service THẬT**; bịt lỗ ghi `api/imm00.create_incident` (0 cap-gate · 0 kiểm asset · `doc.update(form_dict)` mở ⇒ `rbac.require('corrective.create')` + `NOT_FOUND` in-envelope + **whitelist 12 field**, cấm `status`/`reported_by`/`docstatus`/`reported_to_byt`); land **EC-12-05** đã đặc tả từ lâu mà chưa có code (`report_incident` chặn `Decommissioned`, mã mới `IMM12-ASSET-DECOMMISSIONED` 422 — BR-12-29). **0 DocType ⇒ 0 migrate**; **0 op OAS ⇒ guard count delta 0**. Docs: `02 §IV.39` FR-00-CONN-03 + BR-00-CONN-25..34 · `04 §V.8` · `05 §III.24.7`+`§III.9` · `06 §VIII.6` · `07 §XVIII.6` TC-CONN4-01..17 + TC-CONNFE4-01..06 · ADR §12 · `docs/imm-12/02` BR-12-29) · 2026-07-28 (**🧩 AC-CR-89 [BA/Trục UX-dữ liệu, IMM-00 cross-cutting — vòng 3/5, FE] — «Bản ghi liên quan» thành TAB RIÊNG + MOUNT LƯỜI:** sau vòng 2 khối đã "nói bằng dữ liệu thật" nhưng vẫn **nối đuôi thân trang** ở cả 5 màn chi tiết **và** vẫn gọi `get_connections` **eager** lúc tải trang (`RelatedRecords.vue:58 onMounted(load)` × 5 điểm gắn) — với `AC Asset` là **19 truy vấn permission-aware** cho một khối người dùng có thể không bao giờ cuộn tới. Chốt vòng đời TAB (ADR §11 — **D-TAB-1..12 · INV-CONNTAB-1..12**): component dùng chung `DetailTabBar.vue` (**MỚI**, `role="tablist"`/`role="tab"`/`aria-selected` + hợp đồng cuộn ngang TC-RWD-07) cho **cả 5** màn; panel chính **`v-show`** (giữ nguyên dữ liệu đang nhập, không nạp lại phiếu) ⊕ panel liên quan **`v-if`** (⇒ **0** request trước khi mở tab, **1** request sau khi bấm — đo bằng spy, không suy từ mã nguồn); điều kiện render tab bar = **đúng** điều kiện gác cũ (chưa tải / 403 in-envelope ⇒ 0 tab bar, `detailReadForbiddenGate.integration.test.ts` xanh **không sửa assert**); nhãn tab **VI đầy đủ** SSoT `DETAIL_RELATED_TABS` (Tài sản giữ 5 nhãn cũ + «Bản ghi liên quan» = 6 tab); **supersede** mệnh đề badge-tab của D-FE-1 ⇒ **vòng 3 tab không badge** (badge = phải gọi API sớm = phá chính mục tiêu); mở lại tab = nạp lại (**không** `<KeepAlive>` — D-TAB-8). Biên A10: **0 dòng** dưới `assetcore/`, **không** chạm `RelatedRecords.vue`/`api/connections.ts`. Docs: `02 §IV.39` FR-00-CONN-02 + BR-00-CONN-18..24 · `06 §VIII.5` · `07 §XVIII.5` TC-CONNTAB-01..15 · ADR §11) · 2026-07-28 (**🧩 AC-CR-88 [BA/Trục UX-dữ liệu, IMM-00 cross-cutting — vòng 2/5, FE] — «Bản ghi liên quan» thành NỘI DUNG MỘT TAB, nói bằng DỮ LIỆU THẬT:** vòng 1 đã cho BE trả cây 12 khoá nhưng `RelatedRecords.vue` vẫn render **19 chip đếm nhãn Anh thô** bọc trong card riêng ⇒ dữ liệu mới là **state chết trên web**. Chốt hợp đồng hiển thị (ADR §10 — **D-FE-1..11 · INV-CONNFE-1..11**): 1 root duy nhất (giữ class fallthrough), **bỏ** heading + card chrome + dòng "Tổng N", `defineExpose({reload,total})` cho badge tab vòng 3; nhãn qua **một** accessor `label_vi→label→doctype` (doctype thô chỉ được ở `data-doctype` ⇒ assert trên `wrapper.text()`); mỗi ô ≤5 dòng preview THẬT (`title` + `status_label` VI + `formatDate`, **cấm** `status` thô/`'undefined'`); dòng mở **đúng bản ghi** qua `detailRouteForDoctype`, thiếu màn chi tiết ⇒ **text tĩnh** (0 nút chết); «Xem tất cả» chỉ tồn tại khi **route ∧ ≥1 khoá lọc ∧ total>0**, **`deep_link_filters === {}` ⇒ TUYỆT ĐỐI 0 nút** (đúng bug user báo "bấm ra list chung/trống"), BE cũ ⇒ chiếu `filters` **chỉ giữ value scalar** (đóng URL rác `?name=in,A,B`); cắt nói thật bằng **một** câu `Đang xem {shown}/{badge}`, `capped ⇒ '100+'` và **cấm** `total − shown`; ô `total===0` gộp **1 dòng/nhóm**; vòng 2 render **0 nút tạo** nhưng ghim sẵn bất biến 2 chiều `can_create ⟺ create_route_hint`. Scope kín 4 file FE, **0 file BE**, **0 màn Detail**, **KHÔNG** `npm run build`. Spec: [ADR §10](./ADR-IMM00-CONNECTIONS-TREE.md) · [02 §IV.39 BR-00-CONN-11..17](./02_Analysis_Design.md) · [05 §III.24.6](./05_API_Specification.md) · [06 §VIII.4.2](./06_Frontend_Design.md) · [07 §XVIII.4 TC-CONNFE-01..17](./07_Testing_QA.md)) · 2026-07-27 (**🌳 AC-CR-87 [BA/Trục UX-dữ liệu, IMM-00 cross-cutting — vòng 1/5] — «Bản ghi liên quan» thành CÂY DỮ LIỆU THẬT:** `api/connections.py::get_connections` chỉ trả 19 **badge đếm** nhãn tiếng Anh thô ⇒ khối UI *"chiếm nhiều diện tích mà chỉ dẫn tới chức năng, không dẫn tới bản ghi"* (nguyên văn người dùng). Chốt: mỗi ô mang **12 khoá** (+`label_vi`/`total`/`truncated`/`items[]` preview 5 dòng THẬT/`deep_link_filters`/`can_create`/`create_route_hint`), **MỘT** `frappe.get_list` duy nhất vừa preview vừa đếm (bất biến `len(items)==min(total,preview_limit)` ∧ `count==total`; cấm `db.count`/`get_all`/`ignore_permissions`), `truncated` **int 0|1** (KHÔNG bool — CR-01), nhãn VI **SSoT ở BE** cho **41 doctype** với test parity duyệt **12 module dashboard THẬT**, `can_create` = giao 4 điều kiện gồm cổng vòng đời **cùng hằng** `AssetStatus.BLOCKED_FOR_WO` với validator. **Đính chính acceptance A6** (A6 ∧ A9 loại trừ nhau ⇒ **A9 thắng**, xem ADR §D6). 0 DocType ⇒ 0 migrate; FE vòng 1 chỉ thêm kiểu optional. Spec: [ADR-IMM00-CONNECTIONS-TREE](./ADR-IMM00-CONNECTIONS-TREE.md) · [02 §IV.39](./02_Analysis_Design.md) · [04 §V.7](./04_Backend_Design.md) · [05 §III.24](./05_API_Specification.md) · [06 §VIII.4](./06_Frontend_Design.md) · [07 §XVIII](./07_Testing_QA.md)) · 2026-07-27 (**👥 AC-CR-80 [BA/Trục contract, IMM-00 — đóng mobile CR-75] — picker "người nhận việc" nói ĐÚNG SỰ THẬT:** `api/user.py:1057 list_assignable_users` ĐÃ lọc theo **capability/DocPerm** (mirror `services/imm09.py:1657 _is_repair_capable`, KHÔNG role-name) nhưng (a) **vắng mirror mobile** ⇒ app buộc dùng `listUsers.role` đơn-trị (lọc sai ⇒ giấu mất PM Manager/Vendor Engineer; không lọc ⇒ chọn nhầm → 422 tại giường bệnh), (b) **cắt IM LẶNG** ở `limit` ⇒ "khoa mình chỉ có 20 KTV" trong khi có 47. Chốt: curate op `listAssignableUsers` vào OAS (**paths 107→108 · schemas 281→283 · parameters GIỮ 38**) + `data` đổi từ **mảng trần** sang **object** `{items,total,truncated,limit}` derive qua SSoT `truncation_meta`; `truncated` **integer 0|1** (KHÔNG boolean — crash codegen); `total` đếm **SAU** lọc năng lực (ADR-IMM00-ASSIGN-02 — đếm trước = tái sinh `count != rows`); `context` lạ ⇒ **400 in-envelope** trên HTTP-200 (KHÔNG LOGOUT); FE render dải "Đang hiển thị N/M người — gõ tên để tìm thêm". Guard `test_mobile_oas` **967→975** (`cr80_a..h`, mutation-verified ×3). Spec: [ADR-IMM00-TRUNCATION-SSOT §7](./ADR-IMM00-TRUNCATION-SSOT.md) · [05 §III.23](./05_API_Specification.md) · [04 §V.6](./04_Backend_Design.md) · [06 §VIII.3](./06_Frontend_Design.md) · [07 §XVII](./07_Testing_QA.md) · [02 §IV.38](./02_Analysis_Design.md)) · 2026-07-25 (**🔒 INV-ROWSCOPE [BA/Trục-A security, cross-cutting IMM-00/08/09] — row-scope list invariant:** `BaseRepository.list` đếm `total` bằng `frappe.get_list` (permission-aware, `services/shared/filters.py:275-281`) nhưng lấy `rows` bằng `frappe.get_all` (`repositories/base.py:67-75`, **KHÔNG** áp `permission_query_conditions`) ⇒ với 4 DocType WO còn row-scope, **count < rows + KTV ĐỌC được phiếu KHÔNG được giao** trong khi write-gate (`_assert_can_attach_repair_photo`) vẫn chặn ⇒ vỡ "đọc được ⇒ ghi được" + "Tổng N" ≠ số dòng. Chốt: `BaseRepository.list(scope="user"|"system")` (mặc định `"user"` = fail-safe) + counter cặp `count_ignore_permissions`; 16 call site trên 5 DocType row-scoped khai `scope` tường minh; card-đếm pair-với-drill phải cùng scope (D7); lỗi quyền = **in-handler 403 trên HTTP-200** (BR-00-ROWSCOPE-403), KHÔNG 500/KHÔNG list rỗng giả; **KHÔNG sửa `*_query`, KHÔNG nới DocPerm**. Spec: [ADR-IMM00-LIST-SCOPE §8](./ADR-IMM00-LIST-SCOPE.md) · [04 §III.2 `repositories/base.py`](./04_Backend_Design.md) · [07 §XVI TC-00-RS-01..10](./07_Testing_QA.md) · IMM-09 [BR-09-LISTSCOPE](../imm-09/05_API_Specification.md) + [06 §VI ListTotal](../imm-09/06_Frontend_Design.md) · IMM-08 [BR-08-LISTSCOPE](../imm-08/05_API_Specification.md)) · 2026-07-15 (**🔒 CR-WF-00-EDIT-AUTHZ [Vòng 46, BA/Trục-A security] — missing-authorization write + custody-hole ở CHỈNH-SỬA phiếu Điều chuyển:** `update_transfer` (`api/imm00.py:2616`) CHỈ check `status=='Pending Approval'` (422) rồi `_generic_update`, THIẾU `rbac.require` (P1 — mọi user login, kể cả chỉ `inventory.read`, đổi được đích/khoa/người-nhận/ngày/lý do/ghi-chú phiếu Pending → 200 = custody-hole). HOÀN TẤT bộ-bốn transfer-authz (approve/reject=`commissioning.submit`; receive/cancel/edit=`commissioning.write`). Fix: (1) gate `update_transfer` bằng `_TRANSFER_EDIT_CAP="commissioning.write"` đặt ĐẦU thân handler (mirror `transition_status` `:1163` — rbac TRƯỚC exists, no existence-oracle; base user → `frappe.PermissionError` HTTP-403 cap-403 REACHABLE); (2) status-gate 422 GIỮ NGUYÊN cho user CÓ cap trên phiếu non-Pending (KHÔNG bị rbac che thành 403); (3) not-found nay 404 (trước 422 sai-nghĩa); (4) `transfer_cta_flags` emit `can_edit` (int 0/1 = `commissioning.write` ∧ status=='Pending Approval'), `get_transfer_full` auto-echo; (5) FE `AssetTransferDetailView.vue` `isEditable` ← `!!form.value.can_edit` (thay `isPending` `:26`) → form disabled + nút "Lưu thay đổi" ẩn khi `can_edit=0`. INVARIANT button-affordance ⇔ action parity. RED-first: trước fix user `inventory.read` sửa THÀNH CÔNG + field đổi thật. **0 CAP_SET_VERSION bump** (`commissioning.write` đã có) → **0 migrate**. `update_transfer`/`get_transfer_full` KHÔNG có trong mobile OAS → 0 mobile drift. BA doc-only (Bước-2). ⚠️ `api/imm00.py` edit cần **gunicorn reload** (HARD-STOP user); KHÔNG commit/migrate. Spec: [04 §II.1.13-EDITAUTHZ + ADR-IMM00-EDIT-AUTHZ](./04_Backend_Design.md) · [05 §III.12-EDITAUTHZ](./05_API_Specification.md) · [06 §II.3a-EDITAUTHZ](./06_Frontend_Design.md) · [02 IV.37 / FR-00-TRF-04 / BR-00-TRF-04](./02_Analysis_Design.md) · [07 §XIV-EDIT TC-00-TRF-EDIT-01..06](./07_Testing_QA.md)) · 2026-07-15 (**🔒 CR-WF-00-CANCEL-AUTHZ [Vòng 41, BA/Trục-A security] — missing-authz + silent-audit-loss ở hủy phiếu Điều chuyển:** `cancel_transfer_request` (`services/imm00.py:2788`) THIẾU `rbac.require` (P1 — mọi user login hủy phiếu `Pending/Rejected` được, kể cả base) + THIẾU `log_audit_event` (P1 — hủy KHÔNG để dấu vết, vi phạm CLAUDE.md §5 + NĐ98). Đóng backlog CR-WF-00-TRANSFER-AUTHZ tách ra. Fix: (1) gate `cancel_transfer_request` bằng `_TRANSFER_CANCEL_CAP="commissioning.write"` đặt SAU exists TRƯỚC status (mirror `confirm_receipt` `:2757` — base user hủy phiếu SAI status vẫn 403, KHÔNG rò trạng thái; phiếu∄ → NOT-FOUND); (2) sinh ĐÚNG 1 `IMM Audit Trail` (`event_type='Transfer'`, `change_summary` chứa 'Hủy') mỗi lần hủy; (3) `transfer_cta_flags` emit `can_cancel` (int 0/1 = `commissioning.write` ∧ status∈{Pending Approval, Rejected}), `get_transfer_full` auto-echo; (4) FE `AssetTransferDetailView.vue` nút "Hủy phiếu" gate `canCancel` (thay `isPending`) + hint no-actions. RED-first: trước fix base user hủy THÀNH CÔNG + 0 audit. **0 CAP_SET_VERSION bump** (`commissioning.write` đã có) → **0 migrate**. `delete_transfer`/`get_transfer_full` KHÔNG có trong mobile OAS → 0 mobile drift. BA doc-only (Bước-2). ⚠️ `services/imm00.py` edit cần **gunicorn reload** (HARD-STOP user); KHÔNG commit/migrate. Spec: [04 §II.1.13-CANCELAUTHZ + ADR-IMM00-CANCEL-AUTHZ](./04_Backend_Design.md) · [05 §III.12-CANCELAUTHZ](./05_API_Specification.md) · [06 §II.3a-CANCELAUTHZ](./06_Frontend_Design.md) · [02 IV.36 / FR-00-TRF-03 / BR-00-TRF-03](./02_Analysis_Design.md) · [07 §XIV TC-00-TRF-CANCEL](./07_Testing_QA.md)) · 2026-07-15 (**🔒 CR-WF-00-TRANSFER-AUTHZ [Vòng 48, BA/Trục-A security] — missing-auth-write + dead-button phiếu Điều chuyển:** `confirm_receipt` (`services/imm00.py:2722`) THIẾU `rbac.require` (P1 — mọi user login xác nhận tiếp nhận `Approved→Received` được, kể cả base) trong khi approve/reject gate `commissioning.submit`. Fix: (1) gate `confirm_receipt` bằng `_TRANSFER_RECEIVE_CAP="commissioning.write"` (≠ approve — least-privilege: người NHẬN Commissioning User write=1/submit=0 nhận được nhưng không duyệt; base fail-closed); (2) `get_transfer_full` emit `can_approve`/`can_receive` (int 0/1) qua SoT `transfer_cta_flags` (parity cap constant read↔gate, mirror imm14 R39); (3) FE `AssetTransferDetailView.vue` gate 3 nút CTA theo flag (fail-closed undefined). RED-first (base user xác nhận = false-pass trước fix). **0 CAP_SET_VERSION bump** (`commissioning.write` đã có) → **0 migrate**. ⚠️ Mobile OAS `receiveTransfer` mô tả "403 dispatcher-only" nay STALE (403 REACHABLE) — doc-accuracy delta backlog mobile-BE; `test_mobile_oas.py` GIỮ XANH (shape 403 không đổi). BA doc-only (Bước-2). ⚠️ `services/imm00.py`+`api/imm00.py` edit cần **gunicorn reload** (HARD-STOP user); KHÔNG commit/migrate. Spec: [04 §II.1.13-TRANSFERAUTHZ + ADR-IMM00-TRANSFER-AUTHZ](./04_Backend_Design.md) · [05 §III.12-AUTHZ](./05_API_Specification.md) · [06 §II.3a-TRANSFERAUTHZ](./06_Frontend_Design.md) · [02 IV.35 / FR-00-TRF-02 / BR-00-TRF-02](./02_Analysis_Design.md) · [07 §XIII TC-00-TRF-AUTHZ](./07_Testing_QA.md)) · 2026-07-14 (**🔒 CR-WF-00-LIFECYCLE-SURFACE [Vòng 41, BA/Trục-A] — Surface `allowed_transitions` server-driven vào `get_asset` + xóa bảng transition HARDCODE FE `AssetDetailView.vue:154-163`:** `get_asset` emit field `allowed_transitions: string[]` = `_VALID_ASSET_TRANSITIONS[status]` **loại HẲN target `Decommissioned`** (thanh lý qua nút riêng "Hồ sơ giải nhiệm" IMM-14 closure) − terminal, sorted, LỌC theo capability caller `asset.write` (read-only→[]) — mirror precedent `firmware_allowed_transitions`. FE xóa `const TRANSITIONS`, render nút CHỈ từ `store.currentAsset.allowed_transitions`. **Self-Correction:** công thức acceptance gốc (`− _LIFECYCLE_EXCEPTION_EDGES − terminal`) THIẾU — `Active→Decommissioned` + `Out of Service→Decommissioned` là cạnh valid ĐÃ surface Desk `"Thanh lý"` (KHÔNG trong exception) nên `− exception` VẪN leak Decommissioned; loại-hẳn-Decommissioned bao trùm cả exception LẪN 2 cạnh Thanh lý (ground truth = bảng FE đang xóa). Single-SSoT: `_surfaceable_asset_transitions` DRIVER cho reconcile-test 13280 (thay inline) + `asset_allowed_transitions` DRIVER cho get_asset emit — 0 bản-sao-thứ-2 (BE/FE). 0 cap/schema/enum/migration delta (`CAP_SET_VERSION` GIỮ). BA doc-only (Bước-2) — delta ĐỦ để BE/FE Bước-4. ⚠️ `api/imm00.py`+`services/imm00.py` edit cần **gunicorn reload** (HARD-STOP user); KHÔNG commit/migrate. Spec: [04 §II.1.7-SURFACE + ADR-IMM00-LIFECYCLE-SURFACE](./04_Backend_Design.md) · [05 §get_asset](./05_API_Specification.md) · [06 §III.3-SURFACE](./06_Frontend_Design.md) · [02 IV.34 / FR-00-109 / BR-00-58](./02_Analysis_Design.md) · [07 §XII TC-00-WF-SURFACE](./07_Testing_QA.md)) · 2026-07-14 (**🔒 CR-WF-00-TRANSITION-AUTHZ [Vòng 39, BA/Trục-A security] — Bịt lỗ hổng phân quyền `transition_status` (đổi lifecycle_status AC Asset, missing-authorization write / OWASP A01):** endpoint `api/imm00.py::transition_status` (`:1128`) TRƯỚC Vòng 39 chỉ `frappe.db.exists`→gọi thẳng service ⇒ MỌI user login (kể cả base `AssetCore System User`/vendor ngoài scope) POST là đổi được trạng thái vòng đời bất kỳ asset (FE `AssetDetailView.vue:480` gate `can('asset.write')` chỉ ẩn-nút, KHÔNG chặn HTTP). Fix = SIẾT 3-lớp **MIRROR `get_asset`**: (0) `rbac.require("asset.write")` CÂU-ĐẦU TRƯỚC `exists` → cap-403 **status-line KHÔNG 404** (no existence-oracle), DB bất biến; (1) exists 404; (2) `assert_vendor_can_access`→`ServiceError(FORBIDDEN)`→`_err` (403 in-handler envelope), DB bất biến. **SERVICE `transition_asset_status` GIỮ perm-free** (WO-complete IMM-08/09/11/12/14 + import gọi thẳng service — gate CHỈ ở endpoint ⇒ 0 blast-radius). cap-403 = **raise-based** (parity get_asset + họ `asset.*`; SUPERSEDE dự đoán cũ "cap-403=200-envelope" — ADR-IMM00-TRANSITION-AUTHZ). 0 cap/schema/enum/migration delta (`asset.write` loop-gen sẵn; `CAP_SET_VERSION` GIỮ). AC1 no-write→403+no-mutation · AC2 vendor-OOS→403 · AC3 no-cap+∄→403-not-404 · AC4 happy-path holder→ok+1 ALE+1 audit · AC5 test_imm08/09 WO-complete XANH · AC6 `bench run-tests` Ran N OK. BA doc-only (Bước-2) — delta ĐỦ để BE Bước-4: thêm `rbac.require`+khối try/except vendor vào `transition_status` (mirror get_asset `:471-496`) + class `TestTransitionStatusAuthz`. ⚠️ `api/imm00.py` edit cần **gunicorn reload** (HARD-STOP user); KHÔNG commit/migrate. Spec: [04 §II.1.7-AUTHZ + ADR-IMM00-TRANSITION-AUTHZ](./04_Backend_Design.md) · [05 §transition_status + Permission Matrix §](./05_API_Specification.md) · [02 BR-00-57 / IV.33 / FR-00-108](./02_Analysis_Design.md) · [07 §III.6.0e-TRANSITIONAUTHZ](./07_Testing_QA.md)) · 2026-07-14 (**📱 Mobile-BE CR-30 NOTIF-PREF [Vòng 38, BA/Trục-B mobile OAS] — cặp `getNotificationPreferences` (GET) + `setEmailEnabled` (POST) curate OAS mirror (màn F3 "Tài khoản → Cài đặt nhận thông báo"): +2 path (77→79) + 4 schema (`NotificationPreferences` **additionalProperties:TRUE** forward-reserve loại push khác / `NotificationPreferencesEnvelope` closed / `SetEmailEnabledEnvelope` closed echo / `SetEmailEnabledRequest` closed `required[enabled]`) + param `NotificationPrefUser`; 200 = oneOf[Envelope, Error] Decision-B route-by-VALUE. **Self-Correction 2-loại-403 (ADR-IMM00-OAS-NOTIFPREF-B): `setEmailEnabled` cap-403 (admin→user khác) = in-handler HTTP-200 Error-branch mirror `markNotificationAsRead` — KHÔNG `cancelCalibration` status-line (sai precedent về cơ-chế: `rbac.require`→PermissionError vs `ServiceError`→handle→_err)**. Backlog: getter KHÔNG admin-guard `user=`. CONTRACT-ONLY (0 .py/reload/migrate). Spec `05_API_Specification.md §III.21b`.** · **📱 Mobile-BE CR-TRANSFER-CREATE-01 [Vòng 37, BA/Trục-B mobile OAS] — `createTransfer` (`imm00.create_transfer` → `create_transfer_request`) curate OAS mirror [transfer CREATE-action ĐẦU TIÊN domain Điều chuyển]:** `createTransfer` bồi ĐÚNG 1 POST path TẠO phiếu yêu cầu điều chuyển thiết bị `∅ → Pending Approval` màn "Điều chuyển" luồng KHỞI TẠO feature-12 — **HOÀN TẤT transfer write-action quartet** (`receiveTransfer` ADR-043 / `approveTransfer` ADR-044 / `rejectTransfer` ADR-045 / create). GROUNDED handler LIVE `create_transfer()` @`api/imm00.py:2124-2130` (**0-param** đọc `form_dict` @`:2126`; `try _ok(create_transfer_request(data)) / except ValidationError → _err(str(e),422)` @`:2128-2130`) + service `create_transfer_request(data)` @`services/imm00.py:2568-2612` (`required=("asset","transfer_type","to_department","reason")` @`:2574`; `from_*` auto-derive @`:2592-2594`; `doc.status=_TRANSFER_STATUS_PENDING` @`:2601`; return EXACT 2-key `{name,status}` @`:2612`, `status='Pending Approval'` @`:2561`). 3 schema RIÊNG closed: `CreateTransferRequest` `req[asset,transfer_type,to_department,reason]` props EXACT **9** (4-req + 5-opt) — **⚠️ ĐIỂM KHÁC #1: `from_location`/`from_department`/`from_custodian` KHÔNG khai** (SERVER auto-derive từ asset hiện tại, `additionalProperties:false` chặn); `CreateTransferResponse` EXACT **2-prop** `{name,status}` — `status` enum single-value `['Pending Approval']` GROUNDED verbatim hằng `_TRANSFER_STATUS_PENDING`; `CreateTransferEnvelope` (tên RÚT-GỌN transfer-family, KHÔNG `...CreatedEnvelope`). **200 = `oneOf[CreateTransferEnvelope, Error]`** Decision-B; requestBody 2 media-type json+form; slot `{200,401,403}`. **⚠️ 3 ĐIỂM KHÁC CỐT-LÕI vs 3 write-action trước:** (1) CREATE sinh record MỚI — 0 name-param, request RICHEST 9-prop; (2) **`∈ _MVP_CREATE_ENVELOPE`** (mirror `createRepairWorkOrder`/`createCalibration`) KHÁC receive/approve/reject `∈ _MVP_ACTION_ENVELOPE`; (3) **403 SINGLE `Forbidden` dispatcher-ONLY** (0 cap-403 — KHÔNG `rbac.require`; mirror `receiveTransfer`, KHÁC `approve`/`reject` cap-403 REACHABLE). **⚠️ ANTI-DRIFT `Error.http_status` = 422 ĐỒNG NHẤT** cho CẢ 2 nhánh (missing-required @`:2577` + asset∄ @`:2581`; KHÔNG 404). path/opId `73→74` + c5 `62→63` + parity `62→63`; naming-guard `CreateTransfer*` ∩ ∅. **🟢 CONTRACT-ONLY** `create_transfer` (7 dòng) + `create_transfer_request` (45 dòng) byte-identical HEAD↔working (AST-extract) ⇒ KHÔNG reload/migrate/commit. Guard `TestMobileCreateTransferContract` a..j 10 TC — **test_mobile_oas Ran 704 OK THẬT** (RED-before strip-path → **102 FAIL** → restore → GREEN) · **test_mobile_docset Ran 9 OK** (balance ADR 46==46). ADR-MOBILE-046; §8.48. · PREV **📱 Mobile-BE CR-TRANSFER-REJECT-01 [Vòng 35, BA/Trục-B mobile OAS] — `rejectTransfer` (`imm00.reject_transfer` → `reject_transfer_request`) curate OAS mirror [transfer WRITE-action #3 domain Điều chuyển]:** `rejectTransfer` bồi ĐÚNG 1 POST path TỪ CHỐI phiếu điều chuyển `Pending Approval → Rejected` màn "Điều chuyển – Chờ duyệt" feature-12 luồng DUYỆT — **HOÀN TẤT cặp quyết định duyệt** (`approveTransfer` ADR-044 / reject); `receiveTransfer` (ADR-043) MỞ NHÁNH · approve #2 · reject #3; `create_transfer` FORWARD-RESERVE vòng Trục-B kế. GROUNDED handler LIVE `reject_transfer(name, rejection_reason="")` @`api/imm00.py:2591-2597` + service `reject_transfer_request` @`services/imm00.py:2646-2674` (`rbac.require('commissioning.submit')` @`:2651`; `len(rejection_reason.strip()) < 5 → frappe.throw` @`:2653-2654`; return EXACT 2-key `{name,status}` @`:2674`, `status='Rejected'` @`:2563,2661`). 3 schema RIÊNG closed: `RejectTransferRequest` `req[name, rejection_reason]` — **⚠️ FIRST transfer action có required text-body** `rejection_reason` (`minLength:5` typed-hint, runtime strip-then-≥5; KHÁC `approveTransfer` name-only & `receiveTransfer` `handover_notes` OPTIONAL); `RejectTransferResponse` EXACT **2-prop** `{name,status}` **KHÔNG `rejected_by`/`rejection_reason`** — `status` enum single-value `['Rejected']` GROUNDED verbatim hằng `_TRANSFER_STATUS_REJECTED`; `RejectTransferEnvelope`. **200 = `oneOf[RejectTransferEnvelope, Error]`** Decision-B; requestBody 2 media-type json+form; slot `{200,401,403}`. **⚠️ 3 ĐIỂM KHÁC CỐT-LÕI vs `approveTransfer`:** (1) request có body BẮT BUỘC `rejection_reason`; (2) **NHÁNH 422 THỨ-3** `rejection_reason` thiếu/<5 (ngoài not-found @`:2649` + wrong-status @`:2657-2658`); (3) status enum `['Rejected']`. GIỐNG `approveTransfer`: **403 SINGLE `Forbidden` REACHABLE cap-branch** `commissioning.submit` (`rbac.require` @`:2651` raise `PermissionError` NGOÀI `except-ValidationError` → HTTP-403 THẬT) + **ANTI-DRIFT `Error.http_status` = 422 ĐỒNG NHẤT** 3-nhánh (KHÁC `get_transfer` 404). CONTRACT-ONLY: `reject_transfer` + `reject_transfer_request` **byte-identical HEAD↔working** ⇒ 0 `.py`/reload/migrate. test_mobile_oas **684→694** (+10 TC `TestMobileRejectTransferContract` a..j; RED-before strip-path → 97 FAIL → restore → GREEN) · test_mobile_docset **9 OK** (ADR balance 44→45) · [ADR-MOBILE-045](../mobile/ADR-MOBILE-045.md) · [`05_API_Specification.md` §reject_transfer](./05_API_Specification.md)) · (**📱 Mobile-BE CR-TRANSFER-APPROVE-01 [Vòng 34, BA/Trục-B mobile OAS] — `approveTransfer` (`imm00.approve_transfer` → `approve_transfer_request`) curate OAS mirror [transfer WRITE-action #2 domain Điều chuyển]:** `approveTransfer` bồi ĐÚNG 1 POST path PHÊ DUYỆT phiếu điều chuyển `Pending Approval → Approved` (`transfer_asset` cập vị trí thiết bị NGAY @`:2632-2639`) màn Điều chuyển feature-12 luồng DUYỆT — **write-action #2** sau `receiveTransfer` (ADR-043 MỞ NHÁNH); `reject_transfer`/`create_transfer` FORWARD-RESERVE vòng Trục-B kế. GROUNDED handler LIVE `approve_transfer(name)` @`api/imm00.py:2582-2588` + service `approve_transfer_request` @`services/imm00.py:2615-2643` (`rbac.require('commissioning.submit')` @`:2620`; return EXACT 2-key `{name,status}` @`:2643`, `status='Approved'` @`:2562,2627`). 3 schema RIÊNG closed: `ApproveTransferRequest` `req[name]` **0 optional** (KHÁC `receiveTransfer` `handover_notes`); `ApproveTransferResponse` EXACT **2-prop** `{name,status}` **KHÔNG `approved_by`** (KHÁC `receiveTransfer` 3-prop) — `status` enum single-value `['Approved']` GROUNDED verbatim hằng `_TRANSFER_STATUS_APPROVED`; `ApproveTransferEnvelope`. **200 = `oneOf[ApproveTransferEnvelope, Error]`** Decision-B; requestBody 2 media-type json+form; slot `{200,401,403}`. **⚠️ 2 ĐIỂM KHÁC CỐT-LÕI:** (1) **403 SINGLE `Forbidden` REACHABLE cap-branch** — `rbac.require('commissioning.submit')` @`:2620` raise `PermissionError` NGOÀI `except-ValidationError` → HTTP-403 THẬT (mirror `cancelCalibration`, KHÁC `receiveTransfer` dispatcher-only); (2) **ANTI-DRIFT `Error.http_status` = 422 ĐỒNG NHẤT** cho CẢ phiếu∄ LẪN sai-state (KHÁC `get_transfer` 404). CONTRACT-ONLY: `approve_transfer` + `approve_transfer_request` **byte-identical HEAD↔working** ⇒ 0 `.py`/reload/migrate. test_mobile_oas **674→684** (+10 TC `TestMobileApproveTransferContract` a..j) · test_mobile_docset **9 OK** (ADR balance 43→44) · [ADR-MOBILE-044](../mobile/ADR-MOBILE-044.md). — PREV **📱 Mobile-BE CR-TRANSFER-RECV-01 [Vòng 33, BA/Trục-B mobile OAS] — `receiveTransfer` (`imm00.receive_transfer` → `confirm_receipt`) curate OAS mirror [transfer WRITE-action ĐẦU TIÊN domain Điều chuyển]:** MỞ NHÁNH transfer write-action (READ `getTransfer`/`listTransfers` đã curate — TRANSFER-READ-WIRE; `approve_transfer`/`reject_transfer`/`create_transfer` FORWARD-RESERVE vòng Trục-B kế, mirror `sendToLab` R10). Bồi ĐÚNG 1 POST path `receiveTransfer` xác-nhận-tiếp-nhận thiết bị `Approved → Received` (màn Điều chuyển feature-12 luồng NHẬN scan-confirm) GROUNDED handler LIVE `receive_transfer(name, handover_notes="")` @`api/imm00.py:2600-2606` (`@whitelist(methods=["POST"])`; **KHÔNG `rbac.require` in-handler** ⇒ 0 cap-403; `try _ok(confirm_receipt) / except ValidationError → _err(str(e), 422)`) + service `confirm_receipt` @`services/imm00.py:2677-2708` (return EXACT 3-key `{name,status,received_by}` @`:2708`, `status=_TRANSFER_STATUS_RECEIVED='Received'` @`:2564,2687` + audit `log_audit_event("Transfer")` @`:2695` + `create_lifecycle_event("transferred")` @`:2701`). 3 schema RIÊNG closed: `ReceiveTransferRequest` `req[name]` + `handover_notes` optional **NON-nullable** (default `""` empty-string @`:2601` KHÔNG None); `ReceiveTransferResponse` EXACT 3-prop `{name,status,received_by}` — `status` **enum single-value `['Received']`** GROUNDED verbatim hằng `_TRANSFER_STATUS_RECEIVED` (TC-e import LIVE assert equality, anti-bịa); `ReceiveTransferEnvelope`. **200 = `oneOf[ReceiveTransferEnvelope, Error]`** Decision-B; requestBody 2 media-type json+form; slot `{200,401,403}`. **⚠️ 2 ĐIỂM KHÁC CỐT-LÕI:** (1) **403 SINGLE `Forbidden` dispatcher-ONLY** (0 cap-403 in-handler — mirror `attachIncidentPhoto` ADR-027, KHÁC `sendToLab`/`cancelCalibration`); (2) **ANTI-DRIFT `Error.http_status` = 422 ĐỒNG NHẤT** cho CẢ phiếu∄ @`:2680` LẪN status≠Approved @`:2684` (`frappe.throw`→`ValidationError`→`_err(str(e),422)` @`:2605-2606`, KHÁC `get_transfer` 404 tường minh — `receive_transfer` KHÔNG phát 404). **🟢 CONTRACT-ONLY** pure-yaml BE LIVE (`receive_transfer` + `confirm_receipt` byte-identical HEAD↔working) ⇒ 0 `.py`/reload/migrate. Guard `TestMobileReceiveTransferContract` a..j (10 TC) ⇒ `test_mobile_oas` **664→674** (**Ran 674 OK** THẬT; RED-before strip path → 93 cascade FAIL gồm 6 receiveTransfer TC → restore → GREEN) + `test_mobile_docset` **Ran 9 OK** (`_GUARD_SUITE_SUM` 807→817 · `_MOBILE_OAS_TOTAL` 833→843 · `receive_transfer_wire_delta=10` giữ `pre_fc3_six==191`) + path/opId **70→71** + c5 **59→60** + `_EXPECTED` += `receive_transfer → receiveTransfer` + ADR balance **42==42→43==43**. **⚠️ 10 secondary count-guard** (3 c5 + 1 parity 59→60 + 4 backward-compat opId-set-minus + 2 hardcoded `_EXPECTED_TEST_COUNT==664` + `op_id_unique` 70) KHÔNG dùng token `, 70,` ⇒ full-suite THẬT bắt sót. KHÔNG git commit/reload/migrate (HARD-STOP user). Spec: [05 §III.12 receive_transfer 📱 Mobile-BE cross-ref](./05_API_Specification.md) + [`../mobile/04-api-contract.md` §8.45](../mobile/04-api-contract.md) + [ADR-MOBILE-043](../mobile/ADR-MOBILE-043.md)) · 2026-07-14 (**CR-WF-00-LIFECYCLE [Vòng 32, BA/Trục-A SSoT-divergence] — Đối soát 2-chiều `_VALID_ASSET_TRANSITIONS` (state-machine SSoT trục trung tâm §10) ⇄ `ac_asset_lifecycle_workflow.json` + `fixtures/workflow.json` (workflow DUY NHẤT chưa reconcile qua 31 vòng).** RED-before 7 cạnh drift `map⊋workflow` → phân loại DỨT KHOÁT: **2 SURFACE** (`Commissioned→Out of Service` + `Under Maintenance→Out of Service`; thêm vào cả 2 JSON lockstep, roles `{AssetCore Super Admin, System Manager}`) + **5 EXCEPTION_EDGES** (tất cả →Decommissioned: 3 `NEG-09-superseded` [`Under Maintenance/Under Repair/Calibrating→Decommissioned` chặn runtime imm00.py:1536] + 2 `programmatic-only` [`Draft/Commissioned→Decommissioned` IMM-14-gated; Desk surface = bypass closure NĐ98]). Vá mâu thuẫn helper `is_valid_asset_transition` (imm00.py:71): **trả `False`** cho 3 cạnh NEG-09 (promote `_BLOCK_DECOM_FROM`→hằng module-level SSoT; helper phản chiếu lớp state-machine+NEG-09 cùng ném `InvalidAssetTransition`, KHÔNG IMM-14 gate lớp `ServiceError`). BA doc-only (Bước-2): spec ĐỦ để BE Bước-4 code — INVARIANT test `test_asset_lifecycle_map_matches_workflow` đọc TRỰC-TIẾP workflow JSON edge-by-edge + `test_lifecycle_workflow_source_matches_fixture` lockstep parity + helper-NEG-09 guard; 8 state==8 `AssetStatus` enum; 0 regression 8 test file tham chiếu map. KHÔNG git commit; KHÔNG bench migrate (workflow chỉ cần reload_workflow/backfill live — HARD-STOP user). Spec: [04 §II.1.7 + §II.1.7-RECON + ADR-IMM00-LIFECYCLE-SM](./04_Backend_Design.md) · [02 BR-00-02a/b/c](./02_Analysis_Design.md) · [05 §transition_status error-contract](./05_API_Specification.md) · [07 §XII INVARIANT](./07_Testing_QA.md)) · 2026-07-13 (**📱 Mobile-BE CR-11e — `getAssetDepreciationSchedule` (`imm00.get_depreciation_schedule`) curate OAS mirror [asset-detail sub-tab #5 "Khấu hao"]:** sub-tab THỨ NĂM & **CUỐI** của CR-11 — **HOÀN TẤT bộ-năm** (sau `getAssetKpi` CR-11a + `getAssetDowntimeMetrics` CR-11b + `getAssetVerifyChain` CR-11c + `getAssetCommissioningOrigin` CR-11d) — lịch khấu hao (child `AC Asset Depreciation Schedule`) + tổng-hợp + thông-tin khấu hao 1 asset (dữ-liệu **giá-trị tài-sản**, kế-toán). Bồi 1 GET-read path `getAssetDepreciationSchedule` GROUNDED handler LIVE **self-contained** `get_depreciation_schedule(asset_name)` @`api/imm00.py:2962` (KHÔNG service/`_handle`; `_err(_("Asset not found"), 404)` asset∄ @`:2965` → HTTP-200 nhánh Error Decision-B). **LẦN ĐẦU WRAPPER 4-key với 3 nested-schema** (payload phức-nhất bộ CR-11): `AssetDepreciationSchedule {asset:string, asset_info:object|null (nullable-ref idiom `{}` coalesce @`:2988` mirror ADR-041/039), rows:DepreciationScheduleRow[], summary:DepreciationScheduleSummary}` required=cả 4; **`DepreciationScheduleRow`** closed EXACT 9-prop VERBATIM get_all @`:2969-2971` [`name` req · 3 Currency **FINANCIAL** number nullable · `status` **enum[Pending,Executed,Cancelled]** Select bounded no-blank default 'Pending' **DB-verified 413/79/42 0-blank/534** bám ADR-028 · 2 Date `format:date`] required=EXACT`[name]`; **`DepreciationScheduleSummary`** closed EXACT 4-prop compute @`:2975-2981` [3 integer + `total_depreciated` **FINANCIAL** number] required=đủ 4; **`AssetDepreciationInfo`** closed EXACT 9-prop VERBATIM get_value @`:2984-2986` [4 Currency **FINANCIAL** number nullable · `depreciation_method`/`depreciation_frequency` string nullable **NO-enum** Select LEADING-BLANK `''` hợp-lệ ADR-028 sub-case · 1 Int · 2 Date] **required=∅** TOÀN value-nullable convention `getAssetKpi`; **200 = `oneOf[AssetDepreciationScheduleEnvelope, Error]`** Decision-B; **⚠️ param `asset_name` REQUIRED KHÔNG `name`** (positional no-default @`:2962`, KHÁC sibling year OPTIONAL); slot `{200,401,403}` (403 SINGLE-SHAPE dispatcher-only). Read-only **KHÔNG audit**. **🟢 CONTRACT-ONLY** pure-yaml BE LIVE (SCOPED-HANDLER invariant `get_depreciation_schedule` HEAD↔working) ⇒ 0 `.py`/reload/migrate. Guard `TestMobileAssetDepreciationScheduleCurate` a..h (8 TC; e/f GROUNDING per-doctype get_all/get_value field-list + dict-literal summary/wrapper + doctype json status Select đọc-TRỰC-TIẾP) ⇒ BE Bước-4 `test_mobile_oas` **656→664** + `test_mobile_docset` `_GUARD_SUITE_SUM` **799→807** / `_MOBILE_OAS_TOTAL` **825→833** + delta `asset_depreciation_schedule_curate_delta=8` (giữ `pre_fc3_six==191`) + path/opId **69→70** + c5 **58→59** + `_EXPECTED` convention-map += `get_depreciation_schedule → getAssetDepreciationSchedule` + ADR-MOBILE-042. **⚠️ 10 secondary count-guard** (c5/parity 58→59, backward-compat opId-set-minus 68→69/67→68, push `len(paths)==len(_EXPECTED)`, `test_05_convention`) KHÔNG dùng token `, 69,` ⇒ full-suite THẬT bắt sót. RED-before demo: strip path → 93 cascade FAIL → restore → **Ran 664 OK**. **PREV CR-11c — `getAssetVerifyChain` (`imm00.verify_chain`) curate OAS mirror [asset-detail sub-tab #3 "Chuỗi kiểm toán bất biến"]:** sub-tab THỨ BA của CR-11 (sau `getAssetKpi` CR-11a + `getAssetDowntimeMetrics` CR-11b) — kiểm-tra tính-toàn-vẹn hash-chain SHA-256 của `IMM Audit Trail` 1 asset (bằng-chứng truy-xuất-nguồn-gốc **NĐ98/2021**, BR-00-03 / `AC-E010`). Bồi ĐÚNG 1 GET-read path `getAssetVerifyChain` GROUNDED **HAI hàm-nguồn** LIVE: handler `verify_chain(asset)` @`api/imm00.py:1768-1774` (bare `@whitelist`; `_err(_ERR_ASSET_NOT_FOUND, 404)` asset∄ @`:1771-1772`; `_ok(verify_audit_chain(asset))` @`:1774`) + builder `verify_audit_chain(asset)` @`utils/lifecycle.py:97-114` (**SoT return-shape** — recompute SHA-256 hash-chain; **2 return-shape**: FAIL `{valid:False, broken_at, index, count}` @`:112` / PASS `{valid:True, count}` @`:114`). `AssetVerifyChain` closed `additionalProperties:false` **EXACT 4-prop** = HỢP 2 shape `{valid:boolean-GENUINE, count:integer, broken_at:string, index:integer}`; **`required` = EXACT 2 `{valid, count}`** = GIAO 2 shape; **`broken_at`+`index` OPTIONAL** ∉ required + `nullable:true` — CHỈ present khi `valid=false` (**LẦN ĐẦU success-data schema có property OPTIONAL genuine-absent**, mirror ADR-036 optional-emit); **INV-VC-1** prose-only `broken_at`⟺`index`⟺`valid=false` (3.0.3 KHÔNG mã-hoá — né discriminator/if-then); `AssetVerifyChainEnvelope`; **200 = `oneOf[AssetVerifyChainEnvelope, Error]`** Decision-B; **⚠️ param `asset` KHÔNG `name`/`asset_name`**; slot `{200,401,403}` (403 SINGLE-SHAPE dispatcher-only). Read-only **KHÔNG audit** (verify KHÔNG mutate). **🟢 CONTRACT-ONLY** pure-yaml BE LIVE (DUAL SCOPED-HANDLER invariant CẢ `verify_chain`+`verify_audit_chain` HEAD↔working) ⇒ 0 `.py`/reload/migrate. Guard `TestMobileAssetVerifyChainCurate` a..h (8 TC; g=grounding-2-return-shape đọc-TRỰC-TIẾP `utils/lifecycle.py`) ⇒ BE Bước-4 `test_mobile_oas` **640→648** + `test_mobile_docset` sync (`_GUARD_SUITE_SUM` 783→791 · `_MOBILE_OAS_TOTAL` 809→817 · `asset_verify_chain_curate_delta=8`). path/opId **67→68** + c5 **56→57** + README balance **39==39→40==40**. Spec: [05 §III.20 Mobile-BE cross-ref](./05_API_Specification.md) + [`../mobile/04-api-contract.md` §8.42](../mobile/04-api-contract.md) + [ADR-MOBILE-040](../mobile/ADR-MOBILE-040.md)) · 2026-07-13 (**📱 Mobile-BE CR-11b — `getAssetDowntimeMetrics` curate OAS mirror [asset-detail sub-tab #2 "Dừng máy"]:** sub-tab THỨ HAI của CR-11 (sau `getAssetKpi` CR-11a). Bồi ĐÚNG 1 GET-read path `getAssetDowntimeMetrics` (thống-kê dừng máy 8-key OBJECT PHẲNG của asset) vào OAS mirror — GROUNDED handler LIVE `get_asset_downtime_metrics(asset_name, year="")` @`api/imm00.py:2892-2945` (`_ok({8-key})` @`:2936-2944`: đọc `AC Asset Downtime Log` filter `asset`+`start_time between [year]` → tổng giờ + phân-loại reason + log đang-mở). `AssetDowntimeMetrics` closed EXACT 8-prop VERBATIM `required`=cả-8 `{asset:string, year:integer, total_hours:number, breakdown_count:integer, mttr_hours:number, by_reason:OPEN-MAP, current_open:nullable-ref, logs:array}`; ⚠️ **`by_reason` = open-map `additionalProperties:{type:number}`** (dict động reason→giờ — LẦN ĐẦU open-map number trong success-data, **ADR EXCEPTION**); `current_open` = nullable `$ref AssetDowntimeLogOpen` (OpenAPI 3.0.3 `{nullable:true, allOf:[$ref]}`); sub-schema `AssetDowntimeLog` 8-field closed (`is_open` integer enum[0,1] int-vs-bool trap; `reason` plain string; `start_time/end_time` string no-date-time) + `AssetDowntimeLogOpen` = Log 8 + `downtime_hours_so_far` number; `logs[]` items `AssetDowntimeLog` (≤10). 200 = `oneOf[AssetDowntimeMetricsEnvelope, Error]` Decision-B (`_err(404)` asset∄ @`:2901-2902`); **⚠️ param `asset_name` KHÔNG `name`** + `year` typed-query optional (parity CR-05); slot `{200,401,403}`. **🟢 CONTRACT-ONLY** pure-yaml BE LIVE (SCOPED-HANDLER invariant) ⇒ 0 `.py`/reload/migrate. Guard `TestMobileAssetDowntimeMetricsCurate` a..h (8 TC) ⇒ BE Bước-4 `test_mobile_oas` **632→640** + `test_mobile_docset` sync. path/opId **66→67** + c5 **55→56** (⚠️ Reconciliation: đề mục ghi STALE 65→66/ADR-038/38==38 — getAssetKpi ĐÃ tiêu 038/66/55; CR-11b = **ADR-039**/**66→67**/**39==39**). Spec: [05 §III.19 Mobile-BE cross-ref](./05_API_Specification.md) + [`../mobile/04-api-contract.md` §8.41](../mobile/04-api-contract.md) + [ADR-MOBILE-039](../mobile/ADR-MOBILE-039.md)) · 2026-07-13 (**📱 Mobile-BE CR-11a — `getAssetKpi` curate OAS mirror [asset-detail sub-tab KPI]:** curate ĐẦU TIÊN của CR-11 (MỞ NHÁNH asset-detail sub-tab; 5 sub-tab backend LIVE 0 curated: `kpi`/`verify_chain`/`depreciation`/`commissioning`/`downtime`). Bồi ĐÚNG 1 GET-read path `getAssetKpi` (KPI vận-hành 12-key OBJECT PHẲNG của asset) vào OAS mirror — GROUNDED handler LIVE `get_asset_kpi(name)` @`api/imm00.py:1174-1263` (`_ok({12-key})` compute on-the-fly 12 tháng: uptime/downtime `AC Asset Downtime Log` + MTTR/MTBF/`total_repair_cost` `Asset Repair docstatus=1` + `pm_compliance_pct` `PM Work Order`). `AssetKpi` closed EXACT 12-prop VERBATIM return-dict `required` 5 always-present + nullable 7 return-None-hợp-lệ (`breakdown_count`=integer + 6 number + `name`/`lifecycle_status`/3-date string); ⚠️ `total_repair_cost` = FINANCIAL nullable curate VERBATIM UI-render deferred persona-gate FE. 200 = `oneOf[AssetKpiEnvelope,Error]` Decision-B (`_err(404)` asset∄ @`:1185-1186`); slot `{200,401,403}`. **🟢 CONTRACT-ONLY** pure-yaml BE LIVE (TC-j SCOPED-HANDLER invariant) ⇒ 0 `.py`/reload/migrate. Guard `TestMobileGetAssetKpiContract` a..j (10 TC) ⇒ `test_mobile_oas` **Ran 632 OK** + `test_mobile_docset` **Ran 9 OK**; RED-before strip `AssetKpi`→dangling→restore→GREEN. path/opId 65→66 + c5 54→55. Spec: [05 §III.1 get_asset_kpi Mobile-BE cross-ref](./05_API_Specification.md) + [`../mobile/04-api-contract.md` §8.40](../mobile/04-api-contract.md) + [ADR-MOBILE-038](../mobile/ADR-MOBILE-038.md)) · 2026-06-12 (**ADR-IMM00-QR-SCAN-ACTION §D14 [GATE, Vòng 39] — Self-Correction lỗi thiết kế gốc: bất đối xứng a11y CÙNG card định danh + test bám heuristic `rounded-full` mong manh (FE-only).** Status pill lifecycle ở màn quét QR (`AssetScanInfoView.vue:445-450`) render `{{ statusLabel }}`+`:class="statusClass"` NHƯNG (a) thiếu `role`/`aria-label` trong khi 2 badge "Quá hạn bảo trì"/"Quá hạn hiệu chuẩn" CÙNG màn đã có (vòng 27/28) ⇒ trạng-thái-cốt-lõi truyền tải CHỈ bằng màu chip = vi phạm WCAG 1.4.1 (mù-màu/screen-reader); (b) thiếu `data-test` ⇒ test bám `findAll('span').find(...'rounded-full'...)` NHƯNG `rounded-full` xuất hiện **5 lần** (pill + 2 overdue-badge + CTA-urgency chip + nút CTA) → match NHẦM. FIX (FE-only, 3 attr trên `<span>` pill, `statusLabel`/`statusClass`/class màu GIỮ NGUYÊN byte-for-byte): `data-test="scan-status"` (anchor ổn định, đúng-1-pill) + `role="status"` + `:aria-label="`Trạng thái thiết bị: ${statusLabel}`"` — aria-label dùng CHUNG `statusLabel` (SSoT `lifecycleStatusLabel`, KHÔNG rải literal/hardcode wording riêng). No-EN/raw-code/empty leak parity §II.3e-PILLNOLEAK (vòng 8): `lifecycle_status` rỗng/lạ (`'In Use'`/`'LegacyUnknown'`/`''`) → CẢ text pill LẪN aria-label = `'Trạng thái thiết bị: Không xác định'`, KHÔNG leak mã EN trong aria-label. Guard exactly-one: `findAll('[data-test="scan-status"]').length===1` (CTA buttons + 2 overdue badges + CTA-urgency chip KHÔNG nhận selector). KHÔNG đụng BE / payload / `constants/labels.ts` logic / RBAC; `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`; KHÔNG migration/reload/BE-test. Verify `vitest AssetScanInfoView` (RED-first: anchor+role+aria-label FAIL trước thêm attr; no-EN/empty-leak aria-label; exactly-one; overdue-badge a11y regression) + `vue-tsc` 0 + full asset-domain vitest no-regression. **Playwright/quét-QR-thật BLOCKED reload gunicorn --preload (HARD-STOP USER), KHÔNG đánh DONE live.** KHÔNG commit. Tasks FE-9/QA-12. FR-00-104 / BR-00-53 / 02 §IV.29 / 06 §II.3e-PILLA11Y / 07 §III.6.n-PILLA11Y / ADR-IMM00-QR-SCAN-ACTION §D14) · 2026-06-12 (**ADR-IMM00-QR-SCAN-ACTION §D13 [GATE, Vòng 37] — Self-Correction lỗi thiết kế gốc: bất đối xứng định danh truy xuất NĐ98 giữa 2 đường song sinh.** Đường label-PDF `build_asset_label_data` ĐÃ mang `manufacturer_sn` (Số serial NSX) làm định danh truy xuất NĐ98 trên tem in (D5), NHƯNG đường scan-info `build_asset_scan_info` — payload màn KTV cầm điện thoại quét tem point-of-care — KHÔNG trả `manufacturer_sn`. Bất đối xứng CÙNG 1 thiết bị: 2 máy cùng model dán nhầm/bong tem → KTV chỉ thấy `asset_code`+tên màn quét → KHÔNG đối chiếu serial NSX khắc thân máy ⇒ rủi ro báo hỏng/tạo WO nhầm thiết bị vật lý = sai hồ sơ truy xuất. FIX (BE+FE, KHÔNG schema mới — `manufacturer_sn` đã là field AC Asset `ac_asset.json:296`): **(BE)** `build_asset_scan_info` thêm `"manufacturer_sn"` vào CÙNG `frappe.db.get_value(...as_dict)` (no extra round-trip/no-N+1) + key payload `row.get("manufacturer_sn") or ""` (coalesce `''` parity asset_code/asset_name — str, KHÔNG None/raw-object); early-return rỗng-name→None GIỮ; BR-00-34 `'qr_token' not in payload` GIỮ; KHÔNG field nhạy cảm mới. **(FE)** type `AssetScanInfo` thêm `manufacturer_sn: string`; `AssetScanInfoView.vue` computed `serialText = (info.value?.manufacturer_sn ?? '').trim() || SERIAL_UNKNOWN` (hằng mới `SERIAL_UNKNOWN='Chưa rõ'`) render dòng "Số serial NSX" (`data-test="scan-serial"`); có-giá-trị→nguyên văn; rỗng/null/undefined/whitespace→`'Chưa rõ'` (KHÔNG `'—'` câm, KHÔNG leak `'null'`); TUYỆT ĐỐI KHÔNG fallback `info.name` (docname nội bộ). Payload **11→12-field**; `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`; KHÔNG migration/reload. Verify `bench run-tests test_imm00` (`TestScanInfoManufacturerSn`: payload-key str∀case + no-N+1 + no-leak, RED-first) + `vitest AssetScanInfoView` (serial render + empty-fallback `'Chưa rõ'` + no-docname-leak, RED-first) + `vue-tsc` 0. **HTTP/Playwright/quét-QR-thật BLOCKED reload (api/imm00.py edit cần gunicorn reload — HARD-STOP USER), KHÔNG đánh DONE.** KHÔNG commit. Tasks BE-8/FE-8/QA-11. FR-00-103 / BR-00-52 / 02 §IV.28 / 04 §II.1.8d-SCANSN / 05 §get_asset_scan_info payload 12-field / 06 §II.3d-SERIALSN / 07 §III.6.m-SCANSN / ADR-IMM00-QR-SCAN-ACTION §D13) · 2026-06-12 (**ADR-IMM00-QR-SCAN-ACTION §D11 [GATE, Vòng 25] — Self-Correction lỗi thiết kế gốc: panel meta 5-field kéo full-doc tài chính (over-fetch).** 3 màn tạo WO đến từ deep-link QR scan-action (CM/Hiệu chuẩn/PM) render panel meta ~5 field nhưng nạp qua `getAsset(name)` → BE `get_asset` trả `frappe.get_doc(...).as_dict()` = TOÀN BỘ field AC Asset gồm `gross_purchase_amount`/`accumulated_depreciation`/`current_book_value` (chỉ pop `qr_token`) → tài chính-nhạy-cảm lọt xuống mọi persona vận hành chỉ để render 5-field = over-fetch + bề-mặt-rò (DevTools/cache giữ raw JSON tài chính). FIX: endpoint MỚI `get_asset_action_meta(name: str = "")` (`api/imm00.py` THÊM MỚI) trả ĐÚNG **6 key allowlist** `{name, asset_name, device_model_name, lifecycle_status, risk_classification, location_name}` — dựng qua `frappe.db.get_value(...as_dict)` + enrich `device_model_name`/`location_name` (KHÔNG `as_dict()`, KHÔNG field tài chính/qr_token); `set(keys)`==6-key EXACT, tập cấm tài chính ∩ keys=∅. **3 lớp bảo mật GIỮ như `get_asset`:** name rỗng/∄→`_err(404)` in-handler no-500/no-traceback; `assert_vendor_can_access`→403 IDOR no-leak (phân biệt dispatcher-403 guest vs in-handler cap-403); whitelist session-gate + DocPerm read. `get_asset` GIỮ NGUYÊN (admin-detail `AssetDetailView` còn cần tài chính — no-regression). FE: `getAssetActionMeta(name): Promise<AssetActionMeta>` + type `AssetActionMeta` CHỈ 6 field (KHÔNG `extends AcAsset`); 3 view đổi `getAsset`→`getAssetActionMeta` cho panel (grep `getAsset(` panel meta 3 view=0); nhãn rỗng VI `'Chưa gán'` thay `'—'` câm cho risk_classification (CM `:331`/Cal `:226`) + dòng meta rỗng khác (parity Vòng 22 no-em-dash, no-EN-leak); fail-safe 403/404/network→`assetMeta=null` panel ẩn no-leak (email/qr_token/IDOR); lock SmartSelect (`source==='qr-scan'`) + zero-schedule + canSubmit-Decommissioned GIỮ. **Schema/cap-delta KHÔNG** (`CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`; KHÔNG migration/reload). Verify `bench run-tests test_imm00` (`TestAssetActionMeta`: 6-key EXACT + cấm-financial ∩=∅ + 404 no-500 + 403 IDOR + enrich + get_asset no-regress, RED-first) + `vitest` 3 `*.assetMeta.test.ts` (loader gọi `getAssetActionMeta`, `'Chưa gán'`, fail-safe null) + `vue-tsc` 0 + code-audit grep `getAsset(` panel 3 view=0. **HTTP/Playwright/quét-QR-thật BLOCKED reload (api/imm00.py edit cần gunicorn reload — HARD-STOP USER), KHÔNG đánh DONE.** KHÔNG commit. Tasks BE-7/FE-7/QA-10. FR-00-99 / BR-00-48 / 02 §IV.24 / 04 §II.1.8g-ACTIONMETA / 05 §III.2 / 06 §action-meta-lean / 07 §III.6.f-ACTIONMETA / ADR-IMM00-QR-SCAN-ACTION §D11) · 2026-06-12 (**ADR-IMM00-LABEL-PDF §D20 [GATE, Vòng 24] — Self-Correction lỗi thiết kế gốc D12.7 (giữ song song luồng in legacy):** D12.7 (V2) cố ý GIỮ đường in nhãn `window.print()` HTML cũ cạnh đường PDF như giải-pháp quá-độ, hẹn "Vòng deprecate khi khổ cũ không còn dùng" — V24 chính là Vòng đó. Trong `AssetDetailView` đường PDF khổ tem (D10–D12 + selector 3 preset V4) là lối in DUY NHẤT hiện-hành → modal HTML cũ thành **mồ côi** + mang **rủi ro audit** (mở đường ghi `label_printed` THỨ HAI cho bản in **sai khổ** — browser bỏ qua `@page mm`). FIX (FE-only `frontend/src/views/asset/AssetDetailView.vue`): GỠ HẲN đường legacy — state (`showLabelModal`/`labelData`/`labelLoading`/`labelError`/`labelPrinting`/`labelFormatKey`/`currentLabelFormat`/`labelPageRuleCss`), hàm (`openLabelPreview`+`confirmPrintLabel` — chứa `window.print()` @`:88` + đường ghi audit thứ-2), template (`<div v-if="showLabelModal">` @`:928-992` gồm nút "In tem"/"Thử lại"/`data-testid="label-page-rule"`), import mất-reference (`getAssetLabelData`/`AssetQrLabel`/khối `@/constants/label`), CSS chỉ-legacy (`.qr-label-sheet*`+`.qr-modal-chrome` print-rule — GIỮ `.app-sidebar` print-hide). GIỮ đường PDF (`openPdfLabelPrint`/`showPdfModal`/`markPrintedOnce`); đường ghi `label_printed` DUY NHẤT = `markPrintedOnce` (onafterprint + nút "Đã in xong"). **KHÔNG** đụng SSoT `getAssetLabelData`(`api/imm00.ts:174`)/`AssetQrLabel.vue`/`LABEL_FORMATS`(`constants/label.ts`) — còn dùng `AssetLabelPrintView` batch (ngoài phạm vi V24). DoD: `grep -c 'window.print(' AssetDetailView.vue`==0 (lock `AssetDetailView.qrPrint.test.ts:187-194`) ∧ `grep -c 'markLabelPrinted(' AssetDetailView.vue`==1 ∧ `vue-tsc` 0 dead-import ∧ suite asset vitest GREEN + test mới "no legacy entry-point". FE-only, KHÔNG cap/schema/endpoint/route delta; `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`; KHÔNG git commit/bench restart/migrate. Supersede D12.7+FE6 (cho `AssetDetailView`). 06 §II.3f-PDF / 07 §label-print / ADR-IMM00-LABEL-PDF §D20) · 2026-06-11 (**ADR-IMM00-LABEL-PDF §D19 [GATE, Vòng 15] — Self-Correction lỗi thiết kế gốc D17 chưa đóng (khử trùng-lặp in-call):** SSoT coerce `_coerce_asset_names` (`api/imm00.py:126` — ⚠️ drift-note: NOT `services/imm00.py`) lọc kiểu str non-rỗng NHƯNG KHÔNG khử trùng-lặp → caller gửi `assets` chứa name TRÙNG (`['AC-1','AC-1','AC-2','AC-1']` — FE multi-select buggy / copy-paste / retry-append) gây **khuếch đại TRONG 1 call**: `mark_label_printed` ghi 2×N record cho asset trùng (mỗi bản trùng +1 ALE `label_printed` +1 IMM Audit Trail trên CÙNG asset/transaction → `event_count` sai + bơm audit-chain NĐ98); `print_asset_labels_pdf` render trang trùng (phí giấy/mực); `get_asset_label_data_batch` trả phần tử trùng (vi phạm count==rows ngầm). RED-first @bench-execute (site miyano): `_coerce_asset_names(['AC-1','AC-1','AC-2','AC-1'])`→4 phần tử. FIX (1 dòng return ở SSoT): sau lọc-kiểu `names = [...]` → `return list(dict.fromkeys(names))` — khử trùng-lặp **giữ thứ-tự xuất-hiện-ĐẦU** (`dict.fromkeys` insertion-order Py3.7+, O(n), stdlib KHÔNG import, KHÔNG `set()`); fix 1 nơi tự lan 3 endpoint qua SSoT (KHÔNG sửa 3 handler). Hệ quả đo được: `mark_label_printed([a1,a1,a1])`→ĐÚNG 1 ALE+1 audit (`event_count=1`, KHÔNG 3); `print_asset_labels_pdf([a1,a1])`→PDF **1 trang** (`pypdf.PdfReader.pages==1` + MediaBox khổ mm); `get_asset_label_data_batch([a1,a1])`→1 phần tử. Cap `_MAX_LABEL_BATCH=200` đo TRÊN list dedup (`[a]*300`→KHÔNG 413; `>200 unique`→413 GIỮ — trực giao cap). **BẤT BIẾN CROSS-CALL GIỮ:** dedup CHỈ trong-call (no state xuyên-call) → 2 lần gọi RIÊNG `mark_label_printed([a1])`→2 event (`test_mark_label_printed_idempotent_count` `:4342` PHẢI vẫn XANH). Malformed→`[]` GIỮ (LL-BE-42). FE 0 regression (validNames đã unique — lưới-an-toàn BE; vitest baseline 135 file). Test BE class MỚI `TestLabelCoerceDedup` (TC-LABEL-DEDUP-01..07, RED-first, pypdf page-count). Schema/cap-delta KHÔNG; `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`; BE-only fresh-import `bench run-tests test_imm00` GREEN KHÔNG reload/migrate. FR-00-98 / BR-00-47 / 02 §IV.23 / 04 §II.1.8c-LABELDEDUP / 05 §I.7d / 07 §III.6.0-DEDUP / ADR-IMM00-LABEL-PDF §D19) · 2026-06-11 (**ADR-IMM00-LABEL-PDF §D18 [GATE, Vòng 14] — Self-Correction lỗi thiết kế gốc bất đối xứng write-audit-amplification chưa-throttle:** 2 endpoint nhãn QR còn hở rate-limit — `mark_label_printed` (POST GHI **2×N record/call** = N ALE `label_printed` + N IMM Audit Trail → bơm audit-chain NĐ98 + tải DB) + `get_asset_label_data_batch` (GET ĐỌC N-asset/call → DoS đọc). BR-00-29 mục 6 cố tình miễn nhóm in-nhãn; BR-00-38 đã carve-out rotate nhưng CHƯA đụng 2 endpoint này (PDF đã throttle §D6 nhưng mark — bề-mặt tốn-kém HƠN per-call — vẫn unthrottled). FIX (mirror rotate BR-00-38): THÊM `@rate_limit` bucket RIÊNG (cache key gồm `cmd`) cho cả 2; 2 hằng SSoT RIÊNG khối hằng đầu `api/imm00.py` cạnh `AC_LABEL_PDF_RATE_LIMIT` — `AC_LABEL_MARK_RATE_LIMIT=10` (≤ `AC_QR_REGEN_RATE_LIMIT` — write-amplify cùng họ rotate, ngưỡng THẤP) + `AC_LABEL_BATCH_RATE_LIMIT=20` (read-only CAO hơn mark, = pdf), comment lý do (write-audit-amplification vs read), KHÔNG literal/tái-dùng. 429 NGOÀI/TRƯỚC `rbac.require("asset.print")` ⇒ mark: **0 ALE label_printed + 0 IMM Audit Trail** ở call bị 429 / batch: 0 byte payload, no-leak body generic. CHỈ `get_asset_label_data` (single, low-amplify) còn unthrottled (D18.5). Test BE class MỚI `TestLabelMarkBatchRateLimit` (HTTP-ctx mô phỏng: >10 mark→#11 429 + COUNT ALE/audit KHÔNG tăng; >20 batch→#21 429; bucket-RIÊNG; decorator-presence + constant-value guard) + **ĐẢO `test_write_endpoints_not_rate_limited`** (xoá mark/batch khỏi list miễn, chỉ còn single). CLI/test bypass (`if not frappe.request: return fn`) → toàn bộ test nhãn QR cũ GIỮ GREEN, 0 regression. Schema/cap-delta KHÔNG; `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`; FE KHÔNG đổi (429→RATE_LIMITED+VI ĐÃ CÓ FR-00-87/88). BE-only, fresh-import `bench run-tests test_imm00` GREEN, KHÔNG reload/migrate. FR-00-96/97 / BR-00-45/46 / 02 §IV.22 / 04 §II.1.8b-LABELRL / 05 §I.7c / 07 §III.6.i-LABELRL / ADR-IMM00-LABEL-PDF §D18) · 2026-06-11 (**ADR-IMM00-SEARCH-ESCAPE [GATE, Vòng 13] — Self-Correction lỗi thiết kế gốc nội-suy-LIKE-trần:** `list_assets` dựng `like=f"%{search}%"` (`api/imm00.py:292`) KHÔNG escape metachar → `search='_'`/`'%'` thành wildcard SQL = over-match toàn bảng (probe site miyano: 10/10 asset), `%%%…` = LIKE-backtracking DoS. Nền KT: Frappe DatabaseQuery (`db_query.py:938-940`) nhân đôi backslash + KHÔNG escape `%`/`_` + KHÔNG emit `ESCAPE`. FIX: SSoT `escape_like_term(t)=t.replace("%","\\%").replace("_","\\_")` (escape `%`/`_` literal, KHÔNG đụng `\`); `list_assets` đổi `like=f"%{escape_like_term(str(search))}%"`, dùng CÙNG `or_filters` đã-escape cho count_with_or + get_list → INVARIANT count==rows GIỮ (ADR-LIST-SCOPE §4b nguyên vẹn). Verify thực nghiệm: escape `%`+`_` qua ORM thỏa SE-1..SE-8 kể cả `search='\'` literal/no-throw + `%%%%%%%%%%` finite. SQLi-safe (parametrized). BE-only, schema/cap-delta KHÔNG. FR-00-95 / BR-00-44 / 02 §IV.21 / 04 §II.1.13-SEARCHESCAPE / 05 §list_assets search / 07 §III.6.h-SEARCHESCAPE / ADR-IMM00-SEARCH-ESCAPE) · 2026-06-11 (**ADR-IMM00-LABEL-PDF §D17 [Accepted, factory vòng 10] — Self-Correction lỗi thiết kế gốc parse-trần:** 3 endpoint nhãn QR (`print_asset_labels_pdf`/`get_asset_label_data_batch`/`mark_label_printed`) dùng dòng trần `frappe.parse_json(assets)` (json.loads THẲNG) → input HTTP non-JSON (bare-code `'AC-2026-00001'`/`''`/`'   '`/`'not-json'`) **raise JSONDecodeError → HTTP-500 + traceback leak**; JSON-scalar-string `'"AC-1"'` parse ra str → `len()`/iterate **char-walk** (4 ô lỗi / 4 `db.exists` / 4 IDOR-probe trên ký-tự — vi phạm count==rows); JSON-number/object → TypeError/duyệt-key. RED-first verify @bench-execute. FIX: SSoT helper MỚI `services/imm00.py::_coerce_asset_names(assets)->list[str]` (parse try/except → list-gate → per-element str-filter; total-function KHÔNG raise) — 3 endpoint gọi `names = _coerce_asset_names(assets)` NGAY SAU `rbac.require`, TRƯỚC cap/empty/IDOR. Coerce-`[]`: PDF→`_ERR_LABEL_EMPTY` (422) · batch→`_ok([])` · mark→`_ok` no-side-effect. Đường hợp lệ (list / JSON-array-string) 0 regression byte-for-byte. KHÔNG còn `parse_json(assets)` trần ở `api/imm00.py` (drift-guard test). Guard Python thuần tier API/service → `bench run-tests test_imm00` fresh-import GREEN, KHÔNG reload/migrate. 04 §service-helpers/_coerce_asset_names / 05 §coerce-assets / 07 §III.6.0 TestLabelCoerceAssets / ADR-IMM00-LABEL-PDF D17) · 2026-06-11 (**ADR-IMM00-QR-SCAN-ACTION §D10 [Accepted, factory vòng 8] — Self-Correction lỗi thiết kế gốc FE-formatter:** status pill màn quét QR KHÔNG bao giờ leak mã EN/code thô/rỗng — `lifecycleStatusLabel(v)` (`constants/labels.ts`) với `v` ∉ 7 mã canonical (legacy/drift `'In Use'`/`'Retired'`/`'active'` HOẶC rỗng `''` BE phát `or ""`) đổi fallback `?? v` → hằng MỚI `LIFECYCLE_STATUS_UNKNOWN_LABEL`="Không xác định" (no-EN/raw-code leak + non-empty pill); `lifecycleStatusClass` GIỮ fallback gray trung tính; 7 canonical nhãn+màu byte-for-byte; FE-only — BE `or ""` GIỮ, KHÔNG đổi shape/cap-version/schema/reload. FR-00-93 / BR-00-42 / 02 §IV.19 / 05 §III.1 lifecycle_status / 06 §status-pill-safe / 07 §III.6.e-PILLNOLEAK) · 2026-06-11 (**ADR-IMM00-QR-SCAN-ACTION §D9 [Accepted, factory vòng 7] — Self-Correction lỗi thiết kế gốc D2:** action disabled ở màn quét QR LUÔN kèm `reason` VI khác rỗng (`enabled=False ⟹ reason!=""` với MỌI `lifecycle_status` kể cả rỗng/lạ) — thêm hằng `_LIFECYCLE_REASON_UNKNOWN`="Thiết bị không ở trạng thái cho phép thao tác này" làm fallback bậc-3 trong `_build_available_actions`; FE `AssetScanInfoView` hết dangling `aria-describedby`+trailing-rỗng `aria-label`; reason SSoT VI ở BE no-EN-leak; KHÔNG đổi shape/cap-version/schema. FR-00-92 / BR-00-41 / 02 §IV.18 / 04 §II.1.8f / 05 §III.1 available_actions / 06 §reason-render / 07 §III.6.d-REASONNONEMPTY) · 2026-06-11 (**ADR-IMM00-LABEL-PDF [V1-GATE Accepted]:** in nhãn QR qua PDF server-side đúng khổ tem nhiệt 60×100mm — endpoint `print_asset_labels_pdf` trả PDF bytes, QR `pyqrcode` SVG inline encode `qr_url`, options khổ 60×100mm margin0, cap `asset.print`+IDOR+batch200→413, render≠mark-printed audit-on-cancel guard; tái dùng `build_asset_label_data_batch`; KHÔNG chạm gen/rotate/scan/resolve) · 2026-06-09 (**3 ADR nền Phase A/B — Gate THIẾT KẾ Vòng 1 [Accepted]:** `ADR-IMM00-OPENAPI` (auto-gen OpenAPI 3.1 từ `@frappe.whitelist`, 485 ep/22 module, envelope SSoT `utils/response.py:37/:60`, serve `assetcore.api.openapi.spec`+`www/api-docs.html`), `ADR-IMM00-CMDK` (Command Palette ⌘K, registry SSoT từ `MODULE_NAV`+route, gate tái dùng `itemVisible`/`resolveRouteAccess` capability-only, fuzzy/diacritic-fold VI, a11y component riêng), `ADR-IMM00-RESPONSIVE` (mobile-first breakpoint Tailwind sm/md/lg/xl, KHÔNG PWA, pattern table→card + touch≥44px + modal full-screen mobile + DoD vào skill FE) — gate PHÂN TÍCH, KHÔNG đụng code Vòng 1) · 2026-06-08 (**ADR-IMM00-LIST-SCOPE — P1 factory run2:** chốt KTV nội bộ → read-all, Vendor Engineer → GIỮ isolation, INVARIANT `count==rows` permission-aware; gate phân tích, BE thực thi) · 2026-06-05 (Vòng 28 / B — A6-hardening chiều HIỆU CHUẨN: cờ `calibration_overdue` + `next_calibration_date` server-side trên màn quét QR `get_asset_scan_info` — derive `True ⟺ next_calibration_date<nowdate() (strict) ∧ status∉{Out of Service,Decommissioned}`, mirror `_is_pm_overdue`, timezone-safe SSoT ở BE, FE dòng "Hiệu chuẩn kế tiếp" + badge VI "Quá hạn hiệu chuẩn" render-only + a11y; payload +2 field DISTINCT giữ 9 field cũ, `next_calibration_date` là field AC Asset đã có, 0 cap/schema/field/endpoint/enum/patch delta, `CAP_SET_VERSION` v95.3388ee5629c1; FR-00-86 / BR-00-37) · Vòng 27 / B — A6-hardening: cờ `pm_overdue` server-side trên màn quét QR (FR-00-85 / BR-00-36) · Vòng 26 / B — Self-Correction RC-LIST-VENDORCLOBBER: vá HIGH vendor-isolation regression ở `list_assets` (compose AND filter-list form; BR-00-35 mục 6 / FR-00-84) |
| Khối kiến trúc | Cross-cutting (foundation cho A/B/C/D) |
| Owner | — (Cross-cutting — System Architect + BA Lead) |

> File index của module IMM-00. Tài liệu theo template chuẩn `docs/template/` (v4.1+).
> **Source docs (cũ) đã archive tại `docs/architecture/archive/imm-00/`** (7 files).

## Files hiện có (numbered 02–09)

| File | Nội dung | Trạng thái |
|---|---|---|
| [`02_Analysis_Design.md`](./02_Analysis_Design.md) | Phân tích & thiết kế: Module Overview, Architecture Position, Feature Inventory, FR, NFR, BR-00-01→12 | ✅ Live |
| [`03_Diagrams.md`](./03_Diagrams.md) | Sơ đồ: ERD (foundation + inventory), Class diagram, Sequence (SHA-256 chain, transition, scheduler) | ✅ Live |
| [`04_Backend_Design.md`](./04_Backend_Design.md) | Thiết kế backend: 27 DocType schemas (verified), Service layer 22 functions (verified), shared utilities; 20 roles, state machine đầy đủ incl. Draft + Under Maintenance | ✅ Live — synced vs code 2026-05-18 |
| [`05_API_Specification.md`](./05_API_Specification.md) | API spec: 107 whitelisted endpoints (verified), envelope `{success, data}`, permission matrix | ✅ Live — reviewed vs code |
| [`06_Frontend_Design.md`](./06_Frontend_Design.md) | Thiết kế frontend: Design tokens, Sitemap ([BUILT]/[SPEC] labeled); 10+ views built (asset/, master-data/, audit/), 4 Pinia stores (verified) | ✅ Partial — multiple views built |
| [`07_Testing_QA.md`](./07_Testing_QA.md) | Testing & QA: 13 unit tests (TC-S-001→013, corrected), UAT scenarios, STRIDE security, code quality | Live (BE) / Planned (tests) |
| [`08_Deployment.md`](./08_Deployment.md) | Deployment: Thứ tự deploy (IMM-00 first), env config, migration patches, QMS mapping, rollback | ✅ Live |
| [`09_Release.md`](./09_Release.md) | Release: User guide (System Admin), Release Notes v4.0.0, Traceability matrix, Bảng thống kê | ✅ Live |

## Architecture Decision Records (cross-cutting, chạm registry)

- [`./ADR-IMM00-OPENAPI.md`](./ADR-IMM00-OPENAPI.md) — **[R1-GATE Phase A · Accepted 2026-06-09] OpenAPI 3.1 auto-gen từ `@frappe.whitelist`**: D1 introspect = `importlib`+`inspect.signature`+`ast` walk `assetcore.api.*` (485 ep/22 module-file, đếm-thật); D2 ánh xạ `path=/api/method/assetcore.api.<mod>.<fn>` · `method`=GET trừ `methods=['POST']` · `required`=param-no-default · type-hint→JSON-type; D3 2 component SSoT `SuccessEnvelope{success,data}` + `ErrorEnvelope{success,error,code,http_status,fields}` đọc **DUY NHẤT** từ `utils/response.py:37 ErrorCode` + `:60 _HTTP_FOR_CODE`; D4 body-schema bridge `create_*`/`update_*` → `frappe.get_meta(DocType).fields`; D5 override registry `openapi_overrides.py` cho JSON-string param + `list_*`/`get_*` data shape; D6 enrich plan imm00→imm04→imm12; D7 serve = 1 whitelist GET `assetcore.api.openapi.spec` (session-gated, cached) + `www/api-docs.html` (Swagger UI self-host); D8 root `tags[]`+`x-assetcore-stats` đo-tại-generate (chống hardcode); D9-SYNC spec=dẫn-xuất+guard-test chống drift; D9-TAGS canonicalize operation tags (gỡ leak raw module-slug); D10 JSON-string param `format:json` (AST discovery, đếm động); D11 guest-stat SSoT (đếm `security==[]` thay global registry); D12 baseline error-surface (401/403 typed mọi op, baseline-merge curated-WIN); **D13 SERVERS-BLOCK** (root `servers[]` dẫn xuất `frappe.utils.get_url()` bare-origin, chèn info↔components, fail-safe `'/'`, KHÔNG hardcode host — SPEC-READY Vòng 8); **D-PRECOND:** đổi ≥9 union `X|None=None` (imm01:543/559, imm04:106/116/126/156, imm14:39, dashboard:890 + imm01:284-285) → default-rỗng cùng-kiểu TRƯỚC generate. Test-case TDD TC-OAS-01..12 + D8-D13 (TC-OAS-D13-01..06). KHÔNG re-architect BE/DocType, KHÔNG modify core. Gate phân tích — thực thi Vòng 2+.
- [`./ADR-IMM00-CMDK.md`](./ADR-IMM00-CMDK.md) — **[R1-GATE Phase B · Accepted 2026-06-09] Command Palette ⌘K**: D1 registry SSoT = view thứ 2 trên `MODULE_NAV` (`sidebarNav.ts:59`) + route tĩnh không-nav (`router/index.ts`), composable `useCommandRegistry()`, **KHÔNG fork nguồn thứ 3**; D2 gate = TÁI DÙNG `itemVisible` (`sidebarNav.ts:205`) cho nav-entry + `resolveRouteAccess` (`routeAccess.ts:57`) cho route-entry — **capability KHÔNG role-name**, command gated → ẨN (không bao giờ landing `/unauthorized`); D3 search = fuzzy substring+token + diacritic-fold VI (`foldVi` 'bao tri'→'Bảo trì'), KHÔNG thư viện ngoài; D4 bind ⌘K/Ctrl+K qua `useMagicKeys` (`@vueuse/core ^10.9.0` đã có) ở `AppLayout` + nút hint `AppTopBar` (mobile no-keyboard, touch≥44px); D5 a11y = component RIÊNG `CommandPalette.vue` (KHÔNG `BaseModal` thiếu focus-trap): `role=dialog aria-modal` + combobox/listbox + Arrow/Enter/Escape + focus-trap+return-focus; D6 Gần đây (localStorage max5) + Ghim (vẫn lọc cap). 12 test-case TDD (TC-CMDK-01..12). KHÔNG đổi `sidebarNav`/`routeAccess` (chỉ tái dùng export). Gate phân tích — thực thi Vòng 2+.
- [`./ADR-IMM00-RESPONSIVE.md`](./ADR-IMM00-RESPONSIVE.md) — **[R1-GATE Phase B · Accepted 2026-06-09] Mobile-first responsive web (KHÔNG PWA)**: D1 breakpoint chuẩn Tailwind **default** sm640/md768/lg1024/xl1280 (config KHÔNG custom `screens`), mobile-first, KHÔNG PWA (0 service-worker/manifest); D2 5 pattern DoD bắt buộc — P1 list table→card (`hidden sm:block`+`sm:hidden`, đã có ở 37 view), P2 form `grid-cols-1 md:grid-cols-2`, P3 MỌI `<table>` bọc `overflow-x-auto`, P4 tab/chip-bar dài `overflow-x-auto`/`flex-wrap`, P5 touch `min-h-[44px]`; D3 modal full-screen mobile → centered `sm:`+ (⌘K dialog đồng bộ); D4 backlog 4 gap có địa chỉ verified (P2 `AssetDetailView:555` tab-bar + `PersonaDashboardShell:32` KPI tablet; P3 `RCAListView:117` thiếu card + `ListCard:51`); D5 ghi DoD vào skill FE (`assetcore-fe/references/lessons-learned.md` LL-FE-34 + `component-patterns.md` ## Responsive — file `WAVE2-RECURRING-BUGS.md` cũ đã consolidate, home hiện = references/). 10 test-case TDD (TC-RWD-01..10). Light-touch (KHÔNG rewrite 37 view đúng). Gate phân tích — thực thi Vòng 2+.
- [`./ADR-IMM00-ASSETCODE.md`](./ADR-IMM00-ASSETCODE.md) — **[V1-GATE] Semantic & auto-gen Mã tài sản** (`asset_code`=PK) vs **Số serial NSX** (`manufacturer_sn`): D1 không trộn 2 khái niệm; D2 auto-gen khi trống = series counter `AC-ASSET-.YYYY.-.#####` (KHÔNG default-theo-serial); D3 `asset_code` pattern `^[A-Za-z0-9._\-/]+$` · DB-unique · immutable, `manufacturer_sn` app-unique · mutable (chốt KHÔNG nâng DB-unique); D4 nhãn VI "Mã tài sản"/"Số serial NSX" + helper text nguyên văn; D5 invariant `asset_code==name`, KHÔNG backfill, đính chính reserved-prefix thực = `_`/`SI-` (KHÔNG phải `TS-`/`CAT-`). Gate phân tích — KHÔNG đụng code Vòng 1.
- [`../imm-04/ADR-001-asset-qr.md`](../imm-04/ADR-001-asset-qr.md) — **QR cấp tài sản**: chốt `AC Asset.qr_token` (schema-delta §II.1.8 trong `04_Backend_Design.md`) + `Asset Lifecycle Event.event_type += qr_generated, label_printed` (§II.6) + patch backfill `v3_2.008` (§IV.3a). RBAC: read-only QR (`resolve_qr_token`/`get_asset_scan_info`/`get_asset`) = `asset.read`; **in nhãn (`get_asset_label_data[_batch]`/`mark_label_printed`) = `asset.print`; rotate (`regenerate_asset_qr_token`) = `asset.qr.rotate`** (D6 EXECUTED Vòng 3 — tách cap, persona vận hành in được; `CAP_SET_VERSION` v95.3388ee5629c1 → **v97.c30c69b8974d**); deep-link `/a/<token>`.
- [`./ADR-IMM00-SEARCH-ESCAPE.md`](./ADR-IMM00-SEARCH-ESCAPE.md) — **[GATE 2026-06-11 · Vòng 13] Escape LIKE-metachar (`%`/`_`/`\`) trong `search` của `list_assets`** (RED: `search='_'`/`'%'` → wildcard → over-match toàn bảng; `%%%…` → LIKE-backtracking DoS): D1 SSoT helper `escape_like_term(term)` = `term.replace("%","\\%").replace("_","\\_")` — escape `%`/`_` thành **literal**, **KHÔNG đụng `\`** vì Frappe DatabaseQuery (`db_query.py:938-940`) TỰ nhân đôi backslash + KHÔNG emit `ESCAPE` (verify probe thực nghiệm site `miyano`: escape `%`+`_` qua ORM thỏa MỌI acceptance kể cả `search='\'` literal, no-throw; "backslash-first" SAI cho đường ORM); D2 `list_assets` đổi `like=f"%{search}%"` → `f"%{escape_like_term(str(search))}%"`, dùng CÙNG `or_filters` đã-escape cho CẢ `count_with_or` lẫn `get_list`; D3 **INVARIANT `count==rows` BẤT BIẾN** (cùng động cơ DatabaseQuery, cùng or_filters → mọi biến đổi pattern áp đồng nhất 2 path; ADR-LIST-SCOPE §4b nguyên vẹn); D4 CHỈ `list_assets` (các list endpoint khác nội-suy-trần = **[BACKLOG]** dùng chung `escape_like_term`). 8 acceptance SE-1..SE-8 (literal `_`/`%`/`\` · count==rows · no-regress substring · SQLi-safe · SSoT-grep · DoS-guard `%%%%%%%%%%`). SQLi vẫn an toàn (parametrized). Schema/cap-delta KHÔNG; BE-only; FR-00-95 / BR-00-44 / 02 §IV.21 / 04 §II.1.13-SEARCHESCAPE / 05 §list_assets / 07 §III.6.h-SEARCHESCAPE. Edge metachar-kẹp-giữa (`AC_001`) → literal-match hoàn hảo = [ROADMAP] raw-SQL `ESCAPE '\\'`. Gate thiết kế + verify thực nghiệm; BE thực thi sau gate.
- [`./ADR-IMM00-LIST-SCOPE.md`](./ADR-IMM00-LIST-SCOPE.md) — **[GATE 2026-06-08] Row-scope AC Asset list + INVARIANT `count==rows`** (P1 factory run2: KTV `phamvanduc` thấy "Tổng 1430" nhưng bảng RỖNG): D1 **KTV NỘI BỘ** (`PM/Repair/Calibration/Corrective User` = Role Profile "Kỹ thuật viên") → `ac_asset_query` trả `""` (**read-all**, bỏ scope `responsible_technician`); D2 **Vendor Engineer** (role thủ công nhân sự NCC, KHÔNG thuộc 8 Role Profile) → **GIỮ isolation** `responsible_technician=<safe>` (bất biến CLAUDE.md §5 — đổi nội bộ KHÔNG nới vendor); D3 INVARIANT `count==len(items)` cho MỌI persona → count dùng `frappe.get_list(limit_page_length=0)` permission-aware (KHÔNG `db.count`/`get_all` — gốc lỗi count!=rows). Đồng bộ `ac_asset_has_permission` (KTV read=True). +7 invariant test (INV-3 chứng minh vendor isolated sau fix). Backlog: 4 list endpoint khác (incident/repair/pm/commissioning) cùng lỗ count-vs-permission. Lấp khoảng RC-LIST-VENDORCLOBBER (bỏ sót persona KTV nội bộ).
- [`./ADR-IMM00-TRUNCATION-SSOT.md`](./ADR-IMM00-TRUNCATION-SSOT.md) — **[GATE 2026-07-25 · Accepted, EXTENDS ADR-IMM00-APPROVAL-INBOX §F/CR-43] Hợp đồng TRUNG THỰC khi cắt danh sách (cross-cutting IMM-00/06/08/09/11/12)**: mọi endpoint list KHÔNG-phân-trang PHẢI công bố `total` (COUNT thật trước cắt) + `truncated` (int 0/1) thay vì cắt IM LẶNG. D1 SSoT DUY NHẤT `services/shared/truncation.py::truncation_meta`; D2 `truncated` là **int** KHÔNG bool/None (codegen Dart/Kotlin — CR-01); D3 **ADDITIVE** — `required` OAS GIỮ NGUYÊN ⇒ 0 breaking, client coi `undefined` = "không rõ" (KHÔNG hiện cảnh báo); D4 ZERO-COST — `count_fn` lazy, nguồn qua `BaseRepository.list` **tái dùng `pg["total"]`** (0 query thêm); **D5 INV-TRUNC-LIMIT** — `limit` truyền vào `truncation_meta` PHẢI là **trần THỰC ÁP** lên truy vấn (`pg["page_size"]` đã clamp `[1,100]` @`utils/pagination.py:23`), không phải `limit` thô của client (`limit=500`→rows cắt ở 100 mà báo "không cắt" = nói dối; `limit_page_length=0` của `get_all` = KHÔNG giới hạn mà báo "đã cắt" = báo oan); D6 `total` = **tổng user NÀY được thấy** — CÙNG engine với rows (INV-ROWSCOPE), CẤM "sửa" thành `db.count` thô (tái sinh bug `count != rows`). Áp dụng: CR-43 inbox · CR-46 due-list PM/hiệu chuẩn · CR-47 competencies · **CR-69** 3 endpoint device-profile history (IMM-08/09/12). **§8 (AC-CR-100, 2026-07-28 — áp dụng thứ 6, mở rộng phạm vi sang nguồn ĐÃ phân trang):** cắt IM LẶNG có thể dịch xuống **lớp CLIENT** — `get_asset_timeline` trả `pagination.total` đủ mà view vứt bằng `as unknown` (`AssetDetailView.vue:202`); D-TL-1 CẤM cast trả về api-client tại view · D-TL-2 `ORDER BY` phân trang PHẢI **tiền định** (`"timestamp desc, name desc"`, thiếu tiebreaker ⇒ 2 trang lặp+SÓT im lặng) · D-TL-3 ba trạng thái *chưa tải / rỗng thật / lỗi* có vị-từ RIÊNG · D-TL-4 `page_size` GIỮ trần thật · D-TL-5 APPEND+dedupe theo `name` · D-TL-6..9 (max(total,rendered) · không dead-end · chống «Tải thêm» không tiến · refresh=reset); INV-TL-1..11 + microcopy SSoT §8.8 + nợ khai tên AC-CR-101/102 §8.7.
- [`./ADR-IMM00-ASSET-OP-HISTORY.md`](./ADR-IMM00-ASSET-OP-HISTORY.md) — **[GATE 2026-07-30 · Accepted · SUPERSEDES điều khoản «CẤM phương án (c)» của ADR-IMM00-TRUNCATION-SSOT §8.7 / AC-CR-102] Nhánh dữ liệu VẬN HÀNH của MỘT thiết bị (Bảo trì · Sửa chữa · Sự cố) render THẬT trong tab «Bản ghi liên quan»**: 3 endpoint ĐÃ LIVE on-disk (`api/imm08.py:198` · `api/imm09.py:195` · `api/imm12.py:232`) nhưng **0 caller `.vue`** ⇒ mã chết; PM quyết **LAND RENDER** (ban «xoá mã chết»). Lý do cấm cũ («2 con số chỏi nhau» — IMM-09 lọc `docstatus=1` còn ô đếm connections đếm mọi docstatus) được **giải tán** bằng 3 điều kiện gắn kèm: **C1** vị-từ nằm TRONG tiêu đề section («Kết quả bảo trì» = `PM Task Log` khác doctype ô đếm · «Lần sửa chữa đã **hoàn thành**» = khai đúng `docstatus=1` · «Sự cố đã ghi nhận») · **C2** mỗi dòng in ≥1 trường ngoài `PreviewSpec` của ô (6 tín hiệu: `overall_result`/`is_late`/`mttr_hours`/`sla_breached`/`severity`/`fault_code`) · **C3** 3 section THU mặc định + fetch lười theo section (0 chi phí mở máy). D-OPH-1..16 · INV-OPH-1..18. **FE-only: 0 dòng `.py` prod, 0 OAS delta, 0 blocker reload.** Nợ có tên: `AC-CR-103` (hợp nhất 2 map `REPAIR_TYPE_LABEL(S)`) · `AC-CR-104` (phương án (b): lịch sử cùng thiết bị trên màn chi tiết PM/CM/Sự cố) · ~~dải «Đang xem M/N» = **VÒNG 5** khuôn `AC-CR-96`~~ → ✅ **ĐÓNG bởi `AC-CR-115`**. **[GATE 2026-07-30 · §10 · `AC-CR-115` · Accepted · SUPERSEDES `D-OPH-12` + nửa sau `D-OPH-1`]** dải cắt **render THẬT** trong CHÍNH nhánh bị cắt («Đang xem {M}/{N} — còn {N−M} chưa hiển thị»), điều kiện dẫn xuất từ **`total − rows.length > 0`** *(SỐ LIỆU là SSoT — cờ `truncated` **KHÔNG** cầm lái vì nó tính từ `limit`, `D-OPH-17`)*; **BẢN GHI đứng TRƯỚC Ô CHỨC NĂNG** trong `[tab-panel-related]` + **đúng 2** `[related-block-heading]` VI (`D-OPH-18`); **KHÔNG** «Tải thêm» — 3 endpoint không có `offset` ⇒ nút chết (`D-OPH-19`, LL-FE-47). D-OPH-17..20 · INV-OPH-19..30 · FR-00-OPH-02/BR-00-OPH-19..30. Nợ mới: `AC-CR-116` (`listRouteForAsset` chưa cài — thiếu guard `LIST_TARGET_ANCHOR`) · `AC-CR-117` (dòng bảo trì render `pm_type` Data tự do ⇒ nguy cơ leak EN) · `AC-CR-118` (a11y/microcopy «Xem tất cả» + cấp heading nhóm).
- [`./ADR-IMM00-CONNECTIONS-TREE.md`](./ADR-IMM00-CONNECTIONS-TREE.md) — **[GATE 2026-07-27 · Accepted, EXTENDS ADR-IMM00-LIST-SCOPE §4b + ADR-IMM00-TRUNCATION-SSOT D1–D6 / AC-CR-87] «Bản ghi liên quan» là CÂY DỮ LIỆU THẬT, không phải bảng điều hướng**: khối này đang trả **19 badge đếm** (`{doctype,label,count,capped,filters}`) với nhãn **tiếng Anh thô** (app KHÔNG có `assetcore/translations/` ⇒ `_()` trả nguyên chuỗi) và 21/41 doctype **không có màn danh sách** ⇒ ô thành nút chết; người dùng phàn nàn đúng 2 điều: *chiếm quá nhiều diện tích* + *chỉ liên kết tới chức năng, không tới bản ghi*. D1 mỗi ô mang **12 khoá** (bồi `label_vi`/`total`/`truncated`/`items[]`/`deep_link_filters`/`can_create`/`create_route_hint`, GIỮ nguyên nghĩa 5 khoá legacy); **D2 MỘT `frappe.get_list` duy nhất** (`limit=CAP+1`) vừa là nguồn preview vừa là nguồn đếm ⇒ bất biến `len(items)==min(total,preview_limit)` ∧ `count==total`, **cấm** `frappe.db.count`/`get_all`/`ignore_permissions` (chống tái sinh *"Tổng 1430 / bảng RỖNG"*); D3 `capped: bool` → `truncated: int 0|1` (đóng vi phạm CR-01 do chính file này gây ra, `capped` sống **đúng 1 vòng**); **D4 `total` đã chặn trần 100** ⇒ `capped=true` biến `total` thành **cận dưới** (UI render `"100+"`); D5 preview `{name,title,status,status_label,date}` lấy theo bản đồ `PREVIEW_FIELDS` **41 doctype verify @source** (tồn tại · `permlevel=0` · **cấm** field tài chính/định danh cá nhân — LL-BE-57), mọi giá trị `str` **không bao giờ `null`**; **D6 đính chính acceptance A6** — allowlist 12 hub là **giới hạn đường thực thi**, KHÔNG phải bộ đóng-oracle (A6 ∧ A9 loại trừ nhau; **A9 thắng**, phần giữ được = thống nhất **message** doctype-rác ⇔ bản-ghi-rác); D7 `deep_link_filters` chiếu an-toàn-query-string (`internal_links` ⇒ `name` nối dấu phẩy), khoá derive từ chính đồ thị; D8 `can_create` = giao **4 điều kiện** (có màn tạo THẬT ∧ liên kết **ngược** ∧ `has_permission(dt,"create")` THẬT ∧ cha `AC Asset` ⇒ `lifecycle_status ∉ AssetStatus.BLOCKED_FOR_WO` — **cùng hằng** với `validate_asset_for_operations`/BR-00-05), `can_create=False ⟺ create_route_hint==""`, route vẫn là SSoT của FE (**resolve-or-hide**); D9 tách 3 lớp `api` → `services/connections.py` → `services/shared/connection_meta.py`; D10 nhãn VI SSoT ở **BE** (`LABEL_VI` 41 doctype, test parity duyệt **12 module dashboard THẬT** — thêm doctype mà quên dịch = ĐỎ), nhánh lifecycle **lazy-import** `services/imm00.py:1335 _LIFECYCLE_VI` thay vì chép bản dịch thứ hai. **INV-CONN-1..15**. 0 DocType ⇒ 0 migrate. **§10 bổ sung 2026-07-28 (AC-CR-88 — vòng 2/5, hợp đồng HIỂN THỊ FE)**: D-FE-1 tab-không-card + `defineExpose({reload,total})` · D-FE-2 một accessor nhãn (`data-doctype` là chỗ DUY NHẤT được chứa chuỗi thô) · D-FE-3 hai chế độ CÂY/LEGACY (tolerant reader) · D-FE-4 dòng preview cấm `status` thô + bẫy ICU `formatDate` không zero-pad · D-FE-5 resolve-or-static (0 nút disabled) · **D-FE-6 `deep_link_filters === {}` ⇒ 0 nút «Xem tất cả»** + chiếu `filters` chỉ giữ value scalar · D-FE-7 một câu `Đang xem {shown}/{badge}`, cấm `total − shown` · D-FE-8 ô rỗng gộp 1 dòng/nhóm · D-FE-9 vòng 2 render 0 nút tạo nhưng ghim bất biến 2 chiều · D-FE-11 `data-testid` là hợp đồng test. **INV-CONNFE-1..11**. FR-00-CONN-01 / BR-00-CONN-01..17 · [02 §IV.39](./02_Analysis_Design.md) · [04 §V.7](./04_Backend_Design.md) · [05 §III.24 + §III.24.6](./05_API_Specification.md) · [06 §VIII.4.2](./06_Frontend_Design.md) · [07 §XVIII + §XVIII.4](./07_Testing_QA.md). **§14 bổ sung 2026-07-28 (AC-CR-93 — FE-only, thực thi D-FE-8 vốn CHƯA được cài)**: D-CR93-1 bảng `data-testid` **CHỐT theo mã** (`conn-item`/`conn-count`/`conn-meta`/`conn-row` + `conn-empty-summary`/`conn-group`/`conn-group-label` mới; 8 tên ADR cũ **retired**; **CẤM** `data-doctype`) · D-CR93-2 vị-từ gộp = **một** hàm SSoT đọc `total ?? count ?? 0` (**cấm** `items.length` — ô LEGACY) · D-CR93-3 dòng gộp khuôn **duy nhất** `Chưa có: …`, nhãn qua `viLabel`, nhãn rỗng ⇒ **bỏ khỏi câu** (không in tên Anh) · D-CR93-4 dòng gộp **0 affordance** (supersede INV-CONNFE4-5) · D-CR93-5 nhóm toàn rỗng ⇒ **0 tiêu đề nhóm** (supersede D-FE-8 gạch 2) · D-CR93-6 payload mọi ô rỗng ⇒ câu VI **+** dòng gộp, `expose.total` = `payload.total` · D-CR93-7 nội dung ô có dữ liệu **0 thay đổi**. **INV-CONNFE6-1..9** · §14.7 danh mục supersede/đính chính (6 hàng) · §14.8 breakage đúng **1** TC · §14.9 backlog **AC-CR-94** (đường tạo cho doctype 0 bản ghi). FR-00-CONN-04 / BR-00-CONN-35..41 · [06 §VIII.8](./06_Frontend_Design.md) · [07 §XVIII.8](./07_Testing_QA.md).
- [`./ADR-IMM00-QR-SCAN-ACTION.md`](./ADR-IMM00-QR-SCAN-ACTION.md) — **[R1-GATE] Affordance + capability-gate màn quét QR**: D1 action surface 4 CTA (Báo hỏng→IMM-12 `IncidentCreate` `corrective.create` · PM→IMM-08 `PMWorkOrderCreate` `pm.create` · CM→IMM-09 `CMCreate` `repair.create` · Hiệu chuẩn→IMM-11 `CalibrationCreate` `calibration.create`) — **chốt 1 cap SSoT `<domain>.create`**, ghi mismatch route-guard hiện gate `.write`; D2 `available_actions: list[{key,label,route,enabled,reason}]` derive SERVER-SIDE = capability ∩ lifecycle (bảng lifecycle×action: Decommissioned→cấm tất cả; Out of Service→chỉ báo hỏng+CM; Draft→cấm; còn lại full), 1 predicate SSoT, nút disabled+tooltip (KHÔNG render nút chết); D3 deep-link `?asset=<name>&source=qr-scan` (KHÔNG raw `qr_token`); D4 QR-gen `before_insert→_ensure_qr_token` phủ form+import+registration (KHÔNG code import riêng) + backfill legacy idempotent + `_build_qr_url` đã wire site_config; D5 nhãn 5 field (QR+Model+Số serial NSX+Tên+Mã) — gap `build_asset_label_data` thiếu `manufacturer_sn`+`asset_name` + khổ tem 50×30/70×40mm/A4-grid; **D6 ✅ EXECUTED Vòng 3 (2026-06-08):** tách cap `asset.print`→(AC Asset,"print") + `asset.qr.rotate`→(AC Asset,"write") cho persona vận hành (đổi 4 gate `api/imm00.py`, FE router+views, CAP_SET_VERSION v95.3388ee5629c1→v97.c30c69b8974d; KHÔNG hardcode role — gate thuần DocPerm). Gate phân tích D1–D5 — thực thi Vòng 2+; D6 đã thực thi xong. **D8 (Vòng 6) token whitespace-strip resolve SSoT · D9 (Vòng 7) reason-non-empty invariant action disabled · D10 (Vòng 8) status-pill no-EN-leak `lifecycleStatusLabel`. D11 (Vòng 25, 2026-06-12, Accepted — chờ BE-7/FE-7/QA-10):** lean action-meta endpoint **`get_asset_action_meta(name)`** trả ĐÚNG 6 key allowlist (`name`/`asset_name`/`device_model_name`/`lifecycle_status`/`risk_classification`/`location_name`) — **bịt over-fetch field tài chính** (`gross_purchase_amount`/`accumulated_depreciation`/`current_book_value`/…) ở 3 màn tạo WO scan-action (CM/Hiệu chuẩn/PM dùng `getAsset` full-doc chỉ để render panel 5-field); 3 lớp bảo mật GIỮ như `get_asset` (404 in-handler no-500 · 403 IDOR vendor-scope · whitelist session-gate); `get_asset` GIỮ NGUYÊN cho admin-detail; FE type `AssetActionMeta` 6-field (KHÔNG `extends AcAsset`) + nhãn rỗng VI `'Chưa gán'` thay `'—'` câm (parity Vòng 22) + fail-safe `null` panel ẩn. KHÔNG cap/schema/version delta (`CAP_SET_VERSION` GIỮ v97.c30c69b8974d). FR-00-99 / BR-00-48 / 02 §IV.24 / 04 §II.1.8g-ACTIONMETA / 05 §III.2 / 06 §action-meta-lean / 07 §III.6.f-ACTIONMETA. Verify `bench run-tests test_imm00` + vitest 3 view; HTTP/Playwright BLOCKED reload (HARD-STOP USER).
- [`./ADR-IMM00-LABEL-PDF.md`](./ADR-IMM00-LABEL-PDF.md) — **[V1-GATE BE DONE · V2-GATE FE Accepted · V10-GATE D17 coerce-assets Accepted · V14-GATE D18 mark/batch rate-limit Accepted · V15-GATE D19 in-call dedup Accepted · V24-GATE D20 legacy-retire Accepted 2026-06-12] In nhãn QR qua PDF server-side đúng khổ tem nhiệt 60×100mm** (Phương án A — USER duyệt; window.print()+@page browser KHÔNG đảm bảo khổ tem nhiệt): D1 endpoint MỚI `print_asset_labels_pdf(assets="", preset="tem-60x100")` trả **PDF bytes** (`%PDF-`, `application/pdf` qua `frappe.local.response`), KHÔNG JSON dict; lỗi nghiệp vụ = `_err` HTTP-200 Error envelope; D2 service `render_asset_labels_pdf(names, preset)->bytes` tái dùng `build_asset_label_data_batch` (no N+1) + QR **`pyqrcode` SVG inline** (`qrcode`/`segno` KHÔNG có trong bench — verified; error='M', encode `qr_url` `/a/<token>`) + HTML N block (page-break-after trừ cuối → N asset=N trang) + `get_pdf`; D3 layout ≥5 field (QR+Model+Số serial NSX+Tên+Mã, đồng nhất §D5 ADR-QR-SCAN) + lifecycle dịch VI (no EN-leak); D4 QR server-side encode `qr_url` (KHÔNG raw `qr_token`/URL desk — no-raw-token parity `_strip_qr_token`); D5 khổ tem = options `page-width:60mm`+`page-height:100mm`+4×`margin:0mm`+`Portrait` (bẫy: `prepare_options` default margin 15mm nếu falsy → set `"0mm"` truthy); D6 bảo mật `rbac.require('asset.print')` ĐẦU TIÊN→rate-limit(20/60s)→preset(422)→empty(422)→batch>200(413 SAU rbac no-leak)→IDOR all-or-nothing(403 toàn call) — 2 loại 403 (dispatcher vs in-handler cap); D7 list rỗng→422, asset∄ trong batch→ô lỗi an toàn TRONG PDF (KHÔNG vỡ, KHÔNG 404 all-or-nothing — BA chốt); D8 render PDF KHÔNG ghi `label_printed` (audit chỉ ở `mark_label_printed` GỌI RIÊNG sau in — guard audit-on-cancel); D9 KHÔNG chạm gen/rotate/scan/resolve QR, KHÔNG pip install/bench migrate, site_config `assetcore_label_preset` (Vòng 3). **V2 (luồng in FE):** D10 api client `printAssetLabelsPdf(assets, preset='tem-60x100'): Promise<Blob>` qua axios `api` raw (`responseType:'blob'`, body `JSON.stringify(assets)`+preset, withCredentials+CSRF, batch=1 lời gọi giữ thứ tự; NOT `frappeGet/frappePost` — unwrap JSON không đọc Blob); hằng MỚI `DEFAULT_LABEL_PRESET='tem-60x100'` (TÁCH BIỆT `LABEL_FORMATS` cũ); D11 **content-type guard (Self-Correction cốt lõi):** BE trả HTTP-200 cho CẢ thành công (`application/pdf` blob) LẪN 4 nhánh `_err` (preset/empty-422·IDOR-403·batch-413 = `application/json` envelope) → axios interceptor KHÔNG bắt lỗi HTTP-200 → `extractPdfBlobOrThrow` phát hiện content-type: pdf→resolve Blob / khác→`res.data.text()`→JSON.parse unwrap `{message:{error,code,http_status}}`→ném ApiError VI (TUYỆT ĐỐI KHÔNG đưa JSON-blob cho iframe); cap-403(`rbac.require` RAISE)/dispatcher-403/429 vẫn đi axios interceptor (HTTP-403/429); D12 luồng `<iframe>` ẩn `onload→contentWindow.print()` + preview `BaseModal` embed CHÍNH PDF (WYSIWYG cùng-blob) + `markLabelPrinted` chỉ-on-confirm nút "Đã in xong"/`onafterprint` (audit-on-cancel) + `revokeObjectURL` chống leak + gate `v-if can('asset.print')` + ~~GIỮ luồng `window.print()` cũ song song~~ (V24/D20: GỠ HẲN khỏi `AssetDetailView` — xem cuối). 11 TC-LABEL-PDF TDD (BE) + DoD vitest FE suite (assetDetailQrPrint/AssetLabelPrintView/assetLabelFormat/assetListBatchSelect) 0 regression + test mới GREEN, vue-tsc 0. ⚠️ endpoint PDF CHƯA live HTTP tới khi USER reload gunicorn → Playwright BLOCKED, eval KHÔNG tuyên bố "in thật". BE Vòng 1 DONE, FE Vòng 2, polish Vòng 3. **V10 (D17 — Self-Correction parse-trần):** SSoT `_coerce_asset_names(assets)->list[str]` thay dòng trần `frappe.parse_json(assets)` ở CẢ 3 endpoint nhãn — bare-code/non-JSON/'' /'   '/scalar-str/number/object → `[]` (KHÔNG JSONDecodeError-500/no-traceback-leak; KHÔNG char-walk 4×`db.exists`/IDOR trên ký-tự); list/JSON-array-string giữ nguyên (lọc element non-str); coerce-`[]` → PDF 422 `_ERR_LABEL_EMPTY` / batch `_ok([])` / mark no-side-effect; thứ tự gate giữ (rbac ĐẦU). Drift-guard: KHÔNG còn `parse_json(assets)` trần ở `api/imm00.py`. Guard Python thuần → `bench run-tests test_imm00` fresh-import GREEN KHÔNG reload/migrate. Class TDD `TestLabelCoerceAssets` (07 §III.6.0). **V14 (D18 — Self-Correction bất đối xứng write-audit-amplification chưa-throttle, mirror rotate BR-00-38):** THÊM `@rate_limit` bucket RIÊNG (cache key `cmd`) cho `mark_label_printed` (`AC_LABEL_MARK_RATE_LIMIT=10` ≤ regen — GHI 2×N ALE+audit/call) + `get_asset_label_data_batch` (`AC_LABEL_BATCH_RATE_LIMIT=20` = pdf — đọc N-asset/call); 2 hằng SSoT RIÊNG khối hằng đầu `api/imm00.py` cạnh `AC_LABEL_PDF_RATE_LIMIT` (comment lý do, KHÔNG literal/tái-dùng); 429 NGOÀI/TRƯỚC `rbac.require("asset.print")` ⇒ mark 0 ALE+0 audit / batch 0 byte payload, no-leak; CHỈ `get_asset_label_data` (single) còn unthrottled (D18.5); test `TestLabelMarkBatchRateLimit` (HTTP-ctx, decorator-presence + constant-value guard) + ĐẢO `test_write_endpoints_not_rate_limited`; CLI bypass → suite cũ GREEN 0 regression; schema/cap-delta KHÔNG, `CAP_SET_VERSION` GIỮ `v97.c30c69b8974d`, FE KHÔNG đổi. FR-00-96/97 / BR-00-45/46 / 07 §III.6.i-LABELRL. **V15 (D19 — Self-Correction in-call dedup, D17 chưa đóng):** THÊM bước cuối `list(dict.fromkeys(names))` ở SSoT `_coerce_asset_names` (`api/imm00.py:126`) — khử trùng-lặp **giữ thứ-tự xuất-hiện-ĐẦU** (in-call, KHÔNG xuyên-call) → caller gửi name TRÙNG (`[a1,a1,a1]`) KHÔNG còn khuếch đại: mark→1 ALE+1 audit (`event_count=1` KHÔNG 3) / PDF→1 trang (`pypdf.PdfReader.pages==1`) / batch→1 phần tử; cap `_MAX_LABEL_BATCH=200` đo TRÊN list dedup (`[a]*300`→KHÔNG 413, `>200 unique`→413 GIỮ); bất biến cross-call GIỮ (`test_mark_label_printed_idempotent_count` `:4342` — 2 call riêng=2 event); malformed→`[]` GIỮ; FE 0 regression (validNames đã unique). 1 dòng đổi return + class TDD `TestLabelCoerceDedup` (07 §III.6.0-DEDUP, RED-first, pypdf). Schema/cap-delta KHÔNG, `CAP_SET_VERSION` GIỮ. FR-00-98 / BR-00-47 / 02 §IV.23 / 04 §II.1.8c-LABELDEDUP / 05 §I.7d / 07 §III.6.0-DEDUP. **V24 (D20 — Self-Correction retire legacy, supersede D12.7+FE6 cho `AssetDetailView`):** GỠ HẲN đường in nhãn legacy `window.print()` HTML khỏi `AssetDetailView` → màn chi tiết CHỈ còn DUY NHẤT đường PDF khổ tem (`openPdfLabelPrint`/`showPdfModal`/selector 3 preset V4). XOÁ state (`showLabelModal`+7 ref legacy)/hàm (`openLabelPreview`+`confirmPrintLabel`)/template (`<div v-if="showLabelModal">` @`:928-992` nút "In tem"/"Thử lại"/`label-page-rule`)/import mất-reference (`getAssetLabelData`/`AssetQrLabel`/`@/constants/label`)/CSS chỉ-legacy (`.qr-label-sheet*`+`.qr-modal-chrome`; GIỮ `.app-sidebar` print-hide). Đường ghi `label_printed` DUY NHẤT = `markPrintedOnce` (onafterprint + "Đã in xong") → KHÔNG còn đường ghi audit thứ-2 cho bản in sai-khổ. KHÔNG đụng SSoT `getAssetLabelData`/`AssetQrLabel.vue`/`LABEL_FORMATS` (còn dùng `AssetLabelPrintView` batch — ngoài phạm vi). DoD: grep-0 `window.print(` + grep-1 `markLabelPrinted(` trong `AssetDetailView.vue`, `vue-tsc` 0 dead-import, vitest asset GREEN + test "no legacy entry-point", `AssetDetailView.qrPrint.test.ts:187-194` 0 regress. FE-only, KHÔNG cap/schema/route delta, `CAP_SET_VERSION` GIỮ; KHÔNG commit/restart/migrate. 06 §II.3f-PDF / 07 §label-print / ADR-IMM00-LABEL-PDF §D20.1–D20.8.

## Source docs (cũ) — đã archive

Source docs gốc (7 files) đã được move sang `docs/architecture/archive/imm-00/` sau khi review code 2026-05-08:

- `IMM-00_API_Interface.md` — API spec gốc (envelope cũ)
- `IMM-00_Functional_Specs.md` — FR/NFR gốc
- `IMM-00_Inventory_Design.md` — Inventory sub-domain v4 gốc
- `IMM-00_Module_Overview.md` — Module overview gốc
- `IMM-00_Setup_Guide.md` — Setup guide gốc
- `IMM-00_Technical_Design.md` — Technical design gốc (1937 dòng)
- `IMM-00_UI_UX_Guide.md` — UI/UX guide gốc

## Những thay đổi trong review 2026-05-08

Các discrepancy chính được sửa trong lần review này:

**04_Backend_Design.md:**
- DocType catalog: cập nhật từ 18 → 27 DocType (verified vs `assetcore/assetcore/doctype/`)
- DocType path: sửa từ `assetcore/doctype/` → `assetcore/assetcore/doctype/`
- Service functions: cập nhật 10 → 22 functions (thêm transfer, GMDN, scheduler, KPI rollup)
- `transition_asset_status()` return type: sửa từ dict → None
- `services/inventory.py`: xóa section (file không tồn tại trong shared/)
- Import path ServiceError: sửa từ `assetcore.services.exceptions` → `assetcore.services.shared.errors`
- ErrorCode: sửa từ `AC-E001..E012` → string constants (`NOT_FOUND`, `FORBIDDEN`, ...)

**05_API_Specification.md:**
- `transition_asset_status` → `transition_status`
- `get_asset_lifecycle_history` → `get_asset_timeline`
- `search_assets_by_udi`, `get_assets_due_pm` → không tồn tại (removed)
- `get_sla_for` → `resolve_sla_policy`
- `list_audit_events` → `list_audit_trail`
- `get_audit_event` → `get_audit_entry`
- `verify_audit_chain` → `verify_chain`
- `create_capa` → `open_capa`
- `close_capa` → `close_capa_record`
- `list_departments_tree`, `list_locations_tree` → `list_departments`, `list_locations`
- Trigger endpoints: `trigger_check_*` → `trigger_*_check` pattern + GET (không phải POST)
- `update_asset`, `update_supplier`: PUT → POST
- Thêm 8 endpoint groups mới không có trong spec cũ
- `close_incident` → không tồn tại (removed)

**06_Frontend_Design.md:**
- Vue views: chỉ 2 views built (`ReferenceDataView.vue`, `SlaPolicyListView.vue`); sitemap labeled [BUILT]/[SPEC]
- Pinia stores: sửa từ pattern chung → 4 stores thực tế (`useAssetStore`, `useRefDataStore`, `useCapaStore`, `useIncidentStore`)
- API client: sửa từ object-style `imm00Api{}` → named exports; sửa endpoint names
- lifecycle_status colors: thêm đúng values (Commissioned, Calibrating, Under Maintenance)

**07_Testing_QA.md:**
- TC-S-003: `transition_asset_status` return → None (không phải dict); thêm verify downtime log
- TC-S-006: exception type → `frappe.exceptions.ValidationError` (không phải ServiceError)
- TC-S-009: CAPA signature → `due_days: int` (không phải `due_date: str`); xóa `linked_incident`

## Roadmap tiếp theo

- [ ] (Optional) Pentest report upload tại `docs/security/imm00-pentest.md`
- [ ] (Optional) Screenshot UI thực tế trên staging → đính kèm vào `09_Release.md §I`
- [ ] Build còn thiếu: các views [SPEC] trong sitemap

## Tham chiếu

- Template chuẩn: [`../template/`](../template/)
- Codebase ground truth (BE): `assetcore/services/imm00.py` · `assetcore/api/imm00.py` · `assetcore/services/shared/`
- Codebase ground truth (FE): `frontend/src/types/imm00.ts` · `frontend/src/api/imm00.ts` · `frontend/src/stores/imm00.ts`
- Source docs (cũ): `docs/architecture/archive/imm-00/`

---

*IMM-00 là foundation layer — deploy trước mọi module IMM-01→IMM-17.*
