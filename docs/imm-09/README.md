# IMM-09 — Tài liệu module

| Mục | Giá trị |
|---|---|
| Module | **IMM-09 — Sửa chữa (Corrective Maintenance)** |
| Wave | 1 |
| Trạng thái | Mature ⭐ (reference module) |
| Số file hiện có | 14 |
| Cập nhật cuối | 2026-07-27 (**AC-CR-84 [BE Bước-4 — ĐÃ LAND]** — cổng ẢNH BẰNG CHỨNG NĐ98 (Class C/D) nay là CODE SỐNG Ở SERVER: predicate SSoT `_repair_evidence_missing_idxs` + `_repair_evidence_gate_applies` + `_repair_row_is_persisted` + hằng `_EVIDENCE_HIGH_RISK` / `_REPAIR_ACTION_REASON_EVIDENCE_PHOTO` (`services/imm09.py`), wire ĐỦ **4 điểm**: P1 `close_work_order` (guard SAU `_apply_checklist`, TRƯỚC MỌI lệnh lưu ⇒ bị chặn = **0 byte ghi DB**), P2 `confirm_inspection` (pre-check TRƯỚC `doc.submit()` — chống lách bằng cách đặt `status` qua đường khác; `ServiceError` in-envelope, **KHÔNG** 417 thô), P3 `_build_repair_available_actions` (business-gate bậc 3 ⇒ CTA `close_work_order` tắt kèm reason VI hằng), P4 `get_work_order` (3 khoá `evidence_photo_required` **int 0|1** / `evidence_photo_missing_idxs` / `evidence_photo_total_required`, **0 truy vấn thêm** — tái dùng `risk_classification` đã đọc). Mã mới `IMM09-EVIDENCE-PHOTO-REQUIRED` (422 in-envelope + `context.missing_count`/`missing_idxs` + `fields.repair_checklist`) đã regen sang FE (`gen_fe_messages --check` XANH ⇒ 0 SYS-500). Nhánh `cannot_repair=1` MIỄN TRỪ có chủ đích. **Verify THẬT:** `test_imm09` **273 OK** (+14: 12 TC `07 §XII.2` + mutation-probe thường trực + biên predicate), mutation-verify **M1..M7 ĐỎ 7/7** rồi khôi phục; `test_mobile_oas` **1008 OK** (lật `cr84_i`→`cr84_j` parity đầy đủ + REFRESH CITE `services/imm09.py` 41 chỗ + 15 cite `SearchSparePartItem` — LẬT ≠ THÊM ⇒ counter GIỮ); `test_mobile_docset` **9 OK**; `test_imm08`/`imm12`/`imm15` + 6 guard-suite XANH. **0 `bench migrate`** · **KHÔNG commit** · ⚠️ cần USER reload gunicorn `--preload`. 2 điểm chờ [BA] ratify: đếm ảnh qua `_repair_checklist_item_photos` (SoT chung) và cải chính nửa sau `TC-CM-EVID-11`. 🟡 **FE Bước-4 còn lại**) · Trước đó 2026-07-27 (**AC-CR-84 [BA spec — Bước-2, slice contract ĐÓNG]** — **Cổng ẢNH BẰNG CHỨNG NĐ98 (Class C/D) khi đóng phiếu CM hết là CODE CHẾT.** Verify @source: `repair_checklist[].photo` có, endpoint đính ảnh có (`services/imm09.py:1595`) nhưng **0 chỗ** đọc `photo` để CHẶN (`close_work_order` `:2046` + `validate_repair_checklist_complete` `:955` chỉ kiểm `result`) ⇒ cổng chỉ sống ở client mobile, và ở đó cũng **chết** vì suy nhóm nguy cơ từ `risk_class` (ánh xạ MẤT MÁT) — hồ sơ NĐ98 của thiết bị Class C/D đóng được **không kèm bức ảnh nào** (mobile CR-51 + phần enforcement còn nợ của CR-15). **Chốt (BR-09-23):** 1 predicate SSoT `_repair_evidence_missing_idxs` dùng CHUNG cho **enforcement** (`close_work_order` nhánh `cannot_repair=0` + **pre-check `confirm_inspection` chống lách** — đặt `status` bằng đường khác rồi nghiệm thu), **advertise** (`available_actions[close_work_order].enabled` + reason VI hằng) và **read** (3 khoá `evidence_photo_required` int 0|1 · `evidence_photo_missing_idxs` array<int≥1> · `evidence_photo_total_required`). Nguồn = `risk_classification` {High, Critical}, `""` (chưa phân loại) **KHÔNG** chặn. Lỗi mới `IMM09-EVIDENCE-PHOTO-REQUIRED` **422 in-envelope HTTP-200** (KHÔNG 417) kèm `context.missing_count`/`missing_idxs` + `fields.repair_checklist`; guard đứng SAU `_apply_checklist` nhưng TRƯỚC mọi lệnh lưu ⇒ bị chặn = **0 byte ghi DB**. Miễn trừ `cannot_repair=1`. **1 SELF-CORRECTION so với acceptance PM (A7):** 3 khoá **KHÔNG** vào `required` của `RepairWorkOrderDetail` — bất biến kiến trúc *'4 màn Chi tiết chỉ PK là required'* đang được **3 guard độc lập** khoá (sweep 4-schema `30c` · `repairtrans_b` · `cr82_c`), trong khi lợi ích của `required` **không load-bearing** (sau vòng này an toàn nằm ở SERVER; client bỏ qua khoá vẫn bị chặn in-envelope kèm `missing_idxs`) ⇒ nới guard kiến trúc để hợp spec là anti-pattern *'sửa test cho khớp code'* (**ADR-IMM09-EVIDENCE-03**; nếu USER muốn theo đúng A7 thì đó là 1 dòng YAML + 4 dòng test, nhưng phải là quyết định có ý thức). 5 ADR: EVIDENCE-01 (cổng thuộc SERVER, nguồn `risk_classification`) · -02 (chỉ tính dòng checklist **đã lưu** — chống deadlock phiếu legacy) · -03 (không nới `required`) · -04 (miễn trừ `cannot_repair=1`) · -05 (khoá `fields` = tên khoá **ĐỌC** `repair_checklist`, tinh chỉnh ADR-IMM12-14). **BA slice ĐÓNG (CONTRACT-ONLY, verify THẬT):** OAS +3 property (`paths`/`schemas`/`parameters` GIỮ **109/287/38**) + `available_actions` bồi business-gate (3b) + 2 op ghi khai mã lỗi; **bonus** sửa **cite-rot 4 chỗ** (`services/imm09.py:1105/1134/1181`, `api/imm09.py:105`) + **cải chính cap** `closeWorkOrder` `repair.submit`→**`repair.create`** (`api/imm09.py:149`); guard **+9 TC** `cr84_a..i` ⇒ `test_mobile_oas` **1008 OK** · `test_mobile_docset` **9 OK**. File chạm: 02 (BR-09-23 + E-09-16..20 + compliance) · 04 §3.10 · 05 §16 + §10.4 + §11.1 + §15.3 · 06 §CMEvidencePhoto · 07 §XII + BR-map · README · OAS + 2 guard test. Handoff → **[BE] Bước-4** (predicate + 4 điểm tiêu thụ + 1 mã MSG + `gen_fe_messages` + 12 TC; ⚠️ **REFRESH CITE** + **LẬT `cr84_i`** PENDING-BE, counter GIỮ 1008), **[FE] cùng vòng** (dải bằng chứng + chip/nút đính ảnh theo `missing_idxs` + tooltip `reason` nguyên văn + 7 TC RENDER). **KHÔNG `bench migrate`** (0 DocType/field mới)) · Trước đó 2026-07-27 (**AC-CR-82 [BA spec — Bước-2, slice contract ĐÓNG]** — `getRepairWorkOrder` phơi **`available_actions[]` 6 CTA server-driven** (ĐÚNG 6 phần tử, thứ tự CỐ ĐỊNH `[assign_technician, submit_diagnosis, request_spare_parts, start_repair, close_work_order, confirm_inspection]`, đủ 6 kể cả terminal) — **đóng NỬA CM của mobile CR-74**, mirror **AC-CR-77** (nửa PM) ⇒ 3 màn Chi tiết (PM/CM/Sự cố) dùng CHUNG 1 từ vựng CTA `AvailableAction`. `enabled = transition_allowed ∩ has_cap ∩ business_gate`; `reason` VI HẰNG (D9: `enabled=false ⟺ reason≠""`). **2 SELF-CORRECTION so với acceptance PM:** (1) **A3 — cap phải là HỘI 2 tầng**: lớp API gate `repair.write` (`api/imm09.py:123/129/137/143`) nhưng lớp service gate `repair.create` (`services/imm09.py:1882/1724/1748/1767`) ⇒ lấy 1 tầng là **advertise RỘNG HƠN enforce** = đúng bug đang đóng (ADR-IMM09-CTA-03); (2) **A2 — parity là bất biến 1 CHIỀU**: `request_spare_parts` @source **KHÔNG có state-guard** (`services/imm09.py:1953`) nên `⟺` sẽ buộc quảng cáo hành động trên phiếu `Completed`/`Cannot Repair` (**docstatus=1**) ⇒ chốt **INV-CMCTA-1a soundness 54/54** + **1b completeness 45/54** kèm allowlist chỉ-giảm `_ADVERTISE_NARROWER_THAN_ENFORCE` (ADR-IMM09-CTA-02). Thêm **D-CM-3**: `start_repair` có endpoint nhưng màn Chi tiết **0 nút** ⇒ FE thêm `cta-start-repair`. **BA slice ĐÓNG (CONTRACT-ONLY, verify THẬT):** OAS `RepairWorkOrderDetail` += `available_actions` (TÁI DÙNG `AvailableAction`, `paths` GIỮ **108** · `schemas` GIỮ **283** · `parameters` GIỮ **38**) + guard **+8 TC** `cr82_a..h` ⇒ `test_mobile_oas` **991 OK** · `test_mobile_docset` **9 OK**. File chạm: 04 §3.1-ter · 05 §15 + §3.2 + §10.4 · 06 §CMWorkOrderDetail · 07 §XI · README · OAS + 2 guard test. Handoff → **[BE] Bước-4** (6 hằng `*_FROM` + `_REPAIR_ACTION_SPECS` + `_build_repair_available_actions` + 12 TC parity 54 ô; ⚠️ **refresh 8 cite** vì dòng `services/imm09.py` sẽ dịch + bồi `cr82_i` ⇒ 991→992), **[FE] cùng vòng** (7 nút ↔ 6 khoá + tooltip `reason` + fallback + 7 TC render). **KHÔNG `bench migrate`** (0 DocType/field mới)) · Trước đó 2026-07-27 (**AC-CR-79 [BA spec — Bước-2]** — **whitelist khoá `filters` cho `list_repair_work_orders` = SSoT; khoá lạ nay 400 IN-ENVELOPE thay vì HTTP-500 lộ SQL**. **Probe LIVE 2026-07-27:** `imm09.list_work_orders({"khong_ton_tai_abc":"x"})` → `OperationalError (1054, "Unknown column 'tabAsset Repair.khong_ton_tai_abc' in 'WHERE'")`; đối chứng chống vacuous `{"sla_breached":"1","is_repeat_failure":"1"}` → **OK** (2 cột THẬT, web FE gửi đúng). **Chốt:** `_ALLOWED_FILTER_KEYS` **18 khoá** ở `services/imm09.py` (15 cột THẬT + 3 khoá ảo `open`/`sla_breached_live`/`search`); **CỐ Ý loại** `parts_hold_hours`/`parts_hold_started` (nội bộ BR-09-10; `parts_hold_started` còn bị `_finalize_list_row` pop khỏi payload). Validate **TRƯỚC** `pop_search`/`_apply_open_drill`/`_normalize_filters` (AC5) ⇒ `open` vẫn thua `status` đơn lẻ. Envelope + 3 ADR + delta OAS + counters: **canonical** [`../imm-08/05 §14`](../imm-08/05_API_Specification.md). **KHÁC IMM-08:** `CMWorkOrderListView.buildFilters()` gửi 6 khoá **đều hợp lệ** ⇒ FE CM chỉ sửa **banner-không-thay-bảng** (`:219` đang `v-else-if` làm bảng biến mất khi lỗi), **không** sửa `buildFilters`. **Đóng `CR-70` sổ mobile KÈM CẢI CHÍNH** (ledger §10.4): CR-70 nói "BE bỏ qua im lặng" là **SAI** — BE **CRASH**; và ví dụ `asset_ref` của CR-70 là **khoá HỢP LỆ** (còn là khoá `apply_vendor_scope` bơm). **KHÔNG tự sửa repo mobile** (repo KHÁC — chỉ đề nghị sync). File chạm: 04 §3.9 · 05 §14 + §10.4 · 06 §FilterKeyError · 07 §X · README. ⚠️ **Slice contract KHÔNG đóng ở Bước-2** ⇒ [BE] land `.py` TRƯỚC, rồi OAS + `cr79_a..h` + 6 counter cùng vòng; [FE] cùng vòng + test RENDER) · Trước đó 2026-07-27 (**AC-CR-78 [BA spec — Bước-2]** — `getRepairWorkOrder` phơi **`spare_parts_used[]` typed** + **trạng thái phiếu xuất kho THẬT**: mỗi dòng giữ nguyên **9 khoá domain** (1:1 `spare_parts_used.json`) + **ĐÚNG 2** khoá phái sinh `stock_entry_status` ∈ {OK, MISSING, NOT_FOUND} · `stock_entry_ok` **int 0\|1** (quirk CR-01, KHÔNG boolean); thêm top-level `parts_pending_stock_entry`. **Lý do tồn tại:** parity **display ⇔ enforcement** (INV-PARTS-1) — 2 khoá phái sinh tính bằng **CÙNG predicate SSoT** `_spare_parts_stock_entry_statuses` với validator BR-09-02 ⇒ hết **"gõ mã mò"** trên mobile (đóng `CR-71` sổ mobile) và hết **badge XANH GIẢ** cho `stock_entry_ref` treo trên web. **Self-Correction:** mẫu code trong `04` từng ghi `frappe.db.exists("Stock Entry", …)` — DocType thật là **`AC Stock Movement`**, đã sửa. ADR-IMM09-PARTS-01/02/03. OAS delta: `paths` GIỮ **107** · `schemas` **280 → 281** (+`RepairSparePartUsedItem`) · +2 property trên `RepairWorkOrderDetail`. File chạm: 02 (BR-09-02) · 04 §3.8 · 05 §13 + §10.4 + §6 · 06 §SparePartsStockEntry · 07 §IX · README. ⚠️ **Slice contract KHÔNG đóng ở Bước-2** (mirror AC-CR-77 — cite phải trỏ dòng THẬT của symbol chưa tồn tại) ⇒ Handoff → **[BE] Bước-4** land `services/imm09.py` TRƯỚC rồi dán OAS + guard `cr78_a..h` + 7 counter **cùng vòng (atomic)**; **[FE] cùng vòng** cột 3 trạng thái + dải cảnh báo + **test RENDER**) · Trước đó 2026-07-25 (CR-74 [BA spec] — **read-gate CHI TIẾT `get_repair_work_order`**: 3 lớp ROLE→EXISTS→ROW (`assert_doctype_read_permission` TRƯỚC `exists` ⇒ 0 existence-oracle · `assert_can_read_doc` = `frappe.has_permission(doc=…)` dispatch hook), 403 trả **TRONG envelope trên HTTP-200** (client hiển thị message, KHÔNG logout). 0 DocType/field/endpoint/param/DocPerm/cap mới. SSoT [ADR-IMM00-LIST-SCOPE §9](../imm-00/ADR-IMM00-LIST-SCOPE.md) D8/D9/D10. **BA slice ĐÓNG (CONTRACT-ONLY):** OAS 4 op cải chính ngữ nghĩa 403 + guard `test_mobile_oas` **+6 TC** `TestMobileDetailReadGate` (923 OK, mutation-verified) + `test_mobile_docset` sync (9 OK). File: 05 §12 + 07 §VIII + README. Handoff → [BE] Bước-4 (dán khuôn vào `services/imm09.py::get_work_order` + TC-01..06 + guard tĩnh **G5**; **cần USER reload gunicorn `--preload`**), [FE] render 403 in-envelope KHÔNG logout) · Trước đó 2026-07-25 |
| Khối kiến trúc | C. KHỐI 3 |
| Đợt triển khai | 1 |
| Owner | PTP Khối 2 · Workshop / Nhóm TBYT |

> File index của module IMM-09. Map cũ ↔ template chuẩn theo `docs/template/` (v4.1+).
> **Source docs đã archive tại `docs/architecture/archive/imm-09/`** — 6 file format cũ (IMM-09_*.md) đã được chuyển vào archive sau khi template chuẩn 02–09 được bổ sung đầy đủ và cross-check với codebase.

## Map cũ → Template chuẩn

| Template (chuẩn mới) | File hiện có | Trạng thái |
|---|---|---|
| 02 Analysis_Design | [`02_Analysis_Design.md`](./02_Analysis_Design.md) | ✅ Template chuẩn |
| 03 Diagrams (ERD/Class/Sequence/Communication/Package) | [`03_Diagrams.md`](./03_Diagrams.md) | ✅ Template chuẩn |
| 04 Backend_Design | [`04_Backend_Design.md`](./04_Backend_Design.md) | ✅ Template chuẩn |
| 05 API_Specification | [`05_API_Specification.md`](./05_API_Specification.md) | ✅ Template chuẩn (envelope `{success,data}`, đồng bộ codebase) |
| 06 Frontend_Design | [`06_Frontend_Design.md`](./06_Frontend_Design.md) | ✅ Template chuẩn (route/component paths đồng bộ views/cm/) |
| 07 §I Test Plan + §III Security + §IV Code quality | [`07_Testing_QA.md`](./07_Testing_QA.md) | ✅ Có |
| 07 §II UAT Script | Archive: `docs/architecture/archive/imm-09/IMM-09_UAT_Script.md` | ✅ Archived |
| 08 Deployment + QMS Mapping | [`08_Deployment.md`](./08_Deployment.md) | ✅ Có |
| 09 User Guide + Release Notes + Traceability | [`09_Release.md`](./09_Release.md) | ✅ Có |

## Files hiện có

**Template chuẩn mới (02–06):**
- [`02_Analysis_Design.md`](./02_Analysis_Design.md) — Phân tích nghiệp vụ + Use Cases + BR + NFR
- [`03_Diagrams.md`](./03_Diagrams.md) — ERD + Class + Sequence + Communication + Package
- [`04_Backend_Design.md`](./04_Backend_Design.md) — DocType + Workflow + Service + API + Scheduler
- [`05_API_Specification.md`](./05_API_Specification.md) — Catalog 12 endpoints + envelope chuẩn `{success,data}` (đồng bộ codebase, search_spare_parts live)
- [`06_Frontend_Design.md`](./06_Frontend_Design.md) — Sitemap + Components (tên file views/cm/ thực tế) + Store (đồng bộ code) + i18n

**Template chuẩn mới (07–09):**
- [`07_Testing_QA.md`](./07_Testing_QA.md) — Test plan + UAT 12 scenario + Security review
- [`08_Deployment.md`](./08_Deployment.md) — Deployment plan + QMS Mapping (NĐ98/WHO HTM/ISO 13485)
- [`09_Release.md`](./09_Release.md) — User guide tiếng Việt + Release notes + Traceability matrix

**Format cũ (đã archive — xem `docs/architecture/archive/imm-09/`):**
- `IMM-09_API_Interface.md`
- `IMM-09_Functional_Specs.md`
- `IMM-09_Module_Overview.md`
- `IMM-09_Technical_Design.md`
- `IMM-09_UAT_Script.md`
- `IMM-09_UI_UX_Guide.md`

## Roadmap chuẩn hóa

- [x] Bổ sung **`02_Analysis_Design.md`** — Phân tích nghiệp vụ + Use Cases + BR + NFR (template chuẩn)
- [x] Bổ sung **`03_Diagrams.md`** — ERD + Class + Sequence + Communication + Package (template chuẩn)
- [x] Bổ sung **`04_Backend_Design.md`** — DocType + Workflow + Service + Scheduler + Migration (template chuẩn)
- [x] Bổ sung **`05_API_Specification.md`** — API Catalog 12 endpoints + envelope `{success,data}` chuẩn AssetCore
- [x] Bổ sung **`06_Frontend_Design.md`** — Sitemap + Mockup + Components + Store + i18n (template chuẩn)
- [x] Bổ sung **`07_Testing_QA.md`** — Test plan + UAT 12 scenario + Security review + Code quality
- [x] Bổ sung **`08_Deployment.md`** — Deployment plan + QMS Mapping (NĐ98/WHO HTM/ISO 13485)
- [x] Bổ sung **`09_Release.md`** — User guide tiếng Việt + Release notes v1.0.0 + Traceability matrix

## Tham chiếu

- Template chuẩn: [`../template/`](../template/)
- Migration guide: [`../template/MIGRATION_GUIDE.md`](../template/MIGRATION_GUIDE.md)
- Codebase ground truth (BE): `assetcore/services/imm09.py` · `assetcore/api/imm09.py`
- Codebase ground truth (FE): `frontend/src/types/imm09.ts` · `frontend/src/api/imm09.ts`

---

*Module index — auto-generated khi migration. Khi update file thực tế, manual sync README này.*
