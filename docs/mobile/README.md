# AssetCore Mobile — Docset (backend-for-mobile + repo native handoff)

| Mục | Giá trị |
|---|---|
| Initiative | **AssetCore Mobile** — backend (BE) + hợp đồng API trong repo này · APK native ở **repo riêng** |
| Phase hiện tại | **PHASE A — Kiến trúc & Feasibility** — 🟢 hoàn tất (exit-ready) |
| Owner | BA Lead + System Architect (mobile) |
| Trạng thái | Phase A exit-ready (chờ USER mở Phase B — xem [`11-phase-a-exit.md`](./11-phase-a-exit.md)) |
| Cập nhật | 2026-07-13 |

> **Đây là index/cross-link hub của docset mobile.** AssetCore = **backend + hợp đồng API**; UI native nằm ở **repo riêng**, chỉ GỌI API. Docset này chốt **quyết định + roadmap + hợp đồng** (KHÔNG impl code).
> Mọi claim kỹ thuật trong docset đã verify tại source **Frappe v15.107.2** với evidence `file:line` (xem `02-deploy-feasibility.md` + các ADR). Tổng quan đầy đủ: [`00-overview.md`](./00-overview.md).

---

## 1. Chỉ mục đầy đủ (20 mục: 15 chương 00–14 + ADR-001..004 + openapi/)

### Chương đánh số (00–14)

| File | Mô tả 1 dòng |
|---|---|
| [`00-overview.md`](./00-overview.md) | Tổng quan initiative — mục tiêu · **3 quyết định chốt** (§2) · roadmap 6 phase · chỉ mục · convention đặt tên · glossary OAuth2. |
| [`01-architecture.md`](./01-architecture.md) | Kiến trúc Phase A — topology · 3 lằn ranh trách nhiệm · auth-flow PKCE+refresh (mức kiến trúc) · API versioning · OpenAPI-as-contract · điểm chèn push/offline/security. |
| [`02-deploy-feasibility.md`](./02-deploy-feasibility.md) | Khảo sát deploy/OAuth2/CORS read-only tại source — gaps + blocker triển khai Phase B (public host, allow_cors, OAuth Client=0). |
| [`03-auth-oauth2.md`](./03-auth-oauth2.md) | Auth deep-dive (A2) — sequence Authorization Code + PKCE S256 + refresh + revoke (a→f) · vòng đời token (TTL 3600s/refresh/revoke/storage Keychain) · scope↔capability 1 SSoT · checklist OAuth Client. |
| [`04-api-contract.md`](./04-api-contract.md) | Hợp đồng API (A3) — success/error envelope shape THẬT · ErrorCode 15 mã + HTTP map · quirk HTTP-200 wrapper · pagination contract · versioning/deprecation · param convention `str=""` · **§8.1 convention `operationId` (A10)** — luật camelCase verbNoun + bảng 15/15 codegen-able · **§8.2 contract-integrity & codegen-validity (A12/A13)** — 0 dangling `$ref` + bảng RESERVED 10 orphan-component allow-list + **A13 error-response coverage** (401 lên 12 path MVP · 429 lên 2 path `@rate_limit`) (guard `TC-MOB-OAS-09/10/11`). |
| [`05-personas-mvp.md`](./05-personas-mvp.md) | Persona & MVP field-tech (A4) — persona KTV hiện trường · hành trình end-to-end 5 feature · bảng MÀN↔API grounded · phân loại offline per-màn · map quyền/cap per-màn bám SSoT rbac.py. |
| [`06-push-fcm.md`](./06-push-fcm.md) | Push FCM design (A5) — cơ chế push (FCM Admin SDK trực tiếp) · DocType **AC Mobile Device Token** spec · MAP 6-event→FCM (kênh #3 tại `_dispatch`) · payload + deep-link + opt-in/out + bảo mật + audit. |
| [`07-offline-sync.md`](./07-offline-sync.md) | Offline & Sync strategy (A6) — read-cache ETag/If-Modified-Since · write-queue idempotency-key contract · conflict optimistic-lock qua `modified` → 409 · lifecycle hàng đợi · audit chỉ-khi-ghi-thật. |
| [`08-security-compliance.md`](./08-security-compliance.md) | Bảo mật & Tuân thủ (A7) — threat model 7 mối (T1–T7) mỗi dòng evidence · NĐ98 audit-from-mobile · phân loại mitigation 3 nhóm · checklist go-live · KPI. |
| [`09-native-repo-guide.md`](./09-native-repo-guide.md) | **Handoff repo native (A8)** — skeleton repo (Flutter/RN) · sinh API client từ OpenAPI · ENV trỏ BE · wire OAuth2 (link sang 03) · build APK + CI · checklist khởi tạo repo. |
| [`10-deploy-ops.md`](./10-deploy-ops.md) | **Runbook go-live mobile-BE (A9)** — quy trình THỰC THI CÓ THỨ TỰ (khác 02 feasibility): §1 bật OAuth2 · §2 CORS list-origin · §3 public host+QR deep-link · §4 FCM creds · §5 versioning `Sunset`/`Deprecation` · §6 checklist go-live (pre-flight/execute/smoke curl) · §7 rollback. Mỗi bước HARD-STOP USER. |
| [`11-phase-a-exit.md`](./11-phase-a-exit.md) | **Phase-A EXIT GATE (A11 — đóng Phase A)** — §1 traceability matrix 6 flow MVP × 7 cột (màn · endpoint `file:line` @source · capability · operationId · offline-class · push-event · STUB-status; 9 nghiệp vụ + 3 auth verify @source, 15/15 operationId) · §2 Phase-B prereqs/blocker hợp nhất (B-1..B-8, trỏ ngược 02/03 §4/08 §4/10, chủ thể=USER) · §3 checklist go/no-go A→B đo được (13 đã-đạt vs 9 chờ-USER) · §4 KPI/acceptance exit. doc-only. **A14: matrix §1 (endpoint↔capability) nay GUARDED máy-đọc bởi `assetcore/tests/test_mobile_capability_map.py` (`TC-MOB-CAP-01..06`) — binding `(DocType, ptype)` ↔ SSoT `rbac.py` + anti-cap-creep `v97.c30c69b8974d`.** |
| [`12-phase-b-preflight.md`](./12-phase-b-preflight.md) | **Phase-B pre-flight verifier (B0-PREFLIGHT — Phase A→B bridge)** — biến checklist OAuth Client (`03 §4` / blocker B-1) thành hợp đồng CHẠY ĐƯỢC: §0 mục tiêu/out-of-scope + admin-only ngoài hợp đồng app · §1 lệnh `bench execute …verify_oauth_client` + diễn giải 7 check · §2 map 7 check ↔ B-1 field `03 §4` ↔ runbook `10 §1` · §3 đọc report `ready`/`blockers` + khắc phục · §4 acceptance. Verifier READ-ONLY (`assetcore/api/mobile/preflight.py`), gate System Manager, chịu count==0 (KHÔNG raise). doc-only + verifier read-only. |
| [`13-be-completion-roadmap.md`](./13-be-completion-roadmap.md) | **MASTER roadmap BE-completion** (cấu trúc USER duyệt 2026-06-11) — hoàn thiện lớp Backend-for-Mobile để repo native gọi API chạy app. DoD TỔNG = MVP field-tech 6-flow E2E trên cloud. **5 EPIC khoá-ID:** C (API Contract codegen-ready: 4 STUB typed + list-element + userinfo + P1 in-handler-error-on-HTTP-200) · B (Auth & Provisioning: refresh ✅ + TTL 3600s + preflight gate + device-token) · D (Push FCM: kênh #3 `_dispatch` + FCM HTTP v1 + DocType) · G (Go-live & Hardening: 5 knob site_config matrix + traceback/CORS/rate-limit) · V (Codegen Verify + Handoff: gen Dart/TS+Kotlin-config + E2E runbook + gói handoff). Thứ tự C→B∥D-design→G→D→V; mỗi TASK tag `[AUTO]` vs `[HARD-STOP USER]`. doc-only. |
| [`14-e2e-field-tech-runbook.md`](./14-e2e-field-tech-runbook.md) | **E2E field-tech runbook CHẠY-ĐƯỢC (EPIC-V V3)** — hợp nhất coverage phân mảnh (`11 §1` design-matrix + `10 §6.3` smoke 2-lệnh + `09 §6.2` checklist) thành **1 sequence 6 flow tuần tự**: login OAuth2+refresh → quét QR → báo hỏng → WO PM/CM/Cal → "phiếu của tôi" → push FCM. Mỗi bước: **operationId trục** (`getOAuthToken`/`resolveQrToken`/`reportIncident`/`createPmWorkOrder`/`listPmWorkOrders`/`registerDeviceToken`) + lệnh kiểm-được THẬT (curl + dart-dio) + **expected envelope** (`success`+`code`+`http_status`, route-by-VALUE) + tiền-điều-kiện (OAuth Client+bearer) + tag `[AUTO]`/`[HARD-STOP USER]`. §2 hợp đồng đọc-envelope (HTTP-200 quirk + Decision-B closed-schema + refresh-on-401). doc-only. |

### ADR (Architecture Decision Records)

| File | Mô tả 1 dòng |
|---|---|
| [`ADR-MOBILE-001.md`](./ADR-MOBILE-001.md) | ADR kiến trúc nền — 5 quyết định: (a) wire-not-write OAuth · (b) capability=1 SSoT · (c) reuse-endpoint wrapper · (d) OpenAPI=hợp đồng · (e) no session-cookie native + evidence `file:line`. **Accepted.** |
| [`ADR-MOBILE-002.md`](./ADR-MOBILE-002.md) | ADR cơ chế push (A5) — CHỐT **FCM Admin SDK trực tiếp** (credentials site_config), KHÔNG relay Frappe Cloud + alternatives + consequences + evidence. **Accepted.** |
| [`ADR-MOBILE-003.md`](./ADR-MOBILE-003.md) | ADR offline/sync (A6) — CHỐT idempotency-key client-gen + BE-dedupe · conflict optimistic-lock + server-wins · read-cache ETag · no offline-write-audit tới khi sync thật. **Accepted.** |
| [`ADR-MOBILE-004.md`](./ADR-MOBILE-004.md) | ADR mô hình bảo mật (A7) — CHỐT rate-limit oauth2 ở nginx (no core) · 1 SSoT quyền · CORS list-origin (cấm wildcard prod) · audit NĐ98 chuỗi hiện hữu. **Accepted.** |
| [`ADR-MOBILE-005.md`](./ADR-MOBILE-005.md) | ADR `listUsers` pagination 4-key (C-LISTREAD-USERS) — CHỐT **dedicated schema `UserListPagination`** `{page,page_size,total,total_pages}` KHÔNG `offset` (KHÔNG `$ref Pagination` 5-key), vì `user.list_users` build inline KHÔNG `paginate()` → khai đúng wire-shape tránh strict-codegen deser crash. **Accepted.** |
| [`ADR-MOBILE-006.md`](./ADR-MOBILE-006.md) | ADR POST-action contract pattern (C8-ACTION, `acknowledgeIncident` ĐẦU TIÊN) — CHỐT **`IncidentActionEnvelope`/`IncidentActionResponse`** generic (tái dùng start_work/resolve/close) closed `{name,status}` route-by-VALUE C6/C7 · `requestBody` INLINE (không component) · **403 SINGLE-SHAPE `Forbidden`** (in-handler cap-403 đã phủ bởi 200-oneOf Error) KHÁC `reportIncident` DUAL-403. **Accepted.** |
| [`ADR-MOBILE-007.md`](./ADR-MOBILE-007.md) | ADR PM-action contract (C8-ACTION-PM, `submitPmResult` ĐẦU TIÊN cho PM WO → Completed, sinh `pm_completed`) — CHỐT **`PmSubmitResultEnvelope`/`PmSubmitResultResponse`** RIÊNG 5-key `{name,new_status,is_late,next_pm_date,cm_wo_created}` (KHÔNG reuse Repair/IncidentActionResponse — C3-split: `new_status`≠`status` +3 field PM-riêng) · request mang **nested child-array** `checklist_results[]` (`PmChecklistResultInput`) · **⚠️ DIVERGENCE** BE bare `@whitelist` (KHÔNG `methods=['POST']`) ↔ contract POST → fix=backlog HARD-STOP. **Accepted.** |
| [`ADR-MOBILE-008.md`](./ADR-MOBILE-008.md) | ADR session-bootstrap contract (FLOW-1, `getUserContext` who-am-I sau login → persona-aware home + flow-gating) — CHỐT **`allow_guest=True` ⇒ slot {200,401} KHÔNG 403** (Guest VÀO handler → in-handler `_err 401`, KHÔNG dispatcher cap-403) · exempt 401/403 MVP-business symmetry qua tập **`_ALLOW_GUEST_PATHS`** (mirror `openid_profile`, NHƯNG AssetCore-handler KHÔNG nhồi `_AUTH_PATHS`) · NOT ∈ `_MVP_BUSINESS_PATHS`/`_MVP_READ_ENVELOPE`/C5 · `UserContextData` closed **EXACT 13 prop** `required[user]` graceful-degradation · **INT-VS-BOOL TRAP** 2 flag `is_profile_completed`/`has_employee_link` = `integer enum[0,1]` (KHÔNG `boolean` dù BE emit `bool()`). **Accepted.** |
| [`04-api-contract.md §8.15`](./04-api-contract.md) | ADR calibration-action contract (C8-ACTION, `submitCalibration` COMPLETION/TERMINAL cho Calibration Record: `docstatus 0→1` → Passed/Failed/Conditionally Passed) — kế thừa mẫu §8.10/ADR-MOBILE-006; CHỐT **`SubmitCalibrationEnvelope`/`SubmitCalibrationResponse`** RIÊNG 4-key `{name,status,overall_result,next_calibration_date}` (KHÔNG reuse Pm/Repair/IncidentActionResponse — C3-split cross-domain: `overall_result` + `next_calibration_date` calibration-riêng) · request closed `{name}` (0 optional) · **403 SINGLE-SHAPE `Forbidden`** · ~~**⚠️ DIVERGENCE** BE bare `@whitelist` `api/imm11.py:114` (KHÔNG `methods=['POST']`) ↔ contract POST → fix=backlog HARD-STOP (`_PARITY_VERB_ALLOWLIST` 2→3)~~ **[SELF-CORRECTION R34] DIVERGENCE ĐÃ CLOSED** — `submit_calibration` `api/imm11.py:114` nay `methods=['POST']` (R33), `_PARITY_VERB_ALLOWLIST`=`set()`, clean POST. **Accepted.** |
| [`ADR-MOBILE-009.md`](./ADR-MOBILE-009.md) | ADR repair-action TERMINAL-THẬT (C8-ACTION, `confirmInspection` nghiệm thu Repair WO: `Pending Inspection → Completed`, `docstatus 0→1`) — kế thừa §8.10/ADR-MOBILE-006; đóng dead-end CUỐI chuỗi repair (`closeWorkOrder` chỉ tới Pending Inspection NON-terminal; `allowed_transitions[]` surface CTA `Completed` nhưng thiếu endpoint). CHỐT **`ConfirmInspectionEnvelope`/`ConfirmInspectionResponse`** RIÊNG 4-key `{name,status,mttr_hours,sla_breached}` **KHÔNG reuse `CloseWorkOrderResponse` dù SHAPE TRÙNG** (C3-split cross-ACTION: `status` INVARIANT single-value `[Completed]` ≠ 2-value `[Pending Inspection, Cannot Repair]` — precedent ResolveIncident/CloseIncident) · `status.enum=[Completed]` · `sla_breached` integer enum[0,1] · request closed `{name}` (0 optional, oneOf json+form) · cap **`repair.submit`** (phê-duyệt-chất-lượng ≠ `repair.create` KTV) · **403 SINGLE-SHAPE `Forbidden`** · 2 Error-on-HTTP-200 (`IMM09_NOT_FOUND` 404 + `IMM09_BAD_STATE` 409) · **✅ clean POST** (`methods=['POST']` `api/imm09.py:103` — KHÔNG verb-divergence, KHÔNG `_PARITY_VERB_ALLOWLIST`). **Accepted.** |
| [`ADR-MOBILE-010.md`](./ADR-MOBILE-010.md) | ADR repair spare-parts sub-flow (`searchSpareParts` GET RAW-list no-403 + `requestSpareParts` POST CLEAN) — `searchSpareParts` `data` = array `<SearchSparePartItem>` TRẦN (no-pagination) slot `{200,401}` no-403 (handler KHÔNG `rbac.require`, mirror `getUserContext` ADR-MOBILE-008) · `requestSpareParts` **`RequestSparePartsResponse`** RIÊNG 4-key `{name,status,updated,allocation}` (KHÔNG reuse `RepairActionResponse` 2-key — C3-split; `updated` GENUINE integer count, `allocation` Gate-2 IMM-15 nullable) · requestBody oneOf json+form `parts[]` nested · **✅ CLEAN POST** (`methods=['POST']` `api/imm09.py:77` đã sẵn @HEAD — Self-Correction premise "flip bare→POST" STALE) · `_PARITY_VERB_ALLOWLIST`=`set()`. **Accepted.** |
| [`ADR-MOBILE-011.md`](./ADR-MOBILE-011.md) | ADR calibration measurement-entry (`addMeasurement` POST mắt-xích-GIỮA chuỗi calibration-detail: `createCalibration → addMeasurement (×N) → submitCalibration`) — CHỐT **`AddMeasurementEnvelope`/`AddMeasurementResponse`** RIÊNG 2-key `{name,measurement_count}` (KHÔNG reuse `SubmitCalibrationResponse`/`*ActionResponse` — C3-split; `measurement_count` = **GENUINE integer count** `len(doc.measurements)`, KHÔNG enum[0,1], precedent `updated`/ADR-MOBILE-010) · requestBody oneOf json+form required EXACT 6 `[name,parameter_name,unit,nominal_value,tolerance_positive,tolerance_negative]` + optional `measured_value` nullable · **403 SINGLE-SHAPE `Forbidden`** cap `calibration.write` · 2 Error-on-HTTP-200 (`IMM11_CAL_NOT_FOUND` 404 + `IMM11_ALREADY_SUBMITTED` 409) · **⚡ VERB-FLIP-THIS-ROUND** (`api/imm11.py:120` bare→`methods=['POST']`, đóng verb-parity gap R33 BỎ SÓT — KHÁC §8.15 backlog) · `_PARITY_VERB_ALLOWLIST` GIỮ `set()`. **Accepted.** |
| [`ADR-MOBILE-012.md`](./ADR-MOBILE-012.md) | ADR PM-dispatch (`assignPmTechnician` POST mắt-xích-GIỮA PM-detail: `createPmWorkOrder → assignPmTechnician → submitPmResult`; parity repair `create → assignTechnician → start`) — CHỐT **`AssignPmTechnicianEnvelope`/`AssignPmTechnicianResponse`** RIÊNG 3-key `{name,status,assigned_to}` (**C3-split RIÊNG**, KHÔNG reuse repair `AssignTechnician*` dù shape trùng — `status`=**PMStatus** 7-state value "In Progress" ≠ RepairStatus "Assigned"; precedent `confirmInspection` ADR-MOBILE-009) · requestBody INLINE json-only required EXACT 2 `[name,technician]` + optional `scheduled_date` (KHÁC repair `priority`) · **403 SINGLE-SHAPE `Forbidden`** cap `pm.write` · 3 Error-on-HTTP-200 (`IMM08_WO_NOT_FOUND` 404 + `IMM08_BAD_STATE` 409 + `VALIDATION` 422) · asset→Under Maintenance + Lifecycle Event audit (`services/imm08.py:678`) · **⚡ VERB-FLIP-THIS-ROUND** (`api/imm08.py:46` bare→`methods=['POST']`, đóng verb-parity gap R33 BỎ SÓT — sibling imm08 của `add_measurement`) · `_PARITY_VERB_ALLOWLIST` GIỮ `set()`. **Accepted.** |
| [`ADR-MOBILE-013.md`](./ADR-MOBILE-013.md) | ADR PM→CM escalation (`reportMajorFailure` POST escalation PM-detail: PM hỏng nặng → `Halted–Major Failure` + asset `Out of Service` + CM WO khẩn; đóng nút "Báo lỗi nghiêm trọng (→ CM)") — CHỐT **`ReportMajorFailureEnvelope`/`ReportMajorFailureResponse`** RIÊNG closed 4-key `{pm_wo,new_status,cm_wo_created,asset_status}` (`new_status`=**PMStatus** 7-state value "Halted–Major Failure", `asset_status`="Out of Service"; `services/imm08.py:792-797`) · requestBody INLINE json-only required EXACT 2 `[pm_wo_name,failure_description]` · **403 SINGLE-SHAPE `Forbidden`** cap `pm.write` · 1 Error-on-HTTP-200 (`IMM08_WO_NOT_FOUND` 404) · **⚡ VERB-FLIP-THIS-ROUND** (`api/imm08.py:74` bare→`methods=['POST']`, write KHÔNG idempotent) · **🐞 SIGNATURE-FIX** DROP `failed_item_indexes` (handler↔service mismatch → `TypeError`/500) + **CM-WO-FIELD-FIX** (`failure_description` mandatory + `repair_type` "Emergency"→"Breakdown") · `_PARITY_VERB_ALLOWLIST` GIỮ `set()`. **Accepted.** |
| [`ADR-MOBILE-014.md`](./ADR-MOBILE-014.md) | ADR PM reschedule (`reschedulePm` POST, **ĐÓNG NỐT** action-set `PMWorkOrderDetailView`: thiết bị đang dùng → hoãn lịch PM → `Pending–Device Busy`; đóng nút "Hoãn lịch (thiết bị bận)" — mắt-xích CUỐI, 0 nút dead-end) — CHỐT **`ReschedulePmEnvelope`/`ReschedulePmResponse`** RIÊNG closed 4-key `{name,old_date,new_date,status}` (date-pair DUY NHẤT; `status`=**PMStatus** 7-state value "Pending–Device Busy" en-dash U+2013; `services/imm08.py:823`) · requestBody INLINE json-only required EXACT 3 `[name,new_date,reason]` (`reason.minLength:5` mirror guard `:808` · `new_date.format:date`) · **403 SINGLE-SHAPE `Forbidden`** cap `pm.reschedule` (`rbac.py:94`, KHÁC sibling `pm.write`) · 2 Error-on-HTTP-200 (`VALIDATION` 422 reason<5 + `IMM08_WO_NOT_FOUND` 404) → `Error.http_status ⊇ {404,422}` · asset khôi phục Active nếu đang In Progress + Lifecycle Event audit (`services/imm08.py:821-822`) · **🟢 ATOMIC-THIS-ROUND** (KHÔNG verb-flip + KHÔNG signature-fix — handler `api/imm08.py:86` ĐÃ `methods=['POST']` + signature ĐÃ khớp service; KHÔNG đụng api/service ⇒ `generate_spec` UNCHANGED → `test_oas_d12/d15/d17` re-verify, KHÔNG re-baseline) · `_PARITY_VERB_ALLOWLIST` GIỮ `set()`. **Accepted.** |
| [`ADR-MOBILE-015.md`](./ADR-MOBILE-015.md) | ADR `listIncidents` scope `reported_by` (**ĐÓNG known-gap A2** tab "Báo hỏng của tôi" — MyWorkOrdersView › MVP-5c; contract TRƯỚC claim "scope reported_by" mà `list_incidents` KHÔNG có cơ chế → claim suông) — CHỐT **query-param opt-in `mine`** (`components/parameters/IncidentMine` int enum `[0,1]` default 0, mirror `IncidentOpen`) → `_build_incident_filters` seed `extra["reported_by"]=session.user` TRƯỚC quyết định nhánh ⇒ AND `status`/`severity`/`asset`/`open` KỂ CẢ nhánh status return-sớm · **INV count==rows** (`frappe.db.count`+`get_all` cùng `filters`) · `mine=0`/absent **BYTE-IDENTICAL** cũ (backward-compat web-FE `IncidentListView`) · **0 path mới** (path-count GIỮ 46) + **0 schema-component** (chỉ +1 param `$ref`'d ngay) · `mine`=filter-ứng-dụng KHÔNG-phải-security-boundary (read-gating GIỮ DocPerm `corrective.read`) · Guest-guard `api/imm12.py:212` UNCHANGED · **Alternatives loại:** endpoint riêng (+path) / permission auto-scope (vỡ view manager/QA) · BE Bước-4: `services/imm12.py`+`api/imm12.py`+`test_imm12`+`test_mobile_oas`(14b/14d). **Accepted.** |
| [`ADR-MOBILE-016.md`](./ADR-MOBILE-016.md) | ADR `listPmWorkOrders` scope `assigned_to` (**ĐÓNG known-gap A2 ĐỐI XỨNG** tab "Phiếu PM của tôi" — MyWorkOrdersView › MVP-5a; gap "next" nêu đích danh ở ADR-MOBILE-015 §Consequences; contract TRƯỚC claim "Scope theo user (count==rows, permission-aware)" mà `list_pm_work_orders` KHÔNG có cơ chế `assigned_to` → claim suông) — CHỐT **query-param opt-in `mine`** (`components/parameters/WorkOrderMine` int enum `[0,1]` default 0, mirror `IncidentMine`) → `api/imm08.py::list_pm_work_orders` inject `f["assigned_to"]=session.user` **SAU `apply_vendor_scope`** (KHÁC `IncidentMine` seed @service — vì PM filters là JSON-blob đã `parse_json` @api) ⇒ AND với mọi key trong `WorkOrderFilters` blob (kể cả virtual `due_before`/`overdue`) · **INV count==rows** (`count_with_or`+`get_all` cùng `filters`, `BaseRepository.list` base.py:65-71) · `mine=0`/absent **BYTE-IDENTICAL** cũ (backward-compat web-FE `PMWorkOrderListView`) · **0 path mới** (path-count GIỮ 46) + **0 schema-component** (chỉ +1 param `$ref`'d ngay) · `mine`=filter-ứng-dụng KHÔNG-phải-security-boundary (read-gating GIỮ DocPerm `pm.read`+`apply_vendor_scope`) · **🟢 KHÔNG đụng service/repo** (chỉ +1 param +1 nhánh `if mine:` @api) · **Alternatives loại:** endpoint riêng (+path) / permission auto-scope (vỡ view supervisor/QA) / seed @service · BE Bước-4: `api/imm08.py`+`test_imm08`+`test_mobile_oas`(14b/14d `_LIST_PARAM_EXPECT`+`_LIST_LIVE_FN` PM). **Accepted.** |
| [`ADR-MOBILE-017.md`](./ADR-MOBILE-017.md) | ADR `listRepairWorkOrders` scope `assigned_to` (**ĐÓNG known-gap A2 — symmetry CUỐI** tab "Phiếu CM của tôi" — MyWorkOrdersView › MVP-5b; gap "next" nêu đích danh ở ADR-MOBILE-016 §Consequences; contract TRƯỚC claim suông "Scope theo user" mà `list_repair_work_orders` KHÔNG có cơ chế `assigned_to`) — CHỐT **query-param opt-in `mine`** **REUSE `components/parameters/WorkOrderMine`** (R38/ADR-MOBILE-016, int enum `[0,1]` default 0 — **KHÔNG tạo component mới**) → `api/imm09.py::list_repair_work_orders` inject `f["assigned_to"]=session.user` **SAU `apply_vendor_scope`** (mirror BYTE-for-BYTE `list_pm_work_orders` — CM filters JSON-blob `parse_json` @api) ⇒ AND với mọi key trong `WorkOrderFilters` blob (kể cả virtual `open`/`sla_breached`) · **INV count==rows** (`count_with_or`+`get_all` cùng `filters`+cùng `permission_query_conditions` `asset_repair_query`, `BaseRepository.list` base.py:65-71) · `mine=0`/absent **BYTE-IDENTICAL** cũ (backward-compat web-FE `RepairWorkOrderListView`) · **0 path mới** (path-count GIỮ 46) + **0 schema-component mới** (REUSE; param-set `{WorkOrderFilters,Page,PageSize}`→+`WorkOrderMine`=4; `generate_spec` get=232/post=256/total=488 UNCHANGED) · `mine`=filter-ứng-dụng KHÔNG-phải-security-boundary (read-gating GIỮ DocPerm `repair.read`+`asset_repair_query`+`apply_vendor_scope`; senior `""`→thu hẹp về "của tôi" là use-case chính) · **🟢 KHÔNG đụng service/repo** (chỉ +1 param +1 nhánh `if mine:` @api) · **Alternatives loại:** endpoint riêng (+path) / component `RepairWorkOrderMine` mới (shape trùng `WorkOrderMine`) / permission auto-scope (vỡ view senior/QA) / seed @service · BE Bước-4: `api/imm09.py`+`test_imm09`+`test_mobile_oas`(14b `_LIST_PARAM_EXPECT`+`_LIST_LIVE_FN` repair, `_EXPECTED_TEST_COUNT` 437→438). **Accepted.** |
| [`ADR-MOBILE-018.md`](./ADR-MOBILE-018.md) | ADR `markAllAsRead` (BULK read-receipt — **ĐÓNG NỐT notification-center action-set** tab "Thông báo" › nút "Đánh dấu tất cả đã đọc", sau `markNotificationAsRead` single FLOW-6) — CONTRACT-ONLY bồi 1 path POST GROUNDED 1:1 endpoint LIVE `layout.mark_all_as_read:120-134` (`@frappe.whitelist(methods=['POST'])` CLEAN POST, 0-PARAM `mark_all_as_read()` @`:121` ⇒ **KHÔNG requestBody** codegen no-arg POST) → `_ok({updated_rows: affected})` @`:134` · **`MarkAllReadResponse` EXACT 1-prop `{updated_rows: integer}`** GENUINE integer count 0..N **KHÔNG enum[0,1]** (phân biệt với `read` int-enum của `NotificationListItem`; mirror `AddMeasurementResponse.measurement_count` R34) · KHÔNG field `status` (Notification Log KHÔNG `workflow_state` ⇒ C3-split cross-domain ≠ mọi `*ActionResponse`, ADR-MOBILE-009) · `200 = oneOf [MarkAllReadEnvelope \| Error]` closed-schema route-by-VALUE `body.success` (Decision-B 0-discriminator) · slot `{200,401,403}` SINGLE-SHAPE `Forbidden` (guest/no-token dispatcher PermissionError HTTP-403; in-handler guest @`:124-125` → 401 ARRIVE HTTP-200 Error) **KHÔNG 404/409** (scope SQL `WHERE for_user=session.user` no lookup-by-name) · path-count **46→47** + opId 46→47 · `mark_all_as_read` ∈ runtime `generate_spec` POST authed ⇒ ∉ `_PARITY_VERB_ALLOWLIST` · live-sig parity `inspect.signature=={}` · **🟢 CONTRACT-ONLY** `git diff` api/services/layout = TRỐNG (BE LIVE) ⇒ KHÔNG reload/migrate · `generate_spec` get/post UNCHANGED `d12/d15/d17` RE-VERIFY · Guard `TestMobileMarkAllReadContract` a..g+i (8 TC) `_EXPECTED_TEST_COUNT` 439→447. **Accepted.** |
| [`ADR-MOBILE-019.md`](./ADR-MOBILE-019.md) | ADR `listCalibrations` scope `technician` (**ĐÓNG-NỐT quartet phiếu-của-tôi** sau PM/CM/Incident — tab "Phiếu hiệu chuẩn của tôi" — MyWorkOrdersView › MVP-5d; summary `[MVP-5d] Hiệu chuẩn của tôi` hứa semantics "của tôi" mà `list_calibrations` KHÔNG có cơ chế `technician` → claim suông) — CHỐT **query-param opt-in `mine`** **REUSE `components/parameters/WorkOrderMine`** (R38/ADR-MOBILE-016, int enum `[0,1]` default 0 — **KHÔNG tạo component mới**) → `api/imm11.py::list_calibrations` (`mine: int = 0` chèn GIỮA filters↔page, mirror imm08/imm09) inject `f["technician"]=session.user` **SAU `apply_vendor_scope("Calibration Record")`** — **cột `technician`** (Link `User` reqd `imm_asset_calibration.json:131`, **KHÁC PM/CM `assigned_to`**; Calibration Record không có cột này) ⇒ AND với mọi key trong `WorkOrderFilters` blob (vd `status`/`calibration_type`) · **INV count==rows** (`count_with_or`+`get_all` cùng `filters`, `CalibrationRepo(BaseRepository).list` base.py:65-71; Calibration Record **KHÔNG có `permission_query_conditions` riêng** `hooks.py:388` ⇒ KHÔNG nhánh count/rows lệch) · `mine=0`/absent **BYTE-IDENTICAL** cũ (backward-compat web-FE `CalibrationListView`) · **0 path mới** (path-count GIỮ 47) + **0 schema-component mới** (REUSE; param-set `{WorkOrderFilters,Page,PageSize}`→+`WorkOrderMine`=4; `generate_spec` get/post/total UNCHANGED) · `mine`=filter-ứng-dụng KHÔNG-phải-security-boundary (read-gating GIỮ DocPerm `calibration.read`+`apply_vendor_scope`) · **🟢 KHÔNG đụng service/repo** (chỉ +1 param +1 nhánh `if mine:` @api) · tiền-lệ "1 component `WorkOrderMine`, cột scope per-op khác" (`assigned_to`/`technician`/`reported_by` = chuyện @api-handler, param shape = contract) · **Alternatives loại:** inject `assigned_to` (sai source — cột không tồn tại) / endpoint riêng (+path) / component `CalibrationMine` mới (shape trùng) / permission auto-scope (vỡ view Manager/QA) / seed @service · BE Bước-4: `api/imm11.py`+`test_imm11`(+mine TC)+`test_mobile_oas`(`_LIST_PARAM_EXPECT`+`_LIST_LIVE_FN` cal, `_EXPECTED_TEST_COUNT` 447→449)+`test_mobile_docset` count sync. **Accepted.** |
| [`ADR-MOBILE-021.md`](./ADR-MOBILE-021.md) | ADR `listTransfers` + `getTransfer` (**TRANSFER-READ-WIRE** — bồi 2 path READ điều-chuyển IMM-13/imm00 Đợt-2 vào mobile contract; hành-trình field-tech "theo-dõi điều-chuyển / nhận bàn-giao") — CONTRACT-ONLY bồi 2 path GET GROUNDED 1:1 endpoint LIVE `imm00.list_transfers:2047-2077` + `imm00.get_transfer:2080-2085` trên DocType **`Asset Transfer`** (`AT-.YYYY.-.####`, **KHÁC** luồng RAS `imm13.*` Đợt-3 chưa-scaffold) · **`listTransfers`** 4 param DISCRETE query `{asset?,status?,page?,page_size?}` (mirror `listAssets`, KHÔNG JSON-filters) · **200 = SINGLE `TransferListEnvelope` KHÔNG oneOf [Env,Error]** (handler LUÔN `_ok`, **0 `try/except`** ⇒ 0 `_err` in-handler — KHÁC `listAssets`/`IncidentList` có `except`) rows-key `data.items[]` (mirror `IncidentListEnvelope`, KHÁC Pm/Repair `data.data[]`) · `TransferListItem` closed 17-field GROUNDED `fields=[...]`@`:2062-2065`+`asset_name` enrich (`status` enum `[Pending Approval,Approved,Rejected,Received,Cancelled]`@`asset_transfer.json:89` · `transfer_type` enum `[Internal,Loan,External,Return]`@`:77`; **0 boolean** → 0 int-enum trap) · **`getTransfer`** 1 param `name` (query req) · **200 = oneOf [TransferDetailEnvelope \| Error]** closed route-by-VALUE 0-discriminator (404 `_err`@`:2084` → Error@HTTP-200, `Error.http_status ⊇ {404}`; mirror `getIncident`/`getCalibration` C6) · **`TransferDetail` OPEN (`additionalProperties:true`)** = `doc.as_dict()` superset-by-property của `TransferListItem` (mirror `IncidentDetail`/`CalibrationDetail` §3.2 — **[SELF-CORRECTION]** acceptance "closed superset" reconcile = ENVELOPE đóng + Detail mở vì `as_dict()` mang field meta Frappe ngoài DocType ⇒ closed sẽ validate-fail) · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` SINGLE-SHAPE FrappeRawError — bare `@whitelist` KHÔNG `allow_guest` ⇒ guest dispatcher-403) · path/opId **49→51** + 4 schema RIÊNG (`Transfer{ListItem,ListEnvelope,Detail,DetailEnvelope}`) tái-dùng `Pagination`/`Unauthorized401`/`Forbidden`/`Error` (0 orphan) · **🟢 CONTRACT-ONLY** `git diff` api/imm00.py + services/imm00.py phần transfer = TRỐNG (6 endpoint điều-chuyển ĐÃ LIVE) ⇒ KHÔNG reload/migrate · `d12/d15/d17` baseline UNCHANGED (pure mobile-yaml) · membership: 2 path ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry +2), `getTransfer` ∈ `_DETAIL_OPID`+`_MVP_READ_ENVELOPE`, `listTransfers` SINGLE-shape ∉ `_MVP_LIST_ENVELOPE`-oneOf · **[SELF-CORRECTION]** `_EXPECTED_TEST_COUNT` THẬT=**467** (acceptance "186" STALE) + ~25 site path-count `49→51` (acceptance nêu thiếu, chỉ 3 dòng) · BE Bước-4: yaml + `test_mobile_oas` (+2 TC class `TestMobile{ListTransfers,GetTransfer}Contract`, count sync) + `test_mobile_docset` guard-suite sync + `04-api-contract.md §8.27` + `05-personas-mvp.md §3`. **Accepted.** |
| [`ADR-MOBILE-020.md`](./ADR-MOBILE-020.md) | ADR `pingSession` (**SESSION-PROBE — CSRF warm-up + app-resume who-am-I-lite**; ĐÓNG NỐT cặp session-lifecycle còn lại sau notification quartet R38-R41 — cookie-sid mobile app CẦN endpoint nhẹ cho app-resume check + tiền-đề MỌI POST: Frappe set `csrf_token` cookie qua response) — CONTRACT-ONLY bồi 1 path GET GROUNDED 1:1 endpoint LIVE `layout.ping_session:237-258` (`@frappe.whitelist(allow_guest=True)` @`:237`, **0-PARAM** `ping_session()` @`:238` ⇒ **KHÔNG requestBody** codegen no-arg GET + live-sig parity `inspect.signature=={}`) → `_ok({user, authenticated, csrf_token})` @`:254-257` · **200 = SINGLE schema `PingSessionEnvelope` KHÔNG oneOf [Env, Error]** — handler @`:238-258` **LUÔN `_ok`** (0 nhánh `_err` in-handler) ⇒ 0 Error branch trên HTTP-200 · `PingSessionData` closed (`additionalProperties:false`) required EXACT 3 `{user:string, authenticated:**boolean** (GENUINE bool `user != "Guest"` @`:256` — KHÔNG int-enum[0,1] trap), csrf_token:string có-thể-`''` fallback @`:249-253`}` C3-split RIÊNG ≠ `UserContextData` · **response slot `{200}` EXACTLY** — KHÔNG 401 (allow_guest ∧ KHÔNG in-handler guest-guard) / KHÔNG 403 (allow_guest ⇒ 0 dispatcher cap-403) / KHÔNG 429 (0 `@rate_limit`) — **KHÁC `getUserContext` `{200,401}`** (guest-guard `_err(401)` @`:206-207`) · ∈ `_ALLOW_GUEST_PATHS` (exempt symmetry) ∉ `_MVP_BUSINESS_PATHS` ∉ `_AUTH_PATHS` · path-count **48→49** + opId 48→49 · **🟢 CONTRACT-ONLY** `git diff` api/layout.py + services/layout.py (không tồn tại — layout API-only) = TRỐNG ⇒ KHÔNG reload/migrate · `test_oas_generator/d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobilePingSessionContract` a..i (9 TC) `_EXPECTED_TEST_COUNT` 458→467 · **BACKLOG** `logoutUser` (`layout.logout_user` POST @`:262` LIVE) vòng kế. **Accepted.** |
| [`ADR-MOBILE-023.md`](./ADR-MOBILE-023.md) | ADR `getAssetPmHistory` (**FLOW-2 DEVICE-PROFILE / IMM-08** — bồi 1 path GET lịch-sử BẢO-TRÌ PM của asset vào tab "Lịch sử bảo trì" màn hồ-sơ-thiết-bị sau quét QR; **ĐÓNG quartet** device-profile read-history cùng `getAssetIncidentHistory` (sự-cố) + `getAssetTimeline` (vòng-đời) + `getAssetRepairHistory` (CM)) — CONTRACT-ONLY bồi 1 path GET GROUNDED 1:1 endpoint LIVE `imm08.get_asset_pm_history:125` → `svc.get_asset_history` (`services/imm08.py:1012-1021` `PMTaskLogRepo.list` filters`{asset_ref}` **KHÔNG `docstatus`** (PM Task Log `is_submittable=None`) 10-field order_by `completion_date desc` → `{asset_ref, history}`) · **MIRROR `getAssetRepairHistory` (ADR-022)** rows-key **`history`** + asset-key **`asset_ref`** + **200 = SINGLE-shape `AssetPmHistoryEnvelope` KHÔNG `oneOf [Env,Error]`** (handler `handle(svc.get_asset_history)` + svc **0 raise `ServiceError`** ⇒ `handle` LUÔN `_ok`) **NHƯNG 3 KHÁC-BIỆT có-chủ-đích:** (1) `AssetPmHistoryItem` **EXACT 10 prop** `{name,pm_work_order,pm_type,completion_date,technician,overall_result,is_late,days_late,next_pm_date,summary}` `required[name]` grounded `PMTaskLogRepo.list fields`@`:1015-1017` (KHÔNG 9); (2) `overall_result` = **`string` enum `[Pass, Pass with Minor Issues, Fail]`** (Select bounded @`pm_task_log.json` — repair 0 Select-enum); (3) **2 integer field** `is_late` (Check 0/1) + `days_late` (Int) → `integer` KHÔNG `boolean`/`enum[0,1]` (né int-vs-bool trap Open#1; repair 1 Check `sla_breached`) · dates `completion_date`/`next_pm_date` (**Date**) → `string` KHÔNG `format:date-time` · 2 param `asset_ref` (query req string, **NO default** @`:125`) + `limit` (query opt integer default 10 minimum 1; KHÔNG `page`/`page_size`) live-sig parity `inspect.signature(imm08.get_asset_pm_history)=={asset_ref,limit}` · `history[]` RỖNG hợp lệ asset chưa-PM → **KHÔNG 404** · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` SINGLE-SHAPE — bare `@whitelist` `api/imm08.py:124` KHÔNG `allow_guest` ⇒ guest dispatcher-403) · tag `pm` (mirror `getPmWorkOrder`) · path/opId **52→53** + 2 schema RIÊNG (`AssetPmHistory{Envelope,Item}`) tái-dùng `Unauthorized401`/`Forbidden` (0 orphan, 0 dangling) · ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry +1) · **🟢 CONTRACT-ONLY** `git diff` api/imm08.py + services/imm08.py phần `get_asset_pm_history`/`get_asset_history` = TRỐNG (BE LIVE) ⇒ KHÔNG reload/migrate, `[AUTO]` thật · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobileGetAssetPmHistoryContract` a..j (≈10 TC) ⇒ `_EXPECTED_TEST_COUNT` 492→~502 (BE confirm @source) · **Alternatives loại:** reuse `AssetRepairHistory*` (sai 10≠9 field + thiếu enum) / `oneOf [Env,Error]` (sai — handler 0 `_err`) / `boolean` cho `is_late` (strict-codegen deser CRASH 0/1) / bỏ enum `overall_result` (mất Select-bound). **Accepted.** |
| [`ADR-MOBILE-025.md`](./ADR-MOBILE-025.md) | ADR `listDepartments` (**REF-DATA / CR-10a** — curate 1 path GET danh-mục Khoa/Phòng vào OAS mirror, nguồn dropdown "Khoa/Phòng" lọc Asset List, hết chip raw Link-id `AC-DEPT-xxxx`) — CONTRACT-ONLY bồi 1 path GET GROUNDED 1:1 endpoint LIVE `imm00.list_departments` (bare `@frappe.whitelist()` no-`allow_guest`/no-`methods=POST` → GET, guest dispatcher-403) → `_ok(items)` · **`listDepartments`** 1 param `DepartmentParent` (query optional string, **KHÔNG default** — sig `parent=None`, filter `if parent:`) live-sig parity `inspect.signature(imm00.list_departments)=={parent}` · **200 = SINGLE `DepartmentListEnvelope` KHÔNG oneOf [Env,Error]** — handler KHÔNG `handle()`/`try-except` + **0 nhánh `_err`** ⇒ LUÔN `_ok` (mirror `listTransfers`/`getAssetPmHistory` single-shape, KHÁC `searchSpareParts` `handle(svc)`→oneOf) rows `data` = **MẢNG TRẦN** `<DepartmentListItem>` (KHÔNG pagination/wrapper — như `searchSpareParts` data-array; `[]` rỗng hợp lệ **KHÔNG 404**) · `DepartmentListItem` closed (`additionalProperties:false`) **EXACT 11 prop** `{name, department_name, department_code, parent_department, is_group, dept_head, phone, email, is_active, parent_department_name, dept_head_name}` `required[name]` GROUNDED `fields=[9]` + 2× `_enrich` (`parent_department_name` ← AC Department.department_name, `dept_head_name` ← User.full_name — enrich optional, có thể VẮNG) · **int-vs-bool trap (Open#1/CR-01):** `is_group` (tree group, `is_tree=1`) + `is_active` = **`Check` → `integer enum[0,1]`** (KHÔNG `boolean` — strict-codegen deser CRASH; mirror `sla_breached` ADR-022/`is_late` ADR-023) · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` SINGLE-SHAPE dispatcher-403 guest/no-token; in-handler cap-403 KHÔNG áp — ref-data đọc mở 0 `rbac.require`) · **membership:** ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry +1) + `_MVP_SINGLE_LIST_ENVELOPE` (single-shape, ∉ `_MVP_LIST_ENVELOPE` oneOf-set) + c5 map `listDepartments→DepartmentListEnvelope` (giữ `c5==_MVP_BUSINESS_PATHS`) + `_RATE_LIMIT_SOURCE_MAP` no-`@rate_limit` (∉ `_PATHS_REQUIRE_429`) · path/opId **56→57** + 2 schema RIÊNG (`DepartmentList{Item,Envelope}`) + 1 param (`DepartmentParent`) tái-dùng `Unauthorized401`/`Forbidden` (0 orphan, 0 dangling) · **🟢 CONTRACT-ONLY** `git diff -U0 api/imm00.py` vùng `list_departments` = TRỐNG (hunk chỉ ở `_enrich`/version-bump) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobileListDepartmentsContract` a..i (9 TC) ⇒ `_EXPECTED_TEST_COUNT` 513→522 + `_GUARD_SUITE_SUM` 656→665 + `_MOBILE_OAS_TOTAL` 682→691 · **Alternatives loại:** không-curate (CR-10a treo, chip raw id) / `oneOf [Env,Error]` (sai — handler 0 `_err`) / `boolean` cho `is_group`/`is_active` (int-vs-bool CRASH) / `data={pagination,items}` (bịa khoá — svc `_ok(list)` array trần) / required cả 11 (enrich có thể VẮNG). **BACKLOG:** `listLocations` + `listAssetCategories` (ref-data đối xứng). **Accepted.** |
| [`ADR-MOBILE-026.md`](./ADR-MOBILE-026.md) | ADR `listLocations` (**REF-DATA / CR-10b** — curate 1 path GET danh-mục Vị trí vào OAS mirror, nguồn dropdown "Vị trí" lọc Asset List, hết chip raw Link-id `AC-LOC-xxxx`; **đối xứng CR-10a [`listDepartments`](./ADR-MOBILE-025.md)**) — CONTRACT-ONLY bồi 1 path GET GROUNDED 1:1 endpoint LIVE `imm00.list_locations` def@`imm00.py:1354` (bare `@frappe.whitelist()` no-`allow_guest`/no-`methods=POST` → GET, guest dispatcher-403) → `_ok(items)` · **`listLocations`** 1 param `LocationParent` (query optional string, **KHÔNG default** — sig `parent=None`, filter `if parent:`) live-sig parity `inspect.signature(imm00.list_locations)=={parent}` · **200 = SINGLE `LocationListEnvelope` KHÔNG oneOf [Env,Error]** — handler KHÔNG `handle()`/`try-except` + **0 nhánh `_err`** ⇒ LUÔN `_ok` (mirror `listDepartments`/`listTransfers`/`getAssetPmHistory` single-shape, KHÁC `searchSpareParts` `handle(svc)`→oneOf) rows `data` = **MẢNG TRẦN** `<LocationListItem>` (KHÔNG pagination/wrapper; `[]` rỗng hợp lệ **KHÔNG 404**) · `LocationListItem` closed (`additionalProperties:false`) **EXACT 13 prop** `{name, location_name, location_code, parent_location, is_group, clinical_area_type, infection_control_level, power_backup_available, dept_head, contact_phone, notes, parent_location_name, dept_head_name}` `required[name]` GROUNDED `fields=[11]` + 2× `_enrich` (`parent_location_name` ← AC Location.location_name, `dept_head_name` ← User.full_name — enrich optional, có thể VẮNG) · **int-vs-bool trap (Open#1/CR-01):** `is_group` (tree group) + `power_backup_available` = **`Check` → `integer enum[0,1]`** (`get_list` trả int 0/1 SẴN, KHÔNG boolean — strict-codegen deser CRASH; mirror ADR-025) · **Select LEADING-BLANK né enum (sub-case mới):** `clinical_area_type` (ICU/OR/Lab/Imaging/…) + `infection_control_level` (Standard/Enhanced/Isolation) = **`Select` blank-leading (`"\n…"`) ⇒ `''` hợp lệ ⇒ `string nullable` KHÔNG `enum`** (KHÁC `overall_result` ADR-023 khai enum vì Select bounded no-blank; khai enum thiếu `''` ⇒ validate-FAIL) · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` SINGLE-SHAPE dispatcher-403 guest/no-token; in-handler cap-403 KHÔNG áp — ref-data đọc mở 0 `rbac.require`) · **membership:** ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry +1) + `_MVP_SINGLE_LIST_ENVELOPE` (single-shape, ∉ `_MVP_LIST_ENVELOPE` oneOf-set) + c5 map `listLocations→LocationListEnvelope` (**46→47**, giữ `c5==_MVP_BUSINESS_PATHS`) + `_RATE_LIMIT_SOURCE_MAP` no-`@rate_limit` (∉ `_PATHS_REQUIRE_429`) · path/opId **57→58** + 2 schema RIÊNG (`LocationList{Item,Envelope}`) + 1 param (`LocationParent`) tái-dùng `Unauthorized401`/`Forbidden` (0 orphan, 0 dangling) · **🟢 CONTRACT-ONLY** `git diff -U0 api/imm00.py` vùng `list_locations` def@1354 = TRỐNG (hunk chỉ ở `_enrich`/`_enrich_transfer`/version-bump) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobileListLocationsContract` a..i (9 TC) ⇒ `_EXPECTED_TEST_COUNT` 522→531 + `_GUARD_SUITE_SUM` 665→674 + `_MOBILE_OAS_TOTAL` 691→700 · **Alternatives loại:** không-curate (CR-10b treo, chip raw id) / `oneOf [Env,Error]` (sai — handler 0 `_err`) / `boolean` cho `is_group`/`power_backup_available` (int-vs-bool CRASH) / `enum` cho 2 Select-leading-blank (`''` validate-FAIL) / `data={pagination,items}` (bịa khoá — `_ok(list)` array trần) / required cả 13 (enrich có thể VẮNG). **BACKLOG:** `listAssetCategories` (ref-data cuối bộ-ba, 0-param KHÔNG tree). **Accepted.** |
| [`ADR-MOBILE-027.md`](./ADR-MOBILE-027.md) | ADR `attachIncidentPhoto` (**MULTIPART / CR-17 · G6** — curate 1 path POST đính ảnh bằng chứng hiện trường NĐ98 vào Phiếu sự cố F2; **path `multipart/form-data` ĐẦU TIÊN của mirror**, nền cho CR-14/CR-15 ảnh checklist PM/CM) — CONTRACT-ONLY bồi 1 path POST GROUNDED 1:1 endpoint LIVE `imm12.attach_incident_photo` def@`imm12.py:273` (`@whitelist(methods=["POST"])` no-`allow_guest`, single-step multipart đọc `frappe.request.files["file"]` @293 → `handle(svc_attach_photo)` @298; service @`imm12.py:995` `return {file_url,file_name}` @1064) · **`attachIncidentPhoto`** POST-only · **requestBody `multipart/form-data` DUY NHẤT** (`required:true`, **KHÔNG `application/json`/`x-www-form-urlencoded`** — file-upload đọc `request.files` KHÔNG `form_dict`; path multipart đầu tiên) schema `$ref AttachIncidentPhotoRequest` closed `req[incident_name,file]` `file:{type:string,format:binary}` `incident_name` = name Incident Report (KHÔNG asset) · **200 = oneOf [`AttachIncidentPhotoEnvelope` \| `Error`]** closed route-by-VALUE `body.success` (Decision-B ADR-MOBILE-001, 0 discriminator) — `AttachIncidentPhotoEnvelope` closed `{success:true, data:$ref AttachIncidentPhotoResponse}`, `AttachIncidentPhotoResponse` closed EXACT **2 prop** `req[file_url,file_name]` cả 2 `string` (GROUNDED @1064); cả 3 schema `additionalProperties:false` · nhánh `Error` gom **7 nhánh in-handler** ARRIVE HTTP-200 `Error.http_status` ⊇ **{403,404,422}** ladder NOT_FOUND(incident∄ @1018)→FORBIDDEN(cap @987)→VALIDATION×5(file-missing @1020/not-image @1023/too-large @1025/max-count @1027/**corrupt** `UnidentifiedImageError\|OSError` @1040-1051) — content-type allowlist **3 giá-trị** `image/jpeg`/`jpg`/`png` @49, cap 10 MB @48, max-5 @47 (thực-thi ở SERVICE, KHÔNG khai enum/maxLength schema) · slot `{200,401,403}` (`401 Unauthorized401` bearer + guest-guard `_err` @288 · **`403 Forbidden` SINGLE-SHAPE** dispatcher-403 guest/no-token — in-handler cap-403 ĐÃ phủ bởi nhánh Error 200-oneOf ⇒ 403-slot chỉ dispatcher; **KHÁC `reportIncident` DUAL-403**, mirror `acknowledgeIncident`/`startRepair` ADR-MOBILE-006) · **membership:** ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry +1) + `_MVP_ACTION_ENVELOPE` (POST-action-on-existing, `incident_name`=khoá resource có-sẵn; ∉ `_MVP_CREATE_ENVELOPE`) + c5 map `attachIncidentPhoto→AttachIncidentPhotoEnvelope` (**47→48**, giữ `c5==_MVP_BUSINESS_PATHS`) + `_RATE_LIMIT_SOURCE_MAP` no-`@rate_limit` (∉ `_PATHS_REQUIRE_429`) + POST-only-at-source ∉ `_PARITY_VERB_ALLOWLIST` + ∉ `_REQBODY_PATHS` + **media-type guard mở-rộng** hằng RIÊNG `_ATTACH_INCIDENT_PHOTO_BODY_MEDIA_TYPES={"multipart/form-data"}` (multipart-only đầu tiên, EXEMPT sweep `_RPC_FORM_JSON_MEDIA`) · path/opId **58→59** + 3 schema RIÊNG (`AttachIncidentPhoto{Request,Response,Envelope}`) tái-dùng `Unauthorized401`/`Forbidden`/`Error` (0 orphan, 0 dangling) · **🟢 CONTRACT-ONLY** handler+service+hằng photo LIVE trên đĩa (vòng trước) — vòng này KHÔNG hunk `.py` mới ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobileAttachIncidentPhotoContract` a..j (10 TC) ⇒ `_EXPECTED_TEST_COUNT` 531→541 + `_GUARD_SUITE_SUM` 674→684 + `_MOBILE_OAS_TOTAL` 700→710 · **Alternatives loại:** `application/json` body (server `files.get=None`→422 mọi lần) / dual-403 (cap-403 lặp dead-branch) / SINGLE-200 (giấu nhánh Error validation) / `data`=array hay thêm field (svc trả object 2-key) / enum content-type schema (binary vô-nghĩa; server-validate) / 2-bước upload→file_url (orphan; ADR-IMM12-06/07 chốt single-step) / `_MVP_CREATE_ENVELOPE` (attach-on-existing KHÔNG create top-level). **BACKLOG:** `attachPmChecklistPhoto` (CR-14 imm08) + `attachCmChecklistPhoto` (CR-15 imm09) — cùng family multipart, MAX=1/item (KHÁC incident MAX=5). **Accepted.** |
| [`ADR-MOBILE-028.md`](./ADR-MOBILE-028.md) | ADR `listAssetCategories` (**REF-DATA / CR-10c** — curate 1 path GET danh-mục Nhóm/Loại thiết bị vào OAS mirror, nguồn dropdown "Nhóm/Loại thiết bị" lọc Asset List, hết chip raw Link-id `CAT-####`; **hoàn tất bộ-ba ref-data** sau [`listDepartments`](./ADR-MOBILE-025.md) CR-10a + [`listLocations`](./ADR-MOBILE-026.md) CR-10b) — CONTRACT-ONLY bồi 1 path GET GROUNDED 1:1 endpoint LIVE `imm00.list_asset_categories` def@`imm00.py:1391` (bare `@frappe.whitelist()` no-`allow_guest`/no-`methods=POST` → GET, guest dispatcher-403) → `_ok(items)` · **⚠️ ZERO param — KHÁC-BIỆT cốt-lõi:** live-sig `inspect.signature(imm00.list_asset_categories).parameters == {}` (0-arg; AC Asset Category KHÔNG phải tree → danh-mục PHẲNG `order_by category_name asc`, KHÔNG `parent`/`lft`) ⇒ **KHÔNG param component** (KHÁC ADR-025/026 mỗi cái +1 `DepartmentParent`/`LocationParent`) · **200 = SINGLE `AssetCategoryListEnvelope` KHÔNG oneOf [Env,Error]** — handler KHÔNG `handle()`/`try-except` + **0 nhánh `_err`** ⇒ LUÔN `_ok` (mirror `listDepartments`/`listLocations`/`listTransfers` single-shape, KHÁC `searchSpareParts`→oneOf) `data` = **MẢNG TRẦN** `<AssetCategoryListItem>` (KHÔNG pagination/wrapper; `[]` rỗng hợp lệ **KHÔNG 404**) · `AssetCategoryListItem` closed (`additionalProperties:false`) **EXACT 16 prop** `{name, category_name, category_code, description, gmdn_code, gmdn_term, default_pm_required, default_pm_interval_days, default_calibration_required, default_calibration_interval_days, default_depreciation_method, total_depreciation_months, depreciation_frequency, default_residual_value_pct, has_radiation, is_active}` `required[name]` GROUNDED `fields=[16]`@`imm00.py:1395-1401` (**0 enrich** — danh-mục phẳng) · **int-vs-bool trap (Open#1/CR-01):** 4 `Check` `default_pm_required`/`default_calibration_required`/`has_radiation`/`is_active` = **`integer enum[0,1]`** (`get_list` trả int 0/1 SẴN, KHÔNG `_norm_check`, KHÔNG boolean — strict-codegen deser CRASH; mirror ADR-025/026) · **số THẬT KHÔNG int-enum:** 3 `Int` `default_pm_interval_days`/`default_calibration_interval_days`/`total_depreciation_months` = **`integer` nullable** + `default_residual_value_pct` (`Percent`) = **`number` nullable** (số ngày/tháng/% đếm THẬT `0..N`, KHÔNG cờ 0/1) · **2 Select tách sub-case theo options + DB-verify:** `default_depreciation_method` (`Select` LEADING-BLANK `"\nStraight Line\n…"`, DB emit `''`×105/131) = **`string nullable` KHÔNG `enum`** (sub-case ADR-026); `depreciation_frequency` (`Select` bounded no-blank `"Monthly\nQuarterly\nYearly"`, **DB `Monthly`×131/131 0-`''` BA-VERIFY**) = **`string enum [Monthly,Quarterly,Yearly]`** (mirror `overall_result` ADR-023) · **naming guard (enum-trùng-tên≠domain):** `AssetCategoryList{Item,Envelope}` (schemas) KHÔNG đụng `#/components/parameters/AssetCategory` @`yaml:399` (filter `listAssets`, khác namespace + khác domain) · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` SINGLE-SHAPE dispatcher-403 guest/no-token; in-handler cap-403 KHÔNG áp — ref-data đọc mở 0 `rbac.require`) · **membership:** ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry +1) + `_MVP_SINGLE_LIST_ENVELOPE` (single-shape, ∉ `_MVP_LIST_ENVELOPE` oneOf-set) + c5 map `listAssetCategories→AssetCategoryListEnvelope` (**48→49**, giữ `c5==_MVP_BUSINESS_PATHS`) + `_RATE_LIMIT_SOURCE_MAP` no-`@rate_limit` (∉ `_PATHS_REQUIRE_429`) + GET-only ∉ `_PARITY_VERB_ALLOWLIST`/`_REQBODY_PATHS` · path/opId **59→60** + 2 schema RIÊNG (`AssetCategoryList{Item,Envelope}`) + **0 param** tái-dùng `Unauthorized401`/`Forbidden` (0 orphan, 0 dangling) · **tag `asset`** (Self-Correction: acceptance đề-xuất `ref-data` NHƯNG 2 sibling ĐÃ-áp-dụng `tags:[asset]`@`yaml:7830`/`7861` + yaml KHÔNG có top-level `ref-data` tag ⇒ dùng `asset` cho đối-xứng bộ-ba, grounded applied-yaml) · **🟢 CONTRACT-ONLY** `git diff -U0 api/imm00.py` vùng `list_asset_categories` def@1391 = TRỐNG ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobileListAssetCategoriesContract` a..j (10 TC) ⇒ `_EXPECTED_TEST_COUNT` 541→551 + `_GUARD_SUITE_SUM` 684→694 + `_MOBILE_OAS_TOTAL` 710→720 + c5 48→49 · **Alternatives loại:** không-curate (CR-10c treo, chip raw id) / `oneOf [Env,Error]` (sai — handler 0 `_err`) / thêm param `parent` (sai — 0-arg, KHÔNG tree) / `boolean` cho 4 Check (int-vs-bool CRASH) / `int-enum[0,1]` cho 3 Int (số THẬT >1 validate-FAIL) / `enum` cho `default_depreciation_method` (`''` DB-emit validate-FAIL) / `data={pagination,items}` (bịa khoá — `_ok(list)` array trần) / required cả 16 (get_list field nullable) / tag `ref-data` (lệch 2 sibling). **BỘ-BA REF-DATA HOÀN TẤT** (CR-10a/b/c). **Accepted.** |
| [`ADR-MOBILE-029.md`](./ADR-MOBILE-029.md) | ADR `attachPmChecklistPhoto` (**MULTIPART / CR-14 · G6** — curate 1 path POST đính ảnh bằng chứng cho MỘT mục checklist PM NĐ98 Class C/D vào OAS mirror; **path `multipart/form-data` THỨ HAI**, bồi theo template [`attachIncidentPhoto`](./ADR-MOBILE-027.md) CR-17 NHƯNG **response 3-prop**) — CONTRACT-ONLY bồi 1 path POST GROUNDED 1:1 endpoint LIVE `imm08.attach_pm_checklist_photo` def@`imm08.py:62` (`@whitelist(methods=["POST"])` no-`allow_guest`, single-step multipart đọc `frappe.request.files["file"]` @83 + parse `int(checklist_item_idx)` @88 → `handle(svc.attach_pm_checklist_photo)` @91; service @`imm08.py:844` `return {file_url,file_name,checklist_item_idx}` @934-938) · **`attachPmChecklistPhoto`** POST-only, **tag `work-order`** (Self-Correction: acceptance ghi "khớp `submit_pm_result`/`get_pm_work_order`" NHƯNG 2 sibling KHÁC tag — `submit_pm_result`=`work-order`@`yaml:9340`, `get_pm_work_order`=`pm`@`yaml:7713`; ground convention imm08 THẬT = mọi POST write-action `work-order`, chỉ GET-read `pm` ⇒ write-action này `work-order`) · **requestBody `multipart/form-data` DUY NHẤT** (`required:true`, **KHÔNG json/x-www-form-urlencoded** — file-upload đọc `request.files` KHÔNG `form_dict`) schema `$ref AttachPmChecklistPhotoRequest` closed **3-part** `req[work_order_name,checklist_item_idx,file]` (`work_order_name` string = name PM Work Order KHÔNG asset · `checklist_item_idx` **integer** @88 · `file:{type:string,format:binary}`) · **200 = oneOf [`AttachPmChecklistPhotoEnvelope` \| `Error`]** closed route-by-VALUE `body.success` (Decision-B ADR-MOBILE-001, 0 discriminator) — `AttachPmChecklistPhotoEnvelope` closed `{success:true, data:$ref AttachPmChecklistPhotoResponse}`, `AttachPmChecklistPhotoResponse` closed EXACT **3 prop** `req[file_url,file_name,checklist_item_idx]` (`file_url`/`file_name` string, `checklist_item_idx` **integer**) GROUNDED @934-938 — **ĐIỂM KHÁC CỐT-LÕI vs `AttachIncidentPhotoResponse` (2-prop)**; cả 3 schema `additionalProperties:false` · nhánh `Error` gom **8 nhánh in-handler** ARRIVE HTTP-200 `Error.http_status` ⊇ **{403,404,422}** ladder NOT_FOUND(WO∄ @874-876)→FORBIDDEN(cap @836)→VALIDATION×6(**idx-row∄** @878-880/file-missing @881-882/not-image @884-885/too-large @886-887/max-count(**=1**) @888-889/**corrupt** `UnidentifiedImageError\|OSError` @901-912) — mọi VALIDATION `fields.file` (`_pm_photo_validation_error` @839); content-type allowlist **3 giá-trị** `image/jpeg`/`jpg`/`png` @42, cap 10 MB @41, MAX **1**/mục @40 (KHÁC incident MAX=5; thực-thi ở SERVICE, KHÔNG khai enum/maxLength schema) · slot `{200,401,403}` (`401 Unauthorized401` · **`403 Forbidden` SINGLE-SHAPE** dispatcher-403 guest/no-token — in-handler cap-403 ĐÃ phủ bởi nhánh Error 200-oneOf ⇒ 403-slot chỉ dispatcher; **KHÁC `reportIncident` DUAL-403**, mirror `attachIncidentPhoto`/`acknowledgeIncident` ADR-MOBILE-006/027) · **membership:** ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry +1) + `_MVP_ACTION_ENVELOPE` (POST-action-on-existing, `work_order_name`=khoá resource có-sẵn; ∉ `_MVP_CREATE_ENVELOPE`) + c5 map `attachPmChecklistPhoto→AttachPmChecklistPhotoEnvelope` (**49→50**, giữ `c5==_MVP_BUSINESS_PATHS`) + `_RATE_LIMIT_SOURCE_MAP` no-`@rate_limit` (∉ `_PATHS_REQUIRE_429`) + POST-only-at-source ∉ `_PARITY_VERB_ALLOWLIST` + ∉ `_REQBODY_PATHS` + **media-type guard mở-rộng** hằng RIÊNG `_ATTACH_PM_CHECKLIST_PHOTO_BODY_MEDIA_TYPES={"multipart/form-data"}` (multipart-only thứ hai, EXEMPT sweep `_RPC_FORM_JSON_MEDIA`; KEY path riêng, KHÔNG gộp với incident) · path/opId **60→61** + 3 schema RIÊNG (`AttachPmChecklistPhoto{Request,Response,Envelope}`) tái-dùng `Unauthorized401`/`Forbidden`/`Error` (0 orphan, 0 dangling) · **naming guard:** `AttachPmChecklistPhoto*` ∩ `AttachIncidentPhoto*` == ∅ (grep verify 0 collision) · **🟢 CONTRACT-ONLY** `git diff` `api/imm08.py`+`services/imm08.py` vùng `attach_pm_checklist_photo`/hằng `_PM_PHOTO_*` = TRỐNG (BE LIVE vòng trước) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobileAttachPmChecklistPhotoContract` a..j (10 TC) ⇒ `_EXPECTED_TEST_COUNT` 551→561 + `_GUARD_SUITE_SUM` 694→704 + `_MOBILE_OAS_TOTAL` 720→730 + c5 49→50 · **Alternatives loại:** `application/json` body (server `files.get=None`→422) / dual-403 (cap-403 lặp dead-branch) / SINGLE-200 (giấu 8 nhánh Error) / Response 2-prop copy incident (svc trả 3-key → `additionalProperties:false` validate-FAIL) / request 2-part + idx query-param (handler đọc form_dict, KHÔNG query) / `checklist_item_idx` string (int ở boundary @88/@937) / MAX=5 copy incident (hằng=1 @40) / gộp hằng media-type incident (guard KHÔNG phân-biệt path). **BACKLOG:** `attachCmChecklistPhoto` (CR-15 imm09) — cùng family, dự-kiến 3-prop per-mục. **Accepted.** |
| [`ADR-MOBILE-030.md`](./ADR-MOBILE-030.md) | ADR `attachRepairChecklistPhoto` (**MULTIPART / CR-15 · G6** — curate 1 path POST đính ảnh bằng chứng cho MỘT mục checklist SỬA CHỮA (CM) NĐ98 Class C/D vào OAS mirror; **path `multipart/form-data` THỨ BA**, hoàn tất bộ-ba write-path checklist-photo; bồi theo template [`attachPmChecklistPhoto`](./ADR-MOBILE-029.md) CR-14 (**response 3-prop**) NHƯNG **KHÁC module/doctype/discriminator**) — CONTRACT-ONLY bồi 1 path POST GROUNDED 1:1 endpoint LIVE `imm09.attach_repair_checklist_photo` def@`imm09.py:58` (`@whitelist(methods=["POST"])` @57 no-`allow_guest`, single-step multipart đọc `frappe.request.files["file"]` @74-83 + parse `int(checklist_item_idx)` @85 → `handle(svc.attach_repair_checklist_photo)` @88; service @`imm09.py:1162` `return {file_url,file_name,checklist_item_idx}` @1256-1260) · **`attachRepairChecklistPhoto`** POST-only, **tag `work-order`** (grounded: mọi POST write-action imm09 áp-dụng-yaml `tags:[work-order]` — `startRepair`@`yaml:8303`/`createRepairWorkOrder`@`yaml:9360`/`confirmInspection`@`yaml:9676`) · **requestBody `multipart/form-data` DUY NHẤT** (`required:true`, **KHÔNG json/x-www-form-urlencoded** — file-upload đọc `request.files` KHÔNG `form_dict`) schema `$ref AttachRepairChecklistPhotoRequest` closed **3-part** `req[work_order_name,checklist_item_idx,file]` (`work_order_name` string = name Asset Repair KHÔNG asset · `checklist_item_idx` **integer** = **Frappe child `idx`** — `Repair Checklist` KHÔNG có field STT domain riêng, ADR-IMM09-PHOTO-01 · `file:{type:string,format:binary}`) · **200 = oneOf [`AttachRepairChecklistPhotoEnvelope` \| `Error`]** closed route-by-VALUE `body.success` (Decision-B ADR-MOBILE-001, 0 discriminator) — `AttachRepairChecklistPhotoEnvelope` closed `{success:true, data:$ref AttachRepairChecklistPhotoResponse}`, `AttachRepairChecklistPhotoResponse` closed EXACT **3 prop** `req[file_url,file_name,checklist_item_idx]` (`file_url`/`file_name` string, `checklist_item_idx` **integer**) GROUNDED @1256-1260 — **ĐỒNG `AttachPmChecklistPhotoResponse` 3-prop NHƯNG schema RIÊNG; KHÁC `AttachIncidentPhotoResponse` 2-prop**; cả 3 schema `additionalProperties:false` · nhánh `Error` gom **8 nhánh in-handler** ARRIVE HTTP-200 `Error.http_status` ⊇ **{403,404,422}** ladder NOT_FOUND(WO∄ @1195-1197)→FORBIDDEN(cap @1154)→VALIDATION×6(**idx-row∄** @1199-1201/file-missing @1202-1203/not-image @1205-1206/too-large @1207-1208/max-count(**=1**) @1209-1210/**corrupt** `UnidentifiedImageError\|OSError` @1222-1233) — mọi VALIDATION `fields.file` (`_repair_photo_validation_error` @1157); content-type allowlist **3 giá-trị** `image/jpeg`/`jpg`/`png` @131, cap 10 MB @130, MAX **1**/mục @129 (KHÁC incident MAX=5; thực-thi ở SERVICE, KHÔNG khai enum/maxLength schema) · slot `{200,401,403}` (`401 Unauthorized401` · **`403 Forbidden` SINGLE-SHAPE** dispatcher-403 guest/no-token — in-handler cap-403 ĐÃ phủ bởi nhánh Error 200-oneOf ⇒ 403-slot chỉ dispatcher; **KHÁC `reportIncident` DUAL-403**, mirror `attachPmChecklistPhoto`/`attachIncidentPhoto`/`acknowledgeIncident` ADR-MOBILE-006/027/029) · **membership:** ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry +1) + `_MVP_ACTION_ENVELOPE` (POST-action-on-existing, `work_order_name`=khoá resource có-sẵn; ∉ `_MVP_CREATE_ENVELOPE`) + c5 map `attachRepairChecklistPhoto→AttachRepairChecklistPhotoEnvelope` (**50→51**, giữ `c5==_MVP_BUSINESS_PATHS`) + `_RATE_LIMIT_SOURCE_MAP` no-`@rate_limit` (∉ `_PATHS_REQUIRE_429`) + POST-only-at-source ∉ `_PARITY_VERB_ALLOWLIST` + ∉ `_REQBODY_PATHS` + **media-type guard mở-rộng** hằng RIÊNG `_ATTACH_REPAIR_CHECKLIST_PHOTO_BODY_MEDIA_TYPES={"multipart/form-data"}` (multipart-only thứ ba, EXEMPT sweep `_RPC_FORM_JSON_MEDIA`; KEY path riêng, KHÔNG gộp) · path/opId **61→62** + 3 schema RIÊNG (`AttachRepairChecklistPhoto{Request,Response,Envelope}`) tái-dùng `Unauthorized401`/`Forbidden`/`Error` (0 orphan, 0 dangling) · **naming guard 3-way:** `AttachRepairChecklistPhoto*` ∩ (`AttachPmChecklistPhoto*` ∪ `AttachIncidentPhoto*`) == ∅ (grep verify 0 collision) · **🟢 CONTRACT-ONLY** `git diff` `api/imm09.py`+`services/imm09.py` vùng `attach_repair_checklist_photo`/hằng `_REPAIR_PHOTO_*`/`MAX_REPAIR_CHECKLIST_*` = TRỐNG (BE LIVE phiên song song) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobileAttachRepairChecklistPhotoContract` a..j (10 TC) ⇒ `_EXPECTED_TEST_COUNT` 561→571 + `_GUARD_SUITE_SUM` 704→714 + `_MOBILE_OAS_TOTAL` 730→740 + c5 50→51 · **Alternatives loại:** `application/json` body (server `files.get=None`→422) / dual-403 (cap-403 lặp dead-branch) / SINGLE-200 (giấu 8 nhánh Error) / Response 2-prop copy incident (svc trả 3-key → `additionalProperties:false` validate-FAIL) / tái-dùng `AttachPmChecklistPhotoResponse` (coupling giả-tạo xuyên-domain) / discriminator field-STT-domain (Repair Checklist KHÔNG có → dùng Frappe child `idx`) / MAX=5 copy incident (hằng=1 @129) / gộp hằng media-type PM (guard KHÔNG phân-biệt path). **BỘ-BA WRITE-PATH CHECKLIST-PHOTO HOÀN TẤT** (incident CR-17 / PM CR-14 / CM CR-15). **Accepted.** |
| [`ADR-MOBILE-031.md`](./ADR-MOBILE-031.md) | ADR `sendToLab` (**ACTION / CR-CAL-EXT-01 · C8-ACTION** — curate 1 path POST dispatch phiếu hiệu chuẩn NGOẠI KIỂM (External) đi lab vào OAS mirror; **MỞ nhánh External-calibration** còn thiếu — in-house `submitCalibration`+`addMeasurement` đã curate; `receiveCertificate`+`cancelCalibration` forward-reserve vòng kế) — CONTRACT-ONLY bồi 1 path POST GROUNDED 1:1 endpoint LIVE `imm11.send_to_lab` def@`imm11.py:169-170` (`@whitelist(methods=["POST"])`@168 no-`allow_guest`; **signature `send_to_lab(name, sent_date=None, lab_supplier=None, lab_contract_ref=None)`** — CHỈ `name` bắt buộc, 3 param optional; `rbac.require("cal.send_lab")`@171 → `("IMM Asset Calibration","write")` `rbac.py:109`; `handle(svc.send_to_lab,…)`@172-176) service def@`imm11.py:1270` (`Scheduled`/`In Progress` → **`Sent to Lab`** + asset ACTIVE→CALIBRATING + audit `Calibration Sent To Lab`; `return {name,status,sent_date}`@1304 EXACT 3-key) · **⚠️ write-ACTION json+form body — KHÁC 3 path multipart trước (CR-17/14/15):** requestBody **2 media-type** `application/json`+`application/x-www-form-urlencoded` cùng `$ref SendToLabRequest` (Frappe RPC `form_dict` §9; **∈ `_REQBODY_PATHS`**, subject sweep `_RPC_FORM_JSON_MEDIA` — **KHÔNG hằng exempt media-type** như 3 hằng `_ATTACH_*_BODY_MEDIA_TYPES` multipart-only) · bồi theo template **same-module `createCalibration` §8.6 / `submitCalibration` §8.15** (action-on-existing, `rbac.require`→403 single-shape), KHÔNG theo template multipart · `SendToLabRequest` closed (`additionalProperties:false`) `required[name]` EXACT **4 prop** khớp HỆT signature: `name`(string) + `sent_date`/`lab_supplier`/`lab_contract_ref` (**string nullable** — default `None` @169) — KHÔNG `enum`, KHÔNG thêm/bớt prop · **200 = oneOf [`SendToLabResponseEnvelope` \| `Error`]** closed route-by-VALUE `body.success` (Decision-B ADR-MOBILE-001, 0 discriminator) — `SendToLabResponse` closed EXACT **3 prop** `required[name,status,sent_date]` (`name`/`sent_date` string; `status` **string enum 8-canonical** `[Scheduled,Sent to Lab,In Progress,Certificate Received,Passed,Failed,Conditionally Passed,Cancelled]` — giá-trị trả LUÔN `"Sent to Lab"` grounded `CalibrationResult.SENT_TO_LAB`@`constants.py:123`/@`imm11.py:1285`) — **schema RIÊNG, KHÁC `SubmitCalibrationResponse` 4-prop / `CreateCalibrationResponse` 2-prop** · nhánh `Error` gom **4 nhánh service** ARRIVE HTTP-200 `Error.http_status` ⊇ **{404,409,422}** ladder NOT_FOUND(doc∄ @1276)→CONFLICT 409(docstatus==1 đã-chốt @1278)→VALIDATION 422(không-External @1280)→CONFLICT 409(sai-state ∉{Scheduled,In Progress} @1282) grounded `messages.py` (404@904/409@939/422@953/409@960) · slot `{200,401,403}` (`401 Unauthorized401` · **`403 Forbidden` SINGLE-SHAPE** — **cap-403 (`rbac.require("cal.send_lab")`@171 raise TRƯỚC `handle()`) + dispatcher-403 (guest) CÙNG SHAPE `FrappeRawError`@HTTP-403-status-line ⇒ collapse 1 component**; mirror `createCalibration`/`submitCalibration`, **KHÁC `reportIncident` DUAL-403**) · **⚠️ Self-Correction rationale (grounded):** acceptance ghi "cap-403 phủ bởi 200-oneOf Error branch / mirror acknowledgeIncident" — SAI @source: cap-403 qua `rbac.require` = raw-403 status-line (KHÔNG qua `handle()`/200-oneOf); 403 SINGLE vì **same-shape collapse** với dispatcher-403 (mirror same-module `createCalibration` §8.6/`submitCalibration` §8.15), KHÔNG phải cơ-chế `_err(403)`@200 của acknowledgeIncident/attach-photo; cái được 200-oneOf Error phủ = **4 nhánh service {404,409,422}** (KHÔNG 403). Kết-luận (403 SINGLE) không đổi · **naming guard:** `SendToLab{Request,Response,ResponseEnvelope}` ∩ (`SubmitCalibration*`∪`CreateCalibration*`∪`AddMeasurement*`∪`Attach*Photo*`) == ∅ (grep 0) · **tag `calibration`** (grounded: họ action IMM-11 đều `calibration`) · **membership:** ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry +1) + `_MVP_ACTION_ENVELOPE` (POST-action-on-existing, `name`=khoá phiếu có-sẵn; ∉ `_MVP_CREATE_ENVELOPE`) + c5 map `sendToLab→SendToLabResponseEnvelope` (**51→52**, giữ `c5==_MVP_BUSINESS_PATHS`) + `_RATE_LIMIT_SOURCE_MAP` no-`@rate_limit` (∉ `_PATHS_REQUIRE_429`) + POST-only ∉ `_PARITY_VERB_ALLOWLIST` + **∈ `_REQBODY_PATHS`** · path/opId **62→63** + 3 schema RIÊNG (`SendToLab{Request,Response,ResponseEnvelope}`) tái-dùng `Unauthorized401`/`Forbidden`/`Error` (0 orphan, 0 dangling) · **🟢 CONTRACT-ONLY** `git diff` `api/imm11.py`+`services/imm11.py` vùng `send_to_lab` = TRỐNG (BE LIVE phiên song song) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobileSendToLabContract` a..j (10 TC) ⇒ `_EXPECTED_TEST_COUNT` **571→581** + `_GUARD_SUITE_SUM` **714→724** + `_MOBILE_OAS_TOTAL` **740→750** + c5 **51→52** (baseline grounded @source 2026-07-11 sau ADR-030 — **BE grep-verify @source TRƯỚC bump**, đa-phiên race) · **Alternatives loại:** `multipart/form-data` (sai — action KHÔNG upload, đọc `form_dict`) / dual-403 (cap-403 same-shape dispatcher, KHÔNG `_err(403)`@200) / SINGLE-200 (giấu 4 nhánh Error) / reuse `SubmitCalibrationResponse` 4-prop (svc trả 3-key → `additionalProperties:false` FAIL) / reuse `CreateCalibrationResponse` 2-prop (dư `sent_date` FAIL) / `required[name,sent_date]` (sent_date optional @169) / hằng exempt media-type (path RPC-form chuẩn PHẢI qua sweep) / curate luôn receiveCertificate+cancelCalibration (vượt scope 1-path/vòng → forward-reserve). **MỞ nhánh External-calibration** (mắt-xích ĐẦU). **Accepted.** |
| [`ADR-MOBILE-032.md`](./ADR-MOBILE-032.md) | ADR `receiveCertificate` (**ACTION / CR-CAL-EXT-02 · C8-ACTION #2** — curate 1 path POST NHẬN chứng chỉ hiệu chuẩn NGOẠI KIỂM (External) từ lab vào OAS mirror; **mắt-xích GIỮA nhánh External-calibration** tiếp nối [`sendToLab`](./ADR-MOBILE-031.md) CR-CAL-EXT-01 mở — `Sent to Lab` → `In Progress` (chờ nhập measurement + submit); `cancelCalibration` forward-reserve vòng kế) — CONTRACT-ONLY bồi 1 path POST GROUNDED 1:1 endpoint LIVE `imm11.receive_certificate` def@`imm11.py:180-183` (`@whitelist(methods=["POST"])`@179 no-`allow_guest`; **signature `receive_certificate(name, certificate_file, certificate_number, certificate_date, traceability_reference=None, reference_standard_serial=None)`** — **4 param bắt buộc** (positional KHÔNG default) + 2 optional; `rbac.require("calibration.write")`@184 → `("IMM Asset Calibration","write")` cap-family `addMeasurement`@133; `handle(svc.receive_certificate,…)`@185-192) service def@`imm11.py:1307-1311` (`Sent to Lab` → **`In Progress`** @1327 + audit `Calibration Certificate Received`@1334-1338; `return {name,status,certificate_number}`@1339-1340 EXACT 3-key) · **⚠️ write-ACTION json+form body — mirror `sendToLab` §8.37 (KHÁC 3 path multipart CR-17/14/15):** requestBody **2 media-type** `application/json`+`application/x-www-form-urlencoded` cùng `$ref ReceiveCertificateRequest` (Frappe RPC `form_dict` §9; **∈ `_REQBODY_PATHS`**, subject sweep `_RPC_FORM_JSON_MEDIA` — **KHÔNG hằng exempt media-type**) · bồi theo template `sendToLab` §8.37 / `submitCalibration` §8.15 (action-on-existing, `rbac.require`→403 single-shape) · `ReceiveCertificateRequest` closed (`additionalProperties:false`) `required[name,certificate_file,certificate_number,certificate_date]` EXACT **6 prop** khớp HỆT signature: 4 required `string` (`name`/`certificate_file`/`certificate_number`/`certificate_date` — **`certificate_file`=`string` URL KHÔNG `format:binary`**, anti-nhầm multipart) + `traceability_reference`/`reference_standard_serial` (**string nullable** — default `None` @182-183) — **4 required KHÁC `sendToLab` chỉ `name`**, KHÔNG `enum`, KHÔNG thêm/bớt prop · **200 = oneOf [`ReceiveCertificateResponseEnvelope` \| `Error`]** closed route-by-VALUE `body.success` (Decision-B ADR-MOBILE-001, 0 discriminator) — `ReceiveCertificateResponse` closed EXACT **3 prop** `required[name,status,certificate_number]` (`name`/`certificate_number` string; **`status` string KHÔNG enum** — giá-trị trả LUÔN `"In Progress"` grounded `CalibrationResult.IN_PROGRESS`@`constants.py:124`/@`imm11.py:1327,1339`; plain-string cố-ý theo acceptance, KHÁC `sendToLab` enum-8-canonical) — **schema RIÊNG, KHÁC `SendToLabResponse` 3-prop `{name,status,sent_date}` (key#3 khác) / `SubmitCalibrationResponse` 4-prop / `CreateCalibrationResponse` 2-prop** · nhánh `Error` gom **4 nhánh service** ARRIVE HTTP-200 `Error.http_status` ⊇ **{404,409,422}** ladder NOT_FOUND(doc∄ @1315)→CONFLICT 409(docstatus==1 đã-chốt @1317)→CONFLICT 409(sai-state ≠`Sent to Lab` @1319)→VALIDATION 422(thiếu 1/3 cert-field @1321) grounded `messages.py` (404@904/409@939/409@967/422@974) — **KHÁC `sendToLab` KHÔNG có `NOT_EXTERNAL` 422** (state `Sent to Lab` chỉ đạt qua `sendToLab` đã guard type) · slot `{200,401,403}` (`401 Unauthorized401` · **`403 Forbidden` SINGLE-SHAPE** — cap-403 (`rbac.require("calibration.write")`@184 raise TRƯỚC `handle()`) + dispatcher-403 (guest) CÙNG SHAPE `FrappeRawError`@HTTP-403-status-line ⇒ collapse 1 component; mirror `sendToLab`/`submitCalibration`, **KHÁC `reportIncident` DUAL-403**) · **naming guard:** `ReceiveCertificate{Request,Response,ResponseEnvelope}` ∩ (`SendToLab*`∪`SubmitCalibration*`∪`CreateCalibration*`∪`AddMeasurement*`∪`Attach*Photo*`) == ∅ (grep 0) · **tag `calibration`** (grounded: họ action IMM-11 đều `calibration`) · **membership:** ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry +1) + `_MVP_ACTION_ENVELOPE` (POST-action-on-existing, `name`=khoá phiếu có-sẵn; ∉ `_MVP_CREATE_ENVELOPE`) + c5 map `receiveCertificate→ReceiveCertificateResponseEnvelope` (**52→53**, giữ `c5==_MVP_BUSINESS_PATHS`) + `_RATE_LIMIT_SOURCE_MAP` no-`@rate_limit` (∉ `_PATHS_REQUIRE_429`) + POST-only ∉ `_PARITY_VERB_ALLOWLIST` + **∈ `_REQBODY_PATHS`** · path/opId **63→64** + 3 schema RIÊNG (`ReceiveCertificate{Request,Response,ResponseEnvelope}`) tái-dùng `Unauthorized401`/`Forbidden`/`Error` (0 orphan, 0 dangling) · **🟢 CONTRACT-ONLY** `git diff` `api/imm11.py`+`services/imm11.py` vùng `receive_certificate` = TRỐNG (BE LIVE phiên song song) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobileReceiveCertificateContract` a..j (10 TC) ⇒ `_EXPECTED_TEST_COUNT` **581→591** + `_GUARD_SUITE_SUM` **724→734** + `_MOBILE_OAS_TOTAL` **750→760** + c5 **52→53** (baseline grounded @source 2026-07-11 sau ADR-031/sendToLab — **BE grep-verify @source TRƯỚC bump**, đa-phiên race) · **⚠️ Self-Correction đánh-số:** ADR = **032** (đề mục Bước-2 ghi 031 — numbering-drift do sendToLab R10 song song đã chiếm 031; narrative §8.38, binding §0.1.7) · **Alternatives loại:** `multipart/form-data` (sai — `certificate_file`=string URL KHÔNG binary, đọc `form_dict`) / dual-403 (cap-403 same-shape dispatcher) / SINGLE-200 (giấu 4 nhánh Error) / reuse `SendToLabResponse` (cùng 3-key nhưng key#3 `sent_date`≠`certificate_number` → `additionalProperties:false` FAIL) / `status` enum-8-canonical (acceptance chọn plain string đơn-trị) / `required[name]` only (4 cert-param positional KHÔNG default) / hằng exempt media-type (RPC-form PHẢI qua sweep) / curate luôn cancelCalibration (vượt scope → forward-reserve). **Tiếp nối nhánh External-calibration** (mắt-xích GIỮA). **Accepted.** |
| [`ADR-MOBILE-033.md`](./ADR-MOBILE-033.md) | ADR `cancelCalibration` (**ACTION / CR-CAL-EXT-03 · C8-ACTION abort** — curate 1 path POST HỦY phiếu hiệu chuẩn DRAFT vào OAS mirror; **HOÀN TẤT bộ-ba action External-calibration** [`sendToLab`](./ADR-MOBILE-031.md) CR-CAL-EXT-01 mở → [`receiveCertificate`](./ADR-MOBILE-032.md) CR-CAL-EXT-02 giữa → `cancelCalibration` abort/escape-hatch) — CONTRACT-ONLY bồi 1 path POST GROUNDED 1:1 endpoint LIVE `imm11.cancel_calibration` def@`imm11.py:195-198` (`@whitelist(methods=["POST"])`@195 no-`allow_guest`; **signature `cancel_calibration(name, reason)`** — **2 param bắt buộc** (positional KHÔNG default); `rbac.require("calibration.cancel")`@197 → `("IMM Asset Calibration","cancel")` **auto-gen** `rbac.py:100-103`; `handle(svc.cancel_calibration, name, reason)`@198) service def@`imm11.py:1343` (phiếu DRAFT → **`Cancelled`** + `amendment_reason="[Cancelled] {reason}"`@1357 + nếu asset `CALIBRATING`→`ACTIVE`@1359-1362 + audit `Calibration`@1363-1367; `return {name,status}`@1368 EXACT **2-key**) · **⚠️ ĐIỂM KHÁC CỐT-LÕI — 403 cap-branch REACHABLE (shape KHÔNG đổi):** `sendToLab`/`receiveCertificate` gate cap `write` mà `Calibration User` **CÓ** (write=1) → cap-403 gần-như-không-trip; `cancelCalibration` gate cap `calibration.cancel` → permtype `cancel` mà `Calibration User` **cancel=0** (DocPerm `imm_asset_calibration.json`: cancel=1 chỉ `AssetCore Super Admin`+`Calibration Manager`) → cap-403 (`rbac.require` @197 raise TRƯỚC `handle()`) là **HTTP-403 status-line THẬT REACHABLE** cho KTV thường (action IMM-11 ĐẦU TIÊN cap-403 trip trong luồng bình-thường); SHAPE **VẪN SINGLE `Forbidden`** (cap-403 CÙNG raw-403 dispatcher-403 → collapse 1 component), khác-biệt ở **reachability/UX** (app gate nút "Hủy phiếu" theo `calibration.cancel` — chống nút-chết), KHÔNG ở schema; description path GHI RÕ 403 REACHABLE · **⚠️ write-ACTION json+form body — mirror `sendToLab`/`receiveCertificate` (KHÁC 3 path multipart CR-17/14/15):** requestBody **2 media-type** `application/json`+`application/x-www-form-urlencoded` cùng `$ref CancelCalibrationRequest` (Frappe RPC `form_dict` §9; **∈ `_REQBODY_PATHS`**, subject sweep `_RPC_FORM_JSON_MEDIA` — **KHÔNG hằng exempt media-type**) · `CancelCalibrationRequest` closed (`additionalProperties:false`) `required[name, reason]` EXACT **2 prop** khớp HỆT signature: `name`/`reason` `string` (**`reason` BẮT BUỘC — KHÁC `sendToLab` chỉ `name`**), KHÔNG `enum`, KHÔNG thêm/bớt prop · **200 = oneOf [`CancelCalibrationResponseEnvelope` \| `Error`]** closed route-by-VALUE `body.success` (Decision-B ADR-MOBILE-001, 0 discriminator) — `CancelCalibrationResponse` closed EXACT **2 prop** `required[name, status]` (`name` string; **`status` string KHÔNG enum** — giá-trị trả LUÔN `"Cancelled"` grounded `CalibrationResult.CANCELLED`@`constants.py:119`/@`imm11.py:1368`; plain-string mirror `receiveCertificate`, KHÁC `sendToLab` enum-8) — **schema RIÊNG, KHÁC `SendToLabResponse` 3-prop `{…,sent_date}` / `ReceiveCertificateResponse` 3-prop `{…,certificate_number}` / `CreateCalibrationResponse` 2-prop field-set-khác / `SubmitCalibrationResponse` 4-prop** · nhánh `Error` gom **4 nhánh service** ARRIVE HTTP-200 `Error.http_status` ⊇ **{404,409,422}** ladder NOT_FOUND(doc∄ @1347)→CONFLICT 409(docstatus==1 đã-chốt @1349)→CONFLICT 409(đã-hủy @1351)→VALIDATION 422(thiếu lý do @1353) grounded `messages.py` (404@917/409@1001/409@1008/422@994) — **KHÁC `sendToLab`/`receiveCertificate` KHÔNG có `NOT_EXTERNAL`/state-guard** (cancel áp MỌI DRAFT) · slot `{200,401,403}` (`401 Unauthorized401` · **`403 Forbidden` SINGLE-SHAPE REACHABLE** — cap-403 (`rbac.require("calibration.cancel")`@197 raise TRƯỚC `handle()`, Calibration User cancel=0) + dispatcher-403 (guest) CÙNG SHAPE `FrappeRawError`@HTTP-403-status-line ⇒ collapse 1 component; mirror `sendToLab`/`receiveCertificate`/`submitCalibration`, **KHÁC `reportIncident` DUAL-403**) · **⚠️ Self-Correction from-state:** guard THẬT `docstatus!=1` AND `status!=Cancelled` (MỌI DRAFT hủy được: Scheduled/Sent to Lab/In Progress/Certificate Received) — acceptance "Scheduled/In Progress" là case điển-hình, KHÔNG whitelist; BE/FE KHÔNG thêm client-guard hẹp hơn server · **naming guard:** `CancelCalibration{Request,Response,ResponseEnvelope}` ∩ (`SendToLab*`∪`ReceiveCertificate*`∪`SubmitCalibration*`∪`CreateCalibration*`∪`AddMeasurement*`∪`Attach*Photo*`) == ∅ (grep 0) · **tag `calibration`** (grounded: họ action IMM-11 đều `calibration`) · **membership:** ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry +1) + `_MVP_ACTION_ENVELOPE` (POST-action-on-existing, `name`=khoá phiếu có-sẵn; ∉ `_MVP_CREATE_ENVELOPE`) + c5 map `cancelCalibration→CancelCalibrationResponseEnvelope` (**53→54**, giữ `c5==_MVP_BUSINESS_PATHS`) + `_RATE_LIMIT_SOURCE_MAP` no-`@rate_limit` (∉ `_PATHS_REQUIRE_429`) + POST-only ∉ `_PARITY_VERB_ALLOWLIST` + **∈ `_REQBODY_PATHS`** · path/opId **64→65** + 3 schema RIÊNG (`CancelCalibration{Request,Response,ResponseEnvelope}`) tái-dùng `Unauthorized401`/`Forbidden`/`Error` (0 orphan, 0 dangling) · **🟢 CONTRACT-ONLY** `git diff` `api/imm11.py`+`services/imm11.py` vùng `cancel_calibration` = TRỐNG (BE LIVE phiên song song) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobileCancelCalibrationContract` a..j (10 TC, gồm TC-g response-2-prop-status-plain-string anti-drift + TC-i naming-guard + cap-map-grounded `calibration.cancel→(IMM Asset Calibration,cancel)` + TC-j pure-yaml+live-sig `{name,reason}`) ⇒ `_EXPECTED_TEST_COUNT` **591→601** + `_GUARD_SUITE_SUM` **734→744** + `_MOBILE_OAS_TOTAL` **760→770** + c5 **53→54** (baseline grounded @source 2026-07-12 sau ADR-032/receiveCertificate — **BE grep-verify @source TRƯỚC bump**, đa-phiên race) · **Alternatives loại:** `multipart/form-data` (sai — action KHÔNG upload, đọc `form_dict`) / dual-403 (cap-403 REACHABLE nhưng CÙNG shape dispatcher — reachability ≠ shape, ghi ở description) / SINGLE-200 (giấu 4 nhánh Error) / reuse `CreateCalibrationResponse` 2-prop (field-set khác → `additionalProperties:false` FAIL) / `status` enum-8-canonical (acceptance chọn plain string đơn-trị, mirror receiveCertificate) / `required[name]` only (`reason` positional KHÔNG default — bắt buộc) / hằng exempt media-type (RPC-form PHẢI qua sweep) / KHÔNG ghi reachability 403 (app render nút-chết). **HOÀN TẤT bộ-ba action External-calibration.** **Accepted.** |
| [`ADR-MOBILE-034.md`](./ADR-MOBILE-034.md) | ADR `resolveQrToken` + `getAssetScanInfo` typed-query-param (**CR-05 · typed-query-param** — chuyển `?token=`/`?name=` từ prose-only sang query param CÓ KIỂU trong OAS mirror để codegen `typescript-axios` phơi tham số typed, bỏ `axios request options.params` cast) — **PARAMS-ONLY** CONTRACT-ONLY bồi khối `parameters:` cho 2 GET scan-QR read path GROUNDED 1:1 chữ-ký LIVE (introspect argspec): `resolveQrToken` def@`imm00.py:605` `resolve_qr_token(token: str = "")` → **1 param `token`** (`in:query`, `required:false`, `schema.type:string`) · `getAssetScanInfo` def@`imm00.py:648` `get_asset_scan_info(token: str = "", name: str = "")` → **2 param `token`+`name`** (cả hai `in:query`, `required:false`, `schema.type:string`) · **TẤT CẢ 3 param `required:false`** — backend default `""` + **OR-resolve** (resolve theo `token` HOẶC `name`; `required:true` 1 lane sẽ PHÁ lane kia + lệch chữ-ký LIVE) · param khai **== chữ-ký backend** (TC-d introspect `inspect.signature`, 0 bịa/0 thiếu) · **200-shape GIỮ NGUYÊN** `oneOf [<Envelope>, Error]` closed (`QrResolveEnvelope`/`AssetScanInfoEnvelope` + `Error` `additionalProperties:false` bất-biến) — **0 schema drift · 0 path/opId mới** (path-count 65 GIỮ + c5 54 GIỮ; params ≠ path/schema) · inline params (KHÔNG `$ref components/parameters` — chỉ dùng 1 chỗ, mirror `getAssetIncidentHistory` inline; precedent `getAsset` `parameters:[AssetName]`) · **🟢 CONTRACT-ONLY** handler+service LIVE @source ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobileScanQrTypedParams` a..e (5 TC: a=resolve-1-param · b=scan-2-param · c=TẤT CẢ required:false OR-resolve · d=param==live-sig introspect · e=200-shape oneOf GIỮ; **RED-before/GREEN-after** TC-a/TC-c `parameters` key vắng→có) ⇒ `_EXPECTED_TEST_COUNT` **601→606** + `_GUARD_SUITE_SUM` **744→749** + `_MOBILE_OAS_TOTAL` **770→775** (params-only KHÔNG đụng c5/path) · **Alternatives loại:** giữ prose-only (không đóng CR-05) / `required:true` (phá OR-resolve + lệch LIVE) / gộp 1 param `id` hoặc path-template `/scan/{token}` (đổi opId/shape, RPC KHÔNG path-template) / `$ref components/parameters` (không tái dùng). **Accepted.** |
| [`ADR-MOBILE-035.md`](./ADR-MOBILE-035.md) | ADR `getAssetScanInfo` `department_name` scan-schema-parity (**CR-19 · scan-schema-parity** — bồi property `department_name` (Khoa/Phòng thiết bị) vào schema `AssetScanInfo` NGAY CẠNH `location_name` để đóng field-parity gap màn quét QR F1; KTV hiện trường cần biết thiết bị thuộc **khoa nào** — KHÔNG chỉ vị trí lắp đặt — trước khi báo sự cố / mở WO) — **SCHEMA-FIELD ADD** CONTRACT-ONLY bồi 1 property GROUNDED 1:1 builder LIVE `build_asset_scan_info` @`services/imm00.py:825-828` (`"department_name": _str_or_blank(frappe.db.get_value("AC Department", row["department"], "department_name") if row.get("department") else "")` — emit **vô-điều-kiện**, `_str_or_blank` LUÔN trả `str` `''` khi rỗng KHÔNG `None`; điều kiện `if row.department` CHỈ skip N+1 query, KHÔNG làm key vắng) · **`department_name`** `type:string` **KHÔNG `nullable`** (parity `location_name` Vòng 46 — denorm Link → `*_name`, non-null) + `description` VERBATIM (`Tên khoa/phòng; denorm AC Asset.department -> AC Department.department_name; '' khi không gán; imm00.py:825.`) · **∈ `required[]`** NGAY SAU `- location_name` (BE emit vô-điều-kiện non-null ⇒ required, KHÔNG optional; KHÁC `warranty_expiry_date` nullable NGOÀI required) · **precedent verbatim** `getAsset` `AssetDetail.department_name` @`imm00.py:302` (đã có) · **SCHEMA-FIELD ADD** — 0 path/opId/schema-component mới (path-count **65 GIỮ** + c5 **54 GIỮ**), `additionalProperties:false` GIỮ NGUYÊN (closed-schema sweep unbroken) · **🟢 CONTRACT-ONLY** `git diff` `services/imm00.py` vùng `build_asset_scan_info` = TRỐNG round NÀY (BE LIVE source-verified) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml) · Guard `TestMobileScanInfoDepartmentNameParity` a..d (4 TC: a=property-present · b=string-KHÔNG-nullable · c=∈required · d=`additionalProperties:false` GIỮ; **RED-before/GREEN-after** TC-a/TC-c property/required vắng→có) + `TestMobileAssetScanInfoFieldParity` TC-h forward parity-sweep `service_keys`+`department_name` (0 count-change) ⇒ `_EXPECTED_TEST_COUNT` **606→610** + `_GUARD_SUITE_SUM` **749→753** + `_MOBILE_OAS_TOTAL` **775→779** (schema-field add KHÔNG đụng c5/path) · **Alternatives loại:** giữ nguyên (không đóng CR-19 — strict codegen drop field) / `nullable:true`+NGOÀI required (lệch BE `_str_or_blank` LUÔN str) / `additionalProperties:true` (phá closed-schema sweep) / kéo raw Link id `AC-DEPT-xxxx` (rò mã nội bộ — builder cố ý chỉ emit `*_name`). **Accepted.** |
| [`ADR-MOBILE-036.md`](./ADR-MOBILE-036.md) | ADR `getIncident` `IncidentDetail` SLA-flag scan-schema-parity (**CR-21 · SLA-detail-parity** — bồi 2 property `is_response_breached` + `is_resolution_breached` (cờ SLA DERIVED-LIVE `0|1`) vào schema `IncidentDetail` NGAY CẠNH cờ thô `resolution_breached` để đóng typed-contract gap màn **Chi tiết sự cố F2**; badge "Tình trạng SLA" màn Chi tiết PHẢI khớp danh sách/dashboard tại cùng thời điểm — KHÔNG stale-divergence khi cờ thô chưa được scheduler stamp) — **SCHEMA-FIELD ADD** CONTRACT-ONLY bồi 2 property GROUNDED 1:1 service LIVE `get_incident_detail` @`services/imm12.py:1110` gọi `_enrich_sla_breach([data])` @**1132** (set 2 key vô-điều-kiện qua 1 SoT predicate `_row_is_breached` @`imm12.py:209-215` — CÙNG helper `list_incidents`@982/`get_dashboard`@1251, INV-SLA-5) · **mỗi cờ** `type:integer` + `enum:[0,1]` (Open#1 int-vs-bool — derived int KHÔNG `boolean`) + `description` **VERBATIM** precedent `IncidentListItem` (yaml:1272 — 2 cờ đã curate, cùng `_enrich_sla_breach`) · **OPTIONAL** — KHÔNG vào `required` (mirror `IncidentListItem`: 2 cờ derived đều optional; `required` GIỮ `['name']`, pattern optional-emit `allowed_transitions`/`scene_photos`) · **`additionalProperties:true` GIỮ NGUYÊN** (as_dict passthrough có chủ đích — **KHÔNG flip `false`**, KHÁC `AssetScanInfo` closed ADR-035) · cờ thô `response_breached`/`resolution_breached` (raw, backward-compat) + hạn `response_due_at`/`resolution_due_at` GIỮ; tổng property **41→43** · **SCHEMA-FIELD ADD** — 0 path/opId/schema-component mới (path-count **65 GIỮ** + c5 **54 GIỮ**), closed-schema sweep + YAML lint KHÔNG đỏ · **🟢 CONTRACT-ONLY** `git diff` `services/imm12.py`/`api/imm12.py` vùng `get_incident_detail`/`_enrich_sla_breach` = TRỐNG round NÀY (BE + `test_imm12.py::TestIncidentDetailSlaLive` @948-975 LIVE source-verified) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · Guard `TestMobileIncidentDetailSlaFlagParity` a..f (6 TC: a=cả-2-present · b=integer+enum[0,1] · c=description-VERBATIM-parity IncidentListItem · d=OPTIONAL-∉required · e=`additionalProperties:true`+cờ-thô+hạn-SLA-giữ · f=property-count==43; **RED-before/GREEN-after** TC-a/TC-c strip `is_response_breached`→RED "thiếu is_response_breached"→bồi→GREEN) ⇒ `_EXPECTED_TEST_COUNT` **610→616** + `_GUARD_SUITE_SUM` **753→759** + `_MOBILE_OAS_TOTAL` **779→785** + delta var `sla_detail_parity_delta=6` (transition-baseline doc_09); `test_imm12` regression GREEN chứng minh SoT `_enrich_sla_breach` KHÔNG đổi · **Alternatives loại:** giữ nguyên (không đóng CR-21 — codegen mất typed access badge SLA Chi tiết → tái stale-divergence) / thêm vào `required` (lệch precedent optional-emit) / flip `additionalProperties:false` (phá as_dict passthrough — strict codegen crash field web-only) / bỏ cờ thô (phá backward-compat, ADR-IMM12-08 chốt giữ CẢ HAI) / description tự viết (vi phạm parity 1-SoT). **Accepted.** |
| [`ADR-MOBILE-037.md`](./ADR-MOBILE-037.md) | ADR `listRepairWorkOrders` `RepairWorkOrderListItem.status`/`priority` formal enum-parity (**CR-08 · enum-parity-curate** — formal-hoá `enum` cho 2 property list-item `status` (9 `RepairStatus`) + `priority` (3 giá trị) trong OAS mirror; đóng codegen-drift: list-item đang sinh `String` TRẦN thay union typed, dù precedent `CreateRepairWorkOrderResponse.status@yaml:3041` đã khai enum 9-state — cùng `RepairStatus` doctype, 1 SoT) — **FIELD-LEVEL ENUM ADD** CONTRACT-ONLY bồi khoá `enum` vào 2 property CÓ SẴN, GIỮ `type:string` · **status.enum** = `[Open, Assigned, Diagnosing, Pending Parts, In Repair, Pending Inspection, Completed, Cannot Repair, Cancelled]` (9 VERBATIM đúng thứ tự — **bằng-hệt** `CreateRepairWorkOrderResponse.status@yaml:3041` list-equality, 1 SoT precedent KHÔNG chế enum mới) · **priority.enum** = `[Normal, Urgent, Emergency]` (3 Select-canonical) · **grounding chống-bịa**: `set(enum) == set(asset_repair.json Select options)` — TC-e đọc TRỰC TIẾP doctype json (`status` 9-option + `priority` 3-option) · service `list_work_orders` @`services/imm09.py:1044` trả `status`/`priority` **CANONICAL** (∈ `_LIST_WO_FIELDS`@`:958` qua `get_all` — 0 transform; enricher chỉ ĐỌC `status` để derive `sla_paused`) · **zero-footprint** — `additionalProperties:false` GIỮ + `required:['name']` GIỮ + tổng property 21 KHÔNG đổi (chỉ bồi `enum` vào 2 property có sẵn, KHÔNG thêm/bớt property, KHÔNG đổi type/nullable) + path/opId **65 GIỮ** + c5 **54 GIỮ** (KHÔNG path/opId/schema-component/parameter mới) · **🟢 CONTRACT-ONLY** `git diff` `services/imm09.py`/`api/imm09.py` vùng `list_work_orders` = TRỐNG round NÀY (BE LIVE source-verified) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · Guard `TestMobileRepairListItemEnumParity` a..f (6 TC: a=cả-2-enum-present · b=status-9-verbatim-ordered-string · c=priority-3-string · d=status-parity-CreateResponse-1SoT-list-equality · e=grounding-set(enum)==set(asset_repair.json Select options) đọc-trực-tiếp-doctype · f=no-structural-drift+zero-footprint; **RED-before/GREEN-after** strip `status.enum`→TC-a/b/d/e FAIL→bồi→GREEN) ⇒ `_EXPECTED_TEST_COUNT` **616→622** + `_GUARD_SUITE_SUM` **759→765** + `_MOBILE_OAS_TOTAL` **785→791** + delta var `repair_listitem_enum_parity_delta=6` (transition-baseline doc_09); `test_imm09` regression đỏ round NÀY = fixture-contamination môi trường (`DoesNotExistError: Asset None not found` setUp — ĐỘC LẬP pure-yaml, SoT `list_work_orders` KHÔNG đụng) · **Alternatives loại:** giữ nguyên (`string` TRẦN — codegen mất union typed, bất đối xứng vĩnh viễn) / chế enum mới (vi phạm 1-SoT) / đổi type/thêm nullable (sai wire-shape + phá zero-footprint) / thêm vào `required` (vô-nghĩa enum-curate) / tách schema component +`$ref` (over-engineer, phá zero-footprint). **Accepted.** |
| [`ADR-MOBILE-038.md`](./ADR-MOBILE-038.md) | ADR `getAssetKpi` curate vào OAS mirror (**CR-11a · asset-detail sub-tab KPI** — bồi ĐÚNG 1 GET-read path `getAssetKpi` (KPI vận-hành 12-key OBJECT PHẲNG của 1 asset) vào OAS mirror; **MỞ NHÁNH asset-detail sub-tab** — curate ĐẦU TIÊN của CR-11 (5 sub-tab backend LIVE, 0 curated: `kpi`/`verify_chain`/`depreciation`/`commissioning`/`downtime`)) — **PATH ADD** CONTRACT-ONLY bồi 1 GET path GROUNDED 1:1 handler LIVE `get_asset_kpi(name)` @`api/imm00.py:1174-1175` (bare `@whitelist` → GET; guest dispatcher-403) trả `_ok({12-key})` @`:1250-1263` (compute on-the-fly 12 tháng: uptime/downtime `AC Asset Downtime Log` · MTTR/MTBF/`total_repair_cost` `Asset Repair docstatus=1` · `pm_compliance_pct` `PM Work Order`) · **1 param** `name` (`in:query`, **`required:true`**, string) live-sig parity `inspect.signature(get_asset_kpi)=={name}` no-default · **200 = oneOf `[AssetKpiEnvelope, Error]`** closed route-by-VALUE `body.success` (Decision-B 0-discr; `_err(404)` asset∄ @`:1185-1186` → HTTP-200 nhánh Error, `Error.http_status ⊇ {404}`) · **`AssetKpi` = OBJECT PHẲNG single** (`data=$ref AssetKpi` trực-tiếp, KHÔNG `{items}`/`{pagination}` — KHÁC `getAssetTimeline` list) closed `additionalProperties:false` **EXACT 12 prop VERBATIM** return-dict @`:1250-1263` · **required EXACT 5** always-present `{name,lifecycle_status,uptime_pct,breakdown_count,total_downtime_hours}` + **nullable EXACT 7** `{mtbf_days,mttr_hours,pm_compliance_pct,total_repair_cost,next_pm_date,next_calibration_date,byt_reg_expiry}` (return `None` hợp-lệ @source) · invariant `required ∩ nullable == ∅` ∧ `∪ == 12` · type grounded: `breakdown_count`=integer (len) + 6 number (round/sum) + `name`/`lifecycle_status`/3-date string (3 date `format:date`); 0 boolean/int-enum[0,1] · ⚠️ **`total_repair_cost` = FINANCIAL** nullable number (curate VERBATIM theo return-dict, UI-render deferred mobile client — persona-gate FE) · `AssetKpiEnvelope` closed `required[success,data]` `success.enum[true]` · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` SINGLE-SHAPE bare `@whitelist` no-allow_guest → guest dispatcher-403) · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_READ_ENVELOPE` (inline oneOf read-path) · path/opId **65→66** + c5 **54→55** + 2 schema mới (naming guard `AssetKpi*` ∩ schema hiện có == ∅) · **🟢 CONTRACT-ONLY** `git diff` `api/imm00.py` vùng `get_asset_kpi` = TRỐNG round NÀY (BE LIVE source-verified; TC-j SCOPED-HANDLER invariant HEAD↔working) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · Guard `TestMobileGetAssetKpiContract` a..j (10 TC: a=path/opId 66 · b=GET-only opId/tag · c=1-param-name-required+live-sig · d=200-oneOf-route-by-value · e=Envelope-closed-data-flat-object · f=12-prop+required-5+nullable-7 · g=type-grounded+FINANCIAL+no-bool · h=disjointness+0-dangling · i=symmetry-set+slot+Error-404 · j=SCOPED-HANDLER-pure-yaml; **RED-before/GREEN-after** strip schema `AssetKpi`→dangling `#/components/schemas/AssetKpi`+`Thiếu schema AssetKpi`→bồi→GREEN) ⇒ `_EXPECTED_TEST_COUNT` **622→632** + `_GUARD_SUITE_SUM` **765→775** + `_MOBILE_OAS_TOTAL` **791→801** + delta var `asset_kpi_curate_delta=10` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **Alternatives loại:** giữ dead-end (không mở CR-11) / 200 single-shape (giấu nhánh `_err(404)` → parse crash) / bọc `{items}`/`{pagination}` (sai wire flat) / strip `total_repair_cost` FINANCIAL (lệch payload thật) / `additionalProperties:true` (phá closed sweep) / 5 nullable vào required (deser crash null). **Accepted.** |
| [`ADR-MOBILE-039.md`](./ADR-MOBILE-039.md) | ADR `getAssetDowntimeMetrics` curate vào OAS mirror (**CR-11b · asset-detail sub-tab #2 "Dừng máy"** — bồi ĐÚNG 1 GET-read path thống-kê dừng máy của 1 asset; sub-tab THỨ HAI của CR-11 sau `getAssetKpi` (ADR-038); **LẦN ĐẦU open-map `by_reason` `{type:number}` trong success-data schema** — documented exception) — **PATH ADD** CONTRACT-ONLY bồi 1 GET path GROUNDED 1:1 handler LIVE `get_asset_downtime_metrics(asset_name, year="")` @`api/imm00.py:2892-2893` (bare `@whitelist` → GET; guest dispatcher-403) trả `_ok({8-key})` @`:2936-2944` (đọc `AC Asset Downtime Log` filter `asset`+`start_time between [year-01-01,year-12-31]` @`:2909-2919` → tổng giờ + phân-loại reason + log đang-mở) · **2 param** `asset_name` (`in:query`, **`required:true`**, string — **⚠️ KHÔNG `name`**, chữ-ký `get_asset_downtime_metrics(asset_name,…)` positional @`:2893`) + `year` (`in:query`, **`required:false`**, string — `year:str=""` default → năm hiện-tại @`:2905`, typed-query parity CR-05) live-sig `inspect.signature=={asset_name,year}` · **200 = oneOf `[AssetDowntimeMetricsEnvelope, Error]`** closed route-by-VALUE `body.success` (Decision-B 0-discr; `_err(404)` asset∄ @`:2901-2902` → HTTP-200 nhánh Error, `Error.http_status ⊇ {404}`) · **`AssetDowntimeMetrics` = OBJECT PHẲNG single** (`data=$ref` trực-tiếp) closed `additionalProperties:false` **EXACT 8 prop VERBATIM** return-dict @`:2936-2944` **required=cả 8** (mọi key LUÔN emit) `{asset:string, year:integer, total_hours:number, breakdown_count:integer, mttr_hours:number, by_reason:OPEN-MAP, current_open:nullable-ref, logs:array}` · **`by_reason` = open-map** `additionalProperties:{type:number}` (dict động reason→giờ @`:2922/2931` — **ADR EXCEPTION**, schema DUY-NHẤT không-đóng; precision: `Error.fields`@`yaml:730` đã có `{type:string}` open-map → `by_reason` là `{type:number}` ĐẦU-TIÊN + open-map ĐẦU-TIÊN trong success-data) · **`current_open`** = nullable `$ref AssetDowntimeLogOpen` (`{nullable:true, allOf:[{$ref}]}` — OpenAPI 3.0.3 KHÔNG `type:null`; key LUÔN present value `null` khi 0 log-mở @`:2923`) · **`AssetDowntimeLog`** closed EXACT 8 field grounded `get_all fields`@`:2915-2916`+doctype `{name,reason(string plain no-enum — đối-xứng by_reason),start_time(string no-`format:date-time`),end_time(nullable),downtime_hours(number nullable),is_open(integer enum[0,1] — int-vs-bool trap NOT boolean),reference_doctype(nullable),reference_name(nullable)}` required=8 nullable=4 · **`AssetDowntimeLogOpen`** = Log 8 + `downtime_hours_so_far`(number non-null @`:2927`) 9 field closed · `logs[]` items `AssetDowntimeLog` (`rows[:10]` ≤10 gần nhất @`:2944`) · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` SINGLE-SHAPE bare `@whitelist` no-allow_guest → guest dispatcher-403) · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_READ_ENVELOPE` (inline oneOf read-path, mirror getAssetKpi) · path/opId **66→67** + c5 **55→56** + 4 schema mới (naming guard `AssetDowntime*` ∩ schema hiện có == ∅) · **🟢 CONTRACT-ONLY** `git diff` `api/imm00.py` vùng `get_asset_downtime_metrics` = TRỐNG round NÀY (BE LIVE source-verified; SCOPED-HANDLER invariant HEAD↔working) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · Guard `TestMobileAssetDowntimeMetricsCurate` a..h (8 TC: a=path/opId 67 · b=GET-only opId/tag · c=2-param asset_name+year+live-sig+asset_name-NOT-name · d=200-oneOf-route-by-value · e=Envelope-closed-data-flat-object · f=8-prop+required-8+by_reason-open-map+current_open-nullable+logs-array · g=sub-schema-Log-8/LogOpen-9-typed+is_open-int-enum+no-date-time · h=open-map-exception+disjointness+0-dangling+SCOPED-HANDLER-pure-yaml; **RED-before/GREEN-after** strip path→path-count 66≠67+`Thiếu path`→bồi→GREEN) ⇒ `_EXPECTED_TEST_COUNT` **632→640** + `_GUARD_SUITE_SUM` **775→783** + `_MOBILE_OAS_TOTAL` **801→809** + delta var `asset_downtime_metrics_curate_delta=8` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **⚠️ Reconciliation đa-phiên:** đề mục ghi STALE `path 65→66`/`ADR-038`/`balance 38==38` (viết TRƯỚC getAssetKpi land); LIVE baseline post-getAssetKpi → CR-11b = ADR-**039**/path **66→67**/c5 **55→56**/balance **39==39** · **Alternatives loại:** giữ dead-end / 200 single-shape (giấu `_err(404)`) / đóng `by_reason` 6-property (vỡ khi Select đổi) / `by_reason {type:string}` (value là giờ number) / `reason` enum (bất-đối-xứng+over-constrain) / `start_time format:date-time` (Frappe space-sep ≠ RFC3339) / `current_open` type:null (3.1-only). **Accepted.** |
| [`ADR-MOBILE-040.md`](./ADR-MOBILE-040.md) | ADR `getAssetVerifyChain` (`imm00.verify_chain`) curate vào OAS mirror (**CR-11c · asset-detail sub-tab #3 "Chuỗi kiểm toán bất biến"** — bồi ĐÚNG 1 GET-read path kiểm-tra toàn-vẹn hash-chain SHA-256 của IMM Audit Trail 1 asset; sub-tab THỨ BA của CR-11 sau `getAssetKpi` (ADR-038) + `getAssetDowntimeMetrics` (ADR-039); **LẦN ĐẦU success-data schema có property OPTIONAL genuine-absent** — `broken_at`/`index` chỉ emit khi `valid=false`) — **PATH ADD** CONTRACT-ONLY bồi 1 GET path GROUNDED 1:1 **HAI hàm-nguồn** LIVE: handler `verify_chain(asset)` @`api/imm00.py:1768-1774` (bare `@whitelist` → GET; guest dispatcher-403; `_err(_ERR_ASSET_NOT_FOUND, 404)` asset∄ @`:1771-1772` → HTTP-200 nhánh Error; `_ok(verify_audit_chain(asset))` @`:1774`) + builder `verify_audit_chain(asset)` @`utils/lifecycle.py:97-114` (**SoT return-shape** — đọc `tabIMM Audit Trail WHERE asset ORDER BY timestamp ASC` recompute SHA-256 hash-chain; **2 return-shape**: FAIL `{valid:False, broken_at:r.name, index:idx, count:len(rows)}` @`:112` / PASS `{valid:True, count:len(rows)}` @`:114`) · **1 param** `asset` (`in:query`, **`required:true`**, string — **⚠️ KHÔNG `name`/`asset_name`**, chữ-ký `verify_chain(asset)` positional @`:1769`) live-sig `set(inspect.signature)=={asset}` · **200 = oneOf `[AssetVerifyChainEnvelope, Error]`** closed route-by-VALUE `body.success` (Decision-B 0-discr; `Error.http_status ⊇ {404}`) · **`AssetVerifyChain` = OBJECT PHẲNG single** (`data=$ref`) closed `additionalProperties:false` **EXACT 4 prop** = HỢP 2 shape `{valid:boolean(GENUINE — KHÔNG int-enum), count:integer, broken_at:string, index:integer}` · **`required` = EXACT 2 `{valid, count}`** = GIAO 2 shape (key có ở MỌI response) · **`broken_at`+`index` OPTIONAL** ∉ required + `nullable:true` (scalar-nullable đơn-giản, KHÔNG `allOf` — KHÁC `current_open` ADR-039 nullable-ref; **CHỈ present khi `valid=false`** — mirror ADR-036 optional-emit) · **INV-VC-1** (prose-only): `broken_at` present ⟺ `index` present ⟺ `valid=false` (OpenAPI 3.0.3 KHÔNG mã-hoá — né discriminator/if-then) · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` SINGLE-SHAPE bare `@whitelist` no-allow_guest → guest dispatcher-403; KHÔNG in-handler cap-403) · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_READ_ENVELOPE` · path/opId **67→68** + c5 **56→57** + 2 schema mới (naming guard `AssetVerifyChain*` ∩ ∅) · compliance: bằng-chứng toàn-vẹn **NĐ98/2021** truy-xuất-nguồn-gốc (BR-00-03 / `AC-E010` tamper) · read-only **KHÔNG audit** (verify KHÔNG mutate → 0 lifecycle event) · **🟢 CONTRACT-ONLY** `git diff` `api/imm00.py` (verify_chain) + `utils/lifecycle.py` (verify_audit_chain) = TRỐNG round NÀY (BE LIVE; DUAL SCOPED-HANDLER invariant CẢ 2 hàm HEAD↔working) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · Guard `TestMobileAssetVerifyChainCurate` a..h (8 TC: a=path/opId 68 · b=GET-only opId/tag · c=1-param `asset`-required+live-sig+asset-NOT-name/asset_name · d=200-oneOf-route-by-value+Error-404 · e=Envelope-closed-data-flat-object · f=4-prop+required-2{valid,count}+broken_at/index-optional-nullable+valid-GENUINE-boolean · g=grounding-2-return-shape đọc-TRỰC-TIẾP `utils/lifecycle.py` [required==GIAO, props==HỢP, fail-only={broken_at,index}] · h=zero-footprint+naming-guard+0-dangling+DUAL-SCOPED-HANDLER CẢ verify_chain+verify_audit_chain pure-yaml; **RED-before/GREEN-after** strip path → path-count 67≠68+`Thiếu path` → bồi → GREEN) ⇒ `_EXPECTED_TEST_COUNT` **640→648** + `_GUARD_SUITE_SUM` **783→791** + `_MOBILE_OAS_TOTAL` **809→817** + delta var `asset_verify_chain_curate_delta=8` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **Alternatives loại:** giữ dead-end (không mở CR-11c) / 200 single-shape (giấu `_err(404)` → parse crash) / split ValidChain\|InvalidChain discriminated oneOf (`valid` boolean → discriminator illegal + phá acceptance single-closed) / `broken_at`+`index` vào required (deser crash khi `valid=true` thiếu field — nhánh PASS 2-key) / `valid` int-enum[0,1] (là Python bool KHÔNG Check-field) / grounding chỉ đọc handler (return-shape ở builder) / sinh audit khi verify (read-only, tự-thay-chuỗi). **Accepted.** |
| [`ADR-MOBILE-041.md`](./ADR-MOBILE-041.md) | ADR `getAssetCommissioningOrigin` (`imm04.get_commissioning_origin`) curate vào OAS mirror (**CR-11d · asset-detail sub-tab #4 "Nguồn gốc thiết bị"** — bồi ĐÚNG 1 GET-read path trả bản-ghi tiếp-nhận/lắp-đặt (Asset Commissioning) + PO gốc của 1 asset (truy-vết provenance NĐ98); sub-tab THỨ TƯ của CR-11 sau `getAssetKpi` (ADR-038) + `getAssetDowntimeMetrics` (ADR-039) + `getAssetVerifyChain` (ADR-040); **LẦN ĐẦU success-data = WRAPPER 2-tầng nested-nullable-object** — `{asset, commissioning:Record|null}`) — **PATH ADD** CONTRACT-ONLY bồi 1 GET path GROUNDED 1:1 **HAI tầng cùng tên** LIVE: handler `get_commissioning_origin(asset_name)` @`api/imm04.py:314-316` (bare `@whitelist` → GET; guest dispatcher-403; `return _handle(svc.get_commissioning_origin, asset_name)` — `_handle` bắt `ServiceError → _err` HTTP-200 Decision-B; **KHÔNG `rbac.require`** ⇒ 0 in-handler cap-403) + service `get_commissioning_origin(asset_name)` @`services/imm04.py:1972-1997` (**SoT payload** — `raise ServiceError(NOT_FOUND)` asset∄ @`:1975` → HTTP-200 nhánh Error; **2 early-return** `{asset, commissioning:None}` @`:1979` (chưa gắn `commissioning_ref`)/@`:1989` (doc biến-mất); happy-path `get_value` 11 field @`:1983-1985` + `transferred_doc_count`=`frappe.db.count` @`:1996`) · **1 param** `asset_name` (`in:query`, **`required:true`**, string — **⚠️ KHÔNG `name`/`asset`; REQUIRED KHÁC sibling `year` OPTIONAL** vì service `raise NOT_FOUND` asset∄) live-sig `set(inspect.signature(imm04.get_commissioning_origin))=={asset_name}` · **200 = oneOf `[AssetCommissioningOriginEnvelope, Error]`** closed route-by-VALUE `body.success` (Decision-B 0-discr; `Error.http_status ⊇ {404}`) · **`AssetCommissioningOrigin` = WRAPPER** (`data=$ref`) closed EXACT 2 prop `{asset:string, commissioning:object|null}` required=cả 2 (commissioning LUÔN emit value-null) · **`commissioning` = nullable-ref idiom** `{type:object, nullable:true, allOf:[$ref CommissioningOriginRecord]}` (OpenAPI 3.0.3 — `type:object` sibling redocly `nullable-type-sibling`; **mirror `current_open` ADR-039**) · **`CommissioningOriginRecord` closed EXACT 12 prop VERBATIM** get_value list @`:1983-1985` (thứ-tự) + `transferred_doc_count` @`:1996` `{name:string(req), workflow_state:string, po_reference/vendor/master_item/vendor_serial_no/commissioned_by:string nullable, reception_date/commissioning_date/warranty_expiry_date:string format:date nullable, purchase_price:number nullable (**FINANCIAL** curate-verbatim UI-render-deferred per LL-BE-57 + `total_repair_cost` ADR-038), transferred_doc_count:integer(req)}` · **required = EXACT 2 `{name, transferred_doc_count}`** (2 anchor always-non-null); 9 value-nullable ∉ required (convention `getAssetKpi` ADR-038 nullable-value ∉ required) · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` SINGLE-SHAPE bare `@whitelist` no-allow_guest → guest dispatcher-403; KHÔNG in-handler cap-403) · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_READ_ENVELOPE` · path/opId **68→69** + c5 **57→58** + 3 schema mới (naming guard `AssetCommissioningOrigin*` ∩ 2, `CommissioningOrigin*` ∩ 1, ∩ hiện có == ∅) · compliance: bằng-chứng **nguồn-gốc/xuất-xứ NĐ98/2021** (PO→NCC→model, tiếp-nhận/lắp-đặt) · read-only **KHÔNG audit** · **🟢 CONTRACT-ONLY** `git diff` `api/imm04.py` + `services/imm04.py` (get_commissioning_origin) = TRỐNG round NÀY (BE LIVE; DUAL SCOPED-HANDLER invariant CẢ 2 tầng HEAD↔working) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · Guard `TestMobileAssetCommissioningOriginCurate` a..h (8 TC: a=path/opId 69 · b=1-param `asset_name`-required+live-sig+KHÔNG-name/asset · c=200-oneOf-route-by-value+Error-404 · d=WRAPPER-closed-2-prop+commissioning-nullable-ref-idiom · e=Record-12-prop-VERBATIM+required-2+9-nullable+3-date-format+purchase_price-FINANCIAL+GROUNDING đọc-TRỰC-TIẾP `services/imm04.py` get_value-list+subscript · f=slot+403-SINGLE-Forbidden-dispatcher · g=Envelope-closed · h=zero-footprint+naming-guard-3+siblings-BẤT-BIẾN+0-dangling+DUAL-SCOPED-HANDLER pure-yaml; **RED-before/GREEN-after** đổi `transferred_doc_count` integer→string → RED TC-e → restore → GREEN) ⇒ `_EXPECTED_TEST_COUNT` **648→656** + `_GUARD_SUITE_SUM` **791→799** + `_MOBILE_OAS_TOTAL` **817→825** + delta var `asset_commissioning_origin_curate_delta=8` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **Alternatives loại:** giữ dead-end / flat-12-prop (bỏ wrapper → mất null-object) / `commissioning` scalar-nullable KHÔNG allOf (mất typed model) / `commissioning` ∉ required (LUÔN emit) / required=all-12 (deser crash field trống) / strip `purchase_price` FINANCIAL / param OPTIONAL (positional no-default + raise NOT_FOUND) / 200 single-shape (giấu raise) / grounding chỉ đọc handler (payload SoT ở service). **Accepted.** |
| [`ADR-MOBILE-042.md`](./ADR-MOBILE-042.md) | ADR `getAssetDepreciationSchedule` (`imm00.get_depreciation_schedule`) curate vào OAS mirror (**CR-11e · asset-detail sub-tab #5 "Khấu hao"** — bồi ĐÚNG 1 GET-read path trả lịch khấu hao (child `AC Asset Depreciation Schedule`) + tổng-hợp + thông-tin khấu hao của 1 asset; sub-tab THỨ NĂM (CUỐI) của CR-11 sau `getAssetKpi` (ADR-038) + `getAssetDowntimeMetrics` (ADR-039) + `getAssetVerifyChain` (ADR-040) + `getAssetCommissioningOrigin` (ADR-041); **HOÀN TẤT bộ-năm CR-11**; **LẦN ĐẦU success-data = WRAPPER 4-key với 3 nested-schema** — `{asset, asset_info:object\|null, rows[], summary}`) — **PATH ADD** CONTRACT-ONLY bồi 1 GET path GROUNDED 1:1 handler LIVE **tự-chứa** (KHÔNG service/`_handle`): `get_depreciation_schedule(asset_name)` @`api/imm00.py:2961-2989` (bare `@whitelist` → GET; guest dispatcher-403; `return _err(_("Asset not found"), 404)` asset∄ @`:2964-2965` → HTTP-200 nhánh Error Decision-B — **⚠️ `_err` in-handler trực-tiếp**, mirror `getAssetKpi`/`getAssetDowntimeMetrics` KHÁC ADR-041 `raise→_handle`; happy-path `get_all` 9 field @`:2966-2974` + `summary` compute @`:2975-2981` + `get_value` 9 field `or {}` @`:2982-2988`; `_ok({asset, asset_info, rows, summary})` @`:2989`) · **1 param** `asset_name` (`in:query`, **`required:true`**, string — **⚠️ KHÔNG `name`; REQUIRED KHÁC `getAssetKpi`/`getAssetDowntimeMetrics` year OPTIONAL** vì `_err(404)` asset∄) live-sig `set(inspect.signature(imm00.get_depreciation_schedule))=={asset_name}` · **200 = oneOf `[AssetDepreciationScheduleEnvelope, Error]`** closed route-by-VALUE `body.success` (Decision-B 0-discr; `Error.http_status ⊇ {404}`) · **`AssetDepreciationSchedule` = WRAPPER 4-key** (`data=$ref`) closed `{asset:string(req), asset_info:object\|null(req, nullable-ref idiom `{type:object,nullable:true,allOf:[$ref AssetDepreciationInfo]}` `{}` coalesce @`:2988` mirror `commissioning` ADR-041/`current_open` ADR-039), rows:array<DepreciationScheduleRow>(req), summary:$ref DepreciationScheduleSummary(req)}` required=cả 4 (MỌI key LUÔN emit) · **`DepreciationScheduleRow` closed EXACT 9 prop VERBATIM** get_all list @`:2969-2971` `{name:string(req), period_number:integer nullable, scheduled_date/executed_on:string format:date nullable, depreciation_amount/accumulated_amount/remaining_value:number nullable (**FINANCIAL** curate-verbatim), status:string **enum[Pending,Executed,Cancelled]** (Select bounded no-blank default 'Pending' — **DB-verified 413/79/42 0-blank/534** bám ADR-028 `depreciation_frequency`), journal_entry:string nullable}` required=EXACT`[name]` (child docname PK); 7 value-nullable ∉ required · **`DepreciationScheduleSummary` closed EXACT 4 prop** compute @`:2975-2981` `{total_periods/executed_periods/pending_periods:integer, total_depreciated:number (**FINANCIAL**)}` required=đủ 4 (always-non-null) · **`AssetDepreciationInfo` closed EXACT 9 prop VERBATIM** get_value list @`:2984-2986` `{gross_purchase_amount/residual_value/accumulated_depreciation/current_book_value:number nullable (**FINANCIAL**), depreciation_method/depreciation_frequency:string nullable **NO-enum** (Select LEADING-BLANK `''` hợp-lệ — ADR-028 sub-case, KHÁC Row.status no-blank→enum), total_depreciation_months:integer nullable, depreciation_start_date/in_service_date:string format:date nullable}` **required=∅** (TOÀN value-nullable, 0 anchor — convention `getAssetKpi` ADR-038) · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` SINGLE-SHAPE bare `@whitelist` no-allow_guest → guest dispatcher-403; KHÔNG in-handler cap-403) · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_READ_ENVELOPE` · path/opId **69→70** + c5 **58→59** + 5 schema mới (naming guard `AssetDepreciation*` ∩ 3, `DepreciationSchedule*` ∩ 2, ∩ hiện có == ∅) + opId convention-map (`_EXPECTED`) += `get_depreciation_schedule → getAssetDepreciationSchedule` · compliance: dữ-liệu **giá-trị tài-sản** (khấu hao/kế-toán — WHO HTM end-of-life planning) · read-only **KHÔNG audit** · **🟢 CONTRACT-ONLY** `git diff` `api/imm00.py` (get_depreciation_schedule AST) = TRỐNG round NÀY (BE LIVE; SCOPED-HANDLER invariant HEAD↔working) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · Guard `TestMobileAssetDepreciationScheduleCurate` a..h (8 TC: a=path/opId 70 · b=1-param `asset_name`-required+live-sig+KHÔNG-name · c=200-oneOf-route-by-value+Error-404 · d=WRAPPER-closed-4-prop+asset_info-nullable-ref-idiom+rows-array+summary-$ref · e=Row-9-prop-VERBATIM+required-[name]+3-FINANCIAL+status-enum-DB-grounded (đọc-TRỰC-TIẾP `ac_asset_depreciation_schedule.json` Select)+GROUNDING per-doctype get_all-list · f=Summary-4-prop+Info-9-prop-0-required+2-Select-leading-blank-no-enum+GROUNDING per-doctype get_value-list+dict-literal · g=Envelope-closed+membership+403-SINGLE-Forbidden · h=zero-footprint+naming-guard-5+4-siblings-BẤT-BIẾN+0-dangling+SCOPED-HANDLER pure-yaml; **RED-before/GREEN-after** strip path → path-count 69≠70+93 cascade FAIL → restore → **Ran 664 OK**) ⇒ `_EXPECTED_TEST_COUNT` **656→664** + `_GUARD_SUITE_SUM` **799→807** + `_MOBILE_OAS_TOTAL` **825→833** + delta var `asset_depreciation_schedule_curate_delta=8` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **⚠️ 10 secondary count-guard** (3 c5 + 1 parity 58→59 + 4 backward-compat opId-set-minus 68→69/67→68 + push `len(paths)==len(_EXPECTED)` + `test_05_convention` = THÊM `_EXPECTED` entry) KHÔNG dùng token `, 69,` ⇒ full-suite THẬT bắt sót · **Alternatives loại:** giữ dead-end / flatten (mất 3 nested) / `asset_info` scalar-nullable no-allOf (mất typed model) / `asset_info` ∉ required (LUÔN emit) / Row.status string-plain (mất badge typed) / Info Select enum (leading-blank `''` hợp-lệ) / Info required=all-9 (deser crash field trống) / strip 8 FINANCIAL / param OPTIONAL (positional no-default + `_err(404)`) / 200 single-shape (giấu `_err`) / grounding chỉ đọc get_all (3 nguồn khác). **Accepted.** |
| [`ADR-MOBILE-047.md`](./ADR-MOBILE-047.md) | ADR `reportIncident` idempotency key `client_request_id` (**CR-24 · BE-OWNED mechanism + OAS property-add** — bồi 1 **property OPTIONAL** `client_request_id: {type:string}` vào schema `ReportIncidentRequest` (∉ `required[]`) + hiện thực dedupe backend đóng cửa sổ re-drain outbox tạo phiếu sự cố **TRÙNG** khi response `report_incident` rớt mạng; **KHÁC ADR-021..046 contract-only pure-yaml**: ADR này CÓ đụng `.py` + DocType + `bench migrate` (tạo cột + unique index) ⇒ **KHÔNG [AUTO], HARD-STOP USER reload gunicorn** cho HTTP live) — **BE-OWNED** GROUNDED 1:1 source: DocType `Incident Report` field `client_request_id` (`Data`, `read_only`, `no_copy`, `set_only_once`, **`unique:1`**) @`incident_report.json:414` → `bench migrate` cột `varchar(140) NULL UNI` + BTREE unique index (dedupe **O(1) index-seek**, KHÔNG full-table scan; nullable → nhiều phiếu không-khoá lưu NULL, MariaDB multi-NULL hợp lệ → **backward-compat 100%**, precedent `AC Asset.gmdn_code`) · service `services/imm12.py`: `_dedupe_lookup(key)` @`:450` (`frappe.db.get_value("Incident Report",{"client_request_id":key},["name","status","severity"],as_dict=True)` → shape create-response hoặc None) · `report_incident(..., *, client_request_id: str="")` @`:484` — **dedupe pre-check** `existing=_dedupe_lookup(key); if existing: return existing` @`:499-501` (TRƯỚC new_doc → **0 lifecycle `incident_reported` + 0 IMM Audit Trail lần 2**, NĐ98 audit-integrity) · persist `if client_request_id: doc.client_request_id=client_request_id` @`:544-545` (rỗng→NULL) · **race-handler** `try doc.insert() except frappe.UniqueValidationError: frappe.clear_last_message(); winner=_dedupe_lookup(key); if winner: return winner` @`:548-559` (concurrent re-drain 2 request cùng key → unique DB chặn → re-read → return winner, KHÔNG raise) · API `api/imm12.py`: `report_incident(..., client_request_id: str="")` @`:102` (KHÔNG `str|None` → tránh HTTP 417) → `handle(svc_report,...,client_request_id=client_request_id)` @`:131` · OAS `ReportIncidentRequest.properties.client_request_id: {type:string}` @`yaml:3271` **∉ required[]** (GIỮ EXACT 4 `[asset,incident_type,severity,description]`) · **⚠️ SCHEMA GIỮ OPEN** (`additionalProperties` KHÔNG set — 8 field optional server-nhận `fault_code`/`workaround_applied`/… CHƯA bồi vào schema; đóng `false` → codegen reject form-encoded → vỡ; guard 13k khoá invariant OPEN — task/Handoff §2 ghi "additionalProperties:false GIỮ" là **GIẢ ĐỊNH SAI** về schema này, flag open-issue) · handler-parity `client_request_id ∈ inspect.signature(report_incident)` ⇒ MỌI yaml-prop ⊆ live-params (guard 13g GIỮ xanh) · **backward-compat 100%** request KHÔNG khoá → `_dedupe_lookup("")`=None → tạo-mới nguyên vẹn; keyword-only default `""` ⇒ 0 breaking call-site · slot/path KHÔNG đổi (property-add thuần, **0 path/opId mới**) · Guard `TestMobileReportIncidentIdempotencyContract` 13i/13j/13k (3 TC: 13i=property-present+type-string+desc-nêu-'idempotency' · 13j=OPTIONAL-∉-required+required-EXACT-4-backward-compat · 13k=schema-OPEN-additionalProperties≠false+handler-parity-client_request_id∈live-sig+props⊆live-params) + backend `TestReportIncidentIdempotency` TC1-6 (**RED-before proven**: neutralize CR-24 → TC1/TC2/TC5/TC6 FAIL 2-phiếu/None/count-0 → restore → GREEN; TC1=same-key-1-phiếu+name2==name1 · TC2=asset-riêng-lifecycle==1+audit==1 · TC3=no-key-backward-compat-2-phiếu · TC4=distinct-keys-2-phiếu · TC5=field-persisted · TC6=race-UniqueValidationError-mock-return-winner) ⇒ `_EXPECTED_TEST_COUNT` **704→707** + `_GUARD_SUITE_EXPECTED[test_mobile_oas.py]` **704→707** + `_GUARD_SUITE_SUM` **847→850** + `_MOBILE_OAS_TOTAL` **873→876** + delta var `idempotency_cr24_delta=3` (transition-baseline doc_09 giữ `pre_fc3_six==191`) + 2 hardcoded `_EXPECTED_TEST_COUNT==704`→707 · DoD: `test_imm12` **Ran 131 OK** · `test_mobile_oas` **Ran 707 OK** · `test_mobile_docset` **Ran 9 OK** (balance ADR **47==47**) · **Alternatives loại:** HTTP header (form_dict không route header) / `search_index` thay unique (mất race-handler) / lưu `""` (vỡ backward-compat, Frappe→NULL) / TTL (over-engineer MVP) / bỏ race-handler (hở concurrent) / đóng schema false (vỡ 8 field optional) / `client_request_id ∈ required` (vỡ online-first). **Accepted.** |
| [`ADR-MOBILE-048.md`](./ADR-MOBILE-048.md) | ADR `listCommissioning` (`imm04.list_commissioning`) curate vào OAS mirror (**CR-25a · MỞ NHÁNH IMM-04 F6 "Tiếp nhận & Nghiệm thu hiện trường"** — bồi ĐÚNG 1 GET-list path trả danh sách bản-ghi nghiệm thu (Asset Commissioning) permission-aware phân-trang; LIST-ENTRY của màn F6; **op ĐẦU TIÊN tag `commissioning`**) — **PATH ADD** CONTRACT-ONLY GROUNDED 1:1 handler LIVE `list_commissioning(filters="{}", page=1, page_size=20)` @`api/imm04.py:24` (bare `@whitelist` → GET; guest dispatcher-403; `parse_json` try/except `_err(INVALID_PARAMS,400)` malformed → HTTP-200 Error; `_handle(svc.list_commissioning,...)`) + service @`services/imm04.py:831` (`frappe.has_permission(throw=True)` fail → `raise ServiceError(FORBIDDEN)` → `_err` HTTP-200 ⇒ **oneOf**; `safe_filters` lọc `_ALLOWED_FILTER_KEYS`; `frappe.get_all(fields=_LIST_FIELDS)` + enrich `*_name`; `return {"items", "pagination"}` → **rows-key `data.items[]`** Asset-style KHÁC PM/calib `data.data[]`) · **1 param JSON-string** `CommissioningFilters` (`filters` `in:query` `type:string` default `'{}'`, mirror `WorkOrderFilters`@235; mô-tả 12 key `_ALLOWED_FILTER_KEYS`) + `Page` + `PageSize` (đúng 3, thứ-tự `[filters,page,page_size]`) · **200 = oneOf `[CommissioningListEnvelope, Error]`** wire qua **response-component `CommissioningList`** (mirror `AssetList`/`IncidentList` — sweep C7/`_MVP_LIST_ENVELOPE` resolve `content.schema.oneOf`); Decision-B route-by-VALUE 0-discr; `Error.http_status ⊇ {400,403}` · **3 schema RIÊNG closed** (`additionalProperties:false`): `CommissioningListItem` **EXACT 20 prop** = 13 `_LIST_FIELDS` (`name`(req PK)/`workflow_state`/`docstatus:integer`/`po_reference`/`master_item`/`vendor`/`clinical_dept`/`expected_installation_date:date nullable`/`installation_date:string nullable`/`vendor_serial_no`/`internal_tag_qr`/`final_asset`/`modified`) + 7 enrich `*_name` (`master_item_name`/`device_model_name`/`vendor_name`/`supplier_name`/`clinical_dept_name`/`po_ref_name`/`asset_name`), required=`[name]`; **⚠️ list-item MIỄN Check int-0/1** — `is_radiation_device`/`doa_incident` là `Check` NHƯNG chỉ ∈ `_ALLOWED_FILTER_KEYS` (filter-key) **∉ `_LIST_FIELDS`** → 0 prop boolean/int-0/1 (né CR-01 coercion; Check int-0/1 chỉ ở DETAIL `get_form_context`) · `CommissioningListPage` `{items:array<CommissioningListItem>, pagination:$ref Pagination}` (REUSE `Pagination`@752, KHÔNG tạo mới) · `CommissioningListEnvelope` `{success.enum[true], data:$ref CommissioningListPage}` · slot `{200,401,403}` (`401 Unauthorized401` uniform parity `listIncidents` + `403 Forbidden`) · tag `commissioning` (op-level module-tag, KHÔNG top-level `tags:` block) · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_LIST_ENVELOPE` · path/opId **75→76** + `_MVP_LIST_ENVELOPE` **8→9** + c5 **64→65** + parity **64→65** + `_EXPECTED` += `list_commissioning → listCommissioning` + 3 schema + 1 response + 1 param mới (naming guard `Commissioning*List*` ∩ `AssetCommissioning*`/`CommissioningOrigin*` == ∅ — **KHÔNG nhầm `getAssetCommissioningOrigin`/`AssetCommissioningOrigin` ADR-041**, TC-h khẳng-định 2 artefact đó GIỮ NGUYÊN) · **🟢 CONTRACT-ONLY** backend LIVE `api/imm04.py:24` + `services/imm04.py:831` ⇒ **0 `.py`/reload/migrate** ([AUTO]) · Guard `TestMobileListCommissioningContract` a..h (8 TC: a=path/opId 76+GET+opId+tag commissioning · b=Item closed+EXACT-20-prop drift-guard · c=Item KHÔNG is_radiation_device/doa_incident/Check-int01 · d=Page closed+items.$ref+pagination.$ref · e=params 3-$ref thứ-tự filters/page/page_size+CommissioningFilters query-string · f=200 response-component oneOf `[Envelope,Error]`+401 Unauthorized401 · g=Envelope closed+success.enum[true]+data.$ref CommissioningListPage rows-key items · h=no-collision AssetCommissioningOrigin+getAssetCommissioningOrigin BẤT-BIẾN; **RED-before/GREEN-after** proven) ⇒ `_EXPECTED_TEST_COUNT` **707→715** + `_GUARD_SUITE_EXPECTED[test_mobile_oas.py]` **707→715** + `_GUARD_SUITE_SUM` **850→858** + `_MOBILE_OAS_TOTAL` **876→884** · **Alternatives loại:** raw-apiClient mãi (mất typed) / SINGLE-shape (giấu `_err` FORBIDDEN/400 → parse crash) / `data.data[]` (service trả `items`) / inline oneOf (sweep cần response-component) / bồi Check int-0/1 vào list-item (∉ `_LIST_FIELDS`) / tạo `Pagination` mới (REUSE @752) / tên `AssetCommissioning*` (đụng ADR-041). **Accepted.** |
| [`ADR-MOBILE-049.md`](./ADR-MOBILE-049.md) | ADR `listAllocations` (`imm15.list_allocations`) curate vào OAS mirror (**CR-29a · MỞ NHÁNH IMM-15 F9 "Xuất kho phụ tùng phục vụ WO"** — bồi ĐÚNG 1 GET-list path trả danh sách phiếu cấp phát/xuất kho phụ tùng (IMM Spare Allocation) phục vụ Work Order, permission-aware phân-trang; LIST-ENTRY màn F9; **op ĐẦU TIÊN tag `inventory`**; **MỞ NHÁNH IMM-15** = 0 endpoint curated trước round này) — **PATH ADD** CONTRACT-ONLY GROUNDED 1:1 handler LIVE `list_allocations(filters="{}", page=1, page_size=20, workflow_state="", asset="", work_order_ref="", urgency="")` @`api/imm15.py:42` (bare `@whitelist` → GET; guest dispatcher-403; `_parse_json` try/except `_err(INVALID_PARAMS,400)` malformed → HTTP-200 Error @`:49-52`; 4 param discrete **merge lên filter dict** @`:54-61`; **KHÔNG raise HTTP-4xx**) + service @`services/imm15.py:206` (`AllocationRepo.list(fields=11)` + `_enrich_display_names`; **`return {"data":rows,"pagination":pg}`** → `_ok` ⇒ **⚠️ rows-key `data.data[]` DOUBLE-DATA** mirror PM/calib KHÁC `listCommissioning` `data.items[]`) · **7 param typed 1:1 argspec**: `filters` inline JSON-string + `$ref Page` + `$ref PageSize` + 4 inline `workflow_state`/`asset`/`work_order_ref`/`urgency` (string optional) · **200 = oneOf `[AllocationListEnvelope, Error]`** wire qua **response-component `AllocationList`** (Decision-B route-by-VALUE 0-discr) · **3 schema RIÊNG closed** (`additionalProperties:false`): `AllocationListItem` **EXACT 14 prop** = 11 `AllocationRepo.list` @`:210-212` (`name`(req PK)/`work_order_ref`/`work_order_doctype`/`asset`/`warehouse_from`/`requested_by`/`requested_date:date nullable`/`urgency`/`allocation_status`/`total_value:number`/`stock_movement_ref:nullable`) + 3 enrich @`:216-220` special-case @`:189-196` (`asset_name`/**`warehouse_name`** [KHÔNG `warehouse_from_name`]/`requested_by_name`), required=`[name]`; **⚠️ 0 prop Check int-0/1** (né CR-01) · `AllocationListPage` **`{data:array<AllocationListItem>, pagination:$ref Pagination}`** (⚠️ rows-key `data` DOUBLE-DATA, assert `'items'∉keys` chống copy-nhầm; REUSE `Pagination`@785) · `AllocationListEnvelope` `{success.enum[true], data:$ref AllocationListPage}` · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` guest dispatcher-403) · tag `inventory` (op-level module-tag IMM-15) · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_LIST_ENVELOPE` · path/opId **79→80** + `_MVP_LIST_ENVELOPE` **10→11** + c5 **68→69** + parity **68→69** + `_EXPECTED` += `list_allocations → listAllocations` (naming guard `Allocation*` ∩ `Commissioning*`/`DueCalibration*` == ∅) · **🟢 CONTRACT-ONLY** backend LIVE `api/imm15.py:42` + `services/imm15.py:206` ⇒ **0 `.py`/reload/migrate** · Guard `TestMobileListAllocationContract` a..g (7 TC: a=path/opId 80+GET+opId+tag inventory · b=Item closed EXACT-14 + 3 enrich key ground-verbatim + 0 boolean · c=**anti-copy** Page `{data,pagination}` + `'items'∉keys` double-data · d=params 7 typed 1:1 (2 $ref Page/PageSize + 5 inline) · e=200 response-component oneOf `[Envelope,Error]` + Envelope.data→AllocationListPage · f=3 schema closed + `_MVP_LIST_ENVELOPE` 11 + live-sig parity 7 param · g=naming-guard Allocation* == 3 disjoint Commissioning*/DueCalibration*) ⇒ `_EXPECTED_TEST_COUNT` **728→735** + `_GUARD_SUITE_EXPECTED[test_mobile_oas.py]` **728→735** + `_GUARD_SUITE_SUM` **871→878** + `_MOBILE_OAS_TOTAL` **897→904** + delta `list_allocations_delta=7` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **Alternatives loại:** raw-apiClient mãi (mất typed) / `data.items[]` (service trả `data` → deser rỗng rows) / SINGLE-shape (giấu `_err` 400 malformed → parse crash) / `CommissioningFilters` JSON-blob cho 4 discrete (handler merge riêng) / hợp-nhất rows-key về `items` (đụng service `.py` = Phase-E). **Accepted.** |
| [`ADR-MOBILE-050.md`](./ADR-MOBILE-050.md) | ADR `getAllocation` (`imm15.get_allocation`) curate vào OAS mirror (**CR-29b · MỞ NHÁNH IMM-15 F9-DETAIL "Xuất kho phụ tùng phục vụ WO"** — bồi ĐÚNG 1 GET-detail path trả phiếu cấp phát/xuất kho phụ tùng CHI TIẾT (header + `items[]` child table + `allowed_transitions[]` CTA server-driven) phục vụ Work Order, permission-aware; **DETAIL-sibling** của `listAllocations` ADR-MOBILE-049 (CÙNG màn F9, CÙNG DocType `IMM Spare Allocation`, tag `inventory`)) — **PATH ADD** CONTRACT-ONLY GROUNDED 1:1 handler LIVE `get_allocation(name)` @`api/imm15.py:66` (bare `@whitelist` → GET; guest dispatcher-403; `_handle(svc.get_allocation, name)` passthrough) + service @`services/imm15.py:224` (`doc = AllocationRepo.get(name)`; `if not doc: raise ServiceError(NOT_FOUND)` @`:228` → `_handle`→`_err` **LỖI TRÊN HTTP-200** KHÔNG status-line 404; `data = doc.as_dict()` + items[] enrich `part_name`/`uom`/`unit_value` @`:235-237` + 3 header display-name `asset_name`/`warehouse_name`/`requested_by_name` @`:239-241` + `allowed_transitions` @`:244` R16 CTA) · **1 param** `name` `in:query` `required:true` `type:string` (RPC no-template) · **200 = inline `oneOf [SpareAllocationDetailEnvelope, Error]`** Decision-B route-by-VALUE `body.success` 0-discr (mirror `getCalibration` — ∈ `_MVP_READ_ENVELOPE` KHÔNG response-component) · **⚠️ SELF-CORRECTION (Core Doc IMM-15 §3.0 + ADR self-correct #1): `SpareAllocationItem` + `SpareAllocationDetail` = OPEN `additionalProperties:true`** (đề mục yêu cầu CLOSED NHƯNG ground-truth: `doc.as_dict()` surface meta Frappe `owner`/`creation`/`docstatus`/`idx` + child `parent`/`parentfield`/`parenttype` VƯỢT list nghiệp-vụ; **precedent nhất-quán CẢ 6 `*Detail` as_dict-based hiện có đều OPEN** — `get_allocation` COPY Y HỆT `get_calibration` → `CalibrationDetail` OPEN; CLOSED sẽ khiến OAS misrepresent payload) · **CHỈ envelope `SpareAllocationDetailEnvelope` = CLOSED** (`additionalProperties:false` — mirror `PmWorkOrderDetailEnvelope` §3.2) · **schema:** `SpareAllocationItem` (OPEN, `required:[spare_part]`) 13 field nghiệp-vụ liệt kê @`doctype imm_spare_allocation_item.json` (`used_for` enum 4 `[Replacement,Test,Calibration,Spare]` + `return_condition` enum 3 `[Good,Damaged,Used]` KHỚP Select options) · `SpareAllocationDetail` (OPEN, `required:[name]`) 27 prop header @`doctype imm_spare_allocation.json` + `items[]` `$ref SpareAllocationItem` + 3 enrich + `allowed_transitions[]` (array string) + `allocation_status` enum 6 `[Requested,Approved,Picked,Issued,Returned,Cancelled]` · `SpareAllocationDetailEnvelope` `{success.enum[true], data:$ref SpareAllocationDetail}` · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden`) · tag `inventory` · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_READ_ENVELOPE` (KHÔNG `_MVP_LIST_ENVELOPE` — **detail ≠ list** ⇒ `_MVP_LIST_ENVELOPE` GIỮ 11) · path/opId **80→81** + c5 **69→70** + parity **69→70** + `_EXPECTED` += `get_allocation → getAllocation` (naming guard `SpareAllocation*` ∩ `Allocation*List*` == ∅ prefix `Spare*`) · **🟢 CONTRACT-ONLY** backend LIVE `api/imm15.py:66` + `services/imm15.py:224` ⇒ **0 `.py`/reload/migrate** · Guard `TestMobileGetAllocationDetailContract` a..g + live (8 TC: a=parser+MỌI-$ref-resolve+3-schema-KHÔNG-orphan · b=path/opId 81+GET+tag inventory+∈`_MVP_READ_ENVELOPE`+∉`_MVP_LIST_ENVELOPE` · c=param `name` typed query required string · d=200 oneOf `[Envelope,Error]` 0-discr + 401/403 · e=Detail OPEN+required[name]+items.$ref+allowed_transitions+3-enrich+allocation_status-enum-6 · f=Item OPEN+13-field-⊇+used_for/return_condition-enum-KHỚP-doctype · g=guard-reconcile path 81/c5 70/parity 70/`_MVP_LIST_ENVELOPE` GIỮ 11 · live=field-domain-subset-anti-drift + live-sig `get_allocation(name)`) ⇒ `_EXPECTED_TEST_COUNT` **735→743** + `_GUARD_SUITE_EXPECTED[test_mobile_oas.py]` **735→743** + `_GUARD_SUITE_SUM` **878→886** + `_MOBILE_OAS_TOTAL` **904→912** + delta var `get_allocation_detail_delta=8` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **Alternatives loại:** CLOSED Item/Detail (misrepresent as_dict payload, phá precedent 6 *Detail — self-correct OPEN) / response-component (read-detail dùng inline oneOf mirror getCalibration) / ∈ `_MVP_LIST_ENVELOPE` (detail ≠ list) / status-line 404 (Decision-B trên HTTP-200). **Accepted.** |
| [`ADR-MOBILE-051.md`](./ADR-MOBILE-051.md) | ADR `listInternalAudits` (`imm16.list_internal_audits`) curate vào OAS mirror (**CR-27a · MỞ NHÁNH IMM-16 F7 "Audit nội bộ (checklist hiện trường)"** — bồi ĐÚNG 1 GET-list path trả danh sách phiên audit nội bộ (IMM Internal Audit) phục vụ Compliance/QMS, permission-aware phân-trang; LIST-ENTRY màn F7; **op ĐẦU TIÊN tag `compliance`**; **MỞ NHÁNH IMM-16** = 0/4 endpoint curated trước round này) — **PATH ADD** CONTRACT-ONLY GROUNDED 1:1 handler LIVE `list_internal_audits(filters="{}", page=1, page_size=20)` @`api/imm16.py:80` (bare `@whitelist` → GET; guest dispatcher-403; `_parse_json` try/except `_err(INVALID_PARAMS)` malformed → HTTP-200 Error @`:82-85`; `_handle(svc.list_internal_audits, f, page, page_size)`; **KHÔNG param discrete** — 3-param JSON-passthrough thuần KHÁC `listAllocations` 7-param) + service @`services/imm16.py:360` (`InternalAuditRepo.list(fields=8)` @`:365-366` order_by `planned_start desc` + enrich `lead_auditor_name` @`:371-374` CHỈ khi truthy; **`return {"data":rows,"pagination":pg}`** @`:375` → `_ok` ⇒ **⚠️ rows-key `data.data[]` DOUBLE-DATA** mirror PM/calib/allocation KHÁC `listCommissioning` `data.items[]`) · **3 param typed 1:1 argspec**: `filters` inline `type:string` `default:'{}'` + `$ref Page` + `$ref PageSize` (KHÔNG discrete override) · **200 = oneOf `[InternalAuditListEnvelope, Error]`** wire qua **response-component `InternalAuditList`** (Decision-B route-by-VALUE 0-discr) · **3 schema RIÊNG closed** (`additionalProperties:false`): `InternalAuditListItem` **EXACT 9 prop** = 8 `InternalAuditRepo.list` VERBATIM @`:365-366` (`name`(req PK)/`audit_code`/`audit_type`/`planned_start:date nullable`/`planned_end:date nullable`/`lead_auditor:nullable`/`status`/`findings_count:integer`) + 1 enrich `lead_auditor_name` @`:371-374` **OPTIONAL ∉ required[]** (CHỈ set khi `lead_auditor` truthy), required=`[name]`; **⚠️ SELF-CORRECTION §2.c.1: `audit_type`/`status` = `type:string` KHÔNG hard `enum:`** (DocType Select **leading-blank** → `""` value HỢP-LỆ đã-persist; hard-enum reject-valid-row → strict-codegen deser CRASH; enum-values ở `description` = domain-doc; đúng list-item precedent `AllocationListItem`); **⚠️ 0 prop Check int-0/1** (`findings_count`=`Int` count KHÔNG Check) · `InternalAuditListPage` **`{data:array<InternalAuditListItem>, pagination:$ref Pagination}`** (⚠️ rows-key `data` DOUBLE-DATA, assert `'items'∉keys` chống copy-nhầm; REUSE `Pagination`) · `InternalAuditListEnvelope` `{success.enum[true], data:$ref InternalAuditListPage}` · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` guest dispatcher-403) · tag `compliance` (op-level module-tag IMM-16) · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_LIST_ENVELOPE` · path/opId **81→82** + `_MVP_LIST_ENVELOPE` **11→12** + c5 **70→71** + parity **70→71** + `_EXPECTED` += `list_internal_audits → listInternalAudits` (naming guard `InternalAudit*` ∩ `Allocation*`/`Commissioning*`/`DueCalibration*` == ∅) · **🟢 CONTRACT-ONLY** backend LIVE `api/imm16.py:80` + `services/imm16.py:360` ⇒ **0 `.py`/reload/migrate** · Guard `TestMobileListInternalAuditContract` a..g (7 TC: a=path/opId 82+GET+opId+tag compliance · b=Item closed EXACT-9 + `lead_auditor_name`∉required + 0 boolean + `audit_type`/`status` type:string KHÔNG enum · c=**anti-copy** Page `{data,pagination}` + `'items'∉keys` double-data · d=params 3 typed 1:1 (2 $ref Page/PageSize + 1 inline filters default '{}') · e=200 response-component oneOf `[Envelope,Error]` + Envelope.data→InternalAuditListPage · f=3 schema closed + `_MVP_LIST_ENVELOPE` 12 + live-sig parity 3 param · g=naming-guard InternalAudit* == 3 disjoint Allocation*/Commissioning*/DueCalibration*) ⇒ `_EXPECTED_TEST_COUNT` **743→750** + `_GUARD_SUITE_EXPECTED[test_mobile_oas.py]` **743→750** + `_GUARD_SUITE_SUM` **886→893** + `_MOBILE_OAS_TOTAL` **912→919** + delta `list_internal_audits_delta=7` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **Alternatives loại:** raw-apiClient mãi (mất typed) / `data.items[]` (service trả `data` → deser rỗng rows) / SINGLE-shape (giấu `_err` 400 malformed → parse crash) / hard `enum:` audit_type/status (leading-blank "" hợp-lệ → reject-valid-row → deser CRASH) / `lead_auditor_name` ∈ required (service gán conditional) / discrete override param (handler 3-param passthrough) / hợp-nhất rows-key về `items` (đụng service `.py` = Phase-E). **Accepted.** |
| [`ADR-MOBILE-052.md`](./ADR-MOBILE-052.md) | ADR `getInternalAudit` (`imm16.get_audit`) curate vào OAS mirror (**CR-27b · MỞ NHÁNH IMM-16 F7-DETAIL "Audit nội bộ (checklist hiện trường)"** — bồi ĐÚNG 1 GET-detail path trả phiên audit nội bộ CHI TIẾT (header + `checklist_items[]` child + `findings[]` child + `allowed_transitions[]` CTA server-driven + `can_operate`/`can_close` capability flags) phục vụ Compliance/QMS; **DETAIL-sibling** của `listInternalAudits` ADR-MOBILE-051 (CÙNG màn F7, CÙNG DocType `IMM Internal Audit`, tag `compliance`)) — **PATH ADD** CONTRACT-ONLY GROUNDED 1:1 handler LIVE `get_audit(name)` @`api/imm16.py:246` (bare `@whitelist` → GET; guest dispatcher-403; `_handle(svc.get_audit, name)` passthrough) + service @`services/imm16.py:1626` (`doc = InternalAuditRepo.get(name)`; `if not doc: raise ServiceError(NOT_FOUND)` @`:1629` → `_handle`→`_err` **LỖI TRÊN HTTP-200** KHÔNG status-line 404; `data = doc.as_dict()` + `lead_auditor_name` enrich @`:1631-1634` CHỈ khi `lead_auditor` truthy + `allowed_transitions` @`:1637` GATE-8/ADR-IMM-16-02 + `can_operate` @`:1638` + `can_close` @`:1639` rbac.can boolean) · **1 param** `name` `in:query` `required:true` `type:string` (RPC no-template) · **200 = inline `oneOf [InternalAuditDetailEnvelope, Error]`** Decision-B route-by-VALUE `body.success` 0-discr (mirror `getAllocation` R42 — ∈ `_MVP_READ_ENVELOPE` KHÔNG response-component) · **⚠️ SELF-CORRECTION (mở rộng ADR-MOBILE-050 sang IMM-16): `InternalAuditDetail` + `InternalAuditChecklistItem` + `AuditFindingItem` = OPEN `additionalProperties:true`** (acceptance CR-27b yêu cầu CLOSED-4-schema + key-parity EXACT NHƯNG **mâu thuẫn nội tại** — ĐỒNG THỜI yêu cầu "parity `getAllocation` R42" = detail OPEN; ground-truth: `get_audit` trả `doc.as_dict()` surface `services/imm16.py:1630` **Y HỆT** `get_allocation` `services/imm15.py:224` → emit meta Frappe `owner`/`creation`/`docstatus`/`idx` + child `parent`/`parentfield`/`parenttype` VƯỢT list nghiệp-vụ; CLOSED trên as_dict = **hợp đồng nói dối** → strict Dart/Kotlin codegen deser CRASH trên meta-key; "closed" = COPY nhầm precedent R43 list-item [`InternalAuditRepo.list` CURATED `fields=8` → closed OK cho LIST]; **precedent nhất-quán CẢ 6 `*Detail` as_dict-based đều OPEN**) · **CHỈ envelope `InternalAuditDetailEnvelope` = CLOSED** (`additionalProperties:false` — mirror `SpareAllocationDetailEnvelope`) · **schema:** `InternalAuditChecklistItem` (OPEN, `required:[item_description]`) 7 field @`doctype imm_audit_checklist_item.json`; `AuditFindingItem` (OPEN, `required:[severity,description]`) 7 field @`doctype audit_finding.json`; `InternalAuditDetail` (OPEN, `required:[name]`) header @`doctype imm_internal_audit.json` + `checklist_items[]` `$ref InternalAuditChecklistItem` + `findings[]` `$ref AuditFindingItem` (**BẮT BUỘC khai** — as_dict LUÔN kèm) + `lead_auditor_name` OPTIONAL + `allowed_transitions[]` (array string) + `can_operate`/`can_close` (boolean) · **⚠️ `audit_type`/`status` (header) + `result`/`category`/`severity`/`capa_status` (child Select) = `type:string` KHÔNG hard-`enum:`** (Select leading-blank/reqd=None ⇒ `""` hợp-lệ; ADR-MOBILE-051 §2.c.1 — hard-enum reject-valid-row → codegen CRASH) · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden`) · tag `compliance` · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_READ_ENVELOPE` (KHÔNG `_MVP_LIST_ENVELOPE` — **detail ≠ list** ⇒ `_MVP_LIST_ENVELOPE` GIỮ 12) · path/opId **82→83** + c5 **71→72** + parity **71→72** + `_EXPECTED` += `get_audit → getInternalAudit` (naming guard `InternalAudit*` == 6: 3 list + 3 detail; `AuditFindingItem` KHỞI `Audit` ≠ `InternalAudit`) · **🟢 CONTRACT-ONLY** backend LIVE `api/imm16.py:246` + `services/imm16.py:1626` ⇒ **0 `.py`/reload/migrate** · Guard `TestMobileGetInternalAuditDetailContract` a..g + live (8 TC: a=parser+MỌI-$ref-resolve+4-schema-KHÔNG-orphan · b=path/opId 83+GET+tag compliance+∈`_MVP_READ_ENVELOPE`+∉`_MVP_LIST_ENVELOPE` · c=param `name` typed query required string · d=200 oneOf `[Envelope,Error]` 0-discr + 401/403 · e=Detail OPEN+required[name]+checklist_items/findings.$ref+allowed_transitions+can_operate/can_close boolean+lead_auditor_name+audit_type/status string-no-enum · f=2-child OPEN+biz-fields-⊇+result/category/severity/capa_status string-no-enum · g=guard-reconcile path 83/c5 72/parity 72/`_MVP_LIST_ENVELOPE` GIỮ 12 · live=field-domain-subset-anti-drift 3-schema + live-sig `get_audit(name)`) ⇒ `_EXPECTED_TEST_COUNT` **750→758** + `_GUARD_SUITE_EXPECTED[test_mobile_oas.py]` **750→758** + `_GUARD_SUITE_SUM` **893→901** + `_MOBILE_OAS_TOTAL` **919→927** + delta var `get_internal_audit_detail_delta=8` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **Alternatives loại:** CLOSED-4-schema key-parity-EXACT (misrepresent as_dict payload → codegen CRASH meta-key, phá precedent 6 *Detail — self-correct OPEN) / response-component (read-detail dùng inline oneOf mirror getAllocation) / ∈ `_MVP_LIST_ENVELOPE` (detail ≠ list) / status-line 404 (Decision-B trên HTTP-200) / hard-enum audit_type/status/child-Select (leading-blank "" hợp-lệ → reject-valid-row) / bỏ `findings[]` (as_dict LUÔN kèm). **Accepted.** |
| [`ADR-MOBILE-053.md`](./ADR-MOBILE-053.md) | ADR `getCommissioning` (`imm04.get_form_context`) curate vào OAS mirror (**CR-25 · MỞ NHÁNH IMM-04 F6-DETAIL "Tiếp nhận & Nghiệm thu hiện trường"** — bồi ĐÚNG 1 GET-detail path trả phiếu nghiệm thu hiện trường CHI TIẾT (header 44-field + `baseline_tests[]` + `commissioning_documents[]` + `lifecycle_events[]` child + `allowed_transitions[]` CTA server-driven); **DETAIL-sibling** của `listCommissioning` ADR-MOBILE-048 (CÙNG màn F6, CÙNG DocType `Asset Commissioning`, tag `commissioning`)) — **PATH ADD** CONTRACT-ONLY GROUNDED 1:1 handler LIVE `get_form_context(name)` @`api/imm04.py:19` (bare `@whitelist` → GET; guest dispatcher-403; `_handle(svc.get_form_context, name)` passthrough) + service @`services/imm04.py:796` (`doc = CommissioningRepo.get(name)`; `if not doc: nthrow(MSG.IMM04_NOT_FOUND)` @`:801` → `_handle`→`_err` **LỖI TRÊN HTTP-200** KHÔNG status-line 404; `has_permission read throw` @`:803`; `result = _serialize_commissioning(doc)` **dict CURATED** @`:728` [KHÔNG `as_dict()`] 44-field header @`:743-790` + 3 enrich display-name + `allowed_transitions` @`:807`) · **1 param** `name` `in:query` `required:true` `type:string` (RPC no-template) · **opId `getCommissioning` theo DOMAIN** (KHÔNG theo tên hàm `get_form_context` — parity `getRepairWorkOrder`/`getAllocation`/`getInternalAudit`) · **200 = inline `oneOf [CommissioningDetailEnvelope, Error]`** Decision-B route-by-VALUE `body.success` 0-discr (mirror `getAllocation` R42 — ∈ `_MVP_READ_ENVELOPE` KHÔNG response-component) · **⚠️ `CommissioningDetail` + 3 child = OPEN `additionalProperties:true`** (dù serialize CURATED, GIỮ OPEN theo **parity 8 `*Detail` hiện có** — SpareAllocationDetail/InternalAuditDetail/CalibrationDetail; tránh lone CLOSED-detail đòi field-exactness + Hyrum-safe additive; chống false-green bằng live-signature + no-orphan + header-sample guard) · **CHỈ envelope `CommissioningDetailEnvelope` = CLOSED** (`_ok` 2-key `{success.enum[true], data:$ref CommissioningDetail}`) · **5 schema:** `BaselineTestItem` (OPEN, 11 field @`Commissioning Checklist` — `is_critical`/`na_applicable` Check→integer enum[0,1], `expected_min`/`expected_max`/`measured_val` nullable number, `test_result`/`measurement_type` Select→string-no-enum) · `CommissioningDocumentItem` (OPEN, 9 field @`Commissioning Document Record` — `is_mandatory` Check→integer, `doc_type`/`status` Select→string-no-enum) · `CommissioningLifecycleEventItem` (OPEN, 8 field) · `CommissioningDetail` (OPEN, `required:[name]`) 44 header + `allowed_transitions[]` array string + 3 child `$ref` · `CommissioningDetailEnvelope` (CLOSED) · **⚠️ 4 doc-level Check `facility_checklist_pass`/`is_radiation_device`/`doa_incident`/`documents_incomplete` = `type:integer enum[0,1]`** (CR-01 family, KHÔNG boolean — né strict Dart/Kotlin deser CRASH); `is_locked` = Python bool THẬT (`docstatus==1`) → `boolean`; `current_user_roles` → `array<string>`; `risk_class` NĐ98 Select→string-no-enum (KHÁC `risk_classification`) · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden`) · tag `commissioning` · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_READ_ENVELOPE` (KHÔNG `_MVP_LIST_ENVELOPE` — **detail ≠ list** ⇒ GIỮ 12) · path/opId **83→84** + c5 **72→73** + parity **72→73** + `_EXPECTED` += `get_form_context → getCommissioning` · **🟢 CONTRACT-ONLY** backend LIVE `api/imm04.py:19` + `services/imm04.py:796` ⇒ **0 `.py`/reload/migrate** · Guard `TestMobileGetCommissioningDetail` 9 TC (path/opId + name-param + 200-oneOf + envelope-closed + detail+3-child-refs-resolve + check-flags-integer + path-count-delta-+1 + no-orphan + live-signature) ⇒ `_EXPECTED_TEST_COUNT` **758→767** + `_GUARD_SUITE_EXPECTED[test_mobile_oas.py]` **758→767** + `_GUARD_SUITE_SUM` **901→910** + `_MOBILE_OAS_TOTAL` **927→936** + delta var `getcommissioning_detail_delta=9` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **submit_baseline_checklist** (op WRITE còn lại F6) vẫn **PENDING** (curate vòng kế) · **Alternatives loại:** CLOSED-exact detail (lone outlier + field-exactness bảo trì cao — GIỮ OPEN parity) / response-component (read-detail dùng inline oneOf) / ∈ `_MVP_LIST_ENVELOPE` (detail ≠ list) / status-line 404 (Decision-B trên HTTP-200) / boolean cho Check flag (int-vs-bool crash) / opId theo tên hàm `getFormContext` (generic — dùng DOMAIN). **Accepted.** |
| [`ADR-MOBILE-054.md`](./ADR-MOBILE-054.md) | ADR `getDuePmSchedules` (`imm08.get_due_pm_schedules`) build + curate vào OAS mirror (**CR-28b · MỞ NHÁNH F8 "Nhắc việc" nửa-PM** — bồi ĐÚNG 1 GET-list-read path trả LỊCH bảo trì phòng ngừa (PM Schedule) sắp/quá hạn ≤ N ngày, **ĐÓNG nửa PM còn CHẾT** của màn "Nhắc việc" (nửa Hiệu chuẩn `getDueCalibrations` ADR-MOBILE-049 đã LIVE); **SIBLING** trực-tiếp `getDueCalibrations` (CÙNG màn F8, CÙNG shape KHÔNG-pagination `{items, threshold_days}`, `days_left` signed server-derived) NHƯNG **⚠️ endpoint MỚI có `.py` runtime** — KHÁC ADR-049 CONTRACT-ONLY) — **PATH+BE ADD** GROUNDED 1:1 handler MỚI `get_due_pm_schedules(days=30, limit=50)` @`api/imm08.py` (bare `@whitelist` → GET, DocPerm-governed ĐỐI XỨNG `get_due_calibrations` @`api/imm11.py:202` — KHÔNG cap-gate; `handle(svc.get_due_pm_schedules, int(days), int(limit))`) + service MỚI @`services/imm08.py` (`PMScheduleRepo.list(filters=[['status','=','Active'],['next_due_date','is','set'],['next_due_date','<=',add_days(today,days)]], fields=7, order_by='next_due_date asc', page_size=int(limit))` + enrich `asset_name`=`AssetRepo.get_value` + loop `days_left`=`date_diff(next_due_date,today)` signed; `return {'items':rows,'threshold_days':int(days)}`) · **⚠️ NGUỒN = `PM Schedule.next_due_date`** (KHÁC calib dùng `AC Asset.next_calibration_date`; CR-28b explicit — PM Schedule có `responsible_technician`/`alert_days_before` phục vụ nhắc việc) + **filter `status=='Active'`** (LOẠI Paused/Suspended) · **NULL-coerce guard** (`[next_due_date,'is','set']` BẮT BUỘC — mirror `get_due_calibrations`: bỏ ⇒ Frappe query-builder ép `ifnull(next_due_date,'0001-01-01')<=threshold` ⇒ lịch chưa-có-ngày LỌT top, lấp kín limit, đẩy lịch quá-hạn thật khỏi list) · **2 typed query-param inline** `days`(default 30)+`limit`(default 50) integer required:false (mirror CR-28a) · **200 = response-component `DuePmScheduleList` oneOf `[DuePmScheduleListEnvelope, Error]`** closed route-by-VALUE `body.success` (Decision-B 0-discr, ADR-MOBILE-001) · **3 schema RIÊNG closed** (`additionalProperties:false`): `DuePmScheduleListItem` **EXACT 9 field** = 7 `PMScheduleRepo.list` (`name`(req PK)/`asset_ref`/`pm_type`/`status`/`next_due_date:date`/`last_pm_date:date`/`responsible_technician`) + `asset_name` enrich + `days_left` (**⚠️ KHÁC `DueCalibrationListItem` 7 field — nửa PM có `asset_ref`+`asset_name` tường minh**), required=`[name]`, **⚠️ 0 prop Check int-0/1** (né CR-01) · `DuePmScheduleListPage` `{items:array<DuePmScheduleListItem>, threshold_days:integer}` (**⚠️ KHÔNG pagination** — GIỐNG `DueCalibrationListPage`, KHÁC `list_schedules` `{data,pagination}`; assert `'pagination'∉keys` chống copy-nhầm) · `DuePmScheduleListEnvelope` `{success.enum[true], data:$ref DuePmScheduleListPage}` · **`days_left` = signed integer NON-nullable** (âm=quá hạn; filter is-set bảo đảm `nd` LUÔN set ⇒ nhánh `else None` dead-branch ⇒ client dùng TRỰC TIẾP server SSoT overdue, KHÔNG re-derive client-clock) · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` guest dispatcher-403 KHÔNG cap-403-branch) · tag `pm` · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_LIST_ENVELOPE` · path/opId **84→85** + `_MVP_LIST_ENVELOPE` **12→13** + c5 **73→74** + parity **73→74** + `_EXPECTED` += `get_due_pm_schedules → getDuePmSchedules` (naming guard `DuePmSchedule*` ∩ `DueCalibration*`/`PmScheduleList*` == ∅) · **⚠️ endpoint MỚI `.py`** — BE TDD `TestDuePmSchedules` TC-DUE-PM-01..07 @`test_imm08.py` xanh (7 OK); Guard `TestMobileDuePmSchedulesContract` a..g (7 TC: a=path/opId 85+GET+opId+tag pm+∈`_MVP_BUSINESS_PATHS` · b=Item closed EXACT-9+`name`-req+0-boolean+`asset_ref`/`asset_name`∈props · c=**anti-copy** Page `{items,threshold_days}`+`'pagination'∉keys` · d=params 2 typed inline days/limit default 30/50 · e=200 response-component oneOf `[Envelope,Error]`+Envelope.data→DuePmScheduleListPage · f=3 schema closed+`_MVP_LIST_ENVELOPE` 13+live-sig `{days,limit}` · g=`days_left` integer NON-nullable+naming-guard 3 DuePmSchedule* disjoint DueCalibration*) ⇒ `_EXPECTED_TEST_COUNT` **767→774** + `_GUARD_SUITE_EXPECTED[test_mobile_oas.py]` **767→774** + `_GUARD_SUITE_SUM` **910→917** + `_MOBILE_OAS_TOTAL` **936→943** + delta `due_pm_schedules_list_delta=7` (transition-baseline giữ `pre_fc3_six==191`) · **🔴 reload gunicorn PENDING USER** (BE `.py` mới sau `--preload` — `bench run-tests` load fresh nên endpoint mới VẪN test xanh; HTTP-live chờ USER reload) · KHÔNG migrate (0 schema change) · **Alternatives loại:** nguồn `AC Asset.next_pm_date` (CR-28b chọn PM Schedule — có responsible_technician/alert_days_before) / rows-key `data`+pagination (mirror calib `items`+threshold_days) / bỏ filter `status==Active` (lọt Paused/Suspended) / bỏ `is set` guard (NULL-coerce lọt top) / `days_left` client-derive (server SSoT) / `rbac.require('pm.read')` (bare @whitelist DocPerm-governed đối xứng getDueCalibrations). **Accepted.** |
| [`ADR-MOBILE-055.md`](./ADR-MOBILE-055.md) | ADR `getCommissioning.allowed_transitions` **`array[string]` → `array[object]` `$ref CommissioningTransitionItem`** (**CR-25-FIX · P1 codegen-crash · SELF-CORRECTION SC#1 của ADR-MOBILE-053**) — **shape-hotfix** OAS mirror: IMM-04 emit `allowed_transitions` = `list[dict]{action,next_state,allowed_role}` @`_get_workflow_transitions` (`services/imm04.py:667-678`, nguồn `Workflow.transitions`) qua `get_form_context` @`:807` — **KHÁC 4 sibling `*Detail`** (Incident `imm12:778` / PM `imm08` / Repair `imm09` / Calibration `imm11` = `array[string]` mã trạng thái từ `_*_VALID_TRANSITIONS` map). `array[string]` cho object-payload = **strict Dart/Kotlin deser CRASH** (web FE ground-truth `types/imm04.ts:157 WorkflowTransition[]{action,next_state,allowed_role}` + `ApprovalPanel.vue:183 t.action`). **Fix 3 phần:** (1) YAML `CommissioningDetail.allowed_transitions.items` `{type:string}` → `{$ref CommissioningTransitionItem}` · (2) schema MỚI `CommissioningTransitionItem` **CLOSED** (`type:object`, `additionalProperties:false`, `required:[action,next_state,allowed_role]`, cả 3 prop `type:string` — builder emit ĐÚNG 3 key cố định, KHÁC child `doc.as_dict` OPEN) · (3) sửa **false-green** test-assertion `test_detail_child_refs_resolve` (`items.type=='string'` → `items.$ref` resolve + schema-closed-3-prop) + **LIVE-emit source-parity guard** (`inspect.getsource(get_form_context)` ⊇ `_get_workflow_transitions` ∧ source `_get_workflow_transitions` ⊇ keys `action`/`next_state`/`allowed_role`, chống drift contract↔live) + **regression 4 sibling VẪN `array[string]`** (chống over-correction đổi cả 5). **Supersede-in-part ADR-MOBILE-053** (CHỈ claim shape `allowed_transitions=array[string]`; Decision-B / opId-domain / Check→int / OPEN-vs-CLOSED GIỮ NGUYÊN). Core Doc SoT `docs/imm-04/05_API_Specification.md §21`. · **🟢 CONTRACT-ONLY pure-YAML + test, 0 `.py` / reload / migrate** — `_get_workflow_transitions` builder ĐÃ đúng LIVE (KHÔNG đụng runtime logic); count-guard GIỮ (assertion sửa IN-PLACE, 0 test-method mới) ⇒ `_EXPECTED_TEST_COUNT` / `_GUARD_SUITE_SUM` / `_MOBILE_OAS_TOTAL` **774 / 917 / 943 KHÔNG đổi**. **Accepted.** |
| [`ADR-MOBILE-046.md`](./ADR-MOBILE-046.md) | ADR `createTransfer` (`imm00.create_transfer` → `create_transfer_request`) curate vào OAS mirror (**CR-TRANSFER-CREATE-01 · transfer CREATE-action ĐẦU TIÊN** — bồi ĐÚNG 1 POST path TẠO phiếu yêu cầu điều chuyển thiết bị `∅ → Pending Approval` màn "Điều chuyển" luồng KHỞI TẠO; **HOÀN TẤT transfer write-action quartet** (`receiveTransfer` ADR-043 / `approveTransfer` ADR-044 / `rejectTransfer` ADR-045 / create); **FIRST transfer CREATE-action** — sinh record MỚI, **0 name-param đầu vào**) — **PATH ADD** CONTRACT-ONLY bồi 1 POST path GROUNDED 1:1 handler LIVE `create_transfer()` @`api/imm00.py:2124-2130` (`@whitelist(methods=["POST"])` @`:2123` no-allow_guest → guest dispatcher-403; **0-param** đọc `data = frappe.local.form_dict` loại cmd/doctype @`:2126`; `try _ok(create_transfer_request(data))` @`:2128` / `except frappe.exceptions.ValidationError → _err(str(e), 422)` @`:2130`) + service `create_transfer_request(data)` @`services/imm00.py:2568-2612` (`required = ("asset","transfer_type","to_department","reason")` @`:2574`; `frappe.throw 'Thiếu trường bắt buộc'` @`:2577`; asset∄ `frappe.throw` @`:2581`; **`from_*` auto-derive** `prev.get("location"/"department"/"custodian")` @`:2592-2594`; `doc.status=_TRANSFER_STATUS_PENDING` @`:2601`; return EXACT 2-key `{name,status}` @`:2612`, `status='Pending Approval'` @`:2561`) · **requestBody = 2 media-type** `application/json` + `application/x-www-form-urlencoded` CÙNG `$ref CreateTransferRequest` (Frappe RPC `form_dict` §9) · **200 = oneOf `[CreateTransferEnvelope, Error]`** closed route-by-VALUE `body.success` (Decision-B 0-discr) · **3 schema RIÊNG closed** (`additionalProperties:false`): `CreateTransferRequest` `req[asset,transfer_type,to_department,reason]` props EXACT **9** (4-req + 5-opt `{to_location,to_custodian,expected_return_date,notes,transfer_date}`) **⚠️ ĐIỂM KHÁC #1 — `from_location`/`from_department`/`from_custodian` KHÔNG khai** (SERVER auto-derive từ asset hiện tại @`:2592-2594`, `additionalProperties:false` chặn client gán vị trí nguồn giả) · `CreateTransferResponse` EXACT **2-prop** `req[name,status]` `{name:string, status:string **enum single-value `['Pending Approval']`** (GROUNDED verbatim hằng `_TRANSFER_STATUS_PENDING` @`:2561` — TC-e import LIVE assert equality)}` KHÔNG `from_*`/`to_*`/`approved_by`/`received_by`/`rejected_by` · `CreateTransferEnvelope` `req[success,data]` `success.enum[true]` `data=$ref CreateTransferResponse` **⚠️ TÊN RÚT-GỌN** (mirror `ReceiveTransferEnvelope` transfer-family, KHÔNG `...CreatedEnvelope`) · **⚠️ ĐIỂM KHÁC #2 — `∈ _MVP_CREATE_ENVELOPE`** (create-action mirror `createRepairWorkOrder`/`createCalibration`) KHÁC receive/approve/reject `∈ _MVP_ACTION_ENVELOPE` · **⚠️ ĐIỂM KHÁC #3 — 403 SINGLE `Forbidden` dispatcher-ONLY** (0 cap-403 — handler+service KHÔNG `rbac.require`; mirror `receiveTransfer` ADR-043, KHÁC `approveTransfer`/`rejectTransfer` cap-403 REACHABLE; op.description GHI RÕ dispatcher-only + `0 cap-403`) · **⚠️ ANTI-DRIFT `Error.http_status` = 422 ĐỒNG NHẤT** cho CẢ 2 nhánh `frappe.throw` (missing-required @`:2577` + asset∄ @`:2581`) → `ValidationError` → `_err(str(e), 422)` (KHÁC `getTransfer` 404) · slot `{200,401,403}` (`401 Unauthorized401`) · tag `asset` (parity transfer family) · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_CREATE_ENVELOPE` + `_REQBODY_PATHS` · path/opId **73→74** + c5 **62→63** + parity **62→63** + 3 schema mới (naming guard `CreateTransfer*` ∩ ∅; prefix ≠ `ReceiveTransfer`/`ApproveTransfer`/`RejectTransfer`/`Transfer`) + `_EXPECTED` += `create_transfer → createTransfer` · **🟢 CONTRACT-ONLY** `git diff HEAD` `api/imm00.py` (`create_transfer` 7 dòng) + `services/imm00.py` (`create_transfer_request` 45 dòng) = **byte-identical HEAD↔working** (AST-extract so-khớp byte) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · Guard `TestMobileCreateTransferContract` a..j (10 TC: a=path/opId 74+POST-only+tag asset · b=2-media-type json+form CÙNG $ref · c=Request closed req-EXACT-4+props-EXACT-9+`from_*`-∉-props+grounding-required-tuple-verbatim@source · d=Response EXACT 2-prop req[name,status]+SCHEMA-RIÊNG+anti-drift-KHÔNG-from_*/to_*/approver · e=status-enum-`['Pending Approval']`-GROUNDED-import-hằng-LIVE · f=200-oneOf-route-by-value+Envelope-closed · g=slot-`{200,401,403}`+403-SINGLE-Forbidden-dispatcher-ONLY+desc-chứa-'dispatcher-only'+'0 cap-403'+'missing-required'+'asset'+**Error.http_status ⊇ {422}** KHÔNG 404 · h=membership-`∈ _MVP_CREATE_ENVELOPE`-∧-`∉ _MVP_ACTION_ENVELOPE`+401/403-symmetry · i=naming-guard-3-CreateTransfer*-disjoint-Transfer*/Receive/Approve/RejectTransfer*/SendToLab*/CancelCalibration*+**handler-0-param-form_dict**+required-tuple-grounding · j=0-dangling+POST-only@source+registry-resolvable; **RED-before/GREEN-after** strip path → **102 cascade FAIL** (create path-reading TC + path-count 73≠74 + `_EXPECTED` registry mismatch) → restore → **Ran 704 OK**) ⇒ `_EXPECTED_TEST_COUNT` **694→704** + `_GUARD_SUITE_SUM` **837→847** + `_MOBILE_OAS_TOTAL` **863→873** + delta var `create_transfer_wire_delta=10` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **⚠️ secondary count-guard KHÔNG dùng token `, 73,`** (3 c5 62→63 + 1 parity + 4 backward-compat opId-set-minus [pmhist 72→73 / transfer(-new) 71→72 / dept 72→73 / loc 72→73] + 2 hardcoded `_EXPECTED_TEST_COUNT==694` [receivecert_j/cancelcal_j] + `op_id_unique` `len(set)==73` bare) ⇒ full-suite THẬT bắt sót · **Alternatives loại:** giữ dead-end / multipart / khai `from_*` (server-derive, lỗ bảo mật) / `∈ _MVP_ACTION_ENVELOPE` (create ≠ action) / Envelope `...CreatedEnvelope` (transfer-family cohesion) / 403 REACHABLE cap (KHÔNG rbac.require) / `Error.http_status` 404 (422-uniform) / status plain-string (enum grounded) / TC-i sig-parity (handler 0-param). **Accepted.** |
| [`ADR-MOBILE-045.md`](./ADR-MOBILE-045.md) | ADR `rejectTransfer` (`imm00.reject_transfer` → `reject_transfer_request`) curate vào OAS mirror (**CR-TRANSFER-REJECT-01 · transfer WRITE-action #3** — bồi ĐÚNG 1 POST path TỪ CHỐI phiếu điều chuyển `Pending Approval → Rejected` màn "Điều chuyển – Chờ duyệt"; **HOÀN TẤT cặp quyết định duyệt** (`approveTransfer` ADR-044 / reject); `receiveTransfer` (ADR-043) MỞ NHÁNH · `approveTransfer` #2 · reject #3; `createTransfer` FORWARD-RESERVE) — **PATH ADD** CONTRACT-ONLY bồi 1 POST path GROUNDED 1:1 handler LIVE `reject_transfer(name, rejection_reason="")` @`api/imm00.py:2591-2597` (`@whitelist(methods=["POST"])` @`:2591` no-allow_guest → guest dispatcher-403; `try _ok(reject_transfer_request(name, rejection_reason))` @`:2595` / `except frappe.exceptions.ValidationError → _err(str(e), 422)` @`:2596-2597`) + service `reject_transfer_request` @`services/imm00.py:2646-2674` (`rbac.require(_TRANSFER_APPROVE_CAP='commissioning.submit')` @`:2651`; `if not rejection_reason or len(rejection_reason.strip()) < 5: frappe.throw(...)` @`:2653-2654`; return EXACT 2-key `{name,status}` @`:2674`, `status=_TRANSFER_STATUS_REJECTED='Rejected'` @`:2563,2661`) · **requestBody = 2 media-type** `application/json` + `application/x-www-form-urlencoded` CÙNG `$ref RejectTransferRequest` (Frappe RPC `form_dict` §9) · **200 = oneOf `[RejectTransferEnvelope, Error]`** closed route-by-VALUE `body.success` (Decision-B 0-discr) · **3 schema RIÊNG closed** (`additionalProperties:false`): `RejectTransferRequest` `req[name,rejection_reason]` `{name:string, rejection_reason:string **minLength:5**}` **⚠️ ĐIỂM KHÁC #1 — FIRST transfer action có required text-body** (`rejection_reason` ∈ required — service ép `len(strip)<5→frappe.throw` @`:2653-2654`; minLength:5 TYPED-HINT, ngữ nghĩa RUNTIME strip-then-≥5 documented; KHÁC `approveTransfer` name-only & `receiveTransfer` `handover_notes` OPTIONAL) · `RejectTransferResponse` EXACT **2-prop** `req[name,status]` `{name:string, status:string **enum single-value `['Rejected']`** (GROUNDED verbatim hằng `_TRANSFER_STATUS_REJECTED` @`:2563` — TC-e import LIVE assert equality)}` **KHÔNG `rejected_by`/`rejection_reason`** (return @`:2674` CHỈ 2-key) · `RejectTransferEnvelope` `req[success,data]` `success.enum[true]` `data=$ref RejectTransferResponse` · **⚠️ ĐIỂM KHÁC #2 — NHÁNH 422 THỨ-3:** `Error.http_status` = **422 ĐỒNG NHẤT** cho CẢ 3 nhánh `frappe.throw` (not-found @`:2649` + rejection_reason thiếu/<5 @`:2653-2654` + wrong-status @`:2657-2658`) → `ValidationError` → `_err(str(e), 422)` (KHÁC `getTransfer` 404; op.description ghi rõ nhánh rejection_reason) · **⚠️ GIỐNG `approveTransfer` — 403 SINGLE `Forbidden` REACHABLE cap-branch:** `rbac.require('commissioning.submit')` @`:2651` raise `PermissionError` **NGOÀI** `except-ValidationError` @`:2596` → HTTP-403 status-line THẬT (403-slot VẪN SINGLE `Forbidden` reachability ≠ shape — note REACHABLE + cap ở op.description; KHÁC `receiveTransfer` dispatcher-only) · slot `{200,401,403}` (`401 Unauthorized401`) · tag `asset` (parity transfer family) · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_ACTION_ENVELOPE` + `_REQBODY_PATHS` · path/opId **72→73** + c5 **61→62** + parity **61→62** + 3 schema mới (naming guard `RejectTransfer*` ∩ ∅; prefix ≠ `ApproveTransfer`/`ReceiveTransfer`/`Transfer`) + `_EXPECTED` += `reject_transfer → rejectTransfer` · **🟢 CONTRACT-ONLY** `git diff HEAD` `api/imm00.py` (`reject_transfer`) + `services/imm00.py` (`reject_transfer_request`) = **byte-identical HEAD↔working** ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · Guard `TestMobileRejectTransferContract` a..j (10 TC: a=path/opId 73+POST-only+tag asset · b=2-media-type json+form CÙNG $ref · c=Request closed req[name,rejection_reason]+props-EXACT-2+rejection_reason.minLength==5+required-text-body · d=Response EXACT 2-prop req[name,status]+SCHEMA-RIÊNG+anti-drift-KHÔNG-rejected_by/rejection_reason · e=status-enum-`['Rejected']`-GROUNDED-import-hằng-LIVE · f=200-oneOf-route-by-value+Envelope-closed · g=slot-`{200,401,403}`+403-SINGLE-Forbidden-REACHABLE+desc-chứa-'REACHABLE'+cap+'rejection_reason'+**Error.http_status ⊇ {422}** KHÔNG 404 · h=membership+401/403-symmetry · i=naming-guard-3-RejectTransfer*-disjoint-Transfer*/ApproveTransfer*/ReceiveTransfer*/SendToLab*/CancelCalibration*+live-sig-`{name,rejection_reason}`-2-param · j=0-dangling+POST-only@source+registry-resolvable; **RED-before/GREEN-after** strip path → **97 cascade FAIL** (reject path-reading TC a/b/f/g/h/j + path-count) → restore → **Ran 694 OK**) ⇒ `_EXPECTED_TEST_COUNT` **684→694** + `_GUARD_SUITE_SUM` **827→837** + `_MOBILE_OAS_TOTAL` **853→863** + delta var `reject_transfer_wire_delta=10` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **⚠️ secondary count-guard KHÔNG dùng token `, 72,`** (3 c5 61→62 + 1 parity + 4 backward-compat opId-set-minus [pmhist 71→72 / transfer(-new) 70→71 / dept 71→72 / loc 71→72] + 2 hardcoded `_EXPECTED_TEST_COUNT==684` [receivecert_j/cancelcal_j] + `op_id_unique` `len(set)==72` bare + pushdata_g `len(_EXPECTED)` + test_05 opId-convention) ⇒ full-suite THẬT bắt sót · **Alternatives loại:** giữ dead-end / multipart / `rejection_reason` OPTIONAL (service ép required) / 403 dispatcher-only (CÓ rbac.require) / `Error.http_status` chỉ 2-nhánh (bỏ rejection_reason) / += `rejected_by`/`rejection_reason` (return @2674 CHỈ 2-key) / reuse `ApproveTransferResponse` (status enum khác) / `Error.http_status` 404 (frappe.throw→422 uniform) / status plain-string (enum grounded). **Accepted.** |
| [`ADR-MOBILE-044.md`](./ADR-MOBILE-044.md) | ADR `approveTransfer` (`imm00.approve_transfer` → `approve_transfer_request`) curate vào OAS mirror (**CR-TRANSFER-APPROVE-01 · transfer WRITE-action #2** — bồi ĐÚNG 1 POST path PHÊ DUYỆT phiếu điều chuyển `Pending Approval → Approved` (`transfer_asset` cập vị trí thiết bị NGAY) màn Điều chuyển feature-12 luồng DUYỆT; **write-action #2** sau `receiveTransfer` (ADR-043 MỞ NHÁNH); `rejectTransfer`/`createTransfer` FORWARD-RESERVE vòng Trục-B kế) — **PATH ADD** CONTRACT-ONLY bồi 1 POST path GROUNDED 1:1 handler LIVE `approve_transfer(name)` @`api/imm00.py:2582-2588` (`@whitelist(methods=["POST"])` @`:2582` no-allow_guest → guest dispatcher-403; `try _ok(approve_transfer_request(name))` @`:2586` / `except frappe.exceptions.ValidationError → _err(str(e), 422)` @`:2587-2588`) + service `approve_transfer_request` @`services/imm00.py:2615-2643` (`rbac.require(_TRANSFER_APPROVE_CAP='commissioning.submit')` @`:2620`; return EXACT 2-key `{name,status}` @`:2643`, `status=_TRANSFER_STATUS_APPROVED='Approved'` @`:2562,2627`) · **requestBody = 2 media-type** `application/json` + `application/x-www-form-urlencoded` CÙNG `$ref ApproveTransferRequest` (Frappe RPC `form_dict` §9) · **200 = oneOf `[ApproveTransferEnvelope, Error]`** closed route-by-VALUE `body.success` (Decision-B 0-discr) · **3 schema RIÊNG closed** (`additionalProperties:false`): `ApproveTransferRequest` `req[name]` `{name:string}` **0 optional** (KHÁC `receiveTransfer` `handover_notes`) · `ApproveTransferResponse` EXACT **2-prop** `req[name,status]` `{name:string, status:string **enum single-value `['Approved']`** (GROUNDED verbatim hằng `_TRANSFER_STATUS_APPROVED` @`:2562` — TC-e import LIVE assert equality)}` **KHÔNG `approved_by`** (return @`:2643` CHỈ 2-key — KHÁC `receiveTransfer` 3-prop `received_by`) · `ApproveTransferEnvelope` `req[success,data]` `success.enum[true]` `data=$ref ApproveTransferResponse` · **⚠️ ĐIỂM KHÁC #1 — 403 SINGLE `Forbidden` REACHABLE cap-branch:** `rbac.require('commissioning.submit')` @`:2620` raise `PermissionError` **NGOÀI** `except-ValidationError` @`:2587` → propagate dispatcher → HTTP-403 status-line THẬT (mirror `cancelCalibration` ADR-033, **KHÁC `receiveTransfer` dispatcher-only**; 403-slot VẪN SINGLE `Forbidden` reachability ≠ shape — note REACHABLE + cap ở op.description) · **⚠️ ĐIỂM KHÁC #2 — response 2-prop** (KHÔNG `approved_by`) · **⚠️ ANTI-DRIFT `Error.http_status` = 422 ĐỒNG NHẤT:** `frappe.throw` (phiếu∄ @`:2617-2618` + status≠Pending Approval @`:2623-2624`) → `ValidationError` → `_err(str(e), 422)` cho CẢ 2 nhánh (KHÁC `getTransfer` 404) · slot `{200,401,403}` (`401 Unauthorized401`) · tag `asset` (parity transfer family) · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_ACTION_ENVELOPE` + `_REQBODY_PATHS` · path/opId **71→72** + c5 **60→61** + parity **60→61** + 3 schema mới (naming guard `ApproveTransfer*` ∩ ∅; prefix ≠ `ReceiveTransfer`/`Transfer`) + `_EXPECTED` += `approve_transfer → approveTransfer` · **🟢 CONTRACT-ONLY** `git diff HEAD` `api/imm00.py` (`approve_transfer`) + `services/imm00.py` (`approve_transfer_request`) = **byte-identical HEAD↔working** ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · Guard `TestMobileApproveTransferContract` a..j (10 TC: a=path/opId 72+POST-only+tag asset · b=2-media-type json+form CÙNG $ref · c=Request closed req[name]+props-EXACT-1+0-optional · d=Response EXACT 2-prop req[name,status]+SCHEMA-RIÊNG+anti-drift-KHÔNG-approved_by · e=status-enum-`['Approved']`-GROUNDED-import-hằng-LIVE · f=200-oneOf-route-by-value+Envelope-closed · g=slot-`{200,401,403}`+403-SINGLE-Forbidden-REACHABLE+desc-chứa-'REACHABLE'+cap+**Error.http_status ⊇ {422}** KHÔNG 404 · h=membership+401/403-symmetry · i=naming-guard-3-ApproveTransfer*-disjoint-Transfer*/ReceiveTransfer*/SendToLab*/CancelCalibration*+live-sig-`{name}`-1-param · j=0-dangling+POST-only@source+registry-resolvable; **RED-before/GREEN-after** strip path → cascade FAIL → restore → **Ran 684 OK**) ⇒ `_EXPECTED_TEST_COUNT` **674→684** + `_GUARD_SUITE_SUM` **817→827** + `_MOBILE_OAS_TOTAL` **843→853** + delta var `approve_transfer_wire_delta=10` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **Alternatives loại:** giữ dead-end / multipart / 403 dispatcher-only (CÓ rbac.require) / 403 dual-shape hay cap-phủ-200 (PermissionError status-line) / reuse `ReceiveTransferResponse` (2-key≠3-key) / += `approved_by` (return @2643 CHỈ 2-key) / `Error.http_status` 404 (frappe.throw→422 uniform) / status plain-string (enum grounded) / += `handover_notes` (signature 1-param). **Accepted.** |
| [`ADR-MOBILE-043.md`](./ADR-MOBILE-043.md) | ADR `receiveTransfer` (`imm00.receive_transfer` → `confirm_receipt`) curate vào OAS mirror (**CR-TRANSFER-RECV-01 · transfer WRITE-action ĐẦU TIÊN** — bồi ĐÚNG 1 POST path xác-nhận-tiếp-nhận thiết bị điều chuyển `Approved → Received` màn Điều chuyển feature-12 luồng NHẬN scan-confirm; **MỞ NHÁNH transfer write-action** sau READ `getTransfer`/`listTransfers` đã curate (ADR-021 TRANSFER-READ-WIRE); `approveTransfer`/`rejectTransfer`/`createTransfer` FORWARD-RESERVE vòng Trục-B kế mirror `sendToLab` R10) — **PATH ADD** CONTRACT-ONLY bồi 1 POST path GROUNDED 1:1 handler LIVE `receive_transfer(name, handover_notes="")` @`api/imm00.py:2600-2606` (`@whitelist(methods=["POST"])` @`:2600` no-allow_guest → guest dispatcher-403; **KHÔNG `rbac.require` in-handler** (⚠️ AMENDED 2026-07-15 — cap-gate in-service: `confirm_receipt` `rbac.require(commissioning.write)` @`services/imm00.py:2768` ⇒ cap-403 REACHABLE, xem ĐIỂM KHÁC #1); `try _ok(confirm_receipt(name, handover_notes))` @`:2604` / `except frappe.exceptions.ValidationError → _err(str(e), 422)` @`:2605-2606`) + service `confirm_receipt` @`services/imm00.py:2677-2708` (return EXACT 3-key `{name,status,received_by}` @`:2708`, `status=_TRANSFER_STATUS_RECEIVED='Received'` @`:2564,2687`) · **requestBody = 2 media-type** `application/json` + `application/x-www-form-urlencoded` CÙNG `$ref ReceiveTransferRequest` (Frappe RPC `form_dict` §9, mirror `sendToLab`/`cancelCalibration`) · **200 = oneOf `[ReceiveTransferEnvelope, Error]`** closed route-by-VALUE `body.success` (Decision-B 0-discr) · **3 schema RIÊNG closed** (`additionalProperties:false`): `ReceiveTransferRequest` `req[name]` `{name:string, handover_notes:string **NON-nullable optional** (default `""` empty-string @`:2601` KHÔNG None — service patch khi truthy @`:2691-2692`; KHÁC `sendToLab` optionals default None → nullable)}` · `ReceiveTransferResponse` EXACT 3-prop `req[name,status,received_by]` `{name:string, status:string **enum single-value `['Received']`** (GROUNDED verbatim hằng `_TRANSFER_STATUS_RECEIVED` @`:2564` — TC-e import LIVE assert equality, anti-bịa), received_by:string (session.user @`:2708`)}` · `ReceiveTransferEnvelope` (tên RÚT-GỌN mirror `AttachIncidentPhotoEnvelope`) `req[success,data]` `success.enum[true]` `data=$ref ReceiveTransferResponse` · **⚠️ ĐIỂM KHÁC #1 — 403 SINGLE `Forbidden`** *[⚠️ AMENDED 2026-07-15 — CR-WF-00-TRANSFER-AUTHZ: 403 nay **REACHABLE cap-branch `commissioning.write`** — `confirm_receipt` `rbac.require` @`services/imm00.py:2768` propagate NGOÀI `except-ValidationError` @`api/imm00.py:2645-2648` → cap-403 status-line THẬT; 403-slot VẪN SINGLE `Forbidden` (schema BẤT BIẾN — reachability ≠ shape); mirror `approveTransfer`/`rejectTransfer`/`sendToLab` cap-403 REACHABLE (KHÁC cap: receive `commissioning.write` least-privilege vs approve/reject `commissioning.submit`); xem [ADR-043 §AMENDMENT](./ADR-MOBILE-043.md). Đoạn dưới = record point-in-time 2026-07-14]* — [record gốc] `receive_transfer` KHÔNG `rbac.require` in-handler ⇒ 0 in-handler cap-403 ⇒ 403-slot chỉ dispatcher-403 (mirror `attachIncidentPhoto` ADR-027, KHÁC `sendToLab`/`cancelCalibration` cap-403, KHÁC `reportIncident` DUAL-403) · **⚠️ ĐIỂM KHÁC #2 — ANTI-DRIFT `Error.http_status` = 422 ĐỒNG NHẤT:** `confirm_receipt` dùng `frappe.throw` (phiếu∄ @`:2680` + status≠Approved @`:2684`) → `ValidationError` → handler `_err(str(e), 422)` cho CẢ 2 nhánh ⇒ 422-uniform (KHÁC `getTransfer` 404 tường minh — `receive_transfer` KHÔNG phát 404; description GHI RÕ) · slot `{200,401,403}` (`401 Unauthorized401`) · tag `asset` (mirror `getTransfer`/`listTransfers`) · ∈ `_MVP_BUSINESS_PATHS` + `_MVP_ACTION_ENVELOPE` + `_REQBODY_PATHS` · path/opId **70→71** + c5 **59→60** + 3 schema mới (naming guard `ReceiveTransfer*` ∩ ∅; prefix ≠ `ReceiveCertificate`/`Transfer`) + `_EXPECTED` += `receive_transfer → receiveTransfer` · **🟢 CONTRACT-ONLY** `git diff HEAD` `api/imm00.py` (`receive_transfer`) + `services/imm00.py` (`confirm_receipt`) = **byte-identical HEAD↔working** (2 vùng TRỐNG round NÀY) ⇒ KHÔNG reload/migrate ([AUTO], KHÔNG HARD-STOP USER) · Guard `TestMobileReceiveTransferContract` a..j (10 TC: a=path/opId 71+POST-only+tag asset · b=2-media-type json+form CÙNG $ref · c=Request closed req[name]+handover_notes-NON-nullable-optional · d=Response 3-prop VERBATIM req-cả-3+SCHEMA-RIÊNG+anti-drift · e=status-enum-`['Received']`-GROUNDED-import-hằng-LIVE · f=200-oneOf-route-by-value+Envelope-closed · g=slot-`{200,401,403}`+403-SINGLE-Forbidden+**Error.http_status ⊇ {422}** (KHÔNG 404) · h=membership+401/403-symmetry · i=naming-guard-3-ReceiveTransfer*-disjoint-Transfer*/ReceiveCertificate*/SendToLab*+live-sig-`{name,handover_notes}`+`handover_notes.default==''` · j=0-dangling+POST-only@source+registry-resolvable; **RED-before/GREEN-after** strip path → 93 cascade FAIL (6 receiveTransfer TC + path-count) → restore → **Ran 674 OK**) ⇒ `_EXPECTED_TEST_COUNT` **664→674** + `_GUARD_SUITE_SUM` **807→817** + `_MOBILE_OAS_TOTAL` **833→843** + delta var `receive_transfer_wire_delta=10` (transition-baseline doc_09 giữ `pre_fc3_six==191`) · **⚠️ 10 secondary count-guard** (3 c5 59→60 + 1 parity `_PARITY_BUSINESS_PATHS` 59→60 + 4 backward-compat opId-set-minus [transfer_p 68→69 / pmhist_j·dept_h·loc_h 69→70] + 2 hardcoded `_EXPECTED_TEST_COUNT==664` [receivecert_j/cancelcal_j] + `op_id_unique` `len(set)==70`) KHÔNG dùng token `, 70,` ⇒ full-suite THẬT bắt sót · **Alternatives loại:** giữ dead-end / multipart (action KHÔNG upload) / 403 dual-shape hay cap-phủ-200 (0 rbac.require) / `Error.http_status` 404 (source dùng `frappe.throw`→422 uniform) / reuse `SendToLabResponse` (field#3 received_by≠sent_date) / status plain-string (acceptance enum `['Received']` grounded) / `handover_notes` nullable (default `''` never null) / `handover_notes` required (default `''` optional) / 200 single-shape (giấu `_err`). **Accepted.** |
| [`ADR-MOBILE-024.md`](./ADR-MOBILE-024.md) | ADR Account/Profile `getMyProfile` + `updateMyProfile` + `changeMyPassword` (**ACCOUNT-PROFILE-WIRE — màn "Tài khoản" mobile.v1 / IMM-00 auth-profile**; curate 3 endpoint self-service hồ sơ + đổi mật khẩu vào OAS mirror, **đóng CR-20**) — CONTRACT-ONLY curate 3 path GROUNDED 1:1 THIN wrapper LIVE `api/mobile/v1/profile.py:78-181` (DELEGATE `auth.get_user_profile` / `auth.update_my_profile` allowlist `_SELF_EDITABLE={full_name,phone}`@`auth.py:24` / `user.change_my_password`) · **`getMyProfile`** GET 0-param (user ÉP `session.user`) live-sig `{}` · **`updateMyProfile`** POST `requestBody UpdateMyProfileRequest` CHỈ `{full_name,phone}` (KHÔNG `department` — READ-ONLY) live-sig `{full_name,phone}` · **`changeMyPassword`** POST `requestBody ChangeMyPasswordRequest` `required[old_password,new_password]` `new_password.minLength:8` live-sig `{old_password,new_password}` · **6 schema RIÊNG** (all `additionalProperties:false`): `MyProfile` EXACT 7 prop VERBATIM `_build_my_profile`@`:78-86` `{full_name,email,phone,roles[],role_labels[],department,department_name}` `required`=cả 7 (`phone`/`department`/`department_name` nullable) + `MyProfileEnvelope` (dùng CHUNG getMyProfile+updateMyProfile) + `ChangeMyPasswordData.reauth_required` GENUINE `boolean` (KHÔNG int-enum; `logout_all_sessions` False ⇒ sid giữ) + `ChangeMyPasswordEnvelope` + `UpdateMyProfileRequest` + `ChangeMyPasswordRequest` · mỗi path **200 = oneOf [<Envelope> \| Error]** closed route-by-VALUE `body.success` (Decision-B ADR-MOBILE-001, 0 discriminator; lỗi = HTTP-200 + Error `code:VALIDATION`+`fields{old_password\|new_password\|full_name\|phone}`, `ErrorCode` KHÔNG có `INVALID/WEAK_PASSWORD`) · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` SINGLE-SHAPE dispatcher-403 guest/no-token; in-handler cap-403 KHÔNG áp — bearer-gated) · **∉ `_MVP_BUSINESS_PATHS`/c5** (mirror device-token self-service; `_ACCOUNT_PATHS` bucket → `_PATHS_REQUIRE_401/403` symmetry +3, guard `c5==_MVP_BUSINESS_PATHS` GIỮ 45) · ∈ `_RATE_LIMIT_SOURCE_MAP` no-`@rate_limit` (∉ `_PATHS_REQUIRE_429`) · path/opId **53→56** + 6 schema tái-dùng `Unauthorized401`/`Forbidden`/`Error` (0 orphan, 0 dangling) · **🟢 CONTRACT-ONLY** `git diff` `api/mobile/v1/profile.py`+`__init__.py` = TRỐNG (BE LIVE vòng trước) ⇒ KHÔNG reload/migrate · Guard `TestMobileAccountProfileContract` a..i (9 TC) ⇒ `_EXPECTED_TEST_COUNT` 503→512 + `_GUARD_SUITE_SUM` 646→655 + `_MOBILE_OAS_TOTAL` 672→681 + `account_profile_delta=9` (`pre_fc3_six==191`) · **Alternatives loại:** không-curate (CR-20 treo, client hardcode) / vào `_MVP_BUSINESS_PATHS`+c5 (vỡ guard 45) / `reauth_required` int-enum (KHÔNG Check-field) / request khai `department` (delegate DROP) / SINGLE-200 (giấu nhánh Error) / raise→HTTP-4xx (vỡ Decision-B). **Accepted.** |
| [`ADR-MOBILE-022.md`](./ADR-MOBILE-022.md) | ADR `getAssetRepairHistory` (**FLOW-2 DEVICE-PROFILE / IMM-09** — bồi 1 path GET lịch-sử SỬA-CHỮA CM của asset vào tab "Lịch sử sửa chữa" màn hồ-sơ-thiết-bị sau quét QR; ĐÓNG bộ-ba device-profile cùng `getAssetIncidentHistory` (sự-cố) + `getAssetTimeline` (vòng-đời)) — CONTRACT-ONLY bồi 1 path GET GROUNDED 1:1 endpoint LIVE `imm09.get_asset_repair_history:127` → `svc.get_asset_history` (`services/imm09.py:1212-1220` `RepairRepo.list` filters`{asset_ref,docstatus:1}` 9-field order_by `open_datetime desc` → `{asset_ref, history}`) · **MIRROR `getAssetIncidentHistory` (ADR R28) NHƯNG 3 KHÁC-BIỆT có-chủ-đích:** (1) rows-key **`history`** (KHÔNG `items`) + asset-key **`asset_ref`** (KHÔNG `asset`) — khớp svc return shape; (2) **200 = SINGLE-shape `AssetRepairHistoryEnvelope` KHÔNG `oneOf [Env,Error]`** vì handler `handle(svc.get_asset_history)` + svc **0 raise `ServiceError`** ⇒ `handle` LUÔN `_ok`, 0 nhánh `_err` in-handler (mirror `listTransfers`/`pingSession`, KHÁC incident `oneOf`); (3) **1 Check field** `sla_breached` → `integer` (Frappe Check 0/1, KHÔNG `boolean`/`enum[0,1]` — né int-vs-bool trap Open#1; incident có 0 Check) · `AssetRepairHistoryItem` closed (`additionalProperties:false`) EXACT 9 prop `{name,repair_type,priority,open_datetime,completion_datetime,mttr_hours,sla_breached,root_cause_category,repair_summary}` `required[name]` grounded `RepairRepo.list fields`@`:1215-1216` (`mttr_hours` `number` nullable; `open/completion_datetime` `string` KHÔNG `format:date-time` — Frappe naive `'yyyy-MM-dd HH:mm:ss'`) · 2 param `asset_ref` (query req string) + `limit` (query opt integer default 10; signature `limit="10"` str → handler ép `int()`) live-sig parity `inspect.signature(imm09.get_asset_repair_history)=={asset_ref,limit}` · `history[]` RỖNG hợp lệ asset chưa sửa → **KHÔNG 404** · slot `{200,401,403}` (`401 Unauthorized401` + `403 Forbidden` SINGLE-SHAPE — bare `@whitelist` `api/imm09.py:126` KHÔNG `allow_guest` ⇒ guest dispatcher-403) · path/opId **51→52** + 2 schema RIÊNG (`AssetRepairHistory{Envelope,Item}`) tái-dùng `Unauthorized401`/`Forbidden` (0 orphan, 0 dangling) · ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry +1) · **🟢 CONTRACT-ONLY** `git diff` api/imm09.py + services/imm09.py phần `get_asset_repair_history`/`get_asset_history` = TRỐNG (BE LIVE) ⇒ KHÔNG reload/migrate, `[AUTO]` thật · `test_oas_d12/d15/d17` UNCHANGED (pure mobile-yaml, KHÔNG đụng `generate_spec`) · Guard `TestMobileGetAssetRepairHistoryContract` a..i (9 TC) ⇒ `_EXPECTED_TEST_COUNT` 483→492 · **Alternatives loại:** reuse `AssetIncidentHistoryEnvelope` (sai rows/asset-key + sai oneOf-shape) / `oneOf [Env,Error]` (sai — handler 0 `_err`) / `boolean` cho `sla_breached` (strict-codegen Dart/Kotlin deser CRASH 0/1). **Accepted.** |
| [`ADR-MOBILE-056.md`](./ADR-MOBILE-056.md) | ADR `getPmDashboardStats` (`imm08.get_pm_dashboard_stats`) curate vào OAS mirror (**CR-31a · MỞ NHÁNH Dashboard KPI R1 (IMM-07/IMM-08)** — bồi ĐÚNG 1 GET-read path trả BỘ CHỈ SỐ PM (`kpis` 7-key compliance + `trend_6months` 6 phần tử) cho màn "Bảng chỉ số PM", **endpoint ĐẦU TIÊN surface Dashboard-KPI Trục B**; `getCalibrationKpis`/`getRepairKpis` forward-reserve vòng kế) — **CONTRACT-ONLY** GROUNDED 1:1 handler LIVE bare `@whitelist()` `get_pm_dashboard_stats(year=None, month=None)` @`api/imm08.py:163-168` (GET; guest dispatcher-403; `handle(svc.get_dashboard_stats, year=int(year) if year else today.year, month=int(month) if month else today.month)`) + service @`services/imm08.py:1210-1293` (return VERBATIM `{kpis:{7-key}, trend_6months:[6×{month,total,on_time,rate}]}` @`:1279-1293`) · **200 = inline `oneOf [PmDashboardStatsEnvelope, Error]`** Decision-B route-by-VALUE `body.success` 0-discr (∈ `_MVP_READ_ENVELOPE` mirror `getAssetKpi`, KHÔNG response-component, **KHÔNG `_MVP_LIST_ENVELOPE`** — dashboard read ≠ list ⇒ GIỮ 13) · **4 schema CLOSED** (`additionalProperties:false`): `PmDashboardKpis` **EXACT 7 prop** VERBATIM @`:1280-1291` — `compliance_rate_pct`(`number` **`nullable:true` ∉ required** — null khi `total_scheduled==0` INV-PM-KPI-3, **precedent 1:1 `AssetKpi.pm_compliance_pct`**) + 5 `integer`(`total_scheduled`/`completed_on_time`/`overdue_in_month`/`pending_in_month`/`overdue` GLOBAL RC-10) + `avg_days_late`(`number` ∈ required, else=0.0), `required`=6-key ; `PmDashboardTrendItem` EXACT 4 prop `{month:string,total:integer,on_time:integer,rate:number}` req cả 4 ; `PmDashboardStats` `{kpis:$ref, trend_6months:array<$ref>}` req cả 2 ; `PmDashboardStatsEnvelope` `{success.enum[true], data:$ref PmDashboardStats}` req[success,data] · **2 typed query-param** `year`+`month` `in:query` `type:integer` `required:false` **KHÔNG khai `default:` YAML** (backend default `nowdate()` ĐỘNG @`:165`, KHÔNG hằng — static default → client drift stale) · slot `{200,401,403}` **403 SINGLE `Forbidden` dispatcher-ONLY** (bare `@whitelist` 0 `rbac.require` — 0 cap-403) + `401 Unauthorized401` · **tag `pm`** (KHÔNG `dashboard` — module-domain taxonomy, ADR-054 §2(b) precedent read-IMM08=`pm`; forward-reserve siblings đơn-module ⇒ màn dashboard = FE-composition) · opId `getPmDashboardStats` DOMAIN · ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry tự +1) · **🟢 CONTRACT-ONLY** backend LIVE ⇒ **0 `.py`/reload/migrate** · Guard `TestMobileGetPmDashboardStatsContract` a..i (9 TC: a=path/opId 87+GET+tag pm+∈`_MVP_READ_ENVELOPE`+∉`_MVP_LIST_ENVELOPE` · b=2 typed param year/month integer required:false KHÔNG default-key · c=Kpis CLOSED EXACT-7 + required==6 + 5 integer/2 number · d=`compliance_rate_pct` nullable ∉required ∧ `avg_days_late` NON-null ∈required ∧ 0 Check · e=TrendItem CLOSED 4-prop req-cả-4 · f=Stats CLOSED req[kpis,trend_6months]+child-$ref · g=200 oneOf `[Envelope,Error]` 0-discr + Envelope closed + slot{200,401,403} · h=4-schema-KHÔNG-orphan+0-dangling+count-reconcile · i=naming-guard `PmDashboard*` disjoint `PmWorkOrder*`/`PmSchedule*`/`DuePmSchedule*` + live-sig `{year,month}`) ⇒ `_EXPECTED_TEST_COUNT` **783→792** + `_GUARD_SUITE_SUM` **926→935** + `_MOBILE_OAS_TOTAL` **952→961** + path/opId **86→87** + c5/`_PARITY_BUSINESS_PATHS` **75→76** + `_MVP_READ_ENVELOPE` +1 (`_MVP_LIST_ENVELOPE` GIỮ 13) — ⚠️ baseline grep-verify @source TRƯỚC bump (đa-phiên race, CR-25c in-flight) · **Alternatives loại:** tag `dashboard` (fork taxonomy — siblings đơn-module + FE-composition) / `compliance_rate_pct` NON-null (null-khi-mẫu-0 → codegen CRASH, phá precedent AssetKpi) / `avg_days_late` nullable (else=0.0) / khai `default:` YAML (default động nowdate) / response-component (read flat-object dùng inline oneOf) / ∈ `_MVP_LIST_ENVELOPE` (dashboard ≠ list) / status-line 404 (Decision-B HTTP-200) / 403 dual-shape (0 rbac.require) / bịa/±field vs return-dict (VERBATIM 0-invention). **Accepted.** |
| [`ADR-MOBILE-058.md`](./ADR-MOBILE-058.md) | ADR `getRepairKpis` (`imm09.get_repair_kpis`) curate vào OAS mirror (**CR-31c · Dashboard KPI R1 (IMM-09) — HOÀN-TẤT-TRIAD** — bồi ĐÚNG 1 GET-read path trả BỘ CHỈ SỐ SỬA CHỮA (CM) phạm-vi THÁNG (`kpis` **5-key** + `root_cause_breakdown[]`) cho màn "Bảng chỉ số sửa chữa (CM)", **endpoint THỨ BA & CUỐI bộ-ba** Dashboard-KPI Trục B sau `getPmDashboardStats`/ADR-056 + `getCalibrationKpis`/ADR-057) — **CONTRACT-ONLY** GROUNDED 1:1 handler LIVE bare `@whitelist()` `get_repair_kpis(year: str = "", month: str = "")` @`api/imm09.py:166-171` (GET; guest dispatcher-403; `today = getdate(nowdate())`; `handle(svc.get_kpis, int(year) if year else today.year, int(month) if month else today.month)`) + service `get_kpis(year, month)` @`services/imm09.py:1687-1725` (return VERBATIM `{kpis:{5-key}, root_cause_breakdown:[{category,count}]}` @`:1713-1725`) · **200 = inline `oneOf [RepairKpisEnvelope, Error]`** Decision-B route-by-VALUE `body.success` 0-discr (∈ `_MVP_READ_ENVELOPE` mirror `getPmDashboardStats`/`getCalibrationKpis`, KHÔNG response-component, **KHÔNG `_MVP_LIST_ENVELOPE`** — dashboard read ≠ list ⇒ GIỮ 13) · **4 schema CLOSED** (`additionalProperties:false`): `RepairKpis` **EXACT 5 prop** VERBATIM @`:1715-1719` — `total_completed`/`repeat_failure_count`/`open_wos` `integer` + `mttr_avg_hours`/`sla_compliance_pct`(`number` **NON-nullable** — else=0 @`:1698-1700`, **MIRROR `CalibrationKpis.pass_rate_pct`, ĐỐI-NGHỊCH `PmDashboardKpis.compliance_rate_pct` nullable**), `required`=**CẢ 5** ; `RepairRootCauseItem` (**schema MỚI** — KHÔNG có ở Pm/Cal) `{category:string, count:integer}` req cả 2 @`:1722` ; `RepairKpisData` `{kpis:$ref, root_cause_breakdown:array<$ref RepairRootCauseItem>}` `required:[kpis, root_cause_breakdown]` — **HÌNH DẠNG RIÊNG: KHÔNG `trend_6months` (KHÁC Pm) + CÓ `root_cause_breakdown` (KHÁC Cal single-kpis)** ; `RepairKpisEnvelope` `{success.enum[true], data:$ref RepairKpisData}` req[success,data] · **2 typed query-param** `year`+`month` `in:query` **`type:string`** `required:false` **KHÔNG khai `default:` YAML** (⚠️ signature `year: str = ""` @`:167` — **type `string` KHÁC `getCalibrationKpis` `integer`** vì imm11 `year=None`; backend default `getdate(nowdate())` ĐỘNG @`:168`) · slot `{200,401,403}` **403 SINGLE `Forbidden` dispatcher-ONLY** (bare `@whitelist` 0 `rbac.require`) + `401 Unauthorized401` · **tag `work-order`** (KHÔNG `dashboard` — module-domain taxonomy, REUSE op-IMM09; siblings đơn-module ⇒ màn dashboard = FE-composition, ADR-058 §2(c)) · opId `getRepairKpis` DOMAIN · ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry tự +1) · **🟢 CONTRACT-ONLY** backend LIVE ⇒ **0 `.py`/reload/migrate** · Guard `TestMobileGetRepairKpisContract` a..j (**10 TC** — +1 vs Cal cho schema MỚI `RepairRootCauseItem` + array shape: a=path/opId 89+GET+opId · b=tag work-order+∉`_MVP_LIST_ENVELOPE` · c=2 typed param year/month **STRING** required:false KHÔNG default-key + live-sig `{year,month}` default `""` (anti-drift vs Cal `None`) · d=200 oneOf `[Envelope,Error]` 0-discr + closed + success disjoint · e=Kpis CLOSED EXACT-5 + required==5 + 3 integer/2 number NON-null + 0 nullable/Check · f=**RepairRootCauseItem CLOSED `{category,count}` req cả 2** · g=Data CLOSED req[kpis,root_cause_breakdown] + `trend_6months` ABSENT (anti-drift vs Pm) + `root_cause_breakdown` array<$ref> PRESENT (điểm KHÁC vs Cal) · h=Envelope CLOSED {success:[true],data} · i=∈ symmetry-set + slot{200,401,403} · j=naming-guard `RepairKpis*` ĐÚNG-3 + `RepairRootCauseItem` riêng disjoint `RepairWorkOrder*`/`RepairAction*` + 0 dangling) ⇒ `_EXPECTED_TEST_COUNT` **801→811** + `_GUARD_SUITE_SUM` **944→954** + `_MOBILE_OAS_TOTAL` **970→980** + path/opId **88→89** + c5/`_PARITY_BUSINESS_PATHS` **77→78** + `_MVP_READ_ENVELOPE` +1 (`_MVP_LIST_ENVELOPE` GIỮ 13) — ⚠️ baseline grep-verify @source TRƯỚC bump (đa-phiên race) · **Alternatives loại:** thêm `trend_6months` (service KHÔNG trả — bịa field) / bỏ `root_cause_breakdown` (service TRẢ — mất field điểm KHÁC cốt-lõi) / 2-flag nullable (else=0 → NON-null, mirror Cal) / `year`/`month` `integer` (chữ-ký `str` — type phải phản-ánh source) / `root_cause_breakdown` OPEN-MAP (service trả array KHÔNG dict) / tag `dashboard` (fork taxonomy) / inline bỏ `RepairKpisData` (phá đối-xứng 3-tầng) / khai `default:` YAML (default động) / status-line 404 (Decision-B) / 403 dual-shape (0 rbac.require) / bịa/±field (VERBATIM 0-invention). **HOÀN-TẤT-TRIAD Dashboard-KPI. Accepted.** |
| [`ADR-MOBILE-057.md`](./ADR-MOBILE-057.md) | ADR `getCalibrationKpis` (`imm11.get_calibration_kpis`) curate vào OAS mirror (**CR-31b · Dashboard KPI R1 (IMM-11)** — bồi ĐÚNG 1 GET-read path trả BỘ CHỈ SỐ HIỆU CHUẨN phạm-vi THÁNG (`kpis` **6-key**) cho màn "Bảng chỉ số hiệu chuẩn", **SIBLING của `getPmDashboardStats`/ADR-056** trong surface Dashboard-KPI Trục B; `getRepairKpis` forward-reserve) — **CONTRACT-ONLY** GROUNDED 1:1 handler LIVE bare `@whitelist()` `get_calibration_kpis(year=None, month=None)` @`api/imm11.py:146-153` (GET; guest dispatcher-403; `now = datetime.date.today()`; `handle(svc.get_kpis, int(year) if year else now.year, int(month) if month else now.month)`) + service `get_kpis(year, month)` @`services/imm11.py:1171-1201` (return VERBATIM `{kpis:{6-key}}` @`:1192-1201` — **SINGLE khối `kpis`, KHÔNG `trend_6months`, KHÔNG `period`**) · **200 = inline `oneOf [CalibrationKpisEnvelope, Error]`** Decision-B route-by-VALUE `body.success` 0-discr (∈ `_MVP_READ_ENVELOPE` mirror `getPmDashboardStats`/`getAssetKpi`, KHÔNG response-component, **KHÔNG `_MVP_LIST_ENVELOPE`** — dashboard read ≠ list ⇒ GIỮ 13) · **3 schema CLOSED** (`additionalProperties:false`): `CalibrationKpis` **EXACT 6 prop** VERBATIM @`:1194-1199` — `total_this_month`/`completed`/`failed`/`overdue_assets`/`due_soon_assets` `integer` (2 field cuối = **COUNT distinct-asset** `len(_overdue/_due_soon_asset_ids)` @`:1188-1189`, KHÔNG list) + `pass_rate_pct`(`number` **NON-nullable** — else=0.0 @`:1190`, **ĐỐI-NGHỊCH `PmDashboardKpis.compliance_rate_pct` nullable**), `required`=**CẢ 6** (0 field nullable) ; `CalibrationKpisData` `{kpis:$ref}` `required:[kpis]` **KHÔNG có `trend_6months`** (điểm KHÁC cốt-lõi vs `PmDashboardStats`) ; `CalibrationKpisEnvelope` `{success.enum[true], data:$ref CalibrationKpisData}` req[success,data] · **2 typed query-param** `year`+`month` `in:query` `type:integer` `required:false` **KHÔNG khai `default:` YAML** (backend default `datetime.date.today()` ĐỘNG @`:148`, KHÔNG hằng) · slot `{200,401,403}` **403 SINGLE `Forbidden` dispatcher-ONLY** (bare `@whitelist` 0 `rbac.require`) + `401 Unauthorized401` · **tag `calibration`** (KHÔNG `dashboard` — module-domain taxonomy, REUSE read-IMM11; forward-reserve siblings đơn-module ⇒ màn dashboard = FE-composition, ADR-057 §2(c)) · opId `getCalibrationKpis` DOMAIN · ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry tự +1) · **🟢 CONTRACT-ONLY** backend LIVE ⇒ **0 `.py`/reload/migrate** · **+ Self-Correction domain §12** `05_API_Specification.md`: example cũ 7-key STALE (`compliance_rate_pct`/`out_of_tolerance_rate_pct`/`capa_*`/`period` + `overdue_assets[]` LIST) → 6-key LIVE khớp `get_kpis` @source (đồng bộ §6.1 vốn đã đúng) · Guard `TestMobileGetCalibrationKpisContract` a..i (9 TC: a=path/opId 88+GET+tag calibration+∈`_MVP_READ_ENVELOPE`+∉`_MVP_LIST_ENVELOPE` · b=2 typed param year/month integer required:false KHÔNG default-key · c=Kpis CLOSED EXACT-6 + required==6 + 5 integer/1 number · d=`pass_rate_pct` NON-null ∈required ∧ 0 nullable ∧ 0 Check ∧ overdue/due_soon integer-scalar-KHÔNG-array · e=Data CLOSED req[kpis] + `trend_6months` ABSENT (anti-drift vs Pm) · f=Envelope CLOSED {success:[true],data} · g=200 oneOf `[Envelope,Error]` 0-discr + slot{200,401,403} · h=3-schema-KHÔNG-orphan+0-dangling+count-reconcile · i=naming-guard `CalibrationKpis*` disjoint `CalibrationDetail*`/`DueCalibration*`/`SubmitCalibration*`/… + live-sig `{year,month}`) ⇒ `_EXPECTED_TEST_COUNT` **792→801** + `_GUARD_SUITE_SUM` **935→944** + `_MOBILE_OAS_TOTAL` **961→970** + path/opId **87→88** + c5/`_PARITY_BUSINESS_PATHS` **76→77** + `_MVP_READ_ENVELOPE` +1 (`_MVP_LIST_ENVELOPE` GIỮ 13) — ⚠️ baseline grep-verify @source TRƯỚC bump (đa-phiên race) · **Alternatives loại:** thêm `trend_6months` (service trả duy nhất `{kpis}` — bịa field) / `pass_rate_pct` nullable (else=0.0 → NON-null, ĐỐI-NGHỊCH PM) / `overdue_assets`/`due_soon_assets` array (LIVE=COUNT scalar; §12-list STALE) / tag `dashboard` (fork taxonomy) / inline `kpis` bỏ `CalibrationKpisData` (phá đối-xứng 3-tầng) / khai `default:` YAML (default động) / ∈ `_MVP_LIST_ENVELOPE` (dashboard ≠ list) / status-line 404 (Decision-B) / 403 dual-shape (0 rbac.require) / bịa/±field (VERBATIM 0-invention). **Accepted.** |
| [`ADR-MOBILE-059.md`](./ADR-MOBILE-059.md) | ADR `getUserCompetencies` (`imm06.get_user_competencies`) curate vào OAS mirror (**CR-34 · MỞ NHÁNH IMM-06 (Đào tạo & Năng lực)** — bồi ĐÚNG 1 GET-read path trả HỒ SƠ NĂNG LỰC 1 nhân viên (`{user, items[]}`, 10-field/dòng) cho màn "Năng lực của tôi"; **module IMM-06 LẦN ĐẦU vào mirror** (0 endpoint imm06 hiện có) + **tag `training` MỚI 15th** + schema-family `UserCompetenc*` MỚI) — **CONTRACT-ONLY** GROUNDED 1:1 handler LIVE bare `@whitelist()` `get_user_competencies(user: str = "")` @`api/imm06.py:189` (GET; guest dispatcher-403; `_run(svc.get_user_competencies, user or frappe.session.user)`; `_run` @`:34` = `_ok`/`_err` ⇒ CÓ nhánh `_err` ⇒ Decision-B) + service @`services/imm06.py:1527-1546` (return VERBATIM `{user, items:[10-field row]}`) + `_ok` @`utils/response.py:79` (`{success:true, data}`) · **200 = inline `oneOf [UserCompetenciesEnvelope, Error]`** Decision-B 0-discr (∈ `_MVP_READ_ENVELOPE`, **∉ `_MVP_LIST_ENVELOPE`** — flat-object read `page_size=500` chốt-cứng ⇒ GIỮ 13) · **3 schema CLOSED 3-tầng Envelope→Data→Item**: `UserCompetencyListItem` (EXACT 10 prop VERBATIM @`:1539-1541`, `required`=CẢ 10 — 4 non-null [`name`/`workflow_state`/`days_until_expiry` integer SIGNED/`is_expired` integer `enum[0,1]` Check-quirk READ CR-01] + 6 `nullable:true` [`device_model`/`training_program` Link-id-RAW, `competency_level` `enum[Trainee,Operator,Senior Operator,Trainer]` Select, `achieved_date`/`expiry_date` `format:date`, `last_assessment_score` number]; `workflow_state` `enum[Pending Assessment,Active,Expiring,Expired,Suspended,Revoked]` grounded workflow JSON) / `UserCompetenciesData` (`{user:string, items:array<UserCompetencyListItem>}` req cả 2 — payload = đề-mục gọi "UserCompetenciesResponse", đổi tên `*Data` khớp precedent RepairKpisData/CalibrationKpisData) / `UserCompetenciesEnvelope` (`{success.enum[true], data:$ref}` req[success,data]) · **1 typed query-param** `user` `in:query` `type:string` `required:false` **KHÔNG khai `default:` YAML** (backend default động `frappe.session.user`; precedent CR-05) · slot `{200,401,403}` **403 SINGLE `Forbidden` dispatcher-ONLY** (bare `@whitelist` 0 `rbac.require`) + `401 Unauthorized401` · **tag `training`** (MỚI 15th — precedent mở-module IMM-04→`commissioning`/IMM-15→`inventory`/IMM-16→`compliance`) · opId `getUserCompetencies` DOMAIN · ∈ `_MVP_BUSINESS_PATHS` (401/403 symmetry tự +1) · **🟢 CONTRACT-ONLY** backend LIVE ⇒ **0 `.py`/reload/migrate** · **⚠️ SELF-CORRECTION §2(a)**: đề-mục khai **2 schema** (`UserCompetenciesResponse={user,items}` oneOf-member TRỰC-TIẾP; closed-count 203→205) SAI-nguồn — `_ok` wrap `{success,data}` ⇒ CHỐT **3 schema** (`*Data`+`*Envelope`), **closed-count += 3** (203 STALE — live ≈213 sau CR-31a/b/c) · Guard `TestMobileGetUserCompetenciesContract` a..g (7 TC: a=path/opId 90+GET+opId-UNIQUE · b=3-schema-CLOSED · c=Item 10-prop `SET==`+req-10+4-non-null/6-nullable+`format:date` · d=`is_expired` `enum[0,1]`-int + `days_until_expiry` integer + competency_level/workflow_state enum · e=1-param-user-STRING-required:false + live-sig `{user}` default `""` · f=tag `training`-MỚI-15th + distinct-tag 14→15 + ∉`_MVP_LIST_ENVELOPE` · g=200 oneOf `[Env,Error]` 0-discr + Data-CLOSED-`{user,items}` + Envelope-CLOSED-`{success,data}` + ∈symmetry-slots{200,401,403} + naming-guard-`UserCompeten*`-EXACT-3 + 0-dangling) ⇒ `_EXPECTED_TEST_COUNT` **811→818** + `_GUARD_SUITE_SUM` **954→961** + `_MOBILE_OAS_TOTAL` **980→987** + path/opId **89→90** + c5/`_PARITY_BUSINESS_PATHS` **78→79** + `_MVP_READ_ENVELOPE` +1 (`_MVP_LIST_ENVELOPE` GIỮ 13) + distinct-tag **14→15** · **Follow-on:** enrich `device_model_name` (`T-IMM06-*`, backend+reload) + authz cross-user (`?user=`, 0 `rbac.require` LIVE). **Accepted.** |

### Hợp đồng máy đọc

| File | Mô tả 1 dòng |
|---|---|
| [`openapi/assetcore-mobile.openapi.yaml`](./openapi/assetcore-mobile.openapi.yaml) | OpenAPI 3.0.3 (`AssetCore Mobile API`, `0.1.0-skeleton`) — securityScheme OAuth2 · 3 path auth bồi · Envelope/Error/Pagination + responses chuẩn · 6 path nghiệp vụ STUB · 2 path device-token STUB · COMPONENTS offline dùng-lại · **`operationId` đầy đủ 15/15 (codegen-able, A10)** — convention `04-api-contract.md §8.1`. Nguồn sinh API client (repo native). |

> **A15 — Docset-integrity GUARDED (chống regress tầng navigation).** Toàn vẹn chính bộ docset này (index ↔ filesystem parity · link-health · ADR registration · no-placeholder) nay được kiểm chứng tự động bởi `assetcore/tests/test_mobile_docset.py` (`TC-MOB-DOC-01..05`): (1) mọi chương đánh số `NN-*.md` trên đĩa ↔ đúng 1 dòng index §1 (đếm động bằng glob, 0 chương mồ côi / 0 mục index treo); (2) mọi `ADR-MOBILE-*.md` + `openapi/assetcore-mobile.openapi.yaml` được README liệt kê/tham chiếu; (3) mọi link nội bộ tương đối (`./`,`../`) trong `docs/mobile/*.md` resolve về file tồn tại (baseline 405 link / 0 broken); (4) 0 placeholder (`TODO/TBD/FIXME/XXX/<...>/lorem`) NGOÀI code-fence + mỗi chương non-empty có ≥1 H1. Edit Phase B/C/D mà phá parity/link/placeholder ⇒ test ĐỎ. Đây là guard tầng **navigation**, song song guard tầng **contract** (`test_mobile_oas` = yaml · `test_mobile_capability_map` = rbac · `test_mobile_preflight` = oauth-client).

---

## 2. Bảng map Phase A→F ↔ file

> Roadmap 6 phase: [`00-overview.md §3`](./00-overview.md). Ranh giới repo: Phase A/B/C/E/F (BE + contract + deploy) thuộc repo `assetcore`; **Phase D** (UI native) thuộc **repo mobile riêng**.

| Phase | Tên | Trạng thái | File liên quan |
|---|---|---|---|
| **A** | Kiến trúc & Feasibility | 🟢 Hoàn tất (exit-ready) | `00`·`01`·`02`·`03` (A2)·`04` (A3)·`05` (A4)·`06`+ADR-002 (A5)·`07`+ADR-003 (A6)·`08`+ADR-004 (A7)·`09` (A8)·`10` (A9)·**`11` (A11 — EXIT GATE)**·ADR-001·`openapi/` |
| **B** | Provisioning & Auth wiring (HARD-STOP USER) | ⬜ Chưa | **`10-deploy-ops.md` (runbook go-live: numbered steps §1–§5 + checklist + smoke + rollback)** · `02-deploy-feasibility.md` (blocker/survey) · `03-auth-oauth2.md §4` (checklist OAuth Client) · `08 §4` (security go-live) |
| **C** | API contract bồi đắp | ⬜ Chưa | `04-api-contract.md §10` (việc Phase C) · `openapi/*.yaml` (bồi 6 path nghiệp vụ) |
| **D** | Repo native MVP (repo riêng, ngoài `assetcore`) | ⬜ Chưa | `09-native-repo-guide.md` (handoff) · `05-personas-mvp.md` (luồng MVP) · `openapi/*.yaml` (sinh client) |
| **E** | Push / Offline / Sync (impl) | ⬜ Chưa (spec sẵn) | `06-push-fcm.md`+ADR-002 · `07-offline-sync.md`+ADR-003 · `openapi/` (COMPONENTS offline) |
| **F** | Hardening & Go-live | ⬜ Chưa | `08-security-compliance.md` (security review · go-live) + ADR-004 · `03 §2.1` (token TTL backlog) |

---

## 3. Ba quyết định CHỐT (in nguyên ở [`00-overview.md §2`](./00-overview.md))

> 3 quyết định nền do USER chốt (2026-06-09); KHÔNG re-litigate. Mọi thiết kế phải BÁM theo. **Nguồn chân lý = [`00-overview.md §2`](./00-overview.md).**

| Mã | Quyết định | Đặc tả chi tiết |
|---|---|---|
| **D-AUTH** | OAuth2 (Authorization Code + PKCE) + access-token ngắn hạn + refresh + revoke. WIRE provider Frappe có sẵn — KHÔNG tự viết OAuth. Bearer→`set_user`→RBAC capability (1 SSoT). | [`03-auth-oauth2.md`](./03-auth-oauth2.md) · [`ADR-MOBILE-001.md`](./ADR-MOBILE-001.md) (a/b/e) |
| **D-MVP** | MVP nhắm **kỹ thuật viên hiện trường** (6 luồng: login · quét QR→hồ sơ · báo hỏng · yêu cầu PM/CM/Hiệu chuẩn · phiếu của tôi · push). Tái dùng endpoint nghiệp vụ. | [`05-personas-mvp.md`](./05-personas-mvp.md) |
| **D-STACK** | App **native** (Flutter HOẶC React Native), KHÔNG WebView/PWA. Repo UI tách riêng; HTTP-client native; PKCE bắt buộc. | [`01-architecture.md`](./01-architecture.md) · [`09-native-repo-guide.md`](./09-native-repo-guide.md) (skeleton + trade-off chọn stack) |

---

## 4. Điểm vào cho người mới — đọc theo thứ tự

1. **[`00-overview.md`](./00-overview.md)** — bắt đầu ở đây: mục tiêu · 3 quyết định chốt (§2) · roadmap · glossary OAuth2/PKCE/refresh.
2. **[`01-architecture.md`](./01-architecture.md)** — topology + 3 lằn ranh trách nhiệm (repo nào làm gì) + OpenAPI=hợp đồng.
3. **[`ADR-MOBILE-001.md`](./ADR-MOBILE-001.md)** — vì sao 5 quyết định kiến trúc (wire OAuth · 1 SSoT quyền · reuse · contract · no-cookie).
4. **[`03-auth-oauth2.md`](./03-auth-oauth2.md)** + **[`04-api-contract.md`](./04-api-contract.md)** — 2 hợp đồng cốt lõi: xác thực (PKCE/refresh/revoke) + envelope/error/pagination.
5. **[`05-personas-mvp.md`](./05-personas-mvp.md)** — persona field-tech + 6 luồng MVP + bảng MÀN↔API.
6. **[`06-push-fcm.md`](./06-push-fcm.md)** · **[`07-offline-sync.md`](./07-offline-sync.md)** · **[`08-security-compliance.md`](./08-security-compliance.md)** (+ ADR-002/003/004) — push · offline/sync · bảo mật (đặc tả Phase E/F).
7. **[`09-native-repo-guide.md`](./09-native-repo-guide.md)** — khi sẵn sàng dựng repo native (Phase D): skeleton · sinh client từ OpenAPI · ENV · OAuth wiring · build APK + CI · checklist khởi tạo.
8. **[`openapi/assetcore-mobile.openapi.yaml`](./openapi/assetcore-mobile.openapi.yaml)** — hợp đồng máy đọc (nguồn sinh API client). Tra song song với `03`/`04`.
9. **[`11-phase-a-exit.md`](./11-phase-a-exit.md)** — **cổng thoát Phase A:** traceability matrix 6 flow MVP (xem nhanh toàn bộ chuỗi màn→endpoint→cap→opId→offline→push) + danh sách Phase-B prereqs hợp nhất + checklist go/no-go. Đọc khi cần "ảnh chụp" toàn cảnh + quyết định mở Phase B.

> **Lối tắt theo vai:**
> - **Quyết định mở Phase B / nắm toàn cảnh:** **`11-phase-a-exit.md`** (matrix 6 flow + prereqs + go/no-go) — 1 file thấy hết.
> - **Đội repo native (Phase D):** `00 §2` → `09` → `03` → `04` → `openapi/` → `05` (tra nhanh matrix `11 §1`).
> - **USER/Admin (Phase B go-live):** `11 §2/§3` (prereqs B-1..B-8 + checklist) → `10-deploy-ops.md` (runbook numbered steps + checklist + smoke) → `03 §4` (OAuth Client field) → `08 §4` (security checklist). `02-deploy-feasibility.md` = survey nền (đọc trước nếu cần hiểu gap).
> - **BE dev (Phase C/E):** `04 §10` (việc Phase C) → `06`/`07` (push/offline) → `openapi/` (tra STUB-status `11 §1`).

---

## Tham chiếu chéo (ngoài docset)

- **Guard docset (self-referential — kiểm chính bộ docset này):** `../../assetcore/tests/test_mobile_docset.py` (`TC-MOB-DOC-01..05`) — index↔filesystem parity + link-health + ADR registration + no-placeholder. Song song 3 guard contract: `../../assetcore/tests/test_mobile_oas.py` (yaml — `TC-MOB-OAS-*`) · `../../assetcore/tests/test_mobile_capability_map.py` (rbac — `TC-MOB-CAP-*`) · `../../assetcore/tests/test_mobile_preflight.py` (oauth client — `TC-MOB-PRE-*`).
- RBAC SSoT (capability): `../../assetcore/services/shared/rbac.py`
- Envelope/ErrorCode SSoT: `../../assetcore/utils/response.py`
- QR/asset endpoint: `../../assetcore/api/imm00.py` · Báo hỏng: `../../assetcore/api/imm12.py`
- Push engine (channel #3 insert point): `../../assetcore/services/notifications.py::_dispatch`
- Provider Frappe OAuth2: `../../../frappe/frappe/integrations/oauth2.py` · `../../../frappe/frappe/oauth.py` · `../../../frappe/frappe/auth.py`
