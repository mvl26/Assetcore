# 13 — BE Completion Roadmap (MASTER)

| Mục | Giá trị |
|---|---|
| Vai trò file | **MASTER roadmap** hoàn thiện lớp Backend-for-Mobile — chốt 5 EPIC, DoD, dependency, AUTO vs HARD-STOP |
| Initiative | Mobile Backend (AssetCore = BE cho app mobile native; repo UI riêng) |
| Cấu trúc | USER đã duyệt 2026-06-11 |
| Trạng thái | Phase A exit-ready → mở Phase B/C/E/F (xem [`11-phase-a-exit.md`](./11-phase-a-exit.md)) |
| Phạm vi file này | **doc-only** — KHÔNG sửa `api/*.py` / `services/*.py` / yaml-path/operationId; KHÔNG git commit/push/migrate/reload/restart |
| Cập nhật | 2026-06-11 |

> **Nguồn chân lý kiến trúc** = [`00-overview.md §2`](./00-overview.md) (3 quyết định USER) + [`11-phase-a-exit.md §1`](./11-phase-a-exit.md) (traceability matrix 6 flow). File này KHÔNG re-litigate quyết định nền — chỉ TỔ CHỨC việc còn lại thành 5 EPIC khoá-ID + DoD + thứ tự.
> **Mọi claim kỹ thuật** trong file này có `file:line` evidence verify tại source **Frappe v15.107.2** (branch `feature/hieuc/core-refinement`, ground 2026-06-11). Việc CHƯA verify → đánh dấu `[ROADMAP]` / `*(Cần khảo sát)*`.
> ⚠️ **Số dòng `file:line` là CHỈ DẪN tại thời điểm ground (2026-06-11)** — code drift theo thời gian; agent thực thi **PHẢI re-verify @source** (mở file, tìm theo tên symbol/hàm) TRƯỚC khi sửa, KHÔNG tin số dòng tuyệt đối. Tên file + hàm/symbol là ổn định; số dòng thì không.

---

## 0. Mục tiêu (DoD TỔNG)

**DoD TỔNG = MVP field-tech 6-flow END-TO-END chạy THẬT trên cloud:**

| # | Flow MVP | Endpoint trục (file:line @source) | EPIC chốt |
|---|---|---|---|
| 1 | Đăng nhập OAuth2 + refresh | `frappe/integrations/oauth2.py:74` authorize · `:123` get_token · `:144` revoke | **B** |
| 2 | Quét QR → hồ sơ thiết bị | `api/imm00.py:588` resolve_qr_token · `:631` get_asset_scan_info · `:454` get_asset | **C** |
| 3 | Báo hỏng (Incident) | `api/imm12.py:71` report_incident | **C** (typed ✅) |
| 4 | Yêu cầu WO PM/CM/Cal | `api/imm08.py:91` create_pm · `api/imm09.py:35` create_repair · `api/imm11.py:89` create_cal | **C** |
| 5 | "Phiếu của tôi" (list+detail) | `api/imm08.py:28` list_pm · `api/imm09.py` list_repair · `api/imm12.py:197` list_incidents | **C** |
| 6 | Push FCM | `services/notifications.py:366` `_dispatch` (kênh #3 CHƯA có) | **D** |

**Tiêu chí "chạy THẬT trên cloud":** 1 client codegen (Dart/Kotlin) gọi được cả 6 flow trên public HTTPS host → **EPIC-V** chốt cuối.

### Out-of-scope MVP (post-MVP — ghi RÕ, KHÔNG làm round này)

- **Offline-sync** ([`07-offline-sync.md`](./07-offline-sync.md) + ADR-003) — read-cache/write-queue/conflict đã đặc tả, impl Phase E.
- **Manager / luồng duyệt** (approval) — MVP chỉ field-tech (D-MVP, [`05-personas-mvp.md`](./05-personas-mvp.md)).
- **Mở rộng đa-module** ngoài 6 flow — không thêm persona quản lý/giám đốc.

---

## 1. Ba quyết định nền (AUTHORITATIVE — KHÔNG re-litigate)

> In nguyên ở [`00-overview.md §2`](./00-overview.md). USER chốt 2026-06-09. Mọi EPIC BÁM theo.

| Mã | Quyết định | Hệ quả cho BE-completion |
|---|---|---|
| **D-AUTH** | OAuth2 (Authorization Code + PKCE S256) + access-token ngắn hạn + refresh + revoke. WIRE provider Frappe có sẵn — KHÔNG tự viết OAuth. Bearer→`set_user`→RBAC capability (scope↔capability = **1 SSoT**). | EPIC-B = WIRE-not-write; refresh ĐÃ hỗ trợ (`frappe/oauth.py:187`). |
| **D-MVP** | MVP nhắm **kỹ thuật viên hiện trường** (6 flow). Tái dùng endpoint nghiệp vụ permission-aware. | Lớp mobile = **BỌC + TÁI DÙNG** endpoint+capability có sẵn — KHÔNG viết lại logic. |
| **D-STACK** | App **native** (Flutter HOẶC React Native), KHÔNG WebView/PWA. Repo UI tách riêng; PKCE bắt buộc. | Native APK **KHÔNG cần CORS** (không browser engine) — EPIC-G CORS chỉ cho web/PWA/Swagger/WebView-OAuth. |

---

## 2. Năm EPIC (ID KHOÁ — KHÔNG đổi) + thứ tự

```
        EPIC-C  (API Contract codegen-ready)  ── độc lập, doc/yaml/test → LÀM NGAY
           │
           ├──────────────┐
           ▼              ▼
        EPIC-B         thiết kế EPIC-D
   (Auth & Provision)   (Push FCM design)
           │              │
           ▼              │
        EPIC-G ◄──────────┘   (Go-live & Hardening — cần USER: cloud + site_config)
           │
           ▼
        EPIC-D  (Push FCM impl — cần B + FCM creds)
           │
           ▼
        EPIC-V  (Codegen Verify + Handoff — CHỐT CUỐI)
```

| Thứ tự | EPIC | Tên | Phụ thuộc | Gate chính |
|---|---|---|---|---|
| 1 | **C** | API Contract codegen-ready | (độc lập) | `openapi-generator` chạy sạch · `test_mobile_oas` xanh |
| 2 | **B** ∥ thiết kế **D** | Auth & Provisioning | (∥ C) | `preflight.verify_oauth_client()` ready=True |
| 3 | **G** | Go-live & Hardening | C + B | HTTPS reachable + security gate |
| 4 | **D** | Push FCM impl | B + FCM creds + G | báo hỏng → KTV nhận push |
| 5 | **V** | Codegen Verify + Handoff | C + D + G | 1 client gen gọi được 6 flow trên cloud |

**Mỗi TASK tag rõ:** `[AUTO]` = factory tự đóng được (doc/yaml/test/impl-không-deploy) vs `[HARD-STOP USER]` = cloud / `bench migrate` / `bench restart` / `site_config` / FCM creds (BE KHÔNG tự chạy — xem §8 Blockers).

---

## 3. EPIC-C — API Contract (codegen-ready)

> Vùng tài liệu: [`04-api-contract.md §5/§8/§10`](./04-api-contract.md) + `openapi/assetcore-mobile.openapi.yaml`. **CHỈ ĐỌC yaml round này** (BE-completion doc liệt-kê, KHÔNG sửa yaml ở đây).

### 3.1 Hiện trạng

- YAML `openapi 3.0.3`, **53 path, 53/53 operationId** camelCase frozen (grep @working-tree: `grep -cE '^\s{2}/'` = 53 + `grep -cE 'operationId:'` = 53). **ASSET-PM-HISTORY-WIRE (IMM-08 FLOW-2 2026-06-29):** 52→53 path — bồi `getAssetPmHistory` (GET lịch-sử BẢO-TRÌ định-kỳ (PM) của 1 asset cho tab "Lịch sử bảo trì" màn hồ-sơ-thiết-bị flow-2; **ĐÓNG quartet** device-profile read-history cùng `getAssetIncidentHistory`/`getAssetTimeline`/`getAssetRepairHistory`; **MIRROR `getAssetRepairHistory`** rows-key `data.history[]` + asset-key `asset_ref` + **200 = SINGLE-shape `AssetPmHistoryEnvelope`** (handler `handle(svc.get_asset_history)` + svc 0 raise `ServiceError` ⇒ `handle` LUÔN `_ok` ⇒ KHÔNG `oneOf` — mirror `listTransfers`, KHÁC incident `oneOf`) NHƯNG 3 KHÁC-BIỆT: `AssetPmHistoryItem` closed EXACT **10 prop** `{name,pm_work_order,pm_type,completion_date,technician,overall_result,is_late,days_late,next_pm_date,summary}` grounded `PMTaskLogRepo.list` `services/imm08.py:1015-1017`; `overall_result` = `string` **enum** `[Pass, Pass with Minor Issues, Fail]` (Select @`pm_task_log.json`); **2 integer field** `is_late` (Check 0/1) + `days_late` (Int) → `integer` KHÔNG `boolean`; dates `completion_date`/`next_pm_date` `string` KHÔNG `format:date-time`. Slot `{200,401,403}` (bare `@whitelist` no-allow_guest → guest dispatcher-403). **🟢 CONTRACT-ONLY** `git diff` api/imm08.py + services/imm08.py phần `get_asset_pm_history`/`get_asset_history` = TRỐNG (BE LIVE) ⇒ KHÔNG reload/migrate, [AUTO] thật. Guard `TestMobileGetAssetPmHistoryContract` (a..j, 10 TC) ⇒ `_EXPECTED_TEST_COUNT` 492→502. ADR-MOBILE-023. **ASSET-REPAIR-HISTORY-WIRE (IMM-09 vòng-2 2026-06-29):** 51→52 path — bồi `getAssetRepairHistory` (GET lịch-sử SỬA-CHỮA CM của 1 asset cho tab "Lịch sử sửa chữa" màn hồ-sơ-thiết-bị flow-2; **MIRROR `getAssetIncidentHistory`** NHƯNG rows-key `data.history[]` + asset-key `asset_ref` — KHÁC incident `items`/`asset`; **200 = SINGLE-shape `AssetRepairHistoryEnvelope`** vì handler `handle(svc.get_asset_history)` + svc 0 raise `ServiceError` ⇒ `handle` LUÔN `_ok`, 0 nhánh `_err` ⇒ KHÔNG `oneOf [Env, Error]` — mirror `listTransfers`, KHÁC incident `oneOf`). `AssetRepairHistoryItem` closed EXACT 9 prop `{name,repair_type,priority,open_datetime,completion_datetime,mttr_hours,sla_breached,root_cause_category,repair_summary}` grounded `RepairRepo.list` `services/imm09.py:1215-1216`; **0 int-bool-trap** (`sla_breached` Check 0/1 → `integer` KHÔNG `boolean`; `mttr_hours` `number`; dates `string` KHÔNG `format:date-time`). Slot `{200,401,403}` (bare `@whitelist` no-allow_guest → guest dispatcher-403). **🟢 CONTRACT-ONLY** `git diff` api/imm09.py + services/imm09.py phần `get_asset_repair_history`/`get_asset_history` = TRỐNG (BE LIVE) ⇒ KHÔNG reload/migrate, [AUTO] thật. Guard `TestMobileGetAssetRepairHistoryContract` (a..i, 9 TC) ⇒ `_EXPECTED_TEST_COUNT` 483→492. ADR-MOBILE-022. **TRANSFER-READ-WIRE (IMM-13 Đợt-2 2026-06-29):** 49→51 path — bồi `listTransfers` (GET list điều chuyển/luân chuyển thiết bị, SINGLE-shape `TransferListEnvelope` rows-key `data.items[]`; handler `list_transfers` KHÔNG try/except ⇒ 0 nhánh `_err` ⇒ KHÔNG `oneOf [Env, Error]`) + `getTransfer` (GET chi tiết, 200 = `oneOf [TransferDetailEnvelope, Error]` route-by-VALUE 0-discriminator; 404 `_err@imm00.py:2084` → HTTP-200 nhánh Error). **🟢 CONTRACT-ONLY** (6 endpoint điều chuyển ĐÃ LIVE `api/imm00.py:2048 list_transfers` / `:2081 get_transfer` + 4 write; `git diff` api/imm00.py + services/imm00.py = TRỐNG ⇒ KHÔNG reload/migrate, [AUTO] thật). 4 schema (`TransferListItem` 17-field closed / `TransferListEnvelope` single-shape / `TransferDetail` as_dict mở / `TransferDetailEnvelope` closed) + 2 param `TransferAsset`/`TransferStatus` reuse; `status` enum 5 @asset_transfer.json; 0 bool-trap. Guard `TestMobileTransferReadContract` (a..p, 16 TC) ⇒ `_EXPECTED_TEST_COUNT` 467→483. ADR-MOBILE-021. **SESSION-PROBE (2026-06-29):** 48→49 path — bồi `pingSession` (GET session-probe `{user, authenticated, csrf_token}`: CSRF warm-up + app-resume who-am-I-lite, `layout.ping_session:237-258`) — **ĐÓNG NỐT** cặp session-lifecycle còn lại sau notification quartet (R38-R41). Cookie-sid mobile app (auth reconciliation mới nhất) CẦN endpoint nhẹ cho app-resume check + tiền-đề MỌI POST (Frappe set `csrf_token` cookie qua response). ⚠️ **KHÁC `getUserContext` {200,401}**: handler `:237-258` **LUÔN `_ok`** (0 nhánh `_err` in-handler) + `allow_guest=True` ∧ KHÔNG in-handler guest-guard ⇒ **200 = SINGLE schema `PingSessionEnvelope`** (KHÔNG `oneOf [Env, Error]` — 0 Error branch trên HTTP-200) ∧ **response slot `{200}` EXACTLY** (KHÔNG 401/403/429). `PingSessionData` closed (`additionalProperties:false`) required EXACT 3 `{user: string, authenticated: GENUINE type:boolean = user!='Guest' :256 (KHÔNG int-enum trap như getUserContext is_profile_completed/has_employee_link; mirror is_late PmSubmitResultResponse), csrf_token: string có-thể-'' fallback :249-253 KHÔNG nullable}`. ∈ `_ALLOW_GUEST_PATHS` (exempt 401/403 symmetry như getUserContext) NHƯNG slot `{200}` (phân biệt getUserContext `{200,401}`) ∧ ∉ `_MVP_BUSINESS_PATHS`. **🟢 CONTRACT-ONLY** (BE endpoint LIVE `:237`): `git diff` api/layout.py + services/layout.py (không tồn tại — layout API-only) = TRỐNG ⇒ KHÔNG reload/migrate ([AUTO] thật, KHÔNG HARD-STOP USER); `test_oas_generator`/`d12`/`d15`/`d17` UNCHANGED (pure mobile-yaml). Guard `TestMobilePingSessionContract` (a..i, 9 TC) ⇒ `_EXPECTED_TEST_COUNT` 458→467. **NOTIF-UNREAD-FEED (2026-06-29):** 47→48 path — bồi `getUnreadNotifications` (GET unread-feed `{count, items[]}`: tổng chưa-đọc + danh-sách thông-báo chưa-đọc của user, `layout.get_unread_notifications:47-69`) — **ĐÓNG NỐT** notification-center READ quartet (powering unread-badge + tab "Thông báo" mà mobile app GỌI 13× top-usage, sau `listNotifications` + `markNotificationAsRead` + `markAllAsRead`). `UnreadNotificationListEnvelope` closed: data `{count: integer minimum 0 GENUINE count (≠ NotificationListItem.read int-enum[0,1]; mirror updated_rows R40/measurement_count R34), items[]: $ref REUSE NotificationListItem (0 schema-item mới, ĐỒNG $ref NotificationListEnvelope)}` — **KHÔNG** `pagination` (handler `:66-69` `{count,items}` KHÔNG `paginate()`, KHÁC NotificationListEnvelope). 200 = **INLINE** oneOf `[UnreadNotificationListEnvelope, Error]` (read-path mirror searchSpareParts/getAssetIncidentHistory; ∈ `_MVP_READ_ENVELOPE` C5 union 40→41; KHÔNG response-component; KHÔNG `_MVP_LIST_ENVELOPE`). Param `NotifLimit` query integer default 20 min 1 max 100 (clamp `max(1, min(int(limit),100))` `:55`). Slot `{200,401,403}` SINGLE-SHAPE `Forbidden` — **KHÔNG 404/409** (scope `for_user=session.user`, no lookup-by-name). **🟢 CONTRACT-ONLY** (BE endpoint LIVE `:47`): `git diff` api/layout.py + services/layout.py = TRỐNG ⇒ KHÔNG reload/migrate ([AUTO] thật, KHÔNG HARD-STOP USER); `generate_spec` get/post UNCHANGED. Guard `TestMobileGetUnreadNotificationsContract` (a..i, 9 TC) ⇒ `_EXPECTED_TEST_COUNT` 449→458. **R40-MARKALLREAD (2026-06-29):** 46→47 path — bồi `markAllAsRead` (POST BULK read-receipt: set `read=1` cho MỌI Notification Log chưa-đọc của user, `layout.mark_all_as_read:120-134`) — **ĐÓNG NỐT** notification-center action-set (tab "Thông báo" › nút "Đánh dấu tất cả đã đọc", sau `markNotificationAsRead` single FLOW-6). `MarkAllReadResponse` RIÊNG closed 1-key `{updated_rows: integer}` GENUINE count 0..N (KHÔNG enum[0,1]; `affected = ROW_COUNT()` `:132`), KHÔNG field `status` (C3-split cross-domain ≠ mọi `*ActionResponse`). **0-PARAM** `mark_all_as_read()` `:121` ⇒ **KHÔNG requestBody** (codegen no-arg POST). Slot `{200,401,403}` SINGLE-SHAPE `Forbidden` — **KHÔNG 404/409** (scope SQL `WHERE for_user=session.user`, no lookup-by-name). **🟢 CONTRACT-ONLY** (BE endpoint LIVE): `git diff` api/services/layout = TRỐNG ⇒ KHÔNG reload/migrate; `generate_spec` get/post UNCHANGED. Guard `TestMobileMarkAllReadContract` (a..g+i, 8 TC) ⇒ `_EXPECTED_TEST_COUNT` 439→447. ADR-MOBILE-018. **R37-PM-RESCHEDULE (2026-06-28):** 45→46 path — bồi `reschedulePm` (POST hoãn lịch PM: thiết bị đang dùng → `Pending–Device Busy` + ghi lý do bắt buộc, `imm08.reschedule_pm:86`) — **ĐÓNG NỐT** action-set `PMWorkOrderDetailView` (đóng nút "Hoãn lịch (thiết bị bận)", 0 nút dead-end). `ReschedulePmResponse` RIÊNG closed 4-key `{name,old_date,new_date,status}` (date-pair DUY NHẤT; `status`=PMStatus "Pending–Device Busy" en-dash U+2013; `services/imm08.py:823`). Request closed req-3 `[name,new_date,reason]` (`reason.minLength:5` mirror guard `:808` · `new_date.format:date`). 2 Error-on-HTTP-200 → `Error.http_status ⊇ {404,422}` (404 WO∄ + 422 reason<5). Cap `pm.reschedule` (`rbac.py:94`, KHÁC sibling `pm.write`). Guard `TestMobileReschedulePmContract` (+≥8 a..h) ⇒ `_EXPECTED_TEST_COUNT` bump. **🟢 ATOMIC-THIS-ROUND (ADR-MOBILE-014):** handler `imm08.py:86` ĐÃ `methods=['POST']` + signature ĐÃ khớp service ⇒ **KHÔNG verb-flip + KHÔNG signature-fix + KHÔNG đụng api/service** → PURE-YAML+test; `generate_spec` get/post UNCHANGED ⇒ `test_oas_d12/d15/d17` **RE-VERIFY @source** (KHÔNG re-baseline trừ drift — KHÁC R36 verb-flip shift); `_PARITY_VERB_ALLOWLIST` GIỮ `set()`. **R36-PM-ESCALATION (2026-06-28):** 44→45 path — bồi `reportMajorFailure` (POST escalation PM-detail: PM hỏng nặng → `Halted–Major Failure` + asset `Out of Service` + CM WO khẩn, `imm08.report_major_failure:74`) — đóng nút 'Báo lỗi nghiêm trọng (→ CM)' `PMWorkOrderDetailView`. `ReportMajorFailureResponse` RIÊNG 4-key `{pm_wo,new_status,cm_wo_created,asset_status}` (`new_status`=PMStatus 'Halted–Major Failure', `asset_status`='Out of Service'; `services/imm08.py:792-797`). Guard `TestMobileReportMajorFailureContract` (+9 a..i) ⇒ `_EXPECTED_TEST_COUNT` 417→426. **VERB-FLIP-THIS-ROUND + SIGNATURE-FIX (ADR-MOBILE-013):** flip bare `@frappe.whitelist()` `imm08.py:74` → `methods=['POST']` (write KHÔNG idempotent) + DROP `failed_item_indexes` (handler↔service mismatch → TypeError/HTTP-500) ⇒ POST-only @source, `_PARITY_VERB_ALLOWLIST` GIỮ `set()`; re-baseline `test_oas_d12/d15/d17` get 233→232 / post 255→256 (RE-VERIFY @source SAU flip; total GIỮ). **R35-PM-DISPATCH (2026-06-28):** 43→44 path — bồi `assignPmTechnician` (POST mắt-xích-GIỮA PM-detail: phân công KTV cho PM WO `Open`/`Overdue` → `In Progress` + asset→Under Maintenance, `imm08.assign_technician:47`) — đóng action-set THIN `PMWorkOrderDetailView` (`createPmWorkOrder → assignPmTechnician → submitPmResult`), **parity** repair `create → assignTechnician → start`. `AssignPmTechnicianResponse` RIÊNG 3-key `{name,status,assigned_to}` (**C3-split** — `status`=PMStatus "In Progress", KHÔNG reuse repair `AssignTechnician*` "Assigned"; `services/imm08.py:679`). opId `assignPmTechnician` UNIQUE (≠ repair `assignTechnician`). Guard `TestMobileAssignPmTechnicianContract` (+9 a..i — a contract/b verb-parity/c request+live-sig/d 200-oneOf/e response-3key-PMStatus/f error-slot{404,409,422}/g auth-slot/h C3-split+symmetry/i meta-self-count) ⇒ `_EXPECTED_TEST_COUNT` 408→417 (RE-VERIFY @source: introspect count). **VERB-FLIP-THIS-ROUND (ADR-MOBILE-012):** flip bare `@frappe.whitelist()` `imm08.py:46` → `methods=['POST']` (verb-parity gap R33 BỎ SÓT, sibling imm08 của `add_measurement`) ⇒ POST-only @source, `_PARITY_VERB_ALLOWLIST` GIỮ `set()`; re-baseline `test_oas_d12/d17` get 234→233 / post 254→255 (RE-VERIFY @source SAU flip; total GIỮ). **R34 ADD-MEASUREMENT (2026-06-27):** 42→43 path — bồi `addMeasurement` (POST mắt-xích-GIỮA calibration-detail: ghi N điểm-đo vào child table `measurements` TRƯỚC `submitCalibration`, `imm11.add_measurement:120`) — đóng dead-end nhập điểm-đo của tab Calibration MVP-flow-5 (`createCalibration → addMeasurement (×N) → submitCalibration`). `AddMeasurementResponse` RIÊNG 2-key `{name,measurement_count}` (`measurement_count` GENUINE integer count `services/imm11.py:1124`, KHÔNG enum[0,1]). Guard `TestMobileAddMeasurementContract` (+10 a..j) ⇒ `_EXPECTED_TEST_COUNT` 397→407. **VERB-FLIP-THIS-ROUND (ADR-MOBILE-011):** flip bare `@frappe.whitelist()` `imm11.py:120` → `methods=['POST']` (verb-parity gap R33 Bỏ SÓT) ⇒ POST-only @source, `_PARITY_VERB_ALLOWLIST` GIỮ `set()`; re-baseline `test_oas_d12/d17` get 235→234 / post 253→254 (RE-VERIFY @source SAU flip; total 488 GIỮ). **R34 REPAIR SPARE-PARTS sub-flow (2026-06-27):** 40→42 path — bồi `searchSpareParts` (GET autocomplete tra-cứu phụ-tùng, `imm09.search_spare_parts:124`) + `requestSpareParts` (POST cấp phụ-tùng cho WO, `imm09.request_spare_parts:78`) — đóng action-set màn repair-detail (EPIC-C flow-5 repair-detail action set CLOSE). Guard `TestMobileSearchSparePartsContract` (+10) + `TestMobileRequestSparePartsContract` (+9) ⇒ `_EXPECTED_TEST_COUNT` 378→397. DEVIATION grounded @source (re-verify live registry): `request_spare_parts` ĐÃ `@frappe.whitelist(methods=['POST'])` → KHÔNG flip (PURE-YAML, 0 đụng .py); d12/d15/d17 get/post baseline 235/253 GIỮ NGUYÊN (KHÔNG verb-flip); `searchSpareParts` slot {200,401,403} SOURCE-FAITHFUL (bare @whitelist no-allow_guest → guest dispatcher-403, mirror getAssetIncidentHistory/listNotifications — KHÁC đề-xuất task {200,401}). *(version-string `info.version` còn `0.1.0-skeleton` (yaml:89) — KHÔNG là EPIC-C gap; semver-bump = CI-guard concern **EPIC-G** G-A-series, KHÔNG block codegen-readiness EPIC-C.)* **C-LISTREAD-SCHED (open-thread #4):** 16→17 path — bồi `listPmSchedules` (`imm08.list_pm_schedules:122`) để form `createPmWorkOrder` không cụt field bắt buộc `pm_schedule` (imm08.py:788). Guard `TestMobileListPmSchedulesContract` (+8 TC) ⇒ `_EXPECTED_TEST_COUNT` 141→149. **C6-DETAIL (open-thread #4b):** 17→21 path — bồi 4 GET-detail `getPmWorkOrder`/`getRepairWorkOrder`/`getIncident`/`getCalibration` (BE sẵn `imm08.get_pm_work_order:38`·`imm09.get_repair_work_order:28`·`imm12.get_incident:220`·`imm11.get_calibration:81`) để màn detail MVP-flow-5 + push deep-link flow-6 KHÔNG cụt. 200 = oneOf `[<Detail>Envelope, Error]` closed-schema route-by-VALUE `body.success` (C6, 0 discriminator); 4 `*Detail` field-disjoint GROUNDED `svc.get_*` @source; Check-fieldtype prop → `integer enum[0,1]` (Open#1 sweep) trừ imm08 explicit `bool()`-coerce (`pm_sticker_attached`/`is_late`) = boolean. Guard `TestMobileGetDetailContract` (+7 TC, `test_mob_oas_30a..g`) ⇒ `_EXPECTED_TEST_COUNT` 153→160. **C-LISTREAD-CAL (open-thread #4c):** 21→22 path — bồi `listCalibrations` (`imm11.list_calibrations:71`) đóng tab Calibration MVP-flow-5 (list+detail). 200 = oneOf `[CalibrationListEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C7, 0 discriminator); element `CalibrationListItem` field-disjoint GROUNDED `svc.list_calibrations` @source (15 field = 12 base + 3 enrich); `is_recalibration` Check-fieldtype → `integer enum[0,1]` (Open#1 sweep, KHÔNG boolean); 3 financial-field (`gross_purchase_amount`/`accumulated_depreciation`/`current_book_value`) EXCLUDED khỏi list-item (LL-BE-57 mobile-meta no-financial — `list_calibrations` vốn KHÔNG trả, né sẵn); param REUSE `WorkOrderFilters` (JSON-string, mirror `list_pm`/`list_repair`). `imm11.list_calibrations:72-75` ĐÃ có `parse_json`-in-try/except → `_err` (Error-trên-HTTP-200) — KHÁC imm09 round 4 (KHÔNG cần dời `.py`). Guard `TestMobileListCalibrationsContract` (+7 TC, `test_mob_oas_cal_a..g`) ⇒ `_EXPECTED_TEST_COUNT` 167→174. **C-LISTREAD-ASSET (open-thread #4c — đóng nốt):** 22→23 path — bồi `listAssets` (`imm00.list_assets:309`, bare `@whitelist` nhận GET) đóng tra-cứu thiết bị MVP-flow-5. 200 = oneOf `[AssetListEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C7, 0 discriminator); `AssetListEnvelope` rows-key `data.items[]` (mirror `IncidentListEnvelope`, handler `imm00.py:450` trả `_ok({pagination,items})` — KHÁC `data.data[]`); element `AssetListItem` field-disjoint GROUNDED `imm00.list_assets` @source = **20 field** (14 base `imm00.py:427-434` + 6 enrich `imm00.py:444-449`); 3 financial-field (`gross_purchase_amount`/`accumulated_depreciation`/`current_book_value`) handler CÓ trả `@:433` NHƯNG EXCLUDED khỏi list-item (LL-BE-57 mobile-meta no-financial — KTV không cần giá); 0 Check field → 0 prop boolean (Open#1 né sẵn); `lifecycle_status` = string KHÔNG enum cứng (Select nhiều giá trị); param **7 DISCRETE query-string** (`lifecycle_status`/`department`/`location`/`asset_category`/`search`/`gmdn_code`/`byt_status` — optional string default'') + REUSE `Page`/`PageSize` (mirror `listPmSchedules` discrete, KHÔNG JSON `filters`). KHÔNG sửa `api/imm00.py` (handler sẵn + bare whitelist nhận GET + KHÔNG cần `parse_json` — param discrete) ⇒ KHÔNG reload gunicorn (khác imm09 round 4). Guard `TestMobileListAssetsContract` (+7 TC, `test_mob_oas_asset_a..g`) ⇒ `_EXPECTED_TEST_COUNT` 174→181; `_MVP_LIST_ENVELOPE` 5→6 ⇒ C7 sweep (31e) + C5 precond (23a) tự cover. **C-LISTREAD-USERS (open-thread #4 closure):** 23→24 path — bồi `listUsers` (`assetcore.api.user.list_users:268`, bare `@whitelist` nhận GET) đóng required-field cụt cho `createCalibration.technician` (imm11) + `assign_technician.technician`: picker cần API liệt-kê User. 200 = oneOf `[UserListEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C7, 0 discriminator); `UserListEnvelope` rows-key `data.items[]` (mirror `AssetListEnvelope`/`IncidentListEnvelope`, handler `user.py:367` trả `_ok({items,pagination})` — KHÁC `data.data[]`); element `UserListItem` field-disjoint GROUNDED `user.list_users` emit @source = **10 field** (`name`/`full_name`/`email`/`enabled`/`user_image`/`role_profile_name`/`department_name`/`is_active`/`imm_approval_status`/`imm_roles[]`); `enabled`+`is_active` = `User.enabled` Check-fieldtype → `integer enum[0,1]` (Open#1 int-vs-bool né — KHÔNG boolean, chống strict-codegen Dart/Kotlin deser crash); `imm_roles[]` = object array `{name,label,group}` (sanitize qua `ROLE_METADATA` `user.py:359-364`, KHÔNG raw role child-table thô); raw `ac_department` (Link id) EXCLUDED khỏi list-item (mobile-meta no raw internal id — `department_name` mang giá trị readable, mirror `AssetListItem`); 0 financial (`list_users` vốn KHÔNG trả) + KHÔNG leak `password`/`api_secret`/`api_key`; param **6 DISCRETE query-string** (`search`/`department`/`role`/`approval_status` — optional string default''; `is_active` — integer enum[0,1] optional) + REUSE `Page`/`PageSize` (mirror `listPmSchedules`/`listAssets` discrete, KHÔNG JSON `filters`); `role.enum` = `_IMM_ROLES` canonical (`assetcore.services.shared.constants.Roles.ALL` — 4 System + 26 Domain). KHÔNG sửa `api/user.py` (handler sẵn + bare whitelist nhận GET + param discrete) ⇒ KHÔNG reload gunicorn. Guard `TestMobileListUsersContract` (+9 TC) ⇒ `_EXPECTED_TEST_COUNT` 197→206; `_MVP_LIST_ENVELOPE` 6→7 ⇒ C7 sweep (31e) + C5 precond (23a) tự cover. **C8-ACTION (POST-action lifecycle ĐẦU TIÊN):** 24→25 path — bồi `acknowledgeIncident` (`imm12.acknowledge_incident:233`, `@whitelist(methods=['POST'])`) chuyển Incident `Open→Acknowledged` để màn incident-detail (`getIncident`, C6-DETAIL) thoát dead-end read-only — thiết lập pattern **POST-action route-by-VALUE** (mirror create/list nhưng action-on-existing). POST-ONLY (path CHỈ key `post`, khớp `methods=['POST']`). requestBody INLINE `application/json` `AcknowledgeIncidentRequest` {`name` string REQUIRED, `notes`/`assigned_to` string optional default''} `additionalProperties:false` — khớp signature `acknowledge_incident(name, notes='', assigned_to='')` @imm12.py:234 (`name` positional bắt buộc, 2 còn lại default). 200 = oneOf `[IncidentActionEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C6/C7, 0 discriminator); `IncidentActionEnvelope.data` = `IncidentActionResponse` closed {`name`,`status`} khớp `svc_acknowledge` return `{'name':name,'status':doc.status}` @services/imm12.py:469 (`status` Select-canonical, post-ack = `Acknowledged`). **403 = SINGLE-SHAPE `Forbidden`** (dispatcher-403 guest/no-token via `_can_investigate=rbac.can` imm12.py:240; in-handler cap-403 imm12.py:241 ĐÃ phủ bởi nhánh Error 200-oneOf) — **KHÁC `reportIncident` DUAL-403** (`ReportIncidentForbidden`); 401 = `Unauthorized401`. KHÔNG sửa `api/imm12.py`/`services/imm12.py` (handler+methods+role-gate sẵn @imm12.py:233) ⇒ KHÔNG reload gunicorn. Vào `_MVP_BUSINESS_PATHS` ⇒ 401/403 symmetry set tự +1 (test so SET, KHÔNG literal). Guard `TestMobileAcknowledgeIncidentContract` (+9 TC, `test_mob_oas_ack_a..i` — incl. TC-i live-signature parity `inspect.signature(imm12.acknowledge_incident)=={name,notes,assigned_to}`, `name` no-default + `notes`/`assigned_to` default'', chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 211→220. **C8-ACTION startRepair (POST-action lifecycle ĐẦU TIÊN cho Asset Repair):** 25→26 path — bồi `startRepair` (`imm09.start_repair:71`, `@whitelist(methods=['POST'])`) chuyển Asset Repair `Assigned/Diagnosing/Pending Parts → In Repair` (svc.start_repair services/imm09.py:831,844) để màn repair-detail (`getRepairWorkOrder`, C6-DETAIL) thoát dead-end read-only — nối tiếp pattern **POST-action route-by-VALUE** (mirror `acknowledgeIncident`, action-on-existing). POST-ONLY (path CHỈ key `post`, khớp `methods=['POST']` @api/imm09.py:71-72). requestBody INLINE `application/json` `StartRepairRequest` {`name` string REQUIRED} `additionalProperties:false` — khớp signature `start_repair(name)` @api/imm09.py:72 (`name` positional bắt buộc, **0 optional param**); KHÔNG oneOf json+form (đơn-field action). 200 = oneOf `[RepairActionEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C6/C7, 0 discriminator); `RepairActionEnvelope.data` = `RepairActionResponse` closed {`name`,`status`} khớp `svc.start_repair` return `{'name':name,'status':RepairStatus.IN_REPAIR}` @services/imm09.py:847 (`status`='In Repair', RepairStatus-canonical 9-state @services/imm09.py:30-39). **KHÔNG dùng `IncidentActionResponse` cross-domain** (field-disjoint C3-split discipline: repair-enum 9-state ≠ incident-enum 7-state). **403 = SINGLE-SHAPE `Forbidden`** (dispatcher-403 guest/no-token; in-handler cap-403 `repair.write` via `rbac.require` @api/imm09.py:73 ĐÃ phủ bởi nhánh Error 200-oneOf) — **KHÁC `reportIncident` DUAL-403**; 401 = `Unauthorized401`. KHÔNG sửa `api/imm09.py`/`services/imm09.py` (handler + `methods=['POST']` + cap-gate `repair.write` sẵn @source) ⇒ KHÔNG reload gunicorn. Vào `_MVP_BUSINESS_PATHS` ⇒ 401/403 symmetry set tự +1 (test so SET, KHÔNG literal). Guard `TestMobileStartRepairContract` (+9 TC, `test_mob_oas_startrepair_a..i` — incl. TC-i live-signature parity `inspect.signature(imm09.start_repair)=={name}`, `name` no-default, chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 220→229. **C8-ACTION startWork (POST-action lifecycle THỨ HAI cho Incident):** 26→27 path — bồi `startWork` (`imm12.start_work:245`, `@whitelist(methods=['POST'])`) chuyển Incident `Acknowledged → In Progress` (svc_start_work services/imm12.py:470) nối tiếp `acknowledgeIncident` cùng domain IMM-12 để màn incident-detail (`getIncident`, C6-DETAIL) tiến tiếp flow xử lý SAU khi tiếp nhận. POST-ONLY (path CHỈ key `post`, khớp `methods=['POST']` @api/imm12.py:245). requestBody INLINE `application/json` `StartWorkRequest` {`name` string REQUIRED, `notes` string optional default''} `additionalProperties:false` — khớp signature `start_work(name, notes='')` @api/imm12.py:246 (`name` positional bắt buộc, `notes` default); **KHÔNG có `assigned_to`** (KHÁC `AcknowledgeIncidentRequest` — `start_work` auto-gán `doc.assigned_to=session.user` nếu trống @services/imm12.py:483, server-side). 200 = oneOf `[IncidentActionEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C6/C7, 0 discriminator); **REUSE `IncidentActionEnvelope` + `IncidentActionResponse`** {`name`,`status`} (cùng domain IMM-12, data đồng-dạng) khớp `svc_start_work` return `{'name':name,'status':doc.status}` @services/imm12.py:491 (`status`='In Progress', `_STATUS_INVESTIGATING` @services/imm12.py:43 — thuộc enum 7-state có sẵn) ⇒ **KHÔNG sinh envelope/response schema mới** (KHÁC startRepair vốn có `RepairActionEnvelope`/`RepairActionResponse` riêng vì repair-enum 9-state ≠ incident-enum 7-state). **403 = SINGLE-SHAPE `Forbidden`** (dispatcher-403 guest/no-token; in-handler cap-403 `corrective.investigate` via `_can_investigate=rbac.can` @api/imm12.py:252 ĐÃ phủ bởi nhánh Error 200-oneOf) — **KHÁC `reportIncident` DUAL-403**, mirror `acknowledgeIncident`; 401 = `Unauthorized401`. KHÔNG sửa `api/imm12.py`/`services/imm12.py` (handler + `methods=['POST']` + cap-gate sẵn @source) ⇒ KHÔNG reload gunicorn. Vào `_MVP_BUSINESS_PATHS` + `_MVP_ACTION_ENVELOPE` ⇒ 401/403 symmetry set tự +1 (test so SET, KHÔNG literal). Guard `TestMobileStartWorkContract` (+9 TC, `test_mob_oas_startwork_a..i` — incl. TC-i live-signature parity `inspect.signature(imm12.start_work)=={name,notes}`, `name` no-default + `notes` default'', chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 229→238. **C8-ACTION resolveIncident (POST-action lifecycle THỨ BA cho Incident):** 27→28 path — bồi `resolveIncident` (`imm12.resolve_incident:257`, `@whitelist(methods=['POST'])`) chuyển Incident `In Progress → Resolved` (svc_resolve services/imm12.py:494) nối tiếp `acknowledgeIncident`+`startWork` cùng domain IMM-12 để màn incident-detail đóng vòng xử lý (ghi `resolution_notes`+`root_cause`, auto-RCA High/Critical). POST-ONLY (path CHỈ key `post`, khớp `methods=['POST']` @api/imm12.py:257). requestBody INLINE `application/json` `ResolveIncidentRequest` {`name` string REQUIRED, `resolution_notes` string REQUIRED, `root_cause` string optional default''} `additionalProperties:false` — khớp signature `resolve_incident(name, resolution_notes, root_cause='')` @api/imm12.py:258 (`name`+`resolution_notes` positional no-default ⇒ 2 required; `root_cause` default'' ⇒ optional). 200 = oneOf `[ResolveIncidentEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C6/C7, 0 discriminator); **`ResolveIncidentEnvelope.data` = `ResolveIncidentResponse` RIÊNG closed {`name`,`status`,`rca_created` (string|null nullable)}** — **KHÔNG reuse `IncidentActionResponse` {name,status}** (lần ĐẦU domain IMM-12 data ≠ {name,status}: service trả thêm `rca_created` RCA auto-create High/Critical @services/imm12.py:530, `rca_name:str|None` @:519) khớp `svc_resolve` return `{'name':name,'status':doc.status,'rca_created':rca_name}` (`status`='Resolved', `_STATUS_RESOLVED` @services/imm12.py:44 — thuộc enum 7-state có sẵn). **403 = SINGLE-SHAPE `Forbidden`** (dispatcher-403 guest/no-token; in-handler cap-403 `corrective.investigate` via `_can_investigate` @api/imm12.py:264-265 ĐÃ phủ bởi nhánh Error 200-oneOf) — **KHÁC `reportIncident` DUAL-403**, mirror `acknowledgeIncident`/`startWork`; 401 = `Unauthorized401`. KHÔNG sửa `api/imm12.py`/`services/imm12.py` (handler + `methods=['POST']` + cap-gate + return-shape `{name,status,rca_created}` sẵn @source) ⇒ KHÔNG reload gunicorn. Vào `_MVP_BUSINESS_PATHS` + `_MVP_ACTION_ENVELOPE` ⇒ 401/403 symmetry set tự +1 (test so SET, KHÔNG literal). Guard `TestMobileResolveIncidentContract` (+9 TC, `test_mob_oas_resolve_a..i` — incl. TC-i live-signature parity `inspect.signature(imm12.resolve_incident)=={name,resolution_notes,root_cause}`, `name`+`resolution_notes` no-default + `root_cause` default'', chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 238→247. **C8-ACTION-PM submitPmResult (POST-action lifecycle ĐẦU TIÊN cho PM Work Order):** 28→29 path — bồi `submitPmResult` (`imm08.submit_pm_result:54`) chuyển PM Work Order `Assigned/In Progress → Completed` (svc.submit_result services/imm08.py:655 → `wo.status=PMStatus.COMPLETED` imm08.py:671 + `wo.submit()` imm08.py:679 → lifecycle event `pm_completed` + asset→Active imm08.py:686) — ĐÓNG dead-end flow-5: KTV mở `getPmWorkOrder` detail nhưng KHÔNG có endpoint hoàn thành PM. **KHÁC action incident/repair**: (1) mang child-array `checklist_results[]` (nested `PmChecklistResultInput`) thay vì single `name`; (2) response 5-field PM-riêng. requestBody INLINE `application/json` `SubmitPmResultRequest` {`name` string REQUIRED; `checklist_results` array<PmChecklistResultInput> default []; `overall_result` string default 'Pass'; `technician_notes` string default ''; `pm_sticker_attached` integer enum[0,1] default 0; `duration_minutes` integer default 0} `additionalProperties:false` — khớp signature `submit_pm_result(name, checklist_results='[]', overall_result='Pass', technician_notes='', pm_sticker_attached=0, duration_minutes=0)` @api/imm08.py:55 (`name` positional bắt buộc, 5 còn lại default). `PmChecklistResultInput` closed {`idx` integer REQUIRED, `result` string, `measured_value` string nullable, `notes` string default ''} — khớp `result_map`/row-write @services/imm08.py:659-665. 200 = oneOf `[PmSubmitResultEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C6/C7, 0 discriminator); **`PmSubmitResultEnvelope.data` = `PmSubmitResultResponse` RIÊNG closed {`name`,`new_status`='Completed',`is_late` boolean,`next_pm_date` date-string,`cm_wo_created` string|null nullable}** — **KHÔNG reuse `Repair`/`IncidentActionResponse`** (C3-split cross-domain: `new_status`≠field-name `status` + 3 field PM-riêng) khớp return `{name,new_status,is_late,next_pm_date,cm_wo_created}` @services/imm08.py:705-711 (`is_late` genuine `bool()` @:707; `cm_wo_created` = Corrective WO auto-spawn khi Fail @:703-704,710; `next_pm_date` = compute_next_pm_date @:709; `new_status` PMStatus-canonical 7-state 1:1 `pm_work_order.json` Select). **403 = SINGLE-SHAPE `Forbidden`** (dispatcher-403 guest/no-token; in-handler cap-403 `pm.submit` via `rbac.require` @api/imm08.py:58 ĐÃ phủ bởi nhánh Error 200-oneOf) — **KHÁC `reportIncident` DUAL-403**; 401 = `Unauthorized401`. ⚠️ **DIVERGENCE method-verb**: BE @api/imm08.py:54 = **bare `@frappe.whitelist()`** (KHÔNG `methods=['POST']`) ⇒ runtime NHẬN cả GET; contract khai POST vì write-action (mutate docstatus/status, sinh event — không idempotent). Test guard assert path POST tồn-tại + chữ-ký THẬT (KHÔNG assert POST-ONLY-vì-source — anti-false-green). KHÔNG sửa `api/imm08.py`/`services/imm08.py` round này ⇒ KHÔNG reload gunicorn. Vào `_MVP_BUSINESS_PATHS` + `_MVP_ACTION_ENVELOPE` ⇒ 401/403 symmetry set tự +1 (test so SET, KHÔNG literal). Guard `TestMobileSubmitPmResultContract` (+9 TC, `test_mob_oas_submitpm_a..i` — incl. TC-i live-signature parity `inspect.signature(imm08.submit_pm_result)=={name,checklist_results,overall_result,technician_notes,pm_sticker_attached,duration_minutes}`, `name` no-default + 5 default, chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 247→256. **C8-ACTION closeWorkOrder (POST-action lifecycle TERMINAL cho Repair Work Order):** 29→30 path — bồi `closeWorkOrder` (`imm09.close_work_order:84`, `@whitelist(methods=['POST'])` VERIFIED) chuyển Asset Repair `In Repair → Pending Inspection` (svc.close_work_order services/imm09.py:946,958 — chờ nghiệm thu `confirm_inspection`, KHÔNG nhảy thẳng Completed) HOẶC khi `cannot_repair=1`: `In Repair/Diagnosing/Pending Parts/Assigned → Cannot Repair` (TERMINAL, Asset→Out of Service; `_mark_cannot_repair` services/imm09.py:1005,1015) — ĐÓNG dead-end SAU `startRepair`: KTV bắt đầu sửa (→ In Repair) nhưng KHÔNG có endpoint hoàn thành. **KHÁC mọi action đã làm**: (1) **2 child-array RIÊNG** trong request — `checklist_results[]` (`CloseWorkOrderChecklistInput` closed {`idx` integer REQUIRED, `test_description` string, `result` string, `measured_value` string nullable, `notes` string default''} GROUNDED `_apply_checklist` @services/imm09.py:1019-1040 — field-name `test_description` ≠ PM `description` ⇒ **KHÔNG reuse `PmChecklistResultInput`**) + `spare_parts[]` (loose object passthrough `_apply_spare_parts` @services/imm09.py:1042); (2) `dept_head_name` **conditional-required** ở SERVICE (CM-013 @services/imm09.py:929 .strip() rỗng → IMM09_DEPT_HEAD_REQUIRED khi `cannot_repair=0`; `cannot_repair=1` KHÔNG cần) — mô-tả ở description, **KHÔNG ép schema-required** (dù 4th param no-default ở signature); (3) **nhánh `cannot_repair`** trả `status='Cannot Repair'` ⇒ `status` enum gồm CẢ `'Pending Inspection'` (happy @:958) VÀ `'Cannot Repair'` (@:1005). requestBody INLINE `application/json` `CloseWorkOrderRequest` `additionalProperties:false`: **3 required no-default** (`name`, `repair_summary`, `root_cause_category` positional @api/imm09.py:85) + 7 optional default (`dept_head_name`, `checklist_results` array<CloseWorkOrderChecklistInput> default[], `spare_parts` array default[], `firmware_updated` integer enum[0,1] default0, `firmware_change_request` string default'', `cannot_repair` integer enum[0,1] default0, `cannot_repair_reason` string default'') — khớp signature VERIFIED `close_work_order(name, repair_summary, root_cause_category, dept_head_name, checklist_results='[]', spare_parts='[]', firmware_updated=0, firmware_change_request='', cannot_repair=0, cannot_repair_reason='')` @api/imm09.py:85-89; `firmware_updated`/`cannot_repair` = `integer enum[0,1]` (Open#1 int-vs-bool né — KHÔNG boolean, chống strict-codegen deser crash). 200 = oneOf `[CloseWorkOrderEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C6/C7, 0 discriminator); **`CloseWorkOrderEnvelope.data` = `CloseWorkOrderResponse` RIÊNG closed {`name`, `status` enum['Pending Inspection','Cannot Repair'], `mttr_hours` number nullable, `sla_breached` integer enum[0,1] nullable}** — **KHÔNG reuse `RepairActionResponse` {name,status}** (close trả thêm mttr_hours/sla_breached) GROUNDED happy return @services/imm09.py:956-961 + `_mark_cannot_repair` return @:1015-1016 (`mttr_hours`/`sla_breached` absent nhánh cannot_repair ⇒ nullable). **403 = SINGLE-SHAPE `Forbidden`** (dispatcher-403 guest/no-token; in-handler cap-403 `repair.submit` via `rbac.require` @api/imm09.py:90 ĐÃ phủ bởi nhánh Error 200-oneOf) — **KHÁC `reportIncident` DUAL-403**, mirror `startRepair`/`startWork`; 401 = `Unauthorized401`. KHÔNG sửa `api/imm09.py`/`services/imm09.py` (handler + `methods=['POST']` @api/imm09.py:84 + cap-gate `repair.submit` + return-shape `{name,status,mttr_hours,sla_breached}` sẵn @source) ⇒ KHÔNG reload gunicorn. Vào `_MVP_BUSINESS_PATHS` + `_MVP_ACTION_ENVELOPE` ⇒ 401/403 symmetry set tự +1 (test so SET, KHÔNG literal). Guard `TestMobileCloseWorkOrderContract` (+9 TC, `test_mob_oas_closewo_a..i` — incl. TC-i live-signature parity `inspect.signature(imm09.close_work_order)` 10-field, no-default-set THẬT = `{name,repair_summary,root_cause_category,dept_head_name}` 4-field — chênh vs contract.required 3-field = exactly `{dept_head_name}` documented conditional-required divergence, chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 256→265. **C8-ACTION submitCalibration (POST-action lifecycle COMPLETION/TERMINAL cho Calibration Record):** 30→31 path — bồi `submitCalibration` (`imm11.submit_calibration:114`) nâng Calibration Record `docstatus 0→1`, status→`Passed`/`Failed`/`Conditionally Passed` (svc.submit_calibration → `CalibrationRepo.submit` services/imm11.py:1047,1053) — thành viên THỨ BA họ completion-action (sau `submitPmResult` + `closeWorkOrder`), mắt xích CUỐI chuỗi `createCalibration→complete`, ĐÓNG dead-end tab Calibration MVP-flow-5: KTV mở `getCalibration` detail nhưng KHÔNG có endpoint hoàn thành hiệu chuẩn. **KHÁC mọi action đã làm**: response RIÊNG 4-key `{name,status,overall_result,next_calibration_date}` (`overall_result` Select 4-value + `next_calibration_date` date-string-or-empty nullable). requestBody INLINE `application/json` `SubmitCalibrationRequest` {`name` string REQUIRED} `additionalProperties:false` — khớp signature `submit_calibration(name)` @api/imm11.py:115 (`name` positional bắt buộc, **0 optional param**); KHÔNG oneOf json+form (đơn-field action mirror `StartRepairRequest`). 200 = oneOf `[SubmitCalibrationEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C6/C7, 0 discriminator); **`SubmitCalibrationEnvelope.data` = `SubmitCalibrationResponse` RIÊNG closed {`name`, `status` enum 8-value khớp Select `imm_asset_calibration.json` [Scheduled,Sent to Lab,In Progress,Certificate Received,Passed,Failed,Conditionally Passed,Cancelled], `overall_result` enum 4-value ['',Passed,Failed,Conditionally Passed], `next_calibration_date` string nullable}** — **KHÔNG reuse `Pm`/`Repair`/`IncidentActionResponse`** (C3-split cross-domain) GROUNDED return `{name,status,overall_result,next_calibration_date}` @services/imm11.py:1054-1059 (`status`=doc.status @:1056, `overall_result`=doc.overall_result @:1057, `next_calibration_date`=str(doc.next_calibration_date or '') @:1058). **403 = SINGLE-SHAPE `Forbidden`** (dispatcher-403 guest/no-token; in-handler cap-403 `calibration.submit` via `rbac.require` @api/imm11.py:116 ĐÃ phủ bởi nhánh Error 200-oneOf) — **KHÁC `reportIncident` DUAL-403**, mirror `startRepair`/`closeWorkOrder`; 401 = `Unauthorized401`. ⚠️ **DIVERGENCE method-verb**: BE @api/imm11.py:114 = **bare `@frappe.whitelist()`** (KHÔNG `methods=['POST']`) ⇒ runtime NHẬN cả GET; contract khai POST vì write-action (mutate docstatus 0→1 — không idempotent). Test guard assert path POST tồn-tại + chữ-ký THẬT (KHÔNG assert POST-ONLY-vì-source — anti-false-green); `submit_calibration` vào `_PARITY_VERB_ALLOWLIST` (3rd entry — cùng `create_calibration` + `submit_pm_result`; 25c #3 + 25f cardinality 2→3). KHÔNG sửa `api/imm11.py`/`services/imm11.py` round này ⇒ KHÔNG reload gunicorn. Vào `_MVP_BUSINESS_PATHS` + `_MVP_ACTION_ENVELOPE` ⇒ 401/403 symmetry set tự +1 (test so SET, KHÔNG literal). Guard `TestMobileSubmitCalibrationContract` (+9 TC, `test_mob_oas_submitcal_a..i` — incl. TC-i live-signature parity `inspect.signature(imm11.submit_calibration)=={name}`, `name` no-default, chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 265→274. **C8-ACTION closeIncident (POST-action lifecycle TERMINAL cho Incident):** 31→32 path — bồi `closeIncident` (`imm12.close_incident:270`, `@whitelist(methods=['POST'])` SẴN CÓ — clean POST, KHÔNG verb-divergence) chuyển Incident `Resolved → Closed` (svc_close services/imm12.py:533) — **đóng mắt xích CUỐI** chuỗi `report→ack→start→resolve→close` (cùng domain IMM-12), ĐÓNG hoàn toàn vòng-đời incident sau khi đã Resolved: Workshop Lead/QA Officer mở `getIncident` detail → có nút "Đóng sự cố". POST-ONLY (path CHỈ key `post`, khớp `methods=['POST']` @api/imm12.py:270). requestBody INLINE `application/json` `CloseIncidentRequest` {`name` string REQUIRED, `verification_notes` string optional default''} `additionalProperties:false` — khớp signature `close_incident(name, verification_notes='')` @api/imm12.py:271 (`name` positional bắt buộc, `verification_notes` default'' → append `"[Closed] <notes>"` vào `resolution_notes` @services/imm12.py:556-557). 200 = oneOf `[CloseIncidentEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C6/C7, 0 discriminator); **`CloseIncidentEnvelope.data` = `CloseIncidentResponse` RIÊNG closed {`name`,`status`='Closed',`closed_date` string format date NON-nullable}** — **KHÔNG reuse `IncidentActionResponse` {name,status}** (service trả thêm field thứ ba `closed_date`) **NÊN KHÔNG reuse `ResolveIncidentResponse` {name,status,rca_created}** (`closed_date`≠`rca_created` — C3-split field-disjoint) khớp `svc_close` return `{'name':name,'status':doc.status,'closed_date':doc.closed_date}` @services/imm12.py:569 (`status`='Closed', `_STATUS_CLOSED` @services/imm12.py:45 — thuộc enum 7-state có sẵn; `closed_date`=today() set UNCONDITIONAL @:555 ⇒ LUÔN có giá-trị, KHÁC `rca_created`/`next_calibration_date` nullable). **BR-12-02 RCA-gate**: Major/Critical → phải có RCA `Completed` trước Close (chưa đủ → Error IMM12_CLOSE_RCA_INCOMPLETE/IMM12_CLOSE_RCA_REQUIRED @200 @services/imm12.py:541-550); asset-restore Out of Service→Active @:562-566. **403 = SINGLE-SHAPE `Forbidden`** (dispatcher-403 guest/no-token; in-handler cap-403 `incident.close` via `_can_close` @api/imm12.py:277, message 'Không có quyền đóng Incident (cần Workshop Lead hoặc QA Officer)' ĐÃ phủ bởi nhánh Error 200-oneOf) — **KHÁC `reportIncident` DUAL-403**, mirror `acknowledgeIncident`/`startWork`/`resolveIncident`; 401 = `Unauthorized401`. **cap KHÁC** acknowledge/start/resolve `corrective.investigate`: close cần **Workshop Lead HOẶC QA Officer**. ✅ **KHÔNG verb-divergence** (decorator `methods=['POST']` SẴN @api/imm12.py:270 — KHÁC `submitPmResult`/`submitCalibration` bare-whitelist; KHÔNG vào `_PARITY_VERB_ALLOWLIST`, no ADR-fix needed). KHÔNG sửa `api/imm12.py`/`services/imm12.py` (handler + `methods=['POST']` + cap-gate `incident.close` + return-shape `{name,status,closed_date}` sẵn @source) ⇒ KHÔNG reload gunicorn. Vào `_MVP_BUSINESS_PATHS` + `_MVP_ACTION_ENVELOPE` ⇒ 401/403 symmetry set tự +1 (test so SET, KHÔNG literal). Guard `TestMobileCloseIncidentContract` (+9 TC, `test_mob_oas_close_a..i` — incl. TC-a POST-ONLY assert (source THẬT POST-only, KHÔNG anti-false-green) + TC-i live-signature parity `inspect.signature(imm12.close_incident)=={name,verification_notes}`, `name` no-default + `verification_notes` default'', chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 274→283. **C-LISTREAD-NOTIF listNotifications (in-app notification list — đóng gap đọc-lịch-sử flow-6 push):** 32→33 path — bồi `listNotifications` (`assetcore.api.layout.list_notifications:72`, bare `@frappe.whitelist()` nhận GET — đã wire web FE) đóng gap **đọc-lịch-sử** của flow-6 push: push (FCM) chỉ giao 1 deep-link/event đơn lẻ; tab Notifications mobile cần API liệt-kê ĐẦY ĐỦ thông báo đã-đọc + chưa-đọc (chuông). **KHÁC 19 đề mục mobile-BE trước** (20 đề mục = push payload `PushMessageData` + device-token register; CHƯA ai bồi in-app notification LIST từ `api/layout.py`). 3 param **DISCRETE** query-string (`page`/`page_size` REUSE `Page`/`PageSize` int + `only_unread` integer enum[0,1] default 0 — `only_unread=1` lọc `read=0` @layout.py:83-84) — KHÔNG JSON `filters`. 200 = oneOf `[NotificationListEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C7, 0 discriminator); `NotificationListEnvelope` rows-key `data.items[]` (mirror `IncidentListEnvelope`/`AssetListEnvelope`, handler `layout.py:96-99` trả `_ok({pagination,items})` — KHÁC `data.data[]`); pagination = `$ref Pagination` **5-key WITH offset** (handler GỌI `paginate()` @layout.py:87 → `{page,page_size,total,total_pages,offset}` — KHÁC `UserListEnvelope` inline 4-key no-offset; KHÔNG strip offset vì runtime EMIT, mirror IncidentListEnvelope). Element `NotificationListItem` = **ĐÚNG 9 emit field** `_serialize_notification` @layout.py:33-43 (`name`/`subject`/`content`/`document_type`/`document_name`/`type`/`from_user`/`read`/`creation`); `read` = `int(row.get('read'))` @:41 → `integer enum[0,1]` (Open#1 int-vs-bool né — KHÔNG boolean, chống strict-codegen Dart/Kotlin deser crash); 3 field nullable (`from_user`/`document_type`/`document_name` — Notification Log optional Link/sender). KHÔNG sửa `api/layout.py` (handler sẵn + bare whitelist nhận GET + param discrete + đã wire web FE) ⇒ KHÔNG reload gunicorn. Vào `_MVP_BUSINESS_PATHS` ⇒ 401/403 symmetry set tự +1 (test so SET, KHÔNG literal); runtime `openapi.generate_spec()` CÓ serve path (parity 25b GREEN, verb get=get KHÔNG vào `_PARITY_VERB_ALLOWLIST`). Guard `TestMobileListNotificationsContract` (+9 TC, `test_*_a..i` — incl. live-sig parity `inspect.signature(layout.list_notifications)=={page,page_size,only_unread}` + `_serialize_notification` live-emit key-set == contract 9-field, chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 283→292. **[BACKLOG round-riêng — HARD-STOP USER]** `imm11.submit_calibration` @api/imm11.py:114 hiện **bare `@frappe.whitelist()`** (nhận cả GET) — write-action nên thêm `methods=['POST']` (mirror `imm11.send_to_lab:159` / `cancel_calibration:186` đã POST-only) cho khớp contract POST; fix kèm reload gunicorn = HARD-STOP USER (KHÔNG sửa round này). **[OPEN-ISSUE — BA/QA]** nhánh `cannot_repair` return `{name,status,asset_status}` @services/imm09.py:1015-1016 mang field thừa `asset_status` (KHÔNG có trong closed 4-key `CloseWorkOrderResponse`) — runtime sẽ vi phạm `additionalProperties:false` ở nhánh cannot-repair; cần BA chốt: (a) bồi `asset_status` vào contract HOẶC (b) BE strip `asset_status` khỏi return (fix kèm reload = HARD-STOP USER). **[BACKLOG round-riêng — HARD-STOP USER]** `imm08.submit_pm_result` @api/imm08.py:54 hiện **bare `@frappe.whitelist()`** (nhận cả GET) — write-action nên thêm `methods=['POST']` (mirror `imm09.start_repair:71` / `imm12.start_work:245`) cho khớp contract POST; fix kèm reload gunicorn = HARD-STOP USER (KHÔNG sửa round này). **[BACKLOG round-riêng]** `user.list_users` hiện **bare `@frappe.whitelist()`** (KHÔNG cap-gate) — field-tech picker đọc danh sách User toàn hệ thống, chưa qua `rbac.require()`. Cần BA chốt cap đọc phù hợp (vd `user.read` / scoped technician) trước khi thêm gate (fix kèm reload = HARD-STOP USER); KHÔNG sửa round này (chưa có BA spec). **PM-DETAIL allowed_transitions (ASYMMETRY R3 ĐÓNG — KHÔNG path mới, 33 path GIỮ):** bồi property `allowed_transitions: array<string>` vào schema `PmWorkOrderDetail` (đã có từ C6-DETAIL) — server-driven CTA cho màn PM-detail render nút workflow theo server, **xoá client-side state-gate anti-pattern** (lifecycle/RBAC dead-gate, memory `factory_rounds_1_25`). MIRROR `IncidentDetail.allowed_transitions` (`imm12.py:778`, R3) — trước round này CHỈ Incident có; 3 detail khác (PM/Repair/Calibration) KHÔNG. **⚠️ ĐỘNG `.py` round này** (KHÁC các đề mục pure-yaml trước): `services/imm08.py` thêm map tập-trung `_PM_VALID_TRANSITIONS: dict[str,list[str]]` **GROUNDED chính xác** `imm_08_pm_workflow.json` (7 state / 13 transition: `Open→[In Progress,Overdue,Cancelled]`·`Overdue→[In Progress,Cancelled]`·`In Progress→[Completed,Halted–Major Failure,Pending–Device Busy,Cancelled]`·`Pending–Device Busy→[In Progress,Cancelled]`·`Halted–Major Failure→[In Progress,Cancelled]`·terminal `Completed`/`Cancelled→[]`) + `get_work_order` emit `allowed_transitions = _PM_VALID_TRANSITIONS.get(wo.status, [])` (mirror `get_incident_detail` imm12.py:778); **0 đổi** signature handler `getPmWorkOrder` (vẫn `handle(svc.get_work_order, name)`), **KHÔNG** đụng workflow-engine/submit/start. Schema `array<string>` **KHÔNG enum-bound cứng** (né drift khi workflow đổi — codomain-check ở guard phía service); **KHÔNG** thêm vào `required` (emit-luôn nhưng `required` GIỮ `['name']` đồng nhất IncidentDetail R3); `additionalProperties:true` GIỮ NGUYÊN. **handler đã wire web FE** (`get_work_order` là service hiện hữu) ⇒ ⚠️ thêm key vào dict return ⇒ **CẦN reload gunicorn để live HTTP** (HARD-STOP USER — handler `.py` đã sửa). Guard: `TestMobilePmAllowedTransitionsContract` (`test_mobile_oas` +6 TC a..f — shape array<string>/mirror IncidentDetail · NOT-required required GIỮ ['name'] · parity codomain⊆`PMStatus` enum (chống typo) · **SSoT-divergence map↔`imm_08_pm_workflow.json`** (chống drift) · terminal-rỗng · live-emit AST grounding) ⇒ `_EXPECTED_TEST_COUNT` 292→298; BE unit `TestPMAllowedTransitions` (`test_imm08` +2 TC — map-codomain ⊆ enum + khớp workflow JSON, `get_work_order` emit `allowed_transitions`==map[status] cho Open/In Progress/Completed-terminal). ADR đầy đủ: [`docs/imm-08/04_Backend_Design.md §3`](../imm-08/04_Backend_Design.md) (ADR-IMM08-CTA) + [`docs/imm-08/05_API_Specification.md`](../imm-08/05_API_Specification.md). **REPAIR-DETAIL allowed_transitions (ASYMMETRY R3 NỬA-REPAIR ĐÓNG — KHÔNG path mới, 33 path GIỮ):** bồi property `allowed_transitions: array<string>` vào schema `RepairWorkOrderDetail` (đã có từ C6-DETAIL) — server-driven CTA cho màn repair-detail render nút workflow theo server, **xoá client-side state-gate anti-pattern** (lifecycle/RBAC dead-gate). **Thành viên THỨ BA** có `allowed_transitions[]` (sau Incident R3 + PM R21), đóng **NỬA Repair** của ASYMMETRY R3 — nửa còn lại `CalibrationDetail` (state-machine riêng `imm_11_calibration_workflow.json`) = round-riêng sau cùng pattern. MIRROR `IncidentDetail`/`PmWorkOrderDetail.allowed_transitions` (`imm12.py:778` / `imm08.py:651`). **⚠️ ĐỘNG `.py` round này**: `services/imm09.py` thêm map tập-trung `_REPAIR_VALID_TRANSITIONS: dict[str,list[str]]` keyed bằng `RepairStatus.*` constants (KHÔNG literal) **GROUNDED chính xác edge-by-edge** `imm_09_repair_workflow.json` (9 state / 15 transition: `Open→[Assigned,Cancelled]`·`Assigned→[Diagnosing,Cancelled]`·`Diagnosing→[In Repair,Pending Parts,Cancelled]`·`Pending Parts→[In Repair,Cancelled]`·`In Repair→[Pending Inspection,Cannot Repair,Cancelled]`·`Pending Inspection→[Completed,In Repair,Cancelled]`·terminal `Completed`/`Cannot Repair`/`Cancelled→[]`) + `get_work_order` emit `allowed_transitions = _REPAIR_VALID_TRANSITIONS.get(doc.status, [])` (mirror `get_incident_detail` imm12.py:778 / imm08.py:651); **0 đổi** signature handler `getRepairWorkOrder` (vẫn `handle(svc.get_work_order, name)`), **KHÔNG** đụng workflow-engine/submit/start. Schema `array<string>` **KHÔNG enum-bound cứng** (né drift); **KHÔNG** thêm vào `required` (emit-luôn nhưng `required` GIỮ `['name']`); `additionalProperties:true` GIỮ NGUYÊN. **handler đã wire web FE** ⇒ ⚠️ thêm key vào dict return ⇒ **CẦN reload gunicorn để live HTTP** (HARD-STOP USER); guard chạy in-process KHÔNG cần. Guard: `TestMobileRepairAllowedTransitionsContract` (`test_mobile_oas` +6 TC a..f — shape array<string>/mirror IncidentDetail+PmWorkOrderDetail · NOT-required required GIỮ ['name'] · parity codomain⊆`RepairStatus` enum · **SSoT-divergence map↔`imm_09_repair_workflow.json` edge-by-edge** · terminal-rỗng · live-emit AST grounding) ⇒ `_EXPECTED_TEST_COUNT` 298→304; BE unit `TestRepairAllowedTransitions` (`test_imm09` +1 TC class/2 method — map-codomain ⊆ enum + khớp workflow JSON edges, `get_work_order` emit `allowed_transitions`==map[status] cho Open/In Repair/Completed-terminal); docset reconcile `_GUARD_SUITE_SUM` 441→447 + `_MOBILE_OAS_TOTAL` 467→473. ADR đầy đủ: [`docs/imm-09/04_Backend_Design.md §3.1`](../imm-09/04_Backend_Design.md) (ADR-IMM09-CTA) + [`docs/imm-09/05_API_Specification.md §3.2`](../imm-09/05_API_Specification.md). **[OUT-of-scope round này]** `CalibrationDetail.allowed_transitions` (nửa Calibration ASYMMETRY R3 — state-machine riêng → round-riêng sau cùng pattern). **C8-ACTION assignTechnician (lấp HỐ create→start):** 33→34 path — bồi `assign_technician` (`imm09.assign_technician` BE sẵn @`api/imm09.py:58` whitelisted POST + `services/imm09.py:833`) lấp mắt xích DISPATCH `createRepairWorkOrder → [assignTechnician] → startRepair` (giao WO cho KTV hiện trường, Open → Assigned). KHÁC startRepair (đơn-`{name}`): requestBody 2-required `{name,technician}` + optional `priority` (technician = đích tiêu thụ `listUsers` R23-picker); response RIÊNG 3-key `{name,status,assigned_to}` ⇒ `AssignTechnicianEnvelope`/`AssignTechnicianResponse` RIÊNG (service trả thêm `assigned_to` @`services/imm09.py:848` ⇒ KHÔNG reuse `RepairActionEnvelope` 2-key). 200 = oneOf `[AssignTechnicianEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C8-ACTION, 0 discriminator); 403 SINGLE-SHAPE dispatcher-403 (in-handler cap-403 `repair.write` phủ bởi nhánh Error 200-oneOf — mirror startRepair). PURE-YAML + guard test only (KHÔNG đụng `.py`). Guard `TestMobileAssignTechnicianContract` (+9 TC a..i) ⇒ `_EXPECTED_TEST_COUNT` 310→319; docset reconcile `_GUARD_SUITE_SUM` 453→462 + `_MOBILE_OAS_TOTAL` 479→488. ADR đầy đủ: [`docs/imm-09/04_Backend_Design.md §3.2`](../imm-09/04_Backend_Design.md) (ADR-IMM09-ASSIGN). **C8-ACTION submitDiagnosis (MẮT-XÍCH-GIỮA repair, lấp dead-end giữa assign và start/close):** 34→35 path — bồi `submit_diagnosis` (`imm09.submit_diagnosis` BE sẵn @`api/imm09.py:64` `@whitelist(methods=['POST'])` + `services/imm09.py:929`) lấp mắt xích GIỮA `createRepairWorkOrder → [assignTechnician] → [submitDiagnosis] → startRepair/closeWorkOrder`: KTV nộp kết quả chẩn đoán → rẽ nhánh theo `needs_parts`. Chuyển Asset Repair `Assigned/Diagnosing → In Repair | Pending Parts` (svc.submit_diagnosis @`services/imm09.py:938` `RepairStatus.PENDING_PARTS if int(needs_parts) else RepairStatus.IN_REPAIR`; `needs_parts=1` ⇒ Pending Parts + `enter_parts_hold` stamp `parts_hold_started` + ALE, SLA tạm dừng BR-09-10 @`services/imm09.py:941-942`). KHÁC startRepair (đơn-`{name}`): requestBody 2-required `{name,diagnosis_notes}` + optional `needs_parts` (**integer enum[0,1]** — handler ép `int(needs_parts)` @`api/imm09.py:68`; KHÔNG boolean, INT-vs-BOOL discipline Open#4 áp cho REQUEST body field, chống strict-codegen Dart/Kotlin deser crash). **REUSE `RepairActionEnvelope`/`RepairActionResponse`** `{name,status}` (service trả EXACT `{name,status}` @`services/imm09.py:950` — KHÔNG sinh response-schema mới, mirror startRepair cùng domain repair). 200 = oneOf `[RepairActionEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C8-ACTION, 0 discriminator); Error-on-HTTP-200 in-handler: BR-09 invalid-transition `IMM09_BAD_STATE` (status ∉ Assigned/Diagnosing → code `CONFLICT` http_status `409` @`messages.py:641-646`) + `IMM09_NOT_FOUND` (code `NOT_FOUND` http_status `404` @`messages.py:634-639`) + cap-403 `repair.write` (@`api/imm09.py:65`) gom vào nhánh Error 200-oneOf; `http_status` mọi nhánh ∈ bounded enum {400,401,403,404,409,413,422,429,500} (R11). 403 SINGLE-SHAPE dispatcher-403 (in-handler cap-403 phủ bởi nhánh Error — mirror startRepair/assignTechnician); 401 = `Unauthorized401`. **PURE-YAML + guard test only** (KHÔNG đụng `.py` — handler+service LIVE whitelisted, sig nguyên; KHÔNG reload/migrate/commit). Guard `TestMobileSubmitDiagnosisContract` (+9 TC a..i — incl. TC-i live-signature parity `inspect.signature(imm09.submit_diagnosis)=={name,diagnosis_notes,needs_parts}`, `name`+`diagnosis_notes` no-default + `needs_parts` default 0, chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 319→328; docset reconcile `_GUARD_SUITE_SUM` 462→471 + `_MOBILE_OAS_TOTAL` 488→497. SSoT: [`docs/imm-09/05_API_Specification.md §3.5`](../imm-09/05_API_Specification.md) + [`04-api-contract.md §8.11-bis`](./04-api-contract.md) (ADR-MOBILE-006). **FLOW-2 DEVICE-PROFILE getAssetIncidentHistory (lịch-sử sự-cố của asset, màn hồ-sơ sau quét QR — lấp dead-end sau getAssetScanInfo):** 35→36 path — bồi `get_asset_incident_history` (`imm12.get_asset_incident_history` BE sẵn @`api/imm12.py:173` bare `@frappe.whitelist()` nhận GET + `services/imm12.py:834-843`) lấp dead-end màn hồ-sơ-thiết-bị: `getAssetScanInfo` trả profile + `available_actions` của asset NHƯNG KHÔNG có endpoint liệt-kê lịch-sử sự-cố của asset đó (KTV quét QR → "máy này từng hỏng gì?"). **GET-read** (KHÁC 13 action POST trước): 2 query param `asset` (required string) + `limit` (optional integer default 10 — KHỚP signature `get_asset_incident_history(asset, limit=10)` @`api/imm12.py:173`; svc `limit_page_length=limit` @`services/imm12.py:841`). 200 = oneOf `[AssetIncidentHistoryEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C7 read-path, 0 discriminator); **`AssetIncidentHistoryEnvelope`** rows-key `data.items[]` + `data.required=[asset,items]` **KHÔNG pagination** (svc trả `{asset,items}` @`services/imm12.py:843` — chỉ `limit` cap, KHÁC `IncidentListEnvelope.data.required=[pagination,items]`); `data.asset` echo mã thiết bị query; `items[]` RỖNG hợp lệ nếu asset chưa từng có sự-cố (KHÔNG 404). Element **`AssetIncidentHistoryItem`** = **EXACT 9 field** GROUNDED `frappe.get_all(fields=[...])` @`services/imm12.py:838-839` (`name`/`incident_type`/`severity`/`status`/`reported_at`/`fault_code`/`closed_date`/`linked_capa`/`rca_record`), `additionalProperties:false`, `required=[name]`; **0 boolean/Check field** (incident_type/severity/status=Select, reported_at=Datetime, closed_date=Date, fault_code=Data, linked_capa/rca_record=Link) ⇒ int-vs-bool trap né sẵn (Open#1 — KHÁC `IncidentListItem` 28-field có rca_required/chronic_failure_flag/patient_affected Check→int). cap đọc = incident read (handle→svc permission-aware; KHÔNG cap-gate trong handler) → thiếu quyền = cap-403 Error-trên-HTTP-200. 401 = `Unauthorized401` (handler guard `frappe.session.user=='Guest'→_err 401` @`api/imm12.py:175-176`, SINGLE-SHAPE component uniform 36-path mirror `listIncidents`); 403 SINGLE-SHAPE `Forbidden`; response slot CHỈ {200,401,403} (asset∄ → items[] RỖNG KHÔNG 404). **PURE-YAML + guard test only** (KHÔNG đụng `.py` — handler+service LIVE whitelisted, sig nguyên; KHÔNG reload/migrate/commit). Guard `TestMobileGetAssetIncidentHistoryContract` (+9 TC a..i — incl. TC-i live-signature parity `inspect.signature(imm12.get_asset_incident_history)=={asset,limit}`, `asset` no-default + `limit` default 10, chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 328→337; docset reconcile `_GUARD_SUITE_SUM` 471→480 + `_MOBILE_OAS_TOTAL` 497→506. SSoT: [`04-api-contract.md`](./04-api-contract.md) (FLOW-2 device-profile) + `api/imm12.py:172-177` + `services/imm12.py:834-843`. **FLOW-1 BOOTSTRAP getUserContext (session who-am-I, màn home sau login — lấp hardcode "Đã đăng nhập" KHÔNG identity):** 36→37 path — bồi `get_user_context` (`layout.get_user_context` BE sẵn @`api/layout.py:188` `@frappe.whitelist(allow_guest=True)` GET 0-param + helper `_safe_get_user_basic`/`_enrich_from_user_custom_fields`/`_enrich_from_employee`) lấp dead-end POST-LOGIN: app home hiện hardcode "Đã đăng nhập" KHÔNG identity (session chỉ `{email,fullName}` parse từ login-resp) — cần roles/department/persona/profile-completeness cho persona-aware home + flow-gating. **GET-read 0-param** (signature `get_user_context()` — 0 positional/optional @`api/layout.py:188`); KHÔNG requestBody. 200 = oneOf `[UserContextEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C7 read-path, 0 discriminator, mirror `getAssetScanInfo`); **`UserContextEnvelope`** `additionalProperties:false` `required[success,data]` `success.enum[true]`; `data` = `$ref UserContextData`. Element **`UserContextData`** = **EXACT 13 prop** GROUNDED `_ok` payload @`layout.py:220-234` (`user`/`full_name`/`user_image`/`phone`/`role_profile_name`/`roles`/`imm_roles`/`designation`/`hr_docname`/`department`/`department_name`/`is_profile_completed`/`has_employee_link`), `additionalProperties:false`, `required[user]` (LUÔN có khi 200 = `frappe.session.user`; rest nullable theo **graceful-degradation** — AC User custom-field/Employee có thể vắng, `db.get_value` KHÔNG `get_doc`). **⚠️ INT-VS-BOOL TRAP**: 2 boolean field `is_profile_completed` (@:218 `bool(dept and designation)`) + `has_employee_link` (@:233 `emp.get(...,False)`) → **`integer enum[0,1]`** (KHÔNG `type:boolean`) — BE emit `bool()` Python true/false NHƯNG codegen strict-deser cần int-or-bool nhất quán contract (Open#1, mirror `IncidentListItem` Check→int); guard chống regress. `roles`/`imm_roles` = array `items.type:string` (`frappe.get_roles` list @:226-227); `user_image`/`phone`/`designation`/`hr_docname`/`department`/`department_name` = string nullable. **⚠️ allow_guest=True ⇒ KHÔNG dispatcher-403**: Guest VÀO handler → in-handler `_err 401` (@`api/layout.py:206-207`, Error-on-HTTP-200 — FE auth-store catch redirect /login KHÔNG log 403 console). 401 = `Unauthorized401` (SINGLE-SHAPE, uniform 37-path convention; slot 401 dùng component dù body in-handler Error — mirror `listIncidents`/`getAssetIncidentHistory`, KHÔNG drift). **response slot CHỈ {200,401}** (KHÔNG 403 — allow_guest KHÔNG cap-gate; mirror `openid_profile`/`getUserInfo` {200,401}); exempt 401/403 MVP-business symmetry via `_ALLOW_GUEST_PATHS` (ADR-MOBILE-008). **NOT ∈ `_MVP_BUSINESS_PATHS`/`_MVP_READ_ENVELOPE`/C5-registry** (allow_guest bootstrap ≠ business path; typed-200 phủ bởi guard riêng). **PURE-YAML + guard test only** (KHÔNG đụng `.py` — handler+helper LIVE whitelisted, sig nguyên; KHÔNG reload/migrate/commit). Guard `TestMobileGetUserContextContract` (+9 TC a..i — incl. TC-i live-signature parity `inspect.signature(layout.get_user_context)=={}` 0-param, chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 337→346; docset reconcile `_GUARD_SUITE_SUM` 480→489 + `_MOBILE_OAS_TOTAL` 506→515. SSoT: [`04-api-contract.md §8.19`](./04-api-contract.md) (FLOW-1 bootstrap) + [`ADR-MOBILE-008.md`](./ADR-MOBILE-008.md) (allow_guest exempt symmetry) + `api/layout.py:187-234`. **FLOW-5 TERMINAL-THẬT confirmInspection (nghiệm-thu sau sửa chữa, đóng dead-end CUỐI chuỗi repair):** 37→38 path — bồi `confirm_inspection` (`imm09.confirm_inspection` BE sẵn @`api/imm09.py:103-107` `@frappe.whitelist(methods=['POST'])` clean POST + `services/imm09.py:1086-1119`) lấp dead-end CUỐI: `closeWorkOrder` (R17) chỉ đưa WO về `Pending Inspection` (NON-terminal); `getRepairWorkOrder.allowed_transitions[]` (R22) ĐÃ surface CTA `Completed` NHƯNG KHÔNG có endpoint thực thi. confirm_inspection chuyển Asset Repair `Pending Inspection → Completed` (`doc.submit()` docstatus 0→1 → `on_submit` → `complete_repair()`: tính MTTR/SLA, Asset→Active, hook recalibration IMM-11 @`services/imm09.py:1103`). **cap `repair.submit`** (`rbac.require` @`api/imm09.py:106` — QA/trưởng-khoa nghiệm-thu, **KHÁC `repair.create` của KTV** ⇒ gate phê-duyệt-chất-lượng RIÊNG). **POST-only** (path CHỈ key `post`, VERIFIED `methods=['POST']` @`api/imm09.py:103` — clean POST, KHÔNG verb-divergence ⇒ TC-a assert POST-ONLY, KHÁC `submitPmResult`/`submitCalibration` bare-whitelist). requestBody `ConfirmInspectionRequest` `additionalProperties:false` `required[name]` CHỈ 1 field `name:string` — KHỚP signature `confirm_inspection(name)` @`api/imm09.py:104`; **content oneOf `application/json` + `application/x-www-form-urlencoded`** (CÙNG `$ref` — Frappe RPC form_dict, 04 §4/§9). 200 = oneOf `[ConfirmInspectionEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C8-ACTION, 0 discriminator); **`ConfirmInspectionEnvelope.data` = `ConfirmInspectionResponse` RIÊNG closed EXACT 4-prop {`name`, `status` enum['Completed'] **single-value TERMINAL**, `mttr_hours` number nullable, `sla_breached` integer enum[0,1]}** GROUNDED return `{name,status=RepairStatus.COMPLETED,mttr_hours,sla_breached}` @`services/imm09.py:1114-1119` (`mttr_hours`=Float / `sla_breached`=Check @`asset_repair.json` — int-vs-bool né, KHÔNG boolean). ⚠️ **C3-SPLIT cross-ACTION (ADR-MOBILE-009)**: shape-TRÙNG `CloseWorkOrderResponse` {name,status,mttr_hours,sla_breached} NHƯNG **status enum-domain DISJOINT** (`Completed` ∉ {Pending Inspection, Cannot Repair}) + semantics nghiệm-thu (status INVARIANT=Completed) ≠ đóng-phiếu ⇒ schema RIÊNG, **KHÔNG merge** (precedent `ResolveIncidentResponse`/`CloseIncidentResponse` field-disjoint split); REUSE `Error` (KHÔNG schema Error mới). **Error-on-HTTP-200** in-handler: `IMM09_BAD_STATE` (status≠Pending Inspection → code `CONFLICT` http_status `409` @`messages.py:646`) + `IMM09_NOT_FOUND` (code `NOT_FOUND` http_status `404` @`messages.py:639`) gom vào nhánh Error 200-oneOf, KHÔNG status-line key. **403 = SINGLE-SHAPE `Forbidden`** (dispatcher-403 guest/no-token; in-handler cap-403 `repair.submit` @`api/imm09.py:106` phủ bởi nhánh Error 200-oneOf — mirror `closeWorkOrder`/`startRepair`); 401 = `Unauthorized401`; response slot CHỈ {200,401,403}. **PURE-YAML + guard test only** (KHÔNG đụng `.py` — handler+service LIVE whitelisted landed prior round, sig nguyên; `git status api/imm09.py`+`services/imm09.py` KHÔNG do round này; KHÔNG reload/migrate/commit). Guard `TestMobileConfirmInspectionContract` (+8 TC a..g+i — incl. TC-g parity@source `ConfirmInspectionResponse` field-set ⊆ return-dict + `status.enum==[RepairStatus.COMPLETED]` + TC-i live-signature parity `inspect.signature(imm09.confirm_inspection)=={name}`, `name` no-default + clean POST KHÔNG ∈ `_PARITY_VERB_ALLOWLIST`, chống drift contract↔source) ⇒ `_EXPECTED_TEST_COUNT` 346→354. **FLOW-6 READ-RECEIPT markNotificationAsRead (đánh dấu 1 thông báo đã đọc — WRITE-action ĐẦU TIÊN trên domain Notification Log):** 38→39 path — bồi `mark_notification_as_read` (`assetcore.api.layout.mark_notification_as_read` BE sẵn @`api/layout.py:102-117` `@frappe.whitelist(methods=['POST'])` clean POST, ownership-guard `for_user==session.user` @:111-113, trả `_ok({name, read:1})` @:117) đóng dead-end của `listNotifications` (R20): tab chuông + push deep-link (flow-6) CHỈ-ĐỌC danh-sách (read-only), KHÔNG có endpoint đánh-dấu đã-đọc khi user tap/mở. 200 = oneOf `[MarkNotificationReadEnvelope, Error]` closed-schema route-by-VALUE `body.success` (0 discriminator); `MarkNotificationReadResponse` EXACT 2-prop `{name, read}` (`read` integer enum[0,1] mirror `NotificationListItem.read` SSoT int-vs-bool, KHÔNG boolean) — KHÔNG prop `status` vì Notification Log KHÔNG có workflow_state ⇒ C3-split cross-domain ≠ mọi `*ActionResponse` (đều mang status/domain-field). slot `{200,401,403}`; `Notification∄` 404 (@:108-109) + cap-403 owner (@:112-113) ARRIVE HTTP-200 nhánh Error (route theo `http_status`, `Error.http_status` enum bounded chứa 404/403 — KHÔNG status-line key). Guard `TestMobileMarkNotificationReadContract` (+8 TC `test_mob_oas_marknotifread_a..g+i`) ⇒ `_EXPECTED_TEST_COUNT` 354→362; path/operationId count 38→39 (test 01/02/03 + 29b + caltrans_e + 25b/23a). SSoT: [`04-api-contract.md §8.20`](./04-api-contract.md) (FLOW-5 terminal nghiệm-thu) + [`ADR-MOBILE-009.md`](./ADR-MOBILE-009.md) (C3-split cross-ACTION) + `api/imm09.py:103-107` + `services/imm09.py:1086-1119` + `asset_repair.json` (mttr_hours Float / sla_breached Check). **FLOW-2 DEVICE-PROFILE getAssetTimeline (dòng-thời-gian vòng-đời thiết bị — tab "Lịch sử" màn hồ-sơ sau quét QR; GET-read DocType `Asset Lifecycle Event` KHÁC R28 đọc Incident Report):** 39→40 path — bồi `get_asset_timeline` (`assetcore.api.imm00.get_asset_timeline:1127`, `@frappe.whitelist()` bare nhận GET). 3 param `name`(query required string) + `page`(default 1) + `page_size`(default 50) KHỚP EXACT signature imm00.py:1127. 200 = oneOf `[AssetTimelineEnvelope, Error]` closed-schema route-by-VALUE `body.success` (C7 read-path, 0 discriminator). `AssetTimelineEnvelope` CÓ pagination (`data.required=[pagination,items]`, paginate(total,page,page_size) @imm00.py:1133 — KHÁC AssetIncidentHistoryEnvelope KHÔNG paginate chỉ limit-cap). `AssetTimelineEvent` EXACT 7 prop `{name,event_type,actor,from_status,to_status,timestamp,notes}` GROUNDED `frappe.get_list` fields @imm00.py:1137, required[name], 0 boolean/Check prop (né int-vs-bool Open#1); C3-split object-identity ≠ AssetIncidentHistoryItem (R28) ≠ AssetListItem (khác DocType/field-set/domain). slot `{200,401,403}`; `asset∄` 404 in-handler (@imm00.py:1129-1130) ARRIVE HTTP-200 nhánh Error (`Error.http_status` enum bounded chứa 404). Guard `TestMobileGetAssetTimelineContract` (+10 TC `test_mob_oas_assettimeline_a..j`) ⇒ `_EXPECTED_TEST_COUNT` 362→372; path/operationId count 39→40 (test 01/02/03 + 29b + caltrans_e + 25b/23a). SSoT: [`04-api-contract.md`](./04-api-contract.md) (FLOW-2 device-profile) + `api/imm00.py:1127-1142` (frappe.get_list 'Asset Lifecycle Event' fields 7 + return `_ok({pagination,items})`) + `asset_lifecycle_event.json` (Datetime/Select/Link/Text — 0 Check).
- **Bộ-ba CREATE đã typed ✅:** reportIncident / createRepairWorkOrder / createCalibration — requestBody oneOf (json+form-urlencoded), response `200` oneOf `[Created, Error]` **closed-schema route-by-VALUE `body.success`** (Decision-B — `additionalProperties:false` + disjoint required-set, KHÔNG discriminator), 404/4xx wire grounded `messages.py` http_status.
- **`Error.http_status` = integer enum BOUNDED ✅ (ERR-HTTPSTATUS-ENUM, vòng 11):** đóng route-by-VALUE asymmetry — `Error.code` ĐÃ typed-enum 15-value nhưng `http_status` từng free integer (codegen mobile KHÔNG sinh exhaustive switch). NAY `http_status` = `enum [400,401,403,404,409,413,422,429,500]` (9 giá trị sorted) = đúng tập distinct value BE phát ở body, DERIVE @`utils/response.py` (`set(_HTTP_FOR_CODE.values())`); GIỮ `type:integer`+`example:403`+description. **LOẠI 417** (chỉ là KEY `_HTTP_TO_CODE`→BUSINESS_RULE, body resolve qua `_HTTP_FOR_CODE[BUSINESS_RULE]=422` ⇒ không bao giờ surface body). Cả `code` (enum 15) lẫn `http_status` (enum 9) đều typed-bounded ⇒ 2 field route-by-VALUE cùng-độ-phủ (no-asymmetry). Guard `TestMobileErrorHttpStatusEnumBounded` (+5 TC, RED-before/GREEN-after deepcopy drop-element/inject-417; expected-set DERIVE runtime KHÔNG hardcode) ⇒ `_EXPECTED_TEST_COUNT` 206→211. ADR đầy đủ: [`04-api-contract.md §4.1`](./04-api-contract.md). Pure-yaml+test-only (KHÔNG đụng `.py`/reload/migrate).
- **6 LIST đã typed envelope ✅:** `PmWorkOrderListEnvelope` / `RepairWorkOrderListEnvelope` / `PmScheduleListEnvelope` / `CalibrationListEnvelope` (data.data[]) + `IncidentListEnvelope` / `AssetListEnvelope` (data.items[]), `Pagination` dùng chung (Option A, ADR-MOBILE-001 g). `PmScheduleListEnvelope` (element `PmScheduleListItem` field-disjoint C3-split, CHỉ field `svc.list_schedules`) thêm round C-LISTREAD-SCHED; param DISCRETE `asset_ref`/`status` (KHÔNG JSON `filters`). `CalibrationListEnvelope` (element `CalibrationListItem` field-disjoint C3-split, 15 field `svc.list_calibrations`; `is_recalibration` integer enum[0,1]; 0 financial) thêm round C-LISTREAD-CAL; param REUSE `WorkOrderFilters` (JSON-string). `AssetListEnvelope` (element `AssetListItem` field-disjoint, 20 field `imm00.list_assets`; rows-key `data.items[]` mirror `IncidentListEnvelope`; 0 boolean; 3 financial EXCLUDED LL-BE-57; `lifecycle_status` string KHÔNG enum cứng) thêm round C-LISTREAD-ASSET; param 7 DISCRETE query-string (KHÔNG JSON `filters`). 6/6 list response 200 = oneOf `[<ListEnvelope>, Error]` closed-schema route-by-VALUE `body.success` (C7, 0 discriminator).
- **Guard:** `test_mobile_oas` xanh (0 dangling `$ref`; orphan ⊆ `_RESERVED_ORPHANS`).

### 3.2 P1 — `in-handler-error-on-HTTP-200` (ĐÃ XÁC MINH @source — ADR đã CHỐT)

**Cơ chế:** service `nthrow(MSG.X)` (`utils/notify.py:61-87`) → `ServiceError(http_status=entry['http_status'])` (`services/shared/errors.py:36-42`) → `handle()` bắt (`utils/api_handler.py:48-51`) → `_service_error_to_envelope` (`:54-69`) → `_err(...,http_status=e.http_status)` (`utils/response.py:95-154`). **Body chứa `code`+`http_status` (`response.py:133-138`) NHƯNG HTTP status-line VẪN 200** (`handle` return dict; `hooks.py` no `after_request`).

**ADR (đã CHỐT trong yaml — Decision-B):** response `"200"` = `oneOf [<Created>, Error]` **closed-schema** (`additionalProperties:false` CẢ 2 nhánh + disjoint required-set, **KHÔNG `discriminator`**) → client route theo **GIÁ TRỊ** field `body.success` (`Created.success.enum=[true]` vs `Error.success.enum=[false]`), KHÔNG theo status-line. Đã áp 3 CREATE path (+ `createPmWorkOrder` C2 + 3 GET read C6). yaml = 0 `discriminator:` key. *(Bản nháp R1 từng đề xuất `discriminator: success` — BỎ vì `success` boolean → OAS 3.x illegal; xem ADR-MOBILE-001 (f) R1→R4.)*

**Bằng chứng từng path (file:line):**

| Path | 403 shape | in-handler errors (svc) | return |
|---|---|---|---|
| `imm12.report_incident` | **DUAL** — guest 401 `imm12.py:92` = DEAD-CODE over HTTP (dispatcher-403 trip trước, [`04 §5`](./04-api-contract.md)); cap-403 `imm12.py:96` = HTTP-200+Error | 422 BR-12-01 clinical_impact `services/imm12.py:359` (`messages.py:754`); 404 asset∄ `services/imm12.py:361` (`messages.py:747`) | `{name,status,severity}` `imm12.py:410` |
| `imm09.create_repair_work_order` | **SINGLE** — `rbac.require('repair.create')` `api/imm09.py:40` → PermissionError HTTP-403 THẬT | 404 `services/imm09.py:746` (`messages.py:716`); 409 HAS_OPEN_WO `:753` (`messages.py:667`) | `{name,status,sla_target_hours}` `imm09.py:771` |
| `imm11.create_calibration` | **SINGLE** — `rbac.require('calibration.create')` `api/imm11.py:95` | 404 `services/imm11.py:999` (`messages.py:848`); 409 ASSET_BLOCKED CAL-008 `:1002` (`messages.py:855`) | `{name,status=Scheduled}` `imm11.py:1013` |

> **2 loại 403 (DONE-gate spec-contract):** (a) **dispatcher-403** = guest/no-token, raise tại `is_whitelisted` `frappe/__init__.py:876` → HTTP-403 + `FrappeRawError`. (b) **in-handler cap-403** = thiếu capability, `_err(...,403)` → HTTP-200 + Error envelope. Client phân biệt bằng **HTTP status-line** (KHÔNG anyMatch oneOf). Xem [`04 §5a/§5b`](./04-api-contract.md).

### 3.3 STUB = ∅ (0 STUB-on-MVP — EPIC-D D4 typed device-token) · 4 path C2 ĐÃ typed (F-C4 reconciled)

> **Source-truth `_STUB_PATHS` @ SYMBOL `_STUB_PATHS = set()`** trong `assetcore/tests/test_mobile_oas.py` (re-verify @source theo SYMBOL, KHÔNG số-dòng-tuyệt-đối — số dòng cũ đã line-drift). **EPIC-D D4 (Vòng 17):** 2 device-token (`registerDeviceToken`/`unregisterDeviceToken` @ yaml `/register_device_token`, `/unregister_device_token`) **ĐÃ typed** (service D2 `mobile_device_token.py` tồn tại @source) — `requestBody DeviceTokenRequest` + 200 oneOf `[<Created>, Error]` closed-schema **Decision-B route-by-VALUE, 0 discriminator** (04 §8.9). ⇒ **`_STUB_PATHS = ∅`** (0 STUB-on-MVP toàn bộ 24 path — C-LISTREAD-SCHED: +`listPmSchedules` GET list-read typed; C6-DETAIL: +4 GET-detail typed oneOf; C-LISTREAD-CAL: +`listCalibrations` typed oneOf; C-LISTREAD-ASSET: +`listAssets` typed oneOf; C-LISTREAD-USERS: +`listUsers` typed oneOf, KHÔNG Stub). `responses/Stub` HẾT referenced → forward-reserve (`_RESERVED_ORPHANS`). Handler `api/mobile/v1/device_token.py` = BE impl-part PENDING (wrap service D2). 4 path scan/createPm **ĐÃ typed (C2)** — RỜI `_STUB_PATHS`, không còn `responses/Stub`.

| # | Path | yaml | Source | Trạng thái |
|---|---|---|---|---|
| 1 | resolveQrToken | :1268 | `api/imm00.py:588` `resolve_qr_token(token="")` · `@rate_limit:587` (→429) · `rbac.require("asset.read"):615` · 404 `:619` · vendor-IDOR 403 `:622` | ✅ **typed C2** — `QrResolveEnvelope` 200-oneOf[Created,Error] closed-schema route-by-VALUE `body.success` (Decision-B); RỜI `_STUB_PATHS` |
| 2 | getAssetScanInfo | :1280 | `api/imm00.py:631` · `@rate_limit:630` (→429) · build qua `services/imm00.py:637` `build_asset_scan_info` | ✅ **typed C2 + drift-closed C-ASCAN-PARITY** — `AssetScanInfoEnvelope` (**16 field** ↑ từ 12 + `available_actions[]{key,label,route,enabled,reason}`); bồi 4 field BE emit thật: `manufacturer_sn` (imm00.py:775 Data·string) · `risk_classification` (imm00.py:782 Select·string-no-hard-enum) · `warranty_expiry_date` (imm00.py:827 `_date_str_or_none`·date·nullable) · `warranty_expired` (imm00.py:832 `_is_warranty_expired`·boolean server-flag); closed-schema `additionalProperties:false` GIỮ; RỜI `_STUB_PATHS` |
| 3 | getAsset | :1292 | `api/imm00.py:454` `get_asset(name)` · `rbac.require:472` · 404 `:474` · vendor-IDOR `:477` · return `_ok(_strip_qr_token(...))` `:507` | ✅ **typed C2+C6** — `AssetDetailEnvelope` 200-oneOf[Created,Error] (read-path P1 closure C6); RỜI `_STUB_PATHS` |
| 4 | createPmWorkOrder | :1368 | `api/imm08.py:91` `create_pm_work_order()` (`_form_dict()` `:17`) · `rbac.require pm.create:92` · `svc.create_adhoc_work_order(data)` `services/imm08.py:787` · required `("asset_ref","pm_schedule","due_date")` `:788` · return `{name,status,checklist_items_count}` `:836-840` | ✅ **typed C2** — `CreatePmWorkOrderRequest` (required chốt BA) + response, dual json+form (C4); RỜI `_STUB_PATHS` |

**Field thật cho list-element (✅ C3-split DONE — KHÔNG còn `type:object` generic; element = `$ref` field-disjoint):**

- **PM** (`services/imm08.py:531-533`): `[name, asset_ref, pm_type, wo_type, status, due_date, completion_date, assigned_to, supervisor, overall_result, is_late, source_pm_wo]` + enrich `asset_name`/`location_name` (`:566-567`).
- **CM** (`services/imm09.py:675-681`): `[name, asset_ref, asset_name, repair_type, priority, status, open_datetime, completion_datetime, mttr_hours, sla_breached, sla_target_hours, is_repeat_failure, assigned_to, root_cause_category, risk_class, parts_hold_hours, parts_hold_started]`.
- **Incident** (`services/imm12.py:750-756`): `[name, asset, incident_type, severity, status, fault_code, reported_by, reported_at, description, linked_capa, linked_repair_wo, rca_required, rca_record, chronic_failure_flag, patient_affected, closed_date, assigned_to, acknowledged_at, resolved_at, response_breached, resolution_breached, response_due_at, resolution_due_at]` + `_enrich_asset_names`/`_enrich_sla_breach` (`:761/:763`).

> **PM ≠ CM field-set → ✅ ĐÃ TÁCH (C3-split 2026-06-11):** vì FIELD KHÁC nhau, KHÔNG ép chung 1 UNION item. Tách 2 envelope RIÊNG `PmWorkOrderListEnvelope` / `RepairWorkOrderListEnvelope` + 2 item-schema field-disjoint `PmWorkOrderListItem` (16 field imm08) / `RepairWorkOrderListItem` (21 field imm09); mỗi list path trỏ item của nó (rows-key `data` GIỮ nguyên). KNOWN-GAP "KHÔNG ép chung" ĐÓNG — không còn defer Phase-E. Chi tiết: EPIC-C §C3-split + ADR-MOBILE-001 (g).

### 3.4 userinfo / whoami (OIDC) — ✅ ĐÓNG bởi C4 (2026-06-11)

- ✅ **C4 DONE:** path `GET /api/method/frappe.integrations.oauth2.openid_profile` (`operationId getUserInfo`, security `OAuth2: [openid]`) + schema `OidcUserInfo` **RAW passthrough** đã wire vào yaml. App lấy tên+role KTV sau login (flow-1).
- Endpoint Frappe = `frappe/integrations/oauth2.py:163-164` `openid_profile` (`@whitelist`, **KHÔNG allow_guest** → cần bearer) → `create_userinfo_response` → set `frappe.local.response = body` (`:172-174`, RAW). Claims `get_userinfo` `oauth.py:530-555`.
- **`[VERIFIED @source]`** 8 claim GROUNDED (KHÔNG bịa): `sub`(string|null), `name`, `given_name`, `family_name`, `email`, `picture`(string|null), `roles`(array\<string\>), `iss`. `OidcUserInfo` closed-schema (`additionalProperties:false`, Decision-B). Guard `test_mob_oas_22` (TestMobileUserInfo 9 TC) XANH.

### 3.5 TO-BUILD EPIC-C

| Tag | Task |
|---|---|
| ✅ `[DONE C2]` | Doc BE-completion vùng C tích hợp §3 file này (`completion/EPIC-C-api-contract.md`): liệt kê STUB còn lại + `_STUB_PATHS` guard (symbol) + chứng cứ P1 — **.md** xong |
| ✅ `[DONE C2 + drift-closed C-ASCAN-PARITY]` | Type 4 scan/createPm response (RỜI `responses/Stub`): `QrResolveEnvelope` · `AssetScanInfoEnvelope {…16 field` (↑ 12, bồi 4 BE-emit `manufacturer_sn`/`risk_classification`/`warranty_expiry_date`/`warranty_expired` @`imm00.py:775/782/827/832`)` + available_actions:AvailableAction[]{key,label,route,enabled,reason}}` (`services/imm00.py:764-840`) · `AssetDetailEnvelope` (as_dict enrich + overdue). 200-oneOf[Created,Error] closed-schema route-by-VALUE `body.success` (Decision-B, KHÔNG discriminator) + 404 in-handler vào nhánh Error (đồng pattern 3 create); `TestMobileTypedReadCreate` (TC-MOB-OAS-20) + `TestMobileAssetScanInfoFieldParity` (8 TC parity + no-financial LL-BE-57 + forward-sweep service-keys ⊆ schema) xanh |
| ✅ `[DONE C2 — BA chốt requestBody]` | `createPmWorkOrder`: `CreatePmWorkOrderRequest` (required `asset_ref`/`pm_schedule`/`due_date` @`services/imm08.py:788` + optional `pm_type`/`wo_type`/`assigned_to`/`supervisor`/`technician_notes`) + response `{name,status,checklist_items_count}`. BA chốt requestBody @source (required ở service KHÔNG ở `@whitelist` signature → codegen KHÔNG suy được → BA khai tường-minh trong yaml). KHÔNG đổi `api/imm08.py:91` signature (introspection-only, KHÔNG reload gunicorn). dual json+form (C4 vá) |
| ✅ `[DONE C3-split]` | 2 item-schema field-disjoint `PmWorkOrderListItem`/`RepairWorkOrderListItem` + `IncidentListItem` thay `type:object` generic; mỗi list path trỏ envelope+item RIÊNG (KHÔNG còn UNION). `TestMobileListItemTyped` 8 TC (+`21b2`/`21g` disjoint-field) xanh |
| ✅ `[DONE C4]` | userinfo/whoami OIDC path `getUserInfo` + schema `OidcUserInfo` (§3.4) — wire xong, test_mob_oas_22 xanh |
| ✅ `[DONE C2]` | `test_mobile_oas` + `_STUB_PATHS = set(_DEVICE_TOKEN_FROZEN)`/`_EXPECTED_TEST_COUNT`/`_PATHS_REQUIRE_401/403` cập nhật khi 4 scan/createPm rời + userinfo thêm; `bench --site miyano run-tests --module assetcore.tests.test_mobile_oas` XANH |
| ✅ `[DONE C5+C6+F-C2+C-DoD-CFG AUTO]` | codegen-dry introspection proxy PyYAML (`TestMobileCodegenDryDoD`) + read-path oneOf (C6 `TestMobileRead200OneOfClosed`) + drift-guard parity (F-C2 `TestMobileSpecParityRuntime`) + **config-validity (`TestMobileCodegenConfig` — `openapitools.json`↔YAML)**: 0 Stub-on-MVP + 0 dangling + mọi 10 path MVP typed + runnable-config 3 generators trỏ YAML — `test_mobile_oas` **141 OK** (count HIỆN HÀNH @source G3 2026-06-12; `_EXPECTED_TEST_COUNT=141` SSoT; gồm 1 meta-guard `TestMobileOasCountSelfVerify` F-C3 + 10 TC `TestMobileCodegenConfig` C-DoD-CFG + 4 TC `TestMobileRoadmapStateReconciled` F-C4 + 4 TC `TestMobileRefreshOn401DocGuard` F-B2 + 4 TC `TestMobileTracebackHardeningDocGuard` G3; guard-suite 7-module tổng **281 OK** re-run @source G3 2026-06-12). AUTO-part DoD EPIC-C đóng → gate EPIC-V |
| `[V · HARD-STOP USER]` | Smoke `openapi-generator` THẬT (Dart/Kotlin) qua `openapitools.json` runnable-config — cần `java`+`npx` (NOT FOUND @2026-06-11) → thuộc EPIC-V V-U1/V-U2 |

### 3.6 DoD EPIC-C

**AUTO-part (introspection proxy PyYAML — đã đóng):**
- [x] **codegen-dry introspection** (C5 `TestMobileCodegenDryDoD` `test_mob_oas_23a..e`) + **config-validity** (C-DoD-CFG `TestMobileCodegenConfig` `test_mob_oas_28a..j` — `openapitools.json`↔`_MOBILE_YAML`): 0 path MVP còn Stub + 0 dangling `$ref` + mọi 10 path MVP có `data` typed `$ref` + runnable-config 3 generators trỏ YAML — chạy KHÔNG-toolchain (`bench --site miyano run-tests --module assetcore.tests.test_mobile_oas` = **141 OK** count HIỆN HÀNH @source G3 2026-06-12; `_EXPECTED_TEST_COUNT=141` SSoT; guard-suite 7-module = `test_mobile_oas` 141 + `test_oas_generator` 49 + `test_oas_serve` 13 + `test_oas_signatures` 11 + `test_mobile_docset` 9 + `test_mobile_capability_map` 6 + `test_mobile_security_gate` 52 = **281 OK** re-run @source G3 2026-06-12).
- [x] `test_mobile_oas` xanh (10 path MVP rời Stub + list-element + userinfo + C5 dry-DoD); CHỈ 2 device-token GIỮ Stub = BE-PENDING EPIC-D (hợp lệ).
- [x] requestBody oneOf json+form mọi RPC path (`test_mob_oas_22i`); response `200` oneOf[Created,Error] **closed-schema KHÔNG discriminator** (Decision-B) áp đủ 4 create.

**THẬT-part (codegen Dart/Kotlin — HARD-STOP USER, gate EPIC-V):**
- [ ] `openapi-generator` chạy THẬT sạch (Dart/Kotlin) — exit 0, 0 ERROR. `java` **NOT FOUND** + `@openapitools/openapi-generator-cli` chưa cài (probe @2026-06-11) ⇒ **[HARD-STOP USER]** → EPIC-V V-U1/V-U2.

---

## 4. EPIC-B — Auth & Provisioning

> Vùng tài liệu: [`03-auth-oauth2.md`](./03-auth-oauth2.md) · [`10-deploy-ops.md §1`](./10-deploy-ops.md) · [`12-phase-b-preflight.md`](./12-phase-b-preflight.md) · ADR-MOBILE-001.

### 4.1 Auth provider SẴN SÀNG (WIRE-not-write — KHÔNG sửa core)

| Khía cạnh | Trạng thái @source (file:line) |
|---|---|
| authorize | `frappe/integrations/oauth2.py:74-75` (`@whitelist allow_guest`); Guest→302 `/login` `:81-82` |
| get_token | `oauth2.py:123-124` (allow_guest); success body **PASSTHROUGH OAuthlib** `:137` (KHÔNG envelope); error grant đường thường → `http_status_code=400` `:133-134` |
| revoke_token | `oauth2.py:144-145` (allow_guest); **LUÔN 200** body rỗng `frappe._dict({})` `:158-159` (RFC 7009) |
| openid_configuration (discovery) | `oauth2.py:180-181` (allow_guest). introspect: `:205-206` |
| userinfo (openid_profile) | `oauth2.py:163-164` (`@whitelist` KHÔNG allow_guest → cần bearer) → OIDC claims |
| **refresh-token** | **HỖ TRỢ ĐẦY ĐỦ** — `frappe/oauth.py:184-187` `validate_grant_type ∈ [authorization_code, refresh_token, password]`; `validate_refresh_token` chỉ status="Active" `:270-296`; `get_original_scopes` `:244-249` |
| **token lifetime** | **3600s HARD-CODED, KHÔNG site_config knob** — `get_oauth_server()` `oauth2.py:19-24` dựng `WebApplicationServer` KHÔNG truyền `token_expires_in` → oauthlib fallback `or 3600`; `expiration_time = creation + timedelta(expires_in)` `oauth_bearer_token.py:28-31`. OAuth Provider Settings (Single) CHỈ field `skip_authorization`. **KNOWN-LIMIT:** đổi TTL = ADR-MOBILE-001 alt A7 (wrap/fork `get_oauth_server`) Phase F — KHÔNG chặn MVP. |

### 4.2 Provisioning OAuth Client — preflight gate (DoD cơ chế)

- **Preflight verifier (ĐÃ CÓ, READ-ONLY):** `assetcore/api/mobile/preflight.py:146-220` `verify_oauth_client()` (`@frappe.whitelist` KHÔNG allow_guest + `frappe.only_for("System Manager"):171`). Chấm 7 điều kiện B-1: `client_count>=1` `:177-179` · grant_type `:88` · response_type `:96` · `default_redirect_uri∈redirect_uris` `:105` · `scopes='all openid'` `:121` · `skip_authorization==0` `:129` · `allowed_roles` non-empty `:136`. count==0 → `ready=False` + blocker VI, **KHÔNG raise** `:181-190`.
- **Provisioning AUTO = KHÔNG có** — grep `hooks.py::fixtures` / `patches.txt` / `setup/` = 0 → BẮT BUỘC runbook thủ công [`10 §1`](./10-deploy-ops.md). OAuth Client count=0 @ site miyano.
- **Decision (chủ ý):** giữ runbook thủ công + preflight READ-ONLY là **gate khách quan** thay helper write (DB-write = HARD-STOP USER, vi phạm read-only ADR-MOBILE-001 nếu auto). Nếu USER yêu cầu helper idempotent write → đề xuất NHƯNG đánh dấu `[HARD-STOP execute]`.

### 4.3 Device-token cho FCM (chặn EPIC-D)

> **Trạng-thái-hiện-hành @F-B2 2026-06-12 (re-verify @source):** đồng bộ với [§3.3](#33-stub--0-stub-on-mvp--epic-d-d4-typed-device-token--4-path-c2-đã-typed-f-c4-reconciled) cùng file. Snapshot stale 2026-06-11 (device-token "CHƯA tồn tại") đã `[SUPERSEDED]` — xem cuối mục để giữ audit-trail.

- **DocType + service + handler ĐÃ TỒN TẠI @source (EPIC-D D4/D6/D7):** `assetcore/assetcore/doctype/ac_mobile_device_token/{json,py,__init__}` (7 field, autoname=hash, `fcm_token` UNIQUE, track_changes=1) · service `assetcore/services/mobile_device_token.py` (`register_device_token(*, fcm_token, platform, device_label='', app_version='')` ÉP `user=frappe.session.user` chống spoof §6.2 · UPSERT dedup · `unregister_device_token(fcm_token)` enabled=0 giữ audit · `invalidate_token`; gọi `log_audit_event` NĐ98 §5.4) · handler `assetcore/api/mobile/v1/device_token.py` (`@frappe.whitelist(methods=["POST"])` KHÔNG allow_guest → cần bearer; `register_device_token` thêm `@rate_limit` chống spam §5.3 — THỨ TỰ decorator: `@whitelist` NGOÀI, `@rate_limit` TRONG). Spec gốc: [`06-push-fcm.md §2`](./06-push-fcm.md).
- **OpenAPI = 2 path TYPED (KHÔNG STUB):** `registerDeviceToken`/`unregisterDeviceToken` @ yaml `/api/method/assetcore.api.mobile.v1.register_device_token` (+ `…unregister_device_token`) — `requestBody DeviceTokenRequest` oneOf json+form + 200 oneOf `[<Created>, Error]` **closed-schema Decision-B route-by-VALUE, 0 discriminator** (schema `DeviceTokenRequest`/`RegisterDeviceTokenAckEnvelope`/`UnregisterDeviceTokenAckEnvelope` ~yaml L1238–1323; KHÔNG còn yaml:1565/1582). Guard `_STUB_PATHS = set()` (∅) @ `assetcore/tests/test_mobile_oas.py` (SYMBOL — re-verify @source, KHÔNG số-dòng-tuyệt-đối) ⇒ 0 STUB-on-MVP. Chi tiết: [§3.3](#33-stub--0-stub-on-mvp--epic-d-d4-typed-device-token--4-path-c2-đã-typed-f-c4-reconciled) + [`04-api-contract.md §8.9`](./04-api-contract.md) + [`completion/EPIC-D-push-fcm.md §D4`](./completion/EPIC-D-push-fcm.md).
- **→ SINGLE-OWNER = EPIC-D (D1/D2/D4 build DocType + service + endpoint MỘT LẦN) — GIỮ NGUYÊN.** EPIC-B **B3 = dependency-gate** (chỉ xác nhận bearer→`set_user` reach `api/mobile/v1` qua **B2**), **KHÔNG impl device-token** (tránh build trùng giữa B và D). Impl chi tiết = §6 (EPIC-D). `bench migrate` (table+UNIQUE live) + reload gunicorn = **[HARD-STOP USER]** vẫn `[ ]`.

> **`[SUPERSEDED]` snapshot 2026-06-11 (giữ audit-trail tiến độ — KHÔNG đọc như trạng-thái-hiện-hành):** ground ban đầu khi DocType "AC Mobile Device Token" CHƯA tồn tại (grep `device_token`/`fcm_token` = 0 hit ngoài test), `api/mobile/` chỉ có `__init__.py` + `preflight.py` (chưa `v1/`), 2 path OpenAPI còn STUB (yaml:1565/:1582). Đã thay bằng trạng-thái-hiện-hành ở trên @F-B2. Đồng bộ với [`completion/EPIC-B-auth-provisioning.md §3.4 [SUPERSEDED]`](./completion/EPIC-B-auth-provisioning.md).

### 4.4 TO-BUILD + DoD EPIC-B

| Tag | Task |
|---|---|
| `[AUTO]` | Doc "Auth provider sẵn sàng": khẳng định refresh ĐÃ hỗ trợ + TTL 3600s hard-coded (KNOWN-LIMIT) + userinfo=openid_profile — bám file:line §4.1, KHÔNG re-litigate |
| `[AUTO]` | Doc "Provisioning": runbook thủ công `10 §1` + preflight gate = cơ chế DoD; ghi RÕ KHÔNG fixture/patch auto |
| `[ref EPIC-D]` | Device-token DocType + service + `register/unregister_device_token` = **OWNED & impl bởi EPIC-D (D1/D2/D4)**; EPIC-B B3 CHỈ gate dependency (bearer reach `api/mobile/v1`), **KHÔNG impl trùng** |
| ✅ `[DONE]` | Backlog đóng vòng: **(a) ✅ DONE (C-A6)** userinfo/whoami ĐÃ wire vào yaml — path `GET …openid_profile` `operationId getUserInfo` (yaml:2030/2033) + schema `OidcUserInfo` RAW passthrough (yaml:1581), security `[openid]` (§3.4); **(b) ✅ DONE (B-A4)** ví dụ refresh-on-401 sequence ĐÃ có ở [`03 §1.3e/§2.5`](./03-auth-oauth2.md) (`grant_type=refresh_token`) + [`04 §9d(n)`](./04-api-contract.md) (401 → đổi refresh_token → retry MỘT lần → fail re-auth). Cross-ref [`ACCEPTANCE-CHECKLIST.md`](./completion/ACCEPTANCE-CHECKLIST.md) C-A6 + B-A4 `[x]`. |

**DoD checklist EPIC-B:**

| # | Tiêu chí | Ai chạy |
|---|---|---|
| 1 | `preflight.verify_oauth_client()` ready=True | **[HARD-STOP USER]** tạo OAuth Client record |
| 2 | authorize→token→refresh→revoke + PKCE smoke trên cloud | **[HARD-STOP USER]** reload + host |
| 3 | device-token doctype sẵn sàng (impl ở **EPIC-D D1/D2/D4** — KHÔNG ở B) | **[AUTO impl EPIC-D]** + **[HARD-STOP USER]** migrate |

**4-bước go-live HARD-STOP (LL-DEPLOY-03):** `bench migrate` (OAuth Client/Bearer Token native + device-token + bust `ac_caps`) → `bench restart` (gunicorn --preload) → `site_config` (allow_cors LIST / OAuth Client / qr_base_url / FCM) → verify preflight. **Toàn bộ HARD-STOP USER — doc nêu, BE KHÔNG chạy.**

---

## 5. EPIC-G — Go-live & Hardening

> Vùng tài liệu: [`10-deploy-ops.md`](./10-deploy-ops.md) + [`08-security-compliance.md`](./08-security-compliance.md) + ADR-MOBILE-004 + [`12-phase-b-preflight.md`](./12-phase-b-preflight.md). **CHỈ ĐỌC code** — chỉ viết .md.

### 5.1 GO-LIVE KNOB MATRIX (5 knob × hiện trạng/evidence/giá-trị/ai-chạy/verify)

> TẤT CẢ knob ABSENT ở `site_config.json` (miyano) + `common_site_config.json` → mobile-BE CHƯA go-live. `gunicorn_workers=41` + boot `--preload` (LL-DEPLOY-01 staleness đứng).

| Knob | Hiện trạng (evidence file:line) | Giá trị go-live | Ai chạy | Verify |
|---|---|---|---|---|
| `allow_cors` | ABSENT → CORS OFF. `frappe/app.py:268-269` (`if allowed_origins := conf.allow_cors`) None⇒return sớm; wildcard `:275-280` BỎ lọc + LUÔN echo `Allow-Credentials:"true"` `:283` + echo Origin `:284` = lỗ credential-echo T3 | **native APK → GIỮ OFF (None) hợp lệ.** web/PWA/Swagger/WebView-OAuth → list-origin tường minh. **CẤM wildcard `*` prod** | **[HARD-STOP USER]** | smoke OPTIONS preflight từ origin cho phép |
| `host_name` | ABSENT. `frappe/utils/data.py:1599` (`def get_url`) · `:1605` (`host_name = ...conf.host_name or ...conf.hostname`); vắng ⇒ fallback Host header `:1611-1614` → `protocol + ...site` `:1631` = `http://miyano` nội bộ (camera/QR deep-link KHÔNG mở được). **GUARD-9 machine-check** (`TestSecGateHostNameIssuerDoc`) source-grounded @source + prose-invariant `08 §5.1(f)`/`10 §3`+§6.2(3c0)+§6.3 | public HTTPS host | **[HARD-STOP USER]** | `get_url()` / `openid_configuration issuer == public host` (KHÔNG `http://miyano`) |
| `assetcore_qr_base_url` | ABSENT. `services/imm00.py:635` `_QR_BASE_URL_CONF_KEY`, `_build_qr_url` `:685` | public HTTPS QR base | **[HARD-STOP USER]** | QR deep-link mở được trên thiết bị |
| `allow_error_traceback` | **System Setting (KHÔNG site_config) default=1 (ON)** — `system_settings.json:262-265`. Gate THẬT `frappe/utils/response.py:60-65` `is_traceback_allowed()`; dùng `:36`/`:182`/`:190`/`:203`. PROD hiện LEAK traceback/SQL ở 401/403/429 raw | System Setting → **0** | **[HARD-STOP USER]** | `bench --site <site> execute frappe.utils.response.is_traceback_allowed` trên staging |
| `conf.rate_limit` + nginx | ABSENT → `frappe.local.rate_limiter` KHÔNG instantiate. Gate `frappe/rate_limiter.py:17-19`. Headers emit qua `app.py:256-257` → `rate_limiter.py:82-103` (X-RateLimit-*/Retry-After). ⚠️ `@rate_limit` decorator (`imm00.py:311/354/514`) tự đếm cache `:155-161` → `throw(RateLimitExceededError)` `:163-166` = **429 body-only, NO header** | `conf.rate_limit` global HOẶC nginx `limit_req` inject Retry-After | **[HARD-STOP USER]** | curl >limit → kiểm header Retry-After/X-RateLimit-* |

> **CORS phân biệt TƯỜNG MINH:** native APK MVP → `allow_cors` GIỮ OFF, KHÔNG bật chỉ vì mobile. Cross-ref ADR-MOBILE-004(c).
> **rate-limit 2 đường:** (a) `conf.rate_limit` global cho X-RateLimit-* mọi request; HOẶC (b) nginx `limit_req` inject Retry-After cho oauth2.* + RPC. Decorator-path 429 = body-only no-header (KNOWN).

### 5.2 TO-BUILD + DoD EPIC-G

| Tag | Task |
|---|---|
| `[AUTO]` | Viết KNOB MATRIX (§5.1) vào [`10-deploy-ops.md`](./10-deploy-ops.md) (hoặc chương G) — 5 knob × cột [ABSENT/evidence / go-live / ai-chạy / verify] |
| `[AUTO]` | Thêm `08 §4` checklist + ADR-004 Consequences: item "(b) PROD TẮT `allow_error_traceback` (System Setting=0)" evidence `response.py:60-65` — phủ T-leak. Ghi RÕ KHÔNG phải developer_mode/site_config |
| `[AUTO]` | Sửa file:line rate-limit (gate=`rate_limiter.py:17-19`, KHÔNG `:82-92`=headers) + note decorator-429-no-header |
| `[AUTO]` ✅ | Bồi `10 §2` CORS phân biệt native vs web; **checklist `host_name` vào `10 §6.2` (DONE — item `(3c0)` §6.2 + knob row §3 + invariant note; machine-check GUARD-9)**; CI-guard chặn servers placeholder `REPLACE-WITH-PUBLIC-HOST` (yaml:107) + version skeleton (yaml:89) |
| `[AUTO]` | Ghi RÕ: SAU mọi đổi site_config/System Setting → **[HARD-STOP USER]** `bench restart`/reload (--preload, 41 workers) MỚI live HTTP (LL-DEPLOY-01/04) |

> ✅ **G-A2 closed — G3 AUTO doc-part (2026-06-12):** invariant traceback-gate (hàng 2 bảng trên — "PROD TẮT `allow_error_traceback` (System Setting=0)" evidence `response.py:60-65`, KHÔNG `developer_mode`/`site_config`) NAY có **machine-check drift-guard**: `test_mobile_security_gate.py::TestSecGateTracebackGateDoc` (GUARD-5, 5 TC raw-text STDLIB derive-from-source) assert invariant xuyên `08 §4(b)/§5` + `10 §6.2(6)` + `ADR-004 Consequences`, ground-truth @source `frappe/utils/response.py:60` `is_traceback_allowed` + `:63` `get_system_settings('allow_error_traceback')` (anti stale-evidence), RED-before string-mutate (`System Setting`→`developer_mode` / xoá phủ-định) → RAISE. `test_mobile_security_gate` **16→21 OK** (SSoT `_EXPECTED_SECURITY_GATE_TEST_COUNT=21`); guard-suite gộp 6→7 module (GO-2). Runtime knob (System Setting=0 + reload gunicorn + curl live verify body 401/403/429 no-`Traceback`) = **[HARD-STOP USER]** G-U4/G-U6 — AUTO chỉ check PROSE-mechanism, KHÔNG set runtime knob. doc/test introspection-only, `git diff --stat` api/services/*.py + yaml = TRỐNG.

> ✅ **G-A8 closed — host_name/issuer go-live drift-guard (2026-06-12):** invariant knob #1 `host_name`
> (hàng 2 bảng §5.1 — `host_name` set ⇒ `get_url()`/OIDC `openid_configuration issuer == public host`, KHÔNG
> `http://miyano` nội bộ) NAY có **machine-check** `test_mobile_security_gate.py::TestSecGateHostNameIssuerDoc`
> (GUARD-9, 8 TC raw-text STDLIB derive-from-source). Đóng KNOB-MATRIX invariant CUỐI chưa-guard (4/5 đã có:
> CORS=GUARD-3, traceback=GUARD-5, rate-limit-header=GUARD-7, token-leak/audit=GUARD-6/8; host_name = knob #1
> = LAST). Assert: (1) @source `frappe/utils/data.py:1599 def get_url` + `:1605 host_name = ...conf.host_name or
> ...conf.hostname` + fallback `:1631 protocol + ...site` TỒN TẠI (anti stale-evidence); (2) prose-invariant
> raw-text xuyên `08 §5.1(f)` + `10 §3`/§6.2(3c0)/§6.3 + `EPIC-G §3.3`/§8 R4 — mỗi nơi có `host_name` +
> `get_url()`/`openid_configuration issuer == public host` + phủ-định `KHÔNG http://miyano`; (3) RED-before/
> GREEN-after string-mutate bản-sao (xoá `http://miyano` / flip phủ-định / xoá host_name gate-line @source-derive)
> → guard RAISE; control THẬT → GREEN. `test_mobile_security_gate` **44→52 OK** (SSoT
> `_EXPECTED_SECURITY_GATE_TEST_COUNT=52`, meta-guard `TestSecGateSelfCount`); cross-module count-parity:
> `test_mobile_docset` `_GUARD_SUITE_SUM` 273→281 + `_MOBILE_OAS_TOTAL` 299→307 + `_GUARD_SUITE_EXPECTED[sec-gate]`
> 44→52. Live `get_url()`/issuer == public host + curl `openid_configuration issuer` = **[HARD-STOP USER]**
> G-U2/G-U6. doc/test introspection-only, `git diff --stat` api/services/*.py + yaml = TRỐNG.

**DoD EPIC-G:** HTTPS reachable ngoài · security gate: no traceback leak · CORS no-wildcard · no token-leak · rate-limit headers (qua conf/nginx) · host_name/issuer go-live (GUARD-9).

---

## 6. EPIC-D — Push FCM (impl)

> Vùng tài liệu: [`06-push-fcm.md`](./06-push-fcm.md) (spec 327 dòng) + ADR-MOBILE-002 (Accepted — FCM Admin SDK HTTP v1 TRỰC TIẾP, KHÔNG relay Frappe Cloud).
> **Trạng-thái-hiện-hành @F-B2 2026-06-12 (re-verify @source — reconcile stale Vòng-19 snapshot):** **D1/D2/D4-handler/D5/D6/D7 = AUTO-part ĐÃ IMPL @source** (DocType json · `services/mobile_device_token.py` · handler `api/mobile/v1/device_token.py` · sender `utils/fcm.py:272` `send_fcm_message` · RBAC `permissions.py:268/285`+`hooks.py:395/404`). **D6 = AUTO-part ĐÃ ĐÓNG (KHÔNG còn mở):** kênh #3 push wired `_dispatch:416`→`_dispatch_push` (def `:457`, fan-out token enabled=1 + fail-safe try/except `:512`) · `@rate_limit(seconds=60, ip_based=True)` register `device_token.py:62` (decorator-order whitelist-NGOÀI/rate_limit-TRONG, guard `test_mob_oas_22j`). Re-verify @F-B2: `grep -ciE 'fcm\|push' notifications.py`=**18** (KHÔNG 0) · `grep -ciE 'rate_limit' device_token.py`=**7** (KHÔNG 0); test `test_mobile_device_token` **62 OK (10 skip DB-write chờ migrate)** + `test_mobile_oas` **141 OK** @source G3 2026-06-12. **CÒN MỞ = CHỈ HARD-STOP USER:** `site_config.fcm_*` creds + `bench migrate` (table live) + reload gunicorn (xem bảng §6.3 2 dòng `[HARD-STOP USER]`). Chi tiết: [`completion/EPIC-D-push-fcm.md §4 D6 + §5.5`](./completion/EPIC-D-push-fcm.md).
>
> **`[SUPERSEDED]` snapshot Vòng 19 2026-06-12 (giữ audit-trail tiến độ — KHÔNG đọc như trạng-thái-hiện-hành):** từng ghi "**D6 = task AUTO DUY NHẤT còn mở**" với evidence `grep fcm\|push notifications.py`=**0** + `grep rate_limit device_token.py`=**0**. Evidence ĐÃ lật @source (18 / 7) — D6 AUTO-part đã impl sau snapshot. Thay bằng trạng-thái-hiện-hành ở trên @F-B2.

### 6.1 Điểm dispatch hiện tại

- `services/notifications.py:366` `_dispatch` = **1 điểm fan-out duy nhất, CHỈ 2 KÊNH:** in-app `enqueue_create_notification` `:385` (Notification Log Alert = audit NĐ98) + email `_safe_sendmail` `:407-409` (chỉ `_user_wants_email()==True` `:251`). **KHÔNG có kênh push.**
- 7 call-site `_dispatch`: `:452 :498 :562 :627 :791 :931 :1116` → chèn kênh #3 tại `:366` phủ cả 7 event, KHÔNG sửa call-site.
- E3 `notify_incident_created` `:506` wired @`hooks.py:270` (Incident Report after_insert) = **MVP trigger báo hỏng**.

### 6.2 GAPS (chặn flow-6) — ✅ RECONCILED @F-B2 2026-06-12 (AUTO-part đã đóng @source)

> **Trạng-thái-hiện-hành @F-B2:** mọi GAP `[AUTO]` dưới ĐÃ ĐÓNG @source (re-verify `find`/`grep`). Chỉ còn 2 GAP `[HARD-STOP USER]` (creds + migrate/reload). Snapshot "CHƯA/∄" cũ giữ làm `[SUPERSEDED]` audit-trail bên dưới.

- ✅ **(ĐÓNG)** DocType AC Mobile Device Token ĐÃ tạo: `assetcore/assetcore/doctype/ac_mobile_device_token/{json,py,__init__}` (7 field, autoname=hash, `fcm_token` UNIQUE, track_changes=1).
- ✅ **(ĐÓNG)** `register/unregister_device_token`: yaml typed (D4 §8.9 Decision-B oneOf, 0 STUB) **+ handler ĐÃ impl** `api/mobile/v1/device_token.py` (`@whitelist methods=[POST]` wrap service D2; register thêm `@rate_limit:62`).
- ✅ **(ĐÓNG)** RBAC wiring ĐÃ đăng ký `hooks.py:395/404` (`ac_mobile_device_token_query` + `ac_mobile_device_token_has_permission` self-scope, def `permissions.py:268/285`).
- ✅ **(ĐÓNG)** Kênh #3 push ĐÃ chèn `_dispatch:416`→`_dispatch_push:457` (fan-out token enabled=1 + fail-safe). FCM HTTP v1 sender ĐÃ có `utils/fcm.py:272` `send_fcm_message` (STDLIB tự-ký SA JWT — `_sign_jwt:158`/`_fetch_access_token:189`, KHÔNG lib firebase-admin → STDLIB-only guard giữ). Audit register/unregister wire qua `log_audit_event` (service D2). Rate-limit register ĐÃ impl `device_token.py:62`.
- ⚠️ **(CÒN — HARD-STOP USER)** `site_config.fcm_service_account_path`/`fcm_project_id` ABSENT → `send_fcm_message` no-op (push skip, in-app/email VẪN gửi); `bench migrate` (table live) + reload + e2e OAuth-bearer-reach `api/mobile/v1` (Phụ thuộc B / B2) chờ go-live.

> **`[SUPERSEDED]` snapshot Vòng 19 (audit-trail — KHÔNG đọc như hiện-hành):** từng ghi DocType "CHƯA tạo (folder ∄)" · handler "CHƯA impl (`api/mobile/v1/` ∄)" · RBAC "CHƯA đăng ký" · kênh #3 "CHƯA chèn" + "grep fcm `.py` = 0" · rate-limit "chưa impl". Mọi claim ĐÃ lật @source (`find`/`grep` @F-B2). Reconcile bởi F-B2 doc-only.

### 6.3 TO-BUILD EPIC-D (Phase E)

> **Trạng-thái-hiện-hành @F-B2 2026-06-12 (re-verify @source — reconcile stale Vòng-19 BA-GATE):** D1/D2/D4-handler/D5/**D6**/D7 **AUTO-part ĐÃ IMPL** (`find assetcore -iname "*device_token*"` = `doctype/ac_mobile_device_token/` + `services/mobile_device_token.py` + `api/mobile/v1/device_token.py` + `tests/test_mobile_device_token.py`; sender `utils/fcm.py:272`). **DoD-gate D6 ĐÃ THỎA @source:** `grep fcm|push notifications.py`=**18 (>0)** ✅ + `_dispatch_push` kênh #3 `:457` ✅ + `@rate_limit` register `device_token.py:62` ✅ + TC `test_mobile_device_token` **62 OK** + `test_mobile_oas` **141 OK** ✅. Bảng TO-BUILD dưới = **spec build SSoT (audit-trail hợp đồng impl)** — hàng `[AUTO]` NAY ĐÃ ĐÓNG @source; CHỈ **2 hàng `[HARD-STOP USER]`** (creds `site_config.fcm_*` + `bench migrate`/reload) CÒN MỞ.
>
> **`[SUPERSEDED]` snapshot Vòng 19 2026-06-12:** từng ghi "**CÒN MỞ = D6**" + "KHÔNG tick DONE tới khi `grep fcm|push notifications.py`>0". Điều kiện ĐÃ thỏa @source (grep=18) — D6 AUTO-part đóng. Giữ dòng này làm audit-trail tiến độ, KHÔNG đọc như trạng-thái-hiện-hành.

| Tag | Task |
|---|---|
| `[AUTO]` | DocType `assetcore/doctype/ac_mobile_device_token/` (__init__/.json/.py delegate). 7 field [`06 §2.1`]: `user` Link reqd · `fcm_token` UNIQUE reqd · `platform` Select android/ios reqd · `device_label` · `app_version` · `last_seen` Datetime · `enabled` Check default 1. autoname=hash. track_changes=1. Perm: System Manager read-all + field-tech self |
| `[AUTO]` | Service `services/mobile_device_token.py` (3-tier): `register_device_token(*, fcm_token, platform, device_label='', app_version='')` ÉP `user=frappe.session.user` (KHÔNG nhận từ client §5.3); UPSERT dedup theo fcm_token (§2.4); `unregister_device_token(fcm_token)` set enabled=0 giữ audit (§2.5); `invalidate_token(fcm_token)`. Require bearer (KHÔNG cap mới, self-service §2.3). Gọi `log_audit_event` register/unregister (NĐ98 §5.4) |
| `[AUTO]` D4 | **yaml-part ✅ DONE+GREEN (Vòng 17):** requestBody `DeviceTokenRequest` (fcm_token reqd/platform enum[android,ios]/device_label?/app_version?) oneOf json+form + 200 typed oneOf `[<Created>, Error]` closed-schema **Decision-B route-by-VALUE, 0 discriminator**; `_STUB_PATHS=∅`; `test_mobile_oas` **131 OK** + `TestMobileDeviceTokenTyped` 9 TC; yaml + 04 §8.9 + ADR-IMM00-OPENAPI §D-OAS-DEVTOK. **handler-part ⏳ PENDING:** `api/mobile/v1/__init__.py` + `device_token.py` (`@whitelist methods=[POST]`, KHÔNG allow_guest; function-name == operationId; chỉ wrap service D2 qua `handle`, ÉP user=session) |
| `[AUTO]` | RBAC wiring `hooks.py`: thêm `AC Mobile Device Token` vào `permission_query_conditions` (self-scope) + `has_permission` (vendor isolation `_VENDOR_ROLE` pattern `permissions.py:46/90/188`). **Same-commit wiring gate** |
| `[AUTO]` | Chèn KÊNH #3 push `_dispatch` SAU kênh 1+2: per user dedupe → tra device-token enabled=1 → gửi FCM HTTP v1. Payload [`06 §4.1`] (title/body VI strip-HTML ≤1000 + data{doctype,name,event,deeplink}). Lỗi FCM fail-safe (try/except + log_error, KHÔNG vỡ in-app/email — pattern `_safe_sendmail`) |
| `[AUTO]` | Sender `utils/fcm.py`: đọc `site_config` (`fcm_service_account_path`/`fcm_project_id`) — firebase-admin HOẶC REST HTTP v1 + ký OAuth2 SA (quyết Phase E kèm test); lỗi UNREGISTERED/404 → `invalidate_token()`. KHÔNG log credentials |
| `[AUTO]` | Rate-limit register (§5.3/§5.5) theo `@rate_limit` (`imm00.py:311/354`) |
| `[AUTO test]` | `tests/test_mobile_device_token.py`: upsert dedup · ÉP user=session (spoof chặn) · self-scope · unregister enabled=0 · invalidate-on-401 · `_dispatch` fan-out push đúng token enabled (mock FCM) · audit record. Update `test_mobile_oas` (gỡ 2 STUB + assert typed) |
| `[HARD-STOP USER]` | `site_config`: `fcm_service_account_path` + `fcm_project_id` (cùng nhóm allow_cors/qr_base_url); đăng ký Firebase project; cho phép outbound HTTPS `fcm.googleapis.com`. Runbook + rollback xoay key [`10 §4`](./10-deploy-ops.md) |
| `[HARD-STOP USER]` | `bench migrate` (DocType mới) + `bench restart`/reload (`api/mobile/v1` + `_dispatch` sửa) |

**DoD EPIC-D:** báo hỏng → KTV được giao nhận push; test xanh (mock FCM + audit + self-scope).

---

## 7. EPIC-V — Codegen Verify + Handoff

> Vùng tài liệu: [`09-native-repo-guide.md`](./09-native-repo-guide.md) + [`11-phase-a-exit.md §1`](./11-phase-a-exit.md) (matrix) + `openapitools.json`. **CHỈ viết .md** — KHÔNG sửa api/services/yaml-path/operationId.

### 7.1 Toolchain status (probed THẬT — KHÔNG tuyên bố "verified" khi chưa chạy)

- `which java` = **NOT FOUND** (re-probe @2026-06-11). `npx --no-install @openapitools/openapi-generator-cli version` = **canceled** (`@openapitools/openapi-generator-cli@2.35.0` chưa cài, no auto-install). ⇒ codegen THẬT KHÔNG chạy được trong env này. Cả 2 lý do (no JDK + generator chưa cài) = **[HARD-STOP USER cài]** HOẶC chạy ở máy build Phase-D. **C5 codegen-dry + C-DoD-CFG config-validity introspection PyYAML/json = proxy CHÍNH-THỨC** cho codegen-DoD (`test_mobile_oas` **141 OK** count HIỆN HÀNH @source G3 2026-06-12; `_EXPECTED_TEST_COUNT=141` SSoT; guard-suite 7-module tổng **281 OK** re-run @source G3 2026-06-12 · `TestMobileCodegenDryDoD` 10 TC: 0 Stub-on-MVP + 0 dangling + mọi MVP typed · `TestMobileCodegenConfig` 10 TC: `openapitools.json` runnable-config ↔ YAML SSoT).
- `openapitools.json` = **NAY runnable-config** (chuẩn bị V handoff): `generator-cli.version: 7.23.0` + block `generators` 3 target trỏ `docs/mobile/openapi/assetcore-mobile.openapi.yaml` — `mobile-dart` (`dart-dio`), `mobile-kotlin` (`kotlin`, `library: jvm-retrofit2`), `mobile-typescript` (`typescript-axios`). USER cấp `java`+`npx` → `npx @openapitools/openapi-generator-cli generate` (đọc config, sinh cả 3 vào `build/codegen/<lang>`). KHÔNG còn bare version-pin.
- **KOTLIN gap — ĐÓNG:** `openapitools.json` nay khai Kotlin là target THẬT (`mobile-kotlin`). EPIC-V V-U1/V-U2 verify Dart+Kotlin (+ TypeScript bonus). Sample/config docset bổ sung khi USER cấp toolchain (EPIC-V).

### 7.2 GAPS

- ✅ **ĐÓNG (V3 DONE 2026-06-12):** field-tech E2E runbook hợp nhất = chương [`14-e2e-field-tech-runbook.md`](./14-e2e-field-tech-runbook.md) (login→scan→báo hỏng→WO→phiếu→push như 1 sequence chạy-được; 6 operationId trục + curl/dart + expected envelope + tag AUTO/HARD-STOP). `[SUPERSEDED]` "KHÔNG có runbook hợp nhất" — coverage cũ phân mảnh (`11 §1` matrix design-time · `10 §6` smoke · `09 §6.2` 1 dòng) NAY hợp nhất vào chương 14.
- ✅ 4 STUB typed (C2) + list-element typed **field-disjoint 2 item-schema (C3-split)** + **userinfo/whoami = path `getUserInfo` (C4 — codegen sinh method whoami)** ⇒ flow-1/2/4/5 deser typed. flow-6 push CHỜ EPIC-D + FCM creds. **C5+C6+F-C2+C-DoD-CFG DoD codegen-dry+config-validity AUTO-part = DONE** (introspection proxy PyYAML/json XANH, `test_mobile_oas` **141 OK** count HIỆN HÀNH @source G3 2026-06-12, `_EXPECTED_TEST_COUNT=141` SSoT, guard-suite 7-module tổng **281 OK** re-run @source G3 2026-06-12); CHỈ codegen Dart/Kotlin **THẬT** = HARD-STOP USER (java/npx) → gate EPIC-V.
- Handoff bundle documented (`09`) nhưng CHƯA đóng gói artifact tường minh (zip/manifest yaml+base-url+auth-guide+example).

### 7.3 TO-BUILD + DoD EPIC-V

| Tag | Task |
|---|---|
| `[AUTO]` ✅ **DONE 2026-06-12** | Viết chương E2E runbook field-tech [`14-e2e-field-tech-runbook.md`](./14-e2e-field-tech-runbook.md): 6 flow tuần tự, curl/dart-client mỗi bước, expected envelope (success+code+http_status route-by-VALUE), tiền-điều-kiện (OAuth Client+bearer), tag `[AUTO]` vs `[HARD-STOP USER]`. Bám matrix `11 §1` + smoke `10 §6.3` + quirk `04 §5` + refresh-on-401 `03 §2.5`. Index parity README/00-overview GREEN (`test_mobile_docset` 9 OK). |
| `[AUTO]` | Ghi RÕ toolchain status THẬT (§7.1) — KHÔNG tuyên bố "codegen verified" khi chưa chạy |
| `[AUTO+BA]` ✅ **CHỐT 2026-06-12 (V3-BA)** | Kotlin vs TypeScript = **GIỮ "Dart bắt buộc; Kotlin/TS theo `openapitools.json`"** (KHÔNG narrow "Dart + TypeScript"): `openapitools.json` khai 3 generator THẬT gồm `mobile-kotlin` ⇒ Kotlin = codegen-target hợp lệ (DoD "Dart/Kotlin" thoả ở config). Prose-sample `09` = Dart+TS; Kotlin config-only. Chi tiết [`09 §1.1`](./09-native-repo-guide.md) note V3-BA. |
| `[AUTO]` | Bổ sung `openapitools.json` generator-config runnable (input=yaml, output, generatorName) HOẶC `tool/gen-client.sh` mẫu (illustrative) — KHÔNG để config rỗng |
| `[AUTO]` | Mục "gói handoff": (1) yaml copy + version pin · (2) base-url ENV (dev localhost:8000 / prod HTTPS placeholder) · (3) link `03` auth + `04` envelope-quirk · (4) ví dụ 1 call đã-gen + đọc `body.success`/`body.code`. Manifest checklist cho [PM] tick |
| `[QA→CI]` | Guard test: khi toolchain có ở CI, assert gen Dart (+Kotlin nếu chốt) chạy 0-error + 0 dangling `$ref` + sinh 15 operationId method; trước đó giữ PyYAML proxy |

**DoD EPIC-V:** 1 client gen-ra gọi được cả 6 flow trên cloud; runbook validated.

---

## 8. Blockers — AUTO vs HARD-STOP (handoff gate)

> BE KHÔNG tự chạy bất kỳ dòng nào ở cột HARD-STOP. Đây là ranh giới handoff cho USER + orchestrator.

| # | Blocker | Loại | Mở khi |
|---|---|---|---|
| 1 | RELOAD gunicorn (`--preload`, boot Mon Jun 8 08:32) — mọi sửa `api/*.py`/`services/*.py` SAU 08:32 chỉ live ở `run-tests`/`execute`, CHƯA live HTTP | **[HARD-STOP USER]** | USER `bench restart`/reload |
| 2 | `bench migrate` — OAuth Client/Bearer Token native + device-token doctype + bust `ac_caps::*` (v95→v97) | **[HARD-STOP USER]** | USER `bench migrate` |
| 3 | `site_config` go-live — `allow_cors` list / OAuth Client / `assetcore_qr_base_url` / FCM creds / rate-limit (B-1..B-8 + 5 knob §5.1) | **[HARD-STOP USER]** | USER set site_config (preflight gate B-1) |
| 4 | Toolchain codegen — JDK + `@openapitools/openapi-generator-cli` chưa cài | **[HARD-STOP USER]** | USER cài HOẶC máy build Phase-D |
| 5 | USER COMMIT — toàn bộ mobile-BE batch UNCOMMITTED (working tree `??`) | **[HARD-STOP USER]** | USER commit (BE KHÔNG auto-commit) |

**AUTO (factory tự đóng được):** mọi `.md` trong `docs/mobile/` · type 4 STUB + list-element + userinfo trong yaml · DocType+service+api device-token impl (chờ migrate) · `_dispatch` kênh #3 + FCM sender impl (chờ creds/reload) · test mới. **KHÔNG** chạm: commit / migrate / reload / restart / site_config / cài lib.

> **[BACKLOG — recall/FSCA flag · DEFER ngoài MVP 6-flow]** FE GAP-U5 yêu cầu cờ thu hồi/FSCA (Field Safety Corrective Action) trên màn quét QR. Đây là **BE-GAP THẬT, KHÔNG phải yaml-fix:** `grep -i 'recall\|fsca'` @`services/imm00.py` + @`AC Asset` doctype JSON = **0 hit** — BE **KHÔNG emit** field này, `build_asset_scan_info` (imm00.py:764-840) không có nguồn dữ liệu recall. Khác hẳn 4 field AssetScanInfo drift-closed vòng này (4 field BE emit sẵn, chỉ thiếu khai schema). Để bồi recall/FSCA cần: (1) **data-source recall** (field/doctype lưu trạng thái thu hồi — vd `AC Asset.recall_status` Select hoặc bảng `AC Recall Notice` liên kết), (2) **service enrich** trong `build_asset_scan_info`, (3) schema + test. Ngoài MVP 6-flow → **DEFER**; mở khi có yêu cầu NĐ98 FSCA + chốt data-source với [BA].

---

## 9. Traceability — 6 flow MVP × EPIC × DoD-gate

> Xương sống = [`11-phase-a-exit.md §1`](./11-phase-a-exit.md) traceability matrix. Bảng này map flow → EPIC chốt → trạng thái contract.

| Flow | Endpoint (file:line) | operationId | EPIC | Contract status |
|---|---|---|---|---|
| 1 Login | `oauth2.py:74/123/144` + userinfo `:163` | (auth) / getUserInfo | **B** + **C** (userinfo) | auth passthrough ✅; userinfo path `getUserInfo` + `OidcUserInfo` ✅ (C4) |
| 2 Scan QR | `imm00.py:588/631/454` | resolveQrToken / getAssetScanInfo / getAsset | **C** | typed ✅ (C2+C6 — `QrResolveEnvelope`/`AssetScanInfoEnvelope`/`AssetDetailEnvelope`, 200-oneOf closed-schema) |
| 3 Báo hỏng | `imm12.py:71` | reportIncident | **C** | typed ✅ |
| 4 WO PM/CM/Cal | `imm08.py:91` / `imm09.py:35` / `imm11.py:89` | createPmWorkOrder / createRepairWorkOrder / createCalibration | **C** | typed ✅ (cả 3 — repair+cal C1; createPm C2 requestBody BA chốt, dual json+form C4) |
| 5 Phiếu của tôi | `imm08.py:28` / `imm09` / `imm12.py:197` / `imm11.py:71` | listPmWorkOrders / listRepairWorkOrders / listIncidents / listCalibrations | **C** | typed ✅ (envelope + element — C3-split `Pm`/`RepairWorkOrderListItem` + `IncidentListItem` + `CalibrationListItem` field-disjoint, KHÔNG còn generic) |
| 6 Push FCM | `notifications.py:366` `_dispatch` + register/unregister | registerDeviceToken / unregisterDeviceToken | **D** | device-token: **service D2 ✅ + yaml D4 ✅ typed (Decision-B)**; handler/kênh#3/sender = PENDING ⚠️ |

**Invariant (DONE-gate spec-contract):** list `count == rows` (count khớp drill theo `permission_query_conditions`) — đã fix dashboard KPI (count==drill, `api/dashboard.py`), list-endpoint riêng cần test confirm persona technician/vendor (backlog STATE).

---

## 10. Việc còn lại (open issues — feed STATE)

- **EPIC-C AUTO-PART DONE → gate EPIC-V (C1-C6 + F-C1 + F-C2 + C3-split DONE+GREEN re-verify @source); còn lại = codegen THẬT HARD-STOP USER gate EPIC-V.** ✅ 4 STUB typed (C2) · ✅ **list-element 2 item-schema field-disjoint `Pm`/`RepairWorkOrderListItem` + `IncidentListItem` (C3-split — KNOWN-GAP "KHÔNG ép chung" ĐÓNG, không còn UNION/Phase-E defer)** · ✅ **userinfo/whoami = path `getUserInfo` + `OidcUserInfo` (C4)** · ✅ `createPmWorkOrder` requestBody chốt (C2; C4 vá dual json+form) · ✅ `$ref`-sibling-required gỡ (R2) · ✅ **C5 DoD codegen-dry AUTO-part** = introspection PyYAML XANH (`TestMobileCodegenDryDoD` `test_mob_oas_23a..e`) · ✅ **C6 read-path oneOf** (`TestMobileRead200OneOfClosed` `24a..d`) · ✅ **F-C1 Swagger UI unwrap** (`www/api-docs.html` §F-C1) · ✅ **F-C2 drift-guard parity** (`TestMobileSpecParityRuntime` `25a..e`) · ✅ **C1-residual prose-residue cleared (Vòng 8) + continuation 18f (Vòng 9)** — 7 prose-residue discriminator trong SSoT yaml + docset (ADR (f) R1, roadmap §3.2/§3.5, 04 §8.2, EPIC-C) gỡ sạch về route-by-VALUE closed-schema; guard raw-text `TestMobileProseResidueDiscriminator` (`26a/b/c` RED-trước GREEN-sau, `103 OK @landing Vòng 8`) **+ guard STRUCTURAL `TestMobileOas18fNoDiscriminatorRouteProse` (3 TC: `test_mob_oas_18f` create/read prose-route + detector-RED-before, bổ-trợ 26a trên parsed-spec — bổ sung Vòng 9 như phần mở rộng prose-residue closure)** · ✅ **C-DoD-CFG codegen-config-validity (`TestMobileCodegenConfig` `28a..j` — `openapitools.json` runnable-config 3 generators ↔ `_MOBILE_YAML` SSoT, RED-before/GREEN-after)** · ✅ **F-C4 state-reconciliation roadmap §3 + stale-line-ref guard (`TestMobileRoadmapStateReconciled` `29a..d` — 4-STUB/15-path stale prose → 16-path/2-device-token, raw-text + cross-check `len(spec.paths)`, RED-before/GREEN-after)** — `test_mobile_oas` = **141 OK** (count HIỆN HÀNH @source G3 2026-06-12, `_EXPECTED_TEST_COUNT=141` SSoT; chuỗi phân-rã lịch-sử: 103 @landing Vòng 8 + 3 TC 18f -> 106 @landing Vòng 9 -> 107 @baseline Vòng 11 -> 108 @F-C3 Vòng 11 (meta-guard `TestMobileOasCountSelfVerify`) -> 118 @C-DoD-CFG Vòng 12 (+10 `TestMobileCodegenConfig`) -> 122 @landing Vòng 13 (+4 `TestMobileRoadmapStateReconciled`) -> 133 @landing F-B 2026-06-12 -> 137 @F-B4 2026-06-12 (+4 `TestMobileRefreshOn401DocGuard`) -> 141 @G3 2026-06-12 (+4 `TestMobileTracebackHardeningDocGuard`); guard-suite 7-module tổng **281 OK** re-run @source G3 2026-06-12). **CHỈ còn:** codegen Dart/Kotlin **THẬT** = `[HARD-STOP USER]` (`java` NOT FOUND + `@openapitools/openapi-generator-cli` chưa cài, re-probe @2026-06-11) → gate sang **EPIC-V** V-U1/V-U2. discriminator-note đóng = **Decision-B** (closed-schema KHÔNG discriminator). **Verdict ĐÓNG BĂNG = EPIC-C AUTO-PART DONE → gate EPIC-V.**
- **F-C2 (EPIC-C): 2-spec divergent — AUTO-part ĐÓNG, port-runtime = backlog Phase-F.** ✅ **AUTO DONE:** ADR-MOBILE-001 (k) chốt **A1 (2-spec-by-design)** — YAML 16-path (3.0.3, mang Decision-B) = SSoT codegen mobile; runtime `openapi.spec` 487-path (3.1.0, Swagger UI, KHÔNG Decision-B → codegen-against-runtime dead-deser) = SSoT human-browse/integrator; KHÔNG hợp nhất + scope-boundary + **drift-guard introspection-only** (`TestMobileSpecParityRuntime` `test_mob_oas_25a..e` — parity 10 mobile-business path: tail+verb+security-class YAML↔runtime). 04 §9b + EPIC-C §F-C2 + checklist C-A12 cập nhật. **HARD-STOP USER = backlog Phase-F:** (1) **port Decision-B vào `openapi_overrides.py`** (A2 — runtime carry `oneOf[Env,Error]`; đụng `api/*.py` ⇒ reload gunicorn `--preload` + re-verify 487-path no-regress); (2) **codegen-against-runtime live HTTP** (gate EPIC-V, cần java+npx + reload); (3) **`create_calibration` thiếu `methods=["POST"]`** `imm11.py:89` → runtime verb=GET vs YAML POST (allowlist trong guard tới khi fix decorator @source + reload).
- **F-C3 (Vòng 11, EPIC-C): count-truth reconciliation + meta-guard count-self-verify — DONE+GREEN, introspection/doc-only.** reconcile count-drift `106 -> 107 @baseline Vòng 11` (re-verify @source: `test_mobile_oas` THẬT = 107, KHÔNG phải 106 như doc Vòng 9 viết) + thêm **meta-guard** `TestMobileOasCountSelfVerify.test_mob_oas_NN_count_matches_ssot` (introspect mọi `TestCase` trong module → đếm method `test*` load-được → assert == `_EXPECTED_TEST_COUNT` SSoT định-nghĩa MỘT LẦN @`test_mobile_oas.py` line ~92) ⇒ count-after-add **107 -> 108 @F-C3 Vòng 11**. C-A1 literal count này dùng `@source` (KHÔNG để literal 106 sai-lệch gây phantom-red trên gate đã tick). RED-before/GREEN-after đã chứng minh (const lệch 999 → RED `108 != 999`; const đúng 108 → GREEN) ⇒ chống tái count-drift về sau (drift = RED ngay). **F-C3 round-1 (meta-guard test_mobile_oas) thêm CHỈ 1 test ⇒ +1 mỗi tổng** — guard-suite 6-module **191 -> 192 OK** (baseline = 107+49+13+11+5+6; KHÔNG phải 190) + mobile/OAS total **200 -> 201 OK** (+`test_mobile_preflight` 9; baseline 200, KHÔNG phải 199). ⚠️ **Doc Vòng 11 trước ghi `190 -> 192`/`199 -> 201` = off-by-one BASELINE (ngụ ý F-C3 thêm 2 test trong khi chỉ thêm 1) — reconciled.** **F-C3 round-2 (cross-module SUM meta-guard):** thêm class `TestMobileGuardSuiteCountParity` (4 TC) vào `test_mobile_docset` (5 -> 9) → introspect `def test` THẬT của cả 6 module + assert sum == SSoT + transition-baseline self-consistency (bắt off-by-one `190 vs 191`) ⇒ guard-suite 6-module **192 -> 196 OK** + mobile/OAS total **201 -> 205 OK**, re-verify @source Vòng 11 2026-06-11 `bench --site miyano run-tests` (docset 9 OK). `git diff --stat` api/*.py + services/*.py + yaml-schema = TRỐNG (KHÔNG reload gunicorn, KHÔNG migrate). Cross-ref ADR-MOBILE-001 (count-truth = source THẬT, không phải mốc lịch-sử). UNCOMMITTED (chờ USER).
- **C-DoD-CFG (Vòng 12, EPIC-C→V): codegen-config-validity guard `openapitools.json` ↔ `_MOBILE_YAML` — DONE+GREEN, test+doc-only.** Đóng GAP: `openapitools.json` NAY là runnable-config (3 generators `mobile-dart`/`mobile-kotlin`/`mobile-typescript`, version `7.23.0`, mỗi cái `inputSpec` trỏ mobile YAML) NHƯNG TRƯỚC Vòng 12 KHÔNG guard nào kiểm → config có thể drift rời YAML (sai path/xoá YAML) mà suite VẪN xanh = handoff codegen FAIL-CÂM máy USER. Thêm class **`TestMobileCodegenConfig`** (`test_mob_oas_28a..j`, +10 TC) — STDLIB `json.load` (KHÔNG java/npx/toolchain): assert version-pin `7.23.0` + `generators` non-empty ≥1 `generatorName` + **MỌI `inputSpec` resolve == `_MOBILE_YAML` SSoT** (single-path, `test_mobile_oas.py:86`, KHÔNG hardcode lại) + file tồn tại + name/output non-empty. **RED-before/GREEN-after** (inject IN-MEMORY deepcopy, file read-only KHÔNG sửa): sai-path→RED (28g), xoá generators→RED (28h), `version=''`→RED (28i), control config-thật→GREEN (28j). Reconcile EPIC-V §3.2 stale prose 'bare version-pin/KHÔNG runnable' → trạng-thái THẬT runnable + tick V1 [AUTO]-part config-non-empty (guard máy-kiểm thay 1-liner thủ công). Count: `test_mobile_oas` **108 -> 118 @C-DoD-CFG Vòng 12** (+10); guard-suite 6-module **196 -> 206 OK** + mobile/OAS total **205 -> 215 OK** — cross-module count-parity giữ bởi `test_mobile_docset::TestMobileGuardSuiteCountParity` (`_GUARD_SUITE_EXPECTED`/`_GUARD_SUITE_SUM`/`_MOBILE_OAS_TOTAL` cập); meta-guard `TestMobileOasCountSelfVerify` (`_EXPECTED_TEST_COUNT` 108→118). Re-run @source Vòng 12 2026-06-11 `bench --site miyano run-tests` (oas 118 OK, docset 9 OK, 6-module 206 OK). `git diff --stat` api/*.py + services/*.py + yaml-schema-path/operationId = TRỐNG (openapitools.json read-only, KHÔNG reload gunicorn, KHÔNG migrate, KHÔNG cài lib — STDLIB json/pathlib). Decision-B intact (0 discriminator). UNCOMMITTED (chờ USER).
- **F-C4 (Vòng 13, EPIC-C): state-reconciliation roadmap §3 (prose trạng-thái-cũ → trạng-thái-hiện-hành) + stale-line-ref guard — DONE+GREEN, doc+test-only.** Đóng GAP: roadmap §3 (CURRENT + TO-BUILD) còn quảng-cáo việc-đã-xong (4 path scan/createPm typed C2 + list-element C3-split) NHƯ việc-cần-làm: §3.1 ghi path-count CŨ (yaml THẬT=16); §3.3 heading + ref `_STUB_PATHS` bằng số-dòng-tuyệt-đối (line-drift CHẾT; symbol thật `set(_DEVICE_TOKEN_FROZEN)`=2 device-token) + 3 hàng nhãn untyped + list-element generic; §3.5 `[AUTO]`/`[AUTO+BA]` rows + §9 matrix nhãn-cảnh-báo. **LÀM (doc):** reconcile §3.1/§3.3/§3.5/§9 → trạng-thái THẬT (16-path; 4 path ✅ typed C2/C6; list-element ✅ C3-split; STUB còn = 2 device-token EPIC-D); gỡ `0.1.0-skeleton` khỏi gap-framing (= CI-guard concern EPIC-G). **LÀM (test):** class **`TestMobileRoadmapStateReconciled`** (`test_mob_oas_29a..d`, +4 TC) — raw-text scan roadmap: 0 anchor stale (chỉ giữ dạng lịch-sử [SUPERSEDED]/@landing) + claim path-count khớp `len(spec.paths)`=16 THẬT + ref `_STUB_PATHS` dạng-SYMBOL (KHÔNG line-tuyệt-đối) + **RED-before/GREEN-after** PROVEN (inject anchor stale vào file/bản-sao → 29a/29d RED; revert → GREEN). Count: `test_mobile_oas` **118 -> 122 @F-C4 Vòng 13** (+4); guard-suite 6-module **206 -> 210 OK** + mobile/OAS total **215 -> 219 OK** — cross-module count-parity giữ bởi `test_mobile_docset::TestMobileGuardSuiteCountParity` (`_GUARD_SUITE_EXPECTED`/`_GUARD_SUITE_SUM`/`_MOBILE_OAS_TOTAL` cập + transition-baseline +Δ(F-C4=4)); meta-guard `TestMobileOasCountSelfVerify` (`_EXPECTED_TEST_COUNT` 118→122). Re-run @source Vòng 13 2026-06-11 (oas 122 OK, docset 9 OK, 6-module 210 OK). `git diff --stat` api/*.py + services/*.py + yaml-schema = **TRỐNG** (introspection-only, KHÔNG reload gunicorn, KHÔNG migrate, KHÔNG cài lib — STDLIB). 5 QUYẾT ĐỊNH KHOÁ bám (Decision-B no-discriminator · device-token=EPIC-D · re-verify @source theo symbol · 2-spec A1 · count-truth=source). UNCOMMITTED (chờ USER).
- **F-B4 (2026-06-12, EPIC-B): REPORT-SHAPE doc↔code drift-guard (B1/B4 acceptance #3) + count-reconcile @source — DONE+GREEN, doc+test-only (re-verify run-only, KHÔNG sửa code). `[count-claim @F-B4-landing — SUPERSEDED @G3+G-A8: oas 137→141 (+4 TC G3 `TestMobileTracebackHardeningDocGuard`), preflight 17→26 (+G-A push/preflight TC), sec-gate 44→52 (+8 G-A8 GUARD-9), guard-suite 6-module 225→7-module 281, mobile/OAS 242→307 — SSoT HIỆN HÀNH `_EXPECTED_TEST_COUNT=141`/`_GUARD_SUITE_SUM=281`/`_MOBILE_OAS_TOTAL=307`, xem §Baseline + C-A1 + GO-2 + roadmap §3.5/§3.6/§7.1]`** Drift-guard `12 §1.2/§1.3` ↔ `preflight.verify_oauth_client()`: class **`TestMobilePreflightReportShapeDocGuard`** (`TC-MOB-PRE-14..17`, +4 TC `test_mobile_preflight`) — 5 report-key (ready/client_count/checks/blockers/checked_client từ output nhánh count==0) `<key>` trong 12 §1.2 + 7 check-field-name (`client_count` ∪ 6 field `_evaluate_client`) `<field>` trong 12 §1.3, expected-set **DERIVE TỪ `preflight.py` runtime** (KHÔNG hardcode literal → preflight đổi field/key mà doc 12 không cập nhật → ĐỎ) + blocker VI 'Chưa có OAuth Client' nguyên-văn + RED-before/GREEN-after PROVEN (`test_17` xoá field/key in-memory → RAISE; khôi phục → GREEN). `test_mobile_preflight` **13 -> 17 OK @F-B4** (+4). **COUNT-RECONCILE @source (F-B current-claim stale):** `test_mobile_oas` **133 -> 137** (SSoT code `_EXPECTED_TEST_COUNT=137 test_mobile_oas.py:132` ĐÃ bump; +4 `TestMobileRefreshOn401DocGuard` F-B2 TC-MOB-OAS-30a..d landed sau F-B-doc) · `test_mobile_preflight` **9 -> 17** (+4 F-B3 + 4 F-B4) · guard-suite 6-module = 137+49+13+11+9+6 = **221 -> 225 OK** · mobile/OAS total = 225+17 = **242 OK**. Đồng-bộ literal: C-A1/§Baseline/GO-2/B-A1 + roadmap §3.5/§3.6 + EPIC-C verdict line. `git diff --stat` api/*.py + services/*.py + yaml-schema + preflight.py = **TRỐNG** (doc+test-only, KHÔNG sửa code, KHÔNG reload gunicorn, KHÔNG migrate, STDLIB-only). EPIC-B AUTO-part B-A1/B-A6(F-B4) = **PASS**. UNCOMMITTED (chờ USER).
- **F-B (2026-06-12, EPIC-B): acceptance reconciliation @source + count-drift fix — DONE+GREEN, doc-only (re-verify run-only, KHÔNG sửa code). `[count-claim SUPERSEDED @F-B4: preflight 9→17, oas 133→137, suite 221→225 — xem F-B4 ở trên]`** RE-VERIFY @source (mốc F-B, ĐÃ bị F-B4 vượt): `test_mobile_preflight` = **9 OK** `@landing F-B` (B-A1 [x]) · `TestMobileOAuthToken` `test_mob_oauth_token_01..05` = **5 OK** subset (B-A2 [x]) · `test_mobile_oas` toàn suite = **133 OK** `@landing F-B` (`_EXPECTED_TEST_COUNT` SSoT) · guard-suite 6-module = 133+49+13+11+9+6 = **221 OK** `@landing F-B`. GREP-VERIFY B-A4 [x]: `grant_type=refresh_token`/`refresh MỘT lần` ≥1 hit MỖI file (`03-auth-oauth2.md` §1.3e/§2.5/§2.6 dòng 110/236/260-272 + `04-api-contract.md` §9d(n) dòng 894-913). **B-A3 reconcile** = preflight verifier READ-ONLY (`[n/a-by-design]` no-helper-write): DB-write OAuth Client = HARD-STOP USER (EPIC-B §3.3/B1) → idempotent-vì-không-ghi-DB; ngữ-nghĩa MỚI = "preflight READ-ONLY chạy nhiều lần count bất biến" (TC-MOB-PRE-09), BỎ yêu-cầu helper-write. **COUNT-DRIFT reconcile** `122 @F-C4 Vòng 13 -> 133 @F-B` (+11 TC landed sau Vòng 13): C-A1 + §Baseline THẬT + GO-2 + roadmap §3.5/§3.6 literal = **133**. `git diff --stat` api/*.py + services/*.py + yaml-schema = **TRỐNG** (re-verify run-only + doc-only, KHÔNG sửa code, KHÔNG reload gunicorn, KHÔNG migrate). EPIC-B AUTO-part (B-A1/B-A2/B-A4 [x] + B-A5 [x] + B-A3 reconciled) = **PASS**; B-U1..U4 + B-DoD = `[ ]` **[HARD-STOP USER]** (cloud migrate + tạo OAuth Client THẬT + verify_oauth_client ready=True + smoke PKCE). UNCOMMITTED (chờ USER).
- **EPIC-B:** OAuth Client count=0 (HARD-STOP USER) · token TTL 3600s KNOWN-LIMIT · helper provisioning write = **no-helper-write (reconciled F-B):** DB-write OAuth Client = HARD-STOP USER, preflight verifier READ-ONLY = gate khách quan (idempotent-vì-không-ghi).
- **EPIC-G:** 5 knob ABSENT (allow_cors/host_name/qr_base_url/allow_error_traceback/rate_limit) · `allow_error_traceback`=System Setting default=1 → PROD leak traceback · decorator-429-no-header · CI-guard servers placeholder + version skeleton.
- **EPIC-D:** DocType + service + api/mobile/v1 + RBAC wiring + kênh #3 + FCM sender = 0 code (Phase E) · library FCM chưa chọn · FCM creds HARD-STOP USER.
- **EPIC-V:** toolchain (JDK+generator) chưa cài = **[HARD-STOP USER]** (CHỈ còn lại đây) · ~~`openapitools.json` bare version-pin~~ → **ĐÓNG (Vòng 12 C-DoD-CFG):** NAY runnable-config 3 generators trỏ YAML + guard chống drift `inputSpec`↔YAML (`TestMobileCodegenConfig` 28a..j) · ~~Kotlin vs TypeScript chưa chốt~~ → **ĐÓNG:** config khai cả Kotlin (`jvm-retrofit2`) + TypeScript (`typescript-axios`) là target THẬT · ~~E2E runbook hợp nhất chưa viết (V3)~~ → **ĐÓNG (V3 DONE 2026-06-12):** chương [`14-e2e-field-tech-runbook.md`](./14-e2e-field-tech-runbook.md) (6 flow + 6 operationId trục + curl/dart + expected envelope; docset 9 OK + oas 141 OK) · handoff bundle chưa đóng gói (V4).

---

## Tham chiếu chéo

- Roadmap 6 phase: [`00-overview.md §3`](./00-overview.md) · Index docset: [`README.md`](./README.md)
- EPIC-C: [`04-api-contract.md`](./04-api-contract.md) · `openapi/assetcore-mobile.openapi.yaml`
- EPIC-B: [`03-auth-oauth2.md`](./03-auth-oauth2.md) · [`12-phase-b-preflight.md`](./12-phase-b-preflight.md) · [`ADR-MOBILE-001.md`](./ADR-MOBILE-001.md)
- EPIC-G: [`10-deploy-ops.md`](./10-deploy-ops.md) · [`08-security-compliance.md`](./08-security-compliance.md) · [`ADR-MOBILE-004.md`](./ADR-MOBILE-004.md)
- EPIC-D: [`06-push-fcm.md`](./06-push-fcm.md) · [`ADR-MOBILE-002.md`](./ADR-MOBILE-002.md)
- EPIC-V: [`09-native-repo-guide.md`](./09-native-repo-guide.md) · [`11-phase-a-exit.md`](./11-phase-a-exit.md)
- Exit gate Phase A: [`11-phase-a-exit.md`](./11-phase-a-exit.md)
