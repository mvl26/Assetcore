# Mobile BE Completion — ACCEPTANCE CHECKLIST (nghiệm thu 5 EPIC + GO-LIVE GATE)

> **Phạm vi:** bộ tài liệu "BE COMPLETION" cho app mobile (AssetCore = backend-for-mobile). Repo UI mobile native (Flutter/RN, project KHÁC) gọi API để chạy app.
> **DoD TỔNG (MVP field-tech, 6 flow E2E THẬT trên cloud):** (1) đăng nhập OAuth2+refresh · (2) quét QR → hồ sơ thiết bị · (3) báo hỏng (Incident IMM-12) · (4) yêu cầu WO PM/CM/Cal (IMM-08/09/11) · (5) "phiếu của tôi" · (6) push FCM.
> **KHÔNG gồm (post-MVP, ghi rõ):** offline-sync (ch.07), manager/duyệt, mở rộng đa-module.
> **3 quyết định AUTHORITATIVE (USER, 2026-06-11):** OAuth2+refresh (Frappe provider, Authorization Code + PKCE, scope↔capability SSoT) · field-tech MVP · native repo riêng. Lớp mobile = **BỌC + TÁI DÙNG** endpoint+capability có sẵn — KHÔNG viết lại logic.

**Cách đọc bảng:** mỗi dòng = 1 tiêu chí nghiệm thu kiểm-được. Cột `Lệnh / cách kiểm` chạy được THẬT. Cột `Tag`: `[AUTO]` = factory tự đóng được (doc/yaml/test/introspection) · `[HARD-STOP USER]` = cần cloud/migrate/site_config/FCM creds/toolchain. Cột `TT` (trạng thái) tick khi factory/user đóng: `[ ]` chưa · `[~]` đang dở · `[x]` xong+verify.

**Quy ước owner:** `[BA]` doc/yaml/spec · `[BE]` code api/services · `[QA]` test/audit · `[USER]` thao tác cloud/creds.

**Nguồn (grounded — file:line đã verify @working-tree 2026-06-11):** xem từng dòng. Các EPIC chi tiết task ở 5 file `<EPIC>-*.md` cùng thư mục (cross-ref bằng ID task vd C1, B2, D3).

---

## 0. Tổng quan dependency + thứ tự đóng gate

```
C (độc lập: doc/yaml/test — đóng NGAY)
   │
   ├──► B  (∥ thiết kế D)            ── cần USER: bench migrate (OAuth Client/device-token)
   │       │
   │       ▼
   └──►   G (go-live)               ── cần USER: cloud commit+migrate + site_config + HTTPS/nginx
           │
           ▼
          D (push FCM)              ── cần B + FCM creds (USER)
           │
           ▼
          V (codegen verify + handoff)   ── chốt cuối: gen client THẬT + 6-flow smoke + handoff
```

- **EPIC-C** không phụ thuộc gì → đóng được hoàn toàn bằng `[AUTO]` (trừ codegen THẬT cần toolchain → V).
- **EPIC-B/D/G/V** có ≥1 dòng `[HARD-STOP USER]` → KHÔNG thể tuyên bố DONE chỉ bằng factory.
- **Quy tắc gate:** một EPIC = PASS chỉ khi **mọi dòng bắt buộc của EPIC đó = `[x]`**. GO-LIVE = PASS chỉ khi **C+B+D+G+V đều PASS** và **GO-LIVE GATE tổng (§6)** xanh.

---

## 1. EPIC-C — API Contract (codegen-ready)

> Mục tiêu: YAML `docs/mobile/openapi/assetcore-mobile.openapi.yaml` codegen-clean (Dart/Kotlin), 0 dangling `$ref`, mọi path MVP typed. Tham chiếu task: `EPIC-C-api-contract.md` (C1..Cn).

| # | Tiêu chí nghiệm thu | Lệnh / cách kiểm (THẬT) | PASS khi | Owner | Tag | TT |
|---|---|---|---|---|---|---|
| C-A1 | Lint YAML + meta đông cứng (openapi 3.0.3) | `bench --site miyano run-tests --module assetcore.tests.test_mobile_oas` | `Ran 141 tests ... OK` (count HIỆN HÀNH @source G3 2026-06-12, `_EXPECTED_TEST_COUNT=141` SSoT `test_mobile_oas.py:136`; gồm meta-guard F-C3 `test_mob_oas_NN_count_matches_ssot` + C-DoD-CFG `TestMobileCodegenConfig` 28a..j + F-C4 `TestMobileRoadmapStateReconciled` 29a..d + F-B2 `TestMobileRefreshOn401DocGuard` 30a..d + G3 `TestMobileTracebackHardeningDocGuard` 4 TC); `test_mob_oas_01` xanh (safe_load OK, openapi==3.0.3) | BA | [AUTO] | [x] |
| C-A2 | 0 dangling `$ref` toàn spec | cùng lệnh trên — `test_mob_oas_09_no_dangling_refs` (+ C5 `test_mob_oas_23c`) | test xanh (walk STDLIB resolve mọi pointer) | BA | [AUTO] | [x] |
| C-A3 | path STUB còn lại RỜI STUB + typed (~~2 device-token GIỮ Stub~~ **device-token TYPED ở EPIC-D D4 §8.9 — `_STUB_PATHS=∅`**) | `_STUB_PATHS` = ∅ (sau D4); `test_mob_oas_07` xanh + C5 `test_mob_oas_23b/23d` | 10 path MVP (`resolve_qr_token`/`get_asset_scan_info`/`get_asset`/`create_pm_work_order`/report/repair/cal/3-list) có response `data` typed (0 Stub); **3 read sau C6: 200 = oneOf `[<ReadEnvelope>, Error]`** (route-by `body.success` NHƯ creates — KHÔNG còn single-$ref); **2 device-token NAY typed (D4, Decision-B oneOf) → 0 STUB-on-MVP toàn bộ 12 path; `responses/Stub` → forward-reserve `_RESERVED_ORPHANS`** | BA | [AUTO] | [x] |
| C-A4 | P1 in-handler-error-on-HTTP-200 đóng theo **Decision-B** (response "200" oneOf[Env\|Error] **closed-schema KHÔNG discriminator**) cho **create + read** · **prose-residue cleared (Vòng 8 C1-residual)** | grep yaml 4 create + 3 read path; `test_mob_oas_18b_no_boolean_discriminator` + `_assert_200_oneof_closed_distinct` (create) + `test_mob_oas_24a/b` (read, `TestMobileRead200OneOfClosed`) + **`test_mob_oas_26a/b/c` (`TestMobileProseResidueDiscriminator` — prose KHÔNG mô tả discriminator như cơ-chế hiện-hữu)** | mỗi 200 oneOf 2 nhánh CLOSED (`additionalProperties:false`) + disjoint required-set; **KHÔNG** `discriminator` key. **3 read route-by GIÁ-TRỊ `body.success`/`body.http_status` GIỐNG creates** (C6 read-path closure — in-handler 404 + vendor-IDOR-403 arrive HTTP-200+Error). **C1-residual:** 7 prose-residue (yaml description/comment) + docset (ADR (f) R1, roadmap §3.2/§3.5, 04 §8.2) gỡ sạch — mô tả route-by-VALUE closed-schema, KHÔNG nhắc discriminator hiện-hữu (chỉ NEGATED/`[SUPERSEDED]`). grep `discriminator success`/`ROUTE THEO body.success discriminator` = 0; `discriminator:` key VẪN = 0 | BA | [AUTO] | [x] |
| C-A5 | **C3-split — 2 item-schema field-disjoint `Pm`/`RepairWorkOrderListItem`** (KHÔNG còn 1 UNION `WorkOrderListItem`) | grep `PmWorkOrderListItem`/`RepairWorkOrderListItem`/`IncidentListItem` trong yaml (≥1 mỗi) + grep `WorkOrderListItem:` UNION cũ = 0; `test_mob_oas_21` (TestMobileListItemTyped, 8 TC) + `test_mob_oas_14` | 3 item-schema RIÊNG defined; mỗi list path `$ref` item RIÊNG (`listPmWorkOrders`→`PmWorkOrderListItem`, `listRepairWorkOrders`→`RepairWorkOrderListItem`; KHÔNG còn `{type:object}` generic, KHÔNG share 1 ref); rows-key `data` GIỮ; `name` required + `additionalProperties:false`; **disjoint-field assert** (PM-only ∩ CM-only = ∅; `test_mob_oas_21g`) — field-set khớp chữ-ký service (imm08.list_work_orders vs imm09.list_work_orders); 0 dangling | BA | [AUTO] | [x] |
| C-A6 | userinfo/whoami OIDC wire vào spec | grep `openid_profile`/`OidcUserInfo` trong yaml (≥1); `test_mob_oas_22` (TestMobileUserInfo) | path `GET openid_profile` (`getUserInfo`, security `[openid]`) + schema `OidcUserInfo` RAW (8 claim grounded `oauth.py:530-555`) → app hiện tên+role KTV sau login; 0 dangling | BA | [AUTO] | [x] |
| C-A7 | requestBody oneOf json+form mọi RPC path | `test_mob_oas_13g/16f/17f` (Body classes) + guard `test_mob_oas_22i` (`_assert_rpc_requestbody_form_json` 4 RPC) | mỗi RPC requestBody (3 create-triad + `createPm` vá C4) = oneOf `application/json` + `application/x-www-form-urlencoded` (Frappe `form_dict`) — XANH | BA | [AUTO] | [x] |
| C-A8 | 0 `$ref`-with-sibling (codegen `--strict` clean) | `test_mob_oas_19` (TC-MOB-OAS-19) | KHÔNG `$ref` nào kèm sibling-key toàn spec | BA | [AUTO] | [x] |
| C-A9 | 401/403 symmetry 12 path MVP | `test_mob_oas_12_error_status_class_401_403_split` | 12 path declare 401 == 12 declare 403 (mirror `_PATHS_REQUIRE_401`/`_403`) | BA | [AUTO] | [x] |
| C-A10 | orphan-component ⊆ allow-list RESERVED | `test_mob_oas_10_orphan_components_within_reserved_allow_list` | orphan ⊆ `_RESERVED_ORPHANS`; KHÔNG stale | BA | [AUTO] | [x] |
| C-A11 | **C6 — read-path P1 closure: 3 GET read 200 = oneOf `[<ReadEnvelope>, Error]`** | grep yaml `QrResolveEnvelope`/`AssetScanInfoEnvelope`/`AssetDetailEnvelope` mỗi cái trong oneOf cạnh `Error`; `test_mob_oas_24a..d` (`TestMobileRead200OneOfClosed`) + `test_mob_oas_20a` (updated) | mỗi 3 read 200 = oneOf 2 nhánh `[<ReadEnvelope>, Error]` CLOSED (`additionalProperties:false` envelope-level) + disjoint required-set, KHÔNG discriminator (Decision-B); status-set pre-handler-only (404 KHÔNG status-line key); C5 `TestMobileCodegenDryDoD` GIỮ GREEN (read-oneOf = typed). **RED-before/GREEN-after** (revert read→single-$ref → 24a/20a/23d/23e RED) | BA | [AUTO] | [x] |
| C-A12 | **F-C2 — drift-guard parity 16-path YAML ↔ runtime spec (2-spec-by-design A1)** | `test_mob_oas_25a..e` (`TestMobileSpecParityRuntime`) — import IN-PROCESS `openapi.generate_spec()` (KHÔNG HTTP/reload/migrate) | 10 mobile-business path (loại 2 device-token STUB + 4 auth passthrough) TỒN TẠI trong runtime spec với **CÙNG dotted-path-tail + CÙNG verb (allowlist `create_calibration` — runtime GET vs YAML POST do thiếu `methods=["POST"]` `imm11.py:89`, backlog Phase-F) + CÙNG security-class** (authed/guest); path lệch → RED (chống drift câm). **RED-before/GREEN-after** (inject IN-MEMORY: xoá runtime path / đổi security-class → RED; control sạch → GREEN). Quyết định A1 KHÔNG hợp nhất; port Decision-B vào runtime (A2) = **[HARD-STOP USER] backlog Phase-F**. ADR-MOBILE-001 (k) | BA | [AUTO] | [x] |
| C-A13 | **C-DoD-CFG (Vòng 12) — codegen-config-validity guard: `openapitools.json` ↔ `_MOBILE_YAML` consistency** | `test_mob_oas_28a..j` (`TestMobileCodegenConfig`) — STDLIB `json.load`, KHÔNG java/npx/toolchain. Grounded source: `openapitools.json` (3 generators `mobile-dart`/`mobile-kotlin`/`mobile-typescript`, version `7.23.0`) ↔ `_MOBILE_YAML` (`test_mobile_oas.py:86`) | `generator-cli.version=='7.23.0'` (NOT empty/skeleton) + `generators` non-empty ≥1 `generatorName` + **MỌI `inputSpec` resolve (repo-relative) == `_MOBILE_YAML` SSoT** (single-path, KHÔNG hardcode) + file tồn tại + mỗi gen `generatorName`/`output` non-empty. **RED-before/GREEN-after** (inject IN-MEMORY deepcopy, file read-only KHÔNG sửa): sai inputSpec → RED (28g); xoá `generators` → RED (28h); `version=''` → RED (28i); config THẬT → GREEN (28j control). Bắt drift config↔YAML = handoff codegen fail-câm máy USER. SSoT doc: `EPIC-V-codegen-verification.md §3.2` + §4 V1 | BA | [AUTO] | [x] |
| C-A14 | **F-C4 (Vòng 13) — stale-line-ref guard: roadmap §3 state-reconciliation ↔ source** | `test_mob_oas_29a..d` (`TestMobileRoadmapStateReconciled`) — raw-text scan `13-be-completion-roadmap.md` + cross-check `len(spec.paths)`. Reconciled: §3.1 `15→16 path/operationId`; §3.3 heading + ref `_STUB_PATHS = set(_DEVICE_TOKEN_FROZEN)` (symbol, KHÔNG `152-157`) + 3 hàng + list-element → ✅ typed C2/C3-split; §3.5 + §9 matrix → ✅ DONE | `grep -E '15 path\|15/15 operationId\|4 STUB path còn lại\|chưa typed\|còn generic ⚠️\|152-157' roadmap` = **0** (trừ dạng [SUPERSEDED]/@landing); `16 path` claim == `len(spec.paths)`=16 THẬT (29b); ref `_STUB_PATHS` dùng SYMBOL KHÔNG line-tuyệt-đối (29c); **RED-before/GREEN-after** (inject anchor stale vào bản-sao → 29a/29d RED; revert → GREEN; [SUPERSEDED] KHÔNG bị flag). doc/test introspection-only — `git diff --stat` api/services .py + yaml = TRỐNG. SSoT: `EPIC-C-api-contract.md §F-C4` | BA | [AUTO] | [x] |
| **C-DoD-proxy** | **codegen-dry introspection PyYAML (AUTO proxy)** | `test_mob_oas_23a..e` + 5 negative `test_mob_oas_23d_negative_*` (`TestMobileCodegenDryDoD`) | 0 path MVP còn Stub + 0 dangling `$ref` + mọi path MVP `data` typed `$ref` — chạy KHÔNG-toolchain; **anti-false-green**: inject Stub/generic/dangling vào deepcopy MVP path → guard RED (chứng minh guard THẬT bắt regress, KHÔNG pass-suông) | BA | [AUTO] | [x] |
| **C-DoD** | **openapi-generator chạy THẬT sạch (Dart/Kotlin)** | `openapi-generator generate -i docs/mobile/openapi/assetcore-mobile.openapi.yaml -g dart -o /tmp/gen-dart` (+`-g kotlin`) | exit 0, 0 ERROR, 0 dangling `$ref`, mọi path MVP sinh method | QA | **[HARD-STOP USER]** (`java` NOT FOUND + generator chưa cài — probe @2026-06-11) | [ ] |

**Baseline THẬT đã chạy 2026-06-12 (F-B re-ground @source — proxy cho C-DoD khi chưa có toolchain):**
`test_mobile_oas` = **Ran 141 OK** (count HIỆN HÀNH @source G3 2026-06-12: `Ran 141 tests ... OK`; `_EXPECTED_TEST_COUNT=141` SSoT `test_mobile_oas.py:136`, meta-guard `TestMobileOasCountSelfVerify` self-verifies). Vòng 11 re-verify @source = **107** (count-drift reconciled khỏi 106) + meta-guard F-C3 `TestMobileOasCountSelfVerify` ⇒ **108**. Vòng 12 C-DoD-CFG: +10 TC class **`TestMobileCodegenConfig`** (`test_mob_oas_28a..j` — codegen-config-validity guard `openapitools.json` ↔ `_MOBILE_YAML`, STDLIB `json.load`, RED-before/GREEN-after) ⇒ **118**. Vòng 13 F-C4: +4 TC class **`TestMobileRoadmapStateReconciled`** (`test_mob_oas_29a..d` — stale-line-ref guard roadmap §3 ↔ source, raw-text + cross-check `len(spec.paths)`, RED-before/GREEN-after) ⇒ **122 @landing Vòng 13**. F-B 2026-06-12 re-verify @source = **133** (count-drift reconciled khỏi 122). F-B2 +4 TC class **`TestMobileRefreshOn401DocGuard`** (`test_mob_oas_30a..d` — refresh-on-401 doc-guard B-A4, raw-text scan `03 §2.5/§2.6` + `04 §9d(n)` + cross-file `grant_type=refresh_token` parity, RED-before string-mutate) ⇒ **137 @F-B4 2026-06-12** (landing-mark F-B2). G3 +4 TC class **`TestMobileTracebackHardeningDocGuard`** (prod-hardening 'TẮT allow_error_traceback System Setting=0' doc-guard) ⇒ **141 @G3 2026-06-12** (SSoT const = 141). · `test_oas_generator` = **49 OK** · `test_oas_serve` = **13 OK** · `test_oas_signatures` = **11 OK** · `test_mobile_docset` = **9 OK** · `test_mobile_capability_map` = **6 OK** · `test_mobile_security_gate` = **52 OK** → **guard-suite 7-module tổng 281 OK** (141+49+13+11+9+6+52, re-run @source G3 2026-06-12). _Chuỗi phân-rã LỊCH-SỬ (@landing từng mốc, KHÔNG phải count hiện hành): 75 OK @C4-landing → 85 @C5 → 89 @C6 → 96 @F-C2 → 98 @C3-split → 103 @C1-residual Vòng 8 → 106 @18f Vòng 9 → 107 @baseline Vòng 11 → 108 @F-C3 Vòng 11 → 118 @C-DoD-CFG Vòng 12 → 122 @F-C4 Vòng 13 → 133 @F-B → 137 @F-B4 2026-06-12 (+4 TC `TestMobileRefreshOn401DocGuard`) → **141 @G3 2026-06-12** (+4 TC `TestMobileTracebackHardeningDocGuard`)._ STDLIB PyYAML/json introspection (C5 + C-DoD-CFG = proxy CHÍNH-THỨC cho codegen-DoD) PASS thay codegen THẬT (Dart/Kotlin) tới khi USER cấp `java`+`npx` → chuyển sang **EPIC-V**. EPIC-C verdict = **AUTO-PART DONE → gate EPIC-V**.

**Grounded source (4 STUB còn lại — verify @working-tree):**
- `resolve_qr_token` — `api/imm00.py:329` (`@rate_limit` :328 → 429), `rbac.require("asset.read")` :356, 404 `_ERR_ASSET_NOT_FOUND` :360, vendor-IDOR `assert_vendor_can_access` :363.
- `get_asset_scan_info` — `api/imm00.py:372` (`@rate_limit` :371 → 429); build qua `services/imm00.py::build_asset_scan_info` → trả `{name,asset_code,...,available_actions,pm_overdue,calibration_overdue}`.
- `get_asset` — `api/imm00.py:288`, 404 :291, vendor-IDOR :294, `return _ok(_strip_qr_token(doc))` :324 + enrich `pm_overdue`/`calibration_overdue` :318-319.
- `create_pm_work_order` — `api/imm08.py:91` (untyped `_form_dict` :17 + `rbac.require("pm.create")` :92) → `svc.create_adhoc_work_order` return `{name,status,checklist_items_count}` (`services/imm08.py:836-840`). **Lý do giữ STUB lâu nhất:** signature untyped `_form_dict` → cần định nghĩa typed requestBody (required `asset_ref`/`pm_schedule`/`due_date`).

**Grounded P1 (in-handler-error-on-HTTP-200, verify @source):** cơ chế `service nthrow(MSG.X)` (`notify.py`) → `ServiceError(http_status)` (`errors.py`) → `handle()` bắt → `_service_error_to_envelope` → `_err(...,http_status)` (`response.py`). Body chứa `code`+`http_status` NHƯNG status-line VẪN 200 (handle return dict; `hooks.py` no after_request). Bằng chứng/path:
- `imm12.report_incident` (`api/imm12.py:71`): guest `_err(401)` :92 = **dead-code over HTTP** (dispatcher-403 trip trước); cap-403 `_err(_MSG_FORBIDDEN,403)` :96 = in-handler HTTP-200+Error (DUAL-SHAPE 403); svc 422 clinical_impact / 404 asset∄; return `{name,status,severity}` (`services/imm12.py:410`).
- `imm09.create_repair_work_order`: `rbac.require('repair.create')` (`api/imm09.py:40`) → PermissionError HTTP-403 THẬT (SINGLE-shape); svc 404 / 409 HAS_OPEN_WO; return `{name,status,sla_target_hours}` (`services/imm09.py:435`).
- `imm11.create_calibration`: `rbac.require('calibration.create')` (`api/imm11.py:95`) → HTTP-403 THẬT (SINGLE-shape); svc 404 / 409 ASSET_BLOCKED (CAL-008); return `{name,status}` status init `Scheduled` (`services/imm11.py:332`).

> ✅ **SELF-CORRECTION P1 — RESOLVED 2026-06-11 (Decision-B):** discriminator `propertyName: success` = boolean → codegen-illegal. **Đã chọn phương án (B)**: BỎ discriminator hoàn toàn; 2 nhánh oneOf `[<CreatedEnvelope>, Error]` đóng băng bằng **closed-schema** (`additionalProperties:false`) + **disjoint required-set** ⇒ codegen route ĐÚNG nhánh theo shape (KHÔNG cần discriminator). Route runtime theo `body.success`/`body.http_status` (in-handler error arrive HTTP-200 body §5). Guard THẬT: `test_mob_oas_18b_no_boolean_discriminator` (assert KHÔNG `discriminator` key) + `_assert_200_oneof_closed_distinct` (2 nhánh closed + disjoint) → XANH. yaml THẬT: 0 `discriminator:` key (mọi occurrence còn lại CHỈ trong comment/description đã NEGATED — "KHÔNG discriminator"). C-A4 = `[x]`. (Phương án A `result_type` STRING đã BỎ — KHÔNG triển khai.)
>
> ✅ **C1-residual — RESOLVED Vòng 8 (2026-06-11):** sau Decision-B, 7 prose-residue (yaml description/comment 3 create path + comment header) VẪN viết "ROUTE THEO body.success discriminator" / "+ discriminator success" như **cơ-chế hiện-hữu** → mâu thuẫn Decision-B. Đã gỡ sạch + đồng-bộ docset (ADR (f) R1 bullet, roadmap §3.2/§3.5/§3-table, 04 §8.2 note, EPIC-C §3.4/§C2/§C1-close). Nay mọi mô tả = **route-by-VALUE `body.success`** (Created.success.enum=[true] vs Error.success.enum=[false]) qua closed-schema disjoint required-set; discriminator chỉ xuất hiện dạng NEGATED / `[SUPERSEDED]`. Guard chống tái-phát: **`TestMobileProseResidueDiscriminator` (`test_mob_oas_26a/b/c`)** — RED-trước-fix (inject residue → flag) GREEN-sau. yaml giữ **0 `discriminator:` key** (KHÔNG thêm key). `test_mobile_oas` = **103 OK @landing Vòng 8**.
>
> ✅ **C1-residual continuation — Vòng 9 (2026-06-11):** bổ sung guard STRUCTURAL **`TestMobileOas18fNoDiscriminatorRouteProse` (3 TC: `test_mob_oas_18f` create-prose-route + read-prose-route + detector-RED-before)** — parse spec đã-load (summary/description/responses.200.description trên 4 create + 3 read 200-oneOf path), bổ-trợ guard raw-text `26a`, chống prose mô tả `discriminator` NHƯ cơ-chế route hiện-hữu (cho phép NEGATED/`[SUPERSEDED]`). Decision-B intact, yaml VẪN 0 `discriminator:` key. Đây là phần MỞ RỘNG của prose-residue closure (Vòng 8→9), doc-only, KHÔNG đụng `.py`/yaml-schema. `test_mobile_oas` = **106 @landing Vòng 9** (103 @landing Vòng 8 + 3 TC 18f; count HIỆN HÀNH sau C-DoD-CFG Vòng 12 = **118** — xem §Baseline THẬT + dòng C-A1/GO-2).
>
> ✅ **F-C3 count-truth reconciliation + meta-guard — Vòng 11 (2026-06-11):** re-verify @source phát hiện `test_mobile_oas` THẬT = **107** (KHÔNG phải 106 như doc Vòng 9 ghi) — reconcile count-drift `106 → 107 @baseline Vòng 11`. Thêm **meta-guard** `TestMobileOasCountSelfVerify.test_mob_oas_NN_count_matches_ssot`: introspect mọi `TestCase` trong module (STDLIB `inspect`) → đếm method `test*` load-được → assert == `_EXPECTED_TEST_COUNT` (SSoT định-nghĩa MỘT LẦN @`test_mobile_oas.py` line ~92) ⇒ count-after-add **107 → 108 @F-C3 Vòng 11**. Count tự-xác-minh: drift sau-này (thêm/bớt TC quên cập const) = RED ngay. **RED-before/GREEN-after** đã chứng minh THẬT-bắt-drift: const lệch `999` → RED (`108 != 999`); const đúng `108` → GREEN. C-A1 literal count dùng `@source` (KHÔNG để 106 sai gây phantom-red). doc-only, `git diff --stat` api/services/yaml-schema = TRỐNG, KHÔNG reload gunicorn / migrate. **F-C3 round-1 (meta-guard test_mobile_oas) thêm CHỈ 1 test ⇒ +1 mỗi tổng:** guard-suite 6-module **191→192 OK** (baseline 107+49+13+11+5+6=191, KHÔNG phải 190), mobile/OAS total **200→201 OK** (+preflight 9; baseline 200, KHÔNG phải 199). ⚠️ ghi `190→192`/`199→201` trước = off-by-one BASELINE (reconciled). **F-C3 round-2 (cross-module SUM meta-guard):** class `TestMobileGuardSuiteCountParity` (4 TC) vào `test_mobile_docset` (5→9) introspect `def test` THẬT 6 module + assert sum==SSoT + transition-baseline self-consistency (bắt off-by-one `190 vs 191`) ⇒ guard-suite 6-module **192→196 OK** (oas 108 + generator 49 + serve 13 + signatures 11 + docset **9** + capability_map 6), mobile/OAS total **201→205 OK** (+preflight 9) — re-verify @source Vòng 11.

---

## 2. EPIC-B — Auth & Provisioning (OAuth2 + refresh)

> Mục tiêu: runbook + helper idempotent tạo OAuth Client; refresh-token flow; device-token doctype (nếu cần cho FCM). Tham chiếu task: `EPIC-B-auth-provisioning.md` (B1..Bn). DoD: `preflight.verify_oauth_client()` ready=True; authorize→token→refresh→revoke + PKCE chạy trên cloud.

| # | Tiêu chí nghiệm thu | Lệnh / cách kiểm (THẬT) | PASS khi | Owner | Tag | TT |
|---|---|---|---|---|---|---|
| B-A1 | Preflight verifier logic xanh (7 điều kiện B-1) + doc-value parity guard (F-B3) + report-shape doc↔code guard (F-B4) + blocker-VI remediation guard (F-B5) + count-self-verify meta-guard (F-B6) + stale-line-ref reconciliation guard (F-B7) | `bench --site miyano run-tests --module assetcore.tests.test_mobile_preflight` | `Ran 26 tests ... OK` (9 base + 4 doc-value-parity F-B3 `TC-MOB-PRE-10..13` + 4 report-shape F-B4 `TC-MOB-PRE-14..17` + 4 blocker-VI F-B5 `TC-MOB-PRE-18..21` + 1 count-self-verify F-B6 `TC-MOB-PRE-22` + 4 stale-line-ref F-B7 `TC-MOB-PRE-23a..d` = `_EXPECTED_PREFLIGHT_TEST_COUNT=26` SSoT đầu `test_mobile_preflight.py`; re-verify @source 2026-06-12 F-B7: `Ran 26 tests ... OK`) | BE/QA | [AUTO] | [x] |
| B-A2 | OAuth token contract trong spec (200/400 + refresh) | `bench --site miyano run-tests --module assetcore.tests.test_mobile_oas` — `TestMobileOAuthToken` (TC-MOB-OAUTH-TOKEN-01..05 = `test_mob_oauth_token_01..05`) | 5 test xanh; get_token 200 keys `{access_token,expires_in,token_type,scope?,refresh_token?}` passthrough OAuthlib; 400 OAuthError400 wire ĐÚNG 1 path; revoke 200 empty; authorize 302 (re-verify @source 2026-06-12 F-B: `test_mob_oauth_token_01..05` = `Ran 5 tests ... OK`, subset 133) | BA | [AUTO] | [x] |
| B-A3 | Preflight verifier idempotent (READ-ONLY chạy nhiều lần count bất biến — `[n/a-by-design]` no-helper-write) | `bench --site miyano run-tests --module assetcore.tests.test_mobile_preflight` — `TC-MOB-PRE-09` (read-only, no-write) | verifier KHÔNG ghi DB (read-only gate khách quan, idempotent-vì-không-ghi); chạy lại = count bất biến. **HELPER-WRITE BỎ:** DB-write OAuth Client = HARD-STOP USER (EPIC-B §3.3/B1) — KHÔNG còn yêu cầu provision-fn idempotent | BE | [AUTO] (n/a-by-design) | [x] |
| B-A4 | Refresh-token flow + token lifetime documented + ví dụ refresh-on-401 | **(F-B2) Nay MACHINE-CHECKED** bởi `TestMobileRefreshOn401DocGuard` (`TC-MOB-OAS-30a..d`, 4 TC, KHÔNG còn chỉ one-time grep) — raw-text scan `03-auth-oauth2.md` (§2.5 policy + §2.6 sequence 3-bước) + `04-api-contract.md` (§9d(n) sequence 3-bước) + cross-file `grant_type=refresh_token` parity; RED-before string-mutate (xoá block §9d HOẶC đổi `grant_type=refresh_token→authorization_code` → ĐỎ); STDLIB-only no-DB | có ví dụ: 401 → `getOAuthToken grant_type=refresh_token` → retry MỘT lần → fail re-auth + "KHÔNG vòng lặp refresh vô hạn"; `grant_type=refresh_token`/`refresh MỘT lần` ≥1 hit MỖI file (nay guard-enforced mỗi suite, không chỉ grep-once 2026-06-12) | BA | [AUTO] | [x] |
| B-A5 | device-token doctype **thiết kế** (nếu cần FCM) | đọc `06-push-fcm.md §2.3` + spec field-set `06 §2.1` ↔ `EPIC-D §5.1` ↔ roadmap `13 §6.3` | bearer-gated self-service, KHÔNG allow_guest; field-set khớp 06 | BA/BE | [AUTO] (thiết kế) | [x] |
| B-A6 (F-B4) | **REPORT-SHAPE doc↔code drift-guard** (B1/B4 acceptance #3 machine-check): 12 §1.2 (5 report-key của `verify_oauth_client()`) + 12 §1.3 (7 check-field-name = `client_count` ∪ 6 field `_evaluate_client`) mô tả ĐÚNG-VÀ-ĐỦ; expected-set DERIVE TỪ `preflight.py` runtime (KHÔNG hardcode) → preflight đổi field/key mà doc 12 không cập nhật → ĐỎ | `bench --site miyano run-tests --module assetcore.tests.test_mobile_preflight` — `TestMobilePreflightReportShapeDocGuard` (`TC-MOB-PRE-14..17`) | 4 TC xanh; 7 field-name `<field>` trong §1.3 + 5 report-key `<key>` trong §1.2 + blocker VI 'Chưa có OAuth Client' nguyên-văn + RED-before/GREEN-after (xoá field/key in-memory → RAISE); STDLIB-only no-DB-write (re-verify @source 2026-06-12) | BE/QA | [AUTO] | [x] |
| B-A7 (F-B7) | **STALE-LINE-REF reconciliation guard: §3.4 EPIC-B-auth ↔ source (analog F-C4 `test_mob_oas_29c`)** — vùng `[SUPERSEDED]` §3.4 `EPIC-B-auth-provisioning.md` (snapshot device-token 2026-06-11) từng ref source bằng số-dòng TUYỆT-ĐỐI `test_mobile_oas.py:222`/`:108-109`/`:114-115`/`:641` (chết do line-drift: `_STUB_PATHS=set()` nay @:225, names-frozen @:850) → reconciled sang dạng-SYMBOL; nội-dung `[SUPERSEDED]` GIỮ nguyên (audit-trail), chỉ đổi cách-tham-chiếu | `bench --site miyano run-tests --module assetcore.tests.test_mobile_preflight` — `TestMobilePreflightDocLineRefReconciled` (`TC-MOB-PRE-23a..d`) | 4 TC xanh; (a) §3.4 KHÔNG còn `test_mobile_oas.py:<digit>`; (b) §3.4 chứa nguyên-văn SYMBOL `_STUB_PATHS = set()` + `_DEVICE_TOKEN_FROZEN` + `test_mob_oas_06_device_token_names_frozen`; (c) RED-before inject `test_mobile_oas.py:999` → guard RAISE; (d) GREEN-after control region THẬT sạch. doc/test raw-text-only — `git diff --stat` api/services .py + yaml = TRỐNG. STDLIB-only (`Path.read_text`+`re`) no-DB. SSoT: `EPIC-B-auth-provisioning.md §3.4` | BE/QA | [AUTO] | [x] |
| B-U1 | `bench migrate` cloud → OAuth Client doctype + device-token live | USER: `bench --site <cloud> migrate` | migrate OK; doctype `OAuth Client`/device-token tồn tại trên cloud DB | USER | **[HARD-STOP USER]** | [ ] |
| B-U2 | Tạo OAuth Client THẬT (grant Authorization Code + scope `all openid` + redirect `assetcore://oauth/callback` + least-priv roles) | USER chạy provision helper trên cloud; rồi `verify_oauth_client()` | xem B-U3 | USER | **[HARD-STOP USER]** | [ ] |
| **B-DoD** (B-U3) | **`preflight.verify_oauth_client()` ready=True trên cloud** | `bench --site <cloud> execute assetcore.api.mobile.preflight.verify_oauth_client` | output `"ready": True`; 0 blocker (7 check pass — preflight.py:213) | USER/QA | **[HARD-STOP USER]** | [ ] |
| **B-DoD** (B-U4) | **authorize→token→refresh→revoke + PKCE chạy THẬT trên cloud** | curl/Postman: `/api/method/frappe.integrations.oauth2.authorize` (PKCE challenge) → `/.../get_token` → refresh → revoke | đủ 4 bước trả token hợp lệ; PKCE verifier bắt buộc; refresh trả access mới | USER/QA | **[HARD-STOP USER]** | [ ] |

**Grounded:** `verify_oauth_client()` (`api/mobile/preflight.py:147`) — admin-only, KHÔNG raise/KHÔNG leak traceback (preflight.py:23), trả `ready = all(c["pass"] for c in checks)` (preflight.py:213). 7 điều kiện B-1 = `client_count` + 6 cấp-record (`_evaluate_client`, preflight.py:25). **Phụ thuộc:** B-U1 cần USER `bench migrate` (HARD-STOP — xem Blocker #1). Cross-ref: EPIC-G G-U2 (site_config OAuth Client).

---

## 3. EPIC-D — Push FCM

> Mục tiêu: endpoint đăng ký FCM token/device; wire `notifications.py` (6-event) → kênh push; FCM creds site_config (user). Tham chiếu task: `EPIC-D-push-fcm.md` (D1..Dn). DoD: báo hỏng → KTV được giao nhận push; test.

| # | Tiêu chí nghiệm thu | Lệnh / cách kiểm (THẬT) | PASS khi | Owner | Tag | TT |
|---|---|---|---|---|---|---|
| D-A0 | **DocType `AC Mobile Device Token` (7 field, autoname=hash, fcm_token UNIQUE, track_changes=1)** — D1 schema | `bench --site miyano run-tests --module assetcore.tests.test_mobile_device_token` (schema-test); DB-test = RED-pending `bench migrate` | 3 artifact tồn tại (`doctype/ac_mobile_device_token/` + `services/mobile_device_token.py` + `tests/test_mobile_device_token.py`) + schema-test xanh (7 field đúng type, autoname=hash, fcm_token unique) | BE/QA | [AUTO impl]+[HARD-STOP USER migrate] | **[x]** schema-DONE (Vòng 20 — `run-tests test_mobile_device_token` **62 OK/10 skip** @source; DB-UNIQUE = RED-pending `bench migrate` [HARD-STOP USER]) |
| D-A1 | Endpoint registerDeviceToken/unregisterDeviceToken giữ tên (frozen) | `bench --site miyano run-tests --module assetcore.tests.test_mobile_oas` — `test_mob_oas_06_device_token_names_frozen` | test xanh (2 tên đông cứng) | BA | [AUTO] | **[x]** (Vòng 17 — 131 OK) |
| D-A1b | **device-token TYPED (D4 §8.9) — gỡ 2 STUB cuối, Decision-B closed-schema route-by-VALUE** | `TestMobileDeviceTokenTyped` (`test_mob_oas_22a..i`, 9 TC) + `test_mob_oas_07`/`23b` (`_STUB_PATHS=∅`) | requestBody `DeviceTokenRequest` (`fcm_token`reqd · `platform` enum `[android,ios]` · `device_label?` · `app_version?`) oneOf json+form; 200 = oneOf `[<Created>, Error]` closed-schema (register data=`name` STRING `mobile_device_token.py:153/166`; unregister data=null `:172`); **0 `user` field (anti-spoof §6.2)**; 0 discriminator; 0 dangling $ref; `responses/Stub` → forward-reserve | BA | [AUTO] | **[x]** (Vòng 17) |
| D-A2 | device-token endpoint bearer-gated (KHÔNG allow_guest) + 401/403 | `test_mob_oas_12` (device-token ∈ 12 path declare 401==403) + `test_mob_oas_22g/22h` (403=Forbidden single-shape dispatcher-403) | device-token trong cả `_PATHS_REQUIRE_401` và `_403` (symmetry 12==12); status-set `[200,401,403]` | BA/BE | [AUTO] | **[x]** (Vòng 17 — spec-part; BE handler `@whitelist(POST)` no-allow_guest = IMPL-part PENDING) |
| D-A3 | Wire `notifications.py` (6-event) → kênh push — side-effect THẬT | `run-tests test_mobile_device_token` — `TestMobilePushDispatch` (D6, `_dispatch` mock-FCM) + `TestMobileDeviceTokenDoDE3HookChain` (D7, ENTRY-POINT THẬT `notify_incident_created`); + regression `test_notification_framework`/`test_notifications`/`test_imm12_notify` | test xanh; side-effect THẬT (`send_fcm_message` call_args token KTV-A được giao, KTV-B 0 push); event=`incident_created`+deeplink (§5.4/§5.5); anti-spam 2× KHÔNG nhân đôi/dispatch (LL-TEST-18); fail-safe FCM-raise KHÔNG vỡ kênh 1/2 | BE/QA | [AUTO] (logic + test) | **[x]** (Vòng 20 — `grep fcm\|push notifications.py`=18, `rate_limit device_token.py`=7; **62 OK/10 skip** @source + 3 regression 19/61/12 OK; D6 kênh #3 + D7 hook-chain side-effect THẬT) |
| D-A4 | Audit trail NĐ98 cho đăng ký/thu hồi device-token | `run-tests test_mobile_device_token` — `test_d2_07_register_calls_audit` + `test_d2_07_unregister_calls_audit_when_record_exists` (mock `svc00.log_audit_event`, assert called + actor=session.user) | mỗi mutate sinh audit record (CLAUDE.md §5 mọi nghiệp vụ có record) | BE/QA | [AUTO] | **[x]** (Vòng 20 — `test_d2_07_*` audit-call xanh @source; register/unregister gọi `log_audit_event`) |
| D-U1 | FCM credentials nạp vào `site_config` cloud | USER set `fcm_*` keys trong `site_config.json` cloud | key tồn tại; service đọc được (KHÔNG hardcode) | USER | **[HARD-STOP USER]** | [ ] |
| **D-DoD** (D-U2) | **báo hỏng → KTV được giao NHẬN push THẬT trên thiết bị** | USER: device đăng ký token → tạo Incident gán KTV → quan sát push tới app | push notification hiện trên thiết bị KTV; payload đúng (asset + incident) | USER/QA | **[HARD-STOP USER]** | [ ] |

**Phụ thuộc:** D cần **B** (auth để device đăng ký token) + **FCM creds** (D-U1, USER). Cross-ref: B-A5 (device-token doctype), G (cloud reachable). **Anti-false-green (BẮT BUỘC):** D-A3 PHẢI assert side-effect THẬT (push-dispatch/Notification Log), KHÔNG chỉ "hàm return không lỗi" — pass-ngay-lần-đầu = nghi false-green (LL-TEST-21 / Phần 1 §Event-driven rule #4).

> **GATE D — re-verify @source 2026-06-12 (Vòng 20 — EPIC-D AUTO ĐÓNG):** TẤT CẢ task AUTO của EPIC-D **DONE @source**. D-A0 schema-DONE `[x]` · D-A1/A1b/A2 `[x]` · **D-A3 `[x]`** (Vòng 20 closure: `grep -ciE 'fcm\|push' services/notifications.py`=**18** kênh #3 ĐÃ chèn · `grep -ciE 'rate_limit' api/mobile/v1/device_token.py`=**7** · `TestMobilePushDispatch` (D6) + `TestMobileDeviceTokenDoDE3HookChain` (D7 entry-point THẬT `notify_incident_created`) side-effect THẬT chống false-green LL-TEST-21; **62 OK/10 skip** @source + 3 regression `test_notification_framework`/`test_notifications`/`test_imm12_notify` = 19/61/12 OK) · **D-A4 `[x]`** (`test_d2_07_*` audit register/unregister gọi `log_audit_event`) · **D7 RBAC self-scope** `permissions.py:268/285`+`hooks.py:395/404` ĐÃ exercise (GAP Vòng 20 đã đóng: `TestMobileDeviceTokenSelfScope` 20 TC IDOR-negative + 3-vai read-all, trước đó 0 TestCase). **DB-test (TestMobileDeviceTokenDB/TestMobilePushDispatchDB test_d6_06/TestMobileDeviceTokenSelfScopeDB TC-D7-09 IDOR get_list THẬT) = RED-pending-migrate (SKIP sạch).** **D-U1 (FCM creds) + D-U2 (push thiết bị) + `bench migrate` + reload gunicorn = `[ ]` [HARD-STOP USER]** — orchestrator+user, BA KHÔNG tự đóng. RBAC self-scope + push CHƯA live HTTP tới khi USER `bench migrate` + reload gunicorn (--preload, LL-DEPLOY-01/04).

---

## 4. EPIC-G — Go-live & Hardening

> Mục tiêu: deploy code lên cloud (commit+migrate); site_config host_name/HTTPS/nginx; tắt allow_error_traceback prod; rate-limit headers. Tham chiếu task: `EPIC-G-golive-hardening.md` (G1..Gn). DoD: HTTPS reachable ngoài; security gate.

| # | Tiêu chí nghiệm thu | Lệnh / cách kiểm (THẬT) | PASS khi | Owner | Tag | TT |
|---|---|---|---|---|---|---|
| G-A1 | servers placeholder KHÔNG lọt prod build (CI-guard) | `bench --site miyano run-tests --module assetcore.tests.test_oas_d13_servers` (+ G4 `test_mobile_security_gate::TestSecGateCiPlaceholderGuard` GUARD-2) | test xanh; guard chặn `REPLACE-WITH-PUBLIC-HOST` (yaml:108) + version `*-skeleton` (yaml:90) khi cờ prod-build ON (re-verify @source 2026-06-12: `Ran 15 tests ... OK`) | BA | [AUTO] | [x] |
| G-A2 | **Traceback-gate prose drift-guard (AUTO doc-part — machine-check invariant 3 doc)** | `bench --site miyano run-tests --module assetcore.tests.test_mobile_security_gate` — class `TestSecGateTracebackGateDoc` (GUARD-5, 5 TC: `test_sec_05_08_doc_present`/`_10_doc_present`/`_adr004_doc_present`/`_source_gate_grounded`/`_red_before_inject_developer_mode_is_red`) | `Ran 21 tests ... OK` @source 2026-06-12; invariant machine-checked raw-text (STDLIB, derive-from-source): (1) `08 §4(b)/§5` chứa `allow_error_traceback`+`System Setting`+phủ-định `KHÔNG phải developer_mode`/`site_config`+evidence `response.py:60`; (2) `10 §6.2(6)` chứa `allow_error_traceback`+`System Setting=0`+`is_traceback_allowed`+phủ-định; (3) `ADR-004 Consequences` cùng invariant (System Setting + KHÔNG developer_mode/site_config + `response.py:60-65`); (4) ground-truth @source `frappe/utils/response.py:60` `def is_traceback_allowed` + `:63` `get_system_settings('allow_error_traceback')` TỒN TẠI (anti stale-evidence); (5) RED-before/GREEN-after string-mutate bản-sao (`System Setting`→`developer_mode` / xoá phủ-định) → guard RAISE (THẬT bắt drift). `git diff --stat` api/services/*.py + yaml-schema = TRỐNG (doc/test introspection-only — owner constraint EPIC-G G4). Runtime knob (System Setting=0 + reload + curl live) = **[HARD-STOP USER]** G-U4/G-U6. | BA/QA | [AUTO] | [x] |
| G-A3 | OAS serve endpoint healthy (spec phục vụ được) | `bench --site miyano run-tests --module assetcore.tests.test_oas_serve` | `Ran 13 tests ... OK` (re-verify @source 2026-06-12) | QA | [AUTO] | [x] |
| G-A4 | **Security-gate guard (G4 — 9 invariant introspection-only)** | `bench --site miyano run-tests --module assetcore.tests.test_mobile_security_gate` | `Ran 52 tests ... OK` @source 2026-06-12 (16 G4-base + 5 G-A2 GUARD-5 + 7 G-A3 GUARD-6 + 8 G-A5 GUARD-7 + 8 G-A6 GUARD-8 + 8 G-A8 GUARD-9; SSoT `_EXPECTED_SECURITY_GATE_TEST_COUNT=52`, meta-guard `TestSecGateSelfCount` self-verify); GUARD-1 no-traceback-leak (re-use `verify_oauth_client()` no-raise + report KHÔNG marker 'Traceback...') · GUARD-2 CI placeholder/skeleton (prod-flag ON → RED, dev OFF → GREEN, RED-before/GREEN-after) · GUARD-3 CORS no-wildcard literal (docset prod KHÔNG `allow_cors='*'` ở dạng YAML config-form khuyến-nghị; NEGATED prose PHÉP) · GUARD-4 `_err.http_status` invariant §3.2 (cả nhánh int `:127` + chuỗi `:131`, source+behaviour) · GUARD-5 traceback-gate prose drift (G-A2: `08/10/ADR-004` invariant System Setting≠developer_mode + ground-truth `response.py:60`) · **GUARD-6 (G-A3) no-token-leak = G4(c) machine-checked**: source-grounded @source `api/imm00.py` `def _strip_qr_token(doc)` pop `qr_token` (derive-from-source `inspect.getsource`, anti stale-evidence) + `get_asset` `return _ok(_strip_qr_token(doc))`; doc-invariant raw-text `08 §5/T4` + `GO-7` prose phủ-định 'qr_token/token KHÔNG leak' trong MVP read; RED-before/GREEN-after (xoá strip call / xoá `pop('qr_token')` / flip prose phủ-định → guard RAISE). · **GUARD-7 (G-A5) rate-limit-header = G4(d) machine-checked**: source-grounded @source `frappe/rate_limiter.py` — `apply()` conf-gate (`frappe.conf.rate_limit` + gán `frappe.local.rate_limiter` dưới `if rate_limit:` :17-20) + `RateLimiter.headers()` emit 3 `X-RateLimit-*` LUÔN + `Retry-After` CHỈ dưới `if self.rejected` (:82-92); doc-invariant raw-text `08 §5 (d)` (`Retry-After` + `X-RateLimit-*` + KNOWN-clause 'CHỈ phát khi `conf.rate_limit`') + `10 §6.2 note 2` (body-only no-header khi conf vắng); RED-before/GREEN-after (xoá `frappe.conf.rate_limit` token / flip prose 'CHỈ phát khi'→'luôn phát' → guard RAISE), `inspect.getsource` derive-from-source anti stale-evidence. · **GUARD-8 (G-A6) audit-actor NĐ98 = §6 invariant (e) machine-checked** (Đóng LAST unguarded EPIC-G §6): source-grounded @source `utils/lifecycle.py` — `log_audit_event` thân chứa `actor = actor or frappe.session.user` (:44 → actor mặc-định = KTV thật) + `verify_audit_chain` integrity-compare `expected != ...hash_sha256` + `prev_hash` mismatch (:110-111 = hash-chain bất biến + liên-tục) + `frappe/auth.py:667` raw-text `set_user(get_value("OAuth Bearer Token", token, "user"))` (bearer→KTV thật); doc-invariant raw-text `08 §2.2` (`set_user(token.user)`+`log_audit_event`+`frappe.session.user`+phủ-định 'KHÔNG service-account'/'KHÔNG Administrator'+evidence `auth.py:667`/`lifecycle.py`) + `08 §5.1` gate `(e) audit-actor` + `10 §6.3 (verify-audit)` (`verify_audit_chain`, actor=user thật); RED-before/GREEN-after (xoá `actor = actor or frappe.session.user` / flip 'KHÔNG service-account'→'service-account' / xoá set_user-bearer token → guard RAISE), `inspect.getsource` derive-from-source anti stale-evidence. · **GUARD-9 (G-A8) host_name/issuer go-live = G4(f) machine-checked** (Đóng LAST unguarded KNOB-MATRIX invariant — knob #1 `host_name`): source-grounded @source `frappe/utils/data.py` — `def get_url` (:1599) + thân chứa `host_name = ...conf.host_name or ...conf.hostname` (:1605) + fallback `protocol + ...site` nội bộ (:1631); doc-invariant raw-text `08 §5.1(f)` + `10 §3`/§6.2(3c0)/§6.3 (verify-host) + `EPIC-G §3.3`/§8 R4 — mỗi nơi có `host_name` + `get_url()`/`openid_configuration issuer == public host` + phủ-định 'KHÔNG `http://miyano`' nội bộ; RED-before/GREEN-after (xoá `http://miyano` / flip phủ-định / xoá host_name gate-line @source-derive → guard RAISE), `inspect.getsource` derive-from-source anti stale-evidence. (a)/(b)/(c)/(d)/(e)/(f) giờ đủ GUARD-1/3/6/7/8/9 — EPIC-G G4+§6 + KNOB-MATRIX invariant ĐÓNG KÍN AUTO-part. Đặc tả: `08 §5.1` + `10 §3`/§6.2/§6.3. `git diff --stat` api/services/*.py + yaml-schema = TRỐNG (test+doc introspection-only — owner constraint EPIC-G). Curl public-host (a/b/c/d THẬT trên cloud, gồm G4(c) body KHÔNG `qr_token` + G4(d) 429 header `Retry-After`/`X-RateLimit-*` — cần USER set `conf.rate_limit`/nginx `limit_req` + reload, G-U5) + live `verify_audit_chain` với bearer KTV thật (e — G-U?, cần B-U1..U4 OAuth Client) + live `get_url()`/`openid_configuration issuer == public host` (f — G-U2/G-U6) = **[HARD-STOP USER]** sau G1–G3 (G-U2/G-U4/G-U5/G-U6) | BA/QA | [AUTO] | [x] |
| G-A8 | **host_name/issuer go-live drift-guard (G4(f) — KNOB-MATRIX invariant CUỐI)** | `bench --site miyano run-tests --module assetcore.tests.test_mobile_security_gate` — class `TestSecGateHostNameIssuerDoc` (GUARD-9, 8 TC) | `Ran 52 tests ... OK` @source 2026-06-12 (sec-gate 44→52, +8 G-A8); knob #1 `host_name` = LAST KNOB-MATRIX invariant 0-guard nay machine-checked (4/5 trước: CORS=GUARD-3, traceback=GUARD-5, rate-limit-header=GUARD-7, token-leak/audit=GUARD-6/8). Source-grounded @source `frappe/utils/data.py:1599 def get_url`+`:1605 host_name = ...conf.host_name or ...conf.hostname`+`:1631 fallback protocol+site` nội bộ (anti stale-evidence) + prose-invariant raw-text `08 §5.1(f)`/`10 §3`+§6.2(3c0)+§6.3+`EPIC-G §3.3`/§8 R4 (host_name + `get_url()`/issuer == public host + phủ-định 'KHÔNG `http://miyano`') + RED-before/GREEN-after (xoá `http://miyano`/flip phủ-định/xoá gate-line → RAISE). Cross-module count-parity: `test_mobile_docset` `_GUARD_SUITE_SUM` 273→281 + `_MOBILE_OAS_TOTAL` 299→307 + `_GUARD_SUITE_EXPECTED[sec-gate]` 44→52 (`TestMobileGuardSuiteCountParity` xanh). Live `get_url()`/issuer == public host = **[HARD-STOP USER]** G-U2/G-U6. `git diff --stat` api/services/*.py + yaml-schema = TRỐNG (doc/test introspection-only). | BA/QA | [AUTO] | [x] |
| G-U1 | Deploy code lên cloud (commit + migrate) | USER: `git commit`/`push` + `bench --site <cloud> migrate` | working tree `??`/`M` đã commit; migrate OK trên cloud | USER | **[HARD-STOP USER]** | [ ] |
| G-U2 | site_config: host_name/HTTPS/nginx | USER set `host_name` + cert HTTPS + nginx `limit_req` | site phục vụ HTTPS; host_name đúng | USER | **[HARD-STOP USER]** | [ ] |
| G-U3 | CORS: **CHỈ nếu UI web** — native APK KHÔNG cần CORS | USER: nếu cần web → `allow_cors` = list-origin (KHÔNG wildcard `*`+credentials) | nếu set: no-wildcard; nếu native-only: `allow_cors` off (None) hợp lệ | USER | **[HARD-STOP USER]** | [ ] |
| G-U4 | Tắt `allow_error_traceback` prod | USER: System Setting OFF; verify `is_traceback_allowed()` False | gọi endpoint lỗi → body KHÔNG chứa traceback | USER/QA | **[HARD-STOP USER]** | [ ] |
| G-U5 | rate-limit headers (Retry-After / X-RateLimit-*) | USER set `conf.rate_limit` HOẶC nginx `limit_req` inject Retry-After | 429 trả backoff header (hiện `site_config` thiếu `rate_limit` → rate_limiter never instantiates) | USER | **[HARD-STOP USER]** | [ ] |
| **G-DoD** (G-U6) | **HTTPS reachable từ ngoài + security gate** | từ máy ngoài: `curl -sI https://<host>/api/method/frappe.ping` + audit | 200 reachable; **no traceback leak · CORS no-wildcard · no token-leak** | USER/QA | **[HARD-STOP USER]** | [ ] |

**Grounded:** gate traceback ĐÚNG = System Setting `allow_error_traceback` (default ON, `response.py`) — KHÔNG `developer_mode`. Rate-limit: `site_config` không có key `rate_limit` → `frappe.local.rate_limiter` never instantiates (`rate_limiter.py:82-92`) → `@rate_limit` (`imm00.py:328/371`) emit ZERO backoff header tới khi USER set `conf.rate_limit` hoặc nginx. **Phụ thuộc:** G cần B (auth provisioned). Cross-ref: Blocker #1 (reload gunicorn `--preload` + migrate), #3 (Phase B provisioning site_config).

---

## 5. EPIC-V — Codegen Verify + Handoff

> Mục tiêu: gen client Dart/Kotlin THẬT smoke cloud; E2E runbook field-tech; gói handoff repo mobile (yaml+auth+base-url). Tham chiếu task: `EPIC-V-codegen-verification.md` (V1..V4). DoD: 1 client gen-ra gọi được cả 6 flow trên cloud; runbook validated.

| # | Tiêu chí nghiệm thu | Lệnh / cách kiểm (THẬT) | PASS khi | Owner | Tag | TT |
|---|---|---|---|---|---|---|
| V-A1 | E2E runbook field-tech (6 flow) viết đủ, KHÔNG placeholder | `grep` 6 operationId trục trong `14-e2e-field-tech-runbook.md` (6× OK) + `bench run-tests --module assetcore.tests.test_mobile_docset` (9 OK) + `…test_mobile_oas` (141 OK) | 6 flow có bước cụ thể + lệnh/endpoint thật; 0 TBD/TODO | BA | [AUTO] | [x] _(2026-06-12: chương `14-e2e-field-tech-runbook.md` — 6 flow tuần tự, 6 operationId trục grep PASS, 0 placeholder ngoài code-fence; docset 9 OK + oas 141 OK; index parity README/00-overview GREEN; V3-BA chốt GIỮ Dart/Kotlin/TS)_ |
| V-A2 | Gói handoff repo mobile (yaml + auth + base-url + ví dụ) liệt kê đủ | grep 4-key trên `completion/V4-handoff-manifest.md` (`assetcore-mobile.openapi.yaml` · `BASE_URL` · `03-auth-oauth2.md` · `body.success`) = 4× OK + `bench run-tests --module assetcore.tests.test_mobile_docset` (9 OK) | manifest đủ 4 artifact (yaml-pin · base-url ENV · auth-link · envelope-read example) + checklist [PM]; trỏ `09-native-repo-guide.md` | BA | [AUTO] | [x] _(2026-06-12: `completion/V4-handoff-manifest.md` — 4-key grep 4× OK; 16 operationId pinned `info.version=0.1.0-skeleton`; BASE_URL dev `localhost:8000`/prod `REPLACE-WITH-PUBLIC-HOST`; ví dụ 1-call `getAsset` route-by-VALUE `body.success`/`code`/`http_status` Decision-B KHÔNG discriminator; checklist [PM] §5 mirror `09 §6.2`; docset 9 OK — 12 link `./`/`../` resolve, total ≥ baseline 400; doc-only `git diff` api/services/yaml TRỐNG)_ <br/>**↳ F-V1 (Vòng 48, đóng MEDIUM doc-accuracy @QA-Vòng-47):** §4.1 curl example-path reconciled về yaml SSoT — `assetcore.api.imm00.get_asset` (byte-khớp operationId `getAsset` @`assetcore-mobile.openapi.yaml`), gỡ segment `mobile.` BỊA chèn giữa `api.` và `imm00.` (module IMM-00 mobile-namespace KHÔNG tồn tại → dev native copy verbatim sẽ 404 GIẢ; `mobile.*` hợp lệ DUY NHẤT = `mobile.v1.device_token`, EPIC-D). **Machine-checked guard** (anti-re-drift, generalize TOÀN `completion/*.md` @F-V1-residue): `test_mobile_docset` TC-MOB-DOC-03 hai sub-assertion — `_assert_handoff_curl_path_matches_yaml_ssot` (manifest-scoped: path-đúng ∈ ≥1 ∧ bogus-namespace ∉ ==0) + `_assert_completion_curl_paths_resolve_yaml_ssot` (mọi curl `/api/method/...` trong completion ∈ allow-set DERIVE từ yaml SSoT ∪ bench-execute preflight; 0 token `mobile.imm<NN>` bịa DÙ trong prose). **RED-before/GREEN-after** (LL-TEST-26): inject curl-path bịa vào bản completion → guard RAISE; control THẬT → 9 OK (KHÔNG tautology). Cả hai = sub-assertion (KHÔNG `def test` riêng — precedent `_assert_secgate_in_net_2ssot_parity`) ⇒ docset GIỮ **9 `def test`**, guard-suite-sum 281 / mobile-OAS 307 / GO-2-cite KHÔNG đổi. doc+test-only — `git diff --stat` api/services .py + yaml-schema = TRỐNG (F-V1 chỉ đụng manifest + test_mobile_docset.py)._ |
| V-U1 | Gen client Dart THẬT từ spec | USER (sau cấp `java`/`npx`): `openapi-generator generate -g dart -i <yaml> -o <out>` | exit 0; client compile được | USER/QA | **[HARD-STOP USER]** (toolchain) | [ ] |
| V-U2 | Gen client Kotlin THẬT từ spec | `openapi-generator generate -g kotlin -i <yaml> -o <out>` | exit 0; client compile được | USER/QA | **[HARD-STOP USER]** (toolchain) | [ ] |
| V-U3 | Deser route-by-discriminator đúng (3 create path) | client gen → assert 200-oneOf route Created vs Error theo body discriminator STRING (KHÔNG status-line) | deser chọn đúng nhánh; KHÔNG anyMatch ambiguity | QA | **[HARD-STOP USER]** | [ ] |
| **V-DoD** (V-U4) | **1 client gen-ra gọi được cả 6 flow trên cloud** | USER: client → login OAuth → scan QR → report incident → create WO → "phiếu của tôi" → nhận push | đủ 6 flow trả 2xx/envelope success=true (hoặc Error đúng nhánh); push tới | USER/QA | **[HARD-STOP USER]** | [ ] |
| **V-DoD** (V-U5) | **Runbook validated (chạy theo runbook ra kết quả)** | USER chạy runbook V-A1 từ đầu trên cloud | mỗi bước reproduce được; 0 bước thiếu | USER/QA | **[HARD-STOP USER]** | [ ] |

**Phụ thuộc:** V = chốt cuối — cần **C+B+D+G** đã PASS. Cross-ref: C-DoD (codegen sạch là tiền đề), G-U6 (cloud reachable), D-U2 (push). Proxy hiện tại: STDLIB PyYAML introspection (`test_oas_*`) PASS thay codegen THẬT tới khi có toolchain.

---

## 6. 🚦 GO-LIVE GATE TỔNG (chốt MVP field-tech production-ready)

> **Quy tắc:** GO-LIVE = PASS chỉ khi **TẤT CẢ** dòng dưới = `[x]`. Một dòng `[ ]`/`[~]` → **BLOCK go-live**. QA KHÔNG tuyên bố pass khi chưa thấy output xanh THẬT.

| # | Gate | Lệnh / cách kiểm (THẬT) | PASS khi | Tag | TT |
|---|---|---|---|---|---|
| GO-1 | **Codegen sạch** (Dart+Kotlin, 0 dangling `$ref`) | `openapi-generator generate -g dart` + `-g kotlin` (V-U1/U2) | exit 0 cả 2; 0 ERROR | **[HARD-STOP USER]** | [ ] |
| GO-2 | **Test suite mobile/OAS xanh THẬT** | `bench --site miyano run-tests --module assetcore.tests.test_mobile_oas` (+`test_mobile_preflight`/`test_mobile_docset`/`test_mobile_capability_map`/`test_oas_generator`/`test_oas_serve`/`test_oas_signatures`/`test_mobile_security_gate`) | tổng OK, 0 failure/error (re-run @source G-A8 2026-06-12: guard-suite 7-module = `test_mobile_oas` **141** + `test_oas_generator` 49 + `test_oas_serve` 13 + `test_oas_signatures` 11 + `test_mobile_docset` 9 + `test_mobile_capability_map` 6 + `test_mobile_security_gate` **52** = **281 OK guard-suite** + `test_mobile_preflight` 26 = **307 mobile/OAS**). _Reconcile count-drift @source G-A8: sec-gate **44→52** (+8 G-A8 GUARD-9 `TestSecGateHostNameIssuerDoc` host_name/issuer go-live G4(f) — KNOB-MATRIX invariant CUỐI); guard-suite 273→**281**; tổng mobile/OAS 299→**307**. (G-A6 trước: sec-gate 36→44 +8 GUARD-8 audit-actor NĐ98 §6(e); G-A5: sec-gate 28→36 +8 GUARD-7 rate-limit-header G4(d); G-A3: sec-gate 21→28 +7 GUARD-6 no-token-leak G4(c); G-A2: `test_mobile_oas` 137→141 + sec-gate 16→21 +5 GUARD-5.)_ | [AUTO] | [ ] |
| GO-3 | **preflight ready=True trên cloud** | `bench --site <cloud> execute assetcore.api.mobile.preflight.verify_oauth_client` (B-U3) | `"ready": True`, 0 blocker | **[HARD-STOP USER]** | [ ] |
| GO-4 | **6-flow smoke THẬT trên cloud** (1 client gen-ra) | V-U4 runbook | đủ 6 flow (login/scan/report/WO/phiếu/push) chạy được | **[HARD-STOP USER]** | [ ] |
| GO-5 | **Security: no traceback leak** | G-U4: endpoint lỗi → body | KHÔNG có traceback trong 401/403/429 (allow_error_traceback OFF) | **[HARD-STOP USER]** | [ ] |
| GO-6 | **Security: CORS no-wildcard** | G-U3: đọc `allow_cors` | không `*`+credentials (native APK: off hợp lệ) | **[HARD-STOP USER]** | [ ] |
| GO-7 | **Security: no token-leak** | audit response bodies + log: QR payload/token KHÔNG leak (vendor-IDOR 403, `_strip_qr_token`) | 0 token/qr_token trong response cho user ngoài scope | **[HARD-STOP USER]** | [ ] |
| GO-8 | **Audit trail NĐ98** (actor = KTV thật) | **[AUTO] ✅** GUARD-8 (`TestSecGateAuditActorNd98Doc`, G-A6) machine-check cơ-chế @source `bearer→set_user→log_audit_event(actor=session.user)→verify_audit_chain` (`lifecycle.py:44/110-111` + `auth.py:667`) + doc-invariant `08 §2.2/§5.1(e)`/`10 §6.3` — chuỗi đảm bảo actor đúng KÍN @source · **[USER]** live: mỗi nghiệp vụ mobile (report/WO/cal/device-token) sinh IMM Audit Trail row + `verify_audit_chain(asset)` THẬT trên cloud sau action-từ-mobile bằng bearer KTV (G-U?) | AUTO-part ✅ (cơ-chế @source machine-checked); live: 0 mutate thiếu audit record + `valid=True`, actor == KTV thật (KHÔNG service-account/Administrator) | [AUTO]✅+[USER] | [ ] |
| GO-9 | **HTTPS reachable ngoài + host_name/issuer** | **[AUTO] ✅** GUARD-9 (`TestSecGateHostNameIssuerDoc`, G-A8) machine-check cơ-chế @source `frappe/utils/data.py:1605 get_url host_name` + prose-invariant `08 §5.1(f)`/`10 §3`+§6.2+§6.3/`EPIC-G §8 R4` (KHÔNG `http://miyano`) · **[USER]** live: `curl -sI https://<host>/api/method/frappe.ping` từ máy ngoài + `get_url()`/`openid_configuration issuer == public host` (G-U2/G-U6) | AUTO-part ✅ (cơ-chế @source machine-checked: `host_name` set ⇒ `get_url()`/OIDC issuer == public host, KHÔNG `http://miyano`); live: 200 OK qua HTTPS + issuer == public host | [AUTO]✅+[USER] | [ ] |

**VERDICT GO-LIVE:** `[ ] GO` / `[ ] BLOCK` — chỉ tick GO khi GO-1..GO-9 đều `[x]`.

---

## 7. Tham chiếu

- **Chương docset BE-completion:** `00-overview.md` · `01-architecture.md` · `02-deploy-feasibility.md` · `03-auth-oauth2.md` · `04-api-contract.md` · `05-personas-mvp.md` · `06-push-fcm.md` · `07-offline-sync.md` (post-MVP) · `08-security-compliance.md` · `09-native-repo-guide.md` · `10-deploy-ops.md` · `11-phase-a-exit.md` · `12-phase-b-preflight.md`.
- **ADR:** `ADR-MOBILE-001.md` (OpenAPI contract, §f Self-Correction discriminator/form_dict) · `ADR-MOBILE-002/003/004.md`.
- **EPIC chi tiết task:** `EPIC-C-api-contract.md` · `EPIC-B-auth-provisioning.md` · `EPIC-D-push-fcm.md` · `EPIC-G-golive-hardening.md` · `EPIC-V-codegen-verification.md` (cùng thư mục `completion/`).
- **Spec + guard:** `docs/mobile/openapi/assetcore-mobile.openapi.yaml` (16 path, openapi 3.0.3, **0 STUB sau EPIC-D D4** — device-token typed) · `assetcore/tests/test_mobile_oas.py` (`_STUB_PATHS = set()` (∅ sau D4), `_DEVICE_TOKEN_FROZEN`, `_MVP_BUSINESS_PATHS`, `_PATHS_REQUIRE_401/403`).
- **LL skill (assetcore-test):** LL-TEST-18 (hook-chain side-effect), LL-TEST-19 (permission gate mọi mutating endpoint), LL-TEST-21 (chống false-green — đọc output THẬT), LL-TEST-25 (reload gunicorn `--preload` trước Playwright/HTTP live), Phần 1 §Event-driven (assert side-effect THẬT).

---

## 8. Rủi ro

| Rủi ro | Tác động | Giảm thiểu |
|---|---|---|
| ~~discriminator boolean lọt~~ → **CLOSED (Decision-B)** | client deser sai nhánh Created/Error | ✅ Decision-B: BỎ discriminator + closed-schema (`additionalProperties:false`) + disjoint required-set; guard `test_mob_oas_18b` (KHÔNG `discriminator` key) XANH. C-A4 = `[x]` |
| Stale gunicorn `--preload` (Blocker #1) | sửa `api/*.py` không live HTTP → false-fail Playwright | USER reload TRƯỚC khi soi `api/*.py` mới (LL-TEST-25); reload KHÔNG bắt buộc cho round doc/yaml/test |
| Toolchain thiếu (`java`/`npx`) | C-DoD/V-U1..U5 không tự đóng được | STDLIB PyYAML introspection làm proxy; gate THẬT defer sang EPIC-V khi USER cấp |
| `bench migrate` cloud (B-U1) chưa chạy | OAuth Client/device-token doctype không tồn tại → preflight fail | HARD-STOP USER; B-U1 là tiền đề B-U3/B-DoD |
| FCM creds chưa nạp (D-U1) | push không gửi được → flow 6 fail | HARD-STOP USER site_config; D-A3 assert side-effect logic trước, D-DoD verify thiết bị sau |
| rate-limit headers thiếu (G-U5) | client không backoff đúng khi 429 | USER set `conf.rate_limit`/nginx `limit_req`; Phase-B-conditional |
| Test false-green side-effect (D-A3) | feature push "chết" nhưng test xanh | assert side-effect THẬT (Notification Log/push-dispatch), anti-spam 2-lần (Phần 1 §Event-driven) |
| CORS wildcard nếu thêm web sau | leo quyền cross-origin | G-U3 no-wildcard; native APK KHÔNG cần CORS (ADR-MOBILE-004) |

---

*Checklist này là CỔNG nghiệm thu — KHÔNG tuyên bố EPIC/GO-LIVE pass khi chưa thấy output xanh THẬT. Mọi dòng `[HARD-STOP USER]` thuộc orchestrator + user, KHÔNG tự đóng. Cập nhật cột `TT` mỗi khi factory/user đóng 1 dòng.*
