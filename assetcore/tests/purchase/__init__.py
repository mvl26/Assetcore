"""Test của module ``purchase`` (SPEC BE §5.1 nhà #2).

Mỗi file ở đây kiểm **một** module logic — ``services/purchase.py`` và/hoặc
``api/purchase.py``. Test cắt ngang ≥2 module thuộc ``tests/integration/``;
guard đọc đĩa/parity thuộc ``tests/guards/``.
"""
